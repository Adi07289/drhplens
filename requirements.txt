# DRHPLens — runtime dependencies for HF Spaces deployment
# Keep in sync with pyproject.toml [project.dependencies]
# HF Spaces reads this file directly; do not add dev-only packages here.

# Agent orchestration
langgraph>=1.2,<2

# RAG ingestion + query engines
llama-index>=0.14,<0.15
llama-index-vector-stores-qdrant
llama-index-embeddings-huggingface
llama-index-llms-google-genai
llama-index-llms-groq

# PDF parsing
docling>=2.95,<3
pdfplumber>=0.11
pymupdf>=1.27

# Vector store
qdrant-client>=1.18,<2

# Embeddings + reranker
sentence-transformers>=3
FlagEmbedding

# Structured LLM outputs
instructor>=1.15,<2

# Schemas + validation
pydantic>=2.7,<3

# UI
streamlit>=1.36

# Observability
langfuse<3

# Evals — RAG metric frameworks (Phase 6.1). deepeval = faithfulness LLM-judge
# (native GeminiModel judge, no OpenAI dep). langfuse is pinned <3 above because v4
# is a full OTEL rewrite that still needs the `langchain` package (deliberately NOT
# added). ragas is an OPTIONAL offline cross-check only; NEVER imported in CI or the
# release gate (AI-SPEC Pitfall 5).
deepeval>=4.1,<5
ragas>=0.4,<0.5

# Utilities
tenacity
python-dotenv
httpx>=0.27
typer
rich
tiktoken

# Cite-check fuzzy matching (rapidfuzz — Pattern 3 per 01-RESEARCH.md)
rapidfuzz
