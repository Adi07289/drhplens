---
quick_id: "260727-close-04-07-median-sanity"
status: in-progress
created: 2026-07-27
---

# Quick Task: close 04-07 — surface the ~7% median MAAR sanity-check on /methodology

## Finding (corrects the prompt's premise)
`validate.sanity_check_median` uses a sane band `[-5%, +20%]`; it flags only a median
ABOVE 20% (survivor inflation) or BELOW -5%. The committed panel median is **10.19%**
over 1,245 scored IPOs — above the narrow 7.19% Shah & Mehta point estimate but WITHIN
the band → `flag is None`. So there is NO material divergence to warn about. The honest
surfacing is the sanity-check RESULT: median 10.2%, within band, NOT survivor-inflated
(evidence the withdrawn/NaN retention worked, P3). Fabricating a divergence alarm would
be dishonest.

## Constraint
`pages/01_methodology.py` is render-only (T-05-10-ISO) and `test_methodology_render.py`
FORBIDS importing `pipelines.historical`. So the page must read a committed JSON — same
pattern as `model_card/card_data.json`.

## Tasks
1. **`pipelines/historical/validate.py::panel_sanity_summary(df) -> dict`** (TDD) —
   wraps `sanity_check_median` + `band_text` into a render-ready dict
   (median_pct, n_scored, baseline_pct, band, within_band, flag|None, methodology).
2. **`scripts/write_panel_sanity.py`** — reads the committed panel, writes
   `data/historical/panel_sanity.json` (no network; deterministic).
3. **`pages/01_methodology.py`** — read `data/historical/panel_sanity.json` (json only,
   NO pipelines import) and render a "Historical dataset — survivorship sanity" note:
   median + verdict (within-band confirmation, or the divergence flag text if it ever
   fires). Pin with a `test_methodology_render.py` addition.
4. Mark 04-07 `[x]` in ROADMAP. Verify full suite + isolation green; verify /methodology
   in the running app.

## Out of scope
- STATE.md progress / phase-close bookkeeping → prompt #6.
