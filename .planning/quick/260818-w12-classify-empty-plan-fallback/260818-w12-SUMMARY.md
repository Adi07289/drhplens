---
quick_id: 260818-w12
slug: classify-empty-plan-fallback
date: 2026-08-18
status: complete
---

# Summary — 260818-w12 · Fix live classifier returning empty plans

**Outcome:** valid questions are no longer silently dropped when the classify hop fails
on the deploy. The failure path (and any in-scope-but-untooled decision) now degrades to
a `drhp_rag` document-Q&A attempt instead of an empty plan → `_educational_refusal()`,
and the failure is logged for deploy diagnosis.

## What changed

- `agent/nodes/classify.py`
  - `+ import logging`, module `logger`.
  - `+ DEFAULT_TOOL = "drhp_rag"`.
  - `run()`: `except Exception` now `logger.warning(..., exc_info=True)` + `plan = [DEFAULT_TOOL]`;
    in-scope clamp-to-empty now `plan = [DEFAULT_TOOL]`; deliberate advice/OOS path unchanged (`[]`).
  - `run()` docstring point 3–4 updated.
- `tests/unit/test_classify.py`
  - Renamed failure test → `test_run_all_models_fail_falls_back_to_drhp_rag` (asserts `["drhp_rag"]`).
  - `+ test_run_in_scope_but_no_tools_falls_back_to_drhp_rag`.

## Verification evidence

- Unit: **36 passed** (`test_classify` + `test_supervisor_bounds` + `test_supervisor_contracts`).
- Root cause reproduced: local classify empty-plan rate **0/30** on valid questions ⇒ deploy
  empty-plans come only through the swallowed `except` branch.
- E2E (real Groq + Qdrant): forced classify failure now → `tool_calls=1`, 50 chunks retrieved,
  grounded answer (was `tool_calls=0` + instant refusal).

## Follow-ups (not blockers)

- **Deploy diagnosis:** the new `logger.warning` will now surface WHY classify fails on
  Streamlit Cloud (Groq timeout / rate-limit / missing key). Confirm in deploy logs after push.
- Not pushed/deployed — left for the user to ship and re-test the exact live question.
