---
quick_id: "260727-reconcile-forecast-metrics"
status: in-progress
created: 2026-07-27
---

# Quick Task: reconcile forecast records' global metrics to the live backtest

## Problem
`/snapshot`'s forecast block renders SEED global metrics (coverage 0.783, n=247,
window 2016-2025) from `data/forecasts/{swiggy,hyundai}.json`, while `/methodology`
shows the LIVE backtest (0.800, n=1132) and the block now LEADS with the live
P9-fail banner. The "How this was tested" strip therefore contradicts the model
card. The GLOBAL metrics block is D5-12 "identical across every page" and describes
the MODEL's real tested behavior, so it must be the live numbers.

## Scope (explicit)
Regenerate ONLY the 5 global-metrics fields — `coverage_empirical`, `mae_pts`,
`per_year_rmse`, `n`, `backtest_window` — from the committed live OOS frame
(`data/forecasts/_gate/oos_real.parquet` via `diagnostics.global_metrics`). Keep the
honest per-IPO band + abstain posture verbatim (the forecaster FAILED its P9 gate,
so no validated per-IPO band was regenerated — that stays illustrative seed, D5-01).

## Tasks
1. **`pipelines/forecast/reconcile.py`** (TDD): `live_metrics(oos_df) -> ForecastMetrics`
   (global_metrics + year-span `backtest_window`) and `reconcile_record_metrics(record,
   metrics) -> ForecastRecord` (replaces ONLY the metrics block; band/abstain/version
   preserved). Model-side; render isolation unaffected.
2. **`scripts/reconcile_forecast_metrics.py`**: apply live_metrics to the two committed
   records with a minimal (metrics-block-only) diff.
3. Regenerate the two records; verify full unit suite green; verify `/snapshot` shows
   the live strip (0.800 / n=1132) in the running app.

## Out of scope / notes
- `model_version` stays `cqr-xgb-seed-2026.07` (band is seed) — the visible strip does
  not show it; flagged for the user.
- STATE.md quick-tasks row deferred to prompt #6 (STATE.md is mid-reconciliation).
