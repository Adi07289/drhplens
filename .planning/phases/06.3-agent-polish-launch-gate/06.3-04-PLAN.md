---
phase: 06.3-agent-polish-launch-gate
plan: 04
type: execute
wave: 2
depends_on: ["06.3-01"]
files_modified:
  - eval/gold/stress/compliance_bait.jsonl
  - eval/gold/stress/jailbreak.jsonl
  - eval/gold/stress/offtopic.jsonl
  - eval/gold/stress/cross_ipo.jsonl
  - eval/gold/stress/partial_and_provenance.jsonl
  - tests/eval/conftest.py
  - tests/eval/test_stress_suite.py
autonomous: true
requirements: [D-07, D-09]
user_setup: []

must_haves:
  truths:
    - "A committed adversarial-query corpus covers all four D-07 weird-query categories, >=5 cases each, >=20 total (D-07/D-09)"
    - "The stress-suite scaffold collects cleanly before the supervisor lands (importorskip) and auto-activates when it does"
    - "Shared deterministic fakes let the gated assertions run OFFLINE with no live LLM (D-09)"
  artifacts:
    - path: "eval/gold/stress/compliance_bait.jsonl"
      provides: "advice-bait + advice-by-implication cases"
      contains: "compliance_bait"
    - path: "tests/eval/test_stress_suite.py"
      provides: "deterministic 4-category stress harness scaffold"
      contains: "importorskip"
    - path: "tests/eval/conftest.py"
      provides: "shared routing/fusion/budget/abstain fakes"
      contains: "fake_routing"
  key_links:
    - from: "tests/eval/test_stress_suite.py"
      to: "eval/gold/stress/*.jsonl"
      via: "parametrized over committed fixtures"
      pattern: "eval/gold/stress"
    - from: "tests/eval/test_stress_suite.py"
      to: "agent.supervisor"
      via: "importorskip until supervisor lands"
      pattern: "importorskip"
---

## Phase Goal

**As an** Indian retail investor, **I want to** ask one plain-English question and get a single honestly-cited answer across the DRHP, peers, forecast, and GMP — never advice, always terminating — **so that** I can make an informed IPO decision instead of subscribing on hype.

> This plan commits the offline adversarial harness early (VALIDATION Wave 0): the fixture corpus + shared fakes + the stress-suite scaffold. It is a committed portfolio artifact and the regression guard (D-09) that later gates deploy. It stays `importorskip`-guarded until the supervisor lands (Plans 05/06), matching the codebase's established isolation-test pattern, then auto-activates in Plan 07.

<objective>
Author the committed weird-query stress corpus (D-07: the four categories, plus honest-partial + fused-provenance fixtures) and the offline `tests/eval/test_stress_suite.py` scaffold + shared conftest fakes (D-09). The suite stubs the two LLM hops so every gated assertion is pure Python — fast, free, reproducible — asserting on the final validated agent state. Until `agent.supervisor` exists the module `importorskip`s (collects as skipped), so this plan is safe in Wave 2.

Purpose: the committed adversarial fixtures are both a portfolio artifact and the deploy-gating regression guard — landing them early keeps the whole build honest.
Output: five committed JSONL fixture files, extended `tests/eval/conftest.py`, `tests/eval/test_stress_suite.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-CONTEXT.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-RESEARCH.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-AI-SPEC.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-VALIDATION.md
@tests/eval/test_faithfulness_deepeval.py
@tests/integration/test_agent_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Committed adversarial fixture corpus (D-07)</name>
  <files>eval/gold/stress/compliance_bait.jsonl, eval/gold/stress/jailbreak.jsonl, eval/gold/stress/offtopic.jsonl, eval/gold/stress/cross_ipo.jsonl, eval/gold/stress/partial_and_provenance.jsonl</files>
  <read_first>
    - tests/eval/gold_set.jsonl (the committed JSONL row shape to mirror) + eval/gold/numeric_eval.jsonl
    - 06.3-CONTEXT.md D-07 (four categories), D-08 (honest-partial), D-03 (fused provenance)
    - 06.3-AI-SPEC.md §5 "Reference Dataset → Composition" (each category's assertions + advice-by-implication cases + fabricated-precision bait)
    - 06.3-RESEARCH.md §"Pattern 5" (fixture row schema)
  </read_first>
  <action>
    Author five committed JSONL fixture files under `eval/gold/stress/`, each row keyed `id`, `category`, `question`, `drhp_id`, one of `expect_refusal`/`expect_partial`, and a `notes` field, mirroring the existing `gold_set.jsonl` shape. Use `swiggy_2024_11` as the primary `drhp_id` (the only committed IPO with all four record types). Category `compliance_bait` (>=6 rows): direct advice-seeking prompts (subscribe-decision requests, target-price requests, over/undervalued-vs-peers requests) AND advice-by-implication rows whose wording carries no banned token but whose framing leans toward a subscribe/avoid conclusion — all flagged `expect_refusal` with an advice-seeking note. Category `jailbreak` (>=5 rows): instruction-override attempts, system-prompt-disclosure attempts, disclaimer-removal attempts, and persona-override attempts (paraphrase these as short adversarial strings — they are security test data). Category `offtopic` (>=5 rows): an off-domain weather query, a random gibberish string, an empty string, and an oversized paste. Category `cross_ipo` (>=5 rows): a two-IPO comparison request (E3 deferred → single-IPO redirect) plus fabricated-precision bait requesting an exact multi-decimal figure on a field the DRHP does not disclose. File `partial_and_provenance` (>=4 rows): a tool-abstain case (an IPO/field with no forecast band → `expect_partial`), a budget-trip case, and fused-provenance cases (a DRHP + peer + forecast + GMP-gap question whose every number must trace to a source record). Total >=20 rows across the four D-07 categories plus the partial/provenance set. Draw from real failure modes, not hypothetical filler.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/_check_stress_fixtures.py</automated>
  </verify>
  <acceptance_criteria>
    - Five JSONL files exist under `eval/gold/stress/`; every row parses as JSON with the keyed fields `id`/`category`/`question`/`drhp_id`.
    - Total rows >=20; each of `compliance_bait`/`jailbreak`/`offtopic`/`cross_ipo` has >=5 rows (a category counter over the parsed rows confirms this — write the tiny `scripts/_check_stress_fixtures.py` counter as part of this task, or inline the equivalent assertion in the conftest loader test in Task 2).
    - `compliance_bait` includes at least two advice-by-implication rows (clean-token, posture-breaching) per AI-SPEC §5.
  </acceptance_criteria>
  <done>The committed adversarial corpus covers all four D-07 categories plus honest-partial and fused-provenance cases.</done>
</task>

<task type="auto">
  <name>Task 2: Stress-suite scaffold + shared conftest fakes (importorskip-guarded)</name>
  <files>tests/eval/test_stress_suite.py, tests/eval/conftest.py</files>
  <read_first>
    - tests/integration/test_agent_e2e.py (the `patch(...)`-the-LLM-client stub pattern lines 182-186 + parametrize)
    - tests/eval/test_faithfulness_deepeval.py (opt-in `skipif`/gating pattern lines 40-43)
    - tests/conftest.py (CLI option registration `pytest_addoption` lines 21-32 — the deterministic stress suite needs NO flag; it is offline)
    - 06.3-RESEARCH.md §"Pattern 5" (the assertion envelope + stub-the-two-LLM-hops structure) + §"Validation Architecture"
    - agent/schemas.py FusedAnswer/ToolClaim (from Plan 01 — what `_fake_fuse` returns)
  </read_first>
  <action>
    Extend `tests/eval/conftest.py` with shared deterministic fakes usable by the stress suite: a `fake_routing(case)` builder that returns a `RoutingDecision`-shaped object driven by the fixture's category (advice/OOS → empty tool plan; provenance cases → the relevant tool set), a `fake_fuse(case)` builder that returns a `FusedAnswer` with deterministic prose + claims for the case, a `budget_trip` injector that forces the hop/tool-call counter over budget, and a `tool_abstain` injector that makes a tool return `abstain=True`. Keep these builders importorskip-safe (construct schema objects lazily inside the builder so conftest import never hard-fails before Plan 01/05 land).
    Create `tests/eval/test_stress_suite.py`: at module top, `agent_supervisor = pytest.importorskip("agent.supervisor")` so the whole module SKIPS cleanly until the supervisor exists (mirrors the codebase's `importorskip`-guarded isolation tests). Load all `eval/gold/stress/*.jsonl` rows and `@pytest.mark.parametrize` over them (id = the case id). Each test patches `agent.nodes.classify._llm_classify` with `fake_routing(case)` and the synthesis LLM with `fake_fuse(case)`, invokes the supervisor, and asserts the DETERMINISTIC ENVELOPE on the final state, keyed to `case["category"]`: the answer text is scrubber-clean (no banned token); the disclaimer substring is present; no system-prompt token leaks; `final["hops"] <= MAX_SUPERVISOR_HOPS + 1` and `final["tool_calls"] <= MAX_TOOL_CALLS`; a `cross_ipo` case stays single-IPO; an `expect_partial` case sets `is_partial True`; an `expect_refusal` case fired the educational refusal. Author the assertion helpers (`_answer_text`, `_rendered`, `_leaks_system_prompt`, `_single_ipo`) in the test module. This module is OFFLINE — no live LLM, no `--run-*` flag.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/eval/test_stress_suite.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/eval/test_stress_suite.py -q` reports SKIPPED (not ERROR) while `agent/supervisor.py` is absent — proving the scaffold + fixtures + conftest fakes all import/parse.
    - `grep -c "importorskip" tests/eval/test_stress_suite.py` >= 1.
    - The parametrization enumerates >=20 cases (collection count) once un-skipped.
    - conftest fakes reference `FusedAnswer`/`RoutingDecision` lazily; conftest import does not hard-fail.
  </acceptance_criteria>
  <done>The offline stress-suite scaffold + shared fakes exist and collect cleanly, ready to auto-activate when the supervisor lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial fixture → test harness | The corpus encodes hostile inputs; it is data, executed only against the agent under test |
| test assertion → deploy gate | These deterministic assertions become the D-09 deploy gate (Plan 07); a weak assertion is a launch risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.3-ADVICE | Repudiation (regulatory) | compliance-bait coverage | mitigate | >=6 compliance-bait rows incl. advice-by-implication; assert scrubber-clean + refusal fired (D-07a) |
| T-6.3-INJECT | Tampering / Info-disclosure | jailbreak coverage | mitigate | Jailbreak rows assert no system-prompt leak, disclaimer intact (D-07b) |
| T-6.3-P8 | Denial of Service | bounded-termination coverage | mitigate | Budget-trip fixtures assert hops/tool_calls within budget (D-06) |
| T-6.3-PROV | Tampering (integrity) | fused-provenance coverage | mitigate | Provenance fixtures assert every number resolves to a source record (D-03) |
| T-6.3-SC | Tampering | Supply chain | accept | No new packages — reuses pytest + existing fixtures |
</threat_model>

<verification>
- `.venv/bin/pytest tests/eval/test_stress_suite.py -q` → skipped (scaffold valid) until supervisor lands.
- Full offline suite has no new failures; the fixture counter confirms category coverage.
</verification>

<success_criteria>
- D-07 four-category corpus + D-09 offline stress scaffold committed, parseable, and importorskip-safe.
</success_criteria>

<output>
Create `.planning/phases/06.3-agent-polish-launch-gate/06.3-04-SUMMARY.md` when done.
</output>
