---
phase: 06.3-agent-polish-launch-gate
plan: 08
type: execute
wave: 5
depends_on: ["06.3-06"]
files_modified:
  - ui/fused_answer.py
  - ui/snapshot_chat.py
  - ui/copy.py
  - app/static/drhplens.css
  - tests/unit/test_fused_answer_render.py
autonomous: true
requirements: [D-03, D-04, D-08]
user_setup: []

must_haves:
  truths:
    - "A fused answer renders as one prose-first cited paragraph with an always-present mono provenance legend (C1)"
    - "DRHP numbers get numbered superscripts (reusing the existing citation expander); tool numbers get lettered superscripts distinguishable at a glance (C2)"
    - "The honest-partial banner is a muted dashed neutral strip above the answer — never red/alarm (C3)"
    - "Expansion reads the cached answer object only — no live LLM/Qdrant call on expand (P19)"
  artifacts:
    - path: "ui/fused_answer.py"
      provides: "C1/C2/C3 render helpers (render-only)"
      contains: "drhp-fused"
    - path: "app/static/drhplens.css"
      provides: "additive .drhp-fused/.drhp-prov/.drhp-partial classes"
      contains: ".drhp-prov"
    - path: "tests/unit/test_fused_answer_render.py"
      provides: "AST render-only isolation + provenance-resolves + no-red assertions"
      contains: "inspect"
  key_links:
    - from: "ui/fused_answer.py"
      to: "ui/expander.py render_citation_expanders"
      via: "reuse for DRHP Claim chips; sibling path for ToolClaim"
      pattern: "render_citation_expanders"
    - from: "ui/snapshot_chat.py"
      to: "agent.supervisor invoke_supervisor"
      via: "chat routes through the multi-tool supervisor, renders FusedAnswer"
      pattern: "invoke_supervisor"
---

## Phase Goal

**As an** Indian retail investor, **I want to** ask one plain-English question and get a single honestly-cited answer across the DRHP, peers, forecast, and GMP — never advice, always terminating — **so that** I can make an informed IPO decision instead of subscribing on hype.

> This plan makes the fused answer visible: the prose-first cited paragraph (C1), the doc-vs-tool provenance chips (C2 — the honesty payoff: a forecast number is visibly model-derived, not from the DRHP), and the neutral honest-partial banner (C3). Render-only, reusing the locked Graphite+Amber system with additive classes.

<objective>
Build the fused-answer render surfaces C1/C2/C3 (UI-SPEC) and route the chat through the multi-tool supervisor. Reuse the existing citation expander for DRHP `Claim` chips (numbered) and add a sibling path for `ToolClaim` chips (lettered ꜰ/ᴾ/ᴳ). Add the honest-partial banner (muted dashed, never red). Centralize all new copy in `ui/copy.py` (import-time scrubber covers it) and append the additive `.drhp-fused-*`/`.drhp-prov-*`/`.drhp-partial-*` CSS. Pin the render-only contract with an AST isolation test (no live LLM/Qdrant on expand).

Purpose: the provenance chips ARE the honesty thesis made visible; getting doc-vs-tool distinguishable at a glance is the C2 payoff.
Output: `ui/fused_answer.py`, wired `ui/snapshot_chat.py`, `ui/copy.py` strings, additive CSS, a render test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-CONTEXT.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-UI-SPEC.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-PATTERNS.md
@ui/snapshot_chat.py
@ui/expander.py
@ui/eval_inline.py
@ui/copy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: C1/C2/C3 render helpers (render-only, provenance chips, partial banner)</name>
  <files>ui/fused_answer.py, ui/copy.py, app/static/drhplens.css, tests/unit/test_fused_answer_render.py</files>
  <read_first>
    - ui/expander.py (`render_citation_expanders` lines 22-87 — reuse for DRHP Claim chips; note the `html.escape` on every untrusted field lines 77-82)
    - ui/eval_inline.py (the render-only model helper + honesty-invariant styling; its test `tests/unit/test_eval_inline.py` AST import-allowlist lines 40-46)
    - ui/disclaimer.py + ui/refusal_banner.py + ui/failures.py (the `.drhp-fail` muted-dashed neutral styling to reuse for C3)
    - ui/copy.py (the import-time scrubber loop lines 855-864 that auto-enrolls every module-level string; `REFUSAL_BANNED_TOKEN_COPY` line 125)
    - app/util/css_loader.py (`load_global_css` lines 30-49 — additive CSS injected every run) + app/static/drhplens.css (`--drhp-space-*` tokens)
    - 06.3-UI-SPEC.md C1/C2/C3 + Copywriting Contract + "Additive CSS Classes" + the implementation note (use `--drhp-space-*` tokens, not the mock's magic numbers)
  </read_first>
  <action>
    Create `ui/fused_answer.py` (render-only, no live call). C1: render one `<div class="drhp-fused">` cohesive cited paragraph via `st.markdown(..., unsafe_allow_html=True)` with an always-present mono footer legend (`.drhp-fused-legend`) mapping markers → sources (UI-SPEC legend copy). C2: DRHP-grounded `Claim` numbers get NUMBERED superscripts and reuse `ui/expander.py`'s `render_citation_expanders` unchanged (open the DRHP span + `[open page]`); tool-derived `ToolClaim` numbers get LETTERED superscripts (`ꜰ` forecast, `ᴾ` peers, `ᴳ` GMP) via a sibling descriptor path that expands to the one-line source descriptor + surface link (UI-SPEC expanded-source copy) — doc-grounded vs data-grounded distinguishable at a glance. Every marker is `.drhp-prov` (muted, accent on hover/focus, keyboard-focusable); expansion reads the cached answer object only (no live LLM/Qdrant — mirror the methodology-pane no-client rule). A `ToolClaim` with no resolvable source must NOT render (it was dropped at cite-check — FM-3). C3: an honest-partial banner (`.drhp-partial`, muted mono, dashed neutral border, eyebrow `INCOMPLETE ANSWER`) renders ABOVE the answer iff `is_partial` is True, naming what's missing (UI-SPEC two variants: tool-abstain source-specific + budget-trip); never red/alarm, never accompanying a fabricated gap-fill. GMP renders only as the caveated read-only gap (D-04 caveat copy), never green/red. Add ALL new C1/C2/C3 strings to `ui/copy.py` (so the import-time scrubber asserts them); reuse `REFUSAL_BANNED_TOKEN_COPY` for compliance-bait refusals — do NOT add a new advice-adjacent string. Append the additive `.drhp-fused*`/`.drhp-prov*`/`.drhp-partial*` rules to `app/static/drhplens.css` using the declared `--drhp-space-*` tokens (4/8/16/24), NOT the mock's magic numbers. `html.escape` every untrusted scraped field (peer/GMP `source`, company names) before render (XSS T-1-06).
    Write `tests/unit/test_fused_answer_render.py`: an AST import-allowlist (mirror `test_eval_inline.py`) asserting `ui/fused_answer.py` imports no live-call client (no LLM/Qdrant); a fused answer with a DRHP Claim + a forecast ToolClaim renders both a numbered and a lettered marker; a dropped/unresolved ToolClaim renders no marker; `is_partial=True` renders the `.drhp-partial` banner and `is_partial=False` does not; no `.drhp-partial`/`.drhp-ratelimit`/GMP rule uses a red/green/destructive color token (grep the appended CSS for the forbidden palette).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_fused_answer_render.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "render_citation_expanders" ui/fused_answer.py` >= 1 (reuses the existing expander for DRHP chips; not rebuilt).
    - DRHP numbers → numbered markers, tool numbers → lettered ꜰ/ᴾ/ᴳ markers (asserted); a dropped ToolClaim renders nothing (FM-3).
    - The `.drhp-partial` banner shows iff `is_partial=True`; no new CSS rule uses a red/green/destructive token (grep confirms).
    - `ui/fused_answer.py` imports no live LLM/Qdrant client (AST allowlist passes); new copy is in `ui/copy.py` (import-time scrubber covers it).
  </acceptance_criteria>
  <done>Fused answers render prose-first with doc-vs-tool provenance chips and a neutral honest-partial banner, all render-only.</done>
</task>

<task type="auto">
  <name>Task 2: Route chat through the multi-tool supervisor</name>
  <files>ui/snapshot_chat.py, tests/unit/test_fused_answer_render.py</files>
  <read_first>
    - ui/snapshot_chat.py (`_render_chat_history` lines 62-126 + `_render_input_and_invoke` lines 149-185 — currently routes through `invoke_with_tracing`; the `html.escape` posture line 67)
    - agent/supervisor.py (`invoke_supervisor` — the new entry point returning a FusedAnswer/partial state)
    - 06.3-UI-SPEC.md C1 "Rendered via the existing chat answer path"
  </read_first>
  <action>
    Modify `ui/snapshot_chat.py` so the production chat routes through `agent.supervisor.invoke_supervisor(question, drhp_id)` (the multi-tool bounded agent) instead of the single-tool `invoke_with_tracing`, and renders the returned fused answer via `ui/fused_answer.py` (C1/C2/C3). Preserve the existing spinner/loading state, the nothing-grounded refusal path (unchanged), and the existing per-answer footer/disclaimer wiring. Keep the chat-history render shape compatible so prior answers still render. Add a render-path test asserting the chat calls `invoke_supervisor` (patched) and passes its result to the fused-answer renderer, and that a partial result surfaces the C3 banner.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_fused_answer_render.py -q && .venv/bin/pytest tests/unit -k "snapshot_chat or fused" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "invoke_supervisor" ui/snapshot_chat.py` >= 1 (chat routed through the multi-tool agent).
    - A patched `invoke_supervisor` result renders through `ui/fused_answer.py`; a partial result shows the C3 banner (asserted).
    - The refusal + disclaimer + loading paths remain intact (existing chat tests stay green).
  </acceptance_criteria>
  <done>The live chat now drives the multi-tool supervisor and renders fused, provenance-chipped answers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped strings → HTML | Peer/GMP `source` + company names are untrusted; must be html.escaped before render (XSS) |
| cached answer → expand | Expansion must read the cached object only; a live call on expand is a quota/P19 risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.3-XSS | Tampering (client) | scraped peer/GMP/company strings in unsafe_allow_html | mitigate | `html.escape` every untrusted field; inline SVG only; no script in unsafe_allow_html |
| T-6.3-PROV | Tampering (integrity) | rendering an unsourced number | mitigate | Only Claim/ToolClaim with a resolved source render; dropped numbers show nothing (FM-3) |
| T-6.3-GMP | Repudiation (regulatory) | GMP framed as a signal / color-coded | mitigate | GMP renders only as the caveated read-only gap, no red/green (D-04) |
| T-6.3-ADVICE | Repudiation (regulatory) | new advice-adjacent copy | mitigate | New copy centralized in ui/copy.py (import-time scrubber); reuse existing refusal copy (D-07a) |
| T-6.3-DoS | Denial of Service | live call on expand | mitigate | Expansion reads the cached answer object only (P19; AST isolation test) |
| T-6.3-SC | Tampering | Supply chain | accept | No new packages — Streamlit primitives + hand-authored CSS |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit -k "fused or snapshot_chat" -q` green.
- Full offline suite has no new failures; the import-time scrubber in `ui/copy.py` passes with the new strings; existing eval-inline/expander tests stay green.
</verification>

<success_criteria>
- D-03 fused prose + provenance chips (C1/C2), D-04 GMP caveat render, and D-08 honest-partial banner (C3) are rendered render-only and tested; chat routes through the multi-tool supervisor.
</success_criteria>

<output>
Create `.planning/phases/06.3-agent-polish-launch-gate/06.3-08-SUMMARY.md` when done.
</output>
