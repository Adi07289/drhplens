---
phase: 06.3-agent-polish-launch-gate
plan: 09
type: execute
wave: 6
depends_on: ["06.3-08"]
files_modified:
  - agent/policies.py
  - ui/deploy_guard.py
  - ui/snapshot_chat.py
  - ui/copy.py
  - app/static/drhplens.css
  - .github/workflows/ping.yml
  - tests/unit/test_deploy_guard.py
autonomous: true
requirements: [D-12, D-13]
user_setup:
  - service: gemini
    why: "Live public chat needs the reasoning LLM; the daily cap is sized to its free-tier RPD"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> API keys (set as an HF Spaces Secret at deploy, never committed)"

must_haves:
  truths:
    - "A GLOBAL daily cap (app-wide, reliable) protects the Gemini free-tier RPD; per-session throttle rate-limits bursts; per-IP is documented unreliable behind the HF proxy (D-12)"
    - "The daily-cap value is set from a RE-VERIFIED current Gemini free-tier RPD, not the stale 1500/day figure (RESEARCH Pitfall 7)"
    - "On cap exhaustion the chat input is replaced by the C4 fallback card routing to the always-working read-only surfaces; on throttle a non-blocking inline notice appears (D-12/C4)"
    - "v1 ships on Streamlit + HF Spaces; no UI rewrite (D-13)"
  artifacts:
    - path: "ui/deploy_guard.py"
      provides: "global daily cap + per-session throttle (check_and_consume)"
      contains: "cache_resource"
    - path: "agent/policies.py"
      provides: "DEPLOY_DAILY_CAP + MIN_SECONDS_BETWEEN (re-verified RPD source cited)"
      contains: "DEPLOY_DAILY_CAP"
    - path: ".github/workflows/ping.yml"
      provides: "keep-warm cron pinger (copied from scripts/cron_pinger.yml)"
      contains: "schedule"
  key_links:
    - from: "ui/snapshot_chat.py"
      to: "ui/deploy_guard.py check_and_consume"
      via: "guard runs before invoke_supervisor; cap/throttle short-circuits"
      pattern: "check_and_consume"
    - from: "ui/deploy_guard.py"
      to: "agent/policies.py DEPLOY_DAILY_CAP"
      via: "cap value from policies (re-verified RPD)"
      pattern: "DEPLOY_DAILY_CAP"
---

## Phase Goal

**As an** Indian retail investor, **I want to** ask one plain-English question and get a single honestly-cited answer across the DRHP, peers, forecast, and GMP — never advice, always terminating — **so that** I can make an informed IPO decision instead of subscribing on hype.

> This plan makes the live chat public-safe: a global daily cap + per-session throttle bound the free-tier quota, and the C4 quota/rate-limit fallback degrades gracefully to the always-working read-only surfaces (D-12). Streamlit stays for v1 (D-13). The keep-warm pinger fights HF cold starts (P19).

<objective>
Wrap the public chat entrypoint with in-process deploy guards (D-12): a GLOBAL daily cap (`@st.cache_resource`, reliable) sized to a RE-VERIFIED current Gemini free-tier RPD, a per-session throttle (`st.session_state`, reliable), and the C4 quota/rate-limit fallback (cap-exhausted → replace input with the fallback card; throttled → non-blocking inline notice). Per-IP is documented unreliable behind the HF proxy and is NOT the primary control. Add the keep-warm pinger (P19). Record the D-13 "stay Streamlit for v1" decision.

Purpose: the launch is genuinely public + interactive but must survive the free-tier ceiling — guards + graceful degradation are the mechanism, and the daily cap must be sized from a fresh RPD reading.
Output: `agent/policies.py` deploy constants, `ui/deploy_guard.py`, wired chat, C4 copy + CSS, `ping.yml`, a guard unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-CONTEXT.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-RESEARCH.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-UI-SPEC.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-PATTERNS.md
@ui/snapshot_chat.py
@scripts/cron_pinger.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify Gemini free-tier RPD + deploy constants + deploy_guard (D-12)</name>
  <files>agent/policies.py, ui/deploy_guard.py, tests/unit/test_deploy_guard.py</files>
  <read_first>
    - 06.3-RESEARCH.md §"Pattern 4" (global daily cap `@st.cache_resource` + per-session throttle; per-IP unreliable behind proxy; the daily-cap sizing formula) + §"Pitfall 7" (re-verify RPD; 1500/day is stale; spike 002 ~20 RPD) + §"Assumptions Log A1"
    - agent/policies.py (the calibration-docstring posture for new constants) + GEMINI_MODELS line 76
    - 06.3-PATTERNS.md §"No Analog Found → ui/deploy_guard.py"
  </read_first>
  <action>
    First, RE-VERIFY the current Gemini free-tier RPD/RPM for the synthesis model (`gemini-3.5-flash`) and the classify model (`gemini-3.1-flash-lite`) against the current Google AI rate-limit documentation (do NOT copy the stale CLAUDE.md "1500 req/day" figure — RESEARCH Pitfall 7). Add `DEPLOY_DAILY_CAP: int` and `MIN_SECONDS_BETWEEN: float` to `agent/policies.py` with a calibration docstring citing the RPD source + the verification date + the sizing formula (`cap = floor(daily synthesis-model RPD / worst-case Gemini calls per query)` with margin, worst case ≈ 1 classify on `-lite` + ≤MAX_TOOL_CALLS drhp_rag generate + 1 synthesis). If the live docs cannot be reached at execution time, set a conservative cap from the RESEARCH ~20 RPD figure and mark the docstring `[ASSUMED — re-verify before deploy]` so the deploy checkpoint (Plan 10) confirms it.
    Create `ui/deploy_guard.py` with `check_and_consume(daily_cap, min_seconds) -> str` returning `"ok" | "rate_limited" | "cap_exhausted"`: a GLOBAL counter via `@st.cache_resource` (one shared object per Space process, resets on cold start) that rolls over at midnight and returns `"cap_exhausted"` at/above the cap; a per-session throttle via `st.session_state` last-question timestamp returning `"rate_limited"` inside `min_seconds`; otherwise stamps the session ts, increments the global counter, returns `"ok"`. Per-IP is NOT the primary control — add a docstring noting `st.context.ip_address` is None behind the HF proxy (RESEARCH A4); best-effort IP is optional only.
    Write `tests/unit/test_deploy_guard.py` (monkeypatch `st.cache_resource`/`st.session_state`/clock): the Nth+1 call at the cap returns `"cap_exhausted"`; a call within `min_seconds` returns `"rate_limited"`; a spaced call under the cap returns `"ok"` and increments the global counter; the counter rolls over on a new date.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_deploy_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `agent/policies.py` defines `DEPLOY_DAILY_CAP` + `MIN_SECONDS_BETWEEN` with a docstring citing the RPD source + verification date (or `[ASSUMED — re-verify before deploy]`), NOT the 1500/day figure.
    - `check_and_consume` returns `"cap_exhausted"` at the cap, `"rate_limited"` within the throttle window, `"ok"` otherwise (asserted); global counter rolls over by date.
    - The module documents per-IP as unreliable behind the HF proxy (not the primary control).
  </acceptance_criteria>
  <done>A reliable global daily cap + per-session throttle bound the free-tier quota, sized from a re-verified RPD.</done>
</task>

<task type="auto">
  <name>Task 2: C4 quota/rate-limit fallback wiring + keep-warm pinger (D-12/C4, P19)</name>
  <files>ui/snapshot_chat.py, ui/copy.py, app/static/drhplens.css, .github/workflows/ping.yml, tests/unit/test_deploy_guard.py</files>
  <read_first>
    - ui/snapshot_chat.py (`_render_input_and_invoke` — insert the guard BEFORE `invoke_supervisor`)
    - ui/copy.py (import-time scrubber loop; add the C4 strings) + 06.3-UI-SPEC.md C4 + Copywriting Contract (quota card heading/body/walkthrough link + rate-limit inline notice)
    - app/static/drhplens.css (`.drhp-quota-*` / `.drhp-ratelimit` additive classes; `--drhp-space-*` tokens) + app/util/css_loader.py
    - scripts/cron_pinger.yml (the every-8-min keep-warm workflow to copy) + 06.3-PATTERNS.md §".github/workflows/ping.yml"
  </read_first>
  <action>
    In `ui/snapshot_chat.py`, call `deploy_guard.check_and_consume(DEPLOY_DAILY_CAP, MIN_SECONDS_BETWEEN)` BEFORE `invoke_supervisor`. On `"cap_exhausted"` (C4): REPLACE the chat input with a muted `st.container` card (`.drhp-quota`, dashed neutral border) — Serif heading + body routing to the always-working read-only surfaces (snapshot/forecast/peers, `/methodology`, `/failures`) + a `▶ Recorded chat walkthrough` link (UI-SPEC copy); the rest of the app stays fully usable (the read-only surfaces are LLM-free) — NOT a full-page interstitial. On `"rate_limited"`: keep the input enabled and show a brief muted inline notice (`.drhp-ratelimit`) above it (non-blocking, auto-clears). On `"ok"`: proceed to `invoke_supervisor`. Add all C4 strings to `ui/copy.py` (import-time scrubber covers them); no red/green/countdown-alarm styling anywhere (honesty invariant). Append the additive `.drhp-quota-*`/`.drhp-ratelimit` CSS to `app/static/drhplens.css` using `--drhp-space-*` tokens. For the recorded-walkthrough form (CONTEXT.md discretion), wire the link to a committed transcript page/asset (no headless-capture tooling, consistent with the deferred-screenshots decision). Copy `scripts/cron_pinger.yml` to `.github/workflows/ping.yml` (keep-warm every ~8 min, swap the Space URL placeholder) to fight HF cold starts (P19). Add tests: the guard states map to the three mutually-exclusive UI states (normal input / inline notice / card-replaces-input); no C4/rate-limit CSS rule uses a red/green/destructive token.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_deploy_guard.py -q && .venv/bin/pytest tests/unit -k "snapshot_chat or deploy" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "check_and_consume" ui/snapshot_chat.py` >= 1 and the guard runs BEFORE `invoke_supervisor`.
    - Cap-exhausted replaces the input with the `.drhp-quota` card (read-only surfaces still reachable); rate-limited shows the non-blocking `.drhp-ratelimit` notice; the three states are mutually exclusive (asserted).
    - `.github/workflows/ping.yml` exists with a `schedule:` cron (keep-warm, P19).
    - No C4/rate-limit CSS uses a red/green/destructive token; new copy is centralized in `ui/copy.py`.
  </acceptance_criteria>
  <done>The public chat degrades gracefully to the honest read-only surfaces on quota exhaustion and stays warm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public request → LLM quota | Untrusted public traffic can exhaust the free-tier RPD; the cap is the anti-DoS boundary |
| GEMINI_API_KEY | Secret must live only in HF Spaces Secrets → env var, never committed |
| HF proxy → client IP | The proxy makes per-IP unreliable; session + global cap are the trusted controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.3-DoS | Denial of Service | free-tier quota exhaustion / abuse | mitigate | Global daily cap (app-wide) + per-session throttle + semantic cache; C4 graceful fallback (D-12) |
| T-6.3-SECRET | Info-disclosure | GEMINI_API_KEY leakage | mitigate | Key via HF Spaces Secret → env var; never committed; not logged/traced (user_setup) |
| T-6.3-IP | Repudiation | per-IP control assumed reliable | accept | Documented unreliable behind the HF proxy; session+global cap work regardless (RESEARCH A4) |
| T-6.3-ADVICE | Repudiation (regulatory) | new fallback copy leaning advice | mitigate | C4 copy centralized in ui/copy.py (import-time scrubber); no red/green (D-12/UI-SPEC) |
| T-6.3-SC | Tampering | Supply chain | accept | No new packages — Streamlit primitives + GitHub Actions cron |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit -k "deploy or snapshot_chat" -q` green.
- The three guard states render mutually-exclusively; grep confirms no destructive color token in the new CSS.
- `.github/workflows/ping.yml` validates as a well-formed workflow (YAML parses; `schedule:` present).
</verification>

<success_criteria>
- D-12 global daily cap + per-session throttle + C4 fallback + keep-warm implemented with a re-verified RPD; D-13 (stay Streamlit v1) recorded.
</success_criteria>

<output>
Create `.planning/phases/06.3-agent-polish-launch-gate/06.3-09-SUMMARY.md` when done.
</output>
