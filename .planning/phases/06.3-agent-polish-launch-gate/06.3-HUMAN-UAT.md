# Phase 06.3 — Human UAT / Blocking HITL Items (Launch Gate)

**Plan:** 06.3-10 (launch gate) · **Created:** 2026-08-13 · **Mode:** draft-auto-parts +
document-the-human-items (`human_verify_mode: end-of-phase`)

The three `type="auto"` tasks of 06.3-10 are complete and committed (SEBI self-audit
DRAFT, WR-03 dependency sync, `eval-gate.yml`). The items below are the **human-in-the-loop
blocking checkpoints** an executor cannot complete alone (D-14). They were **drafted where
draftable and are recorded here as PENDING** — nothing was faked, nothing hard-halted the
run. **DRHPLens does not ship publicly until every item below is resolved.**

Each item: description + expected outcome + `result: pending`.

---

## H-1 — SEBI self-audit human sign-off (D-10/D-11) — was Task 2 `checkpoint:human-verify`

- **Description:** A SEBI-compliance-literate reviewer (or the operator explicitly standing
  in per D-10) reads `compliance/SEBI-REVIEW.md` against the enforcing code
  (`compliance/scrubber.py`, `banned_tokens.py`, `disclaimer_text.py`) and the D-09 stress
  suite (`tests/eval/test_stress_suite.py`), confirms the honest self-audit label is
  accurate (genuinely a self-audit, not represented as a legal review), and closes or
  honestly leaves open each of the 3 §1b flags (Section 5 of the doc).
- **Expected:** The reviewer fills the **Sign-off Record** (Section 7) of
  `compliance/SEBI-REVIEW.md` — reviewer/role, date, outcome (`signed-off` /
  `signed-off-with-edits` / `changes-requested`), and a disposition for each of Flags 1–3.
  No posture-table claim is unsupported by real enforcing code + a real test. The honesty
  label stands.
- **How folded (end-of-phase mode):** the Task 2 checkpoint is recorded here as a pending
  blocking item instead of hard-halting the run (resolves plan-checker Warning #1).
- **Artifact:** `compliance/SEBI-REVIEW.md` (Section 0 blocking status; Section 7 record).
- **result: pending**

## H-2 — CI repo secrets + PR gate verification (D-14) — part of Task 5 `checkpoint:human-action`

- **Description:** Add the repo Actions secrets so `eval-gate.yml`'s live lane can run:
  `GEMINI_API_KEY`, `QDRANT_URL`, `QDRANT_API_KEY`, `LANGFUSE_PUBLIC_KEY`,
  `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST` — in **GitHub → Settings → Secrets and variables →
  Actions**. The executor cannot supply secrets.
- **Expected:** Open a PR; confirm the `eval-gate` workflow runs; confirm a **forced
  regression** (e.g. a deliberately broken citation) makes the gate exit non-zero and
  blocks merge. No secret is ever committed (keys live only in Actions Secrets).
- **Artifact:** `.github/workflows/eval-gate.yml` (drafted, code half complete).
- **result: pending**

## H-3 — Public HF Spaces deploy (OPS-02 / D-12) — part of Task 5 `checkpoint:human-action`

- **Description:** Create the HF Space, set the same keys as **Space Secrets**, seed the
  **Qdrant Cloud** (free 1GB) collection with the committed corpus (the index must live on
  Qdrant Cloud — HF `/tmp` is ephemeral), deploy the Streamlit app, and replace the
  `<user>` placeholder in `.github/workflows/ping.yml` with the real HF username (then
  enable Actions) so the keep-warm pinger fires.
- **Expected:** On a phone, a real fused question renders a cited answer; exhausting the
  daily cap engages the **C4** quota-fallback card (input replaced) while the read-only
  surfaces (snapshot/forecast/peers, `/methodology`, `/failures`, `/how_it_works`) stay
  usable; the keep-warm pinger keeps the Space responsive. Also re-verify the deploy
  daily-cap value live (the `DEPLOY_DAILY_CAP` constant is `[ASSUMED — re-verify at deploy]`,
  sized from ~20 RPD, NOT the stale 1500/day).
- **Artifacts:** the Streamlit app + deploy guards (Plan 06.3-09); `.github/workflows/ping.yml`.
- **result: pending**

## H-4 — Judge-vs-human calibration ≥50 examples, ≥0.7 (D-14) — was Task 6 `checkpoint:human-action`

- **Description:** The SEBI-compliance-literate reviewer labels **≥ 50** adversarial-
  compliance / faithfulness examples pass/fail and computes judge-vs-human correlation.
  Burns Gemini free-tier quota (≈20/day) — pace across days.
- **Expected:** Record N + correlation + decision in `eval/reports/judge_calibration.md`.
  **Only if correlation ≥ 0.7** may a real (non `-1`) faithfulness number be committed and
  surfaced; **otherwise the surface STAYS the honest `-1`** (do NOT commit a number below
  0.7 — T-6.3-THEATER / the 6.1 P10 guard).
- **Artifact:** `eval/reports/judge_calibration.md` (pending record drafted).
- **result: pending** — faithfulness surface currently `-1` "not measured", preserved.

## H-5 — Gold-set span-tightening by a human reading the Swiggy DRHP (D-14) — was Task 6 `checkpoint:human-action`

- **Description:** An experienced Indian IPO investor/analyst reads the **Swiggy DRHP PDF**
  and sets real `expected_sources` answer pages (span-tightening to ≤ 2–3 pp). **Do NOT
  re-run auto-substring matching** — it was already refused on honesty grounds in 6.1
  (answer numbers not verbatim in chunk text; generic substrings over-match).
- **Expected:** Tight `expected_sources` spans in the gold set so `recall@10` /
  `citation_accuracy` become discriminating signals instead of saturated regression floors
  (which they honestly remain until then).
- **Artifact:** the Swiggy gold set (`tests/eval/gold_set.jsonl` / `eval/gold/*`); tracked
  in `eval/reports/judge_calibration.md`.
- **result: pending**

---

## Summary

| ID | Item | Blocking? | result |
|----|------|-----------|--------|
| H-1 | SEBI self-audit human sign-off (D-10/D-11) | yes | pending |
| H-2 | CI repo secrets + PR gate verification (D-14) | yes | pending |
| H-3 | Public HF Spaces deploy (OPS-02 / D-12) | yes | pending |
| H-4 | Judge-vs-human calibration ≥50/≥0.7 (D-14) | yes | pending |
| H-5 | Gold-set span-tightening — human reads Swiggy DRHP (D-14) | yes | pending |

**Honest-faithfulness invariant preserved:** no real faithfulness number is committed; the
`-1` "not measured" surface stands until H-4 clears ≥0.7. **The auto (code) half of the
launch gate is complete; the human half is enumerated above and remains open.**
