---
phase: 06.3-agent-polish-launch-gate
plan: 10
type: execute
wave: 7
depends_on: ["06.3-07", "06.3-09"]
files_modified:
  - compliance/SEBI-REVIEW.md
  - requirements.txt
  - pyproject.toml
  - .github/workflows/eval-gate.yml
  - eval/reports/judge_calibration.md
  - tests/eval/test_faithfulness_deepeval.py
autonomous: false
requirements: [D-10, D-11, D-13, D-14]
user_setup:
  - service: github
    why: "The CI gate lane runs release_gate + the stress suite on PRs and needs the same secrets as the live app"
    dashboard_config:
      - task: "Add repo secrets GEMINI_API_KEY / QDRANT_URL / QDRANT_API_KEY / LANGFUSE keys"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"
  - service: huggingface
    why: "OPS-02 public deploy of the Streamlit app on HF Spaces"
    dashboard_config:
      - task: "Create the Space, set the same keys as Space Secrets, seed the Qdrant Cloud collection"
        location: "HF Spaces -> Settings -> Secrets; Qdrant Cloud console"

must_haves:
  truths:
    - "compliance/SEBI-REVIEW.md is an honestly-labelled SELF-AUDIT (not a lawyer sign-off) mapping each posture claim to enforcing code + verifying test, covering the new 6.3 conversational surfaces + public exposure (D-10/D-11)"
    - "requirements.txt and pyproject.toml are in sync (WR-03), with the ragas optional-extras decision recorded (D-14)"
    - "The faithfulness surface stays the honest -1 until the >=0.7 judge-vs-human calibration truly passes (D-14 P10 guard)"
    - "Judge calibration, gold-set span-tightening, CI-lane secrets, and the public deploy are HITL and cannot be auto-completed (D-14)"
  artifacts:
    - path: "compliance/SEBI-REVIEW.md"
      provides: "honest self-audit + posture-to-code-to-test table + 3 unverified 1b flags"
      contains: "SELF-AUDIT"
    - path: ".github/workflows/eval-gate.yml"
      provides: "CI gate lane (release_gate + stress suite on PRs)"
      contains: "release_gate"
  key_links:
    - from: "compliance/SEBI-REVIEW.md"
      to: "compliance/scrubber.py + tests/eval/test_stress_suite.py"
      via: "posture -> enforcing code -> verifying test (references existing enforcement, no new gate)"
      pattern: "scrubber"
    - from: ".github/workflows/eval-gate.yml"
      to: "scripts/release_gate.py"
      via: "CI runs the release gate + stress suite; regression blocks merge"
      pattern: "release_gate"
---

## Phase Goal

**As an** Indian retail investor, **I want to** ask one plain-English question and get a single honestly-cited answer across the DRHP, peers, forecast, and GMP — never advice, always terminating — **so that** I can make an informed IPO decision instead of subscribing on hype.

> This is the launch gate. Nothing ships publicly until the SEBI self-audit (D-10/D-11) + the deterministic stress gate (Plan 07) pass. This plan is `autonomous: false` — it draws in the human-in-the-loop items (SEBI sign-off, judge calibration, gold-set span-tightening, CI-lane secrets, the HF deploy) an executor cannot complete alone (D-14). WR-03 is the one fully executor-completable item.

<objective>
Close the launch gate (D-10/D-11/D-13/D-14): draft `compliance/SEBI-REVIEW.md` (honest self-audit re-auditing the new conversational surfaces + public exposure) and gate it on a human SEBI-literate review; complete the WR-03 dependency sync (executor); draft the CI gate lane and gate it on user-provided repo secrets; sequence the remaining HITL items — judge calibration (>=50 examples, >=0.7), gold-set span-tightening (human reads the Swiggy DRHP PDF), and the public HF Spaces deploy — as blocking checkpoints, keeping the honest `-1` faithfulness surface until calibration truly clears.

Purpose: the honesty invariant is load-bearing at launch — the SEBI doc is a self-audit not a lawyer review, and the two HITL evals cannot be auto-faked.
Output: `SEBI-REVIEW.md`, synced deps, `eval-gate.yml`, a calibration record, and the blocking human checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-CONTEXT.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-RESEARCH.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-AI-SPEC.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-VALIDATION.md
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/deferred-items.md
@compliance/scrubber.py
@scripts/release_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft compliance/SEBI-REVIEW.md self-audit (D-10/D-11)</name>
  <files>compliance/SEBI-REVIEW.md</files>
  <read_first>
    - 06.3-AI-SPEC.md 1b "Regulatory/Compliance Context" (the two regimes + finfluencer framework; the 3 honest flags lines 105-108) + "Domain Expert Roles"
    - 06.3-RESEARCH.md lines 489-525 (the recommended section structure + posture-to-enforcing-code-to-verifying-test table + the mandatory top-of-file honest label)
    - compliance/scrubber.py + compliance/banned_tokens.py + compliance/disclaimer_text.py (the enforcement the doc references — no NEW gate)
    - tests/eval/test_stress_suite.py (the D-09 adversarial-compliance set the doc cites as the enforceable proxy)
    - 06.3-PATTERNS.md section "compliance/SEBI-REVIEW.md"
  </read_first>
  <action>
    Draft `compliance/SEBI-REVIEW.md` per the RESEARCH-recommended structure. MANDATORY top-of-file label (D-10 honesty invariant): state it is a SELF-AUDIT by the product owner, NOT a legal review or lawyer sign-off, and that nothing in it is legal advice. Sections: (1) Scope and method — what/by-whom/date/commit, with the operator standing in for the SEBI-literate reviewer said plainly; (2) the boundary DRHPLens stays OUTSIDE — SEBI IA Regs 2013 (no personalization/fees/portfolio), SEBI RA Regs 2014 (no reco/target/fair-value/subscribe-avoid), the Jan-2025 education-vs-advice finfluencer framework + three-month data lag; (3) the posture-to-enforcing-code-to-verifying-test AUDIT TABLE (no banned prescriptive token maps to compliance/scrubber.py import-time assertion + banned_tokens.py verified by test_scrub_node.py + D-09 stress category a; disclaimer coverage maps to disclaimer_text.py ANCHOR_COPY verified by disclaimer tests + D-09 stress b; single-IPO scope maps to tools keyed by drhp_id verified by D-09 stress d; GMP display-only maps to the gmp isolation test; numeric faithfulness maps to cite-check + release_gate); (4) the D-11 RE-AUDIT of the NEW 6.3 surfaces (fused answers + conversational forecast/peer/GMP + public live chat), naming advice-by-implication as the residual risk the scrubber cannot catch, mitigated by the REPORTED judge lane + the describe-never-conclude prompt; (5) the 3 unverified 1b honesty flags carried forward VERBATIM (cite the gazette/circular or mark "unverified — prudent self-imposed posture"); (6) what this audit does NOT establish (not a legal opinion; the committed D-09 set is the enforceable proxy). Add NO new automated enforcement gate — the doc REFERENCES existing enforcement.
  </action>
  <verify>
    <automated>grep -qi "SELF-AUDIT" compliance/SEBI-REVIEW.md && grep -qi "not a legal" compliance/SEBI-REVIEW.md && grep -q "scrubber.py" compliance/SEBI-REVIEW.md && grep -q "test_stress_suite" compliance/SEBI-REVIEW.md && grep -qi "unverified" compliance/SEBI-REVIEW.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The file's first screenful carries the mandatory honest label (SELF-AUDIT, not a legal review / lawyer sign-off).
    - Every posture claim maps to a specific enforcing code file AND a verifying test (the audit table).
    - The D-11 re-audit of the new conversational + public surfaces is present, naming advice-by-implication as the residual risk.
    - The 3 unverified 1b regulatory flags are carried forward, each cited or marked "unverified — prudent self-imposed posture".
    - No new automated enforcement gate is added (the doc references existing enforcement only).
  </acceptance_criteria>
  <done>An honestly-labelled self-audit maps the SEBI posture to enforcing code + tests and re-audits the new 6.3 surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SEBI self-audit human sign-off (D-10/D-11 HITL)</name>
  <action>Pause for a human SEBI-compliance-literate reviewer (or the operator standing in per D-10). No executor code runs; the executor presents the drafted `compliance/SEBI-REVIEW.md` and the enforcing code/tests for review and waits for the resume signal before the phase proceeds.</action>
  <what-built>The drafted compliance/SEBI-REVIEW.md self-audit (Task 1), mapping the no-advice posture to enforcing code + the D-09 stress gate, and re-auditing the new fused/conversational/public surfaces.</what-built>
  <how-to-verify>
    1. A SEBI-compliance-literate reviewer (or the operator explicitly standing in per D-10) reads `compliance/SEBI-REVIEW.md` against the enforcing code (`compliance/scrubber.py`, `banned_tokens.py`, `disclaimer_text.py`) and the D-09 stress suite.
    2. Confirm the honest label is accurate (it is genuinely a self-audit, not represented as legal review).
    3. Confirm each of the 3 unverified 1b flags is either closed against the actual gazette/circular text or honestly left "unverified — prudent self-imposed posture".
    4. Confirm the advice-by-implication residual-risk framing is accurate and the mitigation (REPORTED judge lane + prompt) is honestly described.
  </how-to-verify>
  <acceptance_criteria>
    - A human reviewer records sign-off (or requested edits) on the doc; the honesty label stands.
    - No posture claim in the table is unsupported by real enforcing code + a real test.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the reviewer signs off, or describe required edits.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: WR-03 requirements.txt / pyproject.toml sync + ragas extras (D-14, executor-completable)</name>
  <files>requirements.txt, pyproject.toml</files>
  <read_first>
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/deferred-items.md (WR-03 dependency drift + the ragas-bloat optional-extras note)
    - requirements.txt + pyproject.toml (the two dependency lists to reconcile)
    - 06.3-CONTEXT.md D-14 (WR-03 pure-code; fold the ragas optional-extras decision here) + Deferred Ideas (ragas bloat)
  </read_first>
  <action>
    Reconcile `requirements.txt` and `pyproject.toml` so the runtime dependency sets match (WR-03) — every package the deployed app imports is present in both, at the same pins, with no drift. Fold the deferred `ragas`-bloat decision (D-14): decide whether `ragas` (heavy on HF Spaces) belongs in the core deploy set or an optional extras group (e.g. an `[eval]` extra used only in CI/local eval, not the Space runtime), and record that decision inline as a comment in `pyproject.toml`. Do NOT add or upgrade unrelated packages. This is the one D-14 item fully executor-completable start to finish.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/_check_dep_sync.py</automated>
  </verify>
  <acceptance_criteria>
    - Every runtime package appears in BOTH `requirements.txt` and `pyproject.toml` at consistent pins (write the tiny `scripts/_check_dep_sync.py` diff-checker as part of this task, or assert equivalence in a unit test).
    - The `ragas` optional-extras decision is recorded as a comment/extras group in `pyproject.toml`.
    - No unrelated dependency was added or bumped.
  </acceptance_criteria>
  <done>The two dependency manifests are in sync and the ragas extras decision is recorded (WR-03 closed).</done>
</task>

<task type="auto">
  <name>Task 4: Draft the CI gate lane workflow (D-14, code half)</name>
  <files>.github/workflows/eval-gate.yml</files>
  <read_first>
    - .github/workflows/nightly-nse.yml (the repo's only workflow — copy its checkout@v4 -> setup-python@v5 (3.11) -> pip install -> run shape lines 22-40)
    - scripts/release_gate.py (`main` entry) + tests/eval/test_stress_suite.py (the offline gate)
    - 06.3-PATTERNS.md section ".github/workflows/eval-gate.yml" (HITL — needs repo secrets)
  </read_first>
  <action>
    Author `.github/workflows/eval-gate.yml` mirroring `nightly-nse.yml`'s structure but triggered `on: [pull_request]`: checkout, setup Python 3.11, install deps, then run `python scripts/release_gate.py` (the deterministic eval + numeric + stress lanes) AND `pytest tests/eval/test_stress_suite.py` so a citation/numeric/stress regression physically blocks merge. Reference the required secrets via `${{ secrets.GEMINI_API_KEY }}` / `QDRANT_URL` / `QDRANT_API_KEY` / Langfuse keys as env — the offline stress lane needs no secret, but the live-lane steps do. This is the CODE half only; the secrets are provided by the user (Task 5).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/eval-gate.yml')); sys.exit(0)" && grep -q "release_gate" .github/workflows/eval-gate.yml && grep -q "test_stress_suite" .github/workflows/eval-gate.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/eval-gate.yml` is valid YAML, triggers on `pull_request`, and runs both `scripts/release_gate.py` and the stress suite.
    - Secrets are referenced via `${{ secrets.* }}` (not hard-coded).
  </acceptance_criteria>
  <done>The CI gate lane workflow exists and will block merges on a compliance/eval regression once secrets are set.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Provide CI + deploy secrets and complete the public HF Spaces deploy (D-12/D-14 HITL)</name>
  <action>Pause for the user to add the repo/Space secrets and perform the public HF Spaces deploy (create the Space, set Secrets, seed Qdrant Cloud, deploy the Streamlit app). The executor cannot supply secrets or click the deploy — it provides the exact steps and waits for the resume signal.</action>
  <what-built>The CI gate workflow (Task 4), the deploy guards + C4 fallback + keep-warm pinger (Plan 09), and the full multi-tool agent.</what-built>
  <how-to-verify>
    1. Add the repo Actions secrets (GEMINI_API_KEY / QDRANT_URL / QDRANT_API_KEY / LANGFUSE keys) in GitHub -> Settings -> Secrets so `eval-gate.yml` can run its live steps; open a PR and confirm the gate runs.
    2. Create the HF Space, set the same keys as Space Secrets, and seed the Qdrant Cloud (free 1GB) collection with the committed corpus (the index is the retrieval source; `/tmp` is ephemeral so it must be Qdrant Cloud, not a local /tmp Qdrant).
    3. Deploy the Streamlit app; on a phone, ask a real fused question and confirm a cited answer renders; then exhaust the daily cap and confirm the C4 fallback card replaces the input while the read-only surfaces (snapshot/forecast/peers, /methodology, /failures) still work.
    4. Confirm the keep-warm pinger keeps the Space responsive.
  </how-to-verify>
  <acceptance_criteria>
    - The CI gate runs on a PR (secrets present) and blocks on a forced regression.
    - The public URL serves a live fused chat answer on mobile; the C4 quota fallback engages at the cap and the read-only surfaces stay usable.
    - No secret is committed to the repo (keys live only in Actions/Space Secrets).
  </acceptance_criteria>
  <resume-signal>Type "deployed" with the public URL once the Space is live and the cap fallback is verified, or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Judge-vs-human calibration + gold-set span-tightening (D-14 HITL — keep -1 until cleared)</name>
  <action>Pause for the human labelling work: the SEBI-literate reviewer labels >=50 examples for judge-vs-human correlation and an IPO analyst reads the Swiggy DRHP PDF to tighten `expected_sources`. The executor records the outcome in `eval/reports/judge_calibration.md` and keeps the faithfulness surface at the honest `-1` unless correlation >=0.7; it does not fabricate labels or re-run auto-substring matching.</action>
  <what-built>The REPORTED advice-by-implication / faithfulness judge lane and the coarse Swiggy gold set; both stay honest until a human calibrates them.</what-built>
  <how-to-verify>
    1. The SEBI-literate reviewer labels >=50 adversarial-compliance / faithfulness examples pass/fail and computes judge-vs-human correlation; record it in `eval/reports/judge_calibration.md`. Only if correlation >=0.7 does a real (non `-1`) faithfulness number get committed — otherwise the surface STAYS the honest `-1` "not measured" (the 6.1 P10 guard; do NOT commit a number below 0.7).
    2. An experienced IPO investor/analyst reads the Swiggy DRHP PDF and sets real `expected_sources` answer pages (span-tightening to <=2-3pp). Do NOT re-run auto-substring matching — it was already refused on honesty grounds in 6.1.
    3. Confirm `tests/eval/test_faithfulness_deepeval.py` / the inline surface still shows `-1` until (1) clears.
  </how-to-verify>
  <acceptance_criteria>
    - `eval/reports/judge_calibration.md` records the >=50-example correlation; the faithfulness surface is only upgraded from `-1` if correlation >=0.7 (else it stays `-1`).
    - The Swiggy gold-set span-tightening is done by a human reading the PDF (no auto-substring re-run).
    - The honest `-1` faithfulness surface is preserved wherever calibration has not cleared.
  </acceptance_criteria>
  <resume-signal>Type "calibrated" with the correlation value (and whether faithfulness is now committed or stays -1), or "deferred — staying at -1".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-audit claim -> reality | The doc must honestly reflect enforcing code; overclaiming (calling it legal review) is the load-bearing risk |
| repo/Space secrets | Keys live only in Actions/Space Secrets; a committed key is a disclosure breach |
| judge number -> public surface | Committing a faithfulness number below calibration is evaluation theater (P10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.3-AUDIT | Repudiation (regulatory) | SEBI-REVIEW overclaiming legal review | mitigate | Mandatory honest self-audit label + human sign-off; 3 flags carried forward (D-10) |
| T-6.3-THEATER | Repudiation | committing an uncalibrated faithfulness number | mitigate | Keep `-1` until >=0.7 correlation; do NOT auto-fake (D-14, P10) |
| T-6.3-SECRET | Info-disclosure | secret leakage in CI/deploy | mitigate | Secrets only in Actions/Space Secrets; referenced via `${{ secrets.* }}`; never committed |
| T-6.3-GATE | Repudiation | a regression merging past CI | mitigate | eval-gate.yml runs release_gate + stress suite on PRs; blocks merge |
| T-6.3-SC | Tampering | dependency drift on deploy | mitigate | WR-03 sync; ragas extras decision recorded; no unrelated bumps |
</threat_model>

<verification>
- Task 1/3/4 automated verifies green (self-audit content assertions; dep-sync checker; eval-gate YAML validity).
- The four HITL checkpoints (SEBI sign-off, deploy, calibration, gold-set) are recorded as blocking — the phase does not complete until the human confirms each.
- The honest `-1` faithfulness surface is preserved unless calibration clears >=0.7.
</verification>

<success_criteria>
- D-10/D-11 self-audit drafted + human-signed; D-14 WR-03 closed (executor), CI lane drafted + secrets HITL, judge-calibration + gold-set span-tightening HITL with the honest `-1` preserved; D-13 (stay Streamlit) recorded; the public deploy verified.
</success_criteria>

<output>
Create `.planning/phases/06.3-agent-polish-launch-gate/06.3-10-SUMMARY.md` when done.
</output>
