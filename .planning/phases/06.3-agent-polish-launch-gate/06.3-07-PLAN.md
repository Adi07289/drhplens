---
phase: 06.3-agent-polish-launch-gate
plan: 07
type: execute
wave: 5
depends_on: ["06.3-04", "06.3-06"]
files_modified:
  - tests/eval/test_stress_suite.py
  - tests/eval/conftest.py
  - scripts/release_gate.py
  - Makefile
  - app/observability/trace_enrichment.py
  - tests/unit/test_release_gate_stress.py
autonomous: true
requirements: [D-09, D-07, D-08]
user_setup: []

must_haves:
  truths:
    - "The offline stress suite asserts the full deterministic envelope across the 4 D-07 categories + P8 termination + honest-partial, with the two LLM hops stubbed (D-09)"
    - "A new stress-gate lane in release_gate.py exits non-zero on any stress failure — it GATES deploy (D-09)"
    - "Supervisor-level failure modes are appended to FAILURE_MODES with stable numeric codes (monitoring)"
  artifacts:
    - path: "tests/eval/test_stress_suite.py"
      provides: "activated deterministic 4-category gate"
      contains: "MAX_SUPERVISOR_HOPS"
    - path: "scripts/release_gate.py"
      provides: "stress-gate lane (sys.exit(1) on breach + dated report)"
      contains: "stress"
    - path: "app/observability/trace_enrichment.py"
      provides: "extended FAILURE_MODES tuple (append-only)"
      contains: "budget_trip"
  key_links:
    - from: "scripts/release_gate.py"
      to: "tests/eval/test_stress_suite.py"
      via: "stress-gate lane runs the suite; non-zero blocks deploy"
      pattern: "test_stress_suite"
    - from: "Makefile"
      to: "scripts/release_gate.py"
      via: "gate-test / release targets run the stress lane"
      pattern: "gate-test"
---

## Phase Goal

**As an** Indian retail investor, **I want to** ask one plain-English question and get a single honestly-cited answer across the DRHP, peers, forecast, and GMP — never advice, always terminating — **so that** I can make an informed IPO decision instead of subscribing on hype.

> This plan turns the committed adversarial harness into an actual deploy gate: the stress suite (now that the supervisor + fusion exist) asserts the full deterministic guardrail envelope offline, and a new `release_gate.py` lane blocks any deploy that fails it (D-09). This is the launch-critical regression guard.

<objective>
Activate and finalize the D-09 deterministic stress gate: complete `tests/eval/test_stress_suite.py` so its assertions cover the full envelope across all four D-07 categories + P8 termination + honest-partial (LLM hops stubbed, offline), add a stress-gate lane to `scripts/release_gate.py` that `sys.exit(1)`s on any breach and writes a dated report, extend the `Makefile` `gate-test`/`release` targets, and append the new supervisor failure modes to `FAILURE_MODES` (stable codes).

Purpose: the deterministic envelope (scrubber-clean, disclaimer present, no leak, bounded hops, is_partial correct, single-IPO, no fabricated precision) is the enforceable proxy for the formal review that does not exist — and it must physically block a bad deploy.
Output: activated stress suite, stress-gate lane, Makefile targets, extended FAILURE_MODES, a release-gate unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-CONTEXT.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-RESEARCH.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-AI-SPEC.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-VALIDATION.md
@scripts/release_gate.py
@app/observability/trace_enrichment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate the deterministic stress suite (D-07/D-09)</name>
  <files>tests/eval/test_stress_suite.py, tests/eval/conftest.py</files>
  <read_first>
    - tests/eval/test_stress_suite.py (the Plan-04 scaffold + assertion helpers to finalize)
    - agent/supervisor.py + agent/nodes/synthesize.py (final state shape: hops/tool_calls/is_partial/grounded/refusal)
    - agent/policies.py (MAX_SUPERVISOR_HOPS/MAX_TOOL_CALLS) + compliance/scrubber.py (`scrub().passed`) + compliance/disclaimer_text.py (disclaimer substring)
    - 06.3-RESEARCH.md §"Pattern 5" (the per-category assertion set) + 06.3-AI-SPEC.md §5 Reference Dataset Composition
  </read_first>
  <action>
    Finalize `tests/eval/test_stress_suite.py` now that `agent.supervisor` exists (the `importorskip` guard becomes a no-op and the suite runs). For each committed fixture, patch `agent.nodes.classify._llm_classify` with `fake_routing(case)` and the synthesis LLM with `fake_fuse(case)`, invoke the supervisor, and assert the deterministic envelope keyed to `case["category"]`: the answer text is scrubber-clean (`scrub(...).passed`); the disclaimer substring is present on every rendered answer; no system-prompt token appears in the output (`_leaks_system_prompt` is False); `final["hops"] <= MAX_SUPERVISOR_HOPS + 1` and `final["tool_calls"] <= MAX_TOOL_CALLS` on every case (P8 termination); a `cross_ipo` case stays single-IPO (`_single_ipo`); an `expect_partial` case sets `is_partial True` with grounded content and no fabricated field; an `expect_refusal` case fired the educational refusal (reuse `REFUSAL_BANNED_TOKEN_COPY`, no new advice-adjacent string). Ensure the whole module is OFFLINE (no live LLM, no `--run-*` flag). The advice-by-implication check is NOT in this gated suite — it stays a separate REPORTED lane (Plan 10 HITL), same posture as `faithfulness_deepeval`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/eval/test_stress_suite.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The suite RUNS (not skips) and every fixture passes its category assertions.
    - `grep -c "MAX_SUPERVISOR_HOPS\|MAX_TOOL_CALLS" tests/eval/test_stress_suite.py` >= 1 (P8 termination asserted).
    - No live LLM call and no `--run-*` flag needed (offline, deterministic).
    - Advice-by-implication is absent from the gated assertions (kept REPORTED).
  </acceptance_criteria>
  <done>The four-category offline stress suite is green and asserts the full deterministic guardrail envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Stress-gate lane in release_gate.py + Makefile + FAILURE_MODES</name>
  <files>scripts/release_gate.py, Makefile, app/observability/trace_enrichment.py, tests/unit/test_release_gate_stress.py</files>
  <read_first>
    - scripts/release_gate.py (`enforce_gate` lines 105-145, `enforce_eval_gates` lines 237-299, `main` line 318 — the two-layer pure `enforce_*` + `main()` design, `sys.exit(1)` + dated report)
    - Makefile (`release` line 19, `gate-test` line 23 targets to extend)
    - app/observability/trace_enrichment.py (`FAILURE_MODES` tuple lines 54-62 — APPEND only, keep indices stable; `_failure_mode_value` lines 71-75)
    - 06.3-RESEARCH.md §"Pattern 5" (wire the stress gate as a new deterministic lane) + §"Extend FAILURE_MODES"
  </read_first>
  <action>
    Add a third deterministic lane to `scripts/release_gate.py` that runs the offline stress suite (invoke `pytest tests/eval/test_stress_suite.py` as a subprocess, or import + run the assertions) and `sys.exit(1)`s on ANY failure, writing a dated markdown report alongside the existing eval + numeric gate reports — mirror the two-layer pure-`enforce_*` + `main()` orchestration so the new lane stays offline-unit-testable. Extend the `Makefile` `gate-test`/`release` targets to run the stress lane in the same style (`make release` now runs the deterministic eval gate + numeric gate + the new stress gate). APPEND the new supervisor failure modes to the `FAILURE_MODES` tuple in `app/observability/trace_enrichment.py` — `"budget_trip"`, `"advice_bait_refused"`, `"jailbreak_blocked"`, `"honest_partial"`, `"tool_abstain"` — added at the END so existing numeric codes stay stable (saved Langfuse Cloud views remain filterable). Write `tests/unit/test_release_gate_stress.py`: the stress lane passes on a green stub and `sys.exit(1)`s on a red stub (mirror `test_release_gate.py`); `FAILURE_MODES` retains its original 8 entries at their original indices and the 5 new modes are appended.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_release_gate_stress.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `make release` runs three lanes (eval + numeric + stress); a forced stress failure exits non-zero (asserted via the unit test).
    - The first 8 `FAILURE_MODES` entries keep their original order/indices; `budget_trip`/`advice_bait_refused`/`jailbreak_blocked`/`honest_partial`/`tool_abstain` are appended (asserted).
    - The stress-gate lane writes a dated report like the existing gates.
    - `tests/unit/test_release_gate_stress.py` passes.
  </acceptance_criteria>
  <done>The stress suite gates deploy via release_gate.py and the new supervisor failure modes are traceable in Langfuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stress suite → deploy | The gate is the enforcement boundary between a passing build and a public deploy |
| trace mode codes → saved views | Appending (not reordering) FAILURE_MODES preserves the integrity of saved dashboards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.3-GATE | Repudiation | a bad build shipping past the gate | mitigate | Stress lane `sys.exit(1)`s on any breach; `make release` blocks deploy (D-09) |
| T-6.3-ADVICE | Repudiation (regulatory) | compliance regression | mitigate | Gated scrubber-clean + refusal-fired assertions across compliance_bait fixtures (D-07a) |
| T-6.3-INJECT | Info-disclosure | jailbreak regression | mitigate | Gated no-system-prompt-leak assertions across jailbreak fixtures (D-07b) |
| T-6.3-P8 | Denial of Service | unbounded-loop regression | mitigate | Gated hop/tool-call bound assertions (D-06) |
| T-6.3-SC | Tampering | Supply chain | accept | No new packages — reuses pytest + release_gate + Langfuse |
</threat_model>

<verification>
- `.venv/bin/pytest tests/eval/test_stress_suite.py tests/unit/test_release_gate_stress.py -q` green.
- `make gate-test` (or the documented offline gate command) exercises the stress lane; a deliberately-broken fixture makes it exit non-zero (spot check).
- Full offline suite has no new failures; existing release-gate + trace tests stay green.
</verification>

<success_criteria>
- D-09 stress gate is live and blocks deploy; D-07 categories + D-08 honest-partial are asserted deterministically; failure-mode tracing extended.
</success_criteria>

<output>
Create `.planning/phases/06.3-agent-polish-launch-gate/06.3-07-SUMMARY.md` when done.
</output>
