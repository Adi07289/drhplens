---
phase: 06.3-agent-polish-launch-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/policies.py
  - agent/supervisor_state.py
  - agent/schemas.py
  - tests/unit/test_supervisor_contracts.py
  - tests/unit/test_fused_schema.py
autonomous: true
requirements: [D-06, D-03]   # no new ROADMAP REQ IDs — Phase 6.3 is success-criterion driven; decisions are the coverage surface
user_setup: []

must_haves:
  truths:
    - "The five P8 bound constants exist in agent/policies.py as the single-source control surface (D-06)"
    - "SupervisorState is a superset of GraphState that never redefines an inherited key (Pitfall #1)"
    - "A fused answer can carry both DRHP-grounded Claims and tool-derived ToolClaims without fabricating DRHP-span fields (D-03)"
  artifacts:
    - path: "agent/policies.py"
      provides: "MAX_SUPERVISOR_HOPS, MAX_TOOL_CALLS, WALL_CLOCK_S, DEDUP_THRESHOLD, CACHE_TTL_S constants"
      contains: "MAX_SUPERVISOR_HOPS"
    - path: "agent/supervisor_state.py"
      provides: "SupervisorState(GraphState) superset TypedDict"
      contains: "class SupervisorState"
    - path: "agent/schemas.py"
      provides: "ToolClaim sibling model + FusedAnswer discriminated-union claims list"
      contains: "class ToolClaim"
  key_links:
    - from: "agent/supervisor_state.py"
      to: "agent/state.py"
      via: "SupervisorState subclasses GraphState"
      pattern: "SupervisorState\\(GraphState\\)"
    - from: "agent/schemas.py FusedAnswer"
      to: "agent/schemas.py Claim"
      via: "claims: list[Claim | ToolClaim] union"
      pattern: "list\\[Claim \\| ToolClaim\\]"
---

## Phase Goal

**As an** Indian retail investor, **I want to** ask one plain-English question and get a single honestly-cited answer that fuses what the DRHP says with how comparable IPOs behaved — delivered on a public URL that never gives advice, always terminates, and admits what it doesn't know — **so that** I can make an informed IPO decision instead of subscribing on hype.

> This plan lays the interface-first contracts the whole phase builds on (the bound constants, the supervisor state, the fused-answer schema). It is the "define contracts" task of the phase per the interface-first ordering — no orchestration logic yet.

<objective>
Establish the locked contracts for the bounded multi-tool supervisor: the P8 loop-bound constants (D-06), the `SupervisorState` superset (Pitfall #1), and the `ToolClaim`/`FusedAnswer` provenance schema (D-03, RESEARCH caveat e — a discriminated union, NOT a `FusedClaim(Claim)` subclass).

Purpose: every downstream plan (classify, tools, supervisor, synthesis, cite-check, UI) imports these; getting them right once prevents a scavenger hunt and preserves the cross-phase `Claim` invariants untouched.
Output: extended `agent/policies.py`, new `agent/supervisor_state.py`, extended `agent/schemas.py`, two unit test files — full offline suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-CONTEXT.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-RESEARCH.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-PATTERNS.md
@.planning/phases/06.3-agent-polish-launch-gate/06.3-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: P8 bound constants + SupervisorState superset</name>
  <files>agent/policies.py, agent/supervisor_state.py, tests/unit/test_supervisor_contracts.py</files>
  <read_first>
    - agent/policies.py (self-analog: GATE1_THRESHOLD lines 38-47 + MAX_REGENERATE_ATTEMPTS lines 53-58 — copy the "single value + calibration docstring" posture)
    - agent/state.py (GraphState TypedDict, lines 20-56 — the contract SupervisorState extends, NOT forks)
    - 06.3-RESEARCH.md §"Pattern 2" (route/bounds) + §"Open Questions Q1" (suggested numeric defaults 4/4/45s/0.95/6-24h)
    - 06.3-PATTERNS.md §"agent/policies.py" + §"agent/supervisor_state.py"
  </read_first>
  <action>
    In `agent/policies.py`, append five new constants in the file's established style (a single source-of-truth value + a calibration docstring mirroring GATE1_THRESHOLD), all implementing D-06: `MAX_SUPERVISOR_HOPS: int = 4`, `MAX_TOOL_CALLS: int = 4`, `WALL_CLOCK_S: float = 45.0`, `DEDUP_THRESHOLD: float = 0.95`, `CACHE_TTL_S: int = 21600`. Each docstring states the semantics are locked by D-06 while the numeric value is a calibration/discretion value tuned against the stress fixtures (per CONTEXT.md "numeric budget values"). Do NOT touch `GEMINI_MODELS` or `NUMERIC_GROUNDING_REL_TOLERANCE` — they are reused, not duplicated.
    Create `agent/supervisor_state.py` defining `SupervisorState(GraphState)` — a superset TypedDict that inherits every GraphState key unchanged and ADDS only the disjoint supervisor-only keys: `hops: int`, `tool_calls: int`, `tool_plan: list[str]`, `tool_results: list[dict]`, `started_at: float`, `is_partial: bool`. Reproduce the field-doc-block convention from state.py. Never redefine an inherited key (`question`, `drhp_id`, `grounded_answer`, `refusal`, `regenerate_attempts`, etc.) — the embedded DRHP-RAG subgraph reads/writes those with one fixed meaning (Pitfall #1).
    Write `tests/unit/test_supervisor_contracts.py` asserting: the five constants import and are the documented types/values; `SupervisorState.__annotations__` is a strict superset of `GraphState.__annotations__` (every GraphState key present with the SAME annotation) plus exactly the six new keys; no inherited key was redefined with a different type.
  </action>
  <behavior>
    - Test: importing MAX_SUPERVISOR_HOPS/MAX_TOOL_CALLS/WALL_CLOCK_S/DEDUP_THRESHOLD/CACHE_TTL_S succeeds and each is the documented type.
    - Test: set(SupervisorState.__annotations__) == set(GraphState.__annotations__) | {"hops","tool_calls","tool_plan","tool_results","started_at","is_partial"}.
    - Test: for every key in GraphState.__annotations__, SupervisorState.__annotations__[key] equals GraphState's annotation (no silent redefinition — Pitfall #1).
  </behavior>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_supervisor_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' agent/policies.py | grep -c MAX_SUPERVISOR_HOPS` >= 1 and the four sibling constants likewise present.
    - `python -c "from agent.supervisor_state import SupervisorState; from agent.state import GraphState; assert set(GraphState.__annotations__) <= set(SupervisorState.__annotations__)"` exits 0.
    - `tests/unit/test_supervisor_contracts.py` passes; full offline unit suite has no new failures.
  </acceptance_criteria>
  <done>The five D-06 constants and the SupervisorState superset exist, are tested, and preserve every inherited GraphState key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToolClaim sibling + FusedAnswer discriminated union (D-03)</name>
  <files>agent/schemas.py, tests/unit/test_fused_schema.py</files>
  <read_first>
    - agent/schemas.py (Claim lines 71-131 — claim_id pattern `^c_[a-z0-9]{6,16}$` line 84 + span_offsets validator; GroundedAnswer lines 144-185 incl. the claim_ids_unique_within_answer model_validator lines 173-185)
    - 06.3-RESEARCH.md §"Caveat (e)" (the discriminated-union resolution; ToolClaim fields; why subclassing forces fabricated DRHP-span fields)
    - 06.3-AI-SPEC.md §4b (FusedAnswer sketch lines 402-411 — REFINED by RESEARCH caveat e)
    - 06.3-PATTERNS.md §"agent/schemas.py"
  </read_first>
  <action>
    In `agent/schemas.py`, keep `Claim`, `RetrievedChunkRef`, `GroundedAnswer`, and `RefusalResponse` byte-identical — the `claim_id` regex and span validators are cross-phase invariants (do NOT touch, D-03 caveat e). ADD a sibling `ToolClaim(BaseModel)` — NOT a `Claim` subclass — for tool-derived numbers that have no DRHP chunk/page/span: fields `claim_id: str` reusing the EXACT SAME `pattern=r"^c_[a-z0-9]{6,16}$"` (so the C2 chip renderer resolves it), `text: str`, `value: float | str`, `source_tool: Literal["query_peers","query_forecast","query_redflags"]`, `source_record_id: str` (the `data/*.json` path + field the number traces to). ToolClaim has NO `drhp_page`, `verbatim_span`, `span_offsets`, or `sources` — inventing those would violate the honesty invariant. ADD `FusedAnswer(BaseModel)` with `answer_prose: str` (carries `{{claim_id}}` markers), `claims: list[Claim | ToolClaim]` (Pydantic tagged/duck union), `is_partial: bool = False`, `unaddressed: list[str] = Field(default_factory=list)`. Port the `claim_ids_unique_within_answer` `@model_validator` onto FusedAnswer so a fused answer cannot emit duplicate claim_ids across Claim + ToolClaim.
    Write `tests/unit/test_fused_schema.py` asserting: a ToolClaim rejects a malformed claim_id (regex preserved); a ToolClaim cannot be constructed with a `drhp_page`/`verbatim_span` (those fields do not exist); a FusedAnswer parses a mixed `[Claim, ToolClaim]` list; duplicate claim_ids across the union raise; the untouched `Claim` still enforces its span_offsets start<=end validator.
  </action>
  <behavior>
    - Test: ToolClaim(claim_id="BAD", ...) raises ValidationError (same regex as Claim).
    - Test: ToolClaim model_fields has no "drhp_page"/"verbatim_span"/"span_offsets"/"sources" (honesty: no fabricated DRHP fields).
    - Test: FusedAnswer with claims=[a valid Claim, a valid ToolClaim] parses; both render-addressable by claim_id.
    - Test: FusedAnswer with two claims sharing a claim_id raises (unique-within-answer validator ported).
    - Test: Claim's span_offsets [5,2] still raises (existing invariant untouched).
  </behavior>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_fused_schema.py -q && .venv/bin/pytest tests/unit -k "schema or claim" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^class ToolClaim\|^class FusedAnswer" agent/schemas.py` == 2.
    - `python -c "from agent.schemas import ToolClaim; assert 'drhp_page' not in ToolClaim.model_fields and 'verbatim_span' not in ToolClaim.model_fields"` exits 0.
    - The `Claim` class body is unchanged (claim_id pattern `^c_[a-z0-9]{6,16}$` still present; no diff to lines 71-131 semantics).
    - `tests/unit/test_fused_schema.py` passes; existing schema/claim tests stay green.
  </acceptance_criteria>
  <done>Fused answers carry provenance via a discriminated `Claim | ToolClaim` union that preserves every locked Claim invariant and fabricates no DRHP-span fields for tool numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → schema | Instructor-validated LLM output crosses into typed Pydantic (downstream); the schema is the integrity contract |
| cross-phase contract | `Claim`/`claim_id` regex is consumed by Phase 3 renderer; loosening it is a cross-phase tampering vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.3-01 | Tampering | ToolClaim provenance fabrication | mitigate | ToolClaim omits DRHP-span fields entirely; a tool number cannot masquerade as a DRHP-grounded Claim (D-03) |
| T-6.3-02 | Tampering | `claim_id` regex loosening | mitigate | `Claim` untouched; ToolClaim reuses the identical `^c_[a-z0-9]{6,16}$` pattern; tested |
| T-6.3-03 | Repudiation | Silent SupervisorState key redefinition clobbering subgraph state | mitigate | Superset-only test asserts no inherited-key redefinition (Pitfall #1) |
| T-6.3-SC | Tampering | Supply chain / new packages | accept | No new packages this plan (MemorySaver path; RESEARCH caveat a). No install task ⇒ no supply-chain checkpoint |
</threat_model>

<verification>
- Full offline suite: `.venv/bin/pytest -q --ignore=tests/integration/test_agent_e2e.py` has zero new failures vs baseline.
- `agent/schemas.py` diff shows only additive `ToolClaim`/`FusedAnswer` — no edits inside `Claim`/`GroundedAnswer`/`RetrievedChunkRef`/`RefusalResponse`.
</verification>

<success_criteria>
- D-06 bound constants + D-03 provenance schema + SupervisorState superset all exist, imported cleanly, and tested.
- Every locked cross-phase Claim invariant is provably preserved.
</success_criteria>

<output>
Create `.planning/phases/06.3-agent-polish-launch-gate/06.3-01-SUMMARY.md` when done.
</output>
