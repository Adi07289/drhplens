---
phase: 06.3-agent-polish-launch-gate
plan: 10
subsystem: compliance
tags: [sebi, self-audit, compliance, ci, github-actions, dependency-sync, ragas, launch-gate, hitl, honesty]

# Dependency graph
requires:
  - phase: 06.3-07
    provides: the D-09 deterministic stress gate (tests/eval/test_stress_suite.py) the self-audit cites as its enforceable proxy + the release_gate stress lane the CI workflow runs
  - phase: 06.3-09
    provides: the public-deploy guards + keep-warm ping.yml + the re-verified daily-cap constant the deploy HITL item builds on
  - phase: 06.1
    provides: the eval harness (release_gate.py, faithfulness -1 sentinel, the 4 disclosed follow-ups this plan closes/sequences)
provides:
  - "compliance/SEBI-REVIEW.md — an honest SELF-AUDIT (not a legal review) mapping the no-advice posture to enforcing code + verifying tests, re-auditing the new 6.3 surfaces, carrying the 3 §1b flags verbatim"
  - "WR-03 closed — requirements.txt ↔ pyproject core in sync; ragas moved to a [eval] optional-extra (D-14); scripts/_check_dep_sync.py + regression test"
  - ".github/workflows/eval-gate.yml — CI release-gate lane on PRs (release_gate + stress suite; secrets as ${{ secrets.* }} placeholders)"
  - "06.3-HUMAN-UAT.md + eval/reports/judge_calibration.md — the 5 blocking launch-gate HITL items recorded PENDING (not faked)"
affects: [public-launch, ops-02, sebi-compliance, ci, deploy]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Honest self-audit doc: posture-claim → enforcing-code → verifying-test table; references existing enforcement, adds no new gate (D-10)"
    - "Dependency-manifest sync guard: scripts/_check_dep_sync.py (packaging + tomllib) compares requirements.txt vs pyproject core, exit-non-zero on drift; optional-extras deliberately excluded from the runtime deploy set"
    - "CI release-gate lane mirroring nightly-nse.yml shape but on: pull_request; offline stress lane runs secret-free, live lane gated on ${{ secrets.* }}"
    - "HITL items drafted-where-draftable + recorded PENDING (UAT format) rather than hard-halting — end-of-phase human_verify_mode"

key-files:
  created:
    - compliance/SEBI-REVIEW.md
    - scripts/_check_dep_sync.py
    - tests/unit/test_dep_sync.py
    - .github/workflows/eval-gate.yml
    - eval/reports/judge_calibration.md
    - .planning/phases/06.3-agent-polish-launch-gate/06.3-HUMAN-UAT.md
  modified:
    - requirements.txt
    - pyproject.toml
    - .planning/phases/06.3-agent-polish-launch-gate/deferred-items.md

key-decisions:
  - "SEBI-REVIEW.md is an honest SELF-AUDIT (not a legal review); references existing enforcement + the D-09 stress gate; no new automated gate added"
  - "SEBI human-verify folded into end-of-phase pending item H-1 (resolves plan-checker Warning #1) rather than hard-halting"
  - "D-14 ragas decision: ragas → pyproject [eval] optional-extra (heavy on HF Spaces, offline cross-check only), absent from requirements.txt; deepeval kept in the runtime set"
  - "Honest -1 faithfulness surface preserved — no faithfulness number committed (T-6.3-THEATER / P10); calibration recorded pending (H-4)"

patterns-established:
  - "Posture→code→test audit table with an explicit 'argued, not enforced' row for structural-absence claims (no personalization) — honest about what a test can and cannot assert"
  - "Runtime-vs-eval dependency split enforced by a committed diff-checker + unit test"

requirements-completed: [D-10, D-11, D-13, D-14]

# Metrics
duration: ~90min
completed: 2026-08-13
---

# Phase 6.3 Plan 10: Launch Gate (SEBI self-audit + WR-03 + CI lane + HITL) Summary

**The launch gate's executor-completable half shipped — an honest SEBI SELF-AUDIT mapping the no-advice posture to enforcing code + tests, the WR-03 requirements↔pyproject sync (ragas moved to a `[eval]` extra), and a draft `eval-gate.yml` CI release-gate lane — with the 5 blocking human-in-the-loop items drafted-where-draftable and recorded PENDING, and the honest `-1` faithfulness surface preserved (no number faked).**

## Performance

- **Duration:** ~90 min
- **Started:** 2026-08-13T17:48:20Z
- **Completed:** 2026-08-13T19:19:15Z
- **Tasks:** 3 auto (complete + committed) + 3 human checkpoints (drafted + recorded PENDING, not hard-halted)
- **Files modified:** 9 (6 created, 3 modified)

## Accomplishments

- **compliance/SEBI-REVIEW.md** — honest SELF-AUDIT with a mandatory top-of-file label (self-audit, not a legal review / lawyer sign-off; nothing is legal advice), a posture→enforcing-code→verifying-test audit table (scrubber, disclaimer, GMP isolation, single-IPO scope, numeric faithfulness, bounded termination), a D-11 re-audit of the NEW 6.3 surfaces (fused answers + conversational forecast/peer/GMP + public live chat) naming **advice-by-implication** as the residual risk the scrubber cannot catch, the 3 unverified §1b regulatory flags carried forward verbatim, a "what this audit does NOT establish" section, and a prominent **AWAITING HUMAN SEBI-LITERATE SIGN-OFF** status + empty Sign-off Record.
- **WR-03 closed** — `requirements.txt` now matches pyproject `[project.dependencies]` exactly (13 missing runtime deps added: google-genai, fastembed, pyyaml, yfinance, requests-cache, jugaad-data, xgboost, mapie, scikit-learn, mlflow, matplotlib, shap, nse). The **D-14 ragas decision** is recorded inline: `ragas` moved to a pyproject `[eval]` optional-extra (heavy on HF Spaces, offline cross-check only, never imported in CI/release gate), deliberately absent from requirements.txt. `scripts/_check_dep_sync.py` (diff-checker, exit-non-zero-on-drift) + `tests/unit/test_dep_sync.py` (regression guard) enforce the sync — **37 runtime packages in sync**.
- **.github/workflows/eval-gate.yml** — draft CI release-gate lane, `on: pull_request`, mirroring `nightly-nse.yml`'s shape: runs the offline D-09 stress suite (secret-free) + `scripts/release_gate.py` (eval + stress offline + live numeric); a citation/numeric/stress regression physically blocks merge. Secrets referenced only via `${{ secrets.* }}` with a documented "user must add repo secrets" note.
- **5 blocking HITL items recorded PENDING** (not faked, not hard-halted) in `06.3-HUMAN-UAT.md` + `deferred-items.md`: H-1 SEBI human sign-off, H-2 CI repo secrets, H-3 public HF Spaces deploy (OPS-02), H-4 judge-vs-human calibration ≥50/≥0.7, H-5 gold-set span-tightening (human reads the Swiggy DRHP — no auto-substring re-run).
- **Honest `-1` faithfulness surface preserved** — `eval/reports/judge_calibration.md` is a PENDING record with no fabricated correlation/faithfulness number; the always-on `-1` sentinel test still passes.
- **Full offline suite green:** 799 passed / 16 skipped / 2 xfailed / 0 failed (+2 new dep-sync tests). All 16 skips are documented live-only lanes.

## Task Commits

Each auto task was committed atomically:

1. **Task 1: Draft compliance/SEBI-REVIEW.md self-audit (D-10/D-11)** — `0738774` (docs)
2. **Task 3: WR-03 requirements/pyproject sync + ragas [eval] extra (D-14)** — `0589e55` (chore)
3. **Task 4: Draft eval-gate.yml CI release-gate lane (D-14 code half)** — `8b3d5ef` (ci)
4. **HITL recording: H-1..H-5 pending (SEBI sign-off / CI secrets / deploy / calibration / gold-set)** — `b4bb89b` (docs)

**Plan metadata:** _(this commit)_ (docs: complete plan — SUMMARY + STATE + ROADMAP)

## Files Created/Modified

- `compliance/SEBI-REVIEW.md` (created) — honest SEBI compliance self-audit + posture-to-code-to-test table + D-11 re-audit + 3 §1b flags + pending sign-off record
- `scripts/_check_dep_sync.py` (created) — WR-03 dependency-sync diff-checker (requirements.txt vs pyproject core; exits non-zero on drift)
- `tests/unit/test_dep_sync.py` (created) — regression guard: manifests in sync + ragas in the [eval] extra
- `.github/workflows/eval-gate.yml` (created) — CI release-gate lane on PRs (release_gate + stress suite; ${{ secrets.* }} placeholders)
- `eval/reports/judge_calibration.md` (created) — PENDING judge-vs-human calibration record; honest -1 preserved, no number fabricated
- `.planning/phases/06.3-agent-polish-launch-gate/06.3-HUMAN-UAT.md` (created) — H-1..H-5 blocking HITL items in UAT format (description + expected + result: pending)
- `requirements.txt` (modified) — WR-03 sync: 13 runtime deps added, ragas removed (→ [eval] extra)
- `pyproject.toml` (modified) — ragas moved out of core into a documented `[eval]` optional-dependencies group (D-14)
- `.planning/phases/06.3-agent-polish-launch-gate/deferred-items.md` (modified) — extended with the launch-gate HITL block (extends, does not duplicate, the 6.1 follow-ups)

## Decisions Made

- **SEBI-REVIEW.md is an honest self-audit, not a legal review** — the honesty invariant is load-bearing at launch; the doc references existing enforcement (scrubber / disclaimer / GMP isolation / cite-check) + the D-09 stress gate as the enforceable proxy for the legal review that does not exist, and adds NO new automated gate (D-10).
- **The SEBI human-verify checkpoint (Task 2) is folded into end-of-phase pending item H-1** rather than hard-halting the run — matching `human_verify_mode: end-of-phase` and resolving plan-checker Warning #1. The draft carries a prominent AWAITING-HUMAN-SIGN-OFF status so its provisional standing is unmistakable.
- **D-14 ragas decision: `[eval]` optional-extra, not the runtime set** — ragas is heavy on HF Spaces and never imported by CI or the release gate, so it lives in a pyproject `[eval]` extra (install via `pip install -e ".[eval]"`) and is absent from requirements.txt, keeping the Space image lean. deepeval stays in the runtime set (the REPORTED faithfulness lane; the disclosed WR-03 intent).
- **Honest `-1` faithfulness surface preserved** — no faithfulness number was committed; the ≥50-example ≥0.7 judge calibration (H-4) and gold-set span-tightening (H-5) are recorded pending, and auto-substring matching is NOT re-run (refused on honesty grounds in 6.1). Committing an uncalibrated number would be evaluation theater (T-6.3-THEATER / P10 guard).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added a WR-03 regression test alongside the diff-checker**
- **Found during:** Task 3 (WR-03 sync)
- **Issue:** The plan required either the `_check_dep_sync.py` checker OR a unit test asserting equivalence; the checker alone is not wired into the suite, so future drift would only be caught by a manual run.
- **Fix:** Added `tests/unit/test_dep_sync.py` (2 tests) that runs the checker in the offline suite and pins the ragas-in-[eval]-extra decision, so drift reds CI automatically.
- **Files modified:** tests/unit/test_dep_sync.py
- **Verification:** `pytest tests/unit/test_dep_sync.py -q` → 2 passed; full suite green.
- **Committed in:** `0589e55` (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (1 missing-critical / regression-guard). No scope creep — the plan's acceptance criteria explicitly allowed the checker-or-test choice; both were provided for a durable guard.

## Issues Encountered

- **The four `tests/eval/test_stress_suite.py` cases that route into the live DRHP-RAG subgraph skip offline** (documented since 06.3-06: they need live Qdrant + reranker + Gemini). This is expected and honest — the offline stress suite stubs only classify + synthesize. It does not affect the SEBI-REVIEW.md claim (the deterministic guardrail-envelope cases all run offline and gate deploy).

## User Setup Required

**External services require manual configuration — the launch gate's human half.** See [`06.3-HUMAN-UAT.md`](./06.3-HUMAN-UAT.md) for the full UAT-format detail. Blocking items:

- **H-1** — SEBI-compliance-literate reviewer signs off `compliance/SEBI-REVIEW.md` (fills Section 7 record; closes/leaves-open the 3 §1b flags).
- **H-2** — Add repo Actions secrets (`GEMINI_API_KEY`, `QDRANT_URL`, `QDRANT_API_KEY`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST`) so `eval-gate.yml`'s live lane runs; confirm a forced regression blocks merge.
- **H-3** — Public HF Spaces deploy (OPS-02): create Space, set Space Secrets, seed Qdrant Cloud, deploy Streamlit, replace `<user>` in `ping.yml`; verify mobile fused answer + C4 cap fallback + read-only surfaces; re-verify the deploy daily-cap live.
- **H-4** — Judge-vs-human calibration (≥50 labels, ≥0.7); record in `eval/reports/judge_calibration.md`. Faithfulness stays `-1` unless correlation ≥0.7.
- **H-5** — Gold-set span-tightening: a human reads the Swiggy DRHP PDF to set real `expected_sources` pages (no auto-substring re-run).

## Next Phase Readiness

- The launch gate's **code half is complete**: the SEBI self-audit is drafted, WR-03 is closed, and the CI release-gate lane exists. **Public launch is blocked** until H-1..H-5 resolve — this is the honest, intended state (nothing ships publicly until the SEBI sign-off + the deterministic stress gate + the deploy verification pass).
- No code blockers introduced; the full offline suite is green (799 passed). The remaining work is entirely human-in-the-loop and is enumerated with expected outcomes in `06.3-HUMAN-UAT.md`.

## Self-Check: PASSED

- Created files verified present: `compliance/SEBI-REVIEW.md`, `scripts/_check_dep_sync.py`, `tests/unit/test_dep_sync.py`, `.github/workflows/eval-gate.yml`, `eval/reports/judge_calibration.md`, `06.3-HUMAN-UAT.md` (+ modified `requirements.txt`, `pyproject.toml`, `deferred-items.md`).
- Commits verified in git log: `0738774` (SEBI self-audit), `0589e55` (WR-03 sync), `8b3d5ef` (eval-gate.yml), `b4bb89b` (HITL pending records).
- Task verifies green: SEBI content assertions OK; `_check_dep_sync.py` exit 0 (37 packages in sync); `eval-gate.yml` valid YAML + contains release_gate + test_stress_suite.
- Full offline suite: 799 passed / 16 skipped / 2 xfailed / 0 failed. Honest `-1` faithfulness surface preserved.

---
*Phase: 06.3-agent-polish-launch-gate*
*Completed: 2026-08-13*
