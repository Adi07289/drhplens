# Phase 6.3: Agent Polish + Launch Gate - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-04
**Phase:** 6.3-agent-polish-launch-gate
**Areas discussed:** Agent tool surface + shape, P8 loop-safety + weird queries, SEBI legal-review gate, Public launch shape (OPS-02)

---

## Agent tool surface + shape

### Tool surface
| Option | Description | Selected |
|--------|-------------|----------|
| Full hybrid — all four | RAG + redflags/financials + peers + forecast (GMP read-only) | ✓ |
| Core three (RAG + forecast + peers) | Skip dedicated red-flag tool | |
| RAG + forecaster only | Minimal multi-tool | |

**User's choice:** Full hybrid — all four.
**Notes:** Data + `agent/*_schema.py` schemas already committed; the cost is wiring + stress-test, not new pipelines.

### Graph shape
| Option | Description | Selected |
|--------|-------------|----------|
| Wrap (supervisor above) | Existing 10-node graph = DRHP-RAG sub-agent, unchanged | ✓ |
| Replace (unified tool-calling) | Flat graph, retrieval is one tool | |
| Side-by-side | Supervisor only for multi-tool queries | |

**User's choice:** Wrap (supervisor above).
**Notes:** Preserves cite-check/scrub/gate invariants + the full passing test suite.

### Synthesis
| Option | Description | Selected |
|--------|-------------|----------|
| Fuse into one cited answer | Multi-tool → one answer, per-number provenance + scrub + cite-check | ✓ |
| Single best tool per query | Route to one dominant tool | |
| Fuse, but tools stay non-narrative | Structured widgets appended, no LLM paraphrase | |

**User's choice:** Fuse into one cited answer.
**Notes:** This is the product core value ("fuse what the document says with how comparable IPOs behaved").

### GMP in chat
| Option | Description | Selected |
|--------|-------------|----------|
| Surface the gap signal | GMP-vs-model gap in chat, same caveat, isolated | ✓ |
| GMP only if asked | Mention only on explicit request | |
| No GMP in chat | GMP stays on snapshot page only | |

**User's choice:** Surface the gap signal.
**Notes:** GMP stays read-only, never a model feature (GMP-01/02, P4).

### Routing
| Option | Description | Selected |
|--------|-------------|----------|
| LLM classify + deterministic guardrails | Bounded classify hop + allow-list + budget clamps | ✓ |
| Deterministic intent rules | Keyword/pattern, no LLM | |
| LLM classifier node | Gemini classify, softer bound | |

**User's choice:** LLM classify + deterministic guardrails.
**Notes:** Honors "bounded state machine, NOT freeform ReAct".

---

## P8 loop-safety + weird queries

### TTL + dedup intent
| Option | Description | Selected |
|--------|-------------|----------|
| Belt-and-suspenders (both) | Hard hop/wall-clock bound + semantic result cache w/ TTL spanning runs | ✓ |
| Two lightweight mechanisms | Hop/wall-clock bound + within-run-only dedup | |
| One unified semantic cache w/ TTL | Cache hit IS the loop bound | |

**User's choice:** Belt-and-suspenders (both).

### Weird-query categories (multiSelect)
| Option | Description | Selected |
|--------|-------------|----------|
| Compliance-bait (advice-seeking) | "Should I buy?" → no-advice posture | ✓ |
| Prompt-injection / jailbreak | Reveal-prompt / drop-disclaimer → posture holds | ✓ |
| Off-topic / gibberish / degenerate | Graceful out-of-scope, no thrash/crash | ✓ |
| Cross-IPO + fabricated-precision bait | Single-IPO redirect + honest "not disclosed" | ✓ |

**User's choice:** All four.

### On failure
| Option | Description | Selected |
|--------|-------------|----------|
| Isolate tool fails; budget-trip refuses | Hybrid | |
| Honest partial always | Grounded+cited-so-far labeled incomplete, refuse only if nothing grounded | ✓ |
| Refuse cleanly on both | No partials | |

**User's choice:** Honest partial always.
**Notes:** Max user value + transparency; applies even to the budget-trip path. Never fabricate the missing part.

### Stress-test format
| Option | Description | Selected |
|--------|-------------|----------|
| Committed pytest suite, gates deploy | Deterministic assertions per category, offline, regression guard | ✓ |
| Pytest suite + short red-team writeup | Suite + narrative markdown | |
| Manual red-team writeup only | No automated guard | |

**User's choice:** Committed pytest suite, gates deploy.
**Notes:** Pairs with the CI gate lane.

---

## SEBI legal-review gate

### Gate form
| Option | Description | Selected |
|--------|-------------|----------|
| Self-audit doc + enforcement test | Doc + automated compliance gate | |
| Real professional review | Actual advocate sign-off | |
| Self-audit doc only | Committed, honestly-labelled, human-reviewed, no new gate | ✓ |

**User's choice:** Self-audit doc only.
**Notes:** Honestly labelled a self-audit, NOT lawyer-reviewed. References existing scrubber assertion + stress-test suite rather than adding a new enforcement gate.

### Audit scope
| Option | Description | Selected |
|--------|-------------|----------|
| Full re-audit incl. new 6.3 surfaces | Phase-1 posture + new conversational surfaces + public exposure | ✓ |
| Re-attest + point to stress-test | Lean, leans on the suite | |
| Existing posture only | Inheritance-covered | |

**User's choice:** Full re-audit incl. new 6.3 surfaces.

---

## Public launch shape (OPS-02)

### Public chat
| Option | Description | Selected |
|--------|-------------|----------|
| Live chat + guards + fallback | Rate-limit + daily cap + graceful quota-exhausted fallback | ✓ |
| Read-only demo + recorded chat | No live chat | |
| Gated live chat | Behind a light gate | |

**User's choice:** Live chat + guards + fallback.

### Frontend
| Option | Description | Selected |
|--------|-------------|----------|
| Stay Streamlit for v1 launch | HF Spaces; Next.js → v2 | ✓ |
| Migrate to Next.js now | FastAPI + Next.js 15 | |

**User's choice:** Stay Streamlit for v1 launch.
**Notes:** Resolves the flagged Phase-5-exit re-evaluation gate; no UI rewrite in 6.3.

### Production-stamp follow-ups (multiSelect — which to close now)
| Option | Description | Selected |
|--------|-------------|----------|
| WR-03: requirements↔pyproject sync | Quick, deploy-relevant | ✓ |
| CI gate lane (GitHub Action) | Needs repo secrets | ✓ |
| Judge calibration (≥50-ex, ≥0.7) | Needs human labels + quota | ✓ |
| Gold-set span-tightening (≤2-3pp) | Needs human reading the DRHP PDF | ✓ |

**User's choice:** All four — aim for the full production-ready stamp.
**Notes:** ⚠ Judge calibration + gold-set span-tightening are NOT executor-completable — they need the user's manual labelling / PDF reading; CI lane needs user secrets. Only WR-03 is fully code. Planner must sequence these as HITL checkpoints.

---

## Claude's Discretion

- Infra topology (HF Spaces + Qdrant Cloud free + Langfuse Cloud free + Gemini), carried from the locked STACK.
- P19 warm-keep mechanics (pre-index, cache, cron pinger, offline demo video).
- Numeric budget values (hop count, tool-call cap, wall-clock ceiling, dedup similarity threshold, cache TTL) — calibration in `agent/policies.py`.
- Supervisor node internals, tool-node file layout, synthesis prompt phrasing, stress-test file structure.
- Recorded-walkthrough / example-transcript form for the quota-exhausted fallback.

## Deferred Ideas

- Cross-IPO / multi-IPO compare in the agent → deferred idea E3 (TODOS.md); tools stay single-IPO-scoped.
- Next.js 15 + FastAPI frontend → v2.
- Committed app screenshots → considered, left out (live URL supersedes; revisit only if the link proves unreliable).
- `ragas` dependency bloat on HF Spaces → fold into WR-03 dependency-sync as an optional-extras decision.
