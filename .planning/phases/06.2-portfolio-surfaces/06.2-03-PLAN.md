---
phase: 06.2-portfolio-surfaces
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_eval_dashboard.py
  - scripts/build_eval_dashboard.py
  - eval/dashboards/eval-dashboard.html
autonomous: true
requirements: [OPS-03a]
must_haves:
  truths:
    - "A recruiter can open eval/dashboards/eval-dashboard.html standalone (file://) and read the committed eval numbers off-app"
    - "The dashboard is self-contained â zero external http(s):// asset URLs (inline <style>, base64-inlined calibration.png)"
    - "Faithfulness renders verbatim as 'not measured' â never -1.0 or a fabricated 0.00"
    - "The dashboard is sectioned by surface â RAG / Extraction / Forecast â with the honest Swiggy-only per-IPO caveat inline (D-03)"
  artifacts:
    - path: "scripts/build_eval_dashboard.py"
      provides: "deterministic generator: reads committed JSON + PNG â writes one self-contained HTML"
    - path: "eval/dashboards/eval-dashboard.html"
      provides: "committed self-contained dashboard artifact"
      contains: "data:image/png;base64,"
    - path: "tests/unit/test_eval_dashboard.py"
      provides: "build + numeric-parity + self-contained tests"
  key_links:
    - from: "scripts/build_eval_dashboard.py"
      to: "eval/reports/eval_summary.json + model_card/card_data.json + model_card/calibration.png"
      via: "json.load + base64 inline"
      pattern: "eval_summary.json"
---

<objective>
Ship OPS-03a (part of roadmap OPS-03): a deterministic generator `scripts/build_eval_dashboard.py`
that reads the committed eval artifacts and writes ONE self-contained
`eval/dashboards/eval-dashboard.html` â sectioned by surface (RAG / Extraction / Forecast, D-03),
faithfulness verbatim "not measured", base64-inlined calibration plot, zero external asset URLs â
and commit the generated HTML. Pin it with a build + numeric-parity + self-contained test so the
committed HTML can never silently drift from the committed metrics.

Purpose: A recruiter reading the repo on GitHub can see the honest eval numbers off-app, opening
the file standalone, with the model's baseline loss and the "not measured" faithfulness shown
truthfully.
Output: `scripts/build_eval_dashboard.py`, committed `eval/dashboards/eval-dashboard.html`,
`tests/unit/test_eval_dashboard.py`.
</objective>

## Phase Goal (MVP user story)

**As a** hiring manager / DS recruiter, **I want to** open a committed, self-contained HTML eval
dashboard straight from the repo, **so that** I can read the system's real eval numbers and trust
them without running anything. (Derived from the ROADMAP 6.2 goal + the SPEC goal line.)

Vertical slice: failing parity/self-contained test â generator that produces the committed HTML â
green, so the standalone dashboard surface exists end-to-end when the plan completes.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-portfolio-surfaces/06.2-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-UI-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-RESEARCH.md
@.planning/phases/06.2-portfolio-surfaces/06.2-PATTERNS.md
@eval/reports/eval_summary.json
@model_card/card_data.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the dashboard build + parity + self-contained test (RED)</name>
  <files>tests/unit/test_eval_dashboard.py</files>
  <read_first>
    - RESEARCH Â§"Code Examples" â "Dashboard self-contained + parity assertions" (the exact assertion set) and Â§"Real Failure Sourcing"/Pattern 3 (data sources + Extraction F1 hardcode-with-ref).
    - PATTERNS Â§"tests/unit/test_eval_dashboard.py" (parity keyed to committed JSON; assert each headline number INDEPENDENTLY; self-contained grep on the whole file) and Â§"RESEARCH Verification Notes" (confirmed numbers: faithfulness -1.0, citation 1.0, recall@{5,10,30} 1.0; coverage 0.8004, mae_pts 0.43, n_scored 1132, gate_passed false, r2 -0.0095; extraction Macro F1 0.000).
    - `eval/reports/eval_summary.json` + `model_card/card_data.json` â the pinned source-of-truth values to re-derive parity from.
    - `eval/reports/2026-06-25-extraction-f1.md` â the extraction F1 (0.000) ref for the hardcoded literal.
  </read_first>
  <action>
    Write `tests/unit/test_eval_dashboard.py`. In a single test that runs the generator via
    `subprocess.run([sys.executable, "scripts/build_eval_dashboard.py"], check=True)` then reads
    `eval/dashboards/eval-dashboard.html`, assert (each INDEPENDENTLY, so one drifted figure cannot
    hide behind another):
    (1) self-contained â `not re.search(r'(src|href)\s*=\s*["\']https?://', html)` AND no
        `url(https?://` and no `@import` with an `http` URL (whole-file grep).
    (2) RAG parity â `"1.000"` (or `"1.00"`) present for citation/recall; re-derive from
        `eval_summary.json` and assert the rendered values match the committed aggregate.
    (3) faithfulness â `"not measured"` present verbatim; `"-1.0"` NOT present; the string `0.00`
        does not appear as a fabricated faithfulness (allow `0.000` extraction F1).
    (4) Forecast parity â coverage from `card_data.json` present (`"0.80"` for 0.8004); the honest
        `gate_passed=false` framing present (does-not-beat-baseline); `data:image/png;base64,`
        present (calibration.png inlined).
    (5) Extraction â the hardcoded `F1 = 0.000` literal present with an inline ref to
        `eval/reports/2026-06-25-extraction-f1.md`.
    (6) Sectioned-by-surface (D-03) â the three section labels RAG / Extraction / Forecast present;
        the Swiggy-only per-IPO caveat present inline.
    (7) Honesty-color — no red/green verdict color: assert the HTML has no `color: red`/`color: green`
        and no red/green hex (`#e5484d`/`#d33`/`#2ecc71`/`#3fb950`) in the inline `<style>`/markup
        (mono figures only, per the honesty invariant / UI-SPEC C3).
    This FAILS now (no generator/output) â the intended RED for Task 2.
  </action>
  <acceptance_criteria>
    - `tests/unit/test_eval_dashboard.py` exists and encodes each assertion (self-contained, RAG parity, faithfulness sentinel, forecast coverage + base64, extraction F1 literal, three surface sections, Swiggy caveat, no red/green verdict color) as an INDEPENDENT assert.
    - Running it now FAILS at the `subprocess.run(... build_eval_dashboard.py ...)` step (generator absent) â expected RED.
    - The test re-derives parity numbers from `eval/reports/eval_summary.json` and `model_card/card_data.json` (not hardcoded copies of them), so a metric change forces a regen.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_eval_dashboard.py -x -q || echo "RED-as-expected: test collects but fails at the subprocess build step (generator absent until Task 2)"</automated>
  </verify>
  <done>The parity/self-contained/build test exists, samples the whole HTML file, asserts each headline number independently, and is RED pending the generator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build scripts/build_eval_dashboard.py, generate + commit eval-dashboard.html (GREEN)</name>
  <files>scripts/build_eval_dashboard.py, eval/dashboards/eval-dashboard.html</files>
  <read_first>
    - `scripts/write_panel_sanity.py:14-41` â the generator analog (`_REPO = Path(__file__).resolve().parents[1]`; `main()` + `if __name__ == "__main__"`; `_OUT.write_text(..., encoding="utf-8")`; deterministic, no network).
    - `ui/eval_inline.py:43-63` (`_eval_num`, `_faithfulness_display`) â the -1.0â"not measured" sentinel + em-dash rule to RE-IMPLEMENT inline (do NOT import ui.eval_inline â it pulls streamlit; the generator stays dependency-light).
    - RESEARCH Â§Pitfall 3 (Google-Fonts `@import` at `drhplens.css:12` â do NOT copy the app CSS; author fresh inline `<style>` with font FALLBACKS only) and Â§Pattern 3 (data sources).
    - `06.2-UI-SPEC.md` Â§C3 (dashboard mirrors the `:root` tokens inline; title "DRHPLens â Evaluation Dashboard"; mono figures, no red/green; faithfulness verbatim) and Â§Copywriting Contract.
    - `06.2-CONTEXT.md` D-03 (sectioned RAG / Extraction / Forecast + Swiggy-only caveat inline).
    - `model_card/calibration.png` â the 43 KB plot to base64-inline under Forecast.
  </read_first>
  <action>
    Create `scripts/build_eval_dashboard.py` mirroring `scripts/write_panel_sanity.py`: `_REPO =
    Path(__file__).resolve().parents[1]`; `_OUT = _REPO / "eval" / "dashboards" /
    "eval-dashboard.html"`; a `main()` that reads committed JSON and writes one HTML file, guarded by
    `if __name__ == "__main__": main()`. Read `eval/reports/eval_summary.json` (RAG aggregate:
    citation, recall@{5,10,30}, faithfulness) and `model_card/card_data.json` (forecast: coverage,
    mae_pts, n_scored, gate_passed, r2). Base64-encode `model_card/calibration.png` via `base64` into
    a `data:image/png;base64,â¦` `<img>`. Hardcode Extraction `F1 = 0.000` with an inline citation to
    `eval/reports/2026-06-25-extraction-f1.md` (no machine-readable extraction JSON exists â
    RESEARCH Open-Q#2).

    Render "not measured" for `faithfulness_deepeval == -1.0` using an INLINE re-implementation of the
    `_faithfulness_display` rule (NEVER `-1.0`/`0.00`); em-dash for missing; verbatim 2-3 sig-fig
    figures otherwise. Author a FRESH inline `<style>` mirroring the `:root --drhp-*` token VALUES
    (Graphite+Amber, mono figures, no red/green) with `font-family` FALLBACKS only
    (`'IBM Plex Mono', ui-monospace, monospace` etc.) â NO `@import`, NO `http(s)://` anywhere.
    html.escape any interpolated text. Structure the body sectioned-by-surface (D-03): a **RAG**
    section (recall@k, citation, faithfulness "not measured"), an **Extraction** section (F1 0.000 +
    ref), and a **Forecast** section (conformal coverage + the honest gate-fail framing + the inlined
    calibration plot), with the honest Swiggy-only per-IPO caveat rendered inline. Title
    "DRHPLens â Evaluation Dashboard". Include the live-demo placeholder copy.

    Run the generator (`.venv/bin/python scripts/build_eval_dashboard.py`) and COMMIT the produced
    `eval/dashboards/eval-dashboard.html`. It must reproduce byte-identically over the same committed
    artifacts (deterministic). No app screenshots (D-04) — the dashboard stays text/metrics + the committed calibration plot only, no headless UI capture. Document the regen path (`python scripts/build_eval_dashboard.py`) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_eval_dashboard.py -x` PASSES (build + parity + self-contained + sections + Swiggy caveat).
    - Self-contained proof: `grep -Ec 'https?://' eval/dashboards/eval-dashboard.html` == 0 and `grep -c '@import' eval/dashboards/eval-dashboard.html` == 0.
    - `grep -c 'data:image/png;base64,' eval/dashboards/eval-dashboard.html` >= 1 (calibration.png inlined); `grep -c 'not measured' eval/dashboards/eval-dashboard.html` >= 1; `grep -c '\-1.0' eval/dashboards/eval-dashboard.html` == 0.
    - Honesty-color: `grep -Eic 'color:[[:space:]]*(red|green)|#e5484d|#d33682|#2ecc71|#3fb950' eval/dashboards/eval-dashboard.html` == 0 (mono figures, no verdict color).
    - Generator does NOT import streamlit/agent/model: `grep -Ec 'import streamlit|ui.eval_inline|langgraph|qdrant|import xgboost' scripts/build_eval_dashboard.py` == 0.
    - Determinism: running the generator twice yields a byte-identical file (`build; sha1=$(shasum eval/dashboards/eval-dashboard.html); build; shasum -c <<<"$sha1"`).
    - `eval/dashboards/eval-dashboard.html` is committed.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python scripts/build_eval_dashboard.py && .venv/bin/python -m pytest tests/unit/test_eval_dashboard.py -x -q</automated>
  </verify>
  <done>A deterministic generator writes a committed, self-contained, sectioned-by-surface dashboard containing the committed eval numbers verbatim (faithfulness "not measured", base64 calibration plot, zero external URLs), and its test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON/PNG â generator | read-only committed artifacts under repo root |
| generator â committed HTML â recruiter browser (file://) | static artifact opened standalone by an unauthenticated reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-09 | Info-Disclosure (self-containment) | external CDN/font/image URL in committed HTML | mitigate | fresh inline `<style>` (no `@import`), base64-inline PNG; self-contained test greps zero `http(s)://`/`@import` whole-file |
| T-06.2-10 | Tampering (metric drift) | committed HTML diverging from committed metrics | mitigate | parity test re-derives each headline number from `eval_summary.json`/`card_data.json` independently; deterministic regen |
| T-06.2-11 | Info-Disclosure (honesty) | fabricating a faithfulness number or spinning gate-fail | mitigate | faithfulness verbatim "not measured" (never -1.0/0.00); forecast section states does-not-beat-baseline honestly |
| T-06.2-12 | Tampering/Elevation (V12) | file reads/writes | mitigate | `pathlib` reads of committed repo paths under root; writes one known path `eval/dashboards/eval-dashboard.html` |
| T-06.2-13 | Isolation (P19) | generator importing agent/LLM/model pipeline | mitigate | generator uses only stdlib json/base64/pathlib; sentinel logic inlined, not imported from streamlit module |
</threat_model>

<verification>
- Per-task: run the task's `<automated>` command (feedback < 60s).
- Wave merge: `.venv/bin/python -m pytest tests/unit -q`.
- Self-contained: zero `http(s)://`/`@import` in the committed HTML; PNG base64-inlined.
- Honesty invariant: faithfulness "not measured" verbatim; forecast gate-fail framed honestly; per-IPO Swiggy-only caveat inline.
- Manual (retroactive, non-blocking): open `eval/dashboards/eval-dashboard.html` in a browser standalone; `/gsd-ui-review` against UI-SPEC Â§C3.
</verification>

<success_criteria>
- `eval/dashboards/eval-dashboard.html` is committed, self-contained (zero external URLs), sectioned RAG/Extraction/Forecast, contains the committed `eval_summary.json`+`card_data.json` numbers, faithfulness "not measured", calibration.png base64-inlined.
- The parity/self-contained/build test is green; the generator is deterministic and pipeline-free.
</success_criteria>

<output>
Create `.planning/phases/06.2-portfolio-surfaces/06.2-03-SUMMARY.md` when done.
</output>
