---
phase: 6.2
slug: portfolio-surfaces
status: verified
threats_open: 0
asvs_level: 1
created: 2026-08-02
---

# Phase 6.2 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Register authored at plan time (all 4 PLAN.md files carried `<threat_model>` blocks); every
> threat is a `mitigate` disposition with a **test-enforced** control. Verified via the short-circuit
> path (register_authored_at_plan_time = true, threats_open = 0), corroborated by the independent
> code review (`06.2-REVIEW.md`: no security issues in the render-only paths, 34 security tests green).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| committed data files → renderer/generator | `failures.yaml` / `eval_summary.json` / `card_data.json` / `calibration.png` read into HTML | author-controlled, non-sensitive (public portfolio data) |
| renderer → recruiter browser | `st.markdown(unsafe_allow_html=True)` output to an unauthenticated public reader | HTML (render-time, no live service call — P19) |
| committed dashboard HTML → browser (`file://`) | standalone self-contained artifact opened off-app | static HTML, zero external assets |
| README → GitHub renderer | markdown + Mermaid rendered for an unauthenticated public reader | text (repo-relative links only) |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation (evidence) | Status |
|-----------|----------|-----------|-------------|------------------------|--------|
| T-06.2-01 | Tampering/Elevation (V5) | `ui/failures.py` interpolation → `unsafe_allow_html` | mitigate | `html.escape(str(...))` on every field (11×); `test_render_neutral_no_live` asserts no `style=`/raw-tag/color injection | closed |
| T-06.2-02 | Tampering/Elevation (V12) | YAML parse of `failures.yaml` | mitigate | `yaml.safe_load` only (`grep`==1, no bare `yaml.load(`); pinned by the isolation test | closed |
| T-06.2-03 | Info-Disclosure/Elevation (P19) | render path importing agent/LLM/vector/model | mitigate | `test_read_only_isolation_allowlist` (AST allowlist {`__future__`,`html`,`json`,`pathlib`,`yaml`,`streamlit`} + forbidden-token scan) — green | closed |
| T-06.2-04 | Compliance (banned-token leak) | new chrome strings in `ui/copy.py` | mitigate | import-time scrubber `test_copy_no_banned_tokens.py` — green; evaluative failure copy kept in the un-scrubbed YAML by design | closed |
| T-06.2-SC | Tampering (supply chain) | `pyyaml` declaration | mitigate | No new install — PyYAML already resolved 6.0.3 (canonical, APPROVED in RESEARCH §Package Legitimacy Audit); `pyyaml>=6,<7` declared only | closed |
| T-06.2-05 | Tampering/Elevation (V5) | C1 hero + per-IPO interpolation | mitigate | hero markup is static (no external interpolation); the one dynamic value (`_ipo_note`) is `html.escape`-d; render test green | closed |
| T-06.2-06 | Info-Disclosure (honesty regression) | residual "Docling" on the honesty page | mitigate | whole-page `grep -c Docling pages/01_methodology.py`==0, enforced by `test_land01_c1_hero_names_the_real_pipeline_and_no_docling` | closed |
| T-06.2-07 | Info-Disclosure (fabricated data) | per-IPO summary printing a non-Swiggy figure | mitigate | Swiggy-only guard; `test_land01_per_ipo_framing_is_swiggy_only` asserts no other catalogue id appears | closed |
| T-06.2-08 | Info-Disclosure/Elevation (P19) | `/methodology` render path importing a model module | mitigate | `test_page_is_render_only_imports_no_model_module` — green (no new imports added by 6.2) | closed |
| T-06.2-09 | Info-Disclosure (self-containment) | external CDN/font/image URL in committed dashboard | mitigate | fresh inline `<style>` (no `@import`), base64-inlined PNG; `grep -Ec 'https?://'`==0, `@import`==0; self-contained test green | closed |
| T-06.2-10 | Tampering (metric drift) | committed HTML diverging from committed metrics | mitigate | parity test re-derives each headline number independently from `eval_summary.json`/`card_data.json`; deterministic byte-identical regen | closed |
| T-06.2-11 | Info-Disclosure (honesty) | fabricating a faithfulness number / spinning gate-fail | mitigate | faithfulness verbatim "not measured" (`grep '-1.0'`==0); forecast section states does-not-beat-baseline honestly | closed |
| T-06.2-12 | Tampering/Elevation (V12) | generator file reads/writes | mitigate | `pathlib` reads of committed repo paths under root; writes one known path `eval/dashboards/eval-dashboard.html` | closed |
| T-06.2-13 | Isolation (P19) | generator importing agent/LLM/model pipeline | mitigate | stdlib-only (json/base64/html/pathlib); `grep -Ec 'import streamlit\|langgraph\|qdrant\|import xgboost\|ui.eval_inline'`==0 | closed |
| T-06.2-14 | Info-Disclosure (honesty) | README naming Docling or fabricating a metric | mitigate | `grep -c Docling README.md`==0 (test-enforced); numbers traced to committed `eval_summary.json`/`card_data.json`; forecaster loss stated honestly | closed |
| T-06.2-15 | Tampering/Elevation (V12) | broken or non-repo-relative README links | mitigate | `test_every_repo_relative_file_link_resolves` asserts each referenced committed file exists (`is_file()` + non-empty), repo-relative under root | closed |
| T-06.2-16 | Info-Disclosure (self-contained) | external image/asset URL in README | mitigate | Mermaid is text (zero asset); `test_no_external_image_assets` asserts no `![...](http...)` | closed |
| T-06.2-17 | Availability (deploy) | dropping the HF Spaces front-matter | mitigate | front-matter preserved byte-verbatim; `test_hf_spaces_front_matter_preserved` asserts `title`/`sdk`/`app_file` present | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|

No accepted risks — every plan-time threat is mitigated and test-enforced.

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-08-02 | 18 | 18 | 0 | secure-phase short-circuit (register authored at plan time) + `06.2-REVIEW.md` independent code review |

*Note: the code review (`06.2-REVIEW.md`) found 1 correctness blocker (CR-01: `st.stop()` page abort) — a robustness/availability defect, not a STRIDE threat from the plan register — now fixed (commit `2902ae0`), with a regression test. It is tracked in the review report, not this register.*

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log (none)
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-08-02
