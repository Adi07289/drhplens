# Phase 06.2: Portfolio Surfaces - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-01
**Phase:** 06.2-portfolio-surfaces
**Areas discussed:** README shape & depth, Dashboard depth, Committed app screenshots

**Note:** This phase entered discuss-phase already ~90% locked by upstream artifacts — `06.2-SPEC.md` (ambiguity 0.12, 4 requirements locked), the approved `06.2-UI-SPEC.md` (visual + interaction design), and the brainstorm design doc (`docs/superpowers/specs/2026-08-01-6.2-portfolio-surfaces-design.md`, with locked architecture decisions + a ~4-plan/3-wave decomposition). Discussion was therefore scoped to the genuinely-open implementation-HOW decisions only. Of the four candidate gray areas surfaced, the user chose to discuss three (below) and left the fourth (failure-gallery count & ref rigor) to the planner's discretion.

---

## README shape & depth

| Option | Description | Selected |
|--------|-------------|----------|
| Hybrid: scannable top, deep below | Recruiter hook + headline metrics + architecture diagram up top (~30s read), then deep methodology / evals / honest-limitations sections, ending with links to every surface. | ✓ |
| Tight recruiter one-pager | Story + short architecture blurb + headline metrics + links out; depth lives in /methodology and the dashboard. | |
| Long paper-like writeup | Full methodology narrative, embedded diagram, metrics tables, "what I'd do next"; reads like a mini-paper. | |

**User's choice:** Hybrid: scannable top, deep below.
**Notes:** Serves both the skimming recruiter and the DS reviewer who wants rigor; matches the project's "rigor visible, not buried" ethos. Follow-on (Claude's discretion, driven by the screenshot decision): the README architecture diagram will be a **Mermaid fenced code block** — text-based, GitHub-native, zero external asset, honesty-invariant — depicting the REAL pipeline (not Docling). Recorded as D-02 in CONTEXT.md.

---

## Dashboard depth

| Option | Description | Selected |
|--------|-------------|----------|
| Sectioned by surface | Three sections — RAG (recall@k, citation, faithfulness="not measured"), Extraction (F1), Forecast (conformal coverage + calibration plot) — with the honest Swiggy-only per-IPO caveat inline. | ✓ |
| Single flat overview | One page: all headline numbers stacked + the calibration plot; simplest generator. | |

**User's choice:** Sectioned by surface.
**Notes:** Mirrors the eval harness's own structure and reads as more rigorous; only marginally more generator code. Still one self-contained HTML file (inline CSS + inlined images, zero external URLs) per SPEC/UI-SPEC C3. Recorded as D-03.

---

## Committed app screenshots

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — README + dashboard | Capture /methodology hero, a real cited answer, /failures; commit as static PNGs inlined into README + dashboard (build-time capture, stays render-only/demo-safe). | |
| Yes — README only | Screenshots in the README only; dashboard stays text/metrics-only. | |
| No — text/metrics only until 6.3 | No screenshots this phase; the live UI ships with the 6.3 public deploy. Simplest; nothing to capture or maintain. | ✓ |

**User's choice:** No — text/metrics only until 6.3.
**Notes:** Keeps 6.2 surfaces entirely text/metrics-based and fully demo-safe with no headless-capture tooling to build or keep current. Screenshots revisited in Phase 6.3 alongside the public deploy (OPS-02). Recorded as D-04 + deferred idea.

---

## Claude's Discretion

- **Failure gallery count & ref rigor (FAILGAL-01):** Surfaced as a candidate gray area but not selected for discussion — left to the planner/executor, anchored on the brainstorm design doc's candidate list of ~10 real failures. Guidance carried into CONTEXT.md: target ≥10 (ideally 12–15), balanced across rag/extraction/forecast, preferring a verifiable `refs` path per entry.
- Standard implementation details (file layout beyond SPEC's named paths, test structure, Mermaid node phrasing, dashboard section ordering).

## Deferred Ideas

- **App screenshots of the live UI** → Phase 6.3 (revisit alongside OPS-02 public deploy).
- **Live-from-Langfuse failure feed** → rejected for demo-safety; failures stay curated/committed.
- **OPS-02 public deploy + the four disclosed 6.1 follow-ups** → Phase 6.3 (launch gate).
