---
phase: 06.2-portfolio-surfaces
verified: 2026-08-02T16:05:00Z
status: human_needed
score: 4/4 roadmap truths verified; 17/17 plan-level must-haves verified
overrides_applied: 0
mvp_mode_note: >
  ROADMAP.md tags this phase "Mode: mvp", but the phase goal text is not in
  strict User Story format ("As a X, I want Y, so that Z.") — gsd-sdk
  user-story.validate returns valid=false. The phase was actually executed via
  the traditional SPEC-driven track (06.2-SPEC.md, ambiguity-scored, 4 locked
  requirements, per-plan must_haves frontmatter), which is a stronger contract
  than a bare user story. Verification below uses the standard goal-backward
  methodology (ROADMAP success criteria + merged PLAN must_haves) rather than
  the MVP user-flow-coverage table, since that is the contract this phase was
  actually planned and executed against. Flagged for awareness, not treated as
  a blocker.
human_verification:
  - test: "Push the branch and open README.md on GitHub; confirm the Mermaid flowchart (Architecture section) renders as an SVG diagram, not a raw code block."
    expected: "GitHub's native Mermaid renderer draws the 9-node flowchart (DRHP PDF -> PyMuPDF+pdfplumber -> bge-m3 -> Qdrant -> bge-reranker-v2-m3 -> LangGraph -> Instructor -> XGBoost+MAPIE -> Cited answer)."
    why_human: "GitHub's Mermaid rendering is a client-side JS feature with no offline/CLI equivalent available in this environment. The repo-local test (test_readme_links.py) only proves the ```mermaid fenced block and node labels exist, not that GitHub renders it. Pre-declared as manual-only in 06.2-VALIDATION.md."
  - test: "Run /gsd-ui-review for phase 6.2 against 06.2-UI-SPEC.md (contracts C1 hero, C2 failure card, C3 dashboard) — visually compare the live-rendered /methodology hero, /failures cards, and eval-dashboard.html to the approved design contract."
    expected: "Hero lane/connector/node layout, failure-card amber-rule/status-pill styling, and dashboard section styling match the UI-SPEC's approved visual contract (not just the class-name/token checks the automated tests perform)."
    why_human: "Pixel/layout fidelity is a design-review concern the automated suite cannot judge (it only asserts CSS class tokens are present, not that they render correctly). Pre-declared as manual-only ('retroactive, non-blocking') in every plan's <verification> section and in 06.2-VALIDATION.md's Manual-Only Verifications table. No 06.2-UI-REVIEW.md exists yet in .planning/ui-reviews or the phase directory."
---

# Phase 6.2: Portfolio Surfaces Verification Report

**Phase Goal:** A recruiter reviewing the portfolio can expand a "Show your work" pane on any claim/forecast, read a methodology-forward README + forecaster model card, browse a searchable /failures gallery, and land on a deep-linkable /methodology page — with committed HTML eval dashboards per release.
**Verified:** 2026-08-02T16:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

All functional/programmatic checks pass with strong evidence (test suite green, live Streamlit render smoke-tests exception-free, source-audit greps, git-commit provenance). Two items are routed to human verification per the escalation-gate pattern — both pre-declared as manual-only by the project's own `06.2-VALIDATION.md`, not newly discovered gaps.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|---|---|---|
| 1 | EVAL-04: "Show your work" pane expandable on any claim/forecast (retrieval query, chunks+scores, prompt, sources, eval scores) | VERIFIED | `ui/methodology_pane.py` (Phase 3, untouched — `git diff --stat` shows no change this phase) is wired into BOTH `ui/snapshot_chat.py:99` (per-claim Q&A) and `ui/snapshot_blocks.py:430` (forecast block — wired since Phase 3 commit `40df830`, so "extend to forecasts" was already satisfied and correctly just re-confirmed, not rebuilt). `tests/unit/test_methodology_pane.py` 6/6 PASSED. |
| 2 | OPS-03: portfolio README + model card + failure gallery (>=10 inspected failures) + committed HTML eval dashboard | VERIFIED | `README.md` rewritten (hybrid shape, honest metrics table, Mermaid diagram, cross-links) — `tests/unit/test_readme_links.py` 7/7 PASSED. `model_card/MODEL_CARD.md` pre-existing (Phase 5), linked. `eval/failures/failures.yaml` has 12 entries (>=10), all 3 surfaces present. `eval/dashboards/eval-dashboard.html` committed, self-contained, sectioned by surface — `tests/unit/test_eval_dashboard.py` 10/10 PASSED. |
| 3 | LAND-01: `/methodology` renders model card + methodology writeup + failure-gallery link + per-IPO eval summary; Phase-1 stub replaced | VERIFIED | `pages/01_methodology.py` opens with the C1 systems-breadth hero, model-card section (pre-existing, Phase 5, untouched), `/failures` link at line 76, honest Swiggy-only per-IPO note (`_swiggy_gold` guard at line 168). `tests/unit/test_methodology_render.py` 13/13 PASSED including 5 new LAND-01/EVAL-04 assertions. Live `AppTest` smoke-render: 0 exceptions. |
| 4 | FAILGAL-01: `/failures` renders >=10 documented failures across RAG/extraction/forecast, browseable and searchable | VERIFIED | `eval/failures/failures.yaml`: 12 entries, surfaces = `{extraction, forecast, rag}`, all unique ids, all non-empty postmortems, all refs resolve to real committed files. `pages/04_failures.py` wires `render_failures()` with a surface `st.selectbox` + free-text `st.text_input` (searchable, in-memory, no live call). `tests/unit/test_failures.py` 4/4 PASSED. Live `AppTest` smoke-render: 0 exceptions, 16 markdown blocks emitted. |

**Score:** 4/4 roadmap truths verified.

### Plan-Level Must-Haves (merged from PLAN frontmatter)

| Plan | Requirement | Truths | Artifacts | Key Links | Status |
|---|---|---|---|---|---|
| 06.2-01 | FAILGAL-01 | 5/5 verified | 4/4 verified (failures.yaml, ui/failures.py, pages/04_failures.py, test_failures.py) | 3/3 wired (page→renderer, renderer→yaml, footer→/failures) | VERIFIED |
| 06.2-02 | LAND-01, EVAL-04 | 5/5 verified | 2/2 verified (pages/01_methodology.py, test_methodology_render.py) | 2/2 wired (/failures link, per_ipo guard) | VERIFIED |
| 06.2-03 | OPS-03a | 4/4 verified | 3/3 verified (build script, dashboard HTML, test file) | 1/1 wired (generator reads eval_summary.json/card_data.json/calibration.png) | VERIFIED |
| 06.2-04 | OPS-03b | 4/4 verified | 2/2 verified (README.md, test_readme_links.py) | 1/1 wired (README → dashboard/model card/failures.yaml) | VERIFIED |

**Score:** 17/17 plan-level must-haves verified (5+4+4+4 truths across plans; all artifacts and key links wired).

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `eval/failures/failures.yaml` | >=10 real entries, all 3 surfaces, valid schema | VERIFIED | 12 entries; surfaces `{rag×8, extraction×1, forecast×3}`; all `refs` resolve to real committed files (checked programmatically — 0 missing) |
| `ui/failures.py` | `render_failures()`, allowlist-only imports | VERIFIED | Imports exactly `{__future__, html, pathlib.Path, yaml, streamlit}`; 159 lines; every field `html.escape`d; `yaml.safe_load` only |
| `pages/04_failures.py` | Streamlit page at `/failures` | VERIFIED | `st.set_page_config` first call; imports no agent/LLM/model module; wires `render_failures()` with surface+text filter controls; live `AppTest` render: 0 exceptions |
| `tests/unit/test_failures.py` | schema+count, AST-isolation, render, filter tests | VERIFIED | 4/4 tests pass |
| `pages/01_methodology.py` | C1 hero, Docling-scrubbed, `/failures` link, Swiggy-only per-IPO | VERIFIED | `grep -c Docling` == 0 whole-page; `.drhp-archhero` class present; `href="/failures"` present; `per_ipo` guard present; live `AppTest` render: 0 exceptions |
| `tests/unit/test_methodology_render.py` | extended LAND-01/EVAL-04 assertions | VERIFIED | 13/13 tests pass (5 new + 8 pre-existing, no regression) |
| `scripts/build_eval_dashboard.py` | deterministic, stdlib-only generator | VERIFIED | Imports only `base64/html/json/pathlib`; no streamlit/agent/model import |
| `eval/dashboards/eval-dashboard.html` | committed, self-contained, sectioned dashboard | VERIFIED | 66,695 bytes; 0 `http(s)://` refs; 0 `@import`; `data:image/png;base64,` present (calibration plot inlined); "not measured" present, "-1.0" absent; HTML parses cleanly (`<html>`/`</html>` present, no parse errors) |
| `tests/unit/test_eval_dashboard.py` | build+parity+self-contained tests | VERIFIED | 10/10 tests pass, including a byte-identical determinism check |
| `README.md` | portfolio-grade, Mermaid diagram, cross-links | VERIFIED | HF Spaces front-matter preserved verbatim (lines 1-11); ` ```mermaid ` fenced block present naming PyMuPDF/Qdrant/LangGraph/MAPIE; `Docling` count == 0; all repo-relative link targets exist and are non-empty |
| `tests/unit/test_readme_links.py` | front-matter/Mermaid/link-existence audit | VERIFIED | 7/7 tests pass |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `pages/04_failures.py` | `ui.failures.render_failures` | import + call | WIRED | `from ui.failures import render_failures` + one call site with surface/query args |
| `ui/failures.py` | `eval/failures/failures.yaml` | `yaml.safe_load` of committed file | WIRED | `.is_file()` guard + `yaml.safe_load(...)`, single occurrence |
| `ui/chrome.py` | `/failures` | footer anchor href repoint | WIRED | `href="/failures"` present, mapped to "Failure gallery" label; old `/methodology` mapping removed |
| `pages/01_methodology.py` | `/failures` | recruiter link anchor | WIRED | `href="/failures" target="_self"` inside the hero's `.drhp-archhero-links` block |
| `pages/01_methodology.py` | `eval_summary.json` per_ipo (Swiggy-only) | `"swiggy_2024_11" in _eval_per_ipo` guard | WIRED | Guard present; renders honest note either way, never fabricates a non-Swiggy figure |
| `ui/snapshot_chat.py` / `ui/snapshot_blocks.py` | `ui.methodology_pane.render_methodology_pane` | import + call (Phase 3, reused) | WIRED | Confirmed present and untouched; both the chat-claim and forecast-block surfaces call it |
| `scripts/build_eval_dashboard.py` | `eval_summary.json` + `card_data.json` + `calibration.png` | `json.load` + `base64.b64encode` | WIRED | Numbers in the committed HTML match source JSON (recall/citation 1.000, coverage ≈0.80, gate_passed=false) |
| `README.md` | `eval/dashboards/eval-dashboard.html`, `model_card/MODEL_CARD.md`, `eval/failures/failures.yaml` | repo-relative markdown links | WIRED | All three targets exist on disk and are non-empty; `test_every_repo_relative_file_link_resolves` PASSED |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `ui/failures.py::render_failures` | `entries` (from YAML) | `eval/failures/failures.yaml` (12 real, refs-verified entries) | Yes | FLOWING |
| `pages/01_methodology.py` per-IPO note | `_eval_per_ipo` | `eval/reports/eval_summary.json` (`per_ipo.swiggy_2024_11`, real values) | Yes | FLOWING |
| `scripts/build_eval_dashboard.py` dashboard numbers | RAG/forecast metrics | `eval/reports/eval_summary.json` + `model_card/card_data.json` (re-derived independently in the parity test) | Yes | FLOWING |
| `README.md` headline-metrics table | recall/citation/faithfulness/coverage | Same committed JSON sources, cited in prose | Yes (static text, sourced from committed JSON, not live-computed — matches the render-only phase contract) | FLOWING |

No hollow props or disconnected data sources found — every rendered number traces to a committed, verifiable artifact.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `/failures` renders live without exception | `streamlit.testing.v1.AppTest.from_file('pages/04_failures.py').run()` | 0 exceptions, 16 markdown blocks | PASS |
| `/methodology` renders live without exception | `streamlit.testing.v1.AppTest.from_file('pages/01_methodology.py').run()` | 0 exceptions, 21 markdown blocks (only pre-existing, unrelated `use_container_width` deprecation warnings) | PASS |
| `eval-dashboard.html` is well-formed HTML | `html.parser.HTMLParser().feed(content)` | No parse errors; `<html>`/`</html>` present | PASS |
| Every `failures.yaml` `refs` entry resolves to a real file | inline Python check | 0 missing refs across 12 entries | PASS |
| All git commits cited in SUMMARYs exist | `git log --oneline --all \| grep 06.2-0[1-4]` | All 12 cited commit hashes (de16b68…2595e0f) found in history | PASS |

### Honesty-Invariant Checks (load-bearing)

| Check | Command | Result |
|---|---|---|
| No Docling in methodology page | `grep -c Docling pages/01_methodology.py` | 0 |
| No Docling in README | `grep -c Docling README.md` | 0 |
| Dashboard self-contained | `grep -Ec 'https?://' eval/dashboards/eval-dashboard.html` | 0 |
| Dashboard no `@import` | `grep -c '@import' eval/dashboards/eval-dashboard.html` | 0 |
| Faithfulness verbatim "not measured" | `grep -c "not measured"` / `grep -c "\-1.0"` | 1 / 0 |
| No red/green verdict color (methodology) | `grep -Eic "color:(red|green)\|#e5484d\|#d33682\|#2ecc71\|#3fb950"` | 0 |
| No red/green verdict color (dashboard) | same pattern | 0 |
| No red/green verdict color (failures.py) | same pattern | 0 |
| Per-IPO framing shows only Swiggy | source inspection of `_swiggy_gold` guard + `_ipo_note` | Confirmed — no other catalogue IPO id ever printed as a per-IPO figure |
| `failures.yaml` schema (>=10, 3 surfaces, unique ids, non-empty postmortems) | inline Python check | count=12, surfaces=all 3, unique ids=True, empty postmortems=[] |

All honesty-invariant checks from the task brief pass exactly as specified.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| FAILGAL-01 | 06.2-01 | `/failures` gallery, >=10 real failures, searchable | SATISFIED | See truths #4, artifacts, key links above |
| LAND-01 | 06.2-02 | `/methodology` recruiter landing extension | SATISFIED | See truth #3 above |
| EVAL-04 | 06.2-02 (coverage-confirm) | "Show your work" pane on claims/forecasts | SATISFIED | Delivered Phase 3 (METHOD-01), confirmed reused/untouched, wired to both chat and forecast surfaces |
| OPS-03a | 06.2-03 | Committed self-contained HTML eval dashboard | SATISFIED | See truth #2 above |
| OPS-03b | 06.2-04 | Portfolio README | SATISFIED | See truth #2 above |
| OPS-03 (parent) | 06.2-03 + 06.2-04 | README + model card + failure gallery + dashboards | SATISFIED | Both sub-requirements land; REQUIREMENTS.md traceability table marks OPS-03 "Complete" under Phase 6.2 |

**REQUIREMENTS.md cross-check:** `grep -n "Phase 6.2" .planning/REQUIREMENTS.md` returns exactly 4 rows (EVAL-04, OPS-03, LAND-01, FAILGAL-01), matching the phase's declared requirement IDs exactly. No orphaned requirements found.

**Minor documentation-path note (non-blocking):** ROADMAP.md's SC #2 text says "committed HTML eval dashboards under `eval/reports/` per release," but the actual (and SPEC.md-locked, later-refined) location is `eval/dashboards/eval-dashboard.html` — a dedicated directory, not `eval/reports/`. This is a wording drift between the original ROADMAP phrasing and the more specific 06.2-SPEC.md contract (which consistently specifies `eval/dashboards/` throughout, ambiguity score 0.12, explicitly locked). The artifact itself is fully functional, committed, self-contained, and cross-linked from the README and CONTEXT.md canonical refs — this is a path-label inconsistency in planning docs, not a functional gap.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `pages/01_methodology.py` | 425 | `href="https://github.com/REPLACE-ME/drhplens"` placeholder GitHub URL | Info | Pre-existing since Phase 1 (commit `5fa9662`, explicitly flagged there as `FLAG-GITHUB-URL: placeholder URL for Wave 5 deploy to replace`). Not touched or introduced by Phase 6.2. Out of scope here — OPS-02 public deploy (which will supply the real repo URL) is explicitly deferred to Phase 6.3 per 06.2-SPEC.md boundaries. Not a debt marker requiring a `#issue` reference under this phase's gate since it predates this phase and is tracked for a later phase. |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers, no stray "coming soon"/"not yet implemented" copy, and no empty-implementation stubs found in any of the 15 files modified across the phase's 4 plans.

### Human Verification Required

### 1. GitHub Mermaid Rendering

**Test:** Push the branch and open `README.md` on GitHub; confirm the Architecture section's Mermaid flowchart renders as an SVG diagram.
**Expected:** The 9-node flowchart (DRHP PDF → PyMuPDF+pdfplumber → bge-m3 → Qdrant → bge-reranker-v2-m3 → LangGraph → Instructor → XGBoost+MAPIE → Cited answer) renders visually, not as a raw fenced code block.
**Why human:** GitHub's Mermaid renderer is client-side JS with no offline equivalent in this sandbox. `test_readme_links.py` proves the fenced block and node-label text exist, not that GitHub actually renders it. Pre-declared as manual-only in `06.2-VALIDATION.md`'s "Manual-Only Verifications" table.

### 2. UI-SPEC Visual Fidelity Review

**Test:** Run `/gsd-ui-review` for phase 6.2 against `06.2-UI-SPEC.md` (contracts C1 hero, C2 failure card, C3 dashboard) — visually inspect the live `/methodology` hero, `/failures` cards, and `eval-dashboard.html` against the approved design.
**Expected:** Layout, spacing, connector/lane rendering, failure-card styling, and dashboard section styling match the approved visual contract.
**Why human:** Pixel/layout fidelity cannot be judged by class-name/token greps — the automated suite confirms the right CSS classes and copy exist, not that they render correctly. Declared "retroactive, non-blocking" in every plan's `<verification>` section and listed in `06.2-VALIDATION.md`'s Manual-Only Verifications table. No `06.2-UI-REVIEW.md` exists yet in `.planning/ui-reviews/` or the phase directory, confirming this review has not yet run.

### Gaps Summary

No functional gaps found. All 4 ROADMAP success criteria and all 17 plan-level must-haves (truths/artifacts/key-links) are VERIFIED against the actual codebase — not just SUMMARY.md claims. The full offline unit suite is green (599 passed, 1 env-gated skip, 0 failures), all 5 phase-specific test files pass in full (4+13+6+10+7 = 40 tests), live `AppTest` smoke-renders of both `/failures` and `/methodology` complete with zero exceptions, every honesty-invariant grep specified in the task brief returns the expected result, and every git commit cited across the 4 SUMMARY.md files is verified present in `git log`.

Status is `human_needed` rather than `passed` solely because two pre-declared, non-programmatically-verifiable checks (GitHub Mermaid SVG rendering, UI-SPEC pixel-fidelity review) remain outstanding — both explicitly scoped as manual-only in the phase's own `06.2-VALIDATION.md` before execution began, not gaps discovered during this verification.

---

*Verified: 2026-08-02T16:05:00Z*
*Verifier: Claude (gsd-verifier)*
