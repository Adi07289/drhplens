---
phase: 06.2-portfolio-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["06.2-01"]
files_modified:
  - tests/unit/test_methodology_render.py
  - ui/copy.py
  - app/static/drhplens.css
  - pages/01_methodology.py
  - pages/03_how_it_works.py
autonomous: true
requirements: [LAND-01, EVAL-04]
must_haves:
  truths:
    - "/methodology opens with a systems-breadth hero (C1) naming the REAL pipeline — PyMuPDF, Qdrant, LangGraph, MAPIE present; Docling absent whole-page"
    - "A prominent /failures link is present on /methodology"
    - "The per-IPO eval summary shows only Swiggy — no fabricated per-IPO number for any other IPO"
    - "The METHOD-01 'Show your work' pane (EVAL-04) remains present and passing — reused, not rebuilt"
    - "The /methodology URL is unchanged"
  artifacts:
    - path: "pages/01_methodology.py"
      provides: "C1 hero (.drhp-archhero-*), Docling scrubbed, /failures link, Swiggy-only per-IPO, live-demo placeholder"
      contains: "drhp-archhero"
    - path: "tests/unit/test_methodology_render.py"
      provides: "extended LAND-01 source-audit + EVAL-04 coverage assertion"
  key_links:
    - from: "pages/01_methodology.py"
      to: "/failures"
      via: "recruiter link anchor"
      pattern: "href=\"/failures\""
    - from: "pages/01_methodology.py"
      to: "eval_summary.json per_ipo (swiggy only)"
      via: "in-guard (drhp_id in per_ipo) reused from ui/eval_inline.py"
      pattern: "per_ipo"
---

<objective>
Extend `/methodology` into the recruiter landing (LAND-01): add the UI-SPEC **C1** systems-breadth
architecture hero (new `.drhp-archhero-*` classes) that names the REAL pipeline, scrub every
"Docling" reference whole-page (honesty-invariant regression risk), add a prominent `/failures`
link, an honest Swiggy-only per-IPO summary, and a live-demo placeholder — all extending, not
replacing, the page (URL unchanged). Confirm EVAL-04 coverage: the METHOD-01 "Show your work" pane
already delivers it — verify, do not rebuild.

Purpose: The `/methodology` page is where recruiter deep-links land; it must lead with accurate
systems breadth and never contradict the honesty thesis (no "Docling" that does not run; no
fabricated per-IPO number).
Output: C1 hero + Docling scrub + `/failures` link + Swiggy-only per-IPO + live-demo placeholder on
`pages/01_methodology.py`; stale-copy cleanup on `pages/03_how_it_works.py`; extended
`tests/unit/test_methodology_render.py`; EVAL-04 coverage confirmed.
</objective>

## Phase Goal (MVP user story)

**As a** hiring manager / DS recruiter, **I want to** land on `/methodology` and immediately see an
accurate architecture of the real pipeline plus links to the model card, failures, and per-IPO
evals, **so that** I can understand and trust the system without running it. (Derived from the
ROADMAP 6.2 goal + the SPEC goal line.)

Vertical slice: the page-level surface (tests → hero copy/CSS → page surgery → EVAL-04 coverage)
lands together so a recruiter reaches a coherent, honest landing page when the plan completes.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-portfolio-surfaces/06.2-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-UI-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-RESEARCH.md
@.planning/phases/06.2-portfolio-surfaces/06.2-PATTERNS.md
@.planning/phases/06.2-portfolio-surfaces/06.2-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the methodology render test (RED) + add C1 hero copy + .drhp-archhero-* CSS</name>
  <files>tests/unit/test_methodology_render.py, ui/copy.py, app/static/drhplens.css</files>
  <read_first>
    - `tests/unit/test_methodology_render.py:28-34` (whole-page `PAGE.read_text()` source-audit pattern) and `:111-118` (file-existence audits) — the harness to extend.
    - `06.2-UI-SPEC.md` §C1 (horizontal 3-lane hero: Ingest → Retrieve·Reason → Extract·Forecast·Answer; amber `→` connectors; LangGraph node sub-steps `gate1 · generate · scrub · cite-check`; single amber-filled terminal node; names PyMuPDF/pdfplumber/Qdrant/LangGraph/Instructor/MAPIE, Docling absent) + §Copywriting Contract (hero eyebrow/H1/subhead, diagram caption, per-IPO summary, live-demo placeholder).
    - RESEARCH §Pitfall 2 (`.drhp-arch*` collision — new hero MUST use `.drhp-archhero-*`) and §Pitfall 6 (per-IPO honesty guard `drhp_id in per_ipo`).
    - PATTERNS §"pages/01_methodology.py" (the whole-page Docling-absent scope, Open-Q#1/A4) + `app/static/drhplens.css:1747-1760` (existing `.drhp-arch*` — do not touch/reuse).
    - `ui/copy.py:809-836` scrubber — new hero copy must be UI-SPEC-verbatim (scrubber-clean).
  </read_first>
  <action>
    Extend `tests/unit/test_methodology_render.py` (read the whole page source once via
    `PAGE.read_text()`) with LAND-01 assertions: the C1 hero renders (a `.drhp-archhero`/
    `.drhp-archhero-lane`/`.drhp-archhero-node` class token is present); each of `"PyMuPDF"`,
    `"Qdrant"`, `"LangGraph"`, `"MAPIE"` is present in the page source; `"Docling"` is ABSENT
    whole-page (`assert "Docling" not in source`); a `/failures` link is present
    (`href="/failures"`); the per-IPO summary references only `swiggy` (assert no other catalogue
    drhp_id is printed as a per-IPO figure); and an honesty-color guard — the page markup contains no
    red/green verdict color (no `color:` red/green, no red/green hex such as `#e5484d`/`#d33`/
    `#2ecc71`/`#3fb950`, no `.drhp-verdict-`/badge class) so the new C1 hero can never introduce a
    good/bad color. Add one EVAL-04 coverage assertion: the page still wires
    the METHOD-01 "Show your work" pane (assert the page imports/renders `methodology_pane` — the
    existing pane, not a rebuild). These assertions FAIL now (RED) — Task 2 makes them GREEN.

    Add the C1 hero CHROME strings to `ui/copy.py`, UI-SPEC-verbatim: hero eyebrow
    `METHODOLOGY · HOW IT ACTUALLY WORKS`, H1 "A 400-page prospectus, read honestly.", the subhead,
    the diagram caption `SYSTEM ARCHITECTURE` / `◆ the real pipeline — no black boxes`, the honest
    per-IPO summary "1 IPO has a real gold set (Swiggy); others show the system-level figure.", and
    the live-demo placeholder "Live demo — coming with the public deploy (Phase 6.3)." Re-run the
    module import after editing (Pitfall 4).

    Add ADDITIVE `.drhp-archhero-*` CSS to `app/static/drhplens.css` (append only; NEVER `.drhp-arch*`
    — taken at 1747-1760): `.drhp-archhero` (horizontal flex, 3 lanes), `.drhp-archhero-lane`,
    `.drhp-archhero-node` (mono label + muted sub-label), `.drhp-archhero-node--out` (the single
    amber-filled terminal node), `.drhp-archhero-conn` (amber `→` connector), `.drhp-archhero-chip`
    (LangGraph sub-step micro-chips). Use only existing `:root --drhp-*` tokens; no gradients; no new
    palette. Distinct from the vertical `.drhp-flow` on `/how_it_works` (denser terminal language).
  </action>
  <acceptance_criteria>
    - `tests/unit/test_methodology_render.py` contains new assertions for hero classes, PyMuPDF/Qdrant/LangGraph/MAPIE present, `"Docling" not in source`, `/failures` link, Swiggy-only per-IPO, the honesty-color guard (no red/green verdict color), and the METHOD-01 pane wiring (they are RED until Task 2 — expected).
    - `pytest tests/unit/test_copy_no_banned_tokens.py -x` PASSES (hero copy scrubber-clean).
    - CSS additive-only: `grep -v '^#' app/static/drhplens.css | grep -c "\\.drhp-archhero"` >= 5; existing `.drhp-arch` (non-hero) selectors byte-unchanged; `grep -c "fonts.googleapis" app/static/drhplens.css` unchanged (1, additive edit only).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_copy_no_banned_tokens.py -x -q</automated>
  </verify>
  <done>The methodology render test asserts the full LAND-01 contract (RED), the C1 hero copy is scrubber-clean in ui/copy.py, and additive .drhp-archhero-* CSS exists without touching .drhp-arch*.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the C1 hero + scrub Docling whole-page + /failures link + Swiggy-only per-IPO (GREEN)</name>
  <files>pages/01_methodology.py, pages/03_how_it_works.py</files>
  <read_first>
    - `pages/01_methodology.py` — the three Docling sites verified in PATTERNS: line 7 docstring ("failure gallery"), line 47 (`_ARCH` stage 01 "Docling · layout-aware · page-anchored chunks"), lines 53-70 (dead `_STAGES_UNUSED`, line 55 names Docling), line 391 (`_STACK` chip list contains "Docling"); the existing hero block `:33-42` (.drhp-hero2) and the live-eval read guard `:104-114`.
    - `ui/eval_inline.py:148` (the `if drhp_id in per_ipo:` fabrication guard to copy) + `eval/reports/eval_summary.json` (`per_ipo` has `swiggy_2024_11` only).
    - `06.2-UI-SPEC.md` §C1 (hero visual contract) and the copy added in Task 1.
    - `pages/03_how_it_works.py:6` (stale "failure gallery" docstring) — refresh, gallery now exists at /failures.
  </read_first>
  <action>
    In `pages/01_methodology.py`: add the UI-SPEC **C1** hero at the TOP of the page using the new
    `.drhp-archhero-*` classes and the Task-1 copy — a horizontal 3-lane diagram (Ingest → Retrieve·
    Reason → Extract·Forecast·Answer) with amber `→` connectors, each node a mono label + muted
    sub-label naming the REAL stack (`PyMuPDF + pdfplumber` · `bge-m3` · `Qdrant hybrid` ·
    `bge-reranker-v2-m3` · `LangGraph` with `gate1 · generate · scrub · cite-check` micro-chips ·
    `Instructor` · `XGBoost + MAPIE`), and the SINGLE amber-filled terminal node "Cited, calibrated
    answer ✓". Emit as inline HTML via `st.markdown(..., unsafe_allow_html=True)` (a single
    `<div class="drhp-archhero">…`); html.escape any interpolated value.

    Scrub Docling whole-page (RESEARCH Open-Q#1 / A4 — the acceptance test is whole-page):
    REPLACE the old 5-stage `.drhp-arch` diagram (lines ~46-76) with the C1 hero (they are redundant
    — RESEARCH Open-Q#3 recommends replace); DELETE the dead `_STAGES_UNUSED` block (53-70); DROP the
    `"Docling"` chip from `_STACK` (391), keeping LangGraph/LlamaIndex/bge-m3/bge-reranker-v2-m3/
    Qdrant/Instructor/MAPIE/etc.; refresh the docstring (line 7) to reflect the gallery now living at
    `/failures`. After this, `grep -c Docling pages/01_methodology.py` MUST be 0.

    Add a prominent `/failures` link (`<a href="/failures" target="_self">`) and the honest Swiggy-
    only per-IPO summary using the copied `if drhp_id in per_ipo:` guard (never fabricate a per-IPO
    number for a non-Swiggy IPO), plus the live-demo placeholder (muted, non-link) from Task 1. Reuse
    the existing `.is_file()`+try/except read guard at `:104-114` for any committed read — do NOT add
    a second reader. Do NOT touch or re-implement the METHOD-01 "Show your work" pane wiring (EVAL-04
    — reused as-is).

    In `pages/03_how_it_works.py`: refresh the stale line-6 "failure gallery" docstring reference
    (gallery now exists at /failures) — copy-only, no behavior change.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_methodology_render.py -x` PASSES (hero present; PyMuPDF/Qdrant/LangGraph/MAPIE present; Docling absent; /failures link; Swiggy-only per-IPO; METHOD-01 pane wired).
    - `grep -v '^#' pages/01_methodology.py | grep -c "Docling"` == 0 (whole-page, including the deleted dead block and dropped chip).
    - `grep -c "drhp-archhero" pages/01_methodology.py` >= 1 and `grep -c 'href="/failures"' pages/01_methodology.py` >= 1.
    - `grep -c "per_ipo" pages/01_methodology.py` >= 1 (fabrication guard present); no non-Swiggy per-IPO figure printed.
    - Honesty-color: `grep -Eic "color:[[:space:]]*(red|green)|#e5484d|#d33682|#2ecc71|#3fb950" pages/01_methodology.py` == 0 (the C1 hero introduces no red/green verdict color).
    - The `/methodology` route is unchanged (`pages/01_methodology.py` filename/slug untouched).
    - `grep -c "failure gallery" pages/03_how_it_works.py` == 0 (docstring refreshed).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_methodology_render.py -x -q</automated>
  </verify>
  <done>/methodology leads with the C1 real-pipeline hero, names no Docling anywhere, links /failures, shows only the Swiggy per-IPO figure, and the /methodology URL is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm EVAL-04 coverage (METHOD-01 "Show your work" pane) — verify, do not rebuild</name>
  <files>tests/unit/test_methodology_render.py</files>
  <read_first>
    - `ui/methodology_pane.py` + `tests/unit/test_methodology_pane.py` (the METHOD-01 "Show your work" pane delivered in Phase 3 — EVAL-04) — confirm it exists and passes; do NOT modify it.
    - RESEARCH §Phase Requirements (EVAL-04 row: ALREADY DELIVERED by METHOD-01; reused, NOT rebuilt — confirm coverage only) and STATE.md "Key Decisions (from Phase 3 Wave 4 / 03-06)".
  </read_first>
  <action>
    Confirm EVAL-04 is covered by the existing METHOD-01 pane — this is a coverage-confirmation task,
    NOT a build. Run the existing `tests/unit/test_methodology_pane.py` to prove the "Show your work"
    pane (retrieval query, retrieved chunks with scores, prompt, sources, per-claim eval scores) still
    passes untouched. Ensure the Task-1 EVAL-04 coverage assertion (the methodology page still wires
    `methodology_pane`) is GREEN. Record in the SUMMARY that EVAL-04 is delivered by METHOD-01
    (Phase 3) and verified-present in Phase 6.2 — no new pane was built (per SPEC out-of-scope). Do
    not add, remove, or alter any pane code.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_methodology_pane.py -x` PASSES (METHOD-01 pane intact — EVAL-04 coverage confirmed).
    - `ui/methodology_pane.py` is byte-unchanged by this plan (verify via `git diff --stat ui/methodology_pane.py` shows no change).
    - The methodology render test's EVAL-04 coverage assertion (pane wired) is GREEN.
    - Full offline unit suite green: `.venv/bin/python -m pytest tests/unit -q`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_methodology_pane.py tests/unit/test_methodology_render.py -x -q</automated>
  </verify>
  <done>EVAL-04 is confirmed delivered by the untouched METHOD-01 pane; the coverage assertion is green; nothing was rebuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `eval_summary.json` → page | read-only per-IPO data crosses into the render |
| hero/interpolated strings → recruiter browser | `unsafe_allow_html=True` inline HTML to public reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-05 | Tampering/Elevation (V5) | C1 hero + per-IPO interpolation into `unsafe_allow_html` | mitigate | `html.escape` interpolated values; hero copy is static UI-SPEC-verbatim |
| T-06.2-06 | Info-Disclosure (honesty regression) | residual "Docling" on the honesty page | mitigate | whole-page `assert "Docling" not in source`; delete dead `_STAGES_UNUSED`; drop `_STACK` Docling chip |
| T-06.2-07 | Info-Disclosure (fabricated data) | per-IPO summary printing a non-Swiggy figure | mitigate | reuse `if drhp_id in per_ipo:` guard; test asserts Swiggy-only |
| T-06.2-08 | Info-Disclosure/Elevation (P19) | render path importing agent/LLM/model | mitigate | methodology whole-page source-audit stays clean; reuse existing `.is_file()` read guard, no new reader |
| T-06.2-04 | Compliance banned-token leak | new hero chrome strings in `ui/copy.py` | mitigate | import-time scrubber (`test_copy_no_banned_tokens.py`) |
</threat_model>

<verification>
- Per-task: run the task's `<automated>` command (feedback < 60s).
- Wave merge: `.venv/bin/python -m pytest tests/unit -q`.
- Honesty invariant: `grep -c Docling pages/01_methodology.py` == 0 whole-page; per-IPO Swiggy-only; no fabricated numbers.
- Manual (retroactive, non-blocking): `/gsd-ui-review` against UI-SPEC §C1 for hero visual fidelity.
</verification>

<success_criteria>
- `/methodology` opens with the C1 systems-breadth hero naming PyMuPDF/Qdrant/LangGraph/MAPIE; Docling absent whole-page; `/failures` link present; Swiggy-only per-IPO; URL unchanged.
- EVAL-04 confirmed delivered by the untouched METHOD-01 pane (no rebuild).
- Full offline suite green; no isolation regression.
</success_criteria>

<output>
Create `.planning/phases/06.2-portfolio-surfaces/06.2-02-SUMMARY.md` when done.
</output>
