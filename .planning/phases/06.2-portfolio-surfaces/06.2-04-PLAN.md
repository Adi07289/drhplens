---
phase: 06.2-portfolio-surfaces
plan: 04
type: execute
wave: 2
depends_on: ["06.2-01", "06.2-03"]
files_modified:
  - README.md
  - tests/unit/test_readme_links.py
autonomous: true
requirements: [OPS-03b]
must_haves:
  truths:
    - "The README is portfolio-grade: a scannable ~30s top (recruiter hook + headline metrics + architecture diagram) then deeper methodology/evals/honest-limitations sections (D-01 hybrid shape)"
    - "The architecture diagram is a Mermaid fenced block depicting the REAL pipeline (PyMuPDF+pdfplumber â bge-m3 â Qdrant â bge-reranker â LangGraph â Instructor â XGBoost+MAPIE â cited answer); Docling absent (D-02)"
    - "Repo-relative links to the committed dashboard, model card, /failures data, and /methodology resolve to files that exist"
    - "The Hugging Face Spaces front-matter is preserved verbatim"
  artifacts:
    - path: "README.md"
      provides: "portfolio-grade README with Mermaid diagram + cross-links + preserved HF front-matter"
      contains: "```mermaid"
    - path: "tests/unit/test_readme_links.py"
      provides: "repo-relative link-existence + Mermaid-present + Docling-absent + front-matter-preserved checks"
  key_links:
    - from: "README.md"
      to: "eval/dashboards/eval-dashboard.html + model_card/MODEL_CARD.md + eval/failures/failures.yaml"
      via: "repo-relative markdown links"
      pattern: "eval/dashboards/eval-dashboard.html"
---

<objective>
Ship OPS-03b (part of roadmap OPS-03): rewrite `README.md` into a portfolio-grade, hybrid-shaped
document (D-01) â a ~30-second scannable top (recruiter hook + headline metrics + a Mermaid
architecture diagram of the REAL pipeline, D-02) then deeper methodology / evals / honest-limitations
sections â cross-linking the committed dashboard, the forecaster model card, `/methodology`, and
`/failures`, plus the live-demo placeholder. Preserve the Hugging Face Spaces front-matter verbatim.
Pin it with a link-check test that asserts every referenced committed file exists, the Mermaid block
is present, and Docling is absent.

Purpose: The README is the first thing a recruiter reads on GitHub; it must tell the honest story,
show the real architecture (no Docling), and route to every other surface â all with zero external
assets.
Output: rewritten `README.md`, `tests/unit/test_readme_links.py`.
</objective>

## Phase Goal (MVP user story)

**As a** hiring manager / DS recruiter, **I want to** read a portfolio-grade README with a real
architecture diagram and working links to the dashboard, model card, failures, and methodology,
**so that** I can understand and trust the system without running it. (Derived from the ROADMAP 6.2
goal + the SPEC goal line.)

Vertical slice: failing link-check test â README rewrite â green, so the README surface cross-links
every committed artifact end-to-end when the plan completes. (Depends on 06.2-01 for
`failures.yaml` and 06.2-03 for the committed dashboard so the link-check targets exist.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-portfolio-surfaces/06.2-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-UI-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-RESEARCH.md
@.planning/phases/06.2-portfolio-surfaces/06.2-PATTERNS.md
@.planning/phases/06.2-portfolio-surfaces/06.2-01-PLAN.md
@.planning/phases/06.2-portfolio-surfaces/06.2-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the README link-check test (RED)</name>
  <files>tests/unit/test_readme_links.py</files>
  <read_first>
    - PATTERNS Â§"tests/unit/test_readme_links.py" (analog: the file-existence audits in `tests/unit/test_methodology_render.py:111-118` â `p.is_file() and p.stat().st_size > 0`; the link-parsing half is new).
    - `README.md:1-11` (the HF Spaces YAML front-matter to preserve) and RESEARCH Â§Pattern 4 (Mermaid fence must be exactly `mermaid`).
    - `06.2-CONTEXT.md` D-01 (hybrid shape) + D-02 (Mermaid, real pipeline, NOT Docling) + Â§Copywriting Contract (live-demo placeholder).
  </read_first>
  <action>
    Write `tests/unit/test_readme_links.py`. Read `README.md` once and assert:
    (1) HF front-matter preserved â the file starts with a `---` YAML block containing
        `title: DRHPLens`, `sdk: streamlit`, `app_file: app.py` (verbatim from lines 1-11).
    (2) Mermaid diagram present â a fenced block opening with exactly ```` ```mermaid ````.
    (3) Docling absent â `"Docling" not in readme` (honesty invariant; D-02 real pipeline).
    (4) Real-pipeline names present in the diagram â `"PyMuPDF"`, `"Qdrant"`, `"LangGraph"`,
        `"MAPIE"` each appear.
    (5) Repo-relative link existence â parse markdown links out of the README and assert every
        referenced COMMITTED file exists and is non-empty: at minimum
        `model_card/MODEL_CARD.md`, `eval/dashboards/eval-dashboard.html`,
        `eval/failures/failures.yaml`. Assert these are repo-relative (no `http(s)://` host) using
        `p.is_file() and p.stat().st_size > 0`.
    (6) Section presence (D-01 hybrid) â a project summary/hook, an architecture section, an evals
        section, and a limitations/honesty section all present (heading-level check).
    This FAILS now against the current thin Phase-1 stub README â the intended RED for Task 2.
  </action>
  <acceptance_criteria>
    - `tests/unit/test_readme_links.py` exists with independent asserts for front-matter, Mermaid fence, Docling-absent, real-pipeline names, repo-relative link existence, and hybrid-shape sections.
    - Running it now FAILS against the current README (stub lacks Mermaid/cross-links) â expected RED.
    - The link-existence check uses `p.is_file() and p.stat().st_size > 0` (mirrors `test_methodology_render.py:111-118`), not a network fetch.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_readme_links.py -x -q || echo "RED-as-expected (stub README)"</automated>
  </verify>
  <done>The link-check test encodes front-matter preservation, Mermaid presence, Docling absence, real-pipeline names, repo-relative link existence, and hybrid-shape sections; it is RED against the stub README.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md â hybrid shape + Mermaid real-pipeline diagram + cross-links (GREEN)</name>
  <files>README.md</files>
  <read_first>
    - `README.md:1-11` â the HF Spaces front-matter to keep VERBATIM (`title/emoji/sdk/sdk_version/app_file/license/sleep_time`).
    - PATTERNS Â§"README.md" (keep front-matter; Mermaid block with quoted node labels; repo-relative cross-links) and RESEARCH Â§Pattern 4 (Mermaid gotchas: fence exactly `mermaid`; wrap labels containing `(`/`+`/`Â·`/`:` in quotes) + the example diagram.
    - `06.2-CONTEXT.md` D-01 (hybrid shape â scannable top then deeper sections) + D-02 (Mermaid, real pipeline, NOT Docling) + `06.2-SPEC.md` OPS-03b (summary + architecture + working repo-relative links).
    - The committed numbers for the headline metrics: `eval/reports/eval_summary.json` (citation/recall 1.000, faithfulness "not measured") and `model_card/card_data.json` (coverage 0.800, gate_passed=false â model does not beat baselines). Frame these HONESTLY.
    - `model_card/MODEL_CARD.md`, `eval/dashboards/eval-dashboard.html` (committed by 06.2-03), `eval/failures/failures.yaml` (committed by 06.2-01) â the cross-link targets.
  </read_first>
  <action>
    Rewrite `README.md` KEEPING the HF Spaces YAML front-matter (lines 1-11) byte-verbatim (the
    deploy depends on it). Author the D-01 HYBRID shape:
    - Scannable top (~30s): a recruiter hook (the one-line project thesis â honest, cited, calibrated
      Indian-IPO DRHP decoder), the headline metrics framed honestly (recall/citation saturate at
      1.00 on coarse gold spans â a floor not a win; faithfulness "not measured"; forecaster
      conformal coverage ~0.80 but does NOT beat naive baselines â P9 gate fails by design), and the
      architecture diagram.
    - The Mermaid diagram (D-02): a ` ```mermaid ` fenced block (fence exactly `mermaid`) depicting
      the REAL pipeline â `DRHP PDF â "PyMuPDF + pdfplumber (page-anchored parse)" â "bge-m3 embed"
      â "Qdrant hybrid (dense + sparse)" â "bge-reranker-v2-m3" â "LangGraph agent (gate1 Â· generate
      Â· scrub Â· cite-check)" â "Instructor extract" â "XGBoost + MAPIE conformal interval" â "Cited,
      calibrated answer"`. Quote every node label containing `(`/`+`/`Â·`/`:`. Docling MUST be absent.
    - Deeper sections below (D-01): a methodology narrative, an evals section, and an honest
      limitations section (forecaster loses to baselines; extraction F1 0.000 on a tiny set;
      faithfulness not yet judge-calibrated).
    - A cross-links section with REPO-RELATIVE links: `model_card/MODEL_CARD.md`,
      `eval/dashboards/eval-dashboard.html`, `eval/failures/failures.yaml`, `/methodology`,
      `/failures`, and the live-demo placeholder "Live demo â coming with the public deploy
      (Phase 6.3)." Zero external asset URLs (Mermaid is text; no external images).
    Do NOT name Docling anywhere. Do NOT fabricate a metric â every number traces to the committed
    `eval_summary.json`/`card_data.json`. No app screenshots (D-04) — the README stays text/metrics + the Mermaid diagram; the live UI ships with Phase 6.3.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_readme_links.py -x` PASSES (front-matter preserved, Mermaid present, Docling absent, real-pipeline names, repo-relative links exist, hybrid sections).
    - `grep -c "Docling" README.md` == 0.
    - `grep -c '```mermaid' README.md` >= 1 and the diagram names PyMuPDF/Qdrant/LangGraph/MAPIE.
    - HF front-matter intact: `head -11 README.md` still contains `title: DRHPLens`, `sdk: streamlit`, `app_file: app.py`.
    - Repo-relative link targets exist: `test -f eval/dashboards/eval-dashboard.html && test -f model_card/MODEL_CARD.md && test -f eval/failures/failures.yaml`.
    - No external asset URLs for images: `grep -Ec '!\[[^]]*\]\(https?://' README.md` == 0.
    - Full offline unit suite green: `.venv/bin/python -m pytest tests/unit -q`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_readme_links.py -x -q</automated>
  </verify>
  <done>README.md is portfolio-grade (hybrid shape), leads with an honest metrics summary + a Mermaid real-pipeline diagram (Docling absent), cross-links every committed surface via repo-relative links, preserves the HF front-matter, and its link-check test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README â GitHub renderer | markdown + Mermaid rendered for an unauthenticated public reader |
| README links â committed repo files | repo-relative paths must stay under root and resolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-14 | Info-Disclosure (honesty) | README naming Docling or fabricating a metric | mitigate | link-check asserts `"Docling" not in README`; numbers traced to committed `eval_summary.json`/`card_data.json`; forecaster loss stated honestly |
| T-06.2-15 | Tampering/Elevation (V12) | broken or non-repo-relative links | mitigate | link-check asserts each referenced committed file exists (`is_file()` + non-empty), repo-relative under root |
| T-06.2-16 | Info-Disclosure (self-contained) | external image/asset URL in README | mitigate | Mermaid is text (zero asset); assert no `![...](http...)` image links |
| T-06.2-17 | Availability (deploy) | dropping the HF Spaces front-matter | mitigate | front-matter preserved verbatim; test asserts `title`/`sdk`/`app_file` present |
</threat_model>

<verification>
- Per-task: run the task's `<automated>` command (feedback < 60s).
- Wave merge: `.venv/bin/python -m pytest tests/unit -q`.
- Honesty invariant: `grep -c Docling README.md` == 0; metrics traced to committed artifacts; forecaster loss + not-measured faithfulness stated honestly.
- Manual (retroactive, non-blocking): push branch, open `README.md` on GitHub, confirm the Mermaid architecture diagram renders as an SVG (VALIDATION.md manual-only â the link-check proves the block exists, not that GitHub renders it).
</verification>

<success_criteria>
- README.md is portfolio-grade, hybrid-shaped, with a Mermaid real-pipeline diagram (Docling absent) and honest headline metrics.
- Repo-relative links to the dashboard, model card, and `/failures` data resolve to committed files; HF front-matter preserved.
- The link-check test is green; full offline suite green.
</success_criteria>

<output>
Create `.planning/phases/06.2-portfolio-surfaces/06.2-04-SUMMARY.md` when done.
</output>
