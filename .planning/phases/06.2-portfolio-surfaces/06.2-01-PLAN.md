---
phase: 06.2-portfolio-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/failures/failures.yaml
  - ui/failures.py
  - pages/04_failures.py
  - ui/chrome.py
  - ui/copy.py
  - app/static/drhplens.css
  - pyproject.toml
  - tests/unit/test_failures.py
autonomous: true
requirements: [FAILGAL-01]
must_haves:
  truths:
    - "A recruiter can open /failures and see >=10 real, honestly-labelled failures across rag/extraction/forecast without running the app"
    - "Every failure renders read-only from a committed file with zero live LLM/agent/service call at render (P19)"
    - "open vs mitigated status renders in neutral muted mono (dashed border), never a red/green verdict color"
    - "The gallery is filterable/searchable render-only (by surface + free-text over already-loaded entries, no live call), honoring the ROADMAP/E6 'searchable' requirement"
    - "The footer 'Failure gallery' link points to the real /failures page, not the old /methodology stub"
  artifacts:
    - path: "eval/failures/failures.yaml"
      provides: ">=12 real failure entries (rag/extraction/forecast) with verifiable refs"
      contains: "surface:"
    - path: "ui/failures.py"
      provides: "render_failures() render-only module mirroring ui/eval_inline.py"
      exports: ["render_failures"]
      min_lines: 40
    - path: "pages/04_failures.py"
      provides: "Streamlit page shell mapping to /failures"
    - path: "tests/unit/test_failures.py"
      provides: "schema+count, AST-isolation, render (open+mitigated+empty), surface/text-filter tests"
  key_links:
    - from: "pages/04_failures.py"
      to: "ui.failures.render_failures"
      via: "import + call"
      pattern: "render_failures\\("
    - from: "ui/failures.py"
      to: "eval/failures/failures.yaml"
      via: "yaml.safe_load of committed file"
      pattern: "yaml\\.safe_load"
    - from: "ui/chrome.py"
      to: "/failures"
      via: "footer anchor href repoint"
      pattern: "href=\"/failures\""
---

<objective>
Ship the FAILGAL-01 `/failures` gallery as one complete render-only vertical slice: a committed
`eval/failures/failures.yaml` (>=12 real failures) → `ui/failures.py::render_failures()` →
`pages/04_failures.py` (URL `/failures`), pinned by schema+count, AST-import-isolation, and
neutral-render tests. Repoint the stale footer "Failure gallery" link to the real page and
declare the previously-transitive `pyyaml` dependency.

Purpose: Give a recruiter a browseable, honestly-labelled failure gallery that proves the
project surfaces its own weaknesses — the honesty thesis made concrete — without running anything.
Output: `eval/failures/failures.yaml`, `ui/failures.py`, `pages/04_failures.py`, `.drhp-fail-*`
CSS, chrome/copy strings, `pyyaml` declaration, and `tests/unit/test_failures.py`.
</objective>

## Phase Goal (MVP user story)

**As a** hiring manager / DS recruiter, **I want to** browse a `/failures` gallery of real,
honestly-labelled RAG/extraction/forecast failures, **so that** I can understand and trust the
system without running it. (Derived from the ROADMAP 6.2 goal + the SPEC goal line "so a hiring
manager can understand and trust the system without running it".)

This plan is one vertical slice: committed data → renderer → reachable page → tests, all landing
together so the `/failures` surface is user-reachable end-to-end when the plan completes.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-portfolio-surfaces/06.2-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-UI-SPEC.md
@.planning/phases/06.2-portfolio-surfaces/06.2-RESEARCH.md
@.planning/phases/06.2-portfolio-surfaces/06.2-PATTERNS.md
@.planning/phases/06.2-portfolio-surfaces/06.2-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare pyyaml, author failures.yaml (>=12 real entries), write the FAILGAL-01 test file (RED)</name>
  <files>pyproject.toml, eval/failures/failures.yaml, tests/unit/test_failures.py</files>
  <read_first>
    - RESEARCH §"Real Failure Sourcing" (06.2-RESEARCH.md, the 12-row table) — the exact real failures + committed `refs` to author from. Target 12 (SPEC floor >=10; discretion band 12-15).
    - RESEARCH §"Code Examples" (schema+count test, AST-isolation test, render `_RecordingSt` stub) and PATTERNS §"Test Pattern Assignments" (mirror `tests/unit/test_eval_inline.py`).
    - `tests/unit/test_eval_inline.py` — the literal template for the AST-isolation (`inspect.getsource` + allowlist) and value-invariant render tests.
    - `pyproject.toml` lines 8-42 — `[project].dependencies` block (starts line 13); pin site.
    - RESEARCH §Pitfall 5 (undeclared PyYAML) and §Package Legitimacy Audit (PyYAML APPROVED — no new install, declare only).
  </read_first>
  <action>
    Add `"pyyaml>=6,<7"` to `pyproject.toml [project].dependencies` (the block at line 13) — it is
    only transitively present today (Pitfall 5); do not add any other package.

    Author `eval/failures/failures.yaml` as a top-level YAML list of >=12 entries, each with the
    required keys `id, surface, category, query, expected, actual, postmortem, status, refs`.
    Source every entry from the RESEARCH §Real Failure Sourcing table (rows 1-12): `surface` in
    {rag, extraction, forecast} with all three present (the table gives rag>=1, extraction>=1,
    forecast>=1); `status` in {open, mitigated}; each `refs` cites the real committed path from the
    table (e.g. `model_card/MODEL_CARD.md`, `eval/reports/2026-07-29-rag-eval.md`,
    `eval/reports/2026-06-25-extraction-f1.md`, `tests/unit/test_walkforward_no_lookahead.py`).
    Author the exact `query/expected/actual/postmortem` prose honestly per the table (e.g. entry 1:
    forecast/calibration — model does NOT beat naive baselines, P9 gate FAIL, status `open`). Every
    `id` unique. Never spin a failure as a win. This data file is NOT scrubbed (failures describe
    real behavior with evaluative words) — keep evaluative copy OUT of `ui/copy.py`.

    Write `tests/unit/test_failures.py` with three tests, mirroring `test_eval_inline.py`:
    (a) `test_failures_yaml_schema_and_coverage` — `yaml.safe_load` the committed file; assert
    `len >= 10`, required-key set present per entry, `surface`/`status` valid, non-empty
    `postmortem`, unique `id`, and all three surfaces present.
    (b) `test_read_only_isolation_allowlist` — `ast.parse(inspect.getsource(failures_mod))` over the
    failures module; assert imports are a subset of `{__future__, html, json, pathlib, yaml,
    streamlit}`; assert none of the forbidden tokens (`openai, genai, instructor, groq, qdrant,
    .invoke(, langgraph, llama_index, from deepeval, import deepeval`) appear in source.
    (c) `test_render_neutral_no_live` — using a `_RecordingSt` stub whose `.container(*a,**k)`
    returns `contextlib.nullcontext()`, monkeypatch `failures_mod.st`, call `failures_mod.render_failures()`,
    join emitted markup and assert: only `.drhp-fail-*` (and `st-key-drhpcard-`) class tokens
    appear, no `style=`, no color/badge/emoji, an `open` and a `mitigated` entry render identically
    except the status text, and the empty-state path renders the muted `<p>` (no crash).
    (d) `test_render_surface_and_text_filter` — call `failures_mod.render_failures(surface="forecast")`
    and assert only forecast cards emit; call `render_failures(query=<substring of one entry>)` and
    assert the set narrows to matching entries; call with a no-match query and assert the muted
    empty-state renders (no crash). Backs the FAILGAL-01 "searchable" filter added in Task 2/Task 3.

    **[Blocker fix — deferred import ordering]** Tests (b), (c), and (d) all need `ui.failures`, which
    does NOT exist until Task 2. Import it **inside each test function body** — `import ui.failures as
    failures_mod` as the first line of the test — NOT at module top-level. This deliberately deviates
    from the literal `test_eval_inline.py` template (whose top-level `import ui.eval_inline` was safe
    only because that module already existed): a module-level import here raises `ModuleNotFoundError`
    at pytest **collection** time and fails the WHOLE file — including
    `test_failures_yaml_schema_and_coverage`, which must stay independently collectible and GREEN in
    Task 1. Test (a) imports only `yaml`/`pathlib` and stays at module level.
    Tests (b)/(c)/(d) will FAIL now (no `ui/failures.py`) — that is the intended RED for Task 2.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_failures.py::test_failures_yaml_schema_and_coverage -x` PASSES (data is authored).
    - `grep -c "pyyaml" pyproject.toml` >= 1; `python -c "import yaml"` succeeds.
    - `python -c "import yaml,pathlib; d=yaml.safe_load(pathlib.Path('eval/failures/failures.yaml').read_text()); print(len(d), sorted({e['surface'] for e in d}))"` prints a count >=12 and `['extraction', 'forecast', 'rag']`.
    - `tests/unit/test_failures.py` defines `test_read_only_isolation_allowlist`, `test_render_neutral_no_live`, and `test_render_surface_and_text_filter` (all RED until Task 2 — expected), each importing `ui.failures` INSIDE the function body (deferred), never at module top-level.
    - Collectibility proof (blocker guard): `pytest --collect-only tests/unit/test_failures.py` reports 0 collection errors, and `pytest tests/unit/test_failures.py::test_failures_yaml_schema_and_coverage -x` passes even though `ui/failures.py` does not yet exist.
    - No banned-token regression: `pytest tests/unit/test_copy_no_banned_tokens.py -x` still PASSES (no copy touched here).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_failures.py::test_failures_yaml_schema_and_coverage -x -q</automated>
  </verify>
  <done>failures.yaml has >=12 real entries covering all three surfaces with valid schema and unique ids; pyyaml is declared; the isolation+render tests exist and are RED pending the renderer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ui/failures.py render_failures() + .drhp-fail-* CSS + chrome copy (GREEN isolation & render)</name>
  <files>ui/failures.py, app/static/drhplens.css, ui/copy.py</files>
  <read_first>
    - `ui/eval_inline.py` (lines 22-33 imports/path; 43-68 `_faithfulness_display`/`_figure`; 88-111 `.is_file()`+try/except + html.escape posture) — the exact module to copy-adapt.
    - PATTERNS §"ui/failures.py" and §"Shared Patterns" (lifted-depth card hook `[class*="st-key-drhpcard-"]` at `app/static/drhplens.css:1358-1374`; HTML-escape-before-unsafe_allow_html; single CSS injection point).
    - `pages/02_snapshot.py:348-354` — the `st.container(border=True, key="drhpcard-...")` single-inner-markdown card call-site (never a split `<div>` — the Phase-3 white-bar lesson).
    - `06.2-UI-SPEC.md` §C2 (failure card: amber left-rule, surface chip, category, neutral dashed status pill, serif title, Expected|Actual two-col mono grid, POST-MORTEM label) + §Copywriting Contract (empty-state + status labels).
    - `ui/copy.py:163-170` (`METHODOLOGY_STUB_BODY`) + `:809-836` (import-time scrubber) — where chrome strings live and the Pitfall-4 banned-stem trap.
    - `app/static/drhplens.css:1747-1760` (existing `.drhp-arch*` — DO NOT reuse) and `:12` (Google-Fonts `@import` — irrelevant here, additive CSS only).
  </read_first>
  <action>
    Create `ui/failures.py` as a copy-adapt of `ui/eval_inline.py`: import only
    `from __future__ import annotations, html, pathlib.Path, yaml, streamlit as st` (add `yaml`;
    keep `json` only if used). Define
    `_FAILURES_PATH = Path(__file__).resolve().parents[1] / "eval" / "failures" / "failures.yaml"`.
    Implement `render_failures(surface: str | None = None, query: str | None = None) -> None`: guard
    with `_FAILURES_PATH.is_file()` + `try/except`, reading via `yaml.safe_load` (NEVER `yaml.load` —
    V12); on absent/unreadable/empty, emit the single muted empty-state `<p class="drhp-fail-empty">`
    using the UI-SPEC copy from `ui/copy.py` ("No failures loaded." / "Run the eval suite to populate
    `eval/failures/failures.yaml`.") and return.

    Apply the optional filters render-only over the already-loaded list (in-memory; NO live call — the
    "searchable" FAILGAL-01 mechanism): if `surface` is a real surface (not None / not "all"), keep
    entries where `e.get("surface") == surface`; if `query` is a non-empty string, keep entries where
    `query.strip().lower()` is a substring of any of `id/category/query/expected/actual/postmortem`
    (lowercased). If the filtered list is empty (a no-match search), emit the same muted empty-state
    `<p>` and return — a no-match search must not crash. Then, for each surviving entry, open
    `st.container(border=True, key=f"drhpcard-fail-{html.escape(str(e.get('id','')))}")`
    (the lifted-depth hook — `drhpcard-` prefix inherits the shadow/bevel at css:1358-1374) and emit
    ONE inner `st.markdown(..., unsafe_allow_html=True)` for the whole card. HTML-escape EVERY
    interpolated field (`surface, category, query, expected, actual, postmortem, status`) via
    `html.escape(str(...))` before markup (V5 / T-6.1-19). Map `status` to the UI-SPEC label
    (`OPEN` / `OPEN · BY DESIGN` / `MITIGATED`) rendered in muted mono with a dashed neutral border —
    never a color. Group/order cards by `surface`.

    Add ADDITIVE `.drhp-fail-*` rules to `app/static/drhplens.css` (append; never edit an existing
    selector): `.drhp-fail-card`, `.drhp-fail-chip` (surface pill, mono uppercase, bordered),
    `.drhp-fail-status` (dashed neutral border, muted — NOT colored), `.drhp-fail-title` (serif),
    `.drhp-fail-grid`/`.drhp-fail-expected`/`.drhp-fail-actual` (two-col mono; Expected neutral
    left-rule, Actual amber left-rule = attention, not "bad"), `.drhp-fail-postmortem` (muted body
    under a hairline, mono `POST-MORTEM` label), `.drhp-fail-empty` (muted `<p>`). Use only existing
    `:root --drhp-*` tokens. Do NOT introduce `.drhp-arch*` names (taken at 1747-1760).

    Add only the CHROME strings to `ui/copy.py` (keep evaluative failure content in the YAML, not
    here): the `FAILURE GALLERY` eyebrow, the "Where it breaks — and what I did about it" heading,
    and the empty-state heading/body — all UI-SPEC-verbatim (already scrubber-clean). Re-run the copy
    module import after editing (Pitfall 4).
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_failures.py::test_read_only_isolation_allowlist -x` PASSES (imports subset of allowlist; no agent/LLM token).
    - `pytest tests/unit/test_failures.py::test_render_neutral_no_live -x` PASSES (only `.drhp-fail-*`/`st-key-drhpcard-` classes; no `style=`/color/badge; open==mitigated except status text; empty-state renders).
    - `pytest tests/unit/test_failures.py::test_render_surface_and_text_filter -x` PASSES (surface + free-text filters narrow the rendered set in-memory; a no-match query renders the muted empty-state, no crash).
    - Source proof: `grep -c "yaml.safe_load" ui/failures.py` == 1 and `grep -c "yaml.load(" ui/failures.py` (unfiltered `yaml.load(` calls other than safe) == 0.
    - `grep -c "html.escape" ui/failures.py` >= 6 (every interpolated field escaped).
    - `pytest tests/unit/test_copy_no_banned_tokens.py -x` PASSES (chrome strings scrubber-clean).
    - CSS additive-only: `grep -v '^#' app/static/drhplens.css | grep -c "\\.drhp-fail-"` >= 6 and existing `.drhp-arch` selectors unchanged.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_failures.py tests/unit/test_copy_no_banned_tokens.py -x -q</automated>
  </verify>
  <done>render_failures() renders the committed gallery neutrally, escapes every value, uses yaml.safe_load, imports only the allowlist, and its .drhp-fail-* CSS + chrome copy are additive and scrubber-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pages/04_failures.py (URL /failures) + repoint footer link to /failures</name>
  <files>pages/04_failures.py, ui/chrome.py</files>
  <read_first>
    - `pages/03_how_it_works.py:1-25` (page-shell head: `st.set_page_config` FIRST, `# noqa: E402` deferred imports, non-idempotent `load_global_css` inject, `render_nav`, `init_session_state`) — the exact shell to copy.
    - PATTERNS §"pages/04_failures.py" (URL slug: `pages/NN_<name>.py` auto-maps to `/<name>`; no `st.navigation`/`st.Page` in this app; smoke-check `/failures` once — RESEARCH A1).
    - `ui/chrome.py` footer block (the `'<a href="/methodology" target="_self">Failure gallery</a>'` line — verified line 143 in PATTERNS) — repoint target.
    - `06.2-UI-SPEC.md` §Copywriting Contract (Failures section eyebrow `FAILURE GALLERY` / H "Where it breaks — and what I did about it").
  </read_first>
  <action>
    Create `pages/04_failures.py` mirroring the head of `pages/03_how_it_works.py`:
    `st.set_page_config(page_title="Failures · DRHPLens", page_icon=None, layout="centered",
    initial_sidebar_state="collapsed")` as the FIRST Streamlit call, then the `# noqa: E402`
    deferred imports of `load_global_css`, `render_nav`/`render_site_footer` (from `ui.chrome`),
    `init_session_state` (from `ui.state`), and `render_failures` (from `ui.failures`). Inject css via
    the non-idempotent `load_global_css(st.session_state)` path, `init_session_state`, emit
    `render_nav()`, render the UI-SPEC eyebrow/heading (`FAILURE GALLERY` / "Where it breaks — and
    what I did about it") from `ui/copy.py`, then a render-only filter row —
    `surface_sel = st.selectbox("Surface", ["all", "rag", "extraction", "forecast"], key="fail_surface")`
    and `q = st.text_input("Search failures", key="fail_query", placeholder="filter by keyword…")` —
    and call `render_failures(surface=None if surface_sel == "all" else surface_sel, query=q or None)`
    (the "searchable" FAILGAL-01 control; filtering happens in-memory in `render_failures`, no live
    call). Then `st.markdown(render_site_footer(), unsafe_allow_html=True)`. Import NOTHING from the
    agent/LLM/model pipeline (the page shell stays demo-safe like the other pages).

    In `ui/chrome.py`, repoint ONLY the footer "Failure gallery" anchor from
    `href="/methodology"` to `href="/failures"` (the real page now exists). Leave the "Model card"
    → `/methodology` anchor unchanged (the model card genuinely lives on /methodology).
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import ast; ast.parse(open('pages/04_failures.py').read())"` succeeds and `grep -c "render_failures(" pages/04_failures.py` == 1.
    - `grep -Ec "st\.selectbox|st\.text_input" pages/04_failures.py` >= 2 (the surface filter + free-text search controls are present — the "searchable" requirement).
    - `pages/04_failures.py` first Streamlit call is `st.set_page_config` (before any other `st.`): verified by inspection / import-order test if present.
    - `grep -c 'href="/failures"' ui/chrome.py` >= 1 and `grep -c 'Failure gallery</a>' ui/chrome.py` maps to the `/failures` href (no stale `Failure gallery` → `/methodology`).
    - Full offline unit suite green (no isolation regression): `.venv/bin/python -m pytest tests/unit -q`.
    - Manual smoke (post-merge, non-blocking): `streamlit run app.py` → navigate to `/failures` renders the gallery (RESEARCH A1 URL-slug confirmation).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit -q</automated>
  </verify>
  <done>/failures is a reachable Streamlit page rendering the gallery, the footer "Failure gallery" link points to /failures, and the full offline suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `failures.yaml` → renderer | author-controlled data file crosses into `st.markdown(unsafe_allow_html=True)` |
| renderer → recruiter browser | Streamlit sanitizer emits HTML to an unauthenticated public reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-01 | Tampering/Elevation (V5) | `ui/failures.py` interpolation into `unsafe_allow_html` | mitigate | `html.escape(str(...))` every field before markup; render test asserts no `style=`/raw-tag/color injection |
| T-06.2-02 | Tampering/Elevation (V12) | YAML parse of `failures.yaml` | mitigate | `yaml.safe_load` only (never `yaml.load`); read a committed repo path under root via `pathlib` |
| T-06.2-03 | Info-Disclosure/Elevation (P19) | render path importing agent/LLM/vector/model | mitigate | AST-import-allowlist test (`{__future__,html,json,pathlib,yaml,streamlit}`) + forbidden-token scan on `ui/failures.py` |
| T-06.2-04 | Compliance banned-token leak | new chrome strings in `ui/copy.py` | mitigate | import-time scrubber (`test_copy_no_banned_tokens.py`); failure content kept in un-scrubbed YAML by design |
| T-06.2-SC | Tampering (supply chain) | `pyyaml` declaration | mitigate | No new install — PyYAML already resolved 6.0.3 (RESEARCH §Package Legitimacy Audit: canonical, APPROVED); declaring only. No [ASSUMED]/[SUS] package → no blocking-human legitimacy gate required. |
</threat_model>

<verification>
- Per-task: run the task's `<automated>` command (feedback < 60s).
- Wave merge: `.venv/bin/python -m pytest tests/unit -q` (full offline suite; protects existing isolation tests).
- Honesty invariant: `.drhp-fail-status` renders `open`/`mitigated` in muted mono/dashed border, never color; no failure is spun as a win (data authored from the RESEARCH table honestly).
- Demo-safe: `ui/failures.py` AST-isolation green; `pages/04_failures.py` imports no pipeline module.
</verification>

<success_criteria>
- `/failures` renders >=10 (target 12) real failures across all three surfaces from `eval/failures/failures.yaml`, read-only, no live call.
- `ui/failures.py` passes the AST-import-isolation test (allowlist only) and escapes every value.
- open vs mitigated render identically except the status text (neutral, no verdict color).
- The gallery is filterable by surface + free-text search (render-only, in-memory) — satisfying the ROADMAP/E6 "searchable" requirement with no live call.
- Footer "Failure gallery" link points to `/failures`; `pyyaml` declared; full offline suite green.
</success_criteria>

<output>
Create `.planning/phases/06.2-portfolio-surfaces/06.2-01-SUMMARY.md` when done.
</output>
