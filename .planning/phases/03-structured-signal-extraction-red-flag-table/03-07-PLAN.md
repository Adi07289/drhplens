---
phase: 03-structured-signal-extraction-red-flag-table
plan: 07
type: execute
wave: 5
depends_on: ["03-03", "03-06"]
files_modified:
  - ui/snapshot_blocks.py
  - pages/02_snapshot.py
autonomous: false
requirements: [EXTRACT-01, EXTRACT-02, METHOD-01]
must_haves:
  truths:
    - "The snapshot page renders a Red-flag signals block: 7 stacked field rows in canonical order, monochrome tabular-nums values, confidence as neutral high/med/low text, NO red/green, NO badges (EXTRACT-01/02, L3-1/L3-2)"
    - "A not-disclosed field renders 'Not disclosed in DRHP' via the RefusalResponse path with the confidence label OMITTED — never a hidden row (L3-3/D3-03)"
    - "A field whose number failed cite_check grounding renders the blocked-copy, never an unsourced number (L3-9)"
    - "The IDF risk list is a SINGLE ranked list with a monochrome specificity meter + Issuer-specific/Industry-standard text labels, SUPERSEDING the Phase 2 prioritized-risk ordering — one risk list, not two (L3-4, UI-SPEC IA reconciliation)"
    - "Q&A answers and red-flag fields carry the cached-only Show-your-work methodology pane (METHOD-01)"
  artifacts:
    - path: "ui/snapshot_blocks.py"
      provides: "render_redflag_table + NEW render_idf_risk_list (specificity meter) + methodology-pane wiring; render_risk_block left UNCHANGED as the Phase-2 empty-state fallback"
      contains: "def render_redflag_table"
    - path: "pages/02_snapshot.py"
      provides: "red-flag block + IDF risk block wired into the locked page order; single risk list"
      contains: "render_redflag_table"
  key_links:
    - from: "ui/snapshot_blocks.py"
      to: "ui.methodology_pane.render_methodology_pane"
      via: "Show-your-work pane on each red-flag field + Q&A answer"
      pattern: "render_methodology_pane"
    - from: "ui/snapshot_blocks.py"
      to: "ui.chip.render_answer_with_chips"
      via: "red-flag field values are GroundedAnswers rendered by the UNCHANGED chip renderer"
      pattern: "render_answer_with_chips"
    - from: "pages/02_snapshot.py"
      to: "pipelines.redflag.load_redflag"
      via: "reads the cached RedFlagRecord (no request-time LLM)"
      pattern: "load_redflag"
---

<objective>
Wire the Phase 3 headline surfaces into the live snapshot page — the user-visible
payoff. Three UI deliverables, all rendering from the cached `RedFlagRecord`
(Plan 03) using the copy/CSS/pane foundation (Plan 06): (1) the Red-flag signals
table — 7 stacked field rows in canonical order, monochrome `tabular-nums` values
with inline citation chips, neutral `Confidence: high|medium|low` text, honest
`Not disclosed in DRHP` rows (confidence omitted, L3-3), and the numeric-gate
blocked-copy for ungrounded numbers (L3-9) — no red/green, no badges (EXTRACT-01/02);
(2) the IDF-ranked risk list — a SINGLE ranked list with the monochrome specificity
meter + `Issuer-specific`/`Industry-standard` text labels, which SUPERSEDES the
Phase 2 prioritized-risk ordering (one risk list, not two — UI-SPEC IA reconciliation,
L3-4); (3) the cached-only `Show your work` methodology pane on Q&A answers AND
red-flag fields (METHOD-01). All wired into `pages/02_snapshot.py` in the locked
order, reading the cache with zero request-time LLM calls.

Purpose: This is the phase goal made real — opening any covered IPO shows structured
red-flag signals above the fold with visible confidence + a show-your-work pane.
Output: extended `ui/snapshot_blocks.py` (red-flag table + IDF risk renderer +
pane wiring) and extended `pages/02_snapshot.py` (the two new blocks in the locked
IA, single risk list).

This plan is `autonomous: false`: it includes a `checkpoint:human-verify` for the
mobile-first visual contract (375px field-row stacking, no red/green, confidence
placement, single risk list) that cannot be fully asserted by pytest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-CONTEXT.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-UI-SPEC.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-PATTERNS.md
@ui/snapshot_blocks.py
@ui/chip.py
@ui/expander.py
@pages/02_snapshot.py
@agent/redflag_schema.py
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-03-SUMMARY.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: red-flag table + IDF risk renderer + methodology-pane wiring in snapshot_blocks (EXTRACT-01/02, METHOD-01)</name>
  <read_first>
    - 03-UI-SPEC.md §Visuals — Red-Flag Signal Table Contract (7 field rows, canonical order, tabular-nums values, confidence text-only beneath value, not-disclosed rows with confidence OMITTED, blocked-number copy, per-row citation expander + Show-your-work expander order, the anti-patterns: no red/green, no severity icon, no score-out-of-10)
    - 03-UI-SPEC.md §Visuals — IDF-Ranked Risk List Contract (single ranked list, `Risk {n} of {m} · {specificity}` counter, the `.drhp-spec-meter` construction, descending IDF order, no collapsed bucket, meter direction fixed) + §Information Architecture (the IDF list SUPERSEDES the Phase 2 risk-block ordering — single risk list)
    - ui/snapshot_blocks.py (the renderers to extend/copy: `_render_not_disclosed` :31-36 reused for L3-3 rows; `_render_expanders` :39-54 for citation chips; `render_split_bar` :103-151 — copy the accent-fill-on-neutral-track + `role="img"` aria grammar for `.drhp-spec-meter`; `render_risk_block` :229-264 — COPY its per-claim counter loop :252-261 into the NEW `render_idf_risk_list` (do NOT mutate or rename `render_risk_block`; it stays as the Phase-2 empty-state fallback), appending the specificity word + meter and ordering by idf_score; `render_answer_with_chips` reuse :76)
    - ui/methodology_pane.py (Plan 06 — `render_methodology_pane`; the query for a red-flag field is `REDFLAG_QUERIES[field_key]`)
    - ui/copy.py (Plan 06 — `REDFLAG_BLOCK_HEADING`, the 7 field labels, `CONFIDENCE_LABEL_TEMPLATE`, `FIELD_NUMERIC_GATE_BLOCKED`, `RISK_SPECIFICITY_COUNTER_TEMPLATE`, the specificity-word dict, `SPEC_METER_ARIA_TEMPLATE`, `FIELD_NOT_DISCLOSED_NOTE`)
    - agent/redflag_schema.py (Plan 01 — `RedFlagRecord.fields` (7 keys), `RedFlagField.value`/`confidence_tier`/`confidence_score`, `ranked_risks: list[RankedRisk]` with `idf_score`/`specificity_band`)
    - pipelines/redflag_queries.py (Plan 01 — `REDFLAG_QUERIES` for the pane's retrieval-query line)
    - 03-PATTERNS.md §`ui/snapshot_blocks.py (MODIFY)`
  </read_first>
  <action>
    Extend `ui/snapshot_blocks.py` with `render_redflag_table(record: RedFlagRecord) -> None`: render a `.drhp-redflag-table` block (heading `REDFLAG_BLOCK_HEADING` + sub-line) containing the 7 `.drhp-redflag-row` rows in the canonical `REDFLAG_FIELD_KEYS` order. For each field: render the field label (semibold muted) + the value. When `field.value` is a `GroundedAnswer`, render it via the UNCHANGED `render_answer_with_chips` (tabular-nums numeric values), then the per-row citation expanders via `_render_expanders`, then the `Confidence: {tier}` label (text only, no pill, no color) via `CONFIDENCE_LABEL_TEMPLATE`, then the `Show your work` pane via `render_methodology_pane(query=REDFLAG_QUERIES[field_key], grounded_answer=field.value, confidence_tier=field.confidence_tier, confidence_score=field.confidence_score)`. When `field.value` is a `RefusalResponse`, render `_render_not_disclosed()` with the confidence label OMITTED (L3-3/D3-03) — distinguish the numeric-gate-blocked case (render `FIELD_NUMERIC_GATE_BLOCKED` copy) from plain not-disclosed if the refusal carries the blocked reason. Every of the 7 rows ALWAYS renders (never hidden). Do NOT introduce any red/green color, badge, severity icon, or aggregate score.

    Add a NEW renderer `render_idf_risk_list(ranked_risks)` (do NOT modify or rename the existing `render_risk_block` — it stays UNCHANGED and is reused ONLY as the Phase-2 empty-state fallback in Task 2) that takes `record.ranked_risks` (already ordered by descending `idf_score`) and renders the SINGLE risk list: each item shows `RISK_SPECIFICITY_COUNTER_TEMPLATE.format(n=, m=, specificity=<word from band>)`, a `.drhp-spec-meter` (accent fill proportional to normalized `idf_score` on the neutral track, with the `role="img"` aria-label from `SPEC_METER_ARIA_TEMPLATE` + a text `{pct}%` label — copy `render_split_bar`'s construction), the cited risk text with chips, the citation expander, and optionally the Show-your-work pane. Meter direction fixed: more fill = more issuer-specific (never a danger verdict). No collapsed boilerplate bucket — industry-standard risks render lower in the SAME list, still cited (L3-4). Also wire `render_methodology_pane` onto Q&A answers (the METHOD-01 primary surface) by exposing a small helper the chat renderer can call, or document the single call-site in `render_snapshot_chat` to be added in Task 2.
  </action>
  <verify>
    <automated>python -c "import ui.snapshot_blocks as b; assert hasattr(b,'render_redflag_table')" && grep -v '^#' ui/snapshot_blocks.py | grep -Ec 'background:\s*#?(red|green|#dc2626|#16a34a|#B91C1C)|🚩|⚠'</automated>
  </verify>
  <acceptance_criteria>
    - `render_redflag_table` exists and renders all 7 fields in canonical order; a not-disclosed field renders the not-disclosed note with NO confidence label; a numeric-gate-blocked field renders `FIELD_NUMERIC_GATE_BLOCKED` (never an unsourced number).
    - `render_idf_risk_list` exists (a NEW function) and orders by descending `idf_score`, renders the monochrome `.drhp-spec-meter` with a text % label + the specificity word, and renders a SINGLE list (no collapsed bucket); the existing `render_risk_block` is NOT modified or renamed (its signature stays `render_risk_block(field: GroundedAnswer | RefusalResponse)`).
    - No red/green/severity-icon coding in the new renderers (grep gate above returns 0 for forbidden color/emoji tokens).
    - Red-flag field values + risk items render via the UNCHANGED `render_answer_with_chips` (`grep -c render_answer_with_chips ui/snapshot_blocks.py` increases; chip renderer not modified).
    - `render_methodology_pane` is invoked for red-flag fields (`grep -c render_methodology_pane ui/snapshot_blocks.py` ≥ 1).
  </acceptance_criteria>
  <done>The red-flag table (7 monochrome rows, confidence text, honest refusals, blocked-number copy) + the single IDF-ranked risk list with the specificity meter + the methodology-pane wiring all render from the cached record with no red/green and no live call.</done>
</task>

<task type="auto">
  <name>Task 2: wire the red-flag + IDF blocks into the snapshot page in locked IA order; reconcile to a single risk list (UI-SPEC IA)</name>
  <read_first>
    - 03-UI-SPEC.md §Information Architecture (the locked top-to-bottom order: breadcrumb, metadata header, NEW red-flag block high/above-the-fold, NEW IDF risk block REPLACING the Phase 2 risk-block ordering, remaining Phase 2 blocks, Q&A chat with pane, footer) + §Loading/Empty/Error States (empty red-flag record → empty-state copy; error → `.drhp-refusal` amber banner)
    - pages/02_snapshot.py (the existing render order :126-160 — `render_risk_block(record.fields.get("risks"))` :147 is the Phase 2 risk block to RECONCILE; the allow-list guard :92-97; the `load_snapshot` try/except :106-124; the chat call :167)
    - ui/snapshot_blocks.py (Task 1 — `render_redflag_table` + the IDF risk renderer)
    - pipelines/redflag.py (Plan 03 — `load_redflag`/`_redflag_path`; the cache read mirrors `load_snapshot`)
    - ui/copy.py (Plan 06 — the empty-state heading/body + error-state copy)
    - ui/snapshot_chat.py (the Q&A answer render site to attach the METHOD-01 pane on each answer — read it to find the per-answer render point)
  </read_first>
  <action>
    Extend `pages/02_snapshot.py` to load the cached `RedFlagRecord` via `load_redflag(drhp_id)` inside the same allow-list-guarded, try/except-wrapped block as `load_snapshot` (FileNotFoundError → empty-state copy; other Exception → the `.drhp-refusal` amber error banner — reuse the Phase 2 patterns; the red-flag table must never crash the page). Insert the blocks in the locked IA order: render `render_redflag_table(redflag_record)` HIGH on the page (above-the-fold-adjacent on mobile, after the metadata header and before/alongside the Phase 2 field blocks). RECONCILE the risk list to a SINGLE list: replace the Phase 2 `render_risk_block(record.fields.get("risks"))` call site with `render_idf_risk_list(redflag_record.ranked_risks)` (the IDF list SUPERSEDES the Phase 2 prioritized ordering — do NOT render two competing risk lists). Keep the UNCHANGED `render_risk_block` available as the empty-state fallback ONLY: guard with `if redflag_record.ranked_risks:` → `render_idf_risk_list(...)`, `else:` → `render_risk_block(record.fields.get("risks"))` so the page still shows a risk list when the IDF ranking is empty. Exactly ONE of the two renders at runtime. Keep the remaining Phase 2 blocks unchanged. Wire the METHOD-01 pane onto each Q&A answer at the `ui/snapshot_chat.py` answer-render point (the primary METHOD-01 surface). When no red-flag record exists for the drhp_id, render the empty-state copy in the red-flag block slot (never fabricated rows); the rest of the page still renders.
  </action>
  <verify>
    <automated>python -c "import ast; src=open('pages/02_snapshot.py').read(); ast.parse(src); assert 'render_redflag_table' in src and 'load_redflag' in src and 'render_idf_risk_list' in src; assert src.count('render_risk_block') <= 1, 'single risk list — render_idf_risk_list is primary; render_risk_block appears at most once, only as the empty-state fallback'"</automated>
  </verify>
  <acceptance_criteria>
    - `pages/02_snapshot.py` imports and calls `load_redflag` + `render_redflag_table`, guarded by the same allow-list + try/except as `load_snapshot` (a cache miss → empty-state copy, an error → amber `.drhp-refusal` banner, never an unhandled exception).
    - The red-flag block is rendered high in the page order (after the metadata header, before/with the Phase 2 field blocks).
    - There is a SINGLE risk list: `render_idf_risk_list(redflag_record.ranked_risks)` is the primary renderer; `render_risk_block` is called ONLY in the `else` (empty `ranked_risks`) branch as the fallback (assertion: at most one `render_risk_block` reference; `render_idf_risk_list` present; no two competing risk lists rendered at runtime).
    - The METHOD-01 pane is wired onto Q&A answers (a `render_methodology_pane` call reachable from the chat answer render).
    - `pytest -q` whole suite green (no import/collection regression from the page wiring).
  </acceptance_criteria>
  <done>The red-flag table + single IDF risk list are wired into the snapshot page in the locked IA order with empty/error states, the Phase 2 risk ordering is superseded by the IDF list, and the methodology pane is wired onto Q&A answers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: mobile-first visual verification of the Phase 3 surfaces (375px, no red/green, single list)</name>
  <what-built>
    The red-flag signals table, the single IDF-ranked risk list with the monochrome
    specificity meter, and the Show-your-work methodology pane are wired into
    `pages/02_snapshot.py`, rendering from the cached `RedFlagRecord`. This checkpoint
    confirms the visual/interaction contract that pytest cannot fully assert.
  </what-built>
  <how-to-verify>
    1. Run the app (`streamlit run app.py` or the project's run command) and open the snapshot page for the seeded IPO (e.g. `/snapshot?drhp_id=swiggy_2024_11`).
    2. At a 375px-wide viewport, confirm: the Red-flag signals block appears above-the-fold-adjacent; all 7 field rows render in canonical order; numeric values use tabular-nums and align; field rows stack (label, value, confidence) without horizontal scroll.
    3. Confirm NO red/green/amber on any value, label, row, or the specificity meter; confidence is plain `Confidence: high|medium|low` text (no pill, no color); no 🚩/⚠ icons; no aggregate "risk score" or badge.
    4. Confirm a not-disclosed field shows `Not disclosed in DRHP` with NO confidence label, and the row still renders (never hidden).
    5. Confirm there is exactly ONE risk list (the IDF-ranked list), ordered most-issuer-specific first, each item with a monochrome meter + a text `Issuer-specific`/`Industry-standard` label + `%`.
    6. Expand a `Show your work` pane on a red-flag field and on a Q&A answer; confirm it opens instantly (no spinner/live call) and shows query / chunks+scores / prompt / sources / eval scores (or the eval-not-yet-available note).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual issues (red/green leak, two risk lists, missing row, slow pane, horizontal scroll).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `drhp_id` query param → cached red-flag read | The page reads `data/redflag/<drhp_id>.json`; the id must be allow-list-checked before forming the path (reuse the Phase 2 guard) |
| cached red-flag prose / risk text → page HTML | Cached GroundedAnswer prose + risk spans are injected into the page; must be escaped and chip-rendered, never raw |
| methodology pane on Q&A answers → render | The pane on a live Q&A answer must read only cached/committed data, never trigger a live call on expand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `load_redflag` cache read path on the page | mitigate | The page already allow-list-guards `drhp_id` via `is_known_drhp_id` BEFORE any load (Phase 2, `pages/02_snapshot.py:92-97`); the red-flag read sits inside the same guard — no arbitrary id reaches a cache path. |
| T-03-13 | Tampering / XSS | cached red-flag prose + risk text in page HTML | mitigate | Values render via the UNCHANGED `render_answer_with_chips` (Phase 1 HTML-escaped chip renderer) and `_html.escape` on labels/snippets; no raw cached text is injected with `unsafe_allow_html`. |
| T-03-11 | Information disclosure | methodology pane on Q&A + red-flag fields | mitigate | The pane (Plan 06) is cached-only with a no-LLM-import test; wiring it here adds no live call (the page makes no request-time agent call for the red-flag table). |
| T-03-14 | (UX-as-advice, P21) | inverted/colored specificity meter implying danger | mitigate | Meter is monochrome accent-magnitude only, direction fixed (more fill = more issuer-specific), accompanied by a text % + word; no red/green — verified at the visual checkpoint and the no-forbidden-color grep gate (Task 1). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package — Streamlit + already-present `ui/*` modules only. |
</threat_model>

<verification>
- `pytest -q` whole suite green (page + blocks wiring; no collection regression).
- `render_redflag_table` present; single-risk-list assertion passes; no-forbidden-color grep gate returns 0.
- (Checkpoint) mobile-first visual contract confirmed at 375px: 7 rows, no red/green, single IDF list, instant cached pane.
</verification>

<success_criteria>
- Red-flag signals table renders 7 monochrome rows with neutral confidence text, honest refusals, and blocked-number copy — no red/green, no badges.
- A single IDF-ranked risk list with a monochrome specificity meter supersedes the Phase 2 risk ordering.
- The Show-your-work methodology pane is wired on Q&A answers + red-flag fields, cached-only.
- Wired into the snapshot page in the locked IA order with empty/error states; whole suite green.
</success_criteria>

<output>
Create `.planning/phases/03-structured-signal-extraction-red-flag-table/03-07-SUMMARY.md` when done.
</output>
