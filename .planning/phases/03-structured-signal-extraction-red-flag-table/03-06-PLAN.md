---
phase: 03-structured-signal-extraction-red-flag-table
plan: 06
type: execute
wave: 4
depends_on: ["03-01", "03-03"]
files_modified:
  - ui/copy.py
  - app/static/drhplens.css
  - ui/methodology_pane.py
  - tests/unit/test_methodology_pane.py
autonomous: true
requirements: [METHOD-01, EXTRACT-02]
must_haves:
  truths:
    - "Every new Phase 3 user-facing string lives in ui/copy.py under the import-time scrubber assertion (L3-8) — no banned token reaches production"
    - "The single app/static/drhplens.css gains monochrome .drhp-redflag-*, .drhp-confidence-label, .drhp-spec-meter*, .drhp-methodology-* classes — no new color, no red/green, accent reused only for the meter fill"
    - "The methodology pane renders entirely from cached claim_id trace + the latest committed eval report — NO live LLM/Qdrant call on expand (L3-6/D3-17)"
    - "The numeric confidence score (0.00-1.00) surfaces ONLY inside the pane, never in the up-front row (L3-2/D3-02)"
  artifacts:
    - path: "ui/copy.py"
      provides: "Phase 3 scrubber-guarded copy: red-flag headings, confidence labels, specificity words, Show your work, pane labels"
      contains: "Show your work"
    - path: "app/static/drhplens.css"
      provides: ".drhp-redflag-table/.drhp-redflag-row/.drhp-confidence-label/.drhp-spec-meter*/.drhp-methodology-* rules"
      contains: ".drhp-redflag-row"
    - path: "ui/methodology_pane.py"
      provides: "render_methodology_pane(...) — cached-only Show-your-work expander; no live call"
      contains: "def render_methodology_pane"
  key_links:
    - from: "ui/methodology_pane.py"
      to: "ui/expander.py"
      via: "reuses render_citation_expanders metadata_footer for the Sources cited line"
      pattern: "from ui.expander import"
    - from: "ui/methodology_pane.py"
      to: "eval/reports/"
      via: "reads the latest committed eval report for the Eval scores line (not recomputed)"
      pattern: "eval/reports"
    - from: "ui/copy.py"
      to: "compliance.scrubber"
      via: "import-time scrubber assertion over every Phase 3 string"
      pattern: "_scrub"
---

<objective>
Build the Phase 3 UI FOUNDATION the rendering plan (Plan 07) wires in: (1) all new
user-facing copy added to `ui/copy.py` under its import-time banned-token scrubber
assertion (L3-8) — red-flag headings/sub-lines, the `Confidence: high|medium|low`
labels, the specificity words, `Show your work`, the methodology-pane labels, and
the empty/error/blocked-number states; (2) the new monochrome CSS classes in the
single `app/static/drhplens.css` (`.drhp-redflag-table`, `.drhp-redflag-row`,
`.drhp-confidence-label`, `.drhp-spec-meter*`, `.drhp-methodology-*`) reusing the
inherited tokens — no new color, accent only for the meter fill (L3-1); (3) the
`ui/methodology_pane.py` component — a cached-only `Show your work` expander that
renders query / chunks+scores / prompt / sources / committed-eval-scores with NO
live LLM or Qdrant call (METHOD-01 / D3-17 / Pitfall 5), surfacing the numeric
confidence score only here (L3-2).

Purpose: Isolating copy + CSS + the no-live-call pane into its own plan keeps the
scrubber assertion, the single-CSS-source invariant, and the no-LLM-import gate
clean and independently testable before Plan 07 wires them into the page.
Output: extended `ui/copy.py`, extended `app/static/drhplens.css`, new
`ui/methodology_pane.py`, and `tests/unit/test_methodology_pane.py` flipped from
skip to green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-CONTEXT.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-UI-SPEC.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-PATTERNS.md
@ui/copy.py
@ui/expander.py
@app/static/drhplens.css
@agent/redflag_schema.py
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-01-SUMMARY.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 3 scrubber-guarded copy + monochrome CSS classes (L3-1/L3-8)</name>
  <read_first>
    - 03-UI-SPEC.md §Copywriting Contract (the EXACT strings: `Red-flag signals`, the red-flag sub-line, the 7 field labels in canonical order, `Confidence: high|medium|low`, the confidence-rubric line, `Not disclosed in DRHP`, the numeric-gate-blocked copy `Could not ground this number to a cited DRHP page, so it is not shown.`, `Risk factors, ranked by how specific they are`, the risk sub-line, `Risk {n} of {m} · {specificity}`, the specificity words, the meter aria-label, `Show your work`, the 5 pane labels, the eval-provenance + eval-not-available notes, empty/error states)
    - 03-UI-SPEC.md §Streamlit-Specific Constraints "New CSS classes to add" + §Color + §Typography + §Spacing + §Mobile Responsive Contract (the exact classes, monochrome rules, tabular-nums on values, the 4 breakpoints, 44px touch targets)
    - ui/copy.py (the import-time scrubber block :244-305; `_SAMPLE_FORMAT_VALUES` :260-274 — add new format keys `specificity`, `pct`, `confidence_tier`, `n`, `m`; existing anchors `FIELD_NOT_DISCLOSED_NOTE` :222, `RISK_COUNTER_TEMPLATE` :219, `SPLIT_BAR_*` :210-217 to add beside)
    - app/static/drhplens.css (the `:root` custom-property tokens :8-25; `.drhp-snapshot-block` chrome; `.drhp-split-bar*` :401-440 — copy its accent-fill-on-neutral-track grammar for the spec meter; `.drhp-risk-item*` :494-507; `.drhp-not-disclosed` :507; `.drhp-snippet*` :208-227 reused by the pane; the existing breakpoint media queries)
    - 03-PATTERNS.md §Shared Patterns "Banned-token scrubber on ALL new copy" + the `ui/copy.py` format-key note
  </read_first>
  <action>
    Extend `ui/copy.py` with the Phase 3 strings VERBATIM from the UI-SPEC Copywriting Contract (do NOT paraphrase — scrubber-clean copy stability is load-bearing): `REDFLAG_BLOCK_HEADING`, `REDFLAG_BLOCK_SUBLINE`, the 7 field-label constants in canonical order, `CONFIDENCE_LABEL_TEMPLATE` (`Confidence: {confidence_tier}`), `CONFIDENCE_RUBRIC_LINE`, `FIELD_NUMERIC_GATE_BLOCKED` (the L3-9 blocked-copy), `RISK_BLOCK_HEADING`, `RISK_BLOCK_SUBLINE`, `RISK_SPECIFICITY_COUNTER_TEMPLATE` (`Risk {n} of {m} · {specificity}`), the three specificity words (`Issuer-specific` / `Mostly issuer-specific` / `Industry-standard`), `SPEC_METER_ARIA_TEMPLATE`, `METHODOLOGY_TRIGGER` (`Show your work`), the 5 pane labels (`Retrieval query`, `Retrieved chunks (with scores)`, `Prompt used`, `Sources cited`, `Eval scores (from the latest committed report)`), the eval-provenance + eval-not-available notes, and the empty/error states. Add the new format keys (`specificity`, `pct`, `confidence_tier`, `n`, `m`) to `_SAMPLE_FORMAT_VALUES` so the sample-substituted scrubber assertion covers the templated forms. Reuse the existing `FIELD_NOT_DISCLOSED_NOTE` for not-disclosed rows (do NOT duplicate it). The mapping from `specificity_band` enum (`issuer_specific`/`mostly_issuer_specific`/`industry_standard`) to the three display words is a constant dict here.

    Extend `app/static/drhplens.css` (the SINGLE CSS source — never inline `<style>` elsewhere) with: `.drhp-redflag-table` (mirror `.drhp-snapshot-block` chrome), `.drhp-redflag-row` (field-row label/value layout + `border-subtle` separator + responsive stacking under the existing 4 breakpoint media queries), `.drhp-confidence-label` (Small 12 muted text, NO pill, NO color), `.drhp-spec-meter` / `.drhp-spec-meter-fill` / `.drhp-spec-meter-label` (copy the `.drhp-split-bar` accent-fill-on-`surface-secondary`-track grammar — accent `var(--drhp-accent)` fill is a magnitude indicator only, NOT red/green; meter carries a text % label), and `.drhp-methodology-pane` / `.drhp-methodology-code` (the `surface-secondary` scrollable prompt/chunk panel). Use ONLY the inherited `--drhp-*` tokens; add NO new color hex. Ensure red-flag numeric values inherit `font-variant-numeric: tabular-nums` (UI-SPEC Typography) and the `Show your work` expander header + chips keep the 44×44px tap target.
  </action>
  <verify>
    <automated>python -c "import ui.copy as c; assert c.METHODOLOGY_TRIGGER == 'Show your work'; assert 'high' in c.CONFIDENCE_LABEL_TEMPLATE.format(confidence_tier='high'); assert c.REDFLAG_BLOCK_HEADING and c.RISK_BLOCK_HEADING" && python -c "css=open('app/static/drhplens.css').read(); assert all(k in css for k in ['.drhp-redflag-table','.drhp-redflag-row','.drhp-confidence-label','.drhp-spec-meter','.drhp-methodology-pane'])"</automated>
  </verify>
  <acceptance_criteria>
    - `import ui.copy` succeeds (the import-time scrubber assertion passes — no banned token in any new Phase 3 string, including sample-substituted templates); `METHODOLOGY_TRIGGER == 'Show your work'`.
    - `app/static/drhplens.css` contains all of `.drhp-redflag-table`, `.drhp-redflag-row`, `.drhp-confidence-label`, `.drhp-spec-meter`, `.drhp-methodology-pane` (assertion above exits 0).
    - No new color hex is introduced (`grep -Eo '#[0-9A-Fa-f]{6}' app/static/drhplens.css | sort -u` introduces no hex absent from the Phase 1/2 palette; confidence labels + specificity words are muted text only).
    - `FIELD_NOT_DISCLOSED_NOTE` is reused for not-disclosed rows (not duplicated); the numeric-gate-blocked copy constant exists.
    - No inline `<style>` is added anywhere outside `app/static/drhplens.css` (single CSS source preserved).
  </acceptance_criteria>
  <done>All Phase 3 copy added under the scrubber assertion and all new monochrome CSS classes added to the single stylesheet, with no new color and no red/green coding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cached-only methodology pane component (METHOD-01 / D3-17 / Pitfall 5)</name>
  <read_first>
    - 03-UI-SPEC.md §Visuals — Methodology Pane Contract (the 5 pane lines top-to-bottom; the eval-not-yet-committed state; "no live LLM call"; numeric confidence score surfaces ONLY here)
    - 03-PATTERNS.md §`ui/methodology_pane.py` (the role-mirror: reuse `ui/expander.py` `render_citation_expanders` + `metadata_footer`; the source-of-each-pane-line table; "No LLM/Qdrant import in this module")
    - ui/expander.py (`render_citation_expanders` returns the per-claim descriptor incl. `metadata_footer` :70-72, `snippet`, `source_url`, `page_start`, `section` — reuse verbatim for the Sources cited + Retrieved chunks lines)
    - agent/schemas.py (`GroundedAnswer.claims[].sources[]` → `RetrievedChunkRef.score`/`.section`/`.verbatim_span` — the cached chunk scores the pane shows; do NOT fetch live)
    - agent/redflag_schema.py (Plan 01 — `RedFlagField.confidence_score`/`confidence_tier`, surfaced only in the pane)
    - pipelines/redflag_queries.py (Plan 01 — `REDFLAG_QUERIES[field_key]` IS the retrieval-query line for a red-flag field; for Q&A it is the user question)
    - tests/unit/test_methodology_pane.py (Plan 01 stub: `test_pane_renders_from_cache`, `test_no_llm_or_qdrant_import`) + tests/unit/test_cite_check.py::test_no_llm_judge_fallback :141 (the `inspect.getsource` substring-check pattern to mirror) + tests/eval/conftest.py (`synthetic_redflag_record`)
    - scripts/run_eval.py (the dated `eval/reports/<date>-*.md` filename shape the pane parses for the latest committed eval scores)
  </read_first>
  <behavior>
    - `render_methodology_pane` renders the 5 lines (retrieval query, retrieved chunks + scores, prompt used, sources cited, eval scores) entirely from a cached `GroundedAnswer` + the field's known query constant + the latest committed `eval/reports/*.md` — no argument is a live client.
    - When no committed eval report entry exists for the field/answer, the pane renders the eval-not-yet-available copy and still renders the other four lines from the cached trace.
    - The numeric confidence score (0.00-1.00) + its rubric tier appear inside the pane (and nowhere else).
    - `inspect.getsource(ui.methodology_pane)` contains no LLM/Qdrant client import (mirror `test_no_llm_judge_fallback`).
  </behavior>
  <action>
    Create `ui/methodology_pane.py` with `render_methodology_pane(*, query: str, grounded_answer: GroundedAnswer, prompt_path: str = "agent/prompts/generate.md", confidence_tier: str | None = None, confidence_score: float | None = None, eval_report_dir: str = "eval/reports") -> None`. It opens a single `st.expander(METHODOLOGY_TRIGGER, expanded=False)` and renders, top to bottom, using the Phase 3 copy labels from `ui/copy.py`: (1) the retrieval query string; (2) retrieved chunks with scores — reuse `ui.expander.render_citation_expanders(grounded_answer, chip_map)` descriptors for the per-source `score`/`section`/`snippet` (read from the cached `claims[].sources[]`); (3) the prompt text read from the static `prompt_path` file; (4) sources cited — reuse the `metadata_footer` from the same descriptors; (5) eval scores — call a small helper `latest_eval_scores(eval_report_dir)` that reads the most-recently-dated `eval/reports/*.md` and extracts the answer/field-level faithfulness + citation-accuracy, plus the `confidence_score`/`confidence_tier` passed in; if no report/entry exists, render the eval-not-yet-available copy. The module MUST import NO LLM/Qdrant client and make NO graph call (pure render over cached/committed data). Fill `tests/unit/test_methodology_pane.py`: `test_pane_renders_from_cache` (build the pane from `synthetic_redflag_record`'s GroundedAnswer field + a tmp eval report, assert the 5 labels render without a live call) and `test_no_llm_or_qdrant_import` (`inspect.getsource(ui.methodology_pane)` contains none of `openai`/`genai`/`instructor`/`groq`/`qdrant`/`GRAPH.invoke`).
  </action>
  <verify>
    <automated>pytest tests/unit/test_methodology_pane.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_methodology_pane.py::test_pane_renders_from_cache` and `::test_no_llm_or_qdrant_import` PASS (flipped from skip).
    - `grep -v '^#' ui/methodology_pane.py | grep -Ec 'import (openai|genai|instructor|groq|qdrant)|GRAPH.invoke'` returns 0 (no live call on expand — Pitfall 5).
    - The pane reuses `ui.expander.render_citation_expanders` / `metadata_footer` (no re-implemented citation rendering): `grep -c 'from ui.expander import' ui/methodology_pane.py` ≥ 1.
    - With no committed eval report present, the pane renders the eval-not-yet-available copy and still renders the query/chunks/prompt/sources lines.
    - The numeric `confidence_score` is rendered only inside this pane (not exported into any up-front-row helper).
  </acceptance_criteria>
  <done>A cached-only Show-your-work expander renders all five methodology lines from cached trace + the latest committed eval report, surfaces the numeric confidence only here, and makes zero live LLM/Qdrant calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new UI copy → rendered page | Every new user-facing string must pass the banned-token scrubber before it can be displayed (SEBI RIA boundary) |
| cached prompt text / DRHP snippet → pane render | Cached prompt + chunk text is injected into HTML; must be escaped and must not trigger a live call |
| eval report file → pane eval-scores line | The pane reads a committed markdown report off disk; a missing/malformed report must degrade to the not-available copy, not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information disclosure (compliance / SEBI RIA) | new Phase 3 copy | mitigate | All new strings land in `ui/copy.py` under the import-time scrubber assertion (L3-8), including sample-substituted templates; a banned token raises `AssertionError` at import, before any request is served. |
| T-03-11 | Information disclosure | methodology pane exposing internal prompt/trace | mitigate | The pane renders developer-authored prompt text + cited DRHP spans (already public, already escaped via `ui/expander`); it exposes NO secret and makes NO live call (Pitfall 5), pinned by `test_no_llm_or_qdrant_import`. |
| T-03-12 | Tampering / DoS | XSS via cached snippet/prompt text in pane HTML | mitigate | Reuse `ui/expander.py`'s `_html.escape` on all snippet/metadata strings; prompt text rendered inside an escaped `.drhp-methodology-code` block; no `unsafe_allow_html` on un-escaped cached text. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package — Streamlit + stdlib + already-present `ui/expander`/`compliance.scrubber` only. |
</threat_model>

<verification>
- `pytest tests/unit/test_methodology_pane.py -q` green (flipped from skip).
- `import ui.copy` passes the scrubber assertion (no banned token in Phase 3 copy).
- `pytest -q` whole suite green.
- no-LLM-import grep gate on the pane returns 0; single-CSS-source preserved.
</verification>

<success_criteria>
- All Phase 3 copy under the import-time scrubber assertion; monochrome CSS in the single stylesheet; no new color, no red/green.
- Methodology pane renders the 5 lines from cached trace + committed report with zero live calls.
- Numeric confidence surfaces only inside the pane.
</success_criteria>

<output>
Create `.planning/phases/03-structured-signal-extraction-red-flag-table/03-06-SUMMARY.md` when done.
</output>
