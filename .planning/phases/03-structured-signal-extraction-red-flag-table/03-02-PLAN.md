---
phase: 03-structured-signal-extraction-red-flag-table
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - agent/nodes/cite_check.py
  - pipelines/confidence.py
  - tests/unit/test_numeric_grounding.py
  - tests/unit/test_confidence_rubric.py
autonomous: true
requirements: [EVAL-03, EXTRACT-02]
must_haves:
  truths:
    - "A number expressed as '₹11,247 crore' grounds against a source written '1,12,470 lakh' (unit + tolerance reconciliation), not a false-fail"
    - "A number that traces to no cited DRHP span fails grounding and is blockable (never rendered as an unsourced fact)"
    - "Confidence tier is derived deterministically with no LLM call: verbatim→high, light-parse/aggregation→medium, cross-section→low"
    - "A not-disclosed field (RefusalResponse) carries no confidence tier"
  artifacts:
    - path: "agent/nodes/cite_check.py"
      provides: "per-number unit-aware + tolerance grounding extending _numbers_subset"
      contains: "def _numbers_subset"
    - path: "pipelines/confidence.py"
      provides: "deterministic classify_confidence(ga) -> (tier, score)"
      contains: "def classify_confidence"
  key_links:
    - from: "pipelines/confidence.py"
      to: "agent/nodes/cite_check.py"
      via: "reuses _normalize + _extract_numbers (one normalization path)"
      pattern: "from agent.nodes.cite_check import"
    - from: "agent/nodes/cite_check.py"
      to: "agent/policies.py"
      via: "NUMERIC_GROUNDING_REL_TOLERANCE constant"
      pattern: "NUMERIC_GROUNDING_REL_TOLERANCE"
---

<objective>
Deliver the deterministic truth-grounding core of Phase 3 — the highest-value
extension in the phase. Two deterministic, zero-LLM pieces: (1) extend the
existing non-LLM `cite_check` numeric antibody so per-number grounding is
**unit-aware (lakh/crore/₹) + tolerance-reconciled** (EVAL-03 / D3-10), fixing
the exact-string false-fail that would otherwise sink the 0.95 gate on
legitimate Indian-numeral normalizations; and (2) the deterministic
source-grounding confidence rubric (EXTRACT-02 / D3-01) that anchors high/med/low
to the already-computed cite_check result and the claim's verbatim span.

Purpose: Numbers are grounded or blocked before they ever reach a cache or a
user (the phase's core security-of-truth control). Confidence becomes a measured,
defensible signal — not an LLM self-report.
Output: extended `agent/nodes/cite_check.py`, new `pipelines/confidence.py`, and
two Wave 0 test modules flipped from skip → green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-CONTEXT.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-PATTERNS.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-RESEARCH.md
@agent/nodes/cite_check.py
@agent/schemas.py
@agent/policies.py
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit-aware + tolerance per-number grounding in cite_check (D3-10)</name>
  <read_first>
    - agent/nodes/cite_check.py (the EXACT extension point: `_normalize` :34-45, `_extract_numbers` :53-61 already strips Indian commas, `_numbers_subset` :64-71 exact-string subset — the false-fail risk; the consuming grounding loop :146-155; the SKELETON §D "NO LLM client import" invariant :10-13)
    - agent/policies.py (read `NUMERIC_GROUNDING_REL_TOLERANCE` added in Plan 01; the constant-with-calibration-comment posture)
    - 03-RESEARCH.md §Pattern 6 + §Pitfall 1 (lakh×1e5 / crore×1e7 / million×1e6 reconciliation; the "₹11,247 crore" vs "1,12,470 lakh" worked example; test on real Swiggy figures revenue 11,247 cr / OFS 4,499 cr)
    - 03-PATTERNS.md §`agent/nodes/cite_check.py (MODIFY)` (line-numbered excerpt of the code to extend)
    - tests/unit/test_cite_check.py (existing cite-check test style)
    - tests/unit/test_numeric_grounding.py (the Plan 01 stub with locked test names to fill)
  </read_first>
  <behavior>
    - "₹11,247 crore" in a claim grounds against a window containing "1,12,470 lakh" (crore↔lakh unit shift, within tolerance) → grounded.
    - "11247" grounds against "11247.0" and "11,247" (decimal/comma formatting) → grounded.
    - "₹11,247 crore" vs window number "9,500 crore" (genuinely different magnitude beyond tolerance) → NOT grounded.
    - A claim number with no matching window number at any unit scale → NOT grounded (blockable).
    - Non-numeric claims still trivially pass (no regression to existing cite_check behavior).
  </behavior>
  <action>
    Extend `agent/nodes/cite_check.py` in place — do NOT add a second normalizer and do NOT add any LLM import (the `inspect.getsource` invariant test guards this). Add a helper that maps a number + adjacent unit word/symbol to a canonical magnitude: detect `lakh`/`lac` (×1e5), `crore`/`cr` (×1e7), `million`/`mn` (×1e6), `billion`/`bn` (×1e9) tokens neighbouring an extracted number, and the `₹`/`%` markers; expand `_extract_numbers` (or add a sibling `_extract_scaled_numbers`) to return canonical float magnitudes alongside the raw string tokens. Replace the exact-string `claim_numbers.issubset(window_numbers)` decision in `_numbers_subset` with a tolerance match: a claim number is grounded iff some window number matches at the same OR a reconcilable unit scale within `abs(claim - window)/max(window, 1) <= NUMERIC_GROUNDING_REL_TOLERANCE` (imported from `agent/policies.py`). Preserve the early-return-True for claims with no numbers. Keep the existing exact-string path as a fast first check (exact match short-circuits to grounded) so existing green cite-check tests do not regress. Ensure a claim whose every emitted number fails reconciliation is reported as ungrounded by the existing grounding loop (the field then becomes a blocked RefusalResponse downstream — wired in Plan 03/04).
  </action>
  <verify>
    <automated>pytest tests/unit/test_numeric_grounding.py tests/unit/test_cite_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_numeric_grounding.py::test_lakh_crore_reconciles`, `::test_rupee_symbol_tolerance`, `::test_ungrounded_number_blocked` all PASS (flipped from skip).
    - All pre-existing `test_cite_check.py` tests still PASS (no regression to span/token-ratio behavior).
    - `grep -v '^#' agent/nodes/cite_check.py | grep -Ec 'import (openai|genai|instructor|groq)'` returns 0 (LLM-free invariant preserved).
    - `NUMERIC_GROUNDING_REL_TOLERANCE` is imported from `agent.policies` (not hard-coded in the node).
  </acceptance_criteria>
  <done>Per-number grounding reconciles lakh/crore/₹/million units within the policy tolerance, blocks genuinely-ungrounded numbers, and regresses nothing; cite_check stays LLM-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic source-grounding confidence rubric (D3-01)</name>
  <read_first>
    - 03-PATTERNS.md §`pipelines/confidence.py` (the rubric definition + "reuse cite_check primitives, do not re-implement normalization")
    - agent/nodes/cite_check.py (`_normalize`, `_extract_numbers` — the primitives to import; the `token_set_ratio` usage to optionally derive a score)
    - agent/schemas.py (`GroundedAnswer.claims[]`, `Claim.verbatim_span`, `Claim.sources[]`, `RetrievedChunkRef.section` — the inputs the rubric reads)
    - pipelines/snapshot.py::compute_ofs_fresh (:174-273 — the "deterministic post-extraction derive over claims, no verdict field" posture to mirror)
    - tests/unit/test_confidence_rubric.py (Plan 01 stub with locked test names)
  </read_first>
  <behavior>
    - A GroundedAnswer whose emitted value appears verbatim (after `_normalize`) inside the cited `verbatim_span` → tier `high`.
    - A GroundedAnswer whose numeric value is a transformation/aggregation of source numbers (reconcilable via `_extract_numbers` but not a verbatim substring) → tier `medium`.
    - A GroundedAnswer whose support spans ≥2 `sources[]` with different `.section` values → tier `low`.
    - A RefusalResponse input → returns no tier (None) and no score (D3-03 — absence is not low-confidence).
  </behavior>
  <action>
    Create `pipelines/confidence.py` with a pure, LLM-free `classify_confidence(ga: GroundedAnswer) -> tuple[Literal["high","medium","low"], float]` plus a thin wrapper that returns `(None, None)` for a `RefusalResponse` (so the precompute loop in Plan 03 can call one function per field). Import `_normalize` and `_extract_numbers` from `agent.nodes.cite_check` — do NOT re-implement normalization (one path across cite-check/eval/IDF/confidence). Rubric: (high) emitted value string is a normalized substring of the cited `claim.verbatim_span`; (medium) the value's numbers reconcile with source numbers but are not a verbatim substring (a transformation/aggregation); (low) the claim's `sources[]` cover ≥2 distinct `.section` values (cross-section inference). Map tier→representative score using policy-anchored constants (high≈0.9 / medium≈0.7 / low≈0.5) OR derive from the cite_check `token_set_ratio`; document the chosen mapping in the module docstring. The score is surfaced only in the methodology pane (D3-02) — never in the up-front row.
  </action>
  <verify>
    <automated>pytest tests/unit/test_confidence_rubric.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_confidence_rubric.py::test_verbatim_is_high`, `::test_light_parse_is_medium`, `::test_cross_section_is_low`, `::test_absence_has_no_tier` all PASS (flipped from skip).
    - `classify_confidence` makes no LLM/Qdrant call: `grep -v '^#' pipelines/confidence.py | grep -Ec 'import (openai|genai|instructor|groq|qdrant)'` returns 0.
    - The rubric imports `_normalize`/`_extract_numbers` from `agent.nodes.cite_check` (single normalization path).
    - A `RefusalResponse` passed to the wrapper returns `(None, None)`.
  </acceptance_criteria>
  <done>Deterministic confidence rubric classifies high/medium/low from cite-check primitives + section spread, returns no tier for absence, and makes zero LLM calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM answer prose → cite_check numeric gate | LLM-emitted numbers are untrusted until grounded against retrieved DRHP windows |
| extracted number → unit reconciliation | A claim number + its unit word must be canonicalized before magnitude comparison (mis-scaling = false grounding) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information disclosure / Repudiation | per-number grounding in `cite_check` | mitigate | A number that grounds at no reconcilable unit scale within tolerance is reported ungrounded → downstream blocks it as a RefusalResponse; the user never sees an unsourced number as fact (D3-10/L3-9). |
| T-03-04 | Spoofing (false grounding) | unit reconciliation tolerance | mitigate | Tolerance is a single audited policy constant (`NUMERIC_GROUNDING_REL_TOLERANCE`); reconciliation only matches across documented unit scales (lakh/crore/million/billion), preventing an arbitrarily-scaled number from spoofing a match. Behavior pinned by `test_ungrounded_number_blocked`. |
| T-03-05 | Tampering | confidence rubric inputs | mitigate | Rubric reads only validated `GroundedAnswer`/`Claim` fields (already schema-validated); no untrusted path or eval; deterministic and side-effect-free. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installed; extends already-vendored rapidfuzz + stdlib only. |
</threat_model>

<verification>
- `pytest tests/unit/test_numeric_grounding.py tests/unit/test_confidence_rubric.py tests/unit/test_cite_check.py -q` green.
- `pytest -q` whole suite green (no regression).
- LLM-free grep gates (filtered, `grep -v '^#'`) return 0 on both modules.
</verification>

<success_criteria>
- Indian-numeral numbers reconcile (lakh/crore/₹/million) within the policy tolerance; genuinely-ungrounded numbers fail and are blockable.
- Confidence is deterministic, anchored to cite-check primitives + section spread, with no tier for not-disclosed fields.
- No LLM call in either module; one shared normalization path.
</success_criteria>

<output>
Create `.planning/phases/03-structured-signal-extraction-red-flag-table/03-02-SUMMARY.md` when done.
</output>
