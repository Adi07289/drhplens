---
phase: 03-structured-signal-extraction-red-flag-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/redflag_schema.py
  - pipelines/redflag_queries.py
  - agent/policies.py
  - tests/unit/test_redflag_schema.py
  - tests/unit/test_redflag_precompute.py
  - tests/unit/test_confidence_rubric.py
  - tests/unit/test_numeric_grounding.py
  - tests/unit/test_risk_idf.py
  - tests/unit/test_methodology_pane.py
  - tests/eval/test_extraction_f1.py
  - tests/eval/test_release_gate.py
  - tests/eval/conftest.py
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02, EVAL-03, METHOD-01]
must_haves:
  truths:
    - "A RedFlagRecord round-trips to/from JSON using the same {\"refusal\": ...} discriminator as SnapshotRecord"
    - "The 7 canonical red-flag field keys are locked and validated (unknown keys rejected)"
    - "Every Phase 3 tunable threshold has a named constant in agent/policies.py"
    - "Every Wave 0 test file exists and fails meaningfully (xfail/skip stub) until its implementation lands"
  artifacts:
    - path: "agent/redflag_schema.py"
      provides: "RedFlagField + RankedRisk + RedFlagRecord with to_json/from_dict"
      contains: "class RedFlagRecord"
    - path: "pipelines/redflag_queries.py"
      provides: "REDFLAG_QUERIES dict with 7 canonical keys in UI-SPEC order"
      contains: "REDFLAG_QUERIES"
    - path: "agent/policies.py"
      provides: "Phase 3 tunable constants (numeric tolerance, IDF bands, F1 tolerances, gate threshold)"
      contains: "NUMERIC_GROUNDING_REL_TOLERANCE"
    - path: "tests/eval/conftest.py"
      provides: "shared Phase 3 fixtures (synthetic RedFlagRecord, tiny labels jsonl, 3-doc IDF corpus)"
      contains: "def synthetic_redflag_record"
  key_links:
    - from: "pipelines/redflag_queries.py"
      to: "agent/redflag_schema.py"
      via: "REDFLAG_QUERIES keys == RedFlagRecord field-key allow-list"
      pattern: "REDFLAG_FIELD_KEYS"
---

<objective>
Establish the Phase 3 contracts and the Nyquist Wave 0 test scaffold so every
downstream plan builds against a fixed interface instead of exploring the
codebase. This plan creates: (1) the `RedFlagRecord` cache schema mirroring
`SnapshotRecord` (EXTRACT-01/02), (2) the 7 canonical red-flag canned queries
(EXTRACT-01), (3) all Phase 3 tunable constants in the single-source-of-truth
`agent/policies.py` (EVAL-03/D3-10/D3-14/D3-07/D3-12), and (4) the eight Wave 0
test stub files + shared fixtures referenced by the Validation Architecture.

Purpose: Interface-first ordering. Waves 2-3 implement against the schema, the
query keys, and the policy constants defined here; the Wave 0 tests turn green
as each downstream task lands (the established stub→green-per-wave pattern).
Output: `agent/redflag_schema.py`, `pipelines/redflag_queries.py`, extended
`agent/policies.py`, and 8 failing test files + `tests/eval/conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-CONTEXT.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-PATTERNS.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-RESEARCH.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-VALIDATION.md
@agent/snapshot_schema.py
@agent/schemas.py
@agent/policies.py
@pipelines/snapshot_queries.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RedFlagRecord schema + 7 canned red-flag queries + policy constants</name>
  <read_first>
    - agent/snapshot_schema.py (the EXACT mirror — `_dump_field`/`_load_field` discriminator :39-54, `SnapshotRecord` + `to_dict`/`to_json`/`from_dict`/`from_json` :57-120, `fields_keys_known` frozenset validator :75-87)
    - agent/schemas.py (locked GroundedAnswer / Claim / RefusalResponse / RetrievedChunkRef — reuse verbatim; claim_id regex `^c_[a-z0-9]{6,16}$`; do NOT rename fields)
    - pipelines/snapshot_queries.py (the `dict[str,str]` query shape + the "edits are reviewable" docstring framing to mirror)
    - agent/policies.py (the single-source-of-truth constant pattern; `CITE_CHECK_TOKEN_RATIO` :69, `CITE_CHECK_SPAN_TOLERANCE_CHARS` :75, and the `GATE1_THRESHOLD` calibration-comment posture :38-44)
    - 03-PATTERNS.md §`agent/redflag_schema.py` (line-numbered mirror excerpt) and §`pipelines/redflag_queries.py`
    - 03-UI-SPEC.md R-1 (the 7 canonical field labels + fixed order)
  </read_first>
  <action>
    Create `agent/redflag_schema.py` mirroring `agent/snapshot_schema.py` verbatim in shape. Reuse `_dump_field`/`_load_field` ({"refusal": ...} discriminator) — copy, do not invent a new codec. Define `REDFLAG_FIELD_KEYS: frozenset[str]` = the 7 keys in UI-SPEC fixed order: `rpt_pct`, `ofs_vs_fresh`, `promoter_pledge_pct`, `customer_concentration`, `auditor_history`, `debt_trajectory`, `going_concern`. Define `RedFlagField(BaseModel)` with `value: GroundedAnswer | RefusalResponse`, `confidence_tier: Literal["high","medium","low"] | None = None` (None when not-disclosed, per D3-03), `confidence_score: float | None = None` (0.00-1.00, surfaced only in pane per D3-02). Define `RankedRisk(BaseModel)` with `claim_id: str`, `idf_score: float`, `specificity_band: Literal["issuer_specific","mostly_issuer_specific","industry_standard"]`. Define `RedFlagRecord(BaseModel)` with `drhp_id`, `computed_at`, `fields: dict[str, RedFlagField]`, `ranked_risks: list[RankedRisk]`, plus `to_dict`/`to_json(indent=2)`/`from_dict`/`from_json` copied from snapshot_schema, and a `fields_keys_known` validator over `REDFLAG_FIELD_KEYS`. Store path convention `data/redflag/<drhp_id>.json` (new dir, document in docstring; do not create the dir here).

    Create `pipelines/redflag_queries.py` mirroring `snapshot_queries.py`: a `REDFLAG_QUERIES: dict[str, str]` with the SAME 7 keys, each a plain-English canned query that targets the field (e.g. `rpt_pct` → related-party transactions as a percentage of revenue; `going_concern` → any going-concern / material-uncertainty statement by the auditors). Copy the "treat edits as reviewable" docstring framing and note the key-sync requirement with `REDFLAG_FIELD_KEYS`.

    Extend `agent/policies.py` (append a Phase 3 section, do not edit Phase 1/2 constants) with documented constants, each with a calibration comment mirroring `GATE1_THRESHOLD`: `NUMERIC_GROUNDING_REL_TOLERANCE: float` (relative tolerance for lakh/crore/₹ numeric reconciliation, D3-10), `IDF_BAND_THRESHOLDS: tuple[float, float]` (the two cutpoints for issuer_specific / mostly / industry_standard bands, D3-14), `F1_NUMERIC_TOLERANCES: dict[str, float]` (per-numeric-field absolute tolerances for fields rpt_pct/ofs_vs_fresh/promoter_pledge_pct/debt_trajectory, D3-07), `IDF_BOILERPLATE_FUZZ_THRESHOLD: int` (rapidfuzz token_set_ratio floor for the boilerplate clamp, ~85, D3-14), `NUMERIC_FAITHFULNESS_GATE: float = 0.95` (the release gate, D3-12). Mark each value as "calibrate empirically; procedure documented in eval/gold/extraction_rubric.md".
  </action>
  <verify>
    <automated>python -c "from agent.redflag_schema import RedFlagRecord, REDFLAG_FIELD_KEYS; from pipelines.redflag_queries import REDFLAG_QUERIES; from agent.policies import NUMERIC_GROUNDING_REL_TOLERANCE, IDF_BAND_THRESHOLDS, NUMERIC_FAITHFULNESS_GATE; assert set(REDFLAG_QUERIES) == REDFLAG_FIELD_KEYS == {'rpt_pct','ofs_vs_fresh','promoter_pledge_pct','customer_concentration','auditor_history','debt_trajectory','going_concern'}; assert NUMERIC_FAITHFULNESS_GATE == 0.95"</automated>
  </verify>
  <acceptance_criteria>
    - `set(REDFLAG_QUERIES.keys()) == REDFLAG_FIELD_KEYS` and equals the 7 canonical keys (import assertion above exits 0).
    - A `RedFlagRecord` built with a `GroundedAnswer` field and a `RefusalResponse` field survives `RedFlagRecord.from_json(rec.to_json())` with the union reconstructed correctly (covered by Task 3's `test_redflag_schema.py` once green).
    - `RedFlagRecord(fields={"bogus_key": ...})` raises ValueError (unknown-key validator).
    - `agent/policies.py` exports all five Phase 3 constants; no Phase 1/2 constant value changed (`git diff` shows only additions in the Phase 3 section).
    - No new LLM/Qdrant import added to `redflag_schema.py` or `redflag_queries.py`.
  </acceptance_criteria>
  <done>RedFlagRecord round-trips with the refusal discriminator; the 7 query keys match the schema allow-list; all Phase 3 policy constants exist with calibration comments.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test stubs + shared Phase 3 fixtures (Nyquist scaffold)</name>
  <read_first>
    - 03-RESEARCH.md §Validation Architecture → "Wave 0 Gaps" + "Phase Requirements → Test Map" (the exact 8 test files + the shared-fixture requirement)
    - 03-VALIDATION.md §Wave 0 Requirements
    - tests/conftest.py (the LOCKED-fixture-signature convention + the `pytest.skip(reason=...)` stub posture; mirror it)
    - tests/eval/test_snapshot_eval.py + tests/unit/test_snapshot_cache.py (analog test style for cache + eval tests)
    - tests/unit/test_cite_check.py (analog for the numeric grounding test)
    - agent/redflag_schema.py (Task 1 — fixtures build a synthetic RedFlagRecord)
  </read_first>
  <action>
    Create the eight Wave 0 test files, each as a runnable-but-stubbed module that imports the eventual symbol behind a guard and uses `pytest.skip(reason="Wave N implements <module>")` or `@pytest.mark.xfail(reason=...)` so the suite stays green now and flips to real assertions when its implementation plan lands. Lock the test function names (downstream tasks fill bodies; do NOT rename):
    - `tests/unit/test_redflag_schema.py` → `test_redflag_record_roundtrip`, `test_unknown_field_key_rejected`, `test_refusal_field_has_no_confidence` (EXTRACT-01) — these CAN assert now against Task 1's real schema (no skip; this is the interface test).
    - `tests/unit/test_redflag_precompute.py` → `test_seven_field_loop_monkeypatched`, `test_not_disclosed_becomes_refusal` (EXTRACT-01, skip until Plan 03).
    - `tests/unit/test_confidence_rubric.py` → `test_verbatim_is_high`, `test_light_parse_is_medium`, `test_cross_section_is_low`, `test_absence_has_no_tier` (EXTRACT-02, skip until Plan 02).
    - `tests/unit/test_numeric_grounding.py` → `test_lakh_crore_reconciles`, `test_rupee_symbol_tolerance`, `test_ungrounded_number_blocked` (EVAL-03, skip until Plan 02).
    - `tests/unit/test_risk_idf.py` → `test_issuer_specific_ranks_above_boilerplate`, `test_boilerplate_floor_clamps` (P12, skip until Plan 03).
    - `tests/unit/test_methodology_pane.py` → `test_pane_renders_from_cache`, `test_no_llm_or_qdrant_import` (the `inspect.getsource` substring check mirroring `test_no_llm_judge_fallback`) (METHOD-01, skip until Plan 04).
    - `tests/eval/test_extraction_f1.py` → `test_numeric_tolerance_match`, `test_boolean_exact_match`, `test_set_overlap_f1`, `test_refusal_scored_not_dropped`, `test_bucket_split` (EXTRACT-03, skip until Plan 05).
    - `tests/eval/test_release_gate.py` → `test_gate_exits_nonzero_below_threshold`, `test_gate_passes_at_or_above_threshold` (EVAL-03, skip until Plan 05).
    Create `tests/eval/conftest.py` with three LOCKED fixtures (bodies filled now since they only need Task 1's schema + stdlib): `synthetic_redflag_record` (a `RedFlagRecord` with ≥1 GroundedAnswer field + ≥1 RefusalResponse field + ≥2 RankedRisk items), `tiny_extraction_labels` (a small in-memory list of label dicts covering one numeric, one boolean, one set field + one not-disclosed), `idf_corpus_3doc` (3 fake risk-section strings: 2 sharing a boilerplate phrase, 1 unique). Document each fixture's shape in its docstring.
  </action>
  <verify>
    <automated>pytest tests/unit/test_redflag_schema.py tests/unit/test_redflag_precompute.py tests/unit/test_confidence_rubric.py tests/unit/test_numeric_grounding.py tests/unit/test_risk_idf.py tests/unit/test_methodology_pane.py tests/eval/test_extraction_f1.py tests/eval/test_release_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 8 test files collect without import errors (`pytest --collect-only` lists every locked test function name above).
    - `test_redflag_schema.py` PASSES now (asserts against Task 1's real schema — the interface gate).
    - The 7 implementation-pending test modules SKIP (not error) with a `reason` naming the implementing plan.
    - `tests/eval/conftest.py::synthetic_redflag_record` returns a `RedFlagRecord` that round-trips through `to_json`/`from_json` (assert inside one collected test).
    - `pytest -q` whole suite remains green (237+ baseline + new schema test passing, rest skipped).
  </acceptance_criteria>
  <done>Eight Wave 0 test files + shared fixtures exist; schema interface test green; all implementation-pending tests skip cleanly with named reasons; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache file path ← drhp_id | A `drhp_id` string is interpolated into `data/redflag/<drhp_id>.json`; untrusted ids must not escape the cache dir |
| on-disk JSON → Pydantic load | Cached JSON is deserialized into `RedFlagRecord`; malformed/hostile JSON must be rejected by schema validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `data/redflag/<drhp_id>.json` path construction | mitigate | Schema/path docstring mandates `is_known_drhp_id` allow-list (reused from Phase 2 `data/catalogue_loader.py`) before any path is formed; this plan only documents the convention, Plan 03/04 enforce it at write/read time. No raw user input interpolated into a path in this plan. |
| T-03-02 | Tampering | `RedFlagRecord.from_dict` over cached JSON | mitigate | Reuse the locked `GroundedAnswer`/`Claim` validators (claim_id regex, span_offsets start≤end) verbatim; `fields_keys_known` rejects unknown keys; Pydantic rejects malformed dicts. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package (stdlib + already-vendored pydantic/rapidfuzz only); no supply-chain surface. |
</threat_model>

<verification>
- `pytest -q` green (baseline 237+ unit tests + new schema test pass; implementation-pending tests skip with reasons).
- Import assertion in Task 1 verify exits 0 (keys + constants present).
- `git diff agent/policies.py` shows only additive Phase 3 section.
</verification>

<success_criteria>
- RedFlagRecord schema mirrors SnapshotRecord and round-trips with the refusal discriminator.
- 7 canonical red-flag query keys locked and synced to the schema allow-list.
- All Phase 3 tunable thresholds live as named constants in agent/policies.py.
- Eight Wave 0 test files + shared fixtures exist; the suite stays green.
</success_criteria>

<output>
Create `.planning/phases/03-structured-signal-extraction-red-flag-table/03-01-SUMMARY.md` when done.
</output>
