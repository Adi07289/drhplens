---
phase: 03-structured-signal-extraction-red-flag-table
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02"]
files_modified:
  - eval/gold/numeric_eval.jsonl
  - scripts/run_eval.py
  - scripts/release_gate.py
  - Makefile
  - tests/eval/test_release_gate.py
autonomous: false
requirements: [EVAL-03]
must_haves:
  truths:
    - "A hand-curated ~50-question numeric-only eval set lives at eval/gold/numeric_eval.jsonl, each with a gold numeric answer + source page (D3-11)"
    - "run_eval gains a numeric-faithfulness track: numeric_faithfulness = fraction of eval Qs whose every emitted number grounds via the extended cite_check (D3-11/D3-13)"
    - "make release runs the numeric track against live services and EXITS NON-ZERO + writes the report when numeric_faithfulness < 0.95 (D3-12 — enforcement, not a printed warning)"
    - "The gate LOGIC is unit-tested offline on a fixture: 0.94 -> non-zero exit, 0.96 -> pass — no live services needed (D3-12)"
  artifacts:
    - path: "eval/gold/numeric_eval.jsonl"
      provides: "~50 numeric-only Qs with gold numeric answer + source page"
      min_lines: 40
    - path: "scripts/release_gate.py"
      provides: "the >=0.95 gate body: compute numeric_faithfulness, sys.exit(1)+report below threshold"
      contains: "sys.exit"
    - path: "Makefile"
      provides: "release target wrapping release_gate.py (Make stops on non-zero)"
      contains: "release:"
  key_links:
    - from: "scripts/release_gate.py"
      to: "agent.policies.NUMERIC_FAITHFULNESS_GATE"
      via: "0.95 threshold constant (no hard-coded 0.95)"
      pattern: "NUMERIC_FAITHFULNESS_GATE"
    - from: "scripts/run_eval.py"
      to: "agent.nodes.cite_check"
      via: "per-number grounding reused for the numeric track"
      pattern: "from agent.nodes.cite_check import"
    - from: "Makefile"
      to: "scripts/release_gate.py"
      via: "release target invokes the gate; non-zero exit fails the build"
      pattern: "release_gate"
---

<objective>
Deliver EVAL-03 as ENFORCEMENT, not just a measured number. Four pieces: (1) a
hand-curated ~50-question numeric-only eval set (`eval/gold/numeric_eval.jsonl`),
each carrying a gold numeric answer + source page (D3-11); (2) a numeric-faithfulness
track added to `scripts/run_eval.py` that reuses the Plan 02 extended `cite_check`
per-number grounding to compute `numeric_faithfulness` across all numeric surfaces
(D3-11/D3-13); (3) the `make release` gate (`scripts/release_gate.py` + a `Makefile`
target) that runs the track against live services and **`sys.exit(1)` + writes the
report when < 0.95** (D3-12 — Pitfall 4: the gate must refuse, not warn); (4) an
offline fixture test (`tests/eval/test_release_gate.py`) that unit-tests the gate
LOGIC (0.94 → non-zero, 0.96 → pass) with no live infra.

Purpose: "Enforcement over discipline" — the numeric-faithfulness gate is the
phase's core security-of-truth control; a hallucinated number must physically
block deploy, and the gate's correctness is itself CI-tested.
Output: `eval/gold/numeric_eval.jsonl`, extended `scripts/run_eval.py`, new
`scripts/release_gate.py`, a `Makefile`, and `tests/eval/test_release_gate.py`
flipped from skip to green.

This plan is `autonomous: false`: the `make release` task drives the gate against
LIVE Qdrant + Gemini (requires `GEMINI_API_KEY`/`QDRANT_URL`/`QDRANT_API_KEY`), so
it includes a blocking human-verify checkpoint for the live run; the gate-logic
unit test and all file creation are fully autonomous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-CONTEXT.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-PATTERNS.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-RESEARCH.md
@scripts/run_eval.py
@scripts/calibrate_gate1.py
@agent/nodes/cite_check.py
@agent/policies.py
@tests/eval/gold_set.jsonl
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-01-SUMMARY.md
@.planning/phases/03-structured-signal-extraction-red-flag-table/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ~50-question numeric eval set + numeric-faithfulness track in run_eval (D3-11/D3-13)</name>
  <read_first>
    - 03-CONTEXT.md §Numeric-Faithfulness Gate (D3-11 ~50 numeric-only Qs + gold answer + source page; D3-13 covers Q&A + snapshot + red-flag numbers)
    - 03-RESEARCH.md §Pattern 6 + §Pattern 7 + §Pitfall 1 (numeric_faithfulness = fraction of eval Qs whose every emitted number grounds; lakh/crore/₹ reconciliation already in Plan 02; test on real Swiggy figures revenue 11,247 cr / OFS 4,499 cr)
    - scripts/run_eval.py (the EXACT extension point: jsonl loader :138-142, the main eval loop :155-227, the `_normalize` :49-53, the dated-markdown report writer :244-305, the per-category summary loop :285-292 — add a numeric track alongside, do not rewrite the Phase 1 track)
    - tests/eval/gold_set.jsonl (the jsonl shape + the existing `category: "numeric"` rows to mirror for the new file)
    - agent/nodes/cite_check.py (Plan 02 — the extended per-number grounding to reuse; import its grounding helper rather than re-implementing the subset check)
    - 03-PATTERNS.md §`scripts/run_eval.py (MODIFY)` (the numeric-track addition spec)
  </read_first>
  <action>
    Create `eval/gold/numeric_eval.jsonl` with ~50 numeric-only questions across the ingested IPO(s) (right-size to what is ingested per D3-05; document the true count). Mirror `tests/eval/gold_set.jsonl`'s jsonl shape; each line carries `qid`, `question`, `gold_numeric` (the expected number, in its DRHP unit), `gold_unit` (e.g. `crore`/`%`/`lakh`), and `source_page` (int). Cover all numeric surfaces per D3-13 — issue-size / financials / OFS-vs-fresh / RPT-style numbers — so the gate exercises Q&A + snapshot + red-flag numerics. Include the real Swiggy anchors (revenue 11,247 cr; OFS 4,499 cr) so the lakh/crore reconciliation from Plan 02 is exercised.

    Extend `scripts/run_eval.py` IN PLACE with a numeric-faithfulness track (do NOT rewrite the Phase 1 track): add a function that loads `eval/gold/numeric_eval.jsonl`, invokes the agent per question, and for each emitted number applies the Plan 02 extended `cite_check` per-number grounding against the cited window. Compute `numeric_faithfulness = (# eval Qs whose EVERY emitted number grounds) / (# eval Qs)`. Reuse the existing `_normalize` and the dated-markdown report writer; write a numeric-track section into the report (per-Q pass/fail + the aggregate `numeric_faithfulness`). Expose the computed `numeric_faithfulness` as an importable function return so `release_gate.py` (Task 2) can call the SAME computation without duplicating it.
  </action>
  <verify>
    <automated>python -c "import json; from pathlib import Path; rows=[json.loads(l) for l in Path('eval/gold/numeric_eval.jsonl').read_text().splitlines() if l.strip()]; assert len(rows) >= 40, f'need ~50 numeric Qs, got {len(rows)}'; assert all('gold_numeric' in r and 'source_page' in r for r in rows), 'every row needs gold_numeric + source_page'"; python -c "import ast,inspect; src=open('scripts/run_eval.py').read(); assert 'numeric_faithfulness' in src, 'numeric track missing'"</automated>
  </verify>
  <acceptance_criteria>
    - `eval/gold/numeric_eval.jsonl` has ≥40 rows (targeting ~50), each with `gold_numeric` + `source_page` (import assertion exits 0).
    - `scripts/run_eval.py` defines a numeric-faithfulness computation that returns the aggregate score (importable by `release_gate.py`), reusing Plan 02 `cite_check` per-number grounding (`grep -v '^#' scripts/run_eval.py | grep -Ec 'cite_check'` ≥ 1).
    - The Phase 1 track is unchanged in behavior (existing run_eval report structure preserved; the numeric track is additive).
    - The Swiggy lakh/crore anchors (11,247 cr; 4,499 cr) appear in the eval set.
  </acceptance_criteria>
  <done>A ~50-question numeric-only eval set plus an additive numeric-faithfulness track in run_eval that reuses the extended cite_check grounding and exposes the aggregate score for the gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: release_gate.py + Makefile + offline gate-logic fixture test (D3-12)</name>
  <read_first>
    - 03-CONTEXT.md §Numeric-Faithfulness Gate (D3-12 make-release refuses below 0.95 + offline CI smoke test) + §Specific Ideas ("must actually refuse deploy, not just print a number")
    - 03-RESEARCH.md §Pattern 7 + §Pitfall 4 (sys.exit(1) below threshold; the offline fixture test asserts exit behavior at 0.94 and 0.96; "enforcement over discipline")
    - scripts/run_eval.py (Task 1 — `_check_env` :35-41 to reuse for the live-run env fail-fast; the importable numeric_faithfulness computation from Task 1)
    - scripts/calibrate_gate1.py (the sweep-and-report CLI posture: project-root-on-path, dated report, fail-fast env)
    - agent/policies.py (Plan 01 — `NUMERIC_FAITHFULNESS_GATE = 0.95`; do NOT hard-code 0.95 in the gate)
    - 03-PATTERNS.md §`scripts/release_gate.py` + `Makefile` (the gate body spec; Makefile has NO repo analog — author fresh)
    - tests/eval/test_release_gate.py (Plan 01 stub with locked test names to fill) + tests/eval/conftest.py
  </read_first>
  <behavior>
    - The gate, given a synthetic numeric_faithfulness of 0.94, calls `sys.exit` with a non-zero code AND writes a dated report to `eval/reports/`.
    - The gate, given 0.96, returns/exits zero (pass) — and 0.95 exactly passes (>= threshold).
    - The threshold is read from `agent.policies.NUMERIC_FAITHFULNESS_GATE`, not a literal in the gate body.
    - The offline fixture test drives both branches WITHOUT any live Qdrant/Gemini call (the score is injected, the live computation is monkeypatched).
  </behavior>
  <action>
    Create `scripts/release_gate.py` with a pure, testable gate function `enforce_gate(numeric_faithfulness: float) -> None` that: reads the threshold from `agent.policies.NUMERIC_FAITHFULNESS_GATE`; if `numeric_faithfulness < threshold`, writes `eval/reports/<date>-numeric-gate.md` (mirroring run_eval's dated-markdown writer) AND calls `sys.exit(1)`; if `>=`, prints an OK line and returns/exits 0. Add a `main()` that calls Task 1's importable `numeric_faithfulness` computation against LIVE services (reuse `run_eval._check_env` for env fail-fast) then passes the score to `enforce_gate` — keep `enforce_gate` free of any live-infra call so it is unit-testable in isolation. Create a `Makefile` with a `release:` target that runs `python scripts/release_gate.py` (Make halts the build on the non-zero exit — the enforcement). Fill `tests/eval/test_release_gate.py`: `test_gate_exits_nonzero_below_threshold` (call `enforce_gate(0.94)`, assert `SystemExit` with non-zero code + a report file written, using a tmp report dir) and `test_gate_passes_at_or_above_threshold` (call `enforce_gate(0.96)` and `enforce_gate(0.95)`, assert no non-zero exit) — both fully offline by injecting the score (no monkeypatched live call needed since `enforce_gate` takes the score directly).
  </action>
  <verify>
    <automated>pytest tests/eval/test_release_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_release_gate.py::test_gate_exits_nonzero_below_threshold` and `::test_gate_passes_at_or_above_threshold` PASS (flipped from skip), with no live Qdrant/Gemini call.
    - `enforce_gate(0.94)` raises `SystemExit` with a non-zero code and writes a `*-numeric-gate.md` report; `enforce_gate(0.96)` and `enforce_gate(0.95)` do not exit non-zero.
    - The threshold comes from `agent.policies.NUMERIC_FAITHFULNESS_GATE` (`grep -v '^#' scripts/release_gate.py | grep -c NUMERIC_FAITHFULNESS_GATE` ≥ 1; no literal `0.95` in the gate decision).
    - `Makefile` has a `release:` target invoking `release_gate.py`; `enforce_gate` contains no live-infra import (`grep -v '^#' scripts/release_gate.py | grep -Ec 'qdrant|genai' ` within the enforce_gate region is 0 — the live call lives only in `main`).
  </acceptance_criteria>
  <done>A release gate whose pure `enforce_gate` refuses (sys.exit(1)+report) below the policy threshold and passes at/above it, wrapped by a Makefile target, with the gate logic unit-tested offline at 0.94/0.95/0.96.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live `make release` numeric-gate run (requires live Qdrant + Gemini)</name>
  <what-built>
    The numeric-faithfulness track (Task 1) + the `make release` gate (Task 2) are
    code-complete and the gate LOGIC is green offline. This checkpoint runs the gate
    against LIVE services — the only step that needs `GEMINI_API_KEY` / `QDRANT_URL` /
    `QDRANT_API_KEY` and ingested DRHPs, consistent with the Phase 1/2 CODE-NOW-DEFER
    posture (the live run is the runbook step; all logic is already CI-tested).
  </what-built>
  <how-to-verify>
    1. Ensure `.env` has `GEMINI_API_KEY`, `QDRANT_URL`, `QDRANT_API_KEY` and the gold-set DRHP(s) are ingested into live Qdrant.
    2. Run `make release`.
    3. Confirm: if `numeric_faithfulness >= 0.95`, the command prints OK and exits 0; if `< 0.95`, the command EXITS NON-ZERO and writes `eval/reports/<date>-numeric-gate.md`.
    4. Confirm a dated numeric-gate report exists under `eval/reports/` with the per-Q pass/fail table + the aggregate score.
    5. If the gate fails below 0.95, this is the gate working as designed — inspect the report, tune tolerances/eval scope per the rubric (D3-05), and re-run; do NOT weaken the 0.95 threshold to pass.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed numeric_faithfulness + the committed report path, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-emitted numbers → numeric gate | Every number in an answer is untrusted until grounded against its cited DRHP window |
| numeric_faithfulness score → deploy decision | The gate's exit code controls whether a deploy proceeds — the enforcement boundary |
| live env vars (API keys) → release run | The live `make release` needs secrets; they must come from env, never hard-coded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information disclosure / Repudiation | hallucinated number reaching the user | mitigate | The numeric track reuses Plan 02 per-number grounding; the ≥0.95 gate physically blocks a release whose numbers don't ground (the phase's core security-of-truth control). |
| T-03-09 | Tampering / Repudiation | gate that prints but doesn't enforce (Pitfall 4) | mitigate | `enforce_gate` calls `sys.exit(1)` below threshold and the `Makefile` halts on non-zero; the negative-path is pinned by `test_gate_exits_nonzero_below_threshold` — the gate cannot silently pass. |
| T-03-10 | Information disclosure | API keys in the release run | mitigate | Reuse `run_eval._check_env` fail-fast; keys read from env only, never committed; the live run is a gated checkpoint, not an autonomous step. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package — stdlib + the already-present run_eval/cite_check stack only. |
</threat_model>

<verification>
- `pytest tests/eval/test_release_gate.py -q` green (flipped from skip); gate logic tested offline at 0.94/0.95/0.96.
- `pytest -q` whole suite green.
- Numeric-eval import assertion exits 0; threshold-from-policy + no-literal-0.95 grep gates pass.
- (Checkpoint) live `make release` exits non-zero below 0.95 and writes the dated report.
</verification>

<success_criteria>
- ~50-question numeric-only eval set committed with gold answer + source page.
- run_eval numeric-faithfulness track reuses the extended cite_check grounding (D3-13).
- `make release` refuses deploy (non-zero exit + report) below 0.95 — enforcement, not a warning.
- Gate logic unit-tested offline; live run verified at the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03-structured-signal-extraction-red-flag-table/03-05-SUMMARY.md` when done.
</output>
