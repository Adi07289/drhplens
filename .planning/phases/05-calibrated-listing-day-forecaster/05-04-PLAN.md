---
phase: 05-calibrated-listing-day-forecaster
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - pipelines/features/__init__.py
  - pipelines/features/build.py
  - tests/unit/test_features_available_at.py
autonomous: true
requirements: [FCAST-02]
must_haves:
  truths:
    - "Every feature declares an available_at rule and the builder RAISES if any feature of any row resolves to available_at > T0 (issue-open day, D5-01)"
    - "The thin-slice feature set is issue-structure-only (D5-06a) so the end-to-end model chain can land before regime/DRHP/anchor features (D5-05 verified-subset-first)"
    - "A missing feature is NaN-retained and counted, never fabricated as 0 (replace-with-NaN)"
    - "GMP and final/at-close subscription multiples are excluded by construction (never appear in FEATURE_SPECS)"
  artifacts:
    - path: "pipelines/features/__init__.py"
      provides: "FEATURE_SPECS: each feature -> (dtype, available_at rule); the T0 leakage-gate contract"
      contains: "FEATURE_SPECS"
    - path: "pipelines/features/build.py"
      provides: "build_features(panel) -> X matrix with available_at<=T0 assertion + leakage audit record"
      contains: "available_at"
    - path: "tests/unit/test_features_available_at.py"
      provides: "FCAST-02 leakage gate: a post-T0 feature RAISES; all issue-structure features pass"
      contains: "available_at"
  key_links:
    - from: "pipelines/features/build.py"
      to: "pipelines/historical (panel + compute_listing_day_return target)"
      via: "consume the survivorship panel; never recompute the target"
      pattern: "listing_day_return|compute_listing_day_return"
    - from: "pipelines/features/build.py"
      to: "T0 = issue_date (D5-01)"
      via: "available_at <= issue_date assertion per feature per row"
      pattern: "issue_date"
---

<objective>
Slice 2b (the leakage-gated feature layer, FCAST-02 / D5-01 / D5-08): build the thin issue-structure-only feature matrix (D5-06a) from the survivorship panel, with a hard `available_at <= T0` gate where T0 is the issue-open day (D5-01 supersedes FCAST-02's literal "T−1 of listing" wording — note the reconciliation), and emit a leakage-audit record for the model card. Regime/DRHP/anchor features (D5-06b/c/d) are deliberately deferred to 05-08 so the end-to-end chain lands first (D5-05).

Purpose: the walk-forward model (05-05) consumes this X; the gate is the primary P4/lookahead defense.
Output: `pipelines/features/__init__.py` (FEATURE_SPECS), `pipelines/features/build.py`, and the FCAST-02 leakage-gate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@pipelines/historical/__init__.py
@tests/unit/fixtures/forecast_fixtures.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FEATURE_SPECS contract + available_at rules (issue-structure only)</name>
  <read_first>
    - pipelines/historical/__init__.py (the column-contract-as-constant grammar to mirror: STATUS_VALUES frozenset + PANEL_COLUMNS tuple + PANEL_DTYPES dict at lines ~41-73; the validate-each-row-or-raise pattern at ~116-172)
    - 05-RESEARCH.md §"Pattern 3: available_at <= T0 feature gate + leakage audit" (the family table, lines ~309-318) — use only family (a) here; D5-01 sets T0 = issue-open
    - 05-CONTEXT.md D5-06a (issue structure: issue size ₹, price-band width %, OFS-vs-fresh split, promoter dilution %, lot size / min investment) + D5-08 (every feature carries a verified available_at)
    - 05-PATTERNS.md §"pipelines/features/__init__.py + build.py"
  </read_first>
  <behavior>
    - FEATURE_SPECS declares exactly the issue-structure features, each mapping to a dtype and an available_at rule keyed off the DRHP/RHP filing date (<= T0)
    - no spec references GMP or subscription/at-close multiples
    - the specs are a frozen, importable contract (like PANEL_COLUMNS)
  </behavior>
  <action>
    Create `pipelines/features/__init__.py` mirroring the historical column-contract grammar. Define `FEATURE_SPECS` as an ordered mapping of the D5-06a issue-structure features to `(dtype, available_at_rule)`: `issue_size_cr` (float, filing_date), `price_band_width_pct` (float, filing_date), `ofs_fraction` (float, filing_date), `promoter_dilution_pct` (float, filing_date), `lot_size` (float/int, filing_date). Add `FEATURE_COLUMNS` (the ordered names) and `FEATURE_DTYPES`. Encode the T0 rule as a module constant/docstring: `available_at` for every issue-structure feature is the DRHP/RHP filing date, which is `<= T0` (issue-open day, D5-01). Add an explicit `EXCLUDED_FROM_MODEL = frozenset({"gmp", "subscription_at_close", "qib", "nii", "rii", "listing_day_...")` sentinel set naming what may NEVER be a feature (GMP + at-close subscription, FCAST-02) — a guard the builder asserts against. Keep it feature-declaration only (no pandas frame building here).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pipelines.features as F; assert F.FEATURE_COLUMNS and 'gmp' not in F.FEATURE_COLUMNS and not any('subscri' in c for c in F.FEATURE_COLUMNS); assert 'gmp' in F.EXCLUDED_FROM_MODEL; print('feature specs ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `FEATURE_COLUMNS` lists only issue-structure features; contains no `gmp`, no `subscri*`, no at-close/listing-day column
    - each feature has an `available_at` rule anchored to the filing date (<= T0)
    - `EXCLUDED_FROM_MODEL` names GMP + at-close subscription as never-features
  </acceptance_criteria>
  <done>The frozen issue-structure FEATURE_SPECS contract with per-feature available_at rules exists; GMP/subscription excluded by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_features with the T0 leakage gate + audit, and the FCAST-02 test</name>
  <read_first>
    - pipelines/historical/__init__.py (assemble_panel's validate-or-raise + _to_float_or_nan replace-with-NaN at ~101-172 — the exact grammar the feature builder copies for its leakage gate)
    - pipelines/historical/validate.py (the (value, flag_or_None) plain-text divergence-flag posture at ~40-107 — mirror for the leakage-audit record)
    - tests/unit/fixtures/forecast_fixtures.py (synthetic_panel / synthetic_features — the offline fixture to test against)
    - 05-RESEARCH.md §"Pattern 3" (the audit + the R²>0.5 note — R² alarm itself lands in 05-08, not here)
  </read_first>
  <behavior>
    - build_features(panel) returns an X DataFrame with FEATURE_COLUMNS and a parallel available_at frame
    - if ANY feature's resolved available_at for ANY row is > that row's issue_date (T0), build_features RAISES (leakage), naming the offending feature+row
    - a missing feature value is NaN-retained (never 0); row count preserved
    - build_features returns/emits a leakage_audit() summary: for each feature, its available_at rule + verdict "<= T0 ✓" — the model-card input
  </behavior>
  <action>
    Create `pipelines/features/build.py` with `build_features(panel) -> tuple[DataFrame, DataFrame]` (X matrix + available_at matrix) that derives each `FEATURE_SPECS` column from the panel/catalogue, coerces via the replace-with-NaN helper (NaN retained, counted), and ASSERTS `available_at <= issue_date` (T0) for every feature of every row — raising a `LeakageError` (define it) that names the feature + issuer on violation, mirroring assemble_panel's invalid-status raise. Add `leakage_audit(panel) -> list[dict]` returning, per feature, `{feature, available_at_rule, verdict}` (all "<= T0 ✓" for issue-structure) as the FCAST-02 model-card audit (plain-data, `/methodology`-ready — same posture as validate.sanity_check_median's flag text). Also assert none of `EXCLUDED_FROM_MODEL` appears among the built columns. Write `tests/unit/test_features_available_at.py`: (a) build_features on `synthetic_panel()` succeeds and every column is in FEATURE_COLUMNS; (b) a monkeypatched/crafted feature whose available_at is set to `issue_date + 1 day` RAISES LeakageError; (c) a row with a missing issue-structure value keeps NaN and the row is retained; (d) `leakage_audit` returns a verdict for every feature and none is GMP/subscription.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_features_available_at.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - build_features raises `LeakageError` naming the feature when a feature's available_at > issue_date (T0) for any row (FCAST-02/P4)
    - build_features on the synthetic panel returns X with exactly FEATURE_COLUMNS and preserves row count with NaN-retained missing values
    - `leakage_audit()` returns a `<= T0 ✓` verdict for every issue-structure feature and lists no GMP/subscription feature
    - `test_features_available_at.py` passes
  </acceptance_criteria>
  <done>The issue-structure feature matrix builds behind a hard T0 leakage gate with a committed-ready audit; GMP/subscription cannot enter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel/catalogue values → feature matrix | Coerced numeric inputs cross into the model's X |
| time boundary T0 (issue-open) | The leakage boundary every feature must respect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-LEAK | Tampering (data integrity) | available_at feature gate | mitigate | Hard `available_at <= issue_date (T0)` assertion per feature per row raises `LeakageError`; issue-structure-only thin slice; leakage_audit committed to the model card (FCAST-02/P4) |
| T-05-04-EXCL | (invariant breach) | GMP/subscription exclusion | mitigate | `EXCLUDED_FROM_MODEL` sentinel + builder assertion that no excluded token is a built column |
| T-05-04-FAB | Tampering (honesty) | missing feature values | mitigate | replace-with-NaN (retained, counted); never fabricate 0; row count preserved |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_features_available_at.py -q` passes.
- A post-T0 feature raises; GMP/subscription cannot be a column; missing → NaN retained.
</verification>

<success_criteria>
- Issue-structure feature matrix (D5-06a) built behind a hard T0 leakage gate (FCAST-02).
- Leakage audit emitted for the model card; GMP/subscription excluded by construction.
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-04-SUMMARY.md` when done.
</output>
