---
phase: 05-calibrated-listing-day-forecaster
plan: 10
type: execute
wave: 8
depends_on: ["05-06", "05-07", "05-08", "05-09"]
files_modified:
  - pipelines/forecast/diagnostics.py
  - pipelines/forecast/card.py
  - model_card/MODEL_CARD.md
  - pages/01_methodology.py
  - tests/unit/test_diagnostics_plots.py
  - tests/unit/test_model_card.py
  - tests/unit/test_methodology_render.py
autonomous: true
requirements: [FCAST-05, FCAST-03, UI-03]
must_haves:
  truths:
    - "A committed MODEL_CARD.md covers: training window, the lean feature list with available_at timestamps, the four baselines + Diebold–Mariano table with the P9 release-gate verdict, calibration plot, PIT histogram, the anchor available_at leakage audit, and known limitations (FCAST-05, Success Criterion 4)"
    - "The calibration plot + PIT histogram + SHAP importance are committed PNG artifacts under model_card/ (FCAST-03), built with matplotlib/shap (CLAUDE.md locked)"
    - "Empirical coverage is the real held-out number in the card (never rounded to 0.80); the PIT is labeled grid-derived (P17, A8)"
    - "The /methodology page renders the forecaster model-card section (the destination of the forecast block's 'Full model card →' link) and imports NO model/training library (render-only isolation)"
  artifacts:
    - path: "pipelines/forecast/diagnostics.py"
      provides: "calibration_plot + pit_histogram + shap_summary -> committed PNGs (extends 05-06 global_metrics)"
      contains: "calibration"
    - path: "pipelines/forecast/card.py"
      provides: "build_model_card assembling MODEL_CARD.md from metrics + audit + baselines/DM + plots"
      contains: "build_model_card"
    - path: "model_card/MODEL_CARD.md"
      provides: "committed public model card (FCAST-05 artifact)"
      contains: "Diebold"
    - path: "pages/01_methodology.py"
      provides: "the forecaster model-card section render (UI-03 link destination)"
      contains: "model card"
  key_links:
    - from: "pipelines/forecast/card.py"
      to: "baselines.release_gate + diagnostics plots + features.leakage_audit + features.select importance"
      via: "assemble the committed MODEL_CARD.md from computed artifacts"
      pattern: "release_gate|leakage_audit|calibration"
    - from: "pages/01_methodology.py"
      to: "model_card/*.png + MODEL_CARD.md"
      via: "st.image + section render (render-only, no model import)"
      pattern: "model_card"
---

<objective>
Slice 3c (the committed model card + its /methodology home, FCAST-05 / FCAST-03 / UI-03 — Success Criterion 4). Build the calibration plot, PIT histogram, and SHAP importance as committed matplotlib/SHAP PNG artifacts (FCAST-03, P17), assemble the public `MODEL_CARD.md` from every computed input (training window, the lean feature list with `available_at` timestamps, the anchor leakage audit, the four baselines + Diebold–Mariano table with the P9 release-gate verdict, empirical coverage/MAE/per-year RMSE, the R²>0.5 alarm status, and known limitations), and render the forecaster model-card section on `/methodology` — the destination of the 05-07 forecast block's `Full model card →` link. CODE-NOW-DEFER: assemble from the current seed artifacts now (so /methodology renders offline); the REAL card + PNGs regenerate from the live walk-forward at the 05-11 checkpoint.

Purpose: FCAST-05 is Success Criterion 4 and is uncovered by 05-01..05-07; this closes it. UI-03 is reinforced by giving the forecast block's link a real destination.
Output: extended `diagnostics.py` (plots), new `pipelines/forecast/card.py` (MODEL_CARD.md writer), committed `model_card/` artifacts, the `pages/01_methodology.py` model-card section, and the plots/card/render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-UI-SPEC.md
@pipelines/forecast/diagnostics.py
@pipelines/historical/build.py
@pages/01_methodology.py
@ui/chrome.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibration plot + PIT histogram + SHAP importance PNGs</name>
  <read_first>
    - pipelines/forecast/diagnostics.py (05-06 — `global_metrics`; extend HERE; keep the metrics-only functions untouched)
    - 05-RESEARCH.md §Open Questions Q1 (PIT from a small quantile grid 0.05,0.1,…,0.95 fit purely for the diagnostic; production interval stays 0.1/0.5/0.9; label the PIT grid-derived, A8) + §Pitfall 5 (empirical coverage on held-out OOS, the REAL number, P17) + §Standard Stack (matplotlib PNGs; shap for XGBoost with a feature_importances_ fallback) + §Architectural Responsibility Map (plots = offline artifact -> /methodology)
    - pipelines/forecast/walkforward.py + model.py (the OOS frame + fitted models for the quantile grid + SHAP)
    - pipelines/historical/build.py (write_panel / _write_readme — the committed-artifact writer posture and where artifacts land)
    - CLAUDE.md §Technology Stack (matplotlib + SHAP locked; Plotly is for in-app interactivity, matplotlib for committed static PNGs)
  </read_first>
  <behavior>
    - calibration_plot writes a reliability diagram (nominal vs empirical coverage across a 0.05..0.95 grid), annotating the REAL 80% empirical coverage (never 0.80-rounded)
    - pit_histogram writes the PIT/reliability histogram derived from the grid, labeled grid-derived
    - shap_summary writes a SHAP importance summary over the lean SELECTED_FEATURES, with a feature_importances_ fallback
    - all three import matplotlib/shap lazily inside the functions and use the Agg headless backend
  </behavior>
  <action>
    Extend `pipelines/forecast/diagnostics.py` with matplotlib PNG builders that write into `model_card/`: `calibration_plot(oos_df, out_path)` — a reliability diagram of nominal-vs-empirical coverage across a small quantile grid (0.05, 0.1, …, 0.95, fit purely for the diagnostic per RESEARCH Open Q1; the production interval stays 0.1/0.5/0.9), annotating the real 80% empirical coverage (P17, never rounded to 0.80); `pit_histogram(oos_df, out_path)` — the PIT/reliability histogram derived from that quantile grid (label it grid-derived, A8); `shap_summary(model, X, out_path)` — a SHAP feature-importance summary for the lean `SELECTED_FEATURES` (D5-07 interpretability), falling back to XGBoost `feature_importances_` when SHAP is unavailable. Import `matplotlib`/`shap` LAZILY inside each function and set `matplotlib.use("Agg")` (headless — no display, safe in CI). Leave `global_metrics` (05-06) untouched. Write `tests/unit/test_diagnostics_plots.py`: on the fixture OOS frame + a tiny fitted model, each builder writes a non-empty PNG to a tmp path and returns the path, and the calibration annotation reports the RAW coverage (not 0.80).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_diagnostics_plots.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `calibration_plot` + `pit_histogram` + `shap_summary` each write a non-empty PNG (Agg headless) and return the path; matplotlib/shap are imported lazily (module import does no plotting/network)
    - the calibration diagnostic reports the real empirical coverage (P17); the PIT artifact is labeled grid-derived (0.05..0.95) while the production interval stays 0.1/0.5/0.9
    - `shap_summary` covers the lean `SELECTED_FEATURES` (D5-07) with a `feature_importances_` fallback
    - `global_metrics` behavior from 05-06 is unchanged; `test_diagnostics_plots.py` passes
  </acceptance_criteria>
  <done>Calibration, PIT, and SHAP PNGs build headlessly and honestly (real coverage, grid-derived PIT) — the FCAST-03 committed artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: MODEL_CARD.md assembly (FCAST-05)</name>
  <read_first>
    - pipelines/historical/build.py (`_write_readme` at ~177-251 — the exact "assemble a committed markdown from computed stats" writer analog to mirror)
    - pipelines/forecast/baselines.py (05-09 — `release_gate` verdict + DM table) + diagnostics.py (Task 1 PNGs + `global_metrics`) + pipelines/features/build.py (`leakage_audit` + `anchor_leakage_audit` + `n_per_sector`) + pipelines/features/select.py (importance/stability table + SELECTED_FEATURES)
    - 05-RESEARCH.md §Phase Requirements FCAST-05 (model-card content: data window, feature list + available_at, 4 baselines + DM, calibration plots, PIT, limitations) + §Assumptions A7 (document the cross-sectional-DM caveat) / A8 (PIT grid-derived)
    - 05-CONTEXT.md D5-01 specifics + §Specific Ideas (low R² is a feature — state plainly; R²>0.5 would flag leakage)
  </read_first>
  <action>
    Create `pipelines/forecast/card.py` with `build_model_card(*, write=True) -> str` mirroring `build._write_readme`'s computed-markdown posture: assemble `model_card/MODEL_CARD.md` from the training window + N (panel/`global_metrics`), the LEAN feature list with each feature's `available_at` verdict (from `leakage_audit`) plus the explicit anchor leakage audit (D5-08 — the exact anchor field + T0−1 disclosure timestamp), the four baselines + Diebold–Mariano table with the `release_gate` verdict (P9) + the paired-Wilcoxon robustness + the A7 cross-sectional-DM caveat, the empirical coverage / MAE / per-year RMSE (`global_metrics`), the R²>0.5 leakage-alarm status, embedded references to the committed `calibration.png` / `pit.png` / `shap.png`, the n-per-sector table (D5-10), and a "Known limitations" section stating plainly that a pre-apply no-demand forecast is expected to be humble (low R² is a feature, D5-01) and that R²>0.5 would flag leakage. CODE-NOW-DEFER: assemble from the current seed/fixture artifacts now and commit a seed `model_card/MODEL_CARD.md` + placeholder PNGs so `/methodology` renders offline; the REAL card regenerates from the live walk-forward at the 05-11 checkpoint. Escape or omit any untrusted-origin string; keep it prescriptive-token-free (no buy/sell/subscribe/target as advice — informational only). Write `tests/unit/test_model_card.py`: `build_model_card(write=False)` returns markdown containing a calibration-plot reference, a PIT-histogram reference, a four-row baselines+DM table, an `available_at` leakage-audit section (incl. the anchor pre-open audit), the R²-alarm status, an n-per-sector section, and a limitations section — and it contains no secret/PII and no banned prescriptive token.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_model_card.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_model_card` assembles `MODEL_CARD.md` containing: a calibration plot ref, a PIT histogram ref, a four-row baselines+Diebold–Mariano table with the P9 release-gate verdict, the `available_at` leakage-audit section incl. the anchor pre-open audit (D5-08), the R²>0.5 alarm status, an n-per-sector section (D5-10), and a Known-limitations section (low R² is a feature, D5-01)
    - the committed `model_card/MODEL_CARD.md` + PNGs exist so `/methodology` renders offline (CODE-NOW-DEFER; real regeneration at 05-11)
    - the card contains no secret/PII and no banned prescriptive token (informational only)
    - `test_model_card.py` passes
  </acceptance_criteria>
  <done>The committed public MODEL_CARD.md is assembled from every computed input — the FCAST-05 Success-Criterion-4 artifact.</done>
</task>

<task type="auto">
  <name>Task 3: /methodology forecaster model-card section render (UI-03)</name>
  <read_first>
    - pages/01_methodology.py (the `render_section_head` + `.drhp-metrics` table + `.drhp-hiw-card` grammar; the existing "Forecast coverage · MAPIE conformal · ≈ 80% · Phase 5" metrics row at ~87 — deepen it into the model-card section)
    - 05-UI-SPEC.md §item 2 (the model card on /methodology: calibration plot, PIT histogram, four baselines + Diebold–Mariano, `available_at <= T0` leakage audit, limitations) + §L5-3 (the `Full model card →` link target) + §"pages/01_methodology.py — MODIFY" render grammar (05-PATTERNS.md — st.image embeds from model_card/; reuse .drhp-metrics for the DM table, .drhp-hiw-card for limitations)
    - ui/chrome.py (`render_section_head` signature)
    - tests/unit/test_gmp_isolation.py (the `inspect.getsource` substring-audit grammar — the render must import no model lib)
  </read_first>
  <action>
    Append a "Forecaster model card" section to `pages/01_methodology.py` after the Evaluation section, using `render_section_head("Forecaster", "The listing-day model card")` and the existing markdown grammar: embed the committed `model_card/calibration.png` + `pit.png` + `shap.png` via `st.image(...)` (read from the repo `model_card/` dir); render the four-baselines + Diebold–Mariano results as a `.drhp-metrics` table (columns: baseline · DM stat · p-value · verdict) with the P9 release-gate verdict; render the `available_at <= T0` leakage audit (incl. the anchor pre-open audit, D5-08) and the n-per-sector (D5-10) as `.drhp-metrics`/list markup; render "Known limitations" as a `.drhp-hiw-card` grid (state that low R² is honest for a pre-apply no-demand model, D5-01). Read the committed `MODEL_CARD.md` / plain-data artifacts only — import NO xgboost/mapie/sklearn/shap/model module (the page stays render-only, isolation). This section is the destination of 05-07's `Full model card →` link (`/methodology`, UI-03). Write `tests/unit/test_methodology_render.py`: assert `inspect.getsource(pages/01_methodology.py)` (read the file source) references `model_card/calibration.png`, `pit.png`, `shap.png`, a baselines/Diebold–Mariano table, the `available_at` leakage-audit section, and a limitations grid; and assert the page source contains none of `xgboost`/`mapie`/`sklearn`/`shap` (render-only isolation).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_methodology_render.py -x -q && .venv/bin/python -m pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `pages/01_methodology.py` gains a Forecaster model-card section embedding `calibration.png` + `pit.png` + `shap.png` (st.image from `model_card/`), a four-baselines+DM `.drhp-metrics` table with the P9 verdict, the `available_at` leakage audit (incl. anchor, D5-08), n-per-sector (D5-10), and a limitations grid
    - the section is the destination of the forecast block's `Full model card →` link (`/methodology`, UI-03)
    - the page source contains no `xgboost`/`mapie`/`sklearn`/`shap` (render-only isolation)
    - `test_methodology_render.py` passes and `pytest tests/unit -q` stays green
  </acceptance_criteria>
  <done>The /methodology page renders the committed model card (plots + DM table + leakage audit + limitations); the forecast block's link now has its real destination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline model artifacts -> committed repo (model_card/) | PNGs + MODEL_CARD.md become public, repo-committed artifacts |
| model pipeline <-> /methodology render | The methodology page must import no model/training library (isolation) |
| card-derived strings -> rendered markdown | Any computed string interpolated into the page must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-ISO | (invariant breach) | /methodology render <-> model | mitigate | `pages/01_methodology.py` imports no xgboost/mapie/sklearn/shap/model module; reads committed `model_card/` artifacts only; pinned by `test_methodology_render.py` source audit |
| T-05-10-PII | Information disclosure | committed MODEL_CARD.md + PNGs | mitigate | The card is assembled from aggregate metrics/audit only; test asserts no secret/PII; no raw DRHP text or credentials embedded |
| T-05-10-HONEST | Repudiation (calibration theater) | coverage + PIT + DM presentation | mitigate | Real empirical coverage (never 0.80-rounded, P17); PIT labeled grid-derived (A8); DM cross-sectional caveat documented (A7); low R² stated plainly (D5-01) |
| T-05-10-XSS | Tampering (injection) | card-derived strings in the page | mitigate | Methodology copy is repo-committed trusted config; any computed string interpolated into markup is escaped; formatted floats are safe |
| T-05-10-SC | Tampering | pip installs | mitigate | No new installs; matplotlib/shap already Approved in the 05-01 §Package Legitimacy Audit; imported lazily (offline at module import) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_diagnostics_plots.py tests/unit/test_model_card.py tests/unit/test_methodology_render.py -q` passes; `pytest tests/unit -q` green.
- Committed `model_card/MODEL_CARD.md` + `calibration.png` + `pit.png` + `shap.png` exist; /methodology renders the model-card section offline.
- Coverage is the real number; PIT grid-derived; DM table + P9 verdict present; render imports no model lib.
</verification>

<success_criteria>
- Calibration plot + PIT histogram + SHAP committed as PNG artifacts (FCAST-03, P17).
- MODEL_CARD.md committed covering data/features/baselines+DM/plots/limitations (FCAST-05, Success Criterion 4).
- /methodology renders the forecaster model-card section as the forecast block's link destination (UI-03); render imports no model module.
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-10-SUMMARY.md` when done.
</output>
</content>
