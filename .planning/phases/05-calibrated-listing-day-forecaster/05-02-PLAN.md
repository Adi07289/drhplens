---
phase: 05-calibrated-listing-day-forecaster
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - pipelines/historical/sources.py
  - pipelines/historical/build.py
  - tests/unit/test_historical_panel.py
autonomous: true
requirements: [FCAST-03]
must_haves:
  truths:
    - "The universe assembler merges a listed-core source (NSE past-issues) with a withdrawn/pulled overlay (SEBI/chittorgarh-withdrawn) so a built panel can contain withdrawn AND delisted rows (P3 survivorship control)"
    - "The dead chittorgarh HTML-table fetcher is retired as the primary listed-core path (D5-04)"
    - "Only hard-coded allow-listed hosts are ever fetched (SSRF); www.nseindia.com is added to the allow-list"
    - "A nightly integration test can hit the live NSE past-issues endpoint (CLAUDE.md India-data requirement)"
  artifacts:
    - path: "pipelines/historical/sources.py"
      provides: "fetch_nse_past_issues (Source A) + fetch_sebi_withdrawn (Source B); extended ALLOWED_HOSTS"
      contains: "fetch_nse_past_issues"
    - path: "pipelines/historical/build.py"
      provides: "two-source merge + dedupe by issuer+issue_date into assemble_panel"
      contains: "fetch_nse_past_issues"
    - path: "tests/unit/test_historical_panel.py"
      provides: "offline (monkeypatched) two-source-merge assertion + nightly integration marker test"
      contains: "withdrawn"
  key_links:
    - from: "pipelines/historical/build.py"
      to: "pipelines/historical/sources.fetch_nse_past_issues + fetch_sebi_withdrawn"
      via: "per-source failure-isolated merge"
      pattern: "fetch_nse_past_issues|fetch_sebi_withdrawn"
    - from: "pipelines/historical/build.py"
      to: "pipelines/historical.assemble_panel"
      via: "deduped rows -> typed survivorship panel"
      pattern: "assemble_panel"
---

<objective>
Slice 1 (data foundation, D5-04/D5-05): repoint the historical universe assembler off the dead chittorgarh HTML scraper onto a survivorship-safe two-source merge — NSE `public-past-issues` for the listed core plus a SEBI/chittorgarh-withdrawn overlay for the P3 control — reusing the existing panel schema, validator, typed coercers, SSRF allow-list, and cached polite session verbatim. All live fetchers stay deferred seams; the merge is proven offline with monkeypatched fetchers.

Purpose: without a withdrawn/pulled overlay the universe is survivor-only and FCAST-03/P3 fails; this unblocks the walk-forward backtest's training pool.
Output: extended `sources.py` fetchers + `ALLOWED_HOSTS`, a two-source merge in `build.py`, and an extended offline panel test asserting withdrawn+delisted survive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@pipelines/historical/sources.py
@pipelines/historical/build.py
@pipelines/historical/__init__.py
@pipelines/historical/validate.py
@data/historical/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two survivorship-safe source fetchers + SSRF allow-list extension</name>
  <read_first>
    - pipelines/historical/sources.py (the file being modified — ALLOWED_HOSTS at lines ~38-44, _check_host at ~78-85, coerce_price/coerce_date/normalize_status at ~93-141, _STATUS_ALIASES at ~63-75, lazy _session/_get at ~149-182, the dead fetch_chittorgarh_index at ~190-228, fetch_sebi_offer_documents stub at ~231-239)
    - 05-RESEARCH.md §"Data Sources (D5-04)" — Source A NSE `https://www.nseindia.com/api/public-past-issues` (cookie priming; prefer the `nse` library at the live checkpoint), Source B SEBI `https://www.sebi.gov.in/filings/public-issues.html` + chittorgarh withdrawn report id 202 `https://www.chittorgarh.com/report/ipo-drhp-offer-document-withdrawn/202/`; §Assumptions A1-A3 (field names unconfirmed until first live pull — snapshot raw JSON)
    - 05-PATTERNS.md §"pipelines/historical/sources.py — MODIFY for D5-04"
  </read_first>
  <action>
    Extend `ALLOWED_HOSTS` (SSRF control T-04-07-SSRF) to add `"www.nseindia.com"` (the past-issues JSON host) while keeping the existing hosts; leave the chittorgarh + sebi hosts in place. Add `fetch_nse_past_issues(from_date, to_date) -> list[dict]` as Source A: GET the NSE `public-past-issues` JSON via the existing `_get()` (which enforces `_check_host` + backoff + cached session), routing every parsed field through `coerce_price`/`coerce_date`/`normalize_status`, and — matching the existing defensive posture — persist the raw JSON alongside parsed rows (field names unconfirmed per A1, so save-raw). Mark it `# pragma: no cover - live only` and lazily import any client. Add `fetch_sebi_withdrawn() -> list[dict]` as Source B (the P3 overlay): pull the SEBI public-issues filings and the chittorgarh withdrawn report (id 202), emitting rows whose `status` normalizes to `withdrawn` (extend `_STATUS_ALIASES` for any new SEBI/withdrawn wording so it maps into STATUS_VALUES). Repurpose the existing `fetch_sebi_offer_documents` stub into `fetch_sebi_withdrawn` (or keep the name and have `fetch_sebi_withdrawn` call it). Retire `fetch_chittorgarh_index` as the PRIMARY listed-core path — keep the function (demoted to optional enrichment/cross-check per D5-04) but stop `build.py` calling it first. Do NOT derive any URL from user/DRHP input; every URL stays a module constant.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pipelines.historical.sources as s; assert 'www.nseindia.com' in s.ALLOWED_HOSTS; assert hasattr(s,'fetch_nse_past_issues') and hasattr(s,'fetch_sebi_withdrawn'); import inspect; assert '_check_host' in inspect.getsource(s.fetch_nse_past_issues) or 'src via _get' or '_get' in inspect.getsource(s.fetch_nse_past_issues); print('sources ok')" && .venv/bin/python -m pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `pipelines.historical.sources.ALLOWED_HOSTS` contains `www.nseindia.com` (and still contains the SEBI + chittorgarh + nsearchives hosts)
    - `fetch_nse_past_issues` and `fetch_sebi_withdrawn` exist, are `# pragma: no cover - live only`, and route fetches through `_get()` (so `_check_host` runs) — no bare `requests.get`
    - `fetch_nse_past_issues`/`fetch_sebi_withdrawn` derive no URL from any argument that could be user-influenced (URLs are module constants)
    - importing the module does no network (lazy client imports preserved); full `pytest tests/unit -q` stays green
  </acceptance_criteria>
  <done>The listed-core NSE fetcher and the withdrawn SEBI/chittorgarh overlay exist behind the SSRF allow-list; chittorgarh HTML is demoted from primary.</done>
</task>

<task type="auto">
  <name>Task 2: Two-source merge + dedupe in build.py, and the offline survivorship test</name>
  <read_first>
    - pipelines/historical/build.py (the file being modified — build_panel at ~83-121 with per-source try/except isolation, derive_status at ~65-75, UNIVERSE_START, assemble_panel/write_panel usage, the build vs build-sample CLI split at ~347-366)
    - pipelines/historical/validate.py (sanity_check_median — the >20%-median survivorship alarm to keep running on the built panel)
    - tests/unit/test_historical_panel.py (the file being extended — existing offline sample assertions)
    - tests/unit/test_gmp_precompute.py (the monkeypatch posture: monkeypatch.setattr the source fetchers, no live network under pytest)
    - 05-PATTERNS.md §"pipelines/historical/build.py — MODIFY for the two-source merge"
  </read_first>
  <action>
    In `build_panel`, replace the primary `fetch_chittorgarh_index()` call with `_sources.fetch_nse_past_issues(...)` (Source A) and keep a second per-source-isolated block calling `_sources.fetch_sebi_withdrawn()` (Source B, the P3 overlay) — each in its own `try/except Exception  # noqa: BLE001` so one flaky source never aborts the batch. Merge both row lists, dedupe by `(issuer, issue_date)` (prefer the listed-core row's prices when a withdrawn overlay row collides, but keep `withdrawn` status when only the overlay has the issuer). Keep `derive_status`, the `UNIVERSE_START` window filter, `assemble_panel` (replace-with-NaN, never drop), `write_panel`, and `sanity_check_median` exactly as-is. Add a non-zero-row assertion to the live `build` CLI (RESEARCH Pitfall 7: a silent 0-row build is the 04-07 failure). Then EXTEND `tests/unit/test_historical_panel.py`: add an offline test that monkeypatches `fetch_nse_past_issues` to return a few listed rows and `fetch_sebi_withdrawn` to return withdrawn rows, calls `build_panel(write=False)`, and asserts the built panel's `status` distribution contains a non-zero `withdrawn` count AND a non-zero `delisted`/listed mix, that dedupe removed the duplicate issuer, and that a NaN-return row is RETAINED (row count preserved). Add a separate `@pytest.mark.integration` test that hits the live NSE `public-past-issues` endpoint (CLAUDE.md nightly requirement) — excluded from the quick unit loop.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_historical_panel.py -q</automated>
  </verify>
  <acceptance_criteria>
    - the new offline test monkeypatches both fetchers (no live network) and asserts the built panel has `status=="withdrawn"` count > 0 after the merge (P3)
    - the test asserts dedupe by (issuer, issue_date) collapses a duplicate issuer to one row, and that a listing-price-missing row is retained with NaN return (row count == input minus dedupe only)
    - `build_panel` calls `fetch_nse_past_issues` (primary) + `fetch_sebi_withdrawn` (overlay), each per-source failure-isolated; it no longer calls `fetch_chittorgarh_index` as the primary path
    - a `@pytest.mark.integration` NSE test exists and is deselected by the default `pytest tests/unit -q` run (markers strict)
    - `.venv/bin/python -m pytest tests/unit/test_historical_panel.py -q` passes
  </acceptance_criteria>
  <done>The offline-tested two-source merge yields a survivorship-representable panel; a nightly live-NSE integration test is registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NSE/SEBI/chittorgarh (untrusted web) → offline pipeline | Scraped HTML/JSON is untrusted input crossing into the panel builder |
| fetch host selection → outbound request | Any host the pipeline fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-SSRF | Information disclosure | `fetch_nse_past_issues`/`fetch_sebi_withdrawn` outbound hosts | mitigate | Extend hard-coded `ALLOWED_HOSTS` (+`www.nseindia.com`); every fetch routes through `_get()`→`_check_host`; no URL derived from input (URLs are module constants) |
| T-05-02-VALID | Tampering / DoS | untrusted NSE JSON / SEBI+chittorgarh HTML parse | mitigate | Route every field through `coerce_price`/`coerce_date`/`normalize_status`; missing → None→NaN retained (never a fabricated survivor); per-row + per-source failure isolation; save raw payload alongside parsed rows (A1) |
| T-05-02-SURV | Tampering (data integrity) | survivorship control | mitigate | Two-source merge (listed core ∪ withdrawn overlay); `sanity_check_median` >20%-median alarm still runs; non-zero-row + non-zero-withdrawn assertions |
| T-05-02-SC | Tampering | `nse` library (needed only at live checkpoint) | transfer | Not installed here; gated behind the 05-11 human-verify checkpoint (blocking-human) before the live crawl |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_historical_panel.py -q` passes.
- Offline merge test asserts non-zero withdrawn rows + dedupe + NaN-retention.
- `ALLOWED_HOSTS` contains `www.nseindia.com`; module imports do no network.
</verification>

<success_criteria>
- Survivorship-safe two-source universe merge implemented and offline-tested (FCAST-03/P3).
- Dead chittorgarh HTML fetcher demoted from primary (D5-04).
- Nightly live-NSE integration test registered.
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-02-SUMMARY.md` when done.
</output>
