---
phase: 05-calibrated-listing-day-forecaster
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - pipelines/forecast/diagnostics.py
  - pipelines/forecast/precompute.py
  - tests/unit/test_forecast_metrics.py
  - tests/unit/test_forecast_precompute.py
autonomous: true
requirements: [FCAST-01, FCAST-04]
must_haves:
  truths:
    - "Global empirical coverage, MAE, and per-year RMSE are computed from the out-of-sample walk-forward rows and are identical on every IPO page (D5-12)"
    - "Empirical coverage is the real held-out number (never rounded to 80%) (P17)"
    - "A precompute CLI writes one data/forecasts/<drhp_id>.json per catalogue IPO — each record's band is that IPO's OOS band + the GLOBAL metrics + abstain flag + as-of/OOS provenance + model_version (D5-11)"
    - "The single walk-forward metrics computation is wrapped in an mlflow.start_run() (local file backend) logging coverage/MAE/per-year-RMSE + key CQR/XGBoost params to mlruns/ — the DS-rigor experiment-tracking visibility CLAUDE.md/RESEARCH lock"
    - "The precompute is unit-tested fully offline by monkeypatching the walk-forward output; per-IPO failure isolation matches the GMP/snapshot batch pattern; MLflow tracking points at a tmp dir in tests (no server)"
  artifacts:
    - path: "pipelines/forecast/diagnostics.py"
      provides: "global_metrics(oos_df) -> {coverage_empirical, mae_pts, per_year_rmse, mean_width, n}"
      contains: "coverage"
    - path: "pipelines/forecast/precompute.py"
      provides: "Typer CLI writing data/forecasts/<id>.json per catalogue IPO (allow-list gated, per-IPO isolated) + mlflow.start_run() tracking of the global metrics"
      contains: "is_known_drhp_id"
    - path: "tests/unit/test_forecast_metrics.py"
      provides: "coverage/MAE/per-year-RMSE correctness on known OOS rows"
      contains: "coverage"
  key_links:
    - from: "pipelines/forecast/precompute.py"
      to: "agent.forecast_schema.ForecastRecord + data/forecasts/<id>.json"
      via: "walk-forward OOS band + global metrics -> committed record"
      pattern: "ForecastRecord|data/forecasts"
    - from: "pipelines/forecast/precompute.py"
      to: "data.catalogue_loader.is_known_drhp_id"
      via: "allow-list gate before path formation"
      pattern: "is_known_drhp_id"
    - from: "pipelines/forecast/precompute.py"
      to: "mlflow (local file backend -> mlruns/)"
      via: "mlflow.start_run() + log_params/log_metrics on the single global metrics computation"
      pattern: "mlflow\\.(start_run|log_metrics|log_params)"
---

<objective>
Slice 2d (metrics + the record writer, FCAST-04 / D5-12 / D5-11) — compute the GLOBAL walk-forward honesty metrics (empirical coverage, MAE, per-year RMSE) and the Typer precompute CLI that materialises one `data/forecasts/<drhp_id>.json` per covered IPO. Each record carries that IPO's own out-of-sample band (D5-11: displayed = backtested) plus the global metrics block (identical on every page, D5-12), the abstain flag, and the as-of/OOS provenance + model_version. The single metrics computation is wrapped in an `mlflow.start_run()` (local file backend) that logs coverage/MAE/per-year-RMSE + key CQR/XGBoost params to `mlruns/` — the experiment-tracking visibility CLAUDE.md/RESEARCH lock (kept minimal: log to the local file store, no tracking server). The CLI is CODE-NOW-DEFER: fully unit-tested offline by monkeypatching the walk-forward output (MLflow pointed at a tmp dir); the real committed records + the committed `mlruns/` run are regenerated from the live panel at the 05-11 checkpoint.

Purpose: turns the walk-forward output into the exact records the 05-07 render reads (FCAST-04's always-visible metrics + FCAST-01's band), and gives the forecaster the MLflow tracking rigor the stack locks.
Output: `pipelines/forecast/diagnostics.py::global_metrics`, `pipelines/forecast/precompute.py` (+ CLI + MLflow wiring), and the metrics + precompute tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@pipelines/gmp.py
@pipelines/snapshot.py
@agent/forecast_schema.py
@data/catalogue_loader.py
@tests/unit/fixtures/forecast_fixtures.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: global_metrics — coverage / MAE / per-year RMSE (D5-12)</name>
  <read_first>
    - 05-RESEARCH.md §"Code Examples — Empirical coverage, MAE, per-year RMSE" (lines ~402-415 — the exact one-liner formulas; show the REAL coverage number, P17) + §"Pitfall 5: Calibration theater"
    - pipelines/historical/validate.py (the (value, flag_or_None) plain-text posture for surfacing on /methodology)
    - tests/unit/fixtures/forecast_fixtures.py (oos_rows() — the per-IPO OOS fixture)
    - agent/forecast_schema.py (ForecastMetrics — the fields global_metrics must populate)
  </read_first>
  <behavior>
    - global_metrics(oos_df) returns coverage_empirical = mean((actual>=low)&(actual<=high)), mae_pts = mean(|actual-median|), per_year_rmse = groupby listing_year sqrt(mean(se)), mean_width, n
    - coverage is the raw float (e.g. 0.783), never rounded to 0.80
    - only non-abstain OOS rows contribute; a year with no rows is absent (not fabricated)
  </behavior>
  <action>
    Create `pipelines/forecast/diagnostics.py` with `global_metrics(oos_df) -> dict` per RESEARCH §Code Examples: `coverage_empirical` = float mean of `(actual >= low) & (actual <= high)`; `mae_pts` = float mean of `|actual - median|` (in points, i.e. return-% points — the UI shows errors in points); `per_year_rmse` = groupby `listing_year` of sqrt(mean((actual-median)^2)) rounded to 1-2 dp as a `{str(year): float}` dict; `mean_width` = mean(high-low); `n` = row count of scored (non-abstain) rows. Return the RAW coverage (P17 — never round to 0.80). Exclude abstain rows from every statistic. Keep this metrics-only for now (calibration/PIT/SHAP plots land in 05-10). Write `tests/unit/test_forecast_metrics.py` with a hand-constructed OOS frame of known values asserting exact coverage (e.g. 3 of 4 covered -> 0.75), exact MAE, and per-year RMSE keys/values; and asserting an all-covered frame does NOT get silently rounded to 0.80.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_forecast_metrics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `global_metrics` on a known 4-row frame with 3 covered returns `coverage_empirical == 0.75` exactly (raw, not rounded to 0.80) — P17
    - `mae_pts` and `per_year_rmse` match hand-computed values; `per_year_rmse` keys are the listing years present, no fabricated year
    - abstain rows are excluded from all statistics; `n` counts only scored rows
    - `test_forecast_metrics.py` passes
  </acceptance_criteria>
  <done>Global coverage/MAE/per-year-RMSE compute correctly and honestly from OOS rows (D5-12, FCAST-04).</done>
</task>

<task type="auto">
  <name>Task 2: precompute CLI — write data/forecasts/<id>.json per catalogue IPO (offline-tested) + MLflow tracking</name>
  <read_first>
    - pipelines/gmp.py (the precompute analog — precompute_gmp write path at ~110-170, `_gmp_path` allow-list gate, the precompute-one/precompute-all Typer CLI + per-IPO failure isolation loop at ~178-224; the CODE-NOW-DEFER docstring at ~23-27)
    - pipelines/snapshot.py (the same precompute → write data/<kind>/<id>.json → per-IPO isolation pattern)
    - agent/forecast_schema.py + pipelines/forecast/__init__.py (ForecastRecord + FORECASTS_DIR + _forecast_path allow-list gate to reuse)
    - pipelines/forecast/walkforward.py + diagnostics.py (walk_forward + global_metrics — the inputs)
    - 05-RESEARCH.md §"Runtime → Boundary" MLflow rows (lines ~75-76) + §Standard Stack `mlflow` row (line ~90 — "local file backend, commit `mlruns/`") + §Environment Availability (mlflow degrade note, line ~554) — MLflow is the locked experiment tracker; keep it minimal (local file store, no server)
    - tests/unit/test_gmp_precompute.py (the monkeypatch-the-model-seam offline test posture to mirror)
  </read_first>
  <action>
    Create `pipelines/forecast/precompute.py` (Typer app + rich Console, mirroring `pipelines/gmp.py`). Implement `precompute_forecasts(*, panel=None, write=True, track=True) -> dict` that runs `walk_forward(...)` once over the panel to get OOS rows, computes `global_metrics(oos_rows)` once, then for each catalogue IPO (allow-list gated via `is_known_drhp_id` BEFORE any path is formed) builds a `ForecastRecord`: interval = that IPO's OOS band (low_pct/high_pct/median_pct/width_pts), or abstain=True + reason when the walk-forward abstained; `metrics` = the shared GLOBAL metrics block (identical across IPOs, D5-12); `as_of_listing_date` = the IPO's listing date (D5-11 provenance); `out_of_sample=True`, `walk_forward=True`; `model_version` a pinned string (e.g. "cqr-xgb-2026.07-v1"); `sector` from the catalogue. Write via `_forecast_path(drhp_id)` + `record.to_json()`. When `track` is True, wrap the single `walk_forward` + `global_metrics` computation in an `mlflow.start_run()` (import mlflow LAZILY inside the function; local file backend — do NOT set a remote tracking URI): `mlflow.log_params` the key CQR/XGBoost params (quantile alphas 0.1/0.5/0.9, `confidence_level=0.8`, `min_train`, `cal_frac`, `n_estimators`, `max_depth`, `model_version`) and `mlflow.log_metrics` the global `coverage_empirical`, `mae_pts`, `mean_width`, `n`, and per-year RMSE as `rmse_<year>`. Keep it minimal — this is DS-rigor visibility, not a tracking-server build; the run lands under the default `mlruns/` (committed at 05-11 per the CLAUDE.md deploy plan). Add `precompute-one <drhp_id>` and `precompute-all` CLI commands with per-IPO failure isolation (`# noqa: BLE001`, log-and-skip — one IPO never aborts the batch, P14). Import xgboost/mapie only transitively via walkforward's lazy imports. Mark the real live run CODE-NOW-DEFER (needs the real panel from 05-11). Write `tests/unit/test_forecast_precompute.py`: monkeypatch `walk_forward` to return a small crafted OOS frame (no training, no network); (a) call `precompute_forecasts(write=False, track=False)` and assert each catalogue IPO's record carries its own band and the SHARED global metrics; (b) an abstained IPO yields an abstain record with no fabricated interval; (c) an unknown id is rejected before path formation; (d) per-IPO isolation — one crafted failure does not abort the batch; (e) with `track=True` and `mlflow.set_tracking_uri("file://<tmp_path>/mlruns")` (tmp_path fixture — offline, local file store, no server), `precompute_forecasts` opens a run and logs `coverage_empirical`/`mae_pts` (assert a run dir is created under the tmp mlruns and the logged metrics are readable via `mlflow.search_runs`/the run's `data.metrics`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_forecast_precompute.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `precompute_forecasts(write=False, track=False)` (with walk_forward monkeypatched) yields one ForecastRecord per catalogue IPO, each carrying its own interval and the IDENTICAL global metrics block (D5-12)
    - an abstained IPO produces `abstain=True` with a valid reason and no fabricated interval
    - the writer gates `drhp_id` via `is_known_drhp_id` before forming any `data/forecasts/<id>.json` path (path-traversal)
    - `precompute-all` isolates a single IPO failure (logged, skipped) without aborting (P14)
    - with `track=True` against a tmp `file://.../mlruns` tracking URI, `precompute_forecasts` opens an `mlflow.start_run()` and logs `coverage_empirical`/`mae_pts` + key CQR params to the local file backend — the test asserts an `mlruns/` run dir is created with the logged coverage/MAE metrics (no network, no server)
    - no live network/training under `pytest tests/unit`; `test_forecast_precompute.py` passes
  </acceptance_criteria>
  <done>The precompute CLI writes per-IPO records (own band + global metrics + provenance) offline-testably AND tracks the global metrics to a local MLflow run; real records + mlruns/ regenerate at 05-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `drhp_id` (catalogue loop) → filesystem path | Even catalogue-sourced, the write path is allow-list gated |
| walk-forward output → committed cache | The OOS rows become the user-visible records |
| MLflow tracking URI → local file store | Kept local (`mlruns/` / tmp in tests); never a remote server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-PATH | Tampering | `precompute_forecasts`/`_forecast_path` | mitigate | `is_known_drhp_id` gate before any `data/forecasts/<id>.json` path is formed (verbatim GMP pattern); tested with an unknown id |
| T-05-06-FAB | Repudiation (honesty) | coverage/metrics + abstain records | mitigate | Raw empirical coverage (never rounded to 0.80, P17); abstain records fabricate no interval; global metrics identical on every page (D5-12) |
| T-05-06-ISO | (invariant breach) | precompute ↔ GMP | mitigate | precompute imports no `gmp`; pinned by the 05-03 predictor-side isolation audit |
| T-05-06-BATCH | DoS | precompute-all batch | mitigate | Per-IPO failure isolation (log-and-skip) mirrors the GMP/snapshot P14 pattern |
| T-05-06-MLF | Information disclosure | MLflow tracking backend | mitigate | Local file backend only (default `mlruns/`, tmp dir in tests); mlflow imported lazily; no remote tracking URI, no credentials, no network egress |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_forecast_metrics.py tests/unit/test_forecast_precompute.py -q` passes.
- Coverage is raw (not 0.80-rounded); records carry own band + shared global metrics; writer is allow-list gated; MLflow logs coverage/MAE to a local `mlruns/` run (tmp dir in tests).
</verification>

<success_criteria>
- Global coverage/MAE/per-year-RMSE computed honestly (FCAST-04, D5-12, P17).
- Per-IPO forecast records written (own band + global metrics + provenance, D5-11) offline-testably (FCAST-01).
- The global metrics + key CQR/XGBoost params are MLflow-tracked to a local file-backend run (the locked experiment tracker; minimal, no server).
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-06-SUMMARY.md` when done.
</output>
</output>
