---
phase: 05-calibrated-listing-day-forecaster
plan: 09
type: execute
wave: 7
depends_on: ["05-05", "05-06", "05-08"]
files_modified:
  - pipelines/forecast/baselines.py
  - pipelines/forecast/walkforward.py
  - tests/unit/test_baselines_dm.py
  - tests/unit/test_release_gate.py
  - tests/unit/test_walkforward_abstention.py
autonomous: true
requirements: [FCAST-05, FCAST-03]
must_haves:
  truths:
    - "All four baselines (predict-zero, global-median, trailing-12-IPO-median, sector-mean) are scored under the SAME walk-forward as-of-T0 protocol as the model (P9)"
    - "An inline ~15-line Diebold–Mariano test (Harvey small-sample correction) tests the model's MAE-loss differential against each baseline — the dieboldmariano package is NOT added"
    - "The P9 release gate FAILS on an R²>0.5 leakage alarm OR when a baseline significantly beats the model, and PASSES with an honest 'does not significantly outperform' note on a statistical tie (D5-01, no p-hacking)"
    - "D5-09 abstention fires out_of_support (features outside training support) and interval_too_wide (calibrated width above guard), rendering the locked no-band note"
  artifacts:
    - path: "pipelines/forecast/baselines.py"
      provides: "baselines_asof + score_baselines + inline dm_test + wilcoxon_robustness + release_gate (P9 verdict)"
      contains: "dm_test"
    - path: "pipelines/forecast/walkforward.py"
      provides: "D5-09 abstention: out_of_support + interval_too_wide branches added to walk_forward"
      contains: "out_of_support"
    - path: "tests/unit/test_baselines_dm.py"
      provides: "four baselines as-of-T0 + DM significance on a crafted frame (Wave-0 FCAST-05/P9 test)"
      contains: "dm_test"
    - path: "tests/unit/test_release_gate.py"
      provides: "gate FAILS on R²>0.5 and on baseline-beats-model; PASSES honestly on a tie"
      contains: "passed"
  key_links:
    - from: "pipelines/forecast/baselines.py release_gate"
      to: "pipelines/forecast/walkforward.r2_leakage_alarm (05-05)"
      via: "R²>0.5 leakage alarm consumed as a hard gate (P4/P9)"
      pattern: "r2_leakage_alarm"
    - from: "pipelines/forecast/walkforward.py"
      to: "pipelines/features/select.is_out_of_support (05-08)"
      via: "per-IPO out_of_support abstention (D5-09)"
      pattern: "is_out_of_support"
---

<objective>
Slice 3b (the P9 release gate + D5-09 abstention) — the phase's honesty gate. Score all four locked baselines (predict-zero, global-median, trailing-12-IPO-median, sector-mean) under the identical walk-forward as-of-T0 protocol as the model, then apply an INLINE ~15-line Diebold–Mariano test (Harvey small-sample correction; no `dieboldmariano` dependency) to the MAE-loss differentials. Compose the P9 release gate: it FAILS on the R²>0.5 leakage alarm (the piece 05-04 deferred, function built in 05-05) OR when a naive baseline significantly beats the model, and PASSES honestly — with an explicit "does not significantly outperform" note — on a statistical tie (a pre-apply, no-demand model is expected to be humble, D5-01; never p-hack features to cross the gate). Finally wire the D5-09 conformal-native abstention (out_of_support + interval_too_wide) into the walk-forward.

Purpose: P9 is the ROADMAP-locked release gate; the model card (05-10) embeds the DM table + verdict; abstention completes the three ForecastRecord abstain reasons.
Output: new `pipelines/forecast/baselines.py` (baselines + inline DM + release gate), extended `walkforward.py` (D5-09 abstention), and the baselines/DM, release-gate, and abstention tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-VALIDATION.md
@pipelines/forecast/walkforward.py
@pipelines/historical/validate.py
@agent/forecast_schema.py
@tests/unit/fixtures/forecast_fixtures.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four baselines as-of-T0 + inline Diebold–Mariano + Wilcoxon robustness</name>
  <read_first>
    - 05-RESEARCH.md §"Four baselines, scored as-of-T0" (lines ~417-430 — the exact four: predict_zero=0.0, global_median, trailing_12 (median of the 12 most-recent prior listings), sector_mean (mean of the D5-10 'Other'-pooled prior listings); all from `pool = rows[listing_date < T0_i]`) + §"Diebold–Mariano (inline; avoids the dieboldmariano dependency)" (lines ~432-446 — `d = |e_model| - |e_base|`, Harvey small-sample correction, `scipy.stats.t`; `dm<0 & p<0.05` => model loss significantly LOWER) + §Open Questions Q2 (also report a paired Wilcoxon robustness; document the A7 cross-sectional-DM caveat)
    - pipelines/forecast/walkforward.py (the as-of-T0 pool = rows[listing_date < issue_date] — baselines are scored inside the SAME pool)
    - pipelines/historical/validate.py (the `(value, flag_or_None)` plain-text posture to mirror for verdict text)
    - tests/unit/fixtures/forecast_fixtures.py (oos_rows — the per-IPO OOS fixture)
  </read_first>
  <behavior>
    - baselines_asof(pool, sector) returns the four exact baseline predictions computed only from pool rows with listing_date < T0_i
    - dm_test(e_model, e_base) returns (dm_stat, p_value): dm<0 & p<0.05 when model loss is significantly lower; no significance on a tie
    - wilcoxon_robustness returns a paired signed-rank p-value as the A7 robustness check
  </behavior>
  <action>
    Create `pipelines/forecast/baselines.py` importing only numpy/pandas/scipy (model-free — NO xgboost/mapie/gmp; isolation). Implement `baselines_asof(pool, sector) -> dict` per RESEARCH §Four baselines: `predict_zero`=0.0, `global_median`=median of the pool's `listing_day_return`, `trailing_12`=median of the 12 most-recent prior listings by `listing_date`, `sector_mean`=mean of the pooled-sector prior listings (the D5-10 'Other'-pooled `sector`) — all from `pool = rows[listing_date < T0_i]` (the SAME as-of-T0 protocol as the model, P9). Implement `score_baselines(panel, oos_df) -> pandas.DataFrame` producing, per model OOS IPO, each baseline's prediction + absolute error aligned to the model's OOS rows. Implement the inline `dm_test(e_model, e_base, h=1) -> tuple[float, float]` per RESEARCH §Diebold–Mariano (MAE-loss differential `|e_model| - |e_base|`, `h=1` variance, Harvey small-sample correction, `scipy.stats.t` two-sided p) and `wilcoxon_robustness(e_model, e_base) -> float` (paired signed-rank p, the A7 robustness check). Do NOT add the `dieboldmariano` dependency (the inline test avoids the [ASSUMED] low-download package — pinned-deps threat). Write `tests/unit/test_baselines_dm.py` (the VALIDATION.md Wave-0 `test_baselines_dm.py`): (a) `baselines_asof` on a crafted pool returns the four exact values; (b) `dm_test` returns dm<0 & p<0.05 when model errors are uniformly smaller, and p>=0.05 on a tie; (c) `sector_mean` uses the pooled 'Other' sector for a small-N sector.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_baselines_dm.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `baselines_asof` returns predict_zero/global_median/trailing_12/sector_mean computed ONLY from pool rows with `listing_date < T0_i` (as-of-T0, P9); `sector_mean` uses the D5-10 pooled sector
    - `dm_test` returns `(dm_stat, p_value)` with dm<0 & p<0.05 when model loss is significantly lower, and no significance on a tie; `wilcoxon_robustness` returns a paired p-value
    - `baselines.py` imports no xgboost/mapie/gmp (model-free; predictor-side isolation) and does no network
    - `test_baselines_dm.py` passes
  </acceptance_criteria>
  <done>The four baselines score under the walk-forward as-of-T0 protocol and the inline DM + Wilcoxon tests exist — no fragile dependency added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P9 release gate (DM vs 4 baselines + R²>0.5 leakage alarm)</name>
  <read_first>
    - pipelines/forecast/walkforward.py (`r2_leakage_alarm` from 05-05 — the R²>0.5 `(value, flag_or_None)` gate this gate consumes)
    - pipelines/forecast/baselines.py (Task 1 — `score_baselines` + `dm_test`)
    - 05-RESEARCH.md §"Pitfall 4: Naive baselines beat the model (P9)" (make "model significantly beats baselines" a release gate; if it doesn't, the honest card SAYS so — do NOT p-hack) + §"Pattern 3" R²>0.5 (the leakage alarm is an automated gate, not just prose) + §Locked upstream (four baselines + DM as the release gate, P9)
    - ROADMAP.md §Phase 5 Pitfalls owned (P9 — four baselines + significance test as release gate)
    - pipelines/historical/validate.py (the plain-data verdict posture)
  </read_first>
  <behavior>
    - release_gate scores the four baselines, DM-tests the model's absolute errors vs each, runs r2_leakage_alarm, and returns a plain-data verdict {passed, r2, r2_alarm, per_baseline{...}, notes}
    - passed=False if R²>0.5 fires OR any baseline significantly beats the model (p<0.05)
    - passed=True with an explicit "does not significantly outperform" note on a tie (honest low-R² case, D5-01)
  </behavior>
  <action>
    Add `release_gate(oos_df, panel) -> dict` to `pipelines/forecast/baselines.py` composing the P9 gate: call `score_baselines`, `dm_test` the model's absolute errors against each baseline's absolute errors, import and run `r2_leakage_alarm` from `pipelines.forecast.walkforward` (05-05) on the OOS median, and return an explicit verdict `{"passed": bool, "r2": float, "r2_alarm": str | None, "per_baseline": {name: {"dm_stat": float, "p_value": float, "model_beats_sig": bool, "baseline_beats_model_sig": bool}}, "notes": [str, ...]}`. Encode the gate semantics per RESEARCH Pitfall 4 + Pattern 3: set `passed=False` when the R²>0.5 leakage alarm fires (a T0-violating feature likely slipped in) OR when ANY baseline SIGNIFICANTLY beats the model (baseline loss significantly lower at p<0.05); otherwise `passed=True`. When the model does NOT significantly beat the baselines (the honest low-R² pre-apply case, D5-01), still `passed=True` but append an explicit note that the model does not significantly outperform — the model card states this plainly; NEVER p-hack features to cross the gate. Return plain-data only (no plotting) so 05-10 embeds the DM table + verdict in the model card. Write `tests/unit/test_release_gate.py`: (a) a leaked frame (R²>0.5) yields `passed=False` with `r2_alarm` set; (b) a frame where a baseline significantly beats the model yields `passed=False`; (c) an honest tie yields `passed=True` with the "does not significantly outperform" note.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_release_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `release_gate` returns `passed=False` when `r2_leakage_alarm` fires (R²>0.5) — the deferred 05-04 R²-gate now acts as a release gate (P9/P4)
    - `release_gate` returns `passed=False` when any baseline significantly beats the model (p<0.05); returns `passed=True` with an honest "does not significantly outperform" note on a statistical tie (D5-01, no p-hacking)
    - the verdict is plain-data (`per_baseline` DM stats + `notes`) consumable by the 05-10 model card
    - `test_release_gate.py` passes
  </acceptance_criteria>
  <done>The P9 release gate composes the four-baseline DM tests + the R²>0.5 leakage alarm into an explicit, honest PASS/FAIL verdict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D5-09 conformal abstention (out_of_support + interval_too_wide) in walk_forward</name>
  <read_first>
    - pipelines/forecast/walkforward.py (05-05 — the per-IPO loop already emits `insufficient_history`; extend for the other two reasons; keep the as-of-T0 pool + disjoint calibration split intact)
    - pipelines/features/select.py (05-08 — `training_support` / `is_out_of_support` — the out_of_support detector to consume)
    - agent/forecast_schema.py (the abstain_reason enum: `insufficient_history` | `out_of_support` | `interval_too_wide` — the three reasons this task completes)
    - 05-CONTEXT.md D5-09 (extrapolation-outside-support OR interval-too-wide guard, conformal-native) + 05-RESEARCH.md §Open Questions Q3 (tune MIN_TRAIN/CAL_FRAC + width guard empirically)
  </read_first>
  <behavior>
    - after building the proper-train slice, walk_forward computes training_support and abstains out_of_support when the target IPO's feature vector is is_out_of_support
    - walk_forward abstains interval_too_wide when the predicted 80% width exceeds a tunable max_width_pts guard
    - the existing insufficient_history branch is unchanged; all three reasons map 1:1 to the ForecastRecord enum; abstained IPOs carry no band
  </behavior>
  <action>
    Extend `walk_forward` in `pipelines/forecast/walkforward.py` with the D5-09 conformal-native abstention. After fitting on the proper-train slice, compute `training_support` (from `pipelines.features.select`) on that slice and, before emitting a band, abstain with `abstain_reason="out_of_support"` when the target IPO's feature vector is `is_out_of_support` (any feature outside the training `[q01,q99]`). After `predict_band`, abstain with `abstain_reason="interval_too_wide"` when the predicted 80% interval width exceeds a tunable `max_width_pts` guard (default derived from the training-return IQR — e.g. width above `WIDTH_GUARD` points — tune per Open Q3). Keep the existing `insufficient_history` branch and the strict `pool = rows[listing_date < issue_date]` + disjoint pre-T0 calibration split unchanged. The three reasons map 1:1 to the ForecastRecord `abstain_reason` enum; an abstained IPO carries no fabricated band. Write `tests/unit/test_walkforward_abstention.py`: (a) an IPO whose features sit outside training support gets `out_of_support`; (b) an IPO whose calibrated width exceeds the guard gets `interval_too_wide`; (c) an in-support, tight-interval IPO is scored (not abstained).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_walkforward_abstention.py tests/unit/test_walkforward_no_lookahead.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `walk_forward` emits `abstain_reason="out_of_support"` when the IPO's features fall outside the training support and `"interval_too_wide"` when the 80% width exceeds `max_width_pts` (D5-09)
    - the three abstain reasons match the ForecastRecord enum exactly; abstained IPOs carry no fabricated band
    - the existing no-lookahead invariant (`train.listing_date.max() < ipo.issue_date`) and disjoint calibration split are unchanged (test_walkforward_no_lookahead.py stays green)
    - `test_walkforward_abstention.py` passes
  </acceptance_criteria>
  <done>Walk-forward abstention completes all three ForecastRecord reasons; extrapolation and useless-width IPOs render the honest no-band note (D5-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model OOS errors vs baseline OOS errors | The release-gate comparison that decides whether the phase ships |
| time boundary T0_i | Baselines must be scored as-of-T0, identical to the model |
| predictor <-> GMP | baselines/walkforward must import no GMP (isolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-GATE | Repudiation (shipping a bad model) | release_gate | mitigate | Explicit P9 gate: FAIL on R²>0.5 leakage OR a baseline significantly beating the model; PASS-with-note on a tie; verdict is plain-data + committed to the model card (P9) |
| T-05-09-R2 | Repudiation (silent leakage) | r2_leakage_alarm consumption | mitigate | The R²>0.5 alarm (05-05) is consumed as a HARD gate, not just prose — a T0-violating feature fails the phase (P4) |
| T-05-09-HACK | Tampering (honesty) | baseline comparison | mitigate | A statistical tie PASSES with an honest "does not significantly outperform" note (low R² is a feature, D5-01); no p-hacking features to cross the gate |
| T-05-09-ABST | Tampering (honesty) | D5-09 abstention | mitigate | out_of_support + interval_too_wide render the locked no-band note; abstained IPOs fabricate no interval; three reasons match the record enum |
| T-05-09-ISO | (invariant breach) | predictor <-> GMP | mitigate | baselines.py/walkforward.py import no `gmp`; pinned by the 05-03 predictor-side isolation audit |
| T-05-09-SC | Tampering | pip installs | mitigate | No new installs; the `dieboldmariano` [ASSUMED] package is AVOIDED via the inline DM test (attack-surface reduction) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_baselines_dm.py tests/unit/test_release_gate.py tests/unit/test_walkforward_abstention.py -q` passes.
- Baselines scored as-of-T0; inline DM significance; gate FAILS on R²>0.5 / baseline-beats-model; abstention completes all three reasons.
- No `dieboldmariano` dependency added; baselines import no GMP.
</verification>

<success_criteria>
- Four baselines + inline Diebold–Mariano significance test scored as-of-T0 (FCAST-05, P9).
- Explicit P9 release gate: FAIL on R²>0.5 leakage or baseline-beats-model; honest PASS-with-note on a tie (D5-01).
- D5-09 abstention (out_of_support + interval_too_wide) wired into walk-forward.
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-09-SUMMARY.md` when done.
</output>
</content>
