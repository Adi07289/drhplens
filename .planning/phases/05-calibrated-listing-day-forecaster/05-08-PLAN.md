---
phase: 05-calibrated-listing-day-forecaster
plan: 08
type: execute
wave: 6
depends_on: ["05-02", "05-04", "05-05", "05-06"]
files_modified:
  - pipelines/historical/sources.py
  - pipelines/features/__init__.py
  - pipelines/features/build.py
  - pipelines/features/select.py
  - tests/unit/test_features_available_at.py
  - tests/unit/test_features_expanded.py
  - tests/unit/test_feature_selection.py
autonomous: true
requirements: [FCAST-02, FCAST-05]
must_haves:
  truths:
    - "The candidate feature pool spans all four families (D5-06): issue structure (a, from 05-04) + market regime (b) + DRHP-derived (c) + anchor-investor demand (d), each carrying a verified available_at <= T0"
    - "Anchor features read ONLY the pre-open anchor allocation (disclosed T0−1); GMP and post-open QIB/NII/RII subscription multiples remain excluded by construction, documented in an explicit anchor leakage audit (D5-08)"
    - "Sectors below ~30 IPOs are pooled into 'Other' and an N-per-sector report is produced (D5-10)"
    - "The final production feature set is curated LEAN (~8–15) via walk-forward importance + stability, not the kitchen sink (D5-07)"
    - "A training-support helper flags feature vectors outside the training [q01,q99] support (the out_of_support input for D5-09 abstention)"
  artifacts:
    - path: "pipelines/features/__init__.py"
      provides: "FEATURE_SPECS extended with regime (b) + DRHP-derived (c) + anchor (d) families, each -> (dtype, available_at rule)"
      contains: "nifty_mom"
    - path: "pipelines/features/build.py"
      provides: "expanded build_features + anchor_leakage_audit + pool_sectors + extended leakage_audit"
      contains: "anchor_leakage_audit"
    - path: "pipelines/features/select.py"
      provides: "select_features (lean D5-07 curation) + training_support/is_out_of_support (D5-09 input)"
      contains: "is_out_of_support"
    - path: "tests/unit/test_features_expanded.py"
      provides: "family b/c/d available_at gate + anchor pre-open audit + sector pooling assertions"
      contains: "anchor"
  key_links:
    - from: "pipelines/features/build.py"
      to: "data/snapshots/<id>.json (Phase 2) + data/redflag/<id>.json (Phase 3)"
      via: "allow-list-gated cache reads -> DRHP-derived features (D5-06c)"
      pattern: "is_known_drhp_id"
    - from: "pipelines/features/select.py"
      to: "pipelines/forecast/walkforward + model.make_quantile_models"
      via: "per-fold importance/stability -> lean SELECTED_FEATURES (D5-07)"
      pattern: "make_quantile_models|walk_forward"
---

<objective>
Slice 3a (the full feature pool, D5-06b/c/d + D5-07 + D5-08 + D5-10): expand the leakage-gated feature matrix beyond 05-04's issue-structure-only thin slice to all four candidate families — market regime (NIFTY momentum, India VIX, IPO pipeline density, trailing-N listing gain), DRHP-derived signals (Phase 2 financials + Phase 3 NLP extraction, the "NLP fused into the model" narrative), and anchor-investor demand (the one legitimate T0 demand proxy) — each stamped with a verified `available_at <= T0`. Anchor features get an EXPLICIT pre-open leakage audit (D5-08). Small-N sectors are pooled into 'Other' with an N-per-sector report (D5-10). Finally the wide candidate pool is curated to a LEAN ~8–15 production set via walk-forward importance + stability (D5-07), and a training-support helper is added as the `out_of_support` input for D5-09 abstention (built in 05-09).

Purpose: the model card (05-10) documents this feature list + anchor audit; the release gate + final walk-forward (05-09/05-11) run on the lean set; without regime features P6 (regime-shift blindness) is unaddressed.
Output: extended `sources.py` regime fetchers, extended `FEATURE_SPECS` + `build_features` + `anchor_leakage_audit` + `pool_sectors`, new `pipelines/features/select.py`, and the expanded-feature + selection tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-CONTEXT.md
@pipelines/features/__init__.py
@pipelines/features/build.py
@pipelines/historical/sources.py
@data/catalogue_loader.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Market-regime feature family (b) + regime source fetchers</name>
  <read_first>
    - pipelines/historical/sources.py (fetch_listing_day_close jugaad-data -> yfinance -> None at ~242-283 — the exact fallback grammar the regime fetchers mirror; _get/_check_host/lazy _session; ALLOWED_HOSTS — note 05-02 already added www.nseindia.com; the `# pragma: no cover - live only` seam posture)
    - pipelines/features/__init__.py (the 05-04 FEATURE_SPECS grammar: ordered mapping feature -> (dtype, available_at_rule); FEATURE_COLUMNS/FEATURE_DTYPES; EXCLUDED_FROM_MODEL sentinel)
    - 05-RESEARCH.md §"Pattern 3" family (b) row (NIFTY 3M/6M momentum, India VIX, pipeline density, trailing-N listing gain; available_at = T0−1 EOD snapshot, <= T0 ✓) + §"Supporting" (jugaad-data `index_df`/`NSEIndexHistory` is the NIFTY momentum source + India-VIX) + §Pitfall 2 (P6 regime-shift)
    - 05-CONTEXT.md D5-06b
  </read_first>
  <behavior>
    - FEATURE_SPECS gains the regime family, each keyed to an available_at that resolves to the pre-T0 (T0−1 EOD) snapshot (<= issue_date)
    - regime fetchers reuse _get/_check_host and the jugaad-data -> yfinance -> None fallback, stay `# pragma: no cover - live only`, and do no network at import
    - no regime feature references GMP or any post-T0 subscription/close
  </behavior>
  <action>
    Extend `pipelines/historical/sources.py` with two live-deferred regime fetchers reusing the existing controls (`_get` -> `_check_host` + backoff + cached session; jugaad-data -> yfinance -> None replace-with-NaN posture of `fetch_listing_day_close`): `fetch_nifty_history(end_date)` pulling NIFTY EOD closes up to `end_date` via jugaad-data `index_df`/`NSEIndexHistory` (lazy import, `# pragma: no cover - live only`), and `fetch_india_vix(as_of)` returning the India-VIX level as-of `as_of` (lazy import, live-only). URLs/hosts stay module constants (no URL derived from input — SSRF). Then extend `FEATURE_SPECS` in `pipelines/features/__init__.py` with the D5-06b regime family, each mapping to `(dtype, available_at_rule)` where the rule is the pre-open (T0−1 EOD) snapshot resolving to `<= issue_date`: `nifty_mom_3m`, `nifty_mom_6m` (NIFTY trailing 3M/6M return %, from `fetch_nifty_history`), `india_vix` (level, from `fetch_india_vix`), `ipo_pipeline_density` (count of IPOs with issue_date in a trailing window — derived from the panel, not fetched), `trailing_listing_gain` (trailing-N mean listing-day return of PRIOR listings — derived from the panel). Add each name to `FEATURE_COLUMNS`/`FEATURE_DTYPES`. Document the available_at rule as the pre-T0 snapshot (never a T0+ value). Keep the regime fetchers offline-at-import (lazy clients).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pipelines.historical.sources as s, pipelines.features as F; assert hasattr(s,'fetch_nifty_history') and hasattr(s,'fetch_india_vix'); assert {'nifty_mom_3m','nifty_mom_6m','india_vix','ipo_pipeline_density','trailing_listing_gain'} <= set(F.FEATURE_COLUMNS); assert not any('subscri' in c or c=='gmp' for c in F.FEATURE_COLUMNS); print('regime specs ok')" && .venv/bin/python -m pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `pipelines.historical.sources` has `fetch_nifty_history` + `fetch_india_vix`, both `# pragma: no cover - live only`, routed via `_get`/lazy client imports (no bare `requests.get`, no import-time network)
    - `FEATURE_SPECS` gains `nifty_mom_3m`/`nifty_mom_6m`/`india_vix`/`ipo_pipeline_density`/`trailing_listing_gain`, each with an `available_at` rule resolving to `<= issue_date` (T0−1 snapshot)
    - no regime feature is GMP or a post-T0 subscription/close; `EXCLUDED_FROM_MODEL` is honored
    - importing `pipelines.features` + `pipelines.historical.sources` does no network; `pytest tests/unit -q` stays green
  </acceptance_criteria>
  <done>The regime feature family (D5-06b) is declared with pre-T0 available_at rules and live-deferred fetchers; P6 is now representable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DRHP-derived (c) + anchor (d) features, anchor leakage audit (D5-08), sector pooling (D5-10)</name>
  <read_first>
    - pipelines/features/build.py (the 05-04 build_features + leakage_audit + LeakageError + the `available_at <= issue_date` assertion loop + the replace-with-NaN coercer to reuse for the new families)
    - data/snapshots/swiggy_2024_11.json (Phase 2 financials cache shape) + data/redflag/swiggy_2024_11.json (Phase 3 NLP extraction cache shape) — the DRHP-derived (c) sources
    - data/catalogue_loader.py (CatalogueIPO.sector + issue_size_cr + is_known_drhp_id — the sector source for D5-10 and the allow-list gate for every cache read)
    - 05-RESEARCH.md §"Pattern 3" family (c) DRHP-derived (revenue growth/margins/RoE/debt Phase 2; red-flag count/RPT %/use-of-proceeds mix/promoter holding Phase 3; available_at = prospectus filing, <= T0 ✓) + family (d) Anchor (available_at = anchor allocation disclosed T0−1, BORDERLINE — audit) + §"Anchor leakage audit (D5-08)" (line ~318) + §Pitfall 3 (P7 small-N sector)
    - 05-CONTEXT.md D5-06c, D5-06d, D5-08, D5-10
  </read_first>
  <behavior>
    - build_features derives family (c) from data/snapshots + data/redflag (allow-list gated) and family (d) from ONLY the pre-open anchor allocation, each available_at <= issue_date (T0)
    - anchor_leakage_audit() names the exact anchor source field + its T0−1 disclosure timestamp + verdict, and asserts post-open QIB/NII/RII subscription can never be a feature
    - pool_sectors(min_n=30) maps <30-IPO sectors to 'Other' and returns an n_per_sector report; the built sector column is the pooled value
  </behavior>
  <action>
    Extend `build_features` in `pipelines/features/build.py` to derive the DRHP-derived family (c) — read `data/snapshots/<id>.json` (Phase 2: revenue_growth, ebitda_margin, roe, debt_to_equity) and `data/redflag/<id>.json` (Phase 3: red_flag_count, rpt_intensity, use_of_proceeds_mix, promoter_holding_pct) through `is_known_drhp_id`-gated cache reads (gate the id BEFORE forming any path — path-traversal), each stamped `available_at = prospectus filing date <= T0`, missing values NaN-retained (never fabricated 0). Derive the anchor-demand family (d) reading ONLY the pre-open anchor allocation (`anchor_book_cr`, `anchor_investor_quality`, `anchor_lockin_frac`), stamped `available_at = anchor allocation disclosure = T0−1 (<= T0)`. Add `anchor_leakage_audit() -> list[dict]` returning per anchor feature `{feature, source_field, disclosure_timestamp: "T0-1", verdict: "pre-open allocation only, <= T0 ✓"}` (D5-08), and extend `EXCLUDED_FROM_MODEL` to name the post-open subscription multiples (`subscription_at_close`, `qib`, `nii`, `rii`) so the builder asserts none appears as a built column — raising `LeakageError` if a post-open anchor/subscription field is crafted in. Add `pool_sectors(panel, *, min_n=30) -> tuple[pandas.Series, dict]` mapping sectors with fewer than `min_n` IPOs to `'Other'` and returning an `n_per_sector` report; use the pooled sector as the `sector` value feeding the sector features/baselines/record (D5-10). Extend the existing `available_at <= T0` assertion loop to cover families b/c/d (raise `LeakageError` naming any post-T0 feature+issuer). Extend `leakage_audit()` to emit verdicts for every family (a/b/c/d) plus the anchor audit. Update `tests/unit/test_features_available_at.py` for the new families and add `tests/unit/test_features_expanded.py`: (a) family c reads the Phase 2/3 caches allow-list-gated and stamps filing-date available_at; (b) a crafted post-open subscription/anchor field RAISES LeakageError; (c) anchor_leakage_audit documents the pre-open field + T0−1 timestamp; (d) pool_sectors maps a <30 sector to 'Other' and reports n_per_sector.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_features_available_at.py tests/unit/test_features_expanded.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_features` derives family (c) from `data/snapshots`+`data/redflag` (allow-list gated) and family (d) from the pre-open anchor allocation only, each `available_at <= issue_date` (T0)
    - `anchor_leakage_audit()` names the exact anchor source field + `T0−1` disclosure timestamp + verdict; `EXCLUDED_FROM_MODEL` names `subscription_at_close`/`qib`/`nii`/`rii`, and a crafted post-open feature RAISES `LeakageError` (D5-08)
    - `pool_sectors(min_n=30)` maps <30-IPO sectors to `'Other'` and returns an `n_per_sector` report (D5-10); the built `sector` column is the pooled value
    - `leakage_audit()` returns verdicts for every family (a/b/c/d) incl. the anchor borderline audit; no GMP/subscription column is present
    - `test_features_available_at.py` + `test_features_expanded.py` pass
  </acceptance_criteria>
  <done>Families c/d land behind the T0 gate with a documented anchor pre-open leakage audit; sectors pool into 'Other' with an N-per-sector report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lean feature selection (D5-07) + training-support helper (D5-09 input)</name>
  <read_first>
    - pipelines/forecast/walkforward.py + pipelines/forecast/model.py (05-05 — the expanding-window as-of-T0 loop + make_quantile_models; selection ranks candidate features by importance across folds; import xgboost lazily)
    - pipelines/features/__init__.py + build.py (the full candidate FEATURE_COLUMNS after Tasks 1-2 — the pool to curate from)
    - 05-RESEARCH.md §Locked Decisions D5-07 (lean ~8–15, importance/stability, SHAP/importance-interpretable) + §Open Questions Q3/D5-09 (support-region reasoning for out_of_support abstention) + §Pattern 3 R²>0.5 note (overfit/leakage control)
    - tests/unit/fixtures/forecast_fixtures.py (synthetic_panel/synthetic_features — the offline fixture)
  </read_first>
  <behavior>
    - select_features runs the walk-forward, collects per-fold XGBoost gain importances, ranks by mean importance AND cross-fold stability, and returns a lean set (<= 15, D5-07) plus an importance/stability table
    - SELECTED_FEATURES is an importable constant the production walk-forward trains on (not the kitchen sink)
    - training_support computes per-feature [q01,q99] bounds; is_out_of_support(x_row, support) returns True when any feature falls outside its bound
  </behavior>
  <action>
    Create `pipelines/features/select.py` with `select_features(panel, X, *, max_k=15) -> tuple[list[str], pandas.DataFrame]`: run the expanding-window walk-forward (reuse `pipelines.forecast.walkforward.walk_forward` / `model.make_quantile_models`) collecting per-fold XGBoost gain importances for each candidate feature, rank by mean importance AND stability (low rank-variance across folds), and return the curated lean set (target ~8–15, capped at `max_k`, D5-07) plus an importance/stability table (the model-card input). Expose `SELECTED_FEATURES` as a module constant the production run consumes so the model trains on the lean set (P4/P7 overfit control), not the full candidate pool. Add `training_support(X_train) -> dict` computing per-feature support bounds (`[q01, q99]`) and `is_out_of_support(x_row, support) -> bool` returning True when any feature falls outside its training-support bound — the D5-09 `out_of_support` detector the walk-forward (05-09) consumes. Import xgboost lazily inside the functions (module import stays offline). Write `tests/unit/test_feature_selection.py`: (a) `select_features` on the fixture returns at most `max_k` features ordered by importance/stability with a table row per candidate; (b) `is_out_of_support` returns True for a feature vector pushed outside the training `[q01,q99]` and False for an in-support vector.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_feature_selection.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `select_features` returns a lean list (`<= max_k`, ~8–15 target, D5-07) with an importance/stability table; `SELECTED_FEATURES` is importable for the production run
    - `is_out_of_support` returns True for an out-of-range feature vector and False for an in-support one (the D5-09 `out_of_support` input)
    - importing `pipelines.features.select` triggers no xgboost import at module load (lazy) and no network
    - `test_feature_selection.py` passes
  </acceptance_criteria>
  <done>The wide candidate pool is curated to a lean, importance/stability-ranked production set, and the training-support helper feeds D5-09 abstention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NSE/regime web (untrusted) -> feature matrix | Scraped NIFTY/VIX data crosses into model features |
| Phase 2/3 caches -> features | Committed DRHP-derived signals feed the model |
| time boundary T0 (issue-open) | Every feature (esp. anchor + regime) must resolve `available_at <= T0` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-LEAK | Tampering (data integrity) | available_at gate over families b/c/d | mitigate | Hard `available_at <= issue_date (T0)` assertion per feature per row raises `LeakageError`; regime = pre-T0 snapshot, DRHP = filing date, anchor = T0−1 pre-open; leakage_audit committed to the model card (FCAST-02/P4/D5-08) |
| T-05-08-ANCHOR | Tampering (subtle leakage) | anchor-investor feature (d) | mitigate | Read ONLY the pre-open anchor allocation; `anchor_leakage_audit` documents the exact field + T0−1 timestamp; post-open QIB/NII/RII subscription named in `EXCLUDED_FROM_MODEL` and asserted absent (D5-08) |
| T-05-08-SSRF | Information disclosure | `fetch_nifty_history`/`fetch_india_vix` outbound hosts | mitigate | Route through `_get`/`_check_host` allow-list; hosts stay module constants; no URL derived from input |
| T-05-08-PATH | Tampering | DRHP-derived cache reads | mitigate | `is_known_drhp_id` gate BEFORE forming any `data/snapshots`/`data/redflag` path (path-traversal) |
| T-05-08-FAB | Tampering (honesty) | missing feature values | mitigate | replace-with-NaN (retained, counted); never a fabricated 0; row count preserved |
| T-05-08-SC | Tampering | pip installs | mitigate | No new installs in this plan; xgboost/jugaad-data/yfinance already Approved in the 05-01 §Package Legitimacy Audit; regime fetchers reuse the existing lazy-client posture |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_features_available_at.py tests/unit/test_features_expanded.py tests/unit/test_feature_selection.py -q` passes.
- All four families declared with `available_at <= T0`; anchor pre-open audit present; sectors pool <30 into 'Other'; lean SELECTED_FEATURES importable.
- Module imports do no network; regime fetchers are live-deferred seams.
</verification>

<success_criteria>
- Feature pool spans families a/b/c/d, each `available_at <= T0` (FCAST-02, D5-06b/c/d).
- Anchor pre-open leakage audit documented; GMP/subscription excluded by construction (D5-08).
- Sectors pooled <30 -> 'Other' with N-per-sector report (D5-10); lean ~8–15 set selected (D5-07).
- Training-support helper produced for D5-09 abstention.
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-08-SUMMARY.md` when done.
</output>
</content>
</invoke>
