---
phase: 05-calibrated-listing-day-forecaster
plan: 07
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - ui/copy.py
  - app/static/drhplens.css
  - ui/forecast_block.py
  - pages/02_snapshot.py
  - tests/unit/test_forecast_block_render.py
autonomous: true
requirements: [FCAST-01, FCAST-04, GMP-03, UI-03]
must_haves:
  truths:
    - "A user on the snapshot page sees a calibrated 80% listing-day-return BAND (width is the dominant visual, no point-estimate headline) inserted after the peer block and before the ranked-risks list"
    - "The GMP-implied return sits as a muted hollow marker on the same axis, with the gap printed as a labeled delta; copy states the model is GMP-free"
    - "Empirical coverage, MAE, and per-year RMSE are always visible (never collapsed); coverage shows the real held-out number"
    - "abstain / not-covered / no-GMP / error each render an honest note (never a fabricated band, metric, or GMP)"
    - "The render imports NO model/training module and reads only cached data/forecasts + data/gmp"
  artifacts:
    - path: "ui/forecast_block.py"
      provides: "cache-only forecast render (band + median tick + 0% rule + GMP marker + metrics strip + model-card link)"
      min_lines: 60
    - path: "pages/02_snapshot.py"
      provides: "forecast block wired in after peer, before ranked-risks (L5-4)"
      contains: "forecast"
    - path: "app/static/drhplens.css"
      provides: ".drhp-forecast-* classes on the current dark --drhp-* tokens"
      contains: ".drhp-forecast-band"
    - path: "tests/unit/test_forecast_block_render.py"
      provides: "render states from fixture records (full/covered-no-gmp/abstain/not-covered/error) + axis-position math"
      contains: "abstain"
  key_links:
    - from: "ui/forecast_block.py"
      to: "pipelines.forecast.load_forecast + pipelines.gmp.load_gmp + ui.format_inr"
      via: "cache-only reads; GMP-implied-return = gmp_premium/issue_price*100 in the display layer"
      pattern: "load_forecast|load_gmp|format_inr"
    - from: "pages/02_snapshot.py"
      to: "ui.forecast_block after _render_peer_block, before ranked-risks"
      via: "allow-list-guarded try/except (missing->empty, error->amber .drhp-refusal)"
      pattern: "forecast"
---

<objective>
Slice 2's user-facing payoff (FCAST-01 / FCAST-04 / GMP-03 / UI-03) — implement the LOCKED 05-UI-SPEC forecast section as a cache-only Streamlit block: the 80% prediction-interval band as the dominant width-driven visual, a faint neutral median tick, a marked 0% baseline, a muted hollow-diamond GMP-implied-return marker with a labeled gap delta, the always-visible "How this was tested" metrics strip (empirical coverage / MAE / per-year RMSE), and a "Full model card →" link — inserted after the peer block and before the ranked-risks list. It renders entirely from committed fixture records (05-01) so it lands in parallel with the offline model; the real records arrive in 05-06/05-11.

Purpose: after this plan a real user can SEE the calibrated range + GMP gap + honesty metrics on any covered IPO page — the phase's headline capability, delivered thin.
Output: new `ui/forecast_block.py`, `.drhp-forecast-*` CSS, forecast copy in `ui/copy.py`, the page wiring in `pages/02_snapshot.py`, and the render-state test.

**Honesty invariants pinned here (from the LOCKED UI-SPEC):** band width is the message; NO green/red, NO badges, NO point-estimate headline (P21); coverage shown honestly even if it misses 80% (P17); GMP is display-only + isolated (GMP-02 / D4-03 / FCAST-02); the render imports no model module (D5-11 render side is closed — build the wiring, not the visual design).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-UI-SPEC.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@ui/snapshot_blocks.py
@ui/copy.py
@ui/format_inr.py
@pages/02_snapshot.py
@agent/forecast_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forecast copy strings + .drhp-forecast-* CSS</name>
  <read_first>
    - ui/copy.py (the import-time scrubber loop at lines ~618-691 + `_SAMPLE_FORMAT_VALUES` at ~634-660 — every new placeholder MUST be registered or the template scrub throws KeyError; note `target` is a banned stem)
    - 05-UI-SPEC.md §"Copywriting Contract" (lines ~266-294 — every exact string) + §"New CSS classes to add" (lines ~356-368) + §"Discretion Resolved" D-1/D-2/D-3/D-4 (the geometry + strip layout)
    - app/static/drhplens.css (the single CSS source; existing .drhp-gmp-range/.drhp-gmp-tick/.drhp-metrics/.drhp-not-disclosed/.drhp-refusal + the four breakpoint media queries + st-key-drhpcard-* chrome + the dark --drhp-* :root tokens)
  </read_first>
  <action>
    Add every 05-UI-SPEC §Copywriting-Contract string to `ui/copy.py` as module-level `str` constants (the import-time scrubber auto-enrolls them): the block heading `Listing-day forecast — a calibrated range, not a call.`, the GMP-free framing sub-line, the plain-language interval caption template `In 80% of comparable past IPOs, the listing-day return fell between {low}% and {high}% — a span of {width} points.`, `Model median {median}%`, `0% (issue price)`, the GMP-gap line template with `{gmp}`/`{delta}`/`{direction}` where direction is the word above|below, the no-GMP note, the `How this was tested` eyebrow + the three stat labels + the per-year RMSE caption + the coverage honesty note, `Full model card →`, the not-covered + abstain empty states, the error state, and the two aria-label templates. Register every new placeholder (`low`, `high`, `width`, `median`, `gmp`, `delta`, `direction`, `coverage`, `mae`, `year`, `rmse`, `n`, `window`) in `_SAMPLE_FORMAT_VALUES`. Use ONLY `range`/`interval`/`median` — never `target` (banned stem); avoid `buy`/`sell`/`subscri` stems in the GMP-gap wording. Then add the `.drhp-forecast-*` classes to `app/static/drhplens.css` per UI-SPEC §"New CSS classes": `.drhp-forecast-block/-plot/-axis` (44px --drhp-surface-secondary track), `.drhp-forecast-band` (the ONE amber element — `rgba(224,162,78,0.16)` fill + `rgba(224,162,78,0.60)` 2px edges, 6px radius, 28px tall, 4px min visual width), `.drhp-forecast-median` (+label), `.drhp-forecast-zero` (+label), `.drhp-forecast-gmp` (hollow 12px --drhp-text-muted diamond, transparent, 1.5px border, rotate45 — NEVER amber), `.drhp-forecast-scale`, `.drhp-forecast-caption`, `.drhp-forecast-gap`, `.drhp-forecast-tested` (+ eyebrow/stat/rmse/note), `.drhp-forecast-cardlink` (accent-as-text + 44×44 ::before enlarger), and the responsive rules under the existing four breakpoints (stat row stacks; RMSE table overflow-x:auto; axis labels shrink <375px). No inline `<style>` anywhere — single CSS source. The band wash `rgba(224,162,78,0.16)` + `0.60` edges is the ONLY new hex, derived from `--drhp-accent`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ui.copy" && grep -q 'drhp-forecast-band' app/static/drhplens.css && echo "copy+css ok"</automated>
  </verify>
  <acceptance_criteria>
    - `import ui.copy` succeeds — the import-time scrubber passes every new forecast string (no banned token; `target` absent)
    - every new format placeholder is registered in `_SAMPLE_FORMAT_VALUES` (no KeyError at import)
    - `app/static/drhplens.css` contains `.drhp-forecast-band`, `.drhp-forecast-median`, `.drhp-forecast-zero`, `.drhp-forecast-gmp`, `.drhp-forecast-tested`, `.drhp-forecast-cardlink`; the only new hex is the two accent-derived band washes; no inline `<style>` added elsewhere
    - the GMP marker CSS uses `--drhp-text-muted` (never amber); the band is the only amber element
  </acceptance_criteria>
  <done>All forecast copy passes the import-time scrubber and the `.drhp-forecast-*` classes exist on the current dark tokens.</done>
</task>

<task type="auto">
  <name>Task 2: ui/forecast_block.py — cache-only render with axis math, GMP marker, metrics strip</name>
  <read_first>
    - ui/snapshot_blocks.py (the exact grammar: `_render_gmp_spread` + `render_gmp_block` at ~797-903 — ONE self-contained st.markdown with inline `left:%`, `st.container(border=True, key=...)`, `role="img"` + aria-label, the if/elif/else state fan-out, render_per_answer_footer())
    - ui/format_inr.py (format_inr signature at ~52-73 — the ONE rupee formatter; None -> "—")
    - 05-UI-SPEC.md §"Visuals — Forecast Section Contract" (lines ~186-224 structure + states) + §D-1 axis-domain math (anchor set, pad, floor/ceil to 5, pos(v) clamp) + §D-2 element forms + §D-3 metrics strip + §"The GMP-implied-return conversion" (lines ~222-224)
    - agent/forecast_schema.py (the ForecastRecord fields the render consumes)
    - tests/unit/test_gmp_isolation.py (the isolation grammar this module must satisfy in reverse — no model import)
  </read_first>
  <action>
    Create `ui/forecast_block.py` importing ONLY `streamlit`, `ui.copy`, `ui.format_inr`, `data.catalogue_loader`, `html`, and the two cache loaders `pipelines.forecast.load_forecast` + `pipelines.gmp.load_gmp` — NO `xgboost`/`mapie`/`sklearn`/`shap`/`pipelines.forecast.model`/`walkforward`/`pipelines.features`/`pipelines.historical` (pinned by test_forecast_isolation.py). Implement the D-1 axis math as pure helpers: `_domain(anchors)` (anchor set A = {0, low, high, median} ∪ {gmp_implied if present}; pad = max(2.0, 0.08*(max-min)); domain_lo=floor_to_5(min-pad), domain_hi=ceil_to_5(max+pad)) and `pos(v, lo, hi)` = clamp((v-lo)/(hi-lo)*100, 0, 100). Implement `_gmp_implied_return_pct(gmp_premium_rupees, issue_price_rupees)` = gmp_premium/issue_price*100 as a DISPLAY-layer computation reading cached GMP + cached issue price (route ₹ inputs through `format_inr` where displayed); if issue price is unavailable, omit the marker + gap line (band still renders) with the honest no-gap note. Implement `render_forecast_block(record, gmp_record, issue_price)` wrapped in `st.container(border=True, key="drhpcard-forecast")` (NEVER a split div): heading + `st.caption` sub-line, then the plot as ONE self-contained `st.markdown(..., unsafe_allow_html=True)` string interpolating band (`left:%`/`width:%`, 4px min), median tick, full-height 0% rule, hollow GMP diamond (omitted when no GMP), and axis-end scale labels — with `role="img"` + the aria-label template; then the plain-language caption (the real headline, words not a number), the GMP-vs-model gap line (or no-GMP note), the always-visible `.drhp-forecast-tested` strip (three-up coverage/MAE/window stats — Body-16 mono weight-400, NEVER Display; the per-year RMSE `<table>` with `<th scope="col">`; the coverage honesty note), the `Full model card →` link (`href="/methodology"`, unique key, 44×44), and `render_per_answer_footer()`. Map the four states to an if/elif/else fan-out: covered+GMP (full), covered+no-GMP (band, marker/gap omitted, note), abstain (`.drhp-not-disclosed` abstain note, no band), not-covered (not-covered note). Escape every record-sourced string via `html.escape`; formatted floats are safe. NO green/red, NO Display-size number, median is a faint tick + Small annotation only.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_forecast_block_render.py tests/unit/test_forecast_isolation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.getsource(ui.forecast_block)` contains none of `xgboost`/`mapie`/`sklearn`/`conformal`/`pipelines.forecast.model`/`pipelines.forecast.walkforward`/`pipelines.features`/`pipelines.historical`/`shap` (isolation)
    - `pos(v, lo, hi)` clamps to [0,100]; `_domain` always includes 0 and rounds outward to nearest 5; band width uses a >=4px floor
    - `_gmp_implied_return_pct` computes gmp_premium/issue_price*100 and the marker+gap are omitted when issue price is None (band still renders)
    - the render produces exactly ONE `st.markdown` for the plot (no split div); metrics strip is not behind an expander; median never renders Display-size
    - `test_forecast_block_render.py` passes for full/covered-no-gmp/abstain/not-covered fixture records
  </acceptance_criteria>
  <done>The cache-only forecast block renders the band, GMP marker+gap, and always-visible metrics for every state, importing no model module.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the forecast block into pages/02_snapshot.py (after peer, before ranked-risks)</name>
  <read_first>
    - pages/02_snapshot.py (imports at ~21-60; the peer/GMP guarded try/except reads at ~238-271; the `if record is not None:` body; `_render_peer_block(...)` call at ~298; the ranked-risks branch at ~300-313; the GMP block stays last at ~330-335)
    - 05-UI-SPEC.md §"Information Architecture (Snapshot Page Delta)" (block 8 insertion point, L5-4) + §"Loading / Empty / Error States"
    - 05-PATTERNS.md §"pages/02_snapshot.py — MODIFY: insert the forecast block"
  </read_first>
  <action>
    In `pages/02_snapshot.py`, add `from ui.forecast_block import render_forecast_block` alongside `from pipelines.gmp import load_gmp`, and `from pipelines.forecast import load_forecast`. Add a forecast cache read using the SAME allow-list-guarded try/except posture as the peer/GMP reads (drhp_id already validated at `main()` top, line ~190): `FileNotFoundError` → `forecast_state="missing"` (not-covered empty-state); any other `Exception` → `forecast_state="error"` (amber `.drhp-refusal`, NOT red). Add a `_render_forecast_block(forecast_record, forecast_state, gmp_record, issue_price)` helper mirroring `_render_peer_block`'s error/empty/present fan-out, calling `render_forecast_block(...)` on present. Call it INSIDE the `if record is not None:` body, AFTER `_render_peer_block(peer_record, peer_state)` (line ~298) and BEFORE the ranked-risks branch (line ~300). Pass the issue price sourced from the already-loaded snapshot metadata (the Phase 2 header value) for the GMP-implied conversion. Do NOT move the GMP block (`_render_gmp_block`) — it stays the last read block (D4-02, L5-4).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; src=open('pages/02_snapshot.py').read(); assert 'render_forecast_block' in src and 'load_forecast' in src; assert src.index('_render_forecast_block(') > src.index('_render_peer_block(peer_record') and src.index('_render_forecast_block(') < src.index('render_idf_risk_list('); print('wiring order ok')" && .venv/bin/python -m pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - the forecast block is rendered inside `if record is not None:`, positioned after the peer block call and before the ranked-risks (`render_idf_risk_list`/`render_risk_block`) branch (L5-4)
    - the forecast read uses the guarded try/except (missing→not-covered note; other error→amber `.drhp-refusal`); never an unhandled exception; no re-gate needed (drhp_id validated at main() top)
    - the GMP block remains the LAST read block (its call is unchanged and still after the forecast block)
    - `pytest tests/unit -q` stays green
  </acceptance_criteria>
  <done>The snapshot page renders the forecast section in the locked mid-page slot with honest empty/error states; GMP stays last.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `drhp_id` (st.query_params) → cache read | Already allow-list-validated at main() top; forecast read reuses that gate |
| cached record strings → rendered HTML | Record/GMP source strings are untrusted-origin data rendered via unsafe_allow_html |
| model pipeline ↔ render | The render must import no model/training module (isolation invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-XSS | Tampering (injection) | forecast plot st.markdown(unsafe_allow_html) | mitigate | `html.escape` every record-sourced string; only formatted floats interpolated into `left:%`/`width:%`; aria-labels escaped with quote=True |
| T-05-07-ISO | (invariant breach) | render ↔ model isolation | mitigate | `ui/forecast_block.py` imports only streamlit/ui.copy/ui.format_inr/catalogue_loader + the two cache loaders; pinned by `test_forecast_isolation.py` render-side audit |
| T-05-07-GMP | (invariant breach) | GMP-implied-return conversion | mitigate | Conversion is display-layer (gmp_premium/issue_price*100) reading cached GMP + cached issue price only; never a model feature (GMP-02/D4-03/FCAST-02) |
| T-05-07-HONEST | Repudiation (misleading UX) | states + coverage display | mitigate | abstain/not-covered/no-GMP render honest notes (no fabricated band/metric/GMP); coverage shows the real held-out number (no rounding to 80%); no green/red, no point-estimate headline (P17/P21) |
| T-05-07-ERR | DoS | cache read error | mitigate | guarded try/except → amber `.drhp-refusal` (role=alert), rest of page renders regardless |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green (render-state + isolation tests included).
- `import ui.copy` clean (scrubber passes); CSS contains the `.drhp-forecast-*` classes.
- Forecast block wired after peer, before ranked-risks; GMP stays last.
</verification>

<success_criteria>
- The 80% band renders as the dominant visual with no point-estimate headline (FCAST-01, UI-03, P21).
- GMP-implied marker + labeled gap render on the same axis; copy states model is GMP-free (GMP-03).
- Coverage/MAE/per-year RMSE always visible, coverage honest (FCAST-04, P17).
- Render imports no model module; GMP conversion is display-only (isolation).
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-07-SUMMARY.md` when done.
</output>
