---
phase: 05-calibrated-listing-day-forecaster
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - agent/forecast_schema.py
  - pipelines/forecast/__init__.py
  - tests/unit/test_forecast_schema.py
  - tests/unit/test_forecast_isolation.py
autonomous: true
requirements: [FCAST-01, FCAST-02]
must_haves:
  truths:
    - "A ForecastRecord is the flat, cache-only isolation boundary between the offline model and the cache-only render — it imports only pydantic + stdlib"
    - "abstain / covered / missing are first-class record states (never a fabricated interval or metric)"
    - "load_forecast(drhp_id) gates the id through the catalogue allow-list before forming any data/forecasts/<id>.json path (path-traversal)"
    - "The isolation invariant is pinned in BOTH directions: the render imports no model module; the predictor imports no GMP"
  artifacts:
    - path: "agent/forecast_schema.py"
      provides: "ForecastRecord pydantic model + to_dict/to_json/from_dict/from_json codec + abstain state properties"
      contains: "class ForecastRecord"
    - path: "pipelines/forecast/__init__.py"
      provides: "load_forecast(drhp_id) allow-list-gated cache reader"
      exports: ["load_forecast"]
    - path: "tests/unit/test_forecast_isolation.py"
      provides: "inspect.getsource two-direction import audit (mirror of test_gmp_isolation.py)"
      contains: "inspect.getsource"
  key_links:
    - from: "pipelines/forecast/__init__.py load_forecast"
      to: "data/catalogue_loader.is_known_drhp_id"
      via: "allow-list gate before path formation"
      pattern: "is_known_drhp_id"
    - from: "agent/forecast_schema.py"
      to: "data/forecasts/<id>.json"
      via: "from_json/to_json codec round-trip"
      pattern: "from_json|to_json"
---

<objective>
Slice 2a (the cache-only contract, D5-11 / FCAST-02 isolation): define the `ForecastRecord` schema and the allow-list-gated `load_forecast(drhp_id)` reader that together form the single seam between the offline forecaster and the cache-only Streamlit render. The record carries the per-IPO out-of-sample band + GLOBAL walk-forward metrics + first-class abstain/missing states, and imports NO modeling library — pinned by a two-direction `inspect.getsource` isolation audit that mirrors the Phase 4 GMP test.

Purpose: interface-first — the render (05-07), precompute (05-06), and walk-forward writer all consume this record; defining it now lets the render slice proceed in parallel against the 05-01 fixtures.
Output: `agent/forecast_schema.py`, `pipelines/forecast/__init__.py::load_forecast`, and the schema + isolation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@agent/gmp_schema.py
@pipelines/gmp.py
@data/catalogue_loader.py
@tests/unit/test_gmp_isolation.py
@tests/unit/test_gmp_schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ForecastRecord schema + codec + abstain states</name>
  <read_first>
    - agent/gmp_schema.py (the exact analog — pydantic BaseModel, to_dict/to_json(indent=2, ensure_ascii=False)/from_dict/from_json codec at lines ~121-151, is_absent first-class property, the module ISOLATION docstring at ~24-27)
    - 05-RESEARCH.md §"Forecast Record Schema" (lines ~464-484 — the exact field set) + §"Abstention states (D5-09)"
    - 05-PATTERNS.md §"agent/forecast_schema.py — the ForecastRecord cache schema"
    - tests/unit/test_gmp_schema.py (the round-trip + first-class-state test grammar to mirror)
  </read_first>
  <behavior>
    - to_json → from_json round-trips a full-render record byte-stable (indent=2, ensure_ascii=False)
    - a covered record exposes interval.low_pct/high_pct/median_pct/width_pts and a GLOBAL metrics block (coverage_empirical, mae_pts, backtest_window, n, per_year_rmse)
    - an abstain record (abstain=True, abstain_reason in {"insufficient_history","out_of_support","interval_too_wide"}) exposes `is_abstain==True` and carries no fabricated interval numbers
    - median_pct is a plain field, never surfaced as a headline (P21 note in docstring)
  </behavior>
  <action>
    Create `agent/forecast_schema.py` importing ONLY pydantic + stdlib (mirror the GmpRecord ISOLATION docstring, but for the RECORD boundary — pinned by test_forecast_isolation.py). Define nested pydantic models: `ForecastInterval` (low_pct, high_pct, median_pct, width_pts: float), `ForecastMetrics` (coverage_empirical: float, mae_pts: float, backtest_window: str, n: int, per_year_rmse: dict[str, float]), and `ForecastRecord` with fields per RESEARCH §Forecast Record Schema: drhp_id, computed_at, model_version, as_of_listing_date, out_of_sample: bool, walk_forward: bool, abstain: bool, abstain_reason: str | None, interval: ForecastInterval | None, sector: str, metrics: ForecastMetrics. Add first-class `@property is_abstain` (returns self.abstain). Implement `to_dict`/`to_json(indent=2, ensure_ascii=False)`/`from_dict`/`from_json` exactly like GmpRecord. Keep `median_pct` a Small-annotation value — document in the docstring that it is never the record's headline (UI-SPEC P21). Write `tests/unit/test_forecast_schema.py` covering the round-trip and the covered/abstain first-class states (mirror test_gmp_schema.py).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_forecast_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.getsource(agent.forecast_schema)` references none of `xgboost`/`mapie`/`sklearn`/`shap`/`pipelines.forecast`/`pipelines.features`/`pipelines.historical`/`gmp` (record is import-light)
    - `ForecastRecord.from_json(rec.to_json())` reconstructs an equal record for a full-render fixture
    - an abstain record has `is_abstain is True`, `abstain_reason in {"insufficient_history","out_of_support","interval_too_wide"}`, and `interval is None`
    - `test_forecast_schema.py` passes
  </acceptance_criteria>
  <done>The flat, import-light ForecastRecord with a stable codec and first-class abstain state exists and round-trips.</done>
</task>

<task type="auto">
  <name>Task 2: load_forecast allow-list gate + two-direction isolation audit</name>
  <read_first>
    - pipelines/gmp.py (the verbatim analog — load_gmp + _gmp_path allow-list gate at lines ~57-92; GMP_DIR at line 49)
    - data/catalogue_loader.py (is_known_drhp_id at ~75-88 — the V5 allow-list to reuse before any path is formed)
    - tests/unit/test_gmp_isolation.py (the whole file — FORBIDDEN_TOKENS tuple + inspect.getsource loop to mirror; note it already reserves the reverse audit for Phase 5)
    - 05-RESEARCH.md §"Isolation (the hard invariant)" (lines ~486-491 — both directions)
    - 05-PATTERNS.md §"pipelines/forecast/__init__.py — load_forecast" + §"Tests (mirror the tested GMP analogs)"
  </read_first>
  <action>
    Create `pipelines/forecast/__init__.py` with `FORECASTS_DIR = Path(__file__).parent.parent / "data" / "forecasts"`, a `_forecast_path(drhp_id)` that raises `ValueError` via `is_known_drhp_id(drhp_id)` BEFORE forming any path (verbatim from `_gmp_path`), and `load_forecast(drhp_id) -> ForecastRecord` that gates, then reads `data/forecasts/<drhp_id>.json` (FileNotFoundError if absent → the UI not-covered state) and returns `ForecastRecord.from_dict(...)`. Import modeling libs NOWHERE in this module (keep it import-light — the model wrappers live in `pipelines/forecast/model.py`, imported lazily only by the offline pipeline, never by `__init__`). Write `tests/unit/test_forecast_isolation.py` mirroring `test_gmp_isolation.py` with BOTH directions: (1) render-side — `inspect.getsource` of `ui.forecast_block` (import lazily inside the test so this test can be authored before 05-07; guard with `pytest.importorskip("ui.forecast_block")`) and its `pages/02_snapshot.py` forecast wiring reference none of `{"xgboost","mapie","sklearn","conformal","pipelines.forecast.model","pipelines.forecast.walkforward","pipelines.features","pipelines.historical","shap"}`; (2) predictor-side — `pipelines.forecast` (the loader package) references no `gmp`/`pipelines.gmp`, and (once they exist) `pipelines.forecast.model`/`walkforward`/`pipelines.features` reference no `gmp` (guard each with `importorskip` so the test stays green as modules land). Add a `test_load_forecast_rejects_unknown_id` asserting an un-allow-listed id raises before any path is formed.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_forecast_isolation.py -x -q && .venv/bin/python -c "import pipelines.forecast as f; import inspect; s=inspect.getsource(f); assert 'is_known_drhp_id' in s and 'xgboost' not in s and 'mapie' not in s; print('loader isolation ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `load_forecast("not_a_real_id")` raises `ValueError` (allow-list) BEFORE any filesystem path is formed
    - `load_forecast("swiggy_2024_11")` returns a `ForecastRecord` read from the 05-01 fixture; a missing file raises `FileNotFoundError`
    - `inspect.getsource(pipelines.forecast)` (the loader package `__init__`) contains none of `xgboost`/`mapie`/`sklearn`/`shap`/`gmp`
    - `test_forecast_isolation.py` runs green today (importorskip-guarded for not-yet-built render/model modules) and will execute the real audit once 05-05/05-07 land
    - `.venv/bin/python -m pytest tests/unit/test_forecast_isolation.py -q` passes
  </acceptance_criteria>
  <done>The cache reader is allow-list-gated and import-light; the two-direction isolation audit is in place and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `drhp_id` (st.query_params) → filesystem path | Untrusted id could attempt path traversal into `data/forecasts/` |
| offline model ↔ cache-only render | The record is the only permitted channel; no model import may cross into the render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-PATH | Tampering | `load_forecast`/`_forecast_path` | mitigate | `is_known_drhp_id(drhp_id)` allow-list gate raises before any `data/forecasts/<id>.json` path is formed (verbatim Phase 4 `_gmp_path` pattern); unit-tested with an unknown id |
| T-05-03-ISO | (invariant breach) | render ↔ model isolation, predictor ↔ GMP isolation | mitigate | Two-direction `inspect.getsource` substring audit (mirror `test_gmp_isolation.py`); record schema imports only pydantic+stdlib |
| T-05-03-FAB | Tampering (honesty) | abstain/missing states | mitigate | abstain/missing are first-class; no fabricated interval/metric; enforced by schema test |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_forecast_schema.py tests/unit/test_forecast_isolation.py -q` passes.
- Loader rejects unknown ids before path formation; loader + schema import no modeling libs.
</verification>

<success_criteria>
- ForecastRecord + codec + first-class abstain state (FCAST-01) exist and round-trip.
- Allow-list-gated `load_forecast` (path-traversal safe) reads the committed cache.
- Two-direction isolation audit green (FCAST-02 render/predictor isolation).
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-03-SUMMARY.md` when done.
</output>
