---
phase: 05-calibrated-listing-day-forecaster
plan: 05
type: execute
wave: 4
depends_on: ["05-03", "05-04"]
files_modified:
  - pipelines/forecast/model.py
  - pipelines/forecast/walkforward.py
  - tests/unit/test_cqr_interval.py
  - tests/unit/test_walkforward_no_lookahead.py
autonomous: true
requirements: [FCAST-01, FCAST-03]
must_haves:
  truths:
    - "Three XGBoost quantile regressors (reg:quantileerror at 0.1/0.9/0.5) wrapped in MAPIE ConformalizedQuantileRegressor(prefit=True) produce an ADAPTIVE-width 80% interval (D5-03)"
    - "The walk-forward loop is expanding-window as-of-T0: for each IPO the train+calibration pool is strictly {listing_date < that IPO's issue_date}, so the displayed band = the backtested band (D5-11)"
    - "The calibration slice is disjoint from the proper-train slice and both are before T0 (valid conformal coverage)"
    - "An automated R²>0.5 alarm fires on the walk-forward out-of-sample median (P4 leakage red-flag)"
  artifacts:
    - path: "pipelines/forecast/model.py"
      provides: "make_quantile_models([lower,upper,median]) + fit_cqr(prefit=True) + predict_interval wrapper"
      contains: "reg:quantileerror"
    - path: "pipelines/forecast/walkforward.py"
      provides: "expanding-window as-of-T0 loop -> one OOS band per IPO + insufficient-history abstain"
      contains: "issue_date"
    - path: "tests/unit/test_cqr_interval.py"
      provides: "adaptive-width 80% interval on a fixture panel (offline, tiny)"
      contains: "width"
    - path: "tests/unit/test_walkforward_no_lookahead.py"
      provides: "train.listing_date.max() < ipo.issue_date per fold + R²>0.5 alarm"
      contains: "issue_date"
  key_links:
    - from: "pipelines/forecast/walkforward.py"
      to: "pipelines/forecast/model.make_quantile_models + fit_cqr"
      via: "per-IPO as-of-T0 train/calibrate/predict_interval"
      pattern: "make_quantile_models|fit_cqr|predict_interval"
    - from: "pipelines/forecast/model.py"
      to: "mapie ConformalizedQuantileRegressor(prefit=True)"
      via: "ordered [lower, upper, median] estimator list; confidence_level=0.8"
      pattern: "ConformalizedQuantileRegressor"
---

<objective>
Slice 2c (the modeling core, D5-03 / D5-11 / FCAST-01 / FCAST-03) — the phase's honesty keystone. Build the XGBoost-quantile + MAPIE CQR wrapper via the mandatory prefit path, and the expanding-window as-of-T0 walk-forward loop that produces exactly one out-of-sample adaptive-width band per IPO. Because each IPO's train+calibration pool is strictly the IPOs that listed before its T0, the band shown for a covered IPO IS the number scored by the backtest (D5-11) — no second code path, provably lookahead-free.

Purpose: this is the ≥35% non-LLM modeling budget (P11); the walk-forward loop and the T0 discipline are the load-bearing, hand-written pieces (RESEARCH: spend the budget here, not on re-deriving conformal math).
Output: `pipelines/forecast/model.py`, `pipelines/forecast/walkforward.py`, and the CQR-interval + no-lookahead tests (incl. the automated R²>0.5 alarm).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-PATTERNS.md
@pipelines/historical/__init__.py
@agent/forecast_schema.py
@tests/unit/fixtures/forecast_fixtures.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XGBoost-quantile + MAPIE CQR (prefit) wrapper</name>
  <read_first>
    - 05-RESEARCH.md §"Pattern 1: XGBoost-quantile + MAPIE CQR via the prefit path" (lines ~254-283 — the exact wiring, the ordered `[lower, upper, median]` list, `confidence_level=0.8`, `prefit=True`, `conformalize`, `predict_interval` shape (n,2,1); the anti-pattern) + §"State of the Art" (the MAPIE 1.x ConformalizedQuantileRegressor supersedes the deprecated MapieQuantileRegressor)
    - 05-PATTERNS.md §"No Analog Found" (this file copies RESEARCH §Pattern 1 — the codebase has zero modeling code)
    - 05-PATTERNS.md §"NO NETWORK / NO MODEL AT IMPORT" (import xgboost/mapie lazily INSIDE the functions, so importing the package + running unit tests never pulls the modeling stack at collection time)
    - tests/unit/fixtures/forecast_fixtures.py (synthetic_panel/synthetic_features — the tiny offline fixture)
  </read_first>
  <behavior>
    - make_quantile_models(X_tr, y_tr, params) returns exactly three fitted XGBRegressor(objective="reg:quantileerror", tree_method="hist") at quantile_alpha 0.1 (lower), 0.9 (upper), 0.5 (median) — in that list order
    - fit_cqr(models, X_cal, y_cal) returns a ConformalizedQuantileRegressor(estimator=models, confidence_level=0.8, prefit=True) after .conformalize(X_cal, y_cal)
    - predict_interval on a small held-out X returns (median_points, low, high) with high >= low and the interval width VARYING across at least two fixture IPOs (adaptive, not constant — D5-03)
  </behavior>
  <action>
    Create `pipelines/forecast/model.py`. Import `xgboost` and `mapie.regression.ConformalizedQuantileRegressor` LAZILY inside the functions (module import stays offline). Implement `make_quantile_models(X_tr, y_tr, params) -> list` fitting three `XGBRegressor(objective="reg:quantileerror", tree_method="hist", quantile_alpha=<q>, **params)` at q=0.1, 0.9, 0.5 and returning them in the ORDER `[m_low, m_high, m_med]` (NOT ascending — the MAPIE prefit contract). Implement `fit_cqr(models, X_cal, y_cal) -> ConformalizedQuantileRegressor` with `confidence_level=0.8, prefit=True` then `.conformalize(X_cal, y_cal)`. Implement `predict_band(cqr, X) -> tuple[np.ndarray, np.ndarray, np.ndarray]` returning `(median, low, high)` by unpacking `predict_interval` (`intervals[:,0,0]` low, `intervals[:,1,0]` high, points as median). Define default `PARAMS` (small trees suited to ~200-300 rows: modest n_estimators, shallow max_depth, per D5-07 lean/interpretable + small-N). Add a module docstring noting the anti-pattern (never pass a single XGBRegressor to prefit=False — XGBoost is not in MAPIE's non-prefit auto-support list). Write `tests/unit/test_cqr_interval.py`: on `synthetic_panel()`/`synthetic_features()`, split into train/calib, fit, predict on >=2 held-out rows, assert `high >= low` for all, and assert the interval width DIFFERS across at least two IPOs (adaptive-width, D5-03). Keep the fixture tiny so the test runs in seconds.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_cqr_interval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `make_quantile_models` returns three fitted models in `[lower(0.1), upper(0.9), median(0.5)]` order, each `objective=="reg:quantileerror"`
    - `fit_cqr` builds `ConformalizedQuantileRegressor(confidence_level=0.8, prefit=True)` and conformalizes on the calibration set
    - on the fixture the 80% interval has `high>=low` everywhere and width varies across >=2 IPOs (adaptive — D5-03)
    - importing `pipelines.forecast.model` does not import xgboost/mapie at module load (lazy)
    - `test_cqr_interval.py` passes
  </acceptance_criteria>
  <done>The prefit XGBoost-quantile + MAPIE CQR wrapper produces adaptive-width 80% intervals on a fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expanding-window as-of-T0 walk-forward loop + no-lookahead test + R²>0.5 alarm</name>
  <read_first>
    - 05-RESEARCH.md §"Pattern 2: Expanding-window walk-forward = displayed band = backtested band" (lines ~285-307 — the exact loop: order by listing_date; pool = rows[listing_date < issue_date]; MIN_TRAIN abstain; CAL_FRAC newest-slice calibration split; one OOS band per IPO) + §"Pattern 3" R²>0.5 note (line ~319) + §"Anti-Patterns" (calibration set must not overlap train; never KFold/shuffle)
    - agent/forecast_schema.py (ForecastRecord + the abstain states insufficient_history/out_of_support/interval_too_wide — the loop emits these as record inputs)
    - pipelines/forecast/model.py (make_quantile_models/fit_cqr/predict_band — Task 1)
    - tests/unit/fixtures/forecast_fixtures.py (synthetic_panel — ordered by listing_date)
  </read_first>
  <behavior>
    - walk_forward(panel, X) iterates IPOs ordered by listing_date; for IPO i, pool = {rows with listing_date < ipo.issue_date} (T0 = issue_date, D5-01)
    - if len(pool) < MIN_TRAIN -> emit an abstain(reason="insufficient_history") row, no band
    - else split pool by listing-date quantile into proper-train (older) and calibration (newer) slices — both strictly < T0_i and disjoint — fit + conformalize + predict one OOS band for i
    - returns per-IPO OOS rows (drhp_id/issuer, actual, low, high, median, listing_year, abstain flag) — the exact inputs for the metrics + records
    - a helper computes walk-forward OOS R² of the median and returns an alarm flag when R² > 0.5 (P4)
  </behavior>
  <action>
    Create `pipelines/forecast/walkforward.py` implementing `walk_forward(panel, X, *, min_train=60, cal_frac=0.25, params=None) -> pandas.DataFrame` per RESEARCH §Pattern 2: sort by `listing_date`, drop rows with NaN `listing_day_return` for scoring, and for each IPO compute `pool = rows[listing_date < ipo.issue_date]` (T0 = issue-open, D5-01). If `len(pool) < min_train`, append an abstain row (`abstain=True, abstain_reason="insufficient_history"`). Else split the pool at `pool.listing_date.quantile(1 - cal_frac)` into `tr` (proper-train, older) and `cal` (calibration, newer) — both strictly `< T0_i`, disjoint — call `make_quantile_models(X[tr], y[tr])`, `fit_cqr(models, X[cal], y[cal])`, and `predict_band(cqr, X[[i]])`, appending one OOS row `(drhp_id, issuer, actual=listing_day_return, low, high, median, listing_year, abstain=False)`. Add `r2_leakage_alarm(oos_df) -> tuple[float, str|None]` computing the OOS R² of the median vs actual and returning a plain-text alarm string when `R² > 0.5` (mirror validate.sanity_check_median's `(value, flag_or_None)` posture) — the automated P4 leakage red-flag for the model card. Import xgboost/mapie only transitively via model.py's lazy imports (walkforward import stays offline). Write `tests/unit/test_walkforward_no_lookahead.py`: (a) for EVERY non-abstain fold assert `pool.listing_date.max() < ipo.issue_date` (no lookahead) and that the calibration slice is disjoint from the proper-train slice; (b) IPOs earlier than `min_train` prior listings are abstained (`insufficient_history`); (c) `r2_leakage_alarm` returns an alarm string when handed a synthetically leaked OOS frame (R²>0.5) and None on the honest fixture.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_walkforward_no_lookahead.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - for every non-abstain fold `train.listing_date.max() < ipo.issue_date` holds (T0 = issue-open; no lookahead — P4/D5-01)
    - the calibration slice and proper-train slice are disjoint and both strictly before T0_i (valid conformal coverage)
    - IPOs with fewer than `min_train` prior listings emit an `insufficient_history` abstain row (D5-09), no fabricated band
    - `r2_leakage_alarm` fires (non-None) on an R²>0.5 frame and stays quiet (None) on the honest fixture (P4)
    - `test_walkforward_no_lookahead.py` passes
  </acceptance_criteria>
  <done>The expanding-window as-of-T0 loop yields one lookahead-free OOS band per IPO (displayed = backtested, D5-11) with an automated R²>0.5 leakage alarm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| time boundary T0_i (per-IPO issue-open) | Training/calibration data must never cross forward of T0_i |
| offline training compute | Runs off the render path; no untrusted network input at train time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-LOOK | Tampering (data integrity) | walk-forward train/calibration selection | mitigate | `pool = rows[listing_date < issue_date]` (T0=issue-open); calibration slice disjoint from train, both < T0_i; unit test asserts `train.listing_date.max() < ipo.issue_date` per fold; never KFold/shuffle (CLAUDE.md "What NOT to Use") |
| T-05-05-R2 | Repudiation (silent leakage) | OOS R² | mitigate | Automated `r2_leakage_alarm` fires when OOS R²>0.5 (P4) — a gate, not just prose; surfaced in the model card |
| T-05-05-CONF | Tampering (calibration validity) | conformal coverage | mitigate | MAPIE ConformalizedQuantileRegressor(prefit=True) with a disjoint pre-T0 calibration set; adaptive width; no parametric coverage |
| T-05-05-ISO | (invariant breach) | predictor ↔ GMP | mitigate | model.py/walkforward.py import no `gmp`; pinned by the 05-03 predictor-side isolation audit |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_cqr_interval.py tests/unit/test_walkforward_no_lookahead.py -q` passes.
- Adaptive-width interval confirmed; per-fold no-lookahead asserted; R²>0.5 alarm exercised.
</verification>

<success_criteria>
- Prefit XGBoost-quantile + MAPIE CQR adaptive-width 80% interval (D5-03, FCAST-01).
- Expanding-window as-of-T0 walk-forward: displayed band = backtested band (D5-11, FCAST-03), lookahead-free.
- Automated R²>0.5 leakage alarm (P4).
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-05-SUMMARY.md` when done.
</output>
