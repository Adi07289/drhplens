---
phase: 05-calibrated-listing-day-forecaster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/unit/conftest.py
  - tests/unit/fixtures/__init__.py
  - tests/unit/fixtures/forecast_fixtures.py
  - data/forecasts/swiggy_2024_11.json
  - data/forecasts/hyundai_2024_10.json
autonomous: true
requirements: [FCAST-01]
must_haves:
  truths:
    - "The modeling stack (xgboost, mapie, scikit-learn, mlflow, matplotlib, shap) imports cleanly in the .venv against pandas 3.0 / numpy 2.x"
    - "Downstream forecast/model/metrics tests can build a tiny synthetic panel + feature matrix from one shared offline fixture (no network, no live training)"
    - "The render slice can read a committed data/forecasts/<id>.json for a full-render IPO and an abstaining IPO"
  artifacts:
    - path: "pyproject.toml"
      provides: "xgboost/mapie/scikit-learn/mlflow/matplotlib/shap pinned per RESEARCH version-reconciliation"
      contains: "xgboost"
    - path: "tests/unit/fixtures/forecast_fixtures.py"
      provides: "shared synthetic panel + feature matrix + OOS-rows builders for offline modeling tests"
      min_lines: 30
    - path: "data/forecasts/swiggy_2024_11.json"
      provides: "hand-built full-render ForecastRecord fixture (band + global metrics)"
      contains: "interval"
    - path: "data/forecasts/hyundai_2024_10.json"
      provides: "hand-built abstain ForecastRecord fixture"
      contains: "abstain"
  key_links:
    - from: "tests/unit/fixtures/forecast_fixtures.py"
      to: "downstream modeling tests (cqr/walkforward/metrics/baselines)"
      via: "importable synthetic-panel builder"
      pattern: "def .*panel|def .*feature"
---

<objective>
Phase 5 foundation: install the CLAUDE.md-locked non-LLM modeling stack and lay down the two shared offline assets every later slice consumes — a synthetic-panel/feature test fixture and hand-built committed forecast records the render slice reads.

**Phase Goal (user story, faithfully reformatted from the ROADMAP Phase 5 Goal line):**
**As a** retail investor evaluating an Indian mainboard IPO, **I want to** see a calibrated 80% listing-day-return range (with the GMP-vs-model gap and the backtest's honesty metrics), **so that** I understand the uncertainty honestly instead of chasing a point-estimate or grey-market hype.

This plan produces no user-facing surface yet; it de-risks the environment (RESEARCH Assumption A6: pandas 3.0 / numpy 2.4 are very new) and unblocks parallel work in Wave 2+.

Purpose: every later plan depends on these deps resolving and on the shared fixture, so this is Wave 1.
Output: updated `pyproject.toml`, an installed/verified `.venv`, `tests/unit/fixtures/forecast_fixtures.py`, and two committed `data/forecasts/*.json` render fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-VALIDATION.md
@pyproject.toml
@agent/gmp_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add and verify the locked modeling dependencies</name>
  <read_first>
    - pyproject.toml (the file being modified — note the existing Phase 4 Indian-data pins yfinance==1.5.1 / requests-cache==1.3.3 / jugaad-data==0.33.1 stay untouched)
    - 05-RESEARCH.md §Standard Stack + §"Version reconciliation (flag for the planner)" + §Package Legitimacy Audit (all six deps are Approved/project-locked — NO human-verify needed)
    - CLAUDE.md §Technology Stack (XGBoost/MLflow rows) — text predates the 3.x releases
  </read_first>
  <action>
    Add to `[project].dependencies` in `pyproject.toml`, following the RESEARCH version-reconciliation guidance (allow 3.x, floor at the CLAUDE.md-locked 2.x family): `xgboost>=2.0`, `mapie>=1.4,<2`, `scikit-learn>=1.5`, `mlflow>=2.14`, `matplotlib>=3.8`, `shap>=0.46`. Group them under a `# ML / forecasting (Phase 5)` comment. Do NOT add `nse` here (it is [ASSUMED] provenance and gated behind a human-verify checkpoint in 05-11, needed only at the live universe build). Do NOT add `dieboldmariano` (05-09 implements a ~15-line inline Diebold–Mariano to avoid the low-download dependency). Then install into the existing `.venv` with `uv pip install -e ".[dev]"` (or `uv pip install` of the six names). If the resolver conflicts against the installed pandas 3.0.3 / numpy 2.4.6 (RESEARCH A6), pin the narrowest compatible xgboost/mapie versions that DO resolve and record the resolved versions in the SUMMARY — never downgrade pandas/numpy (they are load-bearing for the rest of the app).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import xgboost, mapie, sklearn, mlflow, matplotlib, shap; print(xgboost.__version__, mapie.__version__, sklearn.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import xgboost, mapie, sklearn, mlflow, matplotlib, shap"` exits 0 (all six import against pandas 3.0.3 / numpy 2.4.6)
    - `pyproject.toml` `[project].dependencies` contains `xgboost`, `mapie`, `scikit-learn`, `mlflow`, `matplotlib`, `shap`; contains NO `nse` and NO `dieboldmariano`
    - the pre-existing yfinance==1.5.1 / requests-cache==1.3.3 / jugaad-data==0.33.1 pins are unchanged
    - `.venv/bin/python -m pytest tests/unit -q` still collects and passes (no import regressions)
  </acceptance_criteria>
  <done>The six modeling deps are declared, installed, and import cleanly; the resolved versions are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Shared offline fixtures — synthetic panel/features + committed forecast records</name>
  <read_first>
    - agent/gmp_schema.py (the GmpRecord codec + on-disk JSON shape to mirror; the abstain-as-first-class posture)
    - pipelines/historical/__init__.py (PANEL_COLUMNS, PANEL_DTYPES, compute_listing_day_return — the panel columns the synthetic fixture must match)
    - 05-RESEARCH.md §"Forecast Record Schema" (lines ~464-484) — the exact data/forecasts/<id>.json field set the JSON fixtures must conform to
    - tests/unit/conftest.py (existing fixtures/markers; extend, do not clobber)
    - tests/unit/test_gmp_precompute.py (the offline monkeypatch posture the modeling tests will reuse)
  </read_first>
  <action>
    Create `tests/unit/fixtures/__init__.py` and `tests/unit/fixtures/forecast_fixtures.py` exporting pure, no-network builders the Wave 2-6 modeling tests import: `synthetic_panel(n=80, seed=0)` returning a pandas DataFrame with the `PANEL_COLUMNS` schema (issuer, issue_date, listing_date, issue_price, listing_day_close, listing_day_return, status) ordered by listing_date, with a realistic listing-day-return spread (roughly -20%..+60%, some heteroscedastic) and every `status` in STATUS_VALUES including >=1 `withdrawn` and >=1 `delisted`; `synthetic_features(panel)` returning an X matrix of a few issue-structure columns (issue_size_cr, price_band_width_pct, ofs_fraction, promoter_dilution_pct, lot_size) each stamped with an `available_at <= issue_date`; and `oos_rows()` returning a small DataFrame of per-IPO out-of-sample rows (actual, low, high, median, listing_year) for the metrics/baselines tests. Keep it deterministic (fixed seed). Then hand-build two committed render fixtures under `data/forecasts/`: `swiggy_2024_11.json` — a FULL-render record conforming to the RESEARCH schema (`abstain:false`, `out_of_sample:true`, `walk_forward:true`, an `interval` block with low_pct/high_pct/median_pct/width_pts, a `sector`, and a GLOBAL `metrics` block with coverage_empirical/mae_pts/backtest_window/n/per_year_rmse) with honest illustrative numbers (coverage NOT exactly 0.80, e.g. 0.783); and `hyundai_2024_10.json` — an ABSTAIN record (`abstain:true`, `abstain_reason:"insufficient_history"`, no interval numbers fabricated). Add a one-line note in the SUMMARY that these two are hand-seeded to unblock the render slice offline and are regenerated from the real walk-forward run in 05-06/05-11 (mirrors the Phase 4 GMP CODE-NOW-DEFER seed posture).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit -q && .venv/bin/python -c "import json; r=json.load(open('data/forecasts/swiggy_2024_11.json')); assert r['abstain'] is False and 'interval' in r and abs(r['metrics']['coverage_empirical']-0.80)>1e-9; a=json.load(open('data/forecasts/hyundai_2024_10.json')); assert a['abstain'] is True and a['abstain_reason']=='insufficient_history'; print('fixtures ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `from tests.unit.fixtures.forecast_fixtures import synthetic_panel, synthetic_features, oos_rows` succeeds and each builder returns a non-empty deterministic object with no network access
    - `synthetic_panel()` status column contains at least one `withdrawn` and one `delisted` row (P3 survivorship-representable)
    - `data/forecasts/swiggy_2024_11.json` parses, has `abstain==false`, an `interval` with the four keys, and a `metrics` block whose `coverage_empirical` is NOT exactly 0.80 (honesty invariant P17)
    - `data/forecasts/hyundai_2024_10.json` parses, has `abstain==true` and `abstain_reason=="insufficient_history"`, and fabricates NO interval numbers
    - full `pytest tests/unit -q` stays green
  </acceptance_criteria>
  <done>Downstream modeling tests can import a deterministic synthetic panel/feature/OOS fixture; the render slice has one full and one abstain committed record to render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local `.venv` | Third-party package code executes at install/import time |
| repo JSON fixtures → committed cache | Hand-authored `data/forecasts/*.json` become the render input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | new pip installs (xgboost/mapie/sklearn/mlflow/matplotlib/shap) | mitigate | All six are Approved in RESEARCH §Package Legitimacy Audit (project-locked, verified on PyPI with source repos); pin floors; `nse`/`dieboldmariano` explicitly excluded here (gated/avoided). No [ASSUMED]/[SUS] package installed in this plan. |
| T-05-01-FAB | Tampering | hand-built forecast fixtures | accept | Fixtures use honest illustrative numbers, coverage != 0.80, abstain record fabricates no interval; flagged in SUMMARY as seed-only, regenerated from the real run later. No PII/secrets. |
</threat_model>

<verification>
- `.venv/bin/python -c "import xgboost, mapie, sklearn, mlflow, matplotlib, shap"` exits 0.
- `.venv/bin/python -m pytest tests/unit -q` green.
- Both `data/forecasts/*.json` fixtures parse and satisfy their state assertions.
</verification>

<success_criteria>
- Modeling stack installed and importing against pandas 3.0.3 / numpy 2.4.6 (or narrowest resolving pins, recorded).
- Shared synthetic-panel/feature/OOS fixture importable and deterministic.
- One full-render and one abstain forecast record committed for the render slice.
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-01-SUMMARY.md` when done (record resolved dep versions).
</output>
