---
phase: 05-calibrated-listing-day-forecaster
plan: 11
type: execute
wave: 9
depends_on: ["05-02", "05-06", "05-08", "05-09", "05-10"]
files_modified:
  - pyproject.toml
  - data/historical/ipo_panel.parquet
  - data/historical/ipo_panel.csv
  - data/forecasts/
  - model_card/MODEL_CARD.md
  - mlruns/
autonomous: false
requirements: [FCAST-03, FCAST-05]
user_setup:
  - service: nse (NseIndiaApi)
    why: "Listed-universe fetch (NSE public-past-issues) at the live universe build; handles NSE cookie/bot-detection"
    package_legitimacy: "[ASSUMED] provenance in 05-RESEARCH §Package Legitimacy Audit — blocking-human verify on pypi.org before install"
    dashboard_config:
      - task: "Verify pypi.org/project/nse (source repo, release cadence, downloads) then approve install"
        location: "https://pypi.org/project/nse"
  - service: network-egress
    why: "Live crawl of NSE/SEBI/chittorgarh for the survivorship-safe universe (sandbox has no egress — 04-07 posture)"
    env_vars: []
must_haves:
  truths:
    - "The nse [ASSUMED] package is verified at a blocking-human checkpoint before install (never auto-approved)"
    - "One live universe build produces a survivorship-safe panel with a non-zero withdrawn/delisted count (FCAST-03/P3)"
    - "The real data/forecasts/<id>.json records + the real model_card regenerate from the live walk-forward on the lean feature set — replacing the hand-seeded fixtures"
    - "The P9 release gate + R²>0.5 leakage alarm are re-run on the REAL walk-forward and HARD-asserted in Task 2's <verify> (release_gate(...)['passed'] is True AND the R² alarm is None) — a failed gate STOPS the commit of data/forecasts/*.json + model_card/MODEL_CARD.md, not merely a SUMMARY note (P9/P4)"
    - "The live walk-forward run is MLflow-tracked: coverage/MAE/per-year-RMSE + the four-baseline DM verdicts + key XGBoost/CQR params are logged to a committed mlruns/ run (DS-rigor visibility)"
    - "A human verifies the /methodology model card (calibration plot, PIT, DM table, leakage audit, limitations) + the honest P9 release-gate verdict, and the card commit is contingent on the recorded PASS verdict"
  artifacts:
    - path: "data/historical/ipo_panel.parquet"
      provides: "the real survivorship-safe universe panel (non-zero withdrawn/delisted)"
      contains: "listing_day_return"
    - path: "data/forecasts/_gate/release_gate.json"
      provides: "the REAL-run P9 release-gate verdict (passed / r2 / r2_alarm / per_baseline DM / notes) persisted at the live build so the record + model-card commit is gate-contingent"
      contains: "passed"
    - path: "model_card/MODEL_CARD.md"
      provides: "the REAL committed model card regenerated from the live walk-forward (FCAST-05)"
      contains: "Diebold"
  key_links:
    - from: "pipelines/historical/build.build (live)"
      to: "pipelines/forecast/precompute.precompute-all + card.build_model_card"
      via: "real panel -> real records + real model card"
      pattern: "precompute|build_model_card"
    - from: "pipelines/forecast/walkforward.walk_forward (live) + baselines.release_gate + walkforward.r2_leakage_alarm"
      to: "05-11 Task 2 <verify> hard assertions (assert alarm is None; assert release_gate(...)['passed'] is True)"
      via: "persisted data/forecasts/_gate/{oos_real.parquet,release_gate.json} recomputed + hard-asserted at the real-artifact commit point (STOP-on-fail)"
      pattern: "release_gate|r2_leakage_alarm"
    - from: "05-RESEARCH §Package Legitimacy Audit (nse [ASSUMED])"
      to: "blocking-human verify checkpoint"
      via: "pypi.org verification before install"
      pattern: "pypi.org/project/nse"
---

<objective>
Slice 3d (the live checkpoint, FCAST-03 / FCAST-05) — turn the fully offline-tested pipeline into the real committed artifacts. This plan is the network/human checkpoint the whole phase deferred (the 04-07 posture): (1) verify the `nse` [ASSUMED] package at a blocking-human legitimacy checkpoint before install, (2) run the live universe build against NSE/SEBI/chittorgarh and confirm a survivorship-safe panel with a non-zero withdrawn/delisted count (FCAST-03/P3), then regenerate the REAL `data/forecasts/<id>.json` records + the REAL `model_card/` (plots + MODEL_CARD.md) from the live walk-forward on the lean feature set — replacing the hand-seeded fixtures, with the P9 release gate + R²>0.5 leakage alarm re-run and HARD-asserted at the commit point so a leaking or naive-baseline-beaten model CANNOT be committed on prose alone, and (3) have a human verify the `/methodology` model card + the honest P9 release-gate verdict.

Purpose: everything upstream is unit-tested offline with monkeypatched fetchers; this is the one step that needs live egress + human approval, so it is isolated in its own non-autonomous plan. The gate that upstream built as real, unit-tested code (05-05 `r2_leakage_alarm`, 05-09 `release_gate`) is enforced HERE as an automated STOP at the real-artifact commit point — not merely recorded.
Output: verified+installed `nse`, a real committed `data/historical/ipo_panel.*`, real `data/forecasts/*.json`, the persisted real-run gate verdict, a committed `mlruns/` run, a real `model_card/`, and a human sign-off on the rendered card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-calibrated-listing-day-forecaster/05-RESEARCH.md
@.planning/phases/05-calibrated-listing-day-forecaster/05-VALIDATION.md
@pipelines/historical/build.py
@pipelines/forecast/precompute.py
@data/historical/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the nse package legitimacy, then install</name>
  <read_first>
    - 05-RESEARCH.md §"Package Legitimacy Audit" (line ~130 — the `nse` [ASSUMED] row: maintainer, source repo, download signal) + §"Optional / evaluate" (nse NseIndiaApi; `dieboldmariano` explicitly avoided via the inline DM in 05-09)
    - pipelines/historical/sources.py (fetch_nse_past_issues — the live-only seam that lazily imports `nse`, with a hand-rolled requests + cookie-priming fallback if nse is rejected)
    - pyproject.toml (the `# ML / forecasting (Phase 5)` dependency group added in 05-01 — where nse would be pinned)
  </read_first>
  <what-built>
    Nothing is auto-installed yet. `nse` (NseIndiaApi) is flagged `[ASSUMED]` provenance in 05-RESEARCH §Package Legitimacy Audit (niche NSE client that handles cookie/bot-detection). Per the package-legitimacy gate this install requires a blocking-human verification — it is NEVER auto-approvable (workflow.auto_advance is ignored here). `dieboldmariano` is NOT installed at all (05-09 uses an inline DM test to avoid it).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/nse and confirm: an active source repository, a plausible release cadence, and non-trivial download counts (not a typosquat/slop package).
    2. Cross-check the import name and maintainer against the RESEARCH §Package Legitimacy Audit row for `nse`.
    3. If legitimate: reply "approved" — the executor pins `nse` in `pyproject.toml` under the `# ML / forecasting (Phase 5)` group and installs it into the existing `.venv` with `uv pip install`, recording the resolved version in the SUMMARY.
    4. If it looks like a typosquat / abandoned / [SLOP]: reply with the concern; the executor uses the hand-rolled `requests` + cookie-priming fallback already seamed in `sources.fetch_nse_past_issues` and does NOT install `nse`.
  </how-to-verify>
  <action>
    Pause for the blocking-human legitimacy verification above BEFORE any install. On "approved": add `nse` to `pyproject.toml` under the `# ML / forecasting (Phase 5)` group and run `uv pip install` into the existing `.venv`; record the resolved version in the SUMMARY. On a legitimacy concern: do NOT install `nse`; document the hand-rolled `requests` + cookie-priming fallback path in `sources.fetch_nse_past_issues` in the SUMMARY. This checkpoint is never auto-advanced regardless of `workflow.auto_advance`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import nse" 2>/dev/null && echo "nse installed" || echo "nse NOT installed — hand-rolled fallback (document in SUMMARY)"; .venv/bin/python -m pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - the `nse` install happens ONLY after an explicit "approved" at the blocking-human checkpoint (never auto-approved); on a legitimacy concern the hand-rolled fallback is used instead
    - `pytest tests/unit -q` still passes after the decision (no import regressions)
    - `dieboldmariano` is NOT installed (the inline DM in 05-09 is used)
  </acceptance_criteria>
  <resume-signal>Type "approved" to install nse, or describe the legitimacy concern to use the hand-rolled fallback.</resume-signal>
  <done>nse is verified + installed, OR the hand-rolled NSE fallback is documented; the unit suite stays green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Live universe build + regenerate real records + real model card (gate HARD-enforced)</name>
  <read_first>
    - pipelines/historical/build.py (the live `build` CLI + two-source merge from 05-02 + `sanity_check_median` survivorship alarm + the non-zero-row assertion)
    - pipelines/features/select.py (05-08 `select_features` — refresh the lean SELECTED_FEATURES on the real panel) + pipelines/forecast/walkforward.py (05-05/05-09 loop + abstention + `r2_leakage_alarm`) + pipelines/forecast/baselines.py (05-09 `release_gate`)
    - pipelines/forecast/precompute.py (05-06 `precompute-all` — regenerate real data/forecasts/*.json; MLflow-tracked at `track=True`) + pipelines/forecast/card.py (05-10 `build_model_card` — regenerate the real model_card/)
    - 05-RESEARCH.md §"Runtime → Boundary" MLflow rows (lines ~75-76, 90 — quantile training + walk-forward backtest are MLflow-tracked, local file backend, commit `mlruns/`) — the DS-rigor visibility this real run realizes
    - 05-VALIDATION.md §"Manual-Only Verifications" (the live universe build with non-zero withdrawn/delisted is manual by nature)
  </read_first>
  <what-built>
    The offline pipeline (05-02 two-source merge, 05-08 features, 05-09 walk-forward + baselines/DM + `release_gate`, 05-10 diagnostics + model card) is fully unit-tested with monkeypatched fetchers. `r2_leakage_alarm` (05-05) and `release_gate` (05-09) are real, unit-tested functions. This step runs the pipeline against LIVE sources (network egress, unavailable in the executor sandbox — a human-run / network-approved step, the 04-07 posture) AND persists the REAL out-of-sample frame + gate verdict so the gate is re-run and HARD-asserted at the commit point (not left as prose in a SUMMARY).
  </what-built>
  <how-to-verify>
    1. Run `pipelines/historical/build.py build` (live) against NSE `public-past-issues` (Source A) + SEBI/chittorgarh-withdrawn (Source B, id 202). Confirm a NON-ZERO withdrawn/delisted count in `data/historical/ipo_panel.parquet` (FCAST-03/P3) and a non-zero total row count (Pitfall 7 — a silent 0-row build is the 04-07 failure); `sanity_check_median` should not flag survivorship.
    2. Refresh the lean `SELECTED_FEATURES` (D5-07) on the real panel, then run the walk-forward (MLflow-tracked) + `baselines.release_gate` (P9): PERSIST the real OOS frame to `data/forecasts/_gate/oos_real.parquet` and the `release_gate(...)` verdict dict (JSON) to `data/forecasts/_gate/release_gate.json` BEFORE committing any records/card. Record the real empirical coverage, the DM verdict vs the four baselines, and the R²>0.5 alarm status. If `r2_leakage_alarm` fires (R²>0.5) OR `release_gate(...)["passed"]` is False, STOP and investigate — do NOT commit fabricated/leaking artifacts.
    3. Regenerate the REAL records with `precompute-all` (`track=True`, overwriting the hand-seeded `data/forecasts/*.json`) and the REAL `model_card/` (plots + MODEL_CARD.md) with `build_model_card`. Commit the real panel, records, the persisted gate verdict, the model card, and the `mlruns/` run.
    4. Confirm the Task 2 `<verify>` HARD assertions pass (survivorship > 0, R² alarm is None, `release_gate(...)["passed"] is True`) and `pytest tests -q` (full suite) stays green against the real artifacts.
  </how-to-verify>
  <action>
    This step requires live network egress (unavailable in the executor sandbox) and human oversight of the crawl. Pause as a human-action checkpoint: on resume, execute the live build, the lean-set refresh, and the walk-forward exactly as in How-to-verify. Wrap the live walk-forward + metrics in an `mlflow.start_run()` on the local file backend (default `mlruns/`, per CLAUDE.md deploy plan), logging coverage / MAE / per-year-RMSE + the four-baseline DM verdicts + key XGBoost/CQR params (this is the real-run instance of the 05-06 `track=True` path). Then PERSIST the real OOS frame to `data/forecasts/_gate/oos_real.parquet` and the `release_gate(oos_real, panel)` verdict dict (JSON-serialised: `passed`, `r2`, `r2_alarm`, `per_baseline`, `notes`) to `data/forecasts/_gate/release_gate.json` — this is what the `<verify>` block recomputes + hard-asserts and what Task 3's card commit is made contingent on. Only if the survivorship count is non-zero AND `r2_leakage_alarm` returns None (R²≤0.5) AND `release_gate(...)["passed"]` is True do you run `precompute-all` + `build_model_card` and commit the real panel + records + persisted verdict + model card + `mlruns/`. If the R²>0.5 alarm fires, a source returns 0 rows, or the release gate does not pass, STOP and report the blocker rather than committing fabricated/leaking artifacts.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import pandas as pd
from pipelines.forecast.walkforward import r2_leakage_alarm
from pipelines.forecast.baselines import release_gate
panel = pd.read_parquet('data/historical/ipo_panel.parquet')
n = int(panel['status'].isin(['withdrawn','delisted']).sum())
print('withdrawn/delisted rows:', n)
assert n > 0, 'P3 survivorship: zero withdrawn/delisted rows'
oos = pd.read_parquet('data/forecasts/_gate/oos_real.parquet')
r2, alarm = r2_leakage_alarm(oos)
print('real OOS R2:', r2, '| leakage alarm:', alarm)
assert alarm is None, 'P4 leakage: R2>0.5 on the real run -> STOP, do not commit forecasts/model card'
verdict = release_gate(oos, panel)
print('release_gate passed:', verdict['passed'], '| notes:', verdict.get('notes'))
assert verdict['passed'] is True, 'P9 release gate FAILED on the real run -> a baseline significantly beats the model or R2>0.5; do not commit'
" && .venv/bin/python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `data/historical/ipo_panel.parquet` has a NON-ZERO withdrawn/delisted count and a non-zero total row count (FCAST-03/P3, Pitfall 7)
    - the real OOS frame + `release_gate(...)` verdict are PERSISTED to `data/forecasts/_gate/{oos_real.parquet,release_gate.json}` and the Task 2 `<verify>` HARD-asserts `r2_leakage_alarm(...)` is None AND `release_gate(...)["passed"] is True` — a failed gate FAILS the task and STOPS the commit of `data/forecasts/*.json` + `model_card/MODEL_CARD.md` (code-enforced, not a SUMMARY note; mirrors the survivorship assert)
    - the live walk-forward is MLflow-tracked: an `mlruns/` run dir is created with logged coverage / MAE / per-year-RMSE + baseline DM verdicts + key CQR/XGBoost params, and `mlruns/` is committed (CLAUDE.md deploy plan)
    - the real `data/forecasts/*.json` + `model_card/` are regenerated from the live walk-forward and committed ONLY after the gate passes (replacing the hand-seeded fixtures)
    - `pytest tests -q` (full suite) stays green against the real artifacts
  </acceptance_criteria>
  <resume-signal>Type "built" once the real panel (non-zero withdrawn), records, persisted PASS verdict, mlruns/, and model card are regenerated and committed; or describe the blocker (source rot / leakage alarm / gate FAIL).</resume-signal>
  <done>The live survivorship-safe universe build + real records + real model card are committed only after the R²/P9 gates re-ran on the real data and HARD-passed at the commit point.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the rendered /methodology model card + honest release-gate verdict (card commit gate-contingent)</name>
  <read_first>
    - pages/01_methodology.py (05-10 forecaster model-card section — the render being verified)
    - 05-UI-SPEC.md §item 2 + §L5-3 (the model-card content contract + the `Full model card →` link target)
    - model_card/MODEL_CARD.md (the committed card the section reads)
    - data/forecasts/_gate/release_gate.json (the persisted REAL-run P9 verdict from Task 2 — the card commit is contingent on `passed` being True)
  </read_first>
  <what-built>
    The real `model_card/` artifacts are committed and the `/methodology` forecaster model-card section (05-10) reads them. The persisted `data/forecasts/_gate/release_gate.json` records the honest PASS/FAIL verdict; the model-card commit is contingent on that recorded verdict being PASS (Task 2 already STOPPED the commit if it were not).
  </what-built>
  <how-to-verify>
    1. Open `/methodology` and confirm the forecaster model-card section renders: the calibration plot + PIT histogram are legible, the SHAP importance shows the lean feature set, the four-baselines + Diebold–Mariano table shows the real p-values with the P9 release-gate verdict, the `available_at <= T0` leakage audit (incl. the anchor pre-open audit) is present, and Known limitations is shown.
    2. Confirm the empirical coverage is the REAL held-out number (not exactly 0.80) and the card states plainly whether the model significantly beats the baselines — including the honest "does not significantly outperform" note if it is a tie (D5-01, P17). Confirm the rendered verdict matches the recorded `release_gate.json` `passed`/`notes`.
    3. From a covered IPO's snapshot page, click `Full model card →` and confirm it lands on this /methodology section (UI-03 link destination).
  </how-to-verify>
  <action>
    Pause for the human visual verification above. On resume, record the human sign-off (or the reported gap) in the SUMMARY: whether the calibration/PIT/SHAP/DM-table/leakage-audit/limitations render legibly, whether the coverage is the honest held-out number, whether the rendered verdict matches the recorded `release_gate.json` PASS verdict, and whether the `Full model card →` link resolves to the section (UI-03).
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json
v = json.load(open('data/forecasts/_gate/release_gate.json'))
print('recorded release_gate verdict passed:', v['passed'])
assert v['passed'] is True, 'model-card commit is contingent on a PASS release-gate verdict (P9)'
" && test -f model_card/MODEL_CARD.md && grep -q 'Diebold' model_card/MODEL_CARD.md && grep -Eq 'calibration|pit|shap' pages/01_methodology.py && echo "methodology model-card wiring present + gate PASS recorded"</automated>
  </verify>
  <acceptance_criteria>
    - the persisted `data/forecasts/_gate/release_gate.json` verdict is `passed: true`; the model-card commit is contingent on this recorded PASS (not merely that the string "Diebold" appears)
    - the /methodology forecaster model-card section renders the calibration plot, PIT histogram, SHAP importance, four-baselines + DM table with the P9 verdict, the anchor `available_at` leakage audit, and Known limitations
    - the empirical coverage is the real held-out number (not 0.80-rounded); the card states honestly whether the model beats the baselines (D5-01/P17) and matches the recorded verdict
    - the `Full model card →` link from a covered IPO resolves to this section (UI-03)
  </acceptance_criteria>
  <resume-signal>Type "approved" if the model card renders honestly, the recorded verdict is PASS, and the link resolves; or describe what is missing/misleading.</resume-signal>
  <done>A human has signed off that the /methodology model card renders honestly, the recorded release-gate verdict is PASS, and the forecast-block link resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI (`nse` [ASSUMED]) -> local `.venv` | Third-party package executes at install/import — legitimacy unverified until the checkpoint |
| NSE/SEBI/chittorgarh (untrusted web) -> committed panel | Live-scraped HTML/JSON crosses into the committed universe |
| live walk-forward output -> committed records + model card | The real user-visible artifacts |
| real-run gate verdict -> commit decision | The R²/P9 gate is HARD-asserted at the commit point; a FAIL stops the commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-SC | Tampering | `nse` [ASSUMED] pip install | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies pypi.org/project/nse BEFORE install; never auto-approved; hand-rolled requests fallback if suspect; `dieboldmariano` avoided entirely (inline DM in 05-09) |
| T-05-11-SSRF | Information disclosure | live crawl outbound hosts | mitigate | The live build fetches only the hard-coded `ALLOWED_HOSTS` (05-02); every fetch routes through `_get`/`_check_host`; no URL derived from input |
| T-05-11-VALID | Tampering / DoS | untrusted live NSE/SEBI/chittorgarh parse | mitigate | Every field routed through `coerce_price`/`coerce_date`/`normalize_status` (05-02); missing -> NaN retained; per-source + per-row failure isolation; raw payload saved (A1) |
| T-05-11-SURV | Tampering (data integrity) | survivorship control | mitigate | Two-source merge asserts a NON-ZERO withdrawn/delisted count (FCAST-03/P3); `sanity_check_median` >20%-median alarm; non-zero total-row assertion (Pitfall 7) |
| T-05-11-LEAK | Repudiation (silent leakage) | real OOS R² + P9 gate | mitigate | The real OOS frame + `release_gate` verdict are PERSISTED, then the R²>0.5 alarm + P9 release gate are recomputed and HARD-asserted in Task 2's `<verify>` (`assert alarm is None` + `assert release_gate(...)["passed"] is True`); a leak or baseline-beats-model FAILS the task and STOPS the commit — code-enforced, not prose |
| T-05-11-PII | Information disclosure | regenerated model card | mitigate | The card is aggregate metrics/audit only; no secret/PII/credentials committed (asserted by the 05-10 card test on the same writer) |
</threat_model>

<verification>
- Task 1: `.venv/bin/python -c "import nse"` exits 0 (post-approval) OR the hand-rolled fallback is documented; `pytest tests/unit -q` green.
- Task 2: real `data/historical/ipo_panel.parquet` has a non-zero withdrawn/delisted count; the persisted real OOS frame + verdict re-run the gate HARD (R² alarm None + `release_gate(...)["passed"] is True`); MLflow `mlruns/` run logged; real `data/forecasts/*.json` + `model_card/` regenerated; `pytest tests -q` green.
- Task 3: the recorded `release_gate.json` verdict is PASS (card commit contingent); /methodology model card renders honestly; `Full model card →` link resolves (UI-03).
</verification>

<success_criteria>
- `nse` verified + installed (or hand-rolled fallback documented) via a blocking-human legitimacy checkpoint.
- One live survivorship-safe universe build with non-zero withdrawn/delisted (FCAST-03/P3).
- Real forecast records + real committed model card regenerated from the live walk-forward (FCAST-05); the R²>0.5 gate + P9 release gate re-run on the real run and HARD-asserted at the commit point (a FAIL stops the commit).
- The live walk-forward run is MLflow-tracked to a committed `mlruns/` run (coverage/MAE/per-year-RMSE + baseline DM verdicts + key params).
- Human sign-off on the rendered /methodology model card + the recorded PASS release-gate verdict.
</success_criteria>

<output>
Create `.planning/phases/05-calibrated-listing-day-forecaster/05-11-SUMMARY.md` when done (record the resolved `nse` version, the real withdrawn/delisted count, the real coverage, the persisted P9 `release_gate` verdict, and the `mlruns/` run id).
</output>
</content>
</output>
