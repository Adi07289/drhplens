---
phase: 01-foundation-mvp-a-cited-q-a-on-one-ipo
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - .gitignore
  - data/swiggy_drhp/swiggy_prospectus_2024_11.pdf
  - data/swiggy_drhp/SHA256SUMS
  - tests/__init__.py
  - tests/conftest.py
  - tests/unit/__init__.py
  - tests/unit/test_schemas.py
  - tests/unit/test_scrubber.py
  - tests/unit/test_disclaimer_surface.py
  - tests/unit/test_parser.py
  - tests/unit/test_chunker.py
  - tests/unit/test_embedder.py
  - tests/unit/test_retrieve.py
  - tests/unit/test_gate1.py
  - tests/unit/test_cite_check.py
  - tests/unit/test_decompose.py
  - tests/unit/test_citation_renderer.py
  - tests/unit/test_copy_no_banned_tokens.py
  - tests/unit/test_drhp_integrity.py
  - tests/integration/__init__.py
  - tests/integration/test_qdrant_ingest.py
  - tests/integration/test_agent_e2e.py
  - tests/integration/test_drhp_prompt_injection.py
  - tests/integration/test_langfuse_trace.py
  - tests/eval/__init__.py
  - tests/eval/gold/swiggy_phase1_gold.jsonl
  - tests/eval/test_numeric_faithfulness.py
  - pipelines/__init__.py
  - pipelines/ingest_swiggy.py
  - agent/__init__.py
  - agent/nodes/__init__.py
  - tools/__init__.py
  - storage/__init__.py
  - ui/__init__.py
  - compliance/__init__.py
  - observability/__init__.py
  - static/.gitkeep
  - pages/.gitkeep
autonomous: true
requirements:
  - INGEST-01
must_haves:
  truths:
    - "Developer can run `pytest tests/unit -x -q` and see all stub tests collected (red XFAIL is acceptable; collection errors are not)"
    - "Swiggy DRHP binary is committed and SHA-pinned; integrity test passes"
    - "All locked-stack packages install via `uv pip install -e .[dev]` (or `pip install -r requirements.txt`)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Pinned locked-stack dependencies + pytest + ruff config"
      contains: "langgraph>=1.2,<2"
    - path: "requirements.txt"
      provides: "HF Spaces deployment-ready dependency list"
      contains: "streamlit>=1.36"
    - path: "data/swiggy_drhp/swiggy_prospectus_2024_11.pdf"
      provides: "The Swiggy Nov 2024 DRHP binary (~10-15 MB)"
    - path: "data/swiggy_drhp/SHA256SUMS"
      provides: "Cryptographic integrity pin for the DRHP PDF"
      contains: "swiggy_prospectus_2024_11.pdf"
    - path: "tests/conftest.py"
      provides: "Shared fixtures: fixture DRHP, mock Qdrant, mock LLM, gold-set loader"
    - path: "tests/eval/gold/swiggy_phase1_gold.jsonl"
      provides: "Schema-shaped stub gold-set (5 factual + 3 numeric + 3 risk + 2 refusal entries — content fills in Wave 5)"
  key_links:
    - from: "all tests/unit/test_*.py"
      to: "pytest collection"
      via: "import-only stubs (one XFAIL per row in 01-VALIDATION.md Per-Task Verification Map)"
      pattern: "pytest tests/unit -x -q --collect-only"
    - from: "data/swiggy_drhp/swiggy_prospectus_2024_11.pdf"
      to: "data/swiggy_drhp/SHA256SUMS"
      via: "sha256 file-integrity match"
      pattern: "sha256sum -c data/swiggy_drhp/SHA256SUMS"
---

<objective>
Establish the repository scaffolding, locked-stack dependencies, test infrastructure, and the Swiggy DRHP binary so every subsequent wave has a runnable test harness from the first commit. This plan ships nothing user-facing — it makes every Per-Task Verification Map row in 01-VALIDATION.md collectible by pytest (red stubs are the goal; green bodies are owned by later waves).

Purpose: Nyquist compliance from Wave 0. Without stub tests for every later-wave behavior, sampling discipline breaks down. The Pitfall P20 antibody: a scaffolded repo is the only thing that prevents Phase 1 from sliding into "let me also wire in structured extraction" — every later plan is now bounded by the test rows declared here.

Output:
- A working `pyproject.toml` + `requirements.txt` mirror with pinned versions
- The Swiggy DRHP committed + SHA-pinned (closes INGEST-01 binary acquisition)
- Stub test files (one per VALIDATION.md row) that collect without error
- Empty package directories + `__init__.py` files for `agent/`, `pipelines/`, `tools/`, `storage/`, `ui/`, `compliance/`, `observability/`
- `.gitignore` covering `.venv/`, `.env`, `__pycache__/`, `*.pyc`, `mlruns/`, `~/.cache/docling/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pyproject.toml, requirements.txt, .gitignore, and the empty package skeleton</name>
  <files>pyproject.toml, requirements.txt, .gitignore, agent/__init__.py, agent/nodes/__init__.py, pipelines/__init__.py, tools/__init__.py, storage/__init__.py, ui/__init__.py, compliance/__init__.py, observability/__init__.py, static/.gitkeep, pages/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Standard Stack" + §"Installation" — the EXACT version pins to use (per D-09 supply-chain mitigation, do not loosen)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Recommended Project Structure" — the EXACT directory layout (locked in SKELETON.md §G)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md §G — directory layout invariant (do NOT reorganize; later phases must add inside these folders)
    - .planning/research/STACK.md §"Recommended Stack" — version verification source-of-truth
    - CLAUDE.md §"Technology Stack" — project-level constraints
  </read_first>
  <action>
    Create `pyproject.toml` with Python 3.11+ target and the locked dependency block from 01-RESEARCH.md §Installation (per supply-chain threat T-1-05): langgraph>=1.2,<2; llama-index>=0.14,<0.15; llama-index-vector-stores-qdrant; llama-index-embeddings-huggingface; llama-index-llms-google-genai; llama-index-llms-groq; qdrant-client>=1.18,<2; instructor>=1.15,<2; pydantic>=2.7,<3; docling>=2.95,<3; pdfplumber>=0.11; pymupdf>=1.27; sentence-transformers>=3; FlagEmbedding; streamlit>=1.36; langfuse; tenacity; python-dotenv; httpx>=0.27; typer; rich; tiktoken; rapidfuzz (for cite-check fuzzy matching per 01-RESEARCH.md Pattern 3). Dev group: pytest>=8; pytest-timeout; ruff; mypy.

    Add `[tool.pytest.ini_options]` section: `testpaths = ["tests"]`, `addopts = "-ra --strict-markers"`, `markers = ["slow", "eval", "integration"]`, `timeout = 60`.

    Add `[tool.ruff]`: `line-length = 100`, `target-version = "py311"`, `select = ["E","F","I","W","UP","B"]`.

    Create `requirements.txt` mirroring the pyproject runtime deps (HF Spaces reads `requirements.txt` directly — keep them in sync; this is a Phase 5 deploy requirement).

    Create `.gitignore` covering: `.venv/`, `.env`, `__pycache__/`, `*.pyc`, `*.pyo`, `.pytest_cache/`, `.ruff_cache/`, `mlruns/`, `~/.cache/docling/`, `.DS_Store`, `*.egg-info/`, `dist/`, `build/`. Do NOT gitignore `data/swiggy_drhp/*.pdf` (the binary is committed per A3 in 01-RESEARCH.md Open Questions).

    Create empty `__init__.py` in each package directory per SKELETON.md §G: `agent/`, `agent/nodes/`, `pipelines/`, `tools/`, `storage/`, `ui/`, `compliance/`, `observability/`. Create `static/.gitkeep` and `pages/.gitkeep` so empty directories track. NO logic in any `__init__.py` — just empty files (per D-09 explicit-imports posture; no implicit star-imports).

    Per project-skill discovery: NO `.claude/skills/` exists yet; do not assume any project-skill patterns. Follow STACK.md conventions verbatim.
  </action>
  <verify>
    <automated>python -c "import tomllib; tomllib.loads(open('pyproject.toml','rb').read().decode())" &amp;&amp; test -f requirements.txt &amp;&amp; test -f .gitignore &amp;&amp; test -f agent/__init__.py &amp;&amp; test -f agent/nodes/__init__.py &amp;&amp; test -f pipelines/__init__.py &amp;&amp; test -f tools/__init__.py &amp;&amp; test -f storage/__init__.py &amp;&amp; test -f ui/__init__.py &amp;&amp; test -f compliance/__init__.py &amp;&amp; test -f observability/__init__.py &amp;&amp; grep -q 'langgraph' pyproject.toml &amp;&amp; grep -q 'streamlit' requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` parses as valid TOML
    - `pyproject.toml` contains substring `langgraph>=1.2,<2` (D-09: exact version pin)
    - `pyproject.toml` contains substring `qdrant-client>=1.18,<2`
    - `pyproject.toml` contains substring `pydantic>=2.7,<3`
    - `pyproject.toml` contains substring `rapidfuzz` (cite-check dependency per 01-RESEARCH.md Pattern 3)
    - `pyproject.toml` `[tool.pytest.ini_options]` block contains `testpaths = ["tests"]`
    - `requirements.txt` contains `streamlit>=1.36` (HF Spaces deploy requirement)
    - `.gitignore` contains `.env` AND `__pycache__/` AND `.venv/` (secret-leak threat T-1-04 mitigation)
    - `.gitignore` does NOT contain `data/` or `*.pdf` (DRHP binary must be committed)
    - Empty `__init__.py` files exist in: `agent/`, `agent/nodes/`, `pipelines/`, `tools/`, `storage/`, `ui/`, `compliance/`, `observability/`
    - `static/.gitkeep` and `pages/.gitkeep` exist
  </acceptance_criteria>
  <done>Locked-stack dependencies declared with version floors and ceilings; package skeleton ready for Waves 1-5 to add files inside without restructuring; secrets-and-cache files are gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the Swiggy DRHP PDF + SHA-256 pin and write the integrity unit test</name>
  <files>data/swiggy_drhp/swiggy_prospectus_2024_11.pdf, data/swiggy_drhp/SHA256SUMS, tests/unit/test_drhp_integrity.py</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Sources" — SEBI URL for the Swiggy Nov 2024 Prospectus (the canonical filed document)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md Open Question 3 — "commit binary directly" decision (under 20 MB, well within GitHub's 100 MB file limit)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Security Domain" "Malicious PDF in repo" — SHA-256 integrity check is the supply-chain control (T-1-05)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md Per-Task Verification Map row `1-01-W0-pyproject` and INGEST-01 row in §"Phase Requirements → Test Map"
  </read_first>
  <action>
    Download the Swiggy Nov 2024 Prospectus PDF from the SEBI canonical URL (https://www.sebi.gov.in/filings/public-issues/nov-2024/swiggy-limited-prospectus_88320.html — the Prospectus link, NOT the DRHP or RHP, since per D-08 Claude's Discretion the Phase 1 IPO is Swiggy and the post-listing Prospectus is the most-final version with the issue size locked). Save to `data/swiggy_drhp/swiggy_prospectus_2024_11.pdf`. Verify file size is between 5 MB and 25 MB (typical DRHP range per 01-RESEARCH.md A12).

    Compute SHA-256: `sha256sum data/swiggy_drhp/swiggy_prospectus_2024_11.pdf > data/swiggy_drhp/SHA256SUMS`. The file must contain exactly one line: `<64-char-hex>  swiggy_prospectus_2024_11.pdf` (two spaces between hash and filename — the GNU `sha256sum` format that `sha256sum -c` consumes).

    Create `tests/unit/test_drhp_integrity.py`:
    - `test_drhp_pdf_exists`: asserts `pathlib.Path("data/swiggy_drhp/swiggy_prospectus_2024_11.pdf").exists()`.
    - `test_drhp_sha256_matches`: reads SHA256SUMS, parses the expected hash, computes the actual SHA-256 over the file bytes (`hashlib.sha256`), asserts they match. Mitigates threat T-1-05 (malicious PDF / supply-chain tampering).
    - `test_drhp_size_in_range`: asserts `5_000_000 < file_size < 25_000_000` — defends against accidental empty-file commits or someone replacing the PDF with a stub.

    If the SEBI URL has 404'd at execution time, the executor MUST surface the failure and try the BSE/NSE mirror or the fallback IPO candidates (Hyundai Motor India / Ola Electric per D-08) — do NOT skip this task. The DRHP binary is a hard prerequisite for Wave 2 ingestion.

    Per the source-audit: this task implements REQ INGEST-01 (the binary acquisition leg).
  </action>
  <verify>
    <automated>test -f data/swiggy_drhp/swiggy_prospectus_2024_11.pdf &amp;&amp; test -f data/swiggy_drhp/SHA256SUMS &amp;&amp; (cd data/swiggy_drhp &amp;&amp; sha256sum -c SHA256SUMS) &amp;&amp; pytest tests/unit/test_drhp_integrity.py -x -q --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - `data/swiggy_drhp/swiggy_prospectus_2024_11.pdf` exists and file size is between 5 MB and 25 MB
    - `data/swiggy_drhp/SHA256SUMS` exists and contains exactly one line matching pattern `^[0-9a-f]{64}  swiggy_prospectus_2024_11\.pdf$`
    - `sha256sum -c data/swiggy_drhp/SHA256SUMS` exits 0 (run from inside `data/swiggy_drhp/` directory)
    - `pytest tests/unit/test_drhp_integrity.py -x -q --timeout=10` exits 0
    - Three test functions exist: `test_drhp_pdf_exists`, `test_drhp_sha256_matches`, `test_drhp_size_in_range`
    - Implements REQ INGEST-01 (binary acquisition + integrity pin)
  </acceptance_criteria>
  <done>The Swiggy DRHP binary is reproducibly committed with a cryptographic integrity pin; any future repo clone or CI run validates the PDF against the same hash; INGEST-01 binary leg is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Stub every test file from 01-VALIDATION.md Per-Task Verification Map + gold-set schema + shared fixtures</name>
  <files>tests/__init__.py, tests/conftest.py, tests/unit/__init__.py, tests/unit/test_schemas.py, tests/unit/test_scrubber.py, tests/unit/test_disclaimer_surface.py, tests/unit/test_parser.py, tests/unit/test_chunker.py, tests/unit/test_embedder.py, tests/unit/test_retrieve.py, tests/unit/test_gate1.py, tests/unit/test_cite_check.py, tests/unit/test_decompose.py, tests/unit/test_citation_renderer.py, tests/unit/test_copy_no_banned_tokens.py, tests/integration/__init__.py, tests/integration/test_qdrant_ingest.py, tests/integration/test_agent_e2e.py, tests/integration/test_drhp_prompt_injection.py, tests/integration/test_langfuse_trace.py, tests/eval/__init__.py, tests/eval/gold/swiggy_phase1_gold.jsonl, tests/eval/test_numeric_faithfulness.py, pipelines/ingest_swiggy.py</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md §"Per-Task Verification Map" — one stub test row per row (this is the Nyquist-compliance gate; missing any row breaks the sampling discipline)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Validation Architecture" §"Wave 0 Gaps" — the master list of test files the planner must stub
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Validation Architecture" §"Phase Requirements → Test Map" — every phase REQ ID mapped to a test command
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md §"Skeleton-Ship Checklist" — the end-of-Wave-3 invariants that integration tests must eventually assert
  </read_first>
  <action>
    Create `tests/conftest.py` with shared fixtures (skeleton-level — bodies fill in their respective waves, but the fixture NAMES + SIGNATURES are locked here so later tests import them without renaming):
    - `fixture_synthetic_drhp_path` — path to a tiny 5-page synthetic DRHP under `tests/fixtures/synthetic_drhp.pdf` (the file itself is created in Wave 2 ingestion task; the fixture just returns the path).
    - `mock_qdrant_client` — pytest fixture returning a `QdrantClient(":memory:")` instance per qdrant-client docs (in-memory mode for testing); collection setup happens per-test.
    - `mock_llm` — pytest fixture returning a Mock object whose `chat.completions.create` returns a canned `GroundedAnswer`-shaped JSON. Body filled in Wave 3.
    - `gold_set` — pytest fixture loading `tests/eval/gold/swiggy_phase1_gold.jsonl` line-by-line as a list of dicts. Body filled in Wave 5.
    Each fixture body for Wave 0 is `pytest.skip(reason="filled in Wave N")` with N noted in the docstring — this makes Nyquist sampling visible: red XFAIL/SKIP is acceptable; collection errors are not.

    Create `tests/__init__.py`, `tests/unit/__init__.py`, `tests/integration/__init__.py`, `tests/eval/__init__.py` as empty files.

    Create one stub file per row in 01-VALIDATION.md Per-Task Verification Map. Each stub MUST:
    - Import the target module (using `pytest.importorskip` if the module doesn't yet exist — this keeps collection green while the implementation file is pending; once the module ships in its wave, `pytest.importorskip` becomes a no-op).
    - Contain ONE function per VALIDATION.md row's "Secure Behavior" description, decorated with `@pytest.mark.xfail(reason="Wave N owns this", strict=False)` where N is the wave that ships the test body.
    - Function body: a single `assert False, "Wave N must implement"` line — this makes the test red on the dot without breaking pytest collection.

    File-by-file requirements (one xfail test per file; signatures are the Phase 3 METHOD-01 contract — keep them stable):

    | File | xfail test | Wave |
    |------|------------|------|
    | `tests/unit/test_schemas.py` | `test_claim_id_pattern_enforced` | 1 |
    | `tests/unit/test_scrubber.py` | `test_every_banned_token_conjugation_blocked` | 1 |
    | `tests/unit/test_disclaimer_surface.py` | `test_three_surfaces_render_anchor_copy` | 1 |
    | `tests/unit/test_parser.py` | `test_docling_parse_emits_sections_with_page_anchors` | 2 |
    | `tests/unit/test_chunker.py` | `test_section_aware_chunks_preserve_page_anchor` | 2 |
    | `tests/unit/test_embedder.py` | `test_bge_m3_returns_1024_dim_normalized` | 2 |
    | `tests/unit/test_retrieve.py` | `test_retrieve_returns_topk_with_scores` | 3 |
    | `tests/unit/test_gate1.py` | `test_below_threshold_routes_to_refusal` | 3 |
    | `tests/unit/test_cite_check.py` | `test_unsupported_claim_rejected` | 3 |
    | `tests/unit/test_decompose.py` | `test_multipart_question_splits_into_subquestions` | 3 |
    | `tests/unit/test_citation_renderer.py` | `test_renderer_emits_sup_chip_html_and_escapes_xss` | 4 |
    | `tests/unit/test_copy_no_banned_tokens.py` | `test_every_copy_string_passes_scrubber` | 4 |
    | `tests/integration/test_qdrant_ingest.py` | `test_swiggy_ingest_upserts_to_qdrant` | 2 |
    | `tests/integration/test_agent_e2e.py` | `test_grounded_question_returns_cited_answer` | 3 |
    | `tests/integration/test_drhp_prompt_injection.py` | `test_drhp_advisory_language_does_not_leak` (T-1-02 mitigation) | 3 |
    | `tests/integration/test_langfuse_trace.py` | `test_every_node_writes_a_span_with_claim_ids` | 5 |
    | `tests/eval/test_numeric_faithfulness.py` | `test_gold_set_numeric_faithfulness_baseline` | 5 |

    Create `tests/eval/gold/swiggy_phase1_gold.jsonl` with a 10-line stub (one JSON object per line). The schema is fixed at Wave 0 even though the content is Wave 5's responsibility — the schema is the Phase 3 METHOD-01 consumer contract:
    ```
    {"qid": "swiggy-001", "category": "factual", "question": "TBD-WAVE5", "expected_answer_contains": [], "expected_sources": [], "is_refusal_expected": false}
    {"qid": "swiggy-002", "category": "numeric", "question": "TBD-WAVE5", "expected_answer_contains": [], "expected_sources": [], "is_refusal_expected": false}
    ```
    10 stub lines total: 5 factual (qids 001-005), 3 numeric (006-008), 3 risk-factor (TBD — repurpose 009-011 if needed; total cap at 13 per 01-VALIDATION.md Wave 0 line "10-15 hand-curated entries"), 2 refusal-eligible (with `is_refusal_expected: true`). The `qid` IDs are the stable foreign keys Phase 3 METHOD-01 will join against.

    Create `pipelines/ingest_swiggy.py` as a stub: a `__main__` block that prints "Wave 2 owns this implementation" and exits 0. This file is the Wave 2 entry point — placing the stub here lets Wave 0's `python -c "import pipelines.ingest_swiggy"` collection check pass.

    Per the source-audit: this task closes the Nyquist-compliance requirement from 01-VALIDATION.md (`nyquist_compliant: false` → `true` once stubs ship).
  </action>
  <verify>
    <automated>pytest tests/ -x -q --collect-only --timeout=10 2>&amp;1 | tee /tmp/collect.txt &amp;&amp; (! grep -E 'ERRORS|error collecting' /tmp/collect.txt) &amp;&amp; pytest tests/ -q --timeout=10 -ra 2>&amp;1 | tee /tmp/run.txt &amp;&amp; grep -E '(xfailed|skipped|passed)' /tmp/run.txt &amp;&amp; (! grep -E '^FAILED ' /tmp/run.txt) &amp;&amp; test $(grep -E '^xfail ' /tmp/run.txt | wc -l) -ge 0 &amp;&amp; test -f tests/eval/gold/swiggy_phase1_gold.jsonl &amp;&amp; test $(wc -l &lt; tests/eval/gold/swiggy_phase1_gold.jsonl) -ge 10 &amp;&amp; python -c "import json, pathlib; [json.loads(l) for l in pathlib.Path('tests/eval/gold/swiggy_phase1_gold.jsonl').read_text().splitlines() if l.strip()]" &amp;&amp; python -c "import pipelines.ingest_swiggy"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -x -q --collect-only --timeout=10` exits 0 with no collection errors (greps for "ERRORS|error collecting" return nothing)
    - `pytest tests/ -q --timeout=10` exits 0 (xfail and skip are acceptable; FAILED is not)
    - All 17 stub test files declared above exist
    - Every stub test is decorated with `@pytest.mark.xfail(strict=False)` with a `Wave N` reason
    - `tests/conftest.py` defines fixtures named exactly: `fixture_synthetic_drhp_path`, `mock_qdrant_client`, `mock_llm`, `gold_set` (signature stability matters — later waves import these names)
    - `tests/eval/gold/swiggy_phase1_gold.jsonl` has ≥ 10 lines, each parseable as JSON, each containing keys `qid`, `category`, `question`, `expected_answer_contains`, `expected_sources`, `is_refusal_expected`
    - `python -c "import pipelines.ingest_swiggy"` exits 0
    - Every row in 01-VALIDATION.md Per-Task Verification Map has a corresponding stub test file (verified by visual cross-check)
  </acceptance_criteria>
  <done>Nyquist sampling discipline is enforceable from Wave 1 onward — every later task has a red stub waiting to be made green; gold-set schema is locked at Wave 0 so Phase 3 METHOD-01 has a stable join key; 01-VALIDATION.md `nyquist_compliant: false → true` once the executor flips the frontmatter at task close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Phase 1, applicable here)

| Boundary | Description |
|----------|-------------|
| developer workstation → repo (this wave) | All file writes happen on the developer's machine; no external attack surface |
| repo → CI / future executor (after commit) | A future executor reading these files trusts their integrity — SHA-256 pin on the DRHP binary is the gate |
| repo → PyPI (install time) | `uv pip install` reaches out to PyPI; supply-chain threat T-1-05 applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering | `pyproject.toml` / `requirements.txt` dependency installs | mitigate | All packages in 01-RESEARCH.md §"Package Legitimacy Audit" disposition `Approved` — no `[ASSUMED]` or `[SUS]` packages installed; version floors AND ceilings pin against silent yanks (`langgraph>=1.2,<2`); future Wave 0 task may add `pip-audit` to CI |
| T-1-05-PDF | Tampering | `data/swiggy_drhp/swiggy_prospectus_2024_11.pdf` | mitigate | SHA-256 pin in `SHA256SUMS` + `test_drhp_sha256_matches` unit test fails CI on drift; PDF sourced from SEBI's authoritative URL |
| T-1-04 | Information Disclosure | `.gitignore` — secret leakage via committed `.env` | mitigate | `.gitignore` blocks `.env` from day one; no Wave 0 task touches any secret material; HF Spaces secrets UI is the only path for keys |
| T-1-07 | Tampering | Test fixtures (mock_llm, mock_qdrant_client) | accept | Wave 0 stubs only; bodies in later waves will be plain-Python doubles, no network calls. No exploit surface in Wave 0. |
| T-1-SC | Tampering | npm/pip/cargo installs | mitigate | No `[ASSUMED]`/`[SUS]` packages per RESEARCH.md Package Legitimacy Audit — every install is approved; supply-chain checkpoint not required (planner-discretion per 01-RESEARCH.md disposition note) |

Phase 1 has no user authentication, no PII, no user uploads (E5 deferred per CONTEXT). Threats T-1-01 (prompt injection via user question), T-1-02 (prompt injection via DRHP content), T-1-03 (rate-limit abuse), T-1-06 (XSS via `unsafe_allow_html`) are deferred to the waves that introduce those surfaces (Wave 3 for T-1-01/02; Wave 4 for T-1-06; Wave 5 for T-1-03).
</threat_model>

<verification>
- All three tasks' automated commands pass: TOML parse + integrity test + collection check
- `pytest tests/ -x -q --collect-only --timeout=10` exits 0 with zero collection errors
- `sha256sum -c data/swiggy_drhp/SHA256SUMS` exits 0
- `grep -c langgraph pyproject.toml` returns ≥ 1 (filter not needed — `pyproject.toml` has no comment-line `#` prefixes by convention; if a comment is added later, switch to `grep -v '^#' pyproject.toml | grep -c langgraph`)
- `python -c "import pipelines.ingest_swiggy, agent, tools, storage, ui, compliance, observability"` exits 0
- 01-VALIDATION.md frontmatter `nyquist_compliant` field can be flipped from `false` → `true` (manual edit by executor after stubs verified)
</verification>

<success_criteria>
- Repository scaffolding ready for Wave 1 to start writing schemas without restructuring
- INGEST-01 binary leg closed (SHA-pinned DRHP committed)
- Every row in 01-VALIDATION.md Per-Task Verification Map has a stub test file ready for its wave to fill
- `pytest tests/ -q --timeout=10` runs in < 10 seconds and exits 0
- Locked-stack dependencies declared with both floor and ceiling versions (supply-chain T-1-05 mitigation)
- Pages and static directory placeholders exist for Waves 4-5 to populate
</success_criteria>

<output>
Create `.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-01-SUMMARY.md` when done. Summary must include:
- Pinned versions actually committed (cross-reference with RESEARCH.md §Standard Stack)
- Actual SHA-256 of the committed Swiggy PDF
- Stub-test count + xfail count after first `pytest tests/ -q` run
- Confirmation that `nyquist_compliant` flag in 01-VALIDATION.md is ready to flip to `true`
</output>
