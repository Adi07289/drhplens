---
phase: 01-foundation-mvp-a-cited-q-a-on-one-ipo
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - app.py
  - pages/01_methodology.py
  - ui/chip.py
  - ui/expander.py
  - ui/disclaimer.py
  - ui/refusal_banner.py
  - ui/state.py
  - app/__init__.py
  - app/static/drhplens.css
  - app/util/__init__.py
  - app/util/css_loader.py
  - tests/unit/test_citation_renderer.py
  - tests/unit/test_disclaimer_surface_render.py
  - tests/unit/test_refusal_banner.py
  - tests/unit/test_session_state.py
  - tests/unit/test_copy_no_banned_tokens.py
  - tests/manual/CITATION_INTERACTION.md
  - scripts/smoke.sh
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "`streamlit run app.py` boots cleanly within 10s on a fresh venv with locked deps; `curl -sf http://localhost:8501/` returns 200 with the hero copy `Ask DRHPLens about Swiggy.` in the rendered HTML (UI-01)"
    - "`curl -sf http://localhost:8501/methodology` returns 200 with the heading `Methodology` and the GitHub link present in the rendered HTML (UI-SPEC L-7 stub link must not 404)"
    - "Citation chip renderer escapes XSS-attempt inputs (`<script>alert(1)</script>` in answer prose) and deduplicates per-cluster (D-03); `claim_id` round-trips from `GroundedAnswer` → chip `data-claim-id` attribute → expander source span without drift (UI-02, PITFALL P5)"
    - "All three disclaimer surfaces render anchor copy from a single source (`compliance/disclaimer_text.py`): modal once per session via `st.dialog`, persistent footer sticky on mobile via CSS, per-answer footer below every grounded answer (TRUST-01, TRUST-03 — completes the Streamlit-rendering side begun in 01-02)"
    - "Refusal banner renders amber-not-red (`#FEF3C7` background, `#D97706` left border, `#78350F` text) with the refusal copy + up to 2 reformulation chips sourced from `RefusalResponse.reformulation_suggestions` (RAG-03 visual surface)"
    - "Mobile-responsive at 375 / 640 / 1024 breakpoints: hero stacks, DRHP metadata header wraps, citation chips remain 44×44px tap-target via transparent padding (UI-01); CSS rules unit-tested for breakpoint presence"
    - "Every user-visible string rendered by Wave 4 (citation expander labels, chip aria-labels, methodology stub body, suggestion chip captions) passes the banned-token scrubber (TRUST-02 extension to UI surface)"
    - "Wave 4 polish does NOT regress the Walking Skeleton: `python -m agent.demo` still works; Wave 3 unit + integration tests remain green"
  artifacts:
    - path: "app.py"
      provides: "Streamlit entry point — hero (expanded/collapsed), DRHP metadata header, chat history, chat_input, empty state, persistent footer; wires the Wave 3 LangGraph agent into the chat flow"
      min_lines: 120
    - path: "pages/01_methodology.py"
      provides: "/methodology stub page (UI-SPEC L-7 — must not 404); Phase 6 LAND-01 replaces"
      min_lines: 25
    - path: "ui/chip.py"
      provides: "Citation chip HTML renderer — XSS-escape, dedupe cluster, per-answer chip numbering, claim_id round-trip"
      exports: ["render_answer_with_chips", "build_chip_html"]
      min_lines: 60
    - path: "ui/expander.py"
      provides: "Citation source-snippet inline expander wrapper — verbatim DRHP text span + SEBI/BSE/NSE PDF link with #page anchor"
      exports: ["render_citation_expanders"]
    - path: "ui/disclaimer.py"
      provides: "Adds Streamlit-rendering functions render_first_use_modal/render_persistent_footer/render_per_answer_footer on top of Wave 1 DisclaimerSurface (preserves all Wave 1 exports — no rename)"
      exports: ["DisclaimerSurface", "render_disclaimer_gate", "render_first_use_modal", "render_persistent_footer", "render_per_answer_footer"]
    - path: "ui/refusal_banner.py"
      provides: "Refusal banner renderer — consumes RefusalResponse; renders amber surface + heading + body + up to 2 reformulation chips"
      exports: ["render_refusal_banner"]
    - path: "ui/state.py"
      provides: "st.session_state helpers — has_seen_modal, mark_modal_seen, append_to_chat_history, reset_chat_history, get_chat_history"
      exports: ["has_seen_modal", "mark_modal_seen", "append_to_chat_history", "reset_chat_history", "get_chat_history", "init_session_state"]
    - path: "app/static/drhplens.css"
      provides: "Single global CSS file (UI-SPEC FLAG: no scattered CSS) — chip, refusal banner, disclaimer surfaces, focus rings, mobile media queries at 375/640/1024px, prefers-reduced-motion"
      min_lines: 120
    - path: "app/util/css_loader.py"
      provides: "Single-load CSS injector — idempotent per st.session_state key (UI-SPEC FLAG-2)"
      exports: ["load_global_css"]
    - path: "scripts/smoke.sh"
      provides: "Boot Streamlit, wait, curl home + /methodology, assert hero + heading copy present in rendered HTML; teardown"
  key_links:
    - from: "app.py"
      to: "agent/graph.py"
      via: "from agent.graph import build_graph; result = build_graph().invoke({...})"
      pattern: "from agent.graph import"
    - from: "app.py"
      to: "ui/chip.py"
      via: "from ui.chip import render_answer_with_chips"
      pattern: "from ui.chip import"
    - from: "app.py"
      to: "ui/disclaimer.py"
      via: "render_disclaimer_gate(st.session_state) + render_persistent_footer()"
      pattern: "from ui.disclaimer import"
    - from: "app.py"
      to: "app/util/css_loader.py"
      via: "load_global_css() called once at top of app.py"
      pattern: "from app.util.css_loader import load_global_css"
    - from: "ui/chip.py"
      to: "agent/schemas.py GroundedAnswer"
      via: "input parameter type annotation"
      pattern: "from agent.schemas import GroundedAnswer"
    - from: "ui/refusal_banner.py"
      to: "agent/schemas.py RefusalResponse"
      via: "input parameter type annotation"
      pattern: "from agent.schemas import RefusalResponse"
    - from: "ui/disclaimer.py (Wave 4 additions)"
      to: "compliance/disclaimer_text.py (Wave 1)"
      via: "import ANCHOR_COPY, MODAL_HEADING, MODAL_BODY_ADDENDUM, MODAL_CTA, PER_ANSWER_FOOTER"
      pattern: "from compliance.disclaimer_text import"
---

<objective>
Wrap the Walking Skeleton (Wave 3 LangGraph agent) in a Streamlit UI that honors `01-UI-SPEC.md` verbatim: 4-size type scale, slate-indigo accent, 60/30/10 split, citation chip CSS contract, three disclaimer surfaces, refusal banner amber-not-red, mobile-responsive at 375/640/1024px, WCAG 2.1 AAA. This plan closes UI-01 (mobile-responsive web) and UI-02 (citation chips clickable) — the two Phase 1 REQs that land in Wave 4 — and completes the Streamlit-rendering side of TRUST-01/TRUST-03 begun in Plan 01-02.

Purpose: The cited-Q&A surface is the **publicly visible artifact** of Phase 1 — every recruiter who lands on the HF Spaces URL (and every retail investor who arrives via the methodology deep-link) sees what this plan ships. Wave 4 is the gate between "it works on the CLI" (Walking Skeleton) and "it's a portfolio piece." Every UI-SPEC anti-pattern (`st.warning` for refusals; scattered CSS; missing aria-describedby; banned tokens in our own copy) is structurally prevented by tests in this plan.

The plan must NOT introduce new architectural decisions. Schemas, agent topology, scrubber semantics, disclaimer copy — all locked in Waves 1-3. Wave 4 is presentation only. If a presentation choice contradicts an earlier wave's contract, the earlier wave wins and the executor flags a `## FLAG` for the Wave 5 planner.

Output:
- `app.py` — Streamlit entry; hero (expanded/collapsed states per UI-SPEC IA section 1/1a); DRHP metadata header (Surface-secondary `#F4F5F7`); chat history (`st.chat_message`); empty state; `st.chat_input` sticky-bottom; persistent footer; wires `agent/graph.py` via `build_graph().invoke(...)`.
- `pages/01_methodology.py` — Display 28px heading, body paragraph, GitHub README link, back-to-home link.
- `ui/chip.py`, `ui/expander.py` — citation chip + source-snippet expander; renderer is the contract Phase 3 METHOD-01 will surface alongside.
- `ui/disclaimer.py` — **adds** Streamlit-rendering functions on top of Wave 1 `DisclaimerSurface` (preserves Wave 1 exports — no rename).
- `ui/refusal_banner.py` — amber banner + reformulation chips per UI-SPEC Visuals.
- `ui/state.py` — `st.session_state` helpers (modal-seen flag, chat history).
- `app/static/drhplens.css` + `app/util/css_loader.py` — single global CSS, single-load injector (UI-SPEC FLAG-2: no scattered CSS).
- 4 new unit-test files + extended `tests/unit/test_copy_no_banned_tokens.py` to cover Wave 4 strings.
- `tests/manual/CITATION_INTERACTION.md` — manual test script for the inline-expand interaction (covered as manual-only in `01-VALIDATION.md`).
- `scripts/smoke.sh` — boots app + curls both routes + verifies first-meaningful-paint copy.

This plan flips two Wave 0 xfail stubs to GREEN: `1-04-citation-chip-html` (`tests/unit/test_citation_renderer.py::test_renderer_emits_sup_chip_html_and_escapes_xss`) and the manual half of `1-04-streamlit-app-smoke` (covered via `scripts/smoke.sh`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-02-PLAN.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-02-SUMMARY.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-04-PLAN.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-04-SUMMARY.md
@.planning/research/STACK.md
@agent/schemas.py
@agent/state.py
@agent/graph.py
@compliance/disclaimer_text.py
@compliance/scrubber.py
@ui/copy.py
@ui/disclaimer.py
@tests/unit/test_citation_renderer.py
@tests/unit/test_copy_no_banned_tokens.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Global CSS contract + single-load injector + mobile media queries</name>
  <files>app/__init__.py, app/util/__init__.py, app/util/css_loader.py, app/static/drhplens.css, tests/unit/test_css_loader.py</files>
  <behavior>
    - `test_css_file_exists_and_nonempty`: `app/static/drhplens.css` exists and `len(content) > 2000` chars (the four sub-contracts — chip, refusal, disclaimer, mobile media queries — cannot fit in less).
    - `test_css_contains_citation_chip_selector`: substring `.drhp-cite` present with `background: #1E40AF` AND `font-size: 0.7em` AND `vertical-align: super` (UI-SPEC Visuals Citation Chip Contract).
    - `test_css_contains_focus_ring_2px_offset`: substring matching pattern `outline:\s*2px\s+solid\s+#1E40AF` AND `outline-offset:\s*2px` (UI-SPEC Accessibility Contract — focus indicators).
    - `test_css_contains_refusal_banner_amber_not_red`: substring `background:\s*#FEF3C7` AND `border-left:\s*4px\s+solid\s+#D97706`; explicit assertion that `#B91C1C` (the reserved destructive hex) does NOT appear anywhere in the CSS (UI-SPEC Color §"no green/red coding").
    - `test_css_contains_three_breakpoints`: substring `@media (max-width: 374px)` AND `@media (min-width: 375px) and (max-width: 639px)` AND `@media (min-width: 640px) and (max-width: 1023px)` AND `@media (min-width: 1024px)` (UI-SPEC Mobile Responsive Contract).
    - `test_css_contains_prefers_reduced_motion`: substring `@media (prefers-reduced-motion: reduce)` present (UI-SPEC Accessibility §Reduced motion).
    - `test_css_hides_default_streamlit_footer`: substring `[data-testid="stFooter"]` with `display: none` (UI-SPEC Streamlit-Specific Constraints — default footer suppression).
    - `test_css_contains_44px_touch_target`: substring `min-width:\s*44px` AND `min-height:\s*44px` for the chip pseudo-element pattern (UI-SPEC Spacing §Touch targets — Apple HIG / WCAG 2.5.5 AAA).
    - `test_load_global_css_is_idempotent`: calls `load_global_css(session_state=fake_dict)` twice with a shared fake dict; first call sets `_drhp_css_loaded=True`, second call detects the flag and skips re-injection (UI-SPEC FLAG-2: no scattered CSS; defensive against st.rerun()-driven duplicate injection that bloats DOM).
    - `test_load_global_css_returns_html_with_style_tag`: first call returns a string starting with `<style>` and ending with `</style>` (the Streamlit `st.markdown(..., unsafe_allow_html=True)` consumer contract).
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Color" — the 60/30/10 split + exact hex values (locked, not editable)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Typography" — 4 sizes (12/16/20/28), 2 weights (400/600), citation chip 0.7em derived
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Spacing Scale" — 7-token grid (xs 4, sm 8, md 16, lg 24, xl 32, 2xl 48, 3xl 64) + 44×44px touch-target exception
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Citation Chip Contract" — visual properties (size, shape, position, states)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Refusal Banner Contract" — amber background + left border + text color (the "no red" invariant)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Mobile Responsive Contract" — exact breakpoint widths
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Accessibility Contract" — 2px focus ring at 2px offset; prefers-reduced-motion
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Streamlit-Specific Constraints" — default footer suppression
  </read_first>
  <action>
    Create `app/__init__.py` and `app/util/__init__.py` as empty files (so `from app.util.css_loader import load_global_css` works under Python 3.11 import semantics).

    Implement `app/static/drhplens.css` as ONE file containing all global styles. Required selectors and rule blocks (every value below is from UI-SPEC verbatim — do not paraphrase hex codes or px sizes):

    - **CSS custom properties block** at `:root` — declare every UI-SPEC color hex and every spacing token as a custom property (`--drhp-accent: #1E40AF`, `--drhp-text-primary: #0F172A`, `--drhp-text-muted: #475569`, `--drhp-border-subtle: #E2E8F0`, `--drhp-surface-secondary: #F4F5F7`, `--drhp-refusal-bg: #FEF3C7`, `--drhp-refusal-border: #D97706`, `--drhp-refusal-text: #78350F`, `--drhp-space-xs: 4px`, ... through `--drhp-space-3xl: 64px`). This makes every later rule reference the locked values by name, so a future change is a single-line edit, not a search-and-replace.

    - **`.drhp-cite`** — the citation chip: `font-size: 0.7em; vertical-align: super; font-weight: 600; color: #FFFFFF; background: #1E40AF; padding: 1px 6px; border-radius: 8px; cursor: pointer; line-height: 1.0; white-space: nowrap; position: relative;`. The `::before` pseudo-element provides the 44×44px transparent tap target: `content: ""; position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); min-width: 44px; min-height: 44px;` (Apple HIG / WCAG 2.5.5 AAA per UI-SPEC Spacing §Touch targets).

    - **`.drhp-cite:hover`** — `background: #1E3A8A;` AND `text-decoration: underline;` on the inner number (UI-SPEC Visuals §Citation Chip §States).

    - **`.drhp-cite:focus-visible`** — `outline: 2px solid #1E40AF; outline-offset: 2px;` (UI-SPEC Accessibility §Focus indicators — never `outline: none` without a replacement).

    - **`.drhp-cite:active, .drhp-cite[aria-expanded="true"]`** — `background: #1E3A8A;` (the "expander open" state).

    - **`.drhp-refusal`** — refusal banner: `background: #FEF3C7; border-left: 4px solid #D97706; color: #78350F; padding: 24px 16px; border-radius: 8px; max-width: 720px;` (UI-SPEC Visuals §Refusal Banner Contract). NO red anywhere. Add a code comment: `/* No #B91C1C — refusal posture is honest, not punitive (UI-SPEC §Color) */`.

    - **`.drhp-refusal-heading`** — `font-size: 20px; font-weight: 600; line-height: 1.3; margin: 0 0 16px 0; color: #78350F;` (UI-SPEC Refusal Banner content layout 1).

    - **`.drhp-suggest`** — reformulation suggestion chip button: `background: #F4F5F7; color: #0F172A; border: 1px solid #E2E8F0; padding: 8px 16px; border-radius: 8px; font-size: 14px; font-weight: 600; min-height: 44px; cursor: pointer;` (UI-SPEC Refusal Banner content layout 3 + Mobile touch-target compliance).

    - **`.drhp-footer`** — persistent slim footer: `position: sticky; bottom: 0; background: #FFFFFF; border-top: 1px solid #E2E8F0; padding: 8px 16px; min-height: 56px; font-size: 12px; color: #475569; line-height: 1.5; z-index: 50;` (UI-SPEC Disclaimer Surfaces §2 — 56px mobile, 48px desktop via media query below).

    - **`.drhp-footer-link`** — `color: #1E40AF; text-decoration: underline;` (UI-SPEC Color §Accent reserved-for §3 methodology stub link).

    - **`.drhp-disclaimer-per-answer`** — `font-size: 12px; font-style: italic; color: #475569; border-top: 1px solid #E2E8F0; padding-top: 8px; margin-top: 16px;` (UI-SPEC Disclaimer Surfaces §3 — Small italic muted; 12px ≥ SEBI 10pt floor).

    - **`.drhp-metadata-header`** — Surface-secondary block: `background: #F4F5F7; padding: 24px; border-radius: 8px; margin-bottom: 24px;`. Inner `.drhp-metadata-label` `font-size: 16px; font-weight: 600; color: #475569;` and `.drhp-metadata-value` `font-size: 16px; font-weight: 600; color: #0F172A;` (UI-SPEC Information Architecture §2).

    - **`.drhp-hero-display`** — `font-size: 28px; font-weight: 600; line-height: 1.2; color: #0F172A; margin: 0 0 16px 0;` (UI-SPEC Typography §Display).

    - **`.drhp-hero-subheading`** — `font-size: 16px; font-weight: 400; line-height: 1.55; color: #0F172A; margin: 0 0 32px 0;` (UI-SPEC Typography §Body + Information Architecture §1 hero block expanded state spacing).

    - **`.drhp-hero-collapsed`** — `font-size: 16px; font-weight: 400; line-height: 1.55; color: #0F172A; margin: 0 0 16px 0;` (UI-SPEC IA §1a hero block collapsed state).

    - **`[data-testid="stChatInput"] textarea:focus`** — `outline: 2px solid #1E40AF; outline-offset: 2px;` (UI-SPEC IA §4 chat input on-focus accent ring).

    - **`[data-testid="stFooter"] { display: none; }`** — suppresses Streamlit's default "Made with Streamlit" footer per UI-SPEC Streamlit-Specific Constraints.

    - **Media queries** (the four breakpoints per UI-SPEC Mobile Responsive Contract — every breakpoint MUST be present even if the rule body is empty, so the test grep passes; populate with the real adjustments below):
      - `@media (max-width: 374px) { .drhp-metadata-header { ... stack vertically with rows } .drhp-modal { padding: var(--drhp-space-lg); } }` (Mobile S)
      - `@media (min-width: 375px) and (max-width: 639px) { .drhp-metadata-header { ... two-column grid } }` (Mobile L)
      - `@media (min-width: 640px) and (max-width: 1023px) { .drhp-content { padding: 0 32px; } .drhp-footer { min-height: 48px; } }` (Tablet)
      - `@media (min-width: 1024px) { .drhp-content { max-width: 720px; margin: 0 auto; padding: 0 32px; } .drhp-footer { min-height: 48px; position: relative; } }` (Desktop — footer goes static at desktop per UI-SPEC §Disclaimer Surfaces §2)

    - **Prefers-reduced-motion block** (UI-SPEC Accessibility §Reduced motion):
      `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; } }`

    Implement `app/util/css_loader.py`:
    - `def load_global_css(session_state) -> str | None:` — accepts a Streamlit `st.session_state` (or any dict-like with `__contains__` and `__setitem__`). If `"_drhp_css_loaded" in session_state` returns None (idempotency — protects against `st.rerun()`-driven double injection that would otherwise bloat the DOM and slow first-meaningful-paint on mobile per UI-SPEC FLAG-2 anti-pattern §"no scattered CSS"). Else reads `app/static/drhplens.css` (use `pathlib.Path(__file__).parent.parent / "static" / "drhplens.css"`), wraps in `<style>...</style>`, sets `session_state["_drhp_css_loaded"] = True`, returns the wrapped string. Caller (`app.py`) is responsible for `st.markdown(html, unsafe_allow_html=True)` if a non-None value comes back.
    - Add module docstring documenting the UI-SPEC FLAG-2 contract: "Single point of CSS injection. Any code that needs custom styles MUST add a rule to `app/static/drhplens.css` and reference it via class; NEVER inject inline `<style>` tags from other modules."

    Create `tests/unit/test_css_loader.py` with the ~10 tests listed in `<behavior>`. CSS substring checks use `pathlib.Path("app/static/drhplens.css").read_text()` and plain `in` operator (or `re.search` for the patterns with whitespace tolerance). Idempotency test uses a plain dict as the session_state stand-in.

    Per the source-audit: this task closes the prerequisite for UI-01 (mobile-responsive contract realized in CSS) and is a structural precondition for UI-02 (citation chip CSS contract). Tasks 2-6 build on top of this CSS.
  </action>
  <threat_model>
    T-1-06 (Cross-Site Scripting via unsafe_allow_html): The CSS file itself is static content — no user input flows into it. The injector reads from disk and wraps in `<style>` tags — there is no string-interpolation vector. The XSS surface is in `ui/chip.py` (Task 2) where user-question text and DRHP-snippet text flow into rendered HTML; that task handles escape semantics.
  </threat_model>
  <verify>
    <automated>pytest tests/unit/test_css_loader.py -x -q --timeout=10 -v 2>&amp;1 | tee /tmp/css_test.txt &amp;&amp; (! grep -E '^FAILED ' /tmp/css_test.txt) &amp;&amp; test $(wc -c &lt; app/static/drhplens.css) -gt 2000 &amp;&amp; python -c "from app.util.css_loader import load_global_css; ss = {}; r1 = load_global_css(ss); assert r1 is not None and r1.startswith('&lt;style&gt;') and r1.endswith('&lt;/style&gt;'); r2 = load_global_css(ss); assert r2 is None; print('OK')" &amp;&amp; (! grep -E '#B91C1C' app/static/drhplens.css) &amp;&amp; grep -q '#FEF3C7' app/static/drhplens.css &amp;&amp; grep -q '#1E40AF' app/static/drhplens.css &amp;&amp; grep -q 'prefers-reduced-motion' app/static/drhplens.css</automated>
  </verify>
  <acceptance_criteria>
    - `app/static/drhplens.css` exists; file size > 2000 bytes
    - File contains exact substring `.drhp-cite` with background `#1E40AF` and `font-size: 0.7em` and `vertical-align: super`
    - File contains `.drhp-refusal` with `#FEF3C7` background AND `#D97706` left border AND `#78350F` text
    - File does NOT contain the destructive hex `#B91C1C` anywhere (UI-SPEC §Color §"no green/red coding" — reserved hex must not leak into Phase 1 CSS)
    - File contains all four breakpoint media queries (374px, 375-639px, 640-1023px, 1024+) with valid CSS bodies
    - File contains `@media (prefers-reduced-motion: reduce)` block
    - File contains `[data-testid="stFooter"] { display: none; }` (default footer suppression per UI-SPEC Streamlit-Specific Constraints)
    - File contains `outline: 2px solid #1E40AF` with `outline-offset: 2px` (focus ring per UI-SPEC Accessibility)
    - `app/util/css_loader.py` exports `load_global_css` callable
    - `load_global_css` is idempotent (second call with same session_state returns None)
    - `pytest tests/unit/test_css_loader.py -x -q --timeout=10` exits 0 with ≥ 10 passed
    - Both `app/__init__.py` and `app/util/__init__.py` exist as empty package markers
  </acceptance_criteria>
  <done>The single global CSS contract is on disk and the single-load injector is in place. Every subsequent Wave 4 task (chip, refusal, footer, modal) references class names defined here — no inline `<style>` injection anywhere else, no exception, no "just this once" CSS sprinkles. UI-SPEC FLAG-2 ("no scattered CSS") is structurally enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Citation chip renderer (ui/chip.py) + source-snippet expander (ui/expander.py) — XSS escape, dedupe cluster, claim_id round-trip</name>
  <files>ui/chip.py, ui/expander.py, tests/unit/test_citation_renderer.py</files>
  <behavior>
    The Wave 0 xfail stub `test_renderer_emits_sup_chip_html_and_escapes_xss` becomes green. Adds ~8 sub-tests for dedupe + round-trip + escape + ordering.

    Test cases that must exist in tests/unit/test_citation_renderer.py:
    - `test_renderer_emits_sup_chip_html_and_escapes_xss`: feed a `GroundedAnswer` whose `answer_prose` is `'The DRHP says X.{{c_4f3a8b}} <script>alert(1)</script> Y.{{c_4f3a8b}}'`. Expected output HTML: contains `<sup class="drhp-cite" data-claim-id="c_4f3a8b" tabindex="0" role="button"` AND the substring `&lt;script&gt;alert(1)&lt;/script&gt;` (escaped) AND does NOT contain the raw substring `<script>alert(1)</script>` (the XSS-attempt input is escaped at the boundary per T-1-06 mitigation).
    - `test_chip_cluster_dedupe_per_uispec_d03`: a prose with three consecutive sentences all citing `c_4f3a8b` ("X.{{c_4f3a8b}} Y.{{c_4f3a8b}} Z.{{c_4f3a8b}}") renders only ONE `[1]` chip at the position of the LAST citation in the cluster (per UI-SPEC Visuals §Citation Chip §Cluster + D-03 "Deduplicate chips at the answer surface"). The first two `{{c_4f3a8b}}` placeholders are removed from the rendered prose without leaving stray text. Underlying `claim_id` traces are NOT modified — the dedupe is presentation-only.
    - `test_chip_numbering_resets_per_answer`: a fresh `render_answer_with_chips(answer)` call assigns chip numbers starting at `[1]` (UI-SPEC §Visuals "Numbers reset per answer (no global counter)" — D-01).
    - `test_distinct_claim_ids_get_sequential_numbers`: prose `'A.{{c_aaaaaa}} B.{{c_bbbbbb}} C.{{c_aaaaaa}}'` renders chips `[1]`, `[2]`, `[1]` (second citation of `c_aaaaaa` reuses chip number 1; the new `c_bbbbbb` gets `[2]`). The claim_id → chip_number map is returned alongside the rendered HTML.
    - `test_chip_html_contains_aria_describedby`: the rendered `<sup>` includes `aria-describedby="cite-N-source"` where N is the chip number, pointing at the corresponding expander region (UI-SPEC Accessibility §Screen reader).
    - `test_period_before_chip_per_uispec`: prose `'X is true.{{c_4f3a8b}}'` renders with the period BEFORE the chip, never `'X is true{{c_4f3a8b}}.'` — the renderer preserves whatever punctuation the LLM put before the placeholder; it does not relocate punctuation (UI-SPEC §Visuals §Citation Chip §Visualization rules).
    - `test_unmatched_placeholder_falls_through_unchanged`: prose `'X.{{c_unknown}} Y.'` where `c_unknown` is NOT in `answer.claims` — the renderer must NOT render a chip for it (PITFALL P5 antibody: no "unsourced" chips). Behavior: emit the literal text `{{c_unknown}}` UNCHANGED (the cite-check node should have rejected this answer upstream; if it slips through, the visible artifact is debuggable rather than silently hidden) AND emit a warning to stderr via `logging.warning`.
    - `test_render_citation_expanders_pairs_with_chip_numbers`: `render_citation_expanders(answer, chip_number_map)` from `ui/expander.py` returns a list of dicts shaped `[{"chip_n": int, "label": str, "snippet": str, "source_url": str, "claim_id": str, "page_start": int, "section": str}, ...]` — ordered by chip number ascending, deduped per claim_id (one expander per unique claim_id), each with the verbatim DRHP source text snippet AND the SEBI/BSE/NSE URL with `#page={N}` anchor per UI-SPEC §Expander reveal content.
    - `test_round_trip_claim_id_through_renderer`: feed `Claim(claim_id="c_4f3a8b", text="X", sources=[ref])`, render via chip renderer, then parse the rendered HTML for `data-claim-id` attribute, assert it equals `c_4f3a8b` byte-for-byte (PITFALL P5 antibody: no claim_id drift from agent output to rendered chip — this is the cross-phase contract Phase 3 METHOD-01 will join on).
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Citation Chip Contract" — the locked HTML structure (lines 173-209): `<sup class="drhp-cite" data-claim-id="..." tabindex="0" role="button" aria-describedby="cite-N-source">[N]</sup>`
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Citation Chip Contract" §"Expander reveal content (per chip)" — the three-part expander payload (DRHP snippet + SEBI link + metadata footer)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md §"Citation Chip Behavior" — D-01 (superscript numbered, reset per answer), D-02 (inline expander, no side viewer), D-03 (dedupe at surface)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md §F "The Renderer Contract" — "The LLM emits `{{claim_id}}` placeholders inside `answer_prose`. The renderer walks the prose, finds placeholders, deduplicates per-cluster, assigns per-answer numbers..."
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md row `1-04-citation-chip-html` — the locked test contract this task closes
    - .planning/research/PITFALLS.md §"Pitfall 3: Citation drift via prose page references (PITFALLS P5)" — context for why claim_id round-trip is load-bearing
    - agent/schemas.py — `GroundedAnswer`, `Claim`, `RetrievedChunkRef` field names (Wave 1 contract; do not refactor)
  </read_first>
  <action>
    Implement `ui/chip.py` with two exports:

    1. `def build_chip_html(claim_id: str, chip_n: int) -> str:` — pure-function HTML builder. Uses `html.escape()` from the Python stdlib on `claim_id` before interpolation (defense-in-depth: claim_id is already regex-validated `^c_[a-z0-9]{6,16}$` by the schema, so escape is structurally redundant — but escape anyway per ASVS V5 "Validation, Sanitization & Encoding" defense-in-depth posture per T-1-06). Returns: `f'<sup class="drhp-cite" data-claim-id="{esc(claim_id)}" tabindex="0" role="button" aria-describedby="cite-{chip_n}-source" aria-label="citation {chip_n}, opens source-text panel">[{chip_n}]</sup>'` — note the `aria-label` per UI-SPEC Accessibility §"Screen reader" announcement template.

    2. `def render_answer_with_chips(answer: GroundedAnswer) -> tuple[str, dict[str, int]]:` — the main renderer.
       - Step 1: build a `valid_claim_ids = {c.claim_id for c in answer.claims}` set.
       - Step 2: walk `answer.answer_prose` with a regex `r"\{\{(c_[a-z0-9]{6,16})\}\}"` (matches the schema pattern). For every match, check whether the claim_id is in `valid_claim_ids`:
         - If NOT in set: leave the literal `{{c_unknown}}` unchanged in the output, emit `logging.warning(...)` (per the `test_unmatched_placeholder_falls_through_unchanged` contract).
         - If in set: this is a citation. Record the position + claim_id in a list.
       - Step 3: HTML-escape the prose using `html.escape(prose_text, quote=True)` BEFORE doing any chip substitution. This is the T-1-06 mitigation: any `<script>` in the LLM output (e.g., echoed from a malicious DRHP per T-1-02 prompt injection) becomes `&lt;script&gt;` BEFORE the chip HTML is interpolated.
       - Step 4: walk the escape-protected prose; for each citation, decide whether to emit a chip OR drop the placeholder (dedupe per D-03):
         - **Dedupe rule per UI-SPEC D-03 + Visuals §Cluster:** "three consecutive sentences citing the same span render ONE chip after the cluster." Practical interpretation: a citation is part of a cluster with the PREVIOUS citation if (a) the previous citation references the same claim_id AND (b) the text between them (after stripping whitespace and one or two sentence-terminator characters `.!?`) is non-empty but contains no other citation. The cluster's chip is emitted at the LAST position in the cluster; earlier positions in the cluster have their placeholder removed (replaced with empty string) cleanly, preserving the surrounding punctuation and whitespace.
         - **Distinct-claim_id rule:** a previously-seen claim_id (not in immediate-previous position — i.e., gap between two citations of `c_aaaaaa` contains a citation of `c_bbbbbb`) re-uses its assigned chip number; the chip IS emitted (not deduped — only contiguous clusters dedupe per UI-SPEC).
       - Step 5: assign chip numbers in first-appearance order: first unique claim_id seen → 1, second → 2, etc. Build `claim_id_to_chip_n: dict[str, int]`.
       - Step 6: for each non-deduped citation position, replace the placeholder with `build_chip_html(claim_id, claim_id_to_chip_n[claim_id])`.
       - Step 7: return `(rendered_html, claim_id_to_chip_n)`.

    Implement `ui/expander.py`:
    - `def render_citation_expanders(answer: GroundedAnswer, claim_id_to_chip_n: dict[str, int]) -> list[dict]:` — returns one expander descriptor per UNIQUE claim_id in `answer.claims` (UI-SPEC: "one expander per deduped chip, ordered to match in-text appearance"). For each unique claim_id, pick the FIRST source from `claim.sources` (per UI-SPEC: one expander per chip with the corresponding source-text snippet; multi-source claims surface the first source in Phase 1, Phase 3 METHOD-01 may surface all). Each dict has shape:
      ```
      {
        "chip_n": int,                # chip number assigned by render_answer_with_chips
        "claim_id": str,
        "label": str,                 # f"[{chip_n}] DRHP page {page_start} · {section}"
        "snippet": str,               # verbatim source.chunk_text[span_start:span_end] from the RetrievedChunkRef
        "source_url": str,            # f"{SEBI_PROSPECTUS_URL}#page={page_start}" — the SEBI canonical PDF + page anchor
        "page_start": int,
        "section": str,
        "metadata_footer": str,       # f"claim_id: {claim_id} · DRHP page {page_start} · section \"{section}\""
      }
      ```
    - The `SEBI_PROSPECTUS_URL` constant lives at the top of `ui/expander.py` as `SEBI_PROSPECTUS_URL = "https://www.sebi.gov.in/filings/public-issues/nov-2024/swiggy-limited-prospectus_88320.html"` (per 01-01-PLAN.md Task 2 — the canonical Phase 1 source URL).
    - **HTML escape every string field** that will reach `st.markdown(unsafe_allow_html=True)` (the snippet may contain DRHP-sourced text that includes characters like `<` or `&`). Use `html.escape` defensively even on schema-validated fields. T-1-06 mitigation.
    - Result list is ordered by `chip_n` ascending; the Streamlit consumer (`app.py` Task 6) iterates and emits one `st.expander` per dict.

    Update `tests/unit/test_citation_renderer.py`: remove the xfail mark; add the 9 test functions listed in `<behavior>`. Use the synthetic `GroundedAnswer` factory pattern (build minimal valid Pydantic instances directly — no LLM mocks needed). Reference `agent/schemas.py` exports directly.

    Per the source-audit: this task closes REQ UI-02 (citation chips clickable — the renderer emits clickable `<sup role="button">` with `tabindex="0"` and `aria-describedby`; the actual click handler is Streamlit's expander pairing in Task 6). The XSS-escape behavior closes the T-1-06 mitigation declared in `01-VALIDATION.md`.
  </action>
  <threat_model>
    | Threat ID | Category | Component | Disposition | Mitigation Plan |
    |-----------|----------|-----------|-------------|-----------------|
    | T-1-06 | XSS via `unsafe_allow_html` (LLM output reaches rendered HTML) | `ui/chip.py::render_answer_with_chips`, `ui/expander.py::render_citation_expanders` | mitigate | `html.escape(prose, quote=True)` applied to LLM-emitted `answer_prose` BEFORE chip interpolation; `html.escape` defensively applied to DRHP-snippet text in expander payload; `claim_id` is regex-validated `^c_[a-z0-9]{6,16}$` by schema (no metacharacters possible) but escaped anyway for defense-in-depth per ASVS V5 |
    | T-1-02 | Prompt injection via DRHP content surfacing in rendered output | escape-then-interpolate in chip renderer | mitigate | DRHP-sourced text reaches the renderer via the chunk_text field; escape is the boundary defense. Combined with Wave 3 cite-check ratio ≥ 80 (which rejects answers where claim text doesn't match the cited window), the surface for advisory language echoing is structurally bounded |
    | T-1-11 | Citation drift (claim_id reassigned between agent output and rendered chip) | `data-claim-id` attribute round-trip test | mitigate | `test_round_trip_claim_id_through_renderer` asserts byte-for-byte preservation; this is the Phase 3 METHOD-01 cross-phase contract |
  </threat_model>
  <verify>
    <automated>pytest tests/unit/test_citation_renderer.py -x -q --timeout=10 -v 2>&amp;1 | tee /tmp/chip_test.txt &amp;&amp; (! grep -E '^FAILED ' /tmp/chip_test.txt) &amp;&amp; python -c "from ui.chip import render_answer_with_chips, build_chip_html; from ui.expander import render_citation_expanders, SEBI_PROSPECTUS_URL; assert SEBI_PROSPECTUS_URL.startswith('https://www.sebi.gov.in'); h = build_chip_html('c_4f3a8b', 1); assert 'data-claim-id=\"c_4f3a8b\"' in h and 'aria-describedby=\"cite-1-source\"' in h and 'role=\"button\"' in h and '[1]' in h, h; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ui/chip.py` exports `render_answer_with_chips` and `build_chip_html`
    - `ui/expander.py` exports `render_citation_expanders` and constant `SEBI_PROSPECTUS_URL`
    - `build_chip_html("c_4f3a8b", 1)` returns a string containing: `<sup`, `class="drhp-cite"`, `data-claim-id="c_4f3a8b"`, `tabindex="0"`, `role="button"`, `aria-describedby="cite-1-source"`, `[1]`, `</sup>` — all on one line (UI-SPEC HTML structure verbatim)
    - `render_answer_with_chips` HTML-escapes any `<script>` in input prose to `&lt;script&gt;` (T-1-06 mitigation; verified by test_renderer_emits_sup_chip_html_and_escapes_xss)
    - Dedupe per D-03: three consecutive citations of the same claim_id render only the LAST chip (verified by test_chip_cluster_dedupe_per_uispec_d03)
    - Chip numbering resets per answer (D-01; verified by test_chip_numbering_resets_per_answer)
    - Distinct claim_ids get sequential numbers; repeated claim_ids reuse their number (verified by test_distinct_claim_ids_get_sequential_numbers)
    - `aria-describedby="cite-N-source"` present in every emitted chip (UI-SPEC Accessibility)
    - `claim_id` round-trips byte-for-byte from `Claim.claim_id` → chip `data-claim-id` attribute (verified by test_round_trip_claim_id_through_renderer — this is the cross-phase contract for Phase 3 METHOD-01)
    - Unknown `{{claim_id}}` placeholders fall through unchanged + log a warning (verified by test_unmatched_placeholder_falls_through_unchanged — PITFALL P5 debuggability over silent failure)
    - `render_citation_expanders` returns list ordered by `chip_n` ascending; one expander per unique claim_id
    - `pytest tests/unit/test_citation_renderer.py -x -q --timeout=10` exits 0 with ≥ 9 passed and 0 xfailed
    - Implements REQ UI-02
  </acceptance_criteria>
  <done>The citation chip rendering contract — the load-bearing UI element of Phase 1 — is locked. Wave 4's `app.py` (Task 6) calls `render_answer_with_chips(answer)` then `render_citation_expanders(answer, chip_map)` and emits one `st.expander` per dict. The XSS surface (T-1-06) is closed by escape-then-interpolate. The cross-phase `claim_id` contract for Phase 3 METHOD-01 is structurally enforced via the round-trip test. Wave 0's `test_renderer_emits_sup_chip_html_and_escapes_xss` xfail flips to GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Disclaimer surfaces — Streamlit-rendering functions (additions to ui/disclaimer.py from Wave 1) + refusal banner</name>
  <files>ui/disclaimer.py, ui/refusal_banner.py, tests/unit/test_disclaimer_surface_render.py, tests/unit/test_refusal_banner.py, tests/unit/test_copy_no_banned_tokens.py</files>
  <behavior>
    Test cases that must exist in tests/unit/test_disclaimer_surface_render.py (NEW file):
    - `test_wave1_exports_preserved`: `from ui.disclaimer import DisclaimerSurface, render_disclaimer_gate` still works (Wave 1 contract MUST NOT be broken — backwards compatibility check, this is the regression guard).
    - `test_render_persistent_footer_html_contains_anchor_copy_and_methodology_link`: `render_persistent_footer()` returns an HTML string containing the byte-for-byte `ANCHOR_COPY` from `compliance/disclaimer_text.py` AND `<a` AND `href="/methodology"` AND the visible text `methodology` AND the CSS class `drhp-footer` AND the CSS class `drhp-footer-link` (delegates styling to the Task-1 CSS; no inline `<style>` injection here per UI-SPEC FLAG-2). The 12px SEBI 10pt-floor is satisfied via the CSS class — verify by greppping the Task 1 CSS contains `.drhp-footer { ... font-size: 12px` (cross-task assertion).
    - `test_render_per_answer_footer_html_uses_class_not_inline_style`: `render_per_answer_footer()` returns `<div class="drhp-disclaimer-per-answer">Informational only — not advice.</div>` — class-based styling per UI-SPEC FLAG-2; NO `style="..."` inline attribute (Wave 1's implementation MAY have had inline-fallback `font-size: 12px` for unit-test detection — Wave 4 removes the inline fallback now that the CSS file ships).
    - `test_render_first_use_modal_data_shape`: `render_first_use_modal()` is a pure-data function (no Streamlit calls at module level — Wave 4 wraps with `@st.dialog` at the call site in `app.py`) returning `{"heading": MODAL_HEADING, "body": ANCHOR_COPY + " " + MODAL_BODY_ADDENDUM, "cta_text": MODAL_CTA, "css_class": "drhp-modal"}`.
    - `test_modal_body_contains_ai_disclosure`: the modal body string contains substring `large language models` (UI-SPEC L-11 / SEBI Jan-2025 RA AI-disclosure — Wave 1 contract reaffirmed).
    - `test_anchor_copy_unchanged_from_wave1`: assert that `compliance/disclaimer_text.ANCHOR_COPY` matches the D-07 byte-for-byte string. Regression guard: Wave 4 must not silently edit Wave 1's locked copy.
    - `test_all_three_render_functions_pass_scrubber`: call each of `render_persistent_footer()`, `render_per_answer_footer()`, `render_first_use_modal()`; for each return value (string or dict), extract every string in the value and assert `scrub(s).passed is True` (TRUST-02 extension to the disclaimer surface).
    - `test_per_answer_footer_does_not_render_inside_refusal_banner`: this is a documentation-shape test — assert that `ui/refusal_banner.py` does NOT import `render_per_answer_footer` (UI-SPEC §"Disclaimer Surfaces §3 — Does NOT render on the refusal banner — the refusal banner has its own per-answer footer baked into its structure"). Use `inspect.getsource(ui.refusal_banner)` and grep.

    Test cases that must exist in tests/unit/test_refusal_banner.py (NEW file):
    - `test_render_refusal_banner_low_retrieval`: feed a `RefusalResponse(reason="low_retrieval_score", message="This DRHP does not address Mars colonization. Try asking about ...", reformulation_suggestions=["What is the issue size?", "Who are the promoters?"])`. Output HTML string contains: `class="drhp-refusal"` AND the heading `This isn't in the DRHP.` (per UI-SPEC §Visuals §Refusal Banner §Content layout 1) AND the message body (escaped) AND two `<button class="drhp-suggest">` (or equivalent class) elements containing the two suggestion strings.
    - `test_render_refusal_banner_partial_grounding`: when `reason="unsupported_claim"` AND the message contains the substring "addresses parts of", the heading is `The DRHP only addresses part of this.` (per UI-SPEC §Visuals §Refusal Banner §Content layout 1 alternate heading for D-06).
    - `test_render_refusal_banner_banned_token`: `reason="banned_token"` AND `reformulation_suggestions=[]` — output HTML contains the refusal body AND zero `.drhp-suggest` elements (UI-SPEC §Loading/Empty/Error §"Banned-token detected" — "No reformulation chips — the LLM tried and failed; suggesting reformulations would invite repeated failures").
    - `test_render_refusal_banner_caps_at_two_suggestions`: feed `reformulation_suggestions=["a", "b", "c"]` (the schema allows max 3 per RefusalResponse; UI-SPEC §Visuals §Refusal Banner §Content layout 3 says "up to three", but this Wave 4 plan's `must_haves` truth says "up to 2 reformulation chips" per Open Question 5 + the `RELAXED_SEARCH_TOP_SECTIONS = 2` policy from 01-04-PLAN.md). Output renders ONLY the first 2 chips. The third is silently truncated. Document this as a deliberate UI choice: Phase 1 surfaces 2 chips to match the `search_relaxed()` top-2-sections policy; UI-SPEC's "up to three" is the theoretical max (Phase 2 may raise the floor if calibration shows >2 useful suggestions).
    - `test_render_refusal_banner_escapes_xss_in_message_and_suggestions`: feed `message='<script>alert(1)</script>'` AND `reformulation_suggestions=["<img onerror=alert(1)>"]`. Output HTML must contain `&lt;script&gt;` AND `&lt;img onerror=alert(1)&gt;` (escaped) AND must NOT contain the raw `<script>` or unescaped `<img onerror`. T-1-06 mitigation extension.
    - `test_refusal_banner_includes_aria_live_polite`: output HTML contains `aria-live="polite"` attribute on the outer div (UI-SPEC Accessibility §Live regions — "Loading state and refusal banner render inside `aria-live=\"polite\"` containers").
    - `test_refusal_banner_includes_per_answer_disclaimer`: output HTML contains the substring `Informational only — not advice.` baked into the refusal banner structure (UI-SPEC §Visuals §Refusal Banner §Content layout 4 — "the same Informational only — not advice. footer that appears below grounded answers").

    Extensions to tests/unit/test_copy_no_banned_tokens.py (Wave 1 file):
    - `test_methodology_stub_body_passes_scrubber`: `from ui.copy import METHODOLOGY_STUB_BODY; assert scrub(METHODOLOGY_STUB_BODY).passed is True` (Wave 1 already locked this constant — this test reaffirms after Wave 4 changes; defense against accidental edits).
    - `test_refusal_banner_module_strings_pass_scrubber`: import `ui.refusal_banner`, walk module-level string constants (e.g., the `REFUSAL_HEADING_LOW_RETRIEVAL` and `REFUSAL_HEADING_PARTIAL_GROUNDING` constants this task adds), assert each passes `scrub()` (TRUST-02 extension).
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Disclaimer Surfaces" — the three-surface contract (modal, persistent footer, per-answer footer); confirm UI-SPEC FLAG-2 means class-based styling not inline (the Wave 1 inline-fallback `font-size: 12px` is now redundant since Task 1 CSS ships)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Refusal Banner Contract" — content layout 1-4 (heading, body, chips, per-answer disclaimer); explicit anti-patterns ("Do not use red", "Do not use exclamation marks", "Do not collapse into st.warning", "Do not hide reformulation chips behind expander")
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Accessibility Contract" §"Live regions" — `aria-live="polite"` on refusal banner
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Loading / Empty / Error States" §"Banned-token detected" — no reformulation chips on banned-token refusal
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-02-PLAN.md Task 3 — the Wave 1 `DisclaimerSurface` exports MUST be preserved (no rename, no removal)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-02-SUMMARY.md — the actual shipped Wave 1 export list (verify backwards compatibility)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-04-PLAN.md Task 1 — `RELAXED_SEARCH_TOP_SECTIONS = 2` (reformulation suggestion count policy)
    - compliance/disclaimer_text.py — the source-of-truth constants (do NOT redefine in Wave 4)
    - compliance/scrubber.py — the scrubber for TRUST-02 extension
    - ui/copy.py — the Wave 1 copy constants (do NOT redefine in Wave 4)
    - agent/schemas.py — `RefusalResponse` shape
  </read_first>
  <action>
    **Extend `ui/disclaimer.py` — do NOT rewrite.** The Wave 1 `DisclaimerSurface` class and `render_disclaimer_gate` function remain as-is (the regression guard test verifies). Add three new module-level functions on top:

    1. `def render_first_use_modal() -> dict:` — alias/passthrough to `DisclaimerSurface().render_modal()` returning the dict shape locked in Wave 1, with one addition: `"css_class": "drhp-modal"` so `app.py` can use the Task 1 CSS class. The actual Streamlit `@st.dialog(...)` decoration happens in `app.py` Task 6 — this function is a pure-data return that `app.py` calls inside its `@st.dialog`-decorated wrapper.

    2. `def render_persistent_footer() -> str:` — returns: `f'<div class="drhp-footer">{html.escape(ANCHOR_COPY)} · <a class="drhp-footer-link" href="/methodology">methodology</a></div>'`. NO inline `style="..."` attribute (delegates to Task 1 CSS per UI-SPEC FLAG-2). This **replaces** the Wave 1 implementation that had inline `style="font-size: 12px; color: #475569;"` — Wave 1 needed inline-fallback for unit tests; Wave 4 has the CSS file. The Wave 1 test `test_render_persistent_footer_includes_methodology_link_token` continues to pass (its assertions are class + link presence, not the inline style).

    3. `def render_per_answer_footer() -> str:` — returns: `f'<div class="drhp-disclaimer-per-answer">{html.escape(PER_ANSWER_FOOTER)}</div>'`. Same class-based delegation; no inline style.

    Add a module-level docstring section "## Wave 4 additions" explaining that the new functions are class-based renderers that delegate styling to `app/static/drhplens.css` per UI-SPEC FLAG-2; the original Wave 1 `DisclaimerSurface` methods are preserved for backwards compatibility but new code should use the new functions.

    Implement `ui/refusal_banner.py` from scratch:
    - Module-level constants (heading strings — keep these as module constants so they're scrubber-testable + locked):
      - `REFUSAL_HEADING_LOW_RETRIEVAL: str = "This isn't in the DRHP."` (UI-SPEC §Visuals §Refusal Banner §Content layout 1)
      - `REFUSAL_HEADING_PARTIAL_GROUNDING: str = "The DRHP only addresses part of this."` (alternate heading for D-06)
      - `REFUSAL_HEADING_BANNED_TOKEN: str = "Couldn't return that answer."` (per UI-SPEC §Copywriting Contract refusal banned-token row, paraphrased to fit the heading length — actual body uses the longer UI-SPEC copy)
      - `REFUSAL_HEADING_INFRASTRUCTURE: str = "Couldn't reach the DRHP."` (generic infra error heading)
      - `MAX_CHIPS_RENDERED: int = 2` (per Phase 1 Open Question 5 + `RELAXED_SEARCH_TOP_SECTIONS = 2` in 01-04-PLAN.md policies; UI-SPEC allows up to 3 — Phase 2 may raise)

    - Function `def _select_heading(refusal: RefusalResponse) -> str:` — branches on `refusal.reason`:
      - `low_retrieval_score` → `REFUSAL_HEADING_LOW_RETRIEVAL`
      - `unsupported_claim` → if "addresses parts of" appears in `refusal.message`, return `REFUSAL_HEADING_PARTIAL_GROUNDING`; else `REFUSAL_HEADING_LOW_RETRIEVAL` (no-grounding fallback)
      - `banned_token` → `REFUSAL_HEADING_BANNED_TOKEN`
      - `infrastructure_error` → `REFUSAL_HEADING_INFRASTRUCTURE`

    - Function `def render_refusal_banner(refusal: RefusalResponse) -> str:` — returns the full HTML string. Structure (with `html.escape` applied to every dynamic string field per T-1-06):
      ```
      <div class="drhp-refusal" role="alert" aria-live="polite">
        <h2 class="drhp-refusal-heading">{escaped heading}</h2>
        <p class="drhp-refusal-body">{escaped refusal.message}</p>
        {if refusal.reason != "banned_token" AND refusal.reformulation_suggestions:
          <div class="drhp-suggest-group">
            {for s in refusal.reformulation_suggestions[:MAX_CHIPS_RENDERED]:
              <button class="drhp-suggest" type="button" data-suggestion="{html.escape(s, quote=True)}">{html.escape(s)}</button>
            }
          </div>
        }
        <div class="drhp-disclaimer-per-answer">{html.escape(PER_ANSWER_FOOTER)}</div>
      </div>
      ```
      The `<button>` is NOT a real Streamlit button (Streamlit's `st.button` rendering is incompatible with inline HTML inside `st.markdown`). The actual "click to fill input" behavior is wired in `app.py` Task 6 via parallel `st.button` widgets rendered ABOVE or BELOW the HTML banner. The HTML `<button>` here is the visual surface (styled per UI-SPEC); the functional click handler is the Streamlit button. UI-SPEC §"Reformulation suggestion chips" Streamlit implementation row confirms this two-layer pattern (`st.columns(3)` with `st.button` per chip, styled via CSS). Document this in the function docstring.
      
      Alternative if Streamlit's `st.button` styling proves insufficient at execute-time: emit just the styled HTML buttons here (visual-only) and pair with a single `st.text_area` or `st.chat_input` pre-fill via `st.session_state.draft_question` set by a different mechanism (UI-SPEC suggests `data-suggestion` attribute + click handler; Phase 1 can settle for "visual chip + Streamlit click below in two-row layout" if that simplifies). Executor decides at implementation time.

      Per UI-SPEC anti-pattern §"Banned-token detected": when `refusal.reason == "banned_token"`, NO reformulation chips render even if `reformulation_suggestions` is non-empty (defensive — the scrubber-refusal node in Wave 3 should be emitting empty list anyway per 01-04-PLAN.md Task 2's `refuse_with_reformulation` policy, but the renderer enforces it as a structural belt-and-suspenders).

      Per UI-SPEC anti-pattern: do NOT use `st.warning` or `st.error` — `<div class="drhp-refusal">` only. Add a comment: `# UI-SPEC anti-pattern: do NOT replace with st.warning — refusals are first-class output, not chrome.`

    Extend `tests/unit/test_copy_no_banned_tokens.py` (Wave 1 file): keep all existing tests; add the 2 new tests listed in `<behavior>` (one for `METHODOLOGY_STUB_BODY` regression guard, one for `ui.refusal_banner` module constants).

    Create `tests/unit/test_disclaimer_surface_render.py` and `tests/unit/test_refusal_banner.py` as NEW files with the test functions listed in `<behavior>`.

    Per the source-audit: this task completes the Streamlit-rendering side of TRUST-01 (modal + footer + per-answer footer ALL renderable from a Streamlit consumer) and TRUST-03 (12px ≥ SEBI 10pt floor delivered via Task 1 CSS class — already verified by Wave 1's `test_render_persistent_footer_includes_methodology_link_token` checking for the `drhp-footer` class which the CSS file rules at 12px; Wave 1's standalone inline-12px check still passes against the Wave 1 export of the class which is preserved). The plan's `requirements` frontmatter lists only UI-01/UI-02 because TRUST-01/02/03 are formally closed by Plan 02 — Wave 4 just renders the Wave 1 surfaces in Streamlit. The refusal banner is the visible artifact of RAG-03 (formally closed by Plan 04) but its presentation contract lands here.
  </action>
  <threat_model>
    | Threat ID | Category | Component | Disposition | Mitigation Plan |
    |-----------|----------|-----------|-------------|-----------------|
    | T-1-06 | XSS via `unsafe_allow_html` | `render_refusal_banner` (message + reformulation_suggestions reach HTML) | mitigate | `html.escape(quote=True)` applied to every dynamic string interpolated into the HTML; `data-suggestion` attribute uses `quote=True` for attribute-context safety; tested in `test_render_refusal_banner_escapes_xss_in_message_and_suggestions` |
    | T-1-09 | Copy drift (Wave 4 silently edits D-07 anchor copy) | `compliance/disclaimer_text.ANCHOR_COPY` regression guard test | mitigate | `test_anchor_copy_unchanged_from_wave1` byte-for-byte assertion; Wave 4 importers do NOT redefine the constant |
    | T-1-12 | Compliance regression (per-answer disclaimer accidentally renders inside refusal banner, doubling the surface and violating UI-SPEC §Disclaimer Surfaces §3 "Does NOT render on the refusal banner") | `test_per_answer_footer_does_not_render_inside_refusal_banner` import-graph assertion | mitigate | Static-source grep test enforces the import-graph contract |
  </threat_model>
  <verify>
    <automated>pytest tests/unit/test_disclaimer_surface_render.py tests/unit/test_refusal_banner.py tests/unit/test_disclaimer_surface.py tests/unit/test_copy_no_banned_tokens.py -x -q --timeout=10 -v 2>&amp;1 | tee /tmp/disc_render_test.txt &amp;&amp; (! grep -E '^FAILED ' /tmp/disc_render_test.txt) &amp;&amp; python -c "from ui.disclaimer import DisclaimerSurface, render_disclaimer_gate, render_first_use_modal, render_persistent_footer, render_per_answer_footer; from ui.refusal_banner import render_refusal_banner, MAX_CHIPS_RENDERED, REFUSAL_HEADING_LOW_RETRIEVAL; from agent.schemas import RefusalResponse; r = RefusalResponse(reason='low_retrieval_score', message='test', reformulation_suggestions=['a','b','c']); h = render_refusal_banner(r); assert 'drhp-refusal' in h and 'aria-live=\"polite\"' in h and h.count('drhp-suggest') &gt;= 2, h; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Wave 1 exports preserved: `from ui.disclaimer import DisclaimerSurface, render_disclaimer_gate` succeeds (regression guard)
    - New exports present: `from ui.disclaimer import render_first_use_modal, render_persistent_footer, render_per_answer_footer` succeeds
    - `render_persistent_footer()` returns HTML containing `class="drhp-footer"` AND `href="/methodology"` AND `methodology` AND the byte-for-byte `ANCHOR_COPY` from Wave 1 (no inline `style="..."` attribute)
    - `render_per_answer_footer()` returns HTML containing `class="drhp-disclaimer-per-answer"` AND the byte-for-byte `PER_ANSWER_FOOTER` from Wave 1
    - `render_first_use_modal()` returns dict with keys `heading`, `body`, `cta_text`, `css_class` matching Wave 1 contract + new `css_class`
    - `ui/refusal_banner.py` exports `render_refusal_banner`, `MAX_CHIPS_RENDERED`, `REFUSAL_HEADING_LOW_RETRIEVAL`, `REFUSAL_HEADING_PARTIAL_GROUNDING`, `REFUSAL_HEADING_BANNED_TOKEN`, `REFUSAL_HEADING_INFRASTRUCTURE`
    - `render_refusal_banner(refusal)` returns HTML containing `class="drhp-refusal"` AND `aria-live="polite"` AND `role="alert"`
    - Banned-token refusal renders zero `.drhp-suggest` elements even when `reformulation_suggestions` is non-empty (defensive UI-SPEC anti-pattern enforcement)
    - More than 2 reformulation suggestions are silently truncated to 2 (Phase 1 policy per `MAX_CHIPS_RENDERED = 2`)
    - XSS in message + reformulation_suggestions is escaped (T-1-06)
    - `ui/refusal_banner.py` does NOT import `render_per_answer_footer` (the per-answer disclaimer is baked into the banner structure, NOT a separate import — UI-SPEC §Disclaimer Surfaces §3 enforcement)
    - All Wave 1 disclaimer tests in `tests/unit/test_disclaimer_surface.py` still pass (backwards compatibility)
    - `pytest tests/unit/test_disclaimer_surface_render.py tests/unit/test_refusal_banner.py tests/unit/test_disclaimer_surface.py tests/unit/test_copy_no_banned_tokens.py -x -q --timeout=10` exits 0 with 0 failures
    - Implements the Streamlit-rendering side of TRUST-01/TRUST-03; visual surface of RAG-03
  </acceptance_criteria>
  <done>The three disclaimer surfaces are now Streamlit-renderable through a single consistent class-based interface, and the refusal banner ships as a first-class artifact (amber-not-red, aria-live, with reformulation chips capped at 2 per Phase 1 policy, with structural enforcement of UI-SPEC anti-patterns). Wave 4's `app.py` (Task 6) imports `render_first_use_modal`, `render_persistent_footer`, `render_per_answer_footer`, `render_refusal_banner` and emits each via `st.markdown(..., unsafe_allow_html=True)` (or `@st.dialog` for the modal).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Session-state helpers (ui/state.py) — modal-seen flag + chat-history accumulator</name>
  <files>ui/state.py, tests/unit/test_session_state.py</files>
  <behavior>
    Test cases (using a plain dict as a stand-in for `st.session_state`; no Streamlit dependency in the test — the helpers must accept any dict-like):
    - `test_init_session_state_idempotent`: `init_session_state(ss)` on an empty dict sets keys `disclaimer_accepted=False`, `chat_history=[]`, `draft_question=""`. Calling twice does NOT reset existing values.
    - `test_init_session_state_preserves_existing_history`: pre-populate `ss = {"chat_history": [{"role":"user","content":"x"}]}`; after `init_session_state(ss)`, the existing history is preserved (defends against accidental erase on st.rerun()-driven reinitialization).
    - `test_has_seen_modal_default_false`: fresh `init_session_state(ss)` → `has_seen_modal(ss)` returns False.
    - `test_has_seen_modal_returns_true_after_mark`: `mark_modal_seen(ss); assert has_seen_modal(ss) is True`.
    - `test_chat_history_append_user_turn`: `append_to_chat_history(ss, role="user", content="What is the issue size?")` adds `{"role":"user", "content":"What is the issue size?", "metadata":{}}` to the history list.
    - `test_chat_history_append_assistant_turn_with_grounded_answer`: `append_to_chat_history(ss, role="assistant", content=grounded_answer)` where content is a `GroundedAnswer` instance — the helper stores it under `content` directly (Pydantic models are JSON-serializable; the renderer in `app.py` Task 6 will distinguish role to pick chip renderer vs refusal renderer).
    - `test_chat_history_append_assistant_turn_with_refusal`: same pattern with a `RefusalResponse` instance — stored under `content`; the role-and-type pair `("assistant", RefusalResponse)` is the signal to use `render_refusal_banner` instead of `render_answer_with_chips`.
    - `test_get_chat_history_returns_list_reference`: `get_chat_history(ss)` returns the same list object as `ss["chat_history"]` (mutating the return value mutates state; this is a Streamlit-style contract; no defensive copy).
    - `test_reset_chat_history_clears_but_preserves_modal_seen`: after several appends + `mark_modal_seen`, `reset_chat_history(ss)` empties `chat_history` but leaves `disclaimer_accepted=True` (the modal is session-scoped per UI-SPEC; chat reset should not re-show the modal).
    - `test_helpers_accept_plain_dict_no_streamlit_import`: `import ui.state; ui.state.has_seen_modal({"disclaimer_accepted": True})` works without importing Streamlit (helper layer is presentation-framework-independent; only `app.py` imports `streamlit` at module level).
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Information Architecture (Cited-Q&A Page)" — hero expanded vs collapsed depends on `len(chat_history)`; chat_history shape per UI-SPEC §IA §3
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Disclaimer Surfaces §1 First-use modal" — "Trigger: first session per browser (gated by `st.session_state`)"
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md §"Disclaimer Copy + Style" D-08 — modal persists "I understand" via session state
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Streamlit `st.dialog` first-use modal" — session state pattern + known limitation (resets on browser-tab close; acceptable per Phase 1)
    - .planning/research/STACK.md §"What NOT to Use" — "Streamlit `st.session_state` as long-term memory — Lost on rerun; not durable" — confirms Phase 1's "session-scoped only" stance is correct
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Information Architecture" §3 — "History is session-scoped only — no persistence between page refreshes in Phase 1"
  </read_first>
  <action>
    Implement `ui/state.py` as a pure-Python helper module (NO `import streamlit` at module level — the module accepts any dict-like; `app.py` passes the real `st.session_state`). Per UI-SPEC §IA §3 "History is session-scoped only" — no persistence layer, no SQLite, no cookies.

    Exports:
    - `def init_session_state(ss) -> None:` — idempotent initialization. For each of `disclaimer_accepted=False`, `chat_history=[]`, `draft_question=""`, `_drhp_css_loaded` (unset; Task 1's `load_global_css` manages it): call `ss.setdefault(key, default)`. Does NOT overwrite existing keys. Call this at the very top of `app.py` Task 6 before any other UI work.
    - `def has_seen_modal(ss) -> bool:` — `return bool(ss.get("disclaimer_accepted", False))`.
    - `def mark_modal_seen(ss) -> None:` — `ss["disclaimer_accepted"] = True`. Wave 6 (`app.py` modal CTA callback) calls this.
    - `def append_to_chat_history(ss, role: str, content) -> None:` — appends `{"role": role, "content": content, "metadata": {}}` to `ss["chat_history"]`. Accepts `content` of any type (string for user turns; `GroundedAnswer` or `RefusalResponse` for assistant turns). Asserts `role in ("user", "assistant")` for safety.
    - `def get_chat_history(ss) -> list[dict]:` — returns `ss.get("chat_history", [])` (the list reference, not a copy — Streamlit-style mutate-in-place contract).
    - `def reset_chat_history(ss) -> None:` — `ss["chat_history"] = []`. Does NOT touch `disclaimer_accepted` (per UI-SPEC §IA §3 chat reset does not re-show the modal).

    Add a module docstring: "Session-state helpers for the Streamlit chat flow. Designed to accept any dict-like (st.session_state, or a plain dict for unit tests) — no `import streamlit` here. Session-scoped only per UI-SPEC §Information Architecture §3 — no persistence across page refreshes in Phase 1."

    Create `tests/unit/test_session_state.py` with the 10 test functions in `<behavior>`. Use plain `dict` as the session_state stand-in.

    Per the source-audit: this task is infrastructure for UI-01 (mobile-responsive web — the hero expanded/collapsed branching needs `get_chat_history` length check) and for the modal-once-per-session behavior asserted by manual test row `TRUST-01 first-use modal appears on first visit + persists "I understand"` in 01-VALIDATION.md.
  </action>
  <verify>
    <automated>pytest tests/unit/test_session_state.py -x -q --timeout=10 -v 2>&amp;1 | tee /tmp/state_test.txt &amp;&amp; (! grep -E '^FAILED ' /tmp/state_test.txt) &amp;&amp; python -c "from ui.state import init_session_state, has_seen_modal, mark_modal_seen, append_to_chat_history, get_chat_history, reset_chat_history; ss = {}; init_session_state(ss); assert has_seen_modal(ss) is False; mark_modal_seen(ss); assert has_seen_modal(ss) is True; append_to_chat_history(ss, role='user', content='q1'); assert len(get_chat_history(ss)) == 1; reset_chat_history(ss); assert get_chat_history(ss) == [] and has_seen_modal(ss) is True; print('OK')" &amp;&amp; python -c "import ui.state; import sys; assert 'streamlit' not in sys.modules or 'streamlit' not in [m for m in dir(ui.state) if not m.startswith('_')], 'ui.state must not import streamlit at module level'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ui/state.py` exports exactly: `init_session_state`, `has_seen_modal`, `mark_modal_seen`, `append_to_chat_history`, `get_chat_history`, `reset_chat_history`
    - `ui/state.py` does NOT contain `import streamlit` anywhere in its source (verified by `grep -L 'import streamlit' ui/state.py`)
    - `init_session_state` is idempotent; second call with existing history preserves it
    - `mark_modal_seen` + `has_seen_modal` round-trip correctly
    - `append_to_chat_history` accepts both string content (user turns) and Pydantic objects (assistant turns); stores `{"role", "content", "metadata"}` shape
    - `reset_chat_history` clears history but preserves `disclaimer_accepted` (UI-SPEC §IA §3)
    - All helpers accept plain dict (no Streamlit dependency at helper layer)
    - `pytest tests/unit/test_session_state.py -x -q --timeout=10` exits 0 with ≥ 10 passed
  </acceptance_criteria>
  <done>The session-state contract is locked: `app.py` Task 6 uses these six helpers to gate the modal, drive the hero expanded/collapsed branching (via `len(get_chat_history(ss))`), and accumulate turns. The Streamlit-independence of the helpers makes them unit-testable without spinning up a Streamlit runtime. The hero expanded/collapsed state in UI-SPEC IA §1/1a has a clean dependency.</done>
</task>

<task type="auto">
  <name>Task 5: /methodology stub page (pages/01_methodology.py) — UI-SPEC L-7 deep-link must not 404</name>
  <files>pages/01_methodology.py</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"`/methodology` Stub Page Contract" — exact page structure (Display heading, body paragraph, GitHub link, back link, persistent footer)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Copywriting Contract" rows `/methodology stub heading` AND `/methodology stub body` — verbatim copy strings
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"`/methodology` Stub Page Contract" §"What NOT to render in Phase 1's stub" — no fake eval scores, no methodology pane preview, no model card stub
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md L-7 — "Phase 6 LAND-01 replaces this. Must not 404."
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md §G — `pages/01_methodology.py` directory layout (locked at Wave 0)
    - .planning/research/STACK.md §"Streamlit 1.36+" — multipage app routing via `pages/NN_name.py` convention
    - ui/copy.py — `METHODOLOGY_STUB_HEADING` and `METHODOLOGY_STUB_BODY` constants (Wave 1 shipped these — use them, do NOT redefine)
    - ui/disclaimer.py — `render_persistent_footer` (Task 3 — the methodology page must render the persistent footer per UI-SPEC L-7)
    - app/util/css_loader.py — `load_global_css` (Task 1 — the methodology page must load the same global CSS as home)
  </read_first>
  <action>
    Create `pages/01_methodology.py`. The filename prefix `01_` controls Streamlit's auto-generated multipage navigation order; the route is `/methodology` (Streamlit strips the `NN_` prefix; underscores become spaces in the nav label but the URL slug uses the lowercase filename without prefix — verify against Streamlit 1.36 docs at execute-time; if the slug differs, rename to `pages/methodology.py` for clean URL).

    Page implementation:
    1. `import streamlit as st` (only `app.py` and `pages/*.py` import Streamlit at module level; the rest of the codebase is Streamlit-independent).
    2. `st.set_page_config(page_title="Methodology · DRHPLens", initial_sidebar_state="collapsed")` (UI-SPEC §Streamlit-Specific Constraints §"pages/01_methodology.py" — suppress the sidebar nav which is bloat for a single-page app).
    3. `from app.util.css_loader import load_global_css; css_html = load_global_css(st.session_state); if css_html: st.markdown(css_html, unsafe_allow_html=True)` — same global CSS load as home.
    4. `from ui.state import init_session_state; init_session_state(st.session_state)` — needed so the persistent footer renders with the same session state shape.
    5. `from ui.copy import METHODOLOGY_STUB_HEADING, METHODOLOGY_STUB_BODY` — verbatim Wave 1 copy.
    6. Render the Display 28px heading: `st.markdown(f'<h1 class="drhp-hero-display">{METHODOLOGY_STUB_HEADING}</h1>', unsafe_allow_html=True)` — uses the same `.drhp-hero-display` class from Task 1 CSS.
    7. Render the body paragraph: `st.markdown(METHODOLOGY_STUB_BODY)` — plain markdown is fine for the body (no special styling needed beyond Streamlit's body defaults which already match the UI-SPEC 16px body).
    8. Render the GitHub link: `st.markdown('[GitHub repository →](https://github.com/{username}/drhplens)', unsafe_allow_html=False)` — Streamlit's native markdown link rendering opens in new tab by default for external URLs as of 1.36. The actual GitHub URL is a placeholder; the executor substitutes the real repo URL discovered from `git remote -v` at implementation time. If no remote exists, use the placeholder string `https://github.com/REPLACE-ME/drhplens` and emit a `## FLAG` note for the Wave 5 deploy planner to update before HF Spaces ship.
    9. Render the back-to-home link: `st.markdown('[← Back to DRHPLens](/)', unsafe_allow_html=False)` — Streamlit multipage routing: the root `/` route is `app.py`.
    10. Render the persistent footer: `from ui.disclaimer import render_persistent_footer; st.markdown(render_persistent_footer(), unsafe_allow_html=True)` — same disclaimer as home (UI-SPEC L-7 §Page structure §5).

    Do NOT render (UI-SPEC §"What NOT to render in Phase 1's stub"):
    - Fake eval scores (no `0.91 faithfulness` placeholder)
    - Methodology pane preview (Phase 3 METHOD-01 owns)
    - Model card stub (Phase 5 FCAST-05 owns)
    - Chat input, hero metadata header, citation chips (these belong to home only)

    Add a top-of-file comment explaining the L-7 contract: "/methodology stub page per UI-SPEC §L-7 — must not 404; Phase 6 LAND-01 replaces with the full recruiter-grade methodology surface."

    Per the source-audit: this task closes the UI-SPEC L-7 stub-link requirement (the methodology link in the persistent footer of `app.py` Task 6 must resolve to a real page, not 404). It does not formally close a phase REQ ID (methodology stub is a UI-SPEC cross-cutting invariant, not a numbered phase REQ — verified in `01-VALIDATION.md` which has no `METHOD-` row in Phase 1).
  </action>
  <verify>
    <automated>test -f pages/01_methodology.py &amp;&amp; python -c "import ast; ast.parse(open('pages/01_methodology.py').read())" &amp;&amp; grep -q 'METHODOLOGY_STUB_HEADING' pages/01_methodology.py &amp;&amp; grep -q 'METHODOLOGY_STUB_BODY' pages/01_methodology.py &amp;&amp; grep -q 'render_persistent_footer' pages/01_methodology.py &amp;&amp; grep -q 'load_global_css' pages/01_methodology.py &amp;&amp; grep -q 'initial_sidebar_state' pages/01_methodology.py &amp;&amp; (! grep -E '0\.[0-9]+ faithfulness' pages/01_methodology.py) &amp;&amp; (! grep -i 'model card' pages/01_methodology.py)</automated>
    <manual>The functional smoke test (Task 8 `scripts/smoke.sh`) curls /methodology and asserts the heading text appears in the rendered HTML — this task is the implementation; the smoke test is the integration verification.</manual>
  </verify>
  <acceptance_criteria>
    - `pages/01_methodology.py` exists; parses as valid Python (no syntax errors)
    - File imports `METHODOLOGY_STUB_HEADING` and `METHODOLOGY_STUB_BODY` from `ui.copy` (verbatim Wave 1 copy)
    - File imports `render_persistent_footer` from `ui.disclaimer` (Task 3 — methodology page renders the same disclaimer)
    - File imports `load_global_css` from `app.util.css_loader` (Task 1 — same global CSS load as home)
    - File calls `st.set_page_config(..., initial_sidebar_state="collapsed")` (suppress sidebar nav for single-page app)
    - File does NOT contain fake eval scores (no regex match for `\d\.\d+ faithfulness`)
    - File does NOT contain `model card` (case-insensitive — Phase 5 owns)
    - File contains a back-to-home link `[← Back to DRHPLens](/)`
    - File contains a GitHub link (placeholder URL acceptable; flagged for Wave 5 to replace before deploy)
  </acceptance_criteria>
  <done>The `/methodology` route is a real page. Recruiter deep-links from a resume don't 404 (UI-SPEC L-7 satisfied). Phase 6 LAND-01 replaces the file in-place — directory layout (per SKELETON §G) stays the same.</done>
</task>

<task type="auto">
  <name>Task 6: app.py — Streamlit entry point wiring hero/metadata/chat-history/input/footer + LangGraph agent invocation</name>
  <files>app.py</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Information Architecture (Cited-Q&A Page)" — top-to-bottom order on mobile: hero (expanded or collapsed) → DRHP metadata header → chat history → question input → empty state (pre-first-question) → persistent footer
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Information Architecture §1 and §1a" — hero expanded vs collapsed state branching on `len(chat_history) == 0`
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Copywriting Contract" — all user-visible strings come from `ui/copy.py` constants (Wave 1)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Loading / Empty / Error States" — empty state, cold-start, generating, refusal-banner-style infra errors
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Disclaimer Surfaces §1" + the `st.dialog` code example in 01-RESEARCH.md §"Streamlit `st.dialog` first-use modal" (lines 729-754) — the modal wiring pattern
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Streamlit-Specific Constraints" — implementation paths for each surface (chat_input, chat_message, expander, dialog, status)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md §C — the LangGraph topology this app.py invokes via `build_graph().invoke(...)`
    - agent/graph.py — the `build_graph` factory + the `GraphState` invocation shape (Wave 3 contract)
    - agent/schemas.py — `GroundedAnswer`, `RefusalResponse` (the two assistant-turn types)
    - ui/copy.py (Wave 1) — every copy constant
    - ui/disclaimer.py (Tasks 3 of this plan) — `render_first_use_modal`, `render_persistent_footer`, `render_per_answer_footer`
    - ui/chip.py + ui/expander.py (Task 2) — `render_answer_with_chips`, `render_citation_expanders`
    - ui/refusal_banner.py (Task 3) — `render_refusal_banner`
    - ui/state.py (Task 4) — session-state helpers
    - app/util/css_loader.py (Task 1) — `load_global_css`
  </read_first>
  <action>
    Implement `app.py` as the Streamlit entry. Top-level structure (linear; each section is a clearly-named Python function called once from a top-level `main()`):

    1. **Page config (must be first Streamlit call):**
       `st.set_page_config(page_title="DRHPLens · Ask about Swiggy", page_icon=None, layout="centered", initial_sidebar_state="collapsed")` — `centered` matches UI-SPEC §Mobile Responsive desktop max-width 720px centered; `collapsed` sidebar suppresses Streamlit's auto-generated nav. No emoji icon (Phase 1 is emoji-free per UI-SPEC §Design System §Icon library).

    2. **Set HTML lang attribute** for screen readers (UI-SPEC Accessibility §Language `<html lang="en-IN">`): Streamlit 1.36 does not expose a config for `lang`. Workaround: `st.markdown('<script>document.documentElement.lang = "en-IN";</script>', unsafe_allow_html=True)` — note this runs after first render but is acceptable for SR announcement on subsequent interactions. Document as a Phase 6 polish item (`## FLAG-LANG-ATTR` — Phase 6 may migrate to a Streamlit-Components solution or to Next.js).

    3. **Load global CSS:**
       `css_html = load_global_css(st.session_state); if css_html: st.markdown(css_html, unsafe_allow_html=True)` — Task 1's idempotent injector. After this call, every component class (`.drhp-cite`, `.drhp-refusal`, etc.) is available.

    4. **Initialize session state:**
       `init_session_state(st.session_state)` — Task 4 helper.

    5. **First-use modal gate (UI-SPEC §Disclaimer Surfaces §1):**
       ```
       @st.dialog(MODAL_HEADING)  # MODAL_HEADING from compliance.disclaimer_text per Wave 1
       def _first_use_modal():
           data = render_first_use_modal()
           st.markdown(data["body"])
           if st.button(data["cta_text"], type="primary", use_container_width=True):
               mark_modal_seen(st.session_state)
               st.rerun()
       if not has_seen_modal(st.session_state):
           _first_use_modal()
           st.stop()
       ```
       The `st.stop()` after the modal call ensures the rest of the page does not render until the user clicks "I understand" — UI-SPEC §Disclaimer Surfaces §1 "Acceptance is intentional, single-step". No close X icon (UI-SPEC anti-pattern).

    6. **Hero block (UI-SPEC §IA §1 expanded vs §1a collapsed):**
       ```
       history = get_chat_history(st.session_state)
       if len(history) == 0:
           # Expanded hero
           st.markdown(f'<h1 class="drhp-hero-display">{HERO_HEADING}</h1>', unsafe_allow_html=True)
           st.markdown(f'<p class="drhp-hero-subheading">{HERO_SUBHEADING}</p>', unsafe_allow_html=True)
       else:
           # Collapsed hero (single line, body 16px, no Display heading)
           st.markdown(f'<p class="drhp-hero-collapsed">{HERO_HEADING}</p>', unsafe_allow_html=True)
       ```
       Per UI-SPEC §IA §1a: "transition is instant swap on first message submit; a half-second crossfade is `prefers-reduced-motion`-respecting Phase 6 polish." Phase 1 ships instant swap. No JS, no animation.

    7. **DRHP metadata header (UI-SPEC §IA §2):** Hand-curated Swiggy metadata (per CONTEXT D-08 Claude's Discretion + 01-SKELETON.md §A "data/swiggy_drhp/" canonical artifacts):
       ```
       st.markdown('''
       <div class="drhp-metadata-header">
         <span class="drhp-metadata-label">Issuer ·</span> <span class="drhp-metadata-value">Swiggy Limited</span> &nbsp;·&nbsp;
         <span class="drhp-metadata-label">Issue size ·</span> <span class="drhp-metadata-value">₹11,327 cr</span> &nbsp;·&nbsp;
         <span class="drhp-metadata-label">Listed ·</span> <span class="drhp-metadata-value">Nov 2024</span> &nbsp;·&nbsp;
         <span class="drhp-metadata-label">DRHP source ·</span> <a class="drhp-footer-link" href="{SEBI_PROSPECTUS_URL}" target="_blank" rel="noopener">SEBI</a>
       </div>
       ''', unsafe_allow_html=True)
       ```
       The issue size `₹11,327 cr` is the lakh/crore notation locked in UI-SPEC §Typography "lakh/crore notation already in Phase 1 for issue size only (low-effort, matches mental model; full UI-04 lands in Phase 4)". The exact number is reading from the actual Swiggy prospectus — the executor verifies against the committed PDF at implementation time and emits a `## FLAG-ISSUE-SIZE` note if the PDF shows a different number (do NOT silently use a placeholder).

       `target="_blank" rel="noopener"` on the SEBI link is the standard external-link safety pattern per ASVS V12 "Files & Resources" — prevents tabnabbing.

    8. **Chat history rendering (UI-SPEC §IA §3):**
       ```
       for turn in history:
           with st.chat_message(turn["role"]):
               if turn["role"] == "user":
                   # User turn: plain text in right-aligned Surface-secondary bubble (Streamlit's chat_message handles the bubble; we just emit the content)
                   st.markdown(html.escape(turn["content"]))
               elif turn["role"] == "assistant":
                   content = turn["content"]
                   if isinstance(content, GroundedAnswer):
                       # Render prose with citation chips
                       rendered_html, chip_map = render_answer_with_chips(content)
                       st.markdown(rendered_html, unsafe_allow_html=True)
                       # Render one expander per unique citation
                       expanders = render_citation_expanders(content, chip_map)
                       for exp in expanders:
                           with st.expander(exp["label"], expanded=False):
                               st.markdown(f'<div class="drhp-snippet">{exp["snippet"]}</div>', unsafe_allow_html=True)
                               st.markdown(f'[View DRHP page {exp["page_start"]} on SEBI →]({exp["source_url"]})')
                               st.markdown(f'<div class="drhp-snippet-metadata">{exp["metadata_footer"]}</div>', unsafe_allow_html=True)
                       # Per-answer disclaimer footer
                       st.markdown(render_per_answer_footer(), unsafe_allow_html=True)
                   elif isinstance(content, RefusalResponse):
                       # Render refusal banner (has its OWN per-answer footer baked in — do NOT also append render_per_answer_footer)
                       st.markdown(render_refusal_banner(content), unsafe_allow_html=True)
                       # Reformulation chip click handlers (Streamlit buttons in two-row layout per UI-SPEC §Streamlit-Specific Constraints reformulation row)
                       if content.reason != "banned_token" and content.reformulation_suggestions:
                           cols = st.columns(min(len(content.reformulation_suggestions), MAX_CHIPS_RENDERED))
                           for i, suggestion in enumerate(content.reformulation_suggestions[:MAX_CHIPS_RENDERED]):
                               with cols[i]:
                                   if st.button(suggestion, key=f"reformulate_{turn_index}_{i}", use_container_width=True):
                                       st.session_state.draft_question = suggestion
                                       st.rerun()
       ```
       The pattern of "visual chip in HTML + functional click in `st.button`" is the UI-SPEC §Streamlit-Specific Constraints reformulation-chip row contract. The CSS in Task 1 styles `[data-testid="stButton"] button` to match `.drhp-suggest` visuals so the two rows look like one.

    9. **Empty state (UI-SPEC §IA §5):** Renders only when `len(history) == 0`:
       ```
       if len(history) == 0:
           st.markdown(f'<h2 class="drhp-empty-heading">{EMPTY_STATE_HEADING}</h2>', unsafe_allow_html=True)
           st.markdown(EMPTY_STATE_BODY)
           # Three example-question chips that fill input on click (UI-SPEC §IA §5)
           example_questions = [
               "What does Swiggy say about its path to profitability?",
               "Who are the promoters and what is their post-issue holding?",
               "What is the use of proceeds breakdown?",
           ]
           cols = st.columns(3)
           for i, q in enumerate(example_questions):
               with cols[i]:
                   if st.button(q, key=f"example_{i}", use_container_width=True):
                       st.session_state.draft_question = q
                       st.rerun()
       ```
       UI-SPEC §IA §5: "clicking fills the input, does not auto-submit — user retains agency." The `draft_question` session state is read by `st.chat_input` below.

    10. **Question input (UI-SPEC §IA §4 + Streamlit §chat_input):**
        ```
        # st.chat_input does NOT support default-value pre-fill in 1.36; use a draft_question session state pattern instead — the user sees the suggestion in a small helper line above the input, NOT pre-filled in the input box.
        if st.session_state.get("draft_question"):
            st.caption(f'Suggested: "{st.session_state.draft_question}" — paste into the box below or type your own.')
        question = st.chat_input(placeholder=QUESTION_PLACEHOLDER)
        if question:
            st.session_state.draft_question = ""  # clear after use
            # Append user turn
            append_to_chat_history(st.session_state, role="user", content=question)
            # Invoke the agent with st.status loading state per UI-SPEC §Loading/Empty/Error
            with st.status(LOADING_ANSWER_COPY, state="running") as status:
                try:
                    graph = build_graph()
                    result_state = graph.invoke({
                        "question": question,
                        "regenerate_attempts": 0,
                    })
                    # Result is GraphState dict; extract grounded_answer or refusal
                    if result_state.get("grounded_answer") is not None:
                        assistant_content = result_state["grounded_answer"]
                    elif result_state.get("refusal") is not None:
                        assistant_content = result_state["refusal"]
                    else:
                        # Safety net: agent returned neither — synthesize an infra refusal
                        assistant_content = RefusalResponse(
                            reason="infrastructure_error",
                            message=ERROR_LLM_TIMEOUT,
                            reformulation_suggestions=[],
                        )
                    status.update(label="Done.", state="complete")
                except Exception as e:
                    logging.exception("Agent invocation failed")
                    assistant_content = RefusalResponse(
                        reason="infrastructure_error",
                        message=ERROR_QDRANT_UNREACHABLE if "qdrant" in str(e).lower() else ERROR_LLM_TIMEOUT,
                        reformulation_suggestions=[],
                    )
                    status.update(label="Failed.", state="error")
            append_to_chat_history(st.session_state, role="assistant", content=assistant_content)
            st.rerun()
        ```
        The `st.status` container per UI-SPEC §Loading/Empty/Error "Generating an answer" row. `st.rerun()` after appending so the new turn renders without waiting for next interaction.

    11. **Persistent footer (UI-SPEC §IA §6):**
        `st.markdown(render_persistent_footer(), unsafe_allow_html=True)` — Task 3 renderer.

    **Wiring summary — every import statement at module level:**
    ```
    import html
    import logging
    import streamlit as st
    from app.util.css_loader import load_global_css
    from agent.graph import build_graph
    from agent.schemas import GroundedAnswer, RefusalResponse
    from compliance.disclaimer_text import MODAL_HEADING
    from ui.chip import render_answer_with_chips
    from ui.copy import (
        HERO_HEADING, HERO_SUBHEADING, QUESTION_PLACEHOLDER,
        EMPTY_STATE_HEADING, EMPTY_STATE_BODY,
        LOADING_ANSWER_COPY, ERROR_QDRANT_UNREACHABLE, ERROR_LLM_TIMEOUT,
    )
    from ui.disclaimer import render_first_use_modal, render_persistent_footer, render_per_answer_footer
    from ui.expander import render_citation_expanders, SEBI_PROSPECTUS_URL
    from ui.refusal_banner import render_refusal_banner, MAX_CHIPS_RENDERED
    from ui.state import (
        init_session_state, has_seen_modal, mark_modal_seen,
        append_to_chat_history, get_chat_history,
    )
    ```

    Structure the code as one `main()` function with the 11 sub-functions called in order; guard with `if __name__ == "__main__":` (Streamlit runs the module top-to-bottom; this guard is defensive and harmless).

    **Anti-pattern enforcement (UI-SPEC anti-patterns explicit comments in code):**
    - Comment near refusal-banner rendering: `# UI-SPEC anti-pattern: do NOT replace render_refusal_banner with st.warning or st.error — refusals are first-class output, not chrome.`
    - Comment near accent-color uses: `# UI-SPEC §Color: accent #1E40AF is reserved for chips + chip focus + methodology link + chat-input focus + modal CTA. Do not paint other interactive elements this color.`
    - Comment near hero-collapsed: `# UI-SPEC §IA §1a: instant swap, no animation. prefers-reduced-motion already respected via Task 1 CSS.`

    Per the source-audit: this task closes the wiring side of UI-01 (mobile-responsive web — every CSS class from Task 1 is actually applied here) and UI-02 (citation chips clickable — Task 2's renderer is invoked, expanders pair with chips). The chat flow exercises the Wave 3 LangGraph agent end-to-end, exercising RAG-01/02/03 + TRUST-04 visually (those REQs are formally closed by Plan 04 — this task is the public-facing surface).
  </action>
  <threat_model>
    | Threat ID | Category | Component | Disposition | Mitigation Plan |
    |-----------|----------|-----------|-------------|-----------------|
    | T-1-06 | XSS via `unsafe_allow_html` | `app.py` rendering loop | mitigate | Every dynamic string interpolated into `st.markdown(..., unsafe_allow_html=True)` is HTML-escaped at its renderer source (Task 2 chip/expander; Task 3 refusal banner; Task 3 disclaimer surfaces); `app.py` itself does not interpolate untrusted strings into HTML at the page level |
    | T-1-01 | Prompt injection via user question | LangGraph agent invocation | mitigate (defense in depth) | Wave 3's scrubber + cite-check are the primary defenses; `app.py` adds a structural fallback: any agent exception or null result synthesizes an `infrastructure_error` refusal (no raw error messages reach the user; user-visible copy is from `ui/copy.py` only) |
    | T-1-04 | Information disclosure (Streamlit exposes secrets in `st.session_state` to client) | session state contains only chat history + flags; no API keys | mitigate | `st.session_state` is server-side per Streamlit architecture; but: assertion that no agent-returned exception message reaches the user (`except Exception as e: ... ERROR_LLM_TIMEOUT`) — the raw `str(e)` is logged via `logging.exception` (server-side), not rendered |
    | T-1-13 | Tabnabbing on external SEBI link | `target="_blank"` on SEBI metadata link | mitigate | `rel="noopener"` accompanies every `target="_blank"` per ASVS V12 |
  </threat_model>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app.py').read())" &amp;&amp; grep -q 'from agent.graph import build_graph' app.py &amp;&amp; grep -q 'from ui.chip import render_answer_with_chips' app.py &amp;&amp; grep -q 'from ui.disclaimer import' app.py &amp;&amp; grep -q 'from ui.refusal_banner import render_refusal_banner' app.py &amp;&amp; grep -q 'from app.util.css_loader import load_global_css' app.py &amp;&amp; grep -q 'st.set_page_config' app.py &amp;&amp; grep -q 'st.chat_input' app.py &amp;&amp; grep -q 'st.chat_message' app.py &amp;&amp; grep -q 'st.status' app.py &amp;&amp; (! grep -E 'st\.warning\(.*refus|st\.error\(.*refus' app.py) &amp;&amp; grep -q 'rel="noopener"' app.py &amp;&amp; grep -q 'init_session_state' app.py &amp;&amp; grep -q 'has_seen_modal' app.py</automated>
    <manual>Functional smoke is Task 8 (`scripts/smoke.sh`). The interactive citation-expand behavior is in `tests/manual/CITATION_INTERACTION.md` (Task 7).</manual>
  </verify>
  <acceptance_criteria>
    - `app.py` parses as valid Python (no syntax errors)
    - File imports `build_graph` from `agent.graph` (Wave 3 contract)
    - File imports the four new Task-3 functions: `render_first_use_modal`, `render_persistent_footer`, `render_per_answer_footer`, `render_refusal_banner`
    - File imports `render_answer_with_chips` (Task 2), `render_citation_expanders` (Task 2)
    - File imports `load_global_css` (Task 1) AND `init_session_state` (Task 4)
    - File calls `st.set_page_config` as the first Streamlit call
    - File uses `st.chat_input` (UI-SPEC §IA §4) and `st.chat_message` (UI-SPEC §IA §3) and `st.status` (UI-SPEC §Loading)
    - File does NOT use `st.warning` or `st.error` for refusals (UI-SPEC anti-pattern; grep absence)
    - File uses `rel="noopener"` on every `target="_blank"` external link (ASVS V12)
    - File guards every agent invocation with try/except and synthesizes an `infrastructure_error` refusal on exception (T-1-04 — raw error messages do NOT reach the user)
    - File uses session-state helpers (init_session_state, has_seen_modal, mark_modal_seen) — does NOT directly mutate `st.session_state["chat_history"]` outside helpers (consistency contract from Task 4)
    - Implements REQ UI-01 (mobile-responsive web) and REQ UI-02 (citation chips clickable) wiring
  </acceptance_criteria>
  <done>The Streamlit UI is fully wired. A user can open the app, see the first-use modal once per session, dismiss it, see the hero + Swiggy metadata header, ask a question via `st.chat_input`, see the LangGraph agent's grounded answer with clickable citation chips (Task 2 renderer) + inline expanders (Task 2 expander) + per-answer disclaimer (Task 3) — OR see the refusal banner (Task 3) with reformulation chips on Gate 1 / cite-check / banned-token failure paths. The persistent footer (Task 3) sticks to the bottom on mobile (Task 1 CSS). UI-01 + UI-02 are visually live.</done>
</task>

<task type="auto">
  <name>Task 7: Manual test script for citation expand interaction (tests/manual/CITATION_INTERACTION.md)</name>
  <files>tests/manual/CITATION_INTERACTION.md, tests/manual/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md §"Manual-Only Verifications" — rows for citation chip click, first-use modal session-state, cold-start UX, mobile responsive 375/640/1024
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Citation Chip Contract" §"States" + §"Expander reveal content (per chip)"
  </read_first>
  <action>
    Create `tests/manual/.gitkeep` (the `tests/manual/` directory does not exist yet; create it with a gitkeep to make `git add` work).

    Create `tests/manual/CITATION_INTERACTION.md` as a hand-runnable test script for the manual-only behaviors declared in `01-VALIDATION.md`. The script is read by the executor at end-of-Wave-4 verification and by the gsd-checker before sign-off. Document structure:

    Section 1: Setup
    - "Run `streamlit run app.py` from the repo root on a fresh terminal."
    - "Wait for the URL to appear (typically `http://localhost:8501`). Open in a fresh browser tab (or incognito window to ensure no prior session state)."

    Section 2: First-use modal (UI-VALIDATION row TRUST-01)
    - Step 1: "On first visit, the modal `Read this once.` appears immediately, the rest of the page is non-interactive (greyed out)."
    - Step 2: "Modal contains the anchor disclaimer copy AND the AI-disclosure sentence beginning `The system uses large language models…`."
    - Step 3: "Modal contains ONE button labelled `I understand — open DRHPLens` (no Decline button, no close X)."
    - Step 4: "Click the button. Modal closes. The hero, DRHP metadata header, empty state, chat input, and persistent footer all become visible."
    - Step 5: "Press F5 to refresh. Modal does NOT re-appear (session-state persisted within the same browser tab)."
    - Step 6: "Close the tab. Open a new tab to the same URL. Modal RE-appears (session-state is tab-scoped per Wave 1 known limitation + RESEARCH.md §Streamlit st.dialog 'Known limitation' note)."

    Section 3: Citation chip click + inline expand (UI-VALIDATION row UI-02)
    - Step 1: "Ask: `What is the use of proceeds breakdown?`"
    - Step 2: "Wait for the answer to render (the `st.status` container shows `Reading Swiggy's DRHP…` while LLM is in flight)."
    - Step 3: "The answer prose contains one or more superscript chips like `[1]` styled as small white-on-slate-indigo pills."
    - Step 4: "Hover the `[1]` chip — background darkens to a slightly darker indigo + the bracketed number gains an underline."
    - Step 5: "Click the `[1]` chip — its corresponding `st.expander` below the answer opens, revealing the verbatim DRHP source-text snippet (italic, 14px) + a link `View DRHP page N on SEBI →`."
    - Step 6: "Click the `View DRHP page N on SEBI →` link — a new browser tab opens at the SEBI Swiggy Prospectus URL. The PDF should scroll to (or be deep-linkable to via `#page=N`) the cited page."
    - Step 7: "Per-answer disclaimer `Informational only — not advice.` appears at the bottom of the answer block, italic, muted."

    Section 4: Refusal flow + reformulation chips (UI-VALIDATION row RAG-03)
    - Step 1: "Ask: `What does Swiggy say about Mars colonization?` (off-topic — Gate 1 triggers)."
    - Step 2: "Expect: the refusal banner renders in place of where an answer would have been. Background is amber (`#FEF3C7`), 4px left border in amber-600 (`#D97706`), heading `This isn't in the DRHP.` in amber-900."
    - Step 3: "Up to 2 reformulation chips appear below the body, styled as light-grey rounded buttons (`#F4F5F7` background)."
    - Step 4: "Click one chip — the suggested question pre-fills the chat-input area as a `Suggested: \"…\"` helper line. The chat input is NOT auto-submitted (UI-SPEC §IA §5 — user retains agency)."
    - Step 5: "Ask: `Should I subscribe to Swiggy?` — banned token triggers post-LLM scrubber → regenerate-once → refuse. Expect: refusal banner with banned-token copy `Couldn't return that answer because it would have implied a recommendation…`. NO reformulation chips (UI-SPEC anti-pattern explicit)."

    Section 5: Mobile responsive (UI-VALIDATION row UI-01)
    - "Open Chrome DevTools (`Cmd+Option+I` on macOS, `Ctrl+Shift+I` on Windows/Linux), toggle device emulation (`Cmd+Shift+M` or `Ctrl+Shift+M`)."
    - Step 1: "Set viewport to 375 × 667 (iPhone SE). Verify: hero stacks; DRHP metadata header wraps to multi-line; chat input stays sticky at bottom; persistent footer stays sticky at bottom (above the device home indicator); citation chips remain tappable (try clicking with the mouse — tap target should feel comfortable)."
    - Step 2: "Set viewport to 640 × 800 (tablet portrait). Verify: DRHP metadata header now single-row; horizontal padding visible on the sides."
    - Step 3: "Set viewport to 1024 × 768. Verify: content max-width 720px, centered; persistent footer is `position: relative` (scrolls with content) not sticky."

    Section 6: Methodology stub (UI-VALIDATION L-7)
    - Step 1: "Click the `methodology` link in the persistent footer."
    - Step 2: "URL becomes `http://localhost:8501/methodology`. Page does NOT 404."
    - Step 3: "Page shows: Display 28px heading `Methodology`; body paragraph from `METHODOLOGY_STUB_BODY`; a `GitHub repository →` link; a `← Back to DRHPLens` link; same persistent footer at bottom."
    - Step 4: "Click `← Back to DRHPLens`. URL returns to `/`. Chat history is preserved (session-scoped)."

    Section 7: Accessibility quick-check (UI-VALIDATION WCAG AAA)
    - Step 1: "On the home page, press `Tab` repeatedly. Focus indicators (2px slate-indigo ring) appear on: chat input (when focused), citation chips (when an answer is present), suggestion buttons, persistent-footer methodology link."
    - Step 2: "On a focused citation chip, press `Enter` — the corresponding expander opens. Press `Tab` — focus moves to the next interactive element in document order."
    - Step 3: "If you have a screen reader (VoiceOver on macOS: `Cmd+F5`), navigate to a citation chip — it should announce `link, citation 1, opens source-text panel` (per UI-SPEC Accessibility §Screen reader)."

    Each section ends with: "PASS / FAIL — note any deviations and link to the relevant UI-SPEC section." The script is meant to be filled in by the developer running the test before they sign off on the wave.

    Per the source-audit: this task covers the 4 manual-only rows in `01-VALIDATION.md §Manual-Only Verifications` that depend on Wave 4 surfaces (citation expand, first-use modal session-state, mobile responsive, cold-start UX defers to Wave 5 deploy).
  </action>
  <verify>
    <automated>test -f tests/manual/CITATION_INTERACTION.md &amp;&amp; test -f tests/manual/.gitkeep &amp;&amp; test $(wc -l &lt; tests/manual/CITATION_INTERACTION.md) -ge 50 &amp;&amp; grep -q 'First-use modal' tests/manual/CITATION_INTERACTION.md &amp;&amp; grep -q 'Citation chip' tests/manual/CITATION_INTERACTION.md &amp;&amp; grep -q 'Refusal' tests/manual/CITATION_INTERACTION.md &amp;&amp; grep -q '375' tests/manual/CITATION_INTERACTION.md &amp;&amp; grep -q '640' tests/manual/CITATION_INTERACTION.md &amp;&amp; grep -q '1024' tests/manual/CITATION_INTERACTION.md &amp;&amp; grep -q '/methodology' tests/manual/CITATION_INTERACTION.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/manual/CITATION_INTERACTION.md` exists with ≥ 50 lines
    - File covers all 4 manual-only rows from `01-VALIDATION.md §Manual-Only Verifications` that depend on Wave 4 (citation expand, first-use modal session-state, mobile responsive at 375/640/1024, methodology stub link resolves)
    - File includes accessibility quick-check (Tab navigation, focus indicators, screen-reader announcement)
    - `tests/manual/.gitkeep` exists so the directory tracks in git
  </acceptance_criteria>
  <done>The manual-only verification surface is documented in a hand-runnable script. The gsd-checker reads this file before Wave 4 sign-off. Phase 6 LAND-01 inherits the same script for regression-checking the recruiter landing experience.</done>
</task>

<task type="auto">
  <name>Task 8: Smoke test script — boot Streamlit, curl home + /methodology, assert first-meaningful-paint copy</name>
  <files>scripts/smoke.sh, scripts/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md row `1-04-streamlit-app-smoke` — the locked verification contract this task closes (`bash scripts/smoke.sh`)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Copywriting Contract" — the hero heading text `Ask DRHPLens about Swiggy.` AND the methodology stub heading `Methodology` — both must appear in the rendered HTML
  </read_first>
  <action>
    Create `scripts/.gitkeep` if `scripts/` directory does not yet exist.

    Implement `scripts/smoke.sh` (`#!/usr/bin/env bash`, `set -euo pipefail`):

    ```
    #!/usr/bin/env bash
    set -euo pipefail

    # Phase 1 Wave 4 smoke test — closes 01-VALIDATION.md row `1-04-streamlit-app-smoke` (UI-01 + UI-02 + TRUST-01).
    # Boots Streamlit in the background, curls home + /methodology, asserts first-meaningful-paint copy
    # appears in the rendered HTML. Tears down cleanly on success or failure.

    PORT="${SMOKE_PORT:-8501}"
    LOGFILE="${SMOKE_LOG:-/tmp/drhplens_smoke.log}"
    PID_FILE="/tmp/drhplens_smoke.pid"

    cleanup() {
      if [[ -f "$PID_FILE" ]]; then
        local pid
        pid=$(cat "$PID_FILE")
        if kill -0 "$pid" 2>/dev/null; then
          kill "$pid" 2>/dev/null || true
          # Give Streamlit a moment to terminate gracefully
          for _ in 1 2 3 4 5; do
            kill -0 "$pid" 2>/dev/null || break
            sleep 0.2
          done
          kill -9 "$pid" 2>/dev/null || true
        fi
        rm -f "$PID_FILE"
      fi
    }
    trap cleanup EXIT INT TERM

    echo "Booting Streamlit on port $PORT..."
    streamlit run app.py \
      --server.port "$PORT" \
      --server.headless true \
      --server.address 127.0.0.1 \
      --browser.gatherUsageStats false \
      > "$LOGFILE" 2>&1 &
    echo $! > "$PID_FILE"

    # Wait up to 30s for first response (cold-start tolerance per UI-SPEC §Loading "Warming up" 30-60s allowance)
    READY=0
    for attempt in $(seq 1 60); do
      if curl -sf "http://127.0.0.1:$PORT/" -o /dev/null 2>/dev/null; then
        READY=1
        break
      fi
      sleep 0.5
    done

    if [[ "$READY" -ne 1 ]]; then
      echo "FAIL: Streamlit did not become ready within 30s."
      echo "--- Streamlit log ---"
      tail -50 "$LOGFILE"
      exit 1
    fi

    echo "Streamlit is up. Checking home page..."
    HOME_BODY=$(curl -sf "http://127.0.0.1:$PORT/")
    # The Streamlit-rendered HTML for the home page MUST contain (in the body or component-tree JSON):
    #  - the hero heading text (UI-SPEC Copywriting Contract row "Home page hero heading")
    # Note: Streamlit renders content via WebSocket-fed component tree, so the initial HTML may not contain
    # the literal hero text — instead it contains the Streamlit boot HTML. We assert the page returns 200
    # AND contains the page title `DRHPLens · Ask about Swiggy` (set via st.set_page_config in app.py Task 6).
    if ! echo "$HOME_BODY" | grep -q 'DRHPLens'; then
      echo "FAIL: home page does not contain 'DRHPLens' in rendered HTML."
      echo "--- HTML head ---"
      echo "$HOME_BODY" | head -50
      exit 1
    fi

    echo "Checking /methodology page..."
    METH_BODY=$(curl -sf "http://127.0.0.1:$PORT/methodology")
    if ! echo "$METH_BODY" | grep -q 'Methodology'; then
      echo "FAIL: /methodology page does not contain 'Methodology' in rendered HTML."
      echo "--- HTML head ---"
      echo "$METH_BODY" | head -50
      exit 1
    fi

    echo "PASS: Streamlit boots; home + /methodology both return 200 with expected copy."
    exit 0
    ```

    Make the file executable: `chmod +x scripts/smoke.sh` (Streamlit `bash scripts/smoke.sh` runs the script via bash regardless of executable bit, but setting +x is convention).

    **Documented limitation:** Streamlit serves the app via a WebSocket-driven component tree. The initial HTTP response for `/` is the Streamlit boot shell HTML — it contains the `<title>DRHPLens · Ask about Swiggy</title>` (from `st.set_page_config`) but NOT the rendered hero heading text. Asserting `DRHPLens` (which is in the page title) is the closest signal we can get from a single `curl` call without driving the WebSocket. A deeper check (e.g., headless Chrome with Playwright) is a Phase 6 polish item — Phase 1's smoke is `boots cleanly + returns 200 + title present + /methodology resolves`. The manual test script (Task 7) covers the deeper interactive verifications.

    Per the source-audit: this task closes `01-VALIDATION.md` row `1-04-streamlit-app-smoke` (Wave 4 manual-but-automatable row). The smoke is run by the executor at end-of-Wave-4 sign-off and by `/gsd-verify-work` before phase close.
  </action>
  <verify>
    <automated>test -x scripts/smoke.sh &amp;&amp; bash -n scripts/smoke.sh &amp;&amp; head -5 scripts/smoke.sh | grep -q '#!/usr/bin/env bash' &amp;&amp; grep -q 'set -euo pipefail' scripts/smoke.sh &amp;&amp; grep -q 'streamlit run app.py' scripts/smoke.sh &amp;&amp; grep -q 'curl' scripts/smoke.sh &amp;&amp; grep -q '/methodology' scripts/smoke.sh &amp;&amp; grep -q 'trap cleanup' scripts/smoke.sh</automated>
    <manual>The functional run-the-smoke verification (`bash scripts/smoke.sh` from a fresh shell after `uv pip install -e .[dev]`) is part of the end-of-Wave-4 sign-off — the executor runs it manually as a Wave 4 close gate.</manual>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke.sh` exists and is executable (`-x` permission)
    - File parses as valid bash (`bash -n scripts/smoke.sh` exits 0)
    - File uses `set -euo pipefail` (defensive bash)
    - File boots Streamlit on a configurable port (default 8501) in the background
    - File registers a `trap cleanup EXIT INT TERM` to tear down the Streamlit process on script exit
    - File curls home (`/`) and asserts `DRHPLens` appears in the rendered HTML
    - File curls `/methodology` and asserts `Methodology` appears in the rendered HTML
    - File times out after 30 seconds if Streamlit fails to become ready
    - File tail-prints the Streamlit log on failure for debuggability
  </acceptance_criteria>
  <done>The smoke test is in place. The executor runs `bash scripts/smoke.sh` at end of Wave 4 as the close gate. `01-VALIDATION.md` row `1-04-streamlit-app-smoke` flips from `❌ W0` to `✅ green`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Wave 4 — Streamlit UI surface)

| Boundary | Description |
|----------|-------------|
| user browser → Streamlit server | HTTP + WebSocket; XSS surface via `unsafe_allow_html` |
| LLM output → rendered HTML | Prose, claim texts, reformulation suggestions all reach `st.markdown(..., unsafe_allow_html=True)` |
| DRHP-sourced snippet text → rendered HTML | Cited spans may contain HTML-meaningful characters; escape boundary at renderer |
| user question text → rendered chat-message | Echoed via `st.chat_message` user role; Streamlit's default rendering is safe but defensive `html.escape` is applied |
| external link (SEBI prospectus URL) → new browser tab | Tabnabbing surface; `rel="noopener"` required per ASVS V12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Cross-Site Scripting via `unsafe_allow_html` (the Streamlit-level entrypoint for XSS) | `ui/chip.py::render_answer_with_chips`, `ui/expander.py::render_citation_expanders`, `ui/refusal_banner.py::render_refusal_banner`, `app.py::user-turn rendering` | mitigate | `html.escape(text, quote=True)` applied at EVERY renderer boundary BEFORE chip/expander/banner interpolation; defense-in-depth `html.escape` on `claim_id` even though regex-validated; unit tests `test_renderer_emits_sup_chip_html_and_escapes_xss`, `test_render_refusal_banner_escapes_xss_in_message_and_suggestions` enforce |
| T-1-02 | Prompt injection via DRHP content surfacing as advisory language in rendered output | escape-at-renderer + Wave 3 scrubber + cite-check + Wave 3 generate-prompt instruction "neutral description of any advisory language in the DRHP" | mitigate | Layered: scrubber rejects banned tokens in LLM output; cite-check ratio ≥ 80 rejects answers where claim text doesn't match window; renderer escape closes the visual XSS path; Wave 3 `tests/integration/test_drhp_prompt_injection.py` is the integration-level gate |
| T-1-04 | Information disclosure via raw exception messages reaching the user | `app.py` agent-invocation try/except | mitigate | Every `except Exception as e:` synthesizes an `infrastructure_error` `RefusalResponse` with user-facing copy from `ui/copy.py`; `str(e)` is logged server-side via `logging.exception` but never rendered |
| T-1-06-SS | Cross-Site Scripting via Streamlit session_state echo | `st.session_state.draft_question` is rendered as `st.caption(f'Suggested: "{...}"')` | mitigate | The `draft_question` value is set ONLY from server-trusted sources (example_questions list constants OR `reformulation_suggestions` from agent output which the agent itself produces deterministically from retrieved DRHP section names — these are sanitized at the agent boundary in Wave 3); user-input draft_question is not a path in Phase 1 |
| T-1-13 | Tabnabbing via external SEBI PDF link in new tab | `target="_blank"` external links in DRHP metadata header + citation expander source-link | mitigate | Every `target="_blank"` accompanied by `rel="noopener"` (ASVS V12); enforced at the link-construction site in `app.py` Task 6 and `ui/expander.py` Task 2 |
| T-1-08 | Banned-token regression in any Wave 4 user-visible string | `tests/unit/test_copy_no_banned_tokens.py` extensions for `ui.refusal_banner` module strings + the existing Wave 1 import-time scrubber check on `ui/copy.py` | mitigate | Wave 1's import-time `ui/copy.py` scrubber assertion already covers the home-page user-visible strings; Wave 4 Task 3 adds module-constant scrubber tests for `ui.refusal_banner` heading constants; no new copy in Wave 4 escapes the scrubber gate |
| T-1-11 | Claim_id drift between agent output and rendered chip (cross-phase contract violation) | `test_round_trip_claim_id_through_renderer` byte-for-byte assertion | mitigate | Renderer never derives a new claim_id; only echoes the schema-validated `Claim.claim_id` into the chip's `data-claim-id` attribute |
| T-1-12 | Compliance regression: per-answer disclaimer doubles inside refusal banner | static-source grep test in `test_per_answer_footer_does_not_render_inside_refusal_banner` | mitigate | Import-graph assertion enforces the UI-SPEC §Disclaimer Surfaces §3 rule structurally |
| T-1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; Wave 0 (Plan 01) closed the legitimacy audit |
</threat_model>

<verification>
- All four new test files pass: `pytest tests/unit/test_css_loader.py tests/unit/test_citation_renderer.py tests/unit/test_disclaimer_surface_render.py tests/unit/test_refusal_banner.py tests/unit/test_session_state.py -x -q --timeout=10` exits 0 with 0 failures
- Wave 1's `tests/unit/test_disclaimer_surface.py` and `tests/unit/test_copy_no_banned_tokens.py` still pass (backwards-compatibility regression guard)
- Wave 3's `pytest tests/integration -x -q --timeout=60` still passes (Wave 4 polish does NOT regress the Walking Skeleton)
- `python -m agent.demo "What is Swiggy's issue size?"` still returns a `GroundedAnswer` (Walking Skeleton survives Wave 4)
- `bash scripts/smoke.sh` exits 0 (Streamlit boots; home + /methodology both return 200 with expected copy)
- `python -c "import ast; ast.parse(open('app.py').read())"` exits 0 (app.py is syntactically valid)
- `grep -L '#B91C1C' app/static/drhplens.css` returns the filename (i.e., destructive red is NOT present — UI-SPEC §Color anti-pattern check)
- `grep -L 'st.warning' app.py` AND `grep -L 'st.error' app.py` return the filename (i.e., UI-SPEC anti-patterns absent for refusal surface)
- `01-VALIDATION.md` Per-Task Verification Map row `1-04-citation-chip-html` and row `1-04-streamlit-app-smoke` both flip from `❌ W0` to `✅ green` after executor verifies
</verification>

<success_criteria>
- The publicly-visible Phase 1 surface ships: hero (expanded/collapsed), DRHP metadata header, chat history with citation chips + inline expanders + per-answer disclaimer, refusal banner with reformulation chips, persistent footer sticky on mobile, first-use modal once per session, `/methodology` stub that does not 404
- Citation chip XSS-escape passes; chip clusters dedupe per D-03; claim_id round-trips byte-for-byte from `GroundedAnswer` → `data-claim-id` attribute → expander source span (Phase 3 METHOD-01 cross-phase contract)
- All three disclaimer surfaces render anchor copy from the single Wave 1 source-of-truth (`compliance/disclaimer_text.py`) — no copy duplication, no drift
- Mobile-responsive at 375/640/1024 breakpoints (visual smoke via Task 7 manual script + key CSS rules unit-tested in Task 1)
- Every Wave 4 user-visible string passes the banned-token scrubber (TRUST-02 extension via Task 3 tests)
- Single global CSS file (UI-SPEC FLAG-2: no scattered CSS) loaded once per session via the idempotent injector
- UI-01 + UI-02 are formally closed; TRUST-01 + TRUST-03 Streamlit-rendering side is delivered (formal closure remains with Plan 02)
- Walking Skeleton survives: `python -m agent.demo` still works; Wave 3 integration tests remain green
- Two Wave 0 xfail stubs flip to GREEN: `test_renderer_emits_sup_chip_html_and_escapes_xss` and the smoke-test side of `1-04-streamlit-app-smoke`
</success_criteria>

<output>
Create `.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-05-SUMMARY.md` when done. Summary must include:
- Confirmation that `bash scripts/smoke.sh` exits 0 on a fresh venv
- The actual GitHub repo URL used in `pages/01_methodology.py` (or `## FLAG` noting it remains a placeholder for Wave 5 deploy)
- The actual Swiggy issue-size string used in the DRHP metadata header (and the page from the PDF where it was confirmed), or `## FLAG` if the executor could not verify against the committed PDF
- The exact list of CSS classes emitted in `app/static/drhplens.css` (output of `grep -oE '^\.[a-z-]+' app/static/drhplens.css | sort -u`)
- Confirmation that all four new test files pass + that Wave 1 + Wave 3 tests remain green
- Any `## FLAG` items surfaced for the Wave 5 planner-of-record (e.g., LANG-ATTR Phase 6 polish; GitHub repo URL substitution; Swiggy issue-size verification; reformulation chip styling fallback if Streamlit's button styling proves insufficient)
- A note that 01-VALIDATION.md row statuses for `1-04-citation-chip-html` and `1-04-streamlit-app-smoke` should be flipped from `❌ W0` to `✅ green` by the executor before phase close

## WAVE 4 COMPLETE

At the close of Wave 4, the user can:
1. Open the public HF Spaces URL (after Wave 5 deploy) OR `streamlit run app.py` locally
2. Click through the first-use modal once per session
3. See the Swiggy DRHP metadata header + the expanded hero (or collapsed after the first message)
4. Ask a plain-English question via `st.chat_input`
5. Receive a grounded answer with clickable superscript citation chips, click a chip to see the verbatim DRHP source snippet in an inline expander + a deep-link to the SEBI prospectus PDF at the cited page
6. Receive an honest refusal banner (amber-not-red) with up to 2 reformulation suggestion chips when the DRHP doesn't address their question
7. See the per-answer "Informational only — not advice." disclaimer below every grounded answer
8. See the persistent slim footer with the anchor disclaimer copy + "methodology" link on every page
9. Click the methodology link, land on the stub page (Display 28px heading, body, GitHub + back links), come back to home with chat history intact

Wave 5 (deploy + Langfuse instrumentation + eval gold set) is unblocked.
</output>
</content>
</invoke>