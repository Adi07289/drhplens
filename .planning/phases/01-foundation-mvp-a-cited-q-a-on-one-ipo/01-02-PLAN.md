---
phase: 01-foundation-mvp-a-cited-q-a-on-one-ipo
plan: 02
type: tdd
wave: 1
depends_on:
  - 01-01
files_modified:
  - agent/schemas.py
  - agent/state.py
  - compliance/banned_tokens.py
  - compliance/disclaimer_text.py
  - compliance/scrubber.py
  - ui/copy.py
  - ui/disclaimer.py
  - tests/unit/test_schemas.py
  - tests/unit/test_scrubber.py
  - tests/unit/test_disclaimer_surface.py
  - tests/unit/test_copy_no_banned_tokens.py
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
  - TRUST-03
must_haves:
  truths:
    - "agent/schemas.py exports Pydantic v2 classes RetrievedChunkRef, Claim, GroundedAnswer, RefusalReason, RefusalResponse — the load-bearing contracts Phase 3 METHOD-01 will consume verbatim"
    - "compliance/scrubber.py blocks every banned-token conjugation (subscribe/avoid/buy/sell/target/recommend + UI-SPEC L-5 extensions + planner additions) with hard-block-and-regenerate semantics per D-09"
    - "compliance/disclaimer_text.py is the single source of truth for the D-07 anchor copy; DisclaimerSurface abstraction (ui/disclaimer.py) renders all three surfaces (modal + persistent footer + per-answer footer) from it"
    - "ui/copy.py — every user-visible string passes the scrubber at import time (TRUST-03 anchor: no banned token leaks into the product's own copy)"
  artifacts:
    - path: "agent/schemas.py"
      provides: "Pydantic v2 schemas locked in SKELETON §B — the cross-phase contract"
      exports: ["RetrievedChunkRef", "Claim", "GroundedAnswer", "RefusalReason", "RefusalResponse"]
      min_lines: 60
    - path: "agent/state.py"
      provides: "TypedDict GraphState consumed by every LangGraph node in Wave 3"
      exports: ["GraphState"]
      min_lines: 20
    - path: "compliance/banned_tokens.py"
      provides: "Locked banned-token regex (case-insensitive, word-boundary, \\w* morphological suffix per PITFALL 5 prevention)"
      exports: ["BANNED_TOKENS", "BANNED_TOKEN_PATTERN"]
    - path: "compliance/scrubber.py"
      provides: "scrub() function + ScrubResult dataclass — deterministic; runs after generate, before cite-check"
      exports: ["scrub", "ScrubResult"]
    - path: "compliance/disclaimer_text.py"
      provides: "Single-source D-07 anchor copy + variant strings (modal heading, footer suffix, per-answer disclaimer)"
      exports: ["ANCHOR_COPY", "MODAL_HEADING", "MODAL_BODY_ADDENDUM", "PERSISTENT_FOOTER", "PER_ANSWER_FOOTER"]
    - path: "ui/copy.py"
      provides: "Every user-visible string in the app (hero, placeholders, empty-state, refusal templates, error states) — banned-token-checked at import"
    - path: "ui/disclaimer.py"
      provides: "DisclaimerSurface abstraction with render_modal / render_persistent_footer / render_per_answer methods"
      exports: ["DisclaimerSurface", "render_disclaimer_gate"]
  key_links:
    - from: "compliance/scrubber.py"
      to: "compliance/banned_tokens.py"
      via: "import BANNED_TOKEN_PATTERN"
      pattern: "from compliance.banned_tokens import"
    - from: "ui/disclaimer.py"
      to: "compliance/disclaimer_text.py"
      via: "import ANCHOR_COPY"
      pattern: "from compliance.disclaimer_text import"
    - from: "ui/copy.py"
      to: "compliance/scrubber.py"
      via: "import-time scrubber assertion (every string in this module passes scrub())"
      pattern: "from compliance.scrubber import scrub"
---

<objective>
Ship the **load-bearing contracts** that every later wave and every later phase depends on: the Pydantic v2 schemas (per SKELETON §B), the deterministic banned-token scrubber (per D-09 + PITFALL 5), and the DisclaimerSurface abstraction (per D-08). This is the single most important plan in Phase 1 — if these contracts ship wrong, every downstream task in Waves 2-5 (and every phase 2-6) inherits a refactor tax.

Purpose: The `claim_id` schema and cite-check contract are the cross-cutting invariant from ROADMAP.md ("Agent traces carry `claim_id` references from Phase 1 day one"). Phase 3's METHOD-01 "Show your work" pane consumes these schemas verbatim. The scrubber and DisclaimerSurface ship the compliance posture (TRUST-01/02/03) that PITFALL 1 (SEBI advice boundary) demands as a hard floor — present from day one, not retrofitted before Phase 6 launch.

Type `tdd`: every code-producing task uses TDD because the behavior is deterministic and the test contract IS the specification. RED (the Wave 0 stub test from Plan 01) becomes GREEN here.

Output:
- `agent/schemas.py` with five Pydantic v2 classes, regex-validated `claim_id`, ≥ 1 source required per claim, structured refusal type
- `agent/state.py` with the GraphState TypedDict (locked for Wave 3 consumption)
- `compliance/banned_tokens.py` + `compliance/scrubber.py` — regex with morphological suffix, ScrubResult dataclass, hard-block-and-regenerate semantics
- `compliance/disclaimer_text.py` — single source of truth for D-07 anchor copy
- `ui/copy.py` — every user-visible string from 01-UI-SPEC.md Copywriting Contract, asserted scrubber-clean at import time
- `ui/disclaimer.py` — DisclaimerSurface class implementing the three surfaces; Streamlit-callable but does not yet render to a page (Wave 4 wires it into `app.py`)
- All four red xfail stubs from Plan 01 (test_schemas, test_scrubber, test_disclaimer_surface, test_copy_no_banned_tokens) flipped to GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-01-PLAN.md
@.planning/research/PITFALLS.md
@agent/__init__.py
@compliance/__init__.py
@ui/__init__.py
@tests/unit/test_schemas.py
@tests/unit/test_scrubber.py
@tests/unit/test_disclaimer_surface.py
@tests/unit/test_copy_no_banned_tokens.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD — Pydantic v2 schemas (RetrievedChunkRef, Claim, GroundedAnswer, RefusalReason, RefusalResponse) + GraphState</name>
  <files>agent/schemas.py, agent/state.py, tests/unit/test_schemas.py</files>
  <behavior>
    RED → GREEN → REFACTOR cycle. The xfail stub `test_claim_id_pattern_enforced` becomes the first green test.

    Test cases that must exist in tests/unit/test_schemas.py after this task (flip xfail → green):
    - `test_claim_id_pattern_enforced`: `Claim(claim_id="bad", text="x", sources=[ref])` raises ValidationError; `Claim(claim_id="c_4f3a8b", ...)` succeeds. The pattern `^c_[a-z0-9]{6,16}$` is the cross-phase contract per SKELETON §B.
    - `test_claim_id_uppercase_rejected`: `c_4F3A` rejected (regex enforces lowercase per the pattern).
    - `test_claim_id_too_short_rejected`: `c_4f3` rejected (min 6 hex chars after prefix).
    - `test_claim_id_too_long_rejected`: `c_` + 17 chars rejected.
    - `test_claim_requires_at_least_one_source`: `Claim(..., sources=[])` raises ValidationError on `min_length=1`.
    - `test_retrieved_chunk_ref_span_offsets_tuple`: `span_offsets=(0, 100)` accepted; `span_offsets=(100, 0)` MUST raise via custom validator (start ≤ end).
    - `test_grounded_answer_serializes_to_json`: round-trip `model_dump_json()` → `model_validate_json()` returns equal instance.
    - `test_grounded_answer_sub_question_lists_default_empty`: `sub_question_addressed` and `sub_question_unaddressed` default to `[]`.
    - `test_refusal_reason_enum_values`: enum values are exactly `low_retrieval_score`, `unsupported_claim`, `banned_token`, `infrastructure_error` (locked vocabulary; Wave 3 nodes branch on these).
    - `test_refusal_response_max_three_suggestions`: `reformulation_suggestions=["a","b","c","d"]` rejected (max_length=3 per UI-SPEC §"Visuals — Refusal Banner Contract" "up to three clickable chips").
    - `test_graph_state_typeddict_shape`: imports `GraphState` from agent/state, instantiates a dict literal with required keys, asserts `mypy --strict` style key presence by introspection of `__annotations__`.
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Pattern 2: Claim-ID Renderer Resolution" — the schema source-of-truth code block (lines 417-454); copy class names + field names + descriptions verbatim from this section
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Pattern 4: LangGraph Linear DAG" — GraphState consumer pattern (`state["gate1_passed"]`, `state["regenerate_attempts"]`, etc.) — keys that the schema must include
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md §B — the locked schema contract; Phase 3 METHOD-01 consumes verbatim
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Refusal Banner Contract" — `reformulation_suggestions` max 3 chips constraint
    - tests/unit/test_schemas.py — the existing xfail stub to flip to green
  </read_first>
  <action>
    Implement `agent/schemas.py` with five Pydantic v2 classes per the 01-RESEARCH.md Pattern 2 contract (lines 417-454). Use `pydantic.BaseModel`, `pydantic.Field`, `pydantic.field_validator`, `pydantic.model_validator`. Per D-06: `GroundedAnswer.sub_question_addressed` and `sub_question_unaddressed` default to `[]` so single-question answers don't require explicit empty lists.

    Field-by-field requirements:
    - `RetrievedChunkRef.span_offsets: tuple[int, int]` — add `@field_validator("span_offsets")` rejecting `start > end` (defends T-1-02 prompt-injection mitigation: a corrupted span can't reach the cite-check window code).
    - `Claim.claim_id: str = Field(..., pattern=r"^c_[a-z0-9]{6,16}$")` — the regex pattern is the cross-phase contract (per SKELETON §B); changing it later breaks Phase 3.
    - `Claim.sources: list[RetrievedChunkRef] = Field(..., min_length=1)` — every claim MUST have ≥ 1 source (PITFALL P18 antibody: agent can never emit a claim without retrieval).
    - `GroundedAnswer.answer_prose: str` — the prose with `{{claim_id}}` placeholders; renderer in Wave 4 resolves them.
    - `RefusalReason` = `Literal["low_retrieval_score", "unsupported_claim", "banned_token", "infrastructure_error"]` — the locked vocabulary (Wave 3 branch keys).
    - `RefusalResponse.reformulation_suggestions: list[str] = Field(default_factory=list, max_length=3)` — UI-SPEC max chips.

    Implement `agent/state.py` with a `GraphState` TypedDict (Python `typing.TypedDict` — keep import compatible with Python 3.11). Required keys for Wave 3 consumption (locked here; renaming later breaks Wave 3):
    - `question: str`
    - `retrieved_chunks: list[dict]` (raw Qdrant payload; converted to `RetrievedChunkRef` at cite-check time)
    - `reranked_top_k: list[dict]`
    - `gate1_passed: bool`
    - `gate1_max_score: float`
    - `sub_questions: list[str]` (from decompose node — D-06)
    - `grounded_answer: GroundedAnswer | None`
    - `scrub_passed: bool`
    - `regenerate_attempts: int` (counter for hard-block-and-regenerate per D-09)
    - `all_claims_grounded: bool` (cite-check output)
    - `cite_check_failures: list[str]` (per-claim failure reasons)
    - `refusal: RefusalResponse | None`

    Flip the xfail stub: remove `@pytest.mark.xfail` from `tests/unit/test_schemas.py::test_claim_id_pattern_enforced` and add the 10 new test functions listed in `<behavior>`.

    Per the source-audit: this task does NOT close any single phase REQ ID in `requirements` frontmatter (schemas are infrastructure consumed by every REQ). However, schemas are PREREQUISITE for INGEST-03 (payload metadata shape consumed in Wave 2), RAG-02 (citation data structure consumed in Wave 3-4), TRUST-04 (cite-check input shape consumed in Wave 3). This plan's frontmatter `requirements` lists only TRUST-01/02/03 (the disclaimer + scrubber + copy work in tasks 2-3); schemas are claimed transitively.
  </action>
  <verify>
    <automated>pytest tests/unit/test_schemas.py -x -q --timeout=10 -v 2>&amp;1 | tee /tmp/schema_test.txt &amp;&amp; grep -c 'passed' /tmp/schema_test.txt &amp;&amp; (! grep -c 'xfailed' /tmp/schema_test.txt | grep -v '^0$' &gt;/dev/null) &amp;&amp; python -c "from agent.schemas import RetrievedChunkRef, Claim, GroundedAnswer, RefusalReason, RefusalResponse; from agent.state import GraphState; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `agent/schemas.py` exports class names exactly: `RetrievedChunkRef`, `Claim`, `GroundedAnswer`, `RefusalReason`, `RefusalResponse`
    - `python -c "from agent.schemas import RetrievedChunkRef, Claim, GroundedAnswer, RefusalReason, RefusalResponse"` exits 0
    - `python -c "from agent.state import GraphState"` exits 0
    - `Claim.model_fields["claim_id"].metadata` contains the regex `^c_[a-z0-9]{6,16}$` (introspectable via Pydantic v2 API)
    - `Claim.model_fields["sources"].metadata` enforces `min_length=1`
    - `RefusalResponse.model_fields["reformulation_suggestions"].metadata` enforces `max_length=3`
    - `pytest tests/unit/test_schemas.py -x -q --timeout=10` exits 0 with ≥ 10 passed tests, 0 xfailed, 0 failed
    - `agent/schemas.py` total line count ≥ 60
    - `GraphState.__annotations__` contains keys: `question`, `retrieved_chunks`, `reranked_top_k`, `gate1_passed`, `gate1_max_score`, `sub_questions`, `grounded_answer`, `scrub_passed`, `regenerate_attempts`, `all_claims_grounded`, `cite_check_failures`, `refusal`
  </acceptance_criteria>
  <done>The load-bearing schema contract that Phase 3 METHOD-01 consumes is locked. Every downstream wave can `from agent.schemas import GroundedAnswer` and trust the shape. Wave 3's LangGraph nodes can `from agent.state import GraphState` and read deterministic keys. The first red xfail flips green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD — Banned-token scrubber (compliance/banned_tokens.py + compliance/scrubber.py)</name>
  <files>compliance/banned_tokens.py, compliance/scrubber.py, tests/unit/test_scrubber.py</files>
  <behavior>
    RED → GREEN. The xfail stub `test_every_banned_token_conjugation_blocked` becomes green; ~20 sub-tests cover every locked token + every conjugation.

    Test cases that must exist:
    - `test_subscribe_blocked`: `scrub("you should subscribe to this IPO")` → ScrubResult(passed=False, match="subscribe")
    - `test_subscribed_blocked` (PITFALL 5 morphological variant): `scrub("we subscribed last week")` → fails on "subscribed"
    - `test_subscribing_blocked`: `scrub("subscribing makes sense here")` → fails
    - `test_recommend_blocked`: `scrub("we recommend caution")` → fails
    - `test_recommended_blocked`: `scrub("the analyst recommended")` → fails
    - `test_recommendation_blocked`: `scrub("our recommendation is")` → fails
    - `test_buy_blocked`: `scrub("buy this stock")` → fails (also: "buying", "bought")
    - `test_sell_blocked`: `scrub("sell now")` → fails (also: "selling", "sold")
    - `test_avoid_blocked`: → fails (incl. "avoided", "avoiding")
    - `test_target_blocked` + `test_target_price_blocked`: both fail
    - `test_fair_value_blocked`: phrase "fair value" fails (multi-word token)
    - `test_overvalued_undervalued_blocked`
    - `test_accumulate_blocked` (planner-discretion extension per Open Question 2)
    - `test_outperform_underperform_bullish_bearish_book_profits_blocked` (planner-discretion extensions)
    - `test_neutral_text_passes`: `scrub("The DRHP discloses a promoter holding of 31.4%.")` → passed=True, match=None
    - `test_case_insensitive`: `scrub("BUY THIS")` → fails
    - `test_word_boundary_no_false_positive`: `scrub("the unsubscribed user list")` → still fails on "subscribed" (the `\w*` morphological suffix is intentional — defends against "subscribed" smuggling through prefix evasion); document this trade-off in the docstring
    - `test_substring_inside_unrelated_word_does_not_fire`: `scrub("syllabus")` → passes (no word boundary collision with any locked token; the regex is `\b...\w*\b` not `...`)
    - `test_scrub_result_dataclass_shape`: `ScrubResult(passed: bool, match: str | None, matched_token: str | None)` — `matched_token` returns the *canonical* root form (e.g., "recommend" not "recommended") so the Wave 3 generate-retry prompt can mention the specific token
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md §"Implementation Decisions" D-09 — locked default: hard-block-and-regenerate
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Copywriting Contract" "Banned tokens" — the locked list (subscribe, avoid, buy, sell, target, recommend, fair value, overvalued, undervalued, target price)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Pitfall 5: Banned-token scrubber missing morphological variants" — the exact regex pattern `\b(subscribe|avoid|buy|sell|target|recommend|fair value|overvalued|undervalued|target price)\w*\b` (case-insensitive, Unicode flag)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md Open Question 2 — planner-discretion extensions: accumulate, outperform, underperform, book profits, bullish, bearish
    - .planning/research/PITFALLS.md §"Pitfall 1: SEBI Investment-Advice / RA Boundary Violation" — the regulatory framing
    - tests/unit/test_scrubber.py — the existing xfail stub
  </read_first>
  <action>
    Implement `compliance/banned_tokens.py` with two module-level exports:
    - `BANNED_TOKENS: tuple[str, ...]` — the canonical token list in lowercase. Phase 1 locks at: subscribe, avoid, buy, sell, target, recommend, fair value, overvalued, undervalued, target price, accumulate, outperform, underperform, book profits, bullish, bearish. Order matters only for documentation; regex compilation treats it as set.
    - `BANNED_TOKEN_PATTERN: re.Pattern` — compiled regex `r"\b(" + "|".join(re.escape(t) for t in BANNED_TOKENS) + r")\w*\b"` with `re.IGNORECASE | re.UNICODE` flags. The `\w*` suffix is the morphological-variant catcher per PITFALL 5 prevention (catches "subscribed", "recommending", "buying", etc.).

    Implement `compliance/scrubber.py`:
    - `@dataclass class ScrubResult: passed: bool; match: str | None; matched_token: str | None` — `match` is the exact span that matched (e.g., "recommended"); `matched_token` is the canonical root (e.g., "recommend") for the Wave 3 retry-prompt.
    - `def scrub(text: str) -> ScrubResult:` — runs `BANNED_TOKEN_PATTERN.search(text)`; if match, returns `ScrubResult(passed=False, match=m.group(0), matched_token=m.group(1).lower())`; else `ScrubResult(passed=True, match=None, matched_token=None)`.
    - Docstring MUST document: (a) the deliberate `\w*` suffix and its "unsubscribed → still fails" implication, (b) the hard-block-and-regenerate posture per D-09, (c) the Wave 3 contract (`regenerate_attempts < 1 → retry; else refuse`).
    - NO LLM call inside scrubber. Pure deterministic regex (per PITFALL 5 prevention + ASVS V11 deterministic-business-logic control).

    Update `tests/unit/test_scrubber.py`: remove the xfail mark, add the ~20 sub-tests listed in `<behavior>`. Use `pytest.mark.parametrize` to keep the file readable.

    Per the source-audit: this task closes REQ TRUST-02 (banned-token scrubber prevents prescriptive language).
  </action>
  <verify>
    <automated>pytest tests/unit/test_scrubber.py -x -q --timeout=10 -v 2>&amp;1 | tee /tmp/scrubber_test.txt &amp;&amp; grep -cE 'passed' /tmp/scrubber_test.txt &amp;&amp; (! grep -E '^FAILED ' /tmp/scrubber_test.txt) &amp;&amp; python -c "from compliance.scrubber import scrub, ScrubResult; r = scrub('you should subscribe'); assert r.passed is False and r.matched_token == 'subscribe', r; r2 = scrub('The DRHP discloses a 31.4% promoter holding.'); assert r2.passed is True, r2; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `compliance/banned_tokens.py` exports `BANNED_TOKENS` (tuple of ≥ 16 strings) and `BANNED_TOKEN_PATTERN` (compiled regex with IGNORECASE flag)
    - `compliance/scrubber.py` exports `scrub` function and `ScrubResult` dataclass with fields `passed: bool`, `match: str | None`, `matched_token: str | None`
    - `pytest tests/unit/test_scrubber.py -x -q --timeout=10` exits 0 with ≥ 18 passed tests
    - Direct CLI check: `scrub("you should subscribe to Swiggy")` returns `ScrubResult(passed=False, matched_token="subscribe")`
    - Direct CLI check: `scrub("subscribed last quarter")` returns `passed=False, matched_token="subscribe"` (morphological variant caught per PITFALL 5)
    - Direct CLI check: `scrub("The DRHP discloses a 31.4% promoter holding.")` returns `passed=True`
    - Direct CLI check: `scrub("syllabus")` returns `passed=True` (no false-positive on substring collision)
    - `BANNED_TOKEN_PATTERN.flags & re.IGNORECASE != 0` AND `BANNED_TOKEN_PATTERN.flags & re.UNICODE != 0`
    - Implements REQ TRUST-02
  </acceptance_criteria>
  <done>The deterministic compliance gate is live. Wave 3's `scrub` node imports `from compliance.scrubber import scrub`, calls it on every LLM output, and branches on `result.passed`. The hard-block-and-regenerate semantics from D-09 are now ready for the regenerate_attempts counter in GraphState to drive.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: TDD — DisclaimerSurface + locked copy module + import-time copy scrub</name>
  <files>compliance/disclaimer_text.py, ui/copy.py, ui/disclaimer.py, tests/unit/test_disclaimer_surface.py, tests/unit/test_copy_no_banned_tokens.py</files>
  <behavior>
    Two red xfail stubs become green: `test_three_surfaces_render_anchor_copy` and `test_every_copy_string_passes_scrubber`.

    Test cases that must exist in tests/unit/test_disclaimer_surface.py:
    - `test_anchor_copy_matches_d07`: `compliance.disclaimer_text.ANCHOR_COPY` is EXACTLY the D-07 string (byte-for-byte; this is the regulatory artifact)
    - `test_render_modal_returns_dict_with_heading_body_cta`: `DisclaimerSurface.render_modal()` returns dict shape `{heading: str, body: str, cta_text: str}`; heading is "Read this once." per UI-SPEC; cta_text is "I understand — open DRHPLens"
    - `test_render_persistent_footer_includes_methodology_link_token`: `DisclaimerSurface.render_persistent_footer()` returns HTML string containing the anchor copy AND the substring "methodology" AND a link to `/methodology` (per UI-SPEC L-7 stub link requirement)
    - `test_render_per_answer_footer_is_italic_12px`: returns HTML string with class `drhp-disclaimer-per-answer` and content exactly "Informational only — not advice." per UI-SPEC
    - `test_render_disclaimer_gate_uses_session_state` (Streamlit-shape test, no rendering): the function signature accepts `session_state: dict` (test passes a plain dict), reads `disclaimer_accepted`, and either renders the modal (returns dict) or returns None
    - `test_sebi_10pt_floor_satisfied`: assert UI-SPEC §"Visuals — Disclaimer Surfaces" "SEBI 10pt-equivalent verification" — the persistent footer CSS class declaration in ui/disclaimer.py includes `font-size: 12px` (≥ 10.5pt) per TRUST-03; this test reads the rendered HTML string and asserts `font-size: 12px` substring is present

    Test cases that must exist in tests/unit/test_copy_no_banned_tokens.py:
    - `test_all_copy_strings_pass_scrubber`: walks every module-level string constant in `ui/copy.py` via `inspect.getmembers(ui.copy, lambda x: isinstance(x, str))`, calls `scrub(string)`, asserts every result is `passed=True`. This is the TRUST-03 anchor: the product's own copy never contains a banned token.
    - `test_required_copy_strings_present`: asserts the following module-level names exist in `ui.copy`:  HERO_HEADING, HERO_SUBHEADING, QUESTION_PLACEHOLDER, MODAL_CTA, EMPTY_STATE_HEADING, EMPTY_STATE_BODY, COLD_START_COPY, LOADING_ANSWER_COPY, REFUSAL_NO_GROUNDING_TEMPLATE, REFUSAL_PARTIAL_GROUNDING_TEMPLATE, REFUSAL_BANNED_TOKEN_COPY, ERROR_QDRANT_UNREACHABLE, ERROR_LLM_TIMEOUT, ERROR_RATE_LIMIT, PER_ANSWER_DISCLAIMER, METHODOLOGY_STUB_HEADING, METHODOLOGY_STUB_BODY
    - `test_modal_copy_includes_ai_disclosure_per_sebi_jan_2025`: asserts the modal body contains the substring "large language models" — UI-SPEC L-11 + SEBI Jan-2025 RA AI-disclosure requirement (TRUST-03)
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md §"Disclaimer Copy + Style" — D-07 anchor copy (EXACT string, byte-for-byte) and D-08 three-surface lock
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Copywriting Contract" — EVERY user-visible string this task implements (verbatim copy strings); also the L-7 methodology stub-link requirement
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-UI-SPEC.md §"Visuals — Disclaimer Surfaces" — three-surface visual contract (modal, persistent footer, per-answer footer)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Streamlit `st.dialog` first-use modal" — the code-example pattern lines 731-754; do NOT inline the implementation in the action, but the executor reads it from this file
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md row `1-01-disclaimer-render` — the validation contract this task closes
    - compliance/scrubber.py (just created in task 2) — the import for the copy-clean assertion
  </read_first>
  <action>
    Implement `compliance/disclaimer_text.py` with module-level constants (single source of truth per CONTEXT code_context bullet "DisclaimerSurface abstraction"):
    - `ANCHOR_COPY: str` — EXACTLY the D-07 string from CONTEXT lines 38-39: "DRHPLens reads prospectuses for you. It cites what the document says and shows historical context. Decisions about investing are yours. This is not investment advice." (no trailing whitespace; no smart quotes — straight ASCII apostrophe).
    - `MODAL_HEADING: str` = "Read this once."
    - `MODAL_BODY_ADDENDUM: str` = "The system uses large language models that occasionally make mistakes — every claim links to its source page so you can verify." (the AI-disclosure addendum per SEBI Jan-2025 RA / UI-SPEC L-11)
    - `MODAL_CTA: str` = "I understand — open DRHPLens"
    - `PERSISTENT_FOOTER_SUFFIX: str` = " · methodology" — appended to ANCHOR_COPY with the "methodology" word wrapped in an `<a href="/methodology">` link by the renderer
    - `PER_ANSWER_FOOTER: str` = "Informational only — not advice."

    Implement `ui/copy.py` with all 17 required module-level constants (test `test_required_copy_strings_present` enumerates them). Source every string verbatim from 01-UI-SPEC.md §"Copywriting Contract" — do NOT paraphrase; the legal-review precedent for Phase 6 depends on copy stability from Phase 1. The string `REFUSAL_NO_GROUNDING_TEMPLATE` uses Python `.format(topic=..., suggestion1=..., suggestion2=...)` placeholders matching the UI-SPEC template "This DRHP does not address {topic}. Try asking about {suggestion 1} or {suggestion 2}…".

    At the END of `ui/copy.py`, add an import-time assertion (this is the TRUST-03 anchor — fails fast on a banned-token regression in copy):
    ```
    from compliance.scrubber import scrub as _scrub
    import inspect as _inspect
    _module = _inspect.getmodule(_inspect.currentframe())
    for _name, _value in list(vars(_module).items()):
        if _name.startswith("_") or not isinstance(_value, str):
            continue
        _r = _scrub(_value)
        if not _r.passed:
            raise AssertionError(f"ui/copy.py contains banned token in {_name!r}: matched {_r.match!r}")
    del _scrub, _inspect, _module
    ```
    This makes any future copy edit that introduces a banned token fail at import time, not at runtime.

    Implement `ui/disclaimer.py` with the `DisclaimerSurface` class:
    - `render_modal() -> dict` returns `{"heading": MODAL_HEADING, "body": ANCHOR_COPY + " " + MODAL_BODY_ADDENDUM, "cta_text": MODAL_CTA}` — pure data return, no Streamlit calls inside (Wave 4 wraps with `st.dialog`).
    - `render_persistent_footer() -> str` returns the HTML: `<div class="drhp-footer">{ANCHOR_COPY}<a class="drhp-footer-link" href="/methodology">methodology</a></div>` styled with inline-fallback `font-size: 12px; color: #475569;` per UI-SPEC Visuals Disclaimer Surfaces 2 (the 12px declaration is the SEBI 10pt-floor verification).
    - `render_per_answer_footer() -> str` returns: `<div class="drhp-disclaimer-per-answer" style="font-size: 12px; font-style: italic; color: #475569;">{PER_ANSWER_FOOTER}</div>` per UI-SPEC Disclaimer Surfaces 3.
    - `render_disclaimer_gate(session_state: dict) -> dict | None` — pure-Python gate (no Streamlit imports at module level): if `session_state.get("disclaimer_accepted", False)` is True returns None; else returns the modal dict. Wave 4 wraps this with the actual `st.dialog` decorator call.

    Update both `tests/unit/test_disclaimer_surface.py` and `tests/unit/test_copy_no_banned_tokens.py`: remove xfail marks, add the test functions listed in `<behavior>`.

    Per the source-audit: this task closes REQ TRUST-01 (three-surface disclaimer) and REQ TRUST-03 (SEBI Jan-2025 RA prominence — 12px ≥ 10pt floor; AI-disclosure copy in modal body). REQ TRUST-02 was closed by task 2's scrubber; the import-time copy assertion here is the TRUST-03 reinforcement that no copy in our own product violates compliance.
  </action>
  <verify>
    <automated>pytest tests/unit/test_disclaimer_surface.py tests/unit/test_copy_no_banned_tokens.py -x -q --timeout=10 -v 2>&amp;1 | tee /tmp/disc_test.txt &amp;&amp; (! grep -E '^FAILED ' /tmp/disc_test.txt) &amp;&amp; python -c "from ui.disclaimer import DisclaimerSurface; d = DisclaimerSurface(); m = d.render_modal(); assert 'investment advice' in m['body'], m; f = d.render_persistent_footer(); assert 'font-size: 12px' in f and 'methodology' in f, f; pa = d.render_per_answer_footer(); assert 'Informational only' in pa, pa; print('OK')" &amp;&amp; python -c "import ui.copy; print('copy module imported cleanly (banned-token assertion passed)')"</automated>
  </verify>
  <acceptance_criteria>
    - `compliance/disclaimer_text.py` exports module-level constants: `ANCHOR_COPY`, `MODAL_HEADING`, `MODAL_BODY_ADDENDUM`, `MODAL_CTA`, `PERSISTENT_FOOTER_SUFFIX`, `PER_ANSWER_FOOTER`
    - `ANCHOR_COPY` matches the D-07 string byte-for-byte (verified by tests/unit/test_disclaimer_surface.py::test_anchor_copy_matches_d07)
    - `ui/copy.py` exports 17 required constants (enumerated in `<behavior>`)
    - `python -c "import ui.copy"` exits 0 (the import-time banned-token assertion passes — this proves no banned token leaked into our own copy)
    - `ui/disclaimer.py` exports `DisclaimerSurface` class with methods `render_modal`, `render_persistent_footer`, `render_per_answer_footer`, `render_disclaimer_gate`
    - `render_persistent_footer()` HTML string contains substring `font-size: 12px` (SEBI 10pt-floor verification)
    - `render_per_answer_footer()` HTML string contains substring `Informational only — not advice.` (exact UI-SPEC copy)
    - Modal body string contains substring `large language models` (TRUST-03 AI-disclosure requirement per UI-SPEC L-11 + SEBI Jan-2025 RA guidelines)
    - `pytest tests/unit/test_disclaimer_surface.py tests/unit/test_copy_no_banned_tokens.py -x -q --timeout=10` exits 0
    - Implements REQ TRUST-01 (three surfaces) AND REQ TRUST-03 (SEBI prominence + AI disclosure)
  </acceptance_criteria>
  <done>The compliance posture infrastructure is alive. Wave 4 imports `DisclaimerSurface` and wraps each method's output in Streamlit `st.markdown(..., unsafe_allow_html=True)` calls without renegotiating any copy. The TRUST-01/02/03 surfaces ship as a coherent package; PITFALL 1 (SEBI advice boundary) has structural defenses in code before any LLM call exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema layer → all downstream waves | Phase 2-6 import `agent/schemas.py`; any schema-level bug propagates downstream |
| scrubber → all LLM-output text | Wave 3 runs scrubber on every generated answer; a missed token = compliance leak (PITFALL 1) |
| copy module import-time assertion | A regression that introduces a banned token in our own copy fails import — defense in depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering (prompt injection via user question) | scrubber (defense in depth) | mitigate | The scrubber catches an LLM emitting prescriptive language even when prompted to via injection; combined with cite-check (Wave 3) the LLM cannot output advisory tokens AND have them grounded in the DRHP |
| T-1-02 | Tampering (prompt injection via DRHP content) | scrubber + GraphState `regenerate_attempts` counter | mitigate | Even if the DRHP contains advisory phrases that survive into LLM output, scrubber blocks; hard-block-and-regenerate gives one retry then refuses; tested in Wave 3 via `tests/integration/test_drhp_prompt_injection.py` |
| T-1-08 | Tampering (banned-token regression in our own copy) | `ui/copy.py` import-time scrubber assertion | mitigate | Import-time AssertionError fires before any HTTP request is served — a faulty copy edit cannot reach production silently |
| T-1-09 | Information Disclosure (D-07 copy drift) | `compliance/disclaimer_text.py` ANCHOR_COPY constant | mitigate | Single source of truth; unit test enforces byte-for-byte match against D-07; future legal-review polish edits this one constant, not three call sites |
| T-1-10 | Tampering (schema break for METHOD-01 cross-phase contract) | `agent/schemas.py` claim_id pattern | accept (low risk) | The regex pattern `^c_[a-z0-9]{6,16}$` is locked in SKELETON §B; any future change requires explicit phase-protocol break discussion; not a Wave 1 exposure |
| T-1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; Wave 0 (Plan 01) already audited |
</threat_model>

<verification>
- All four xfail stub tests from Wave 0 flip to green: `test_claim_id_pattern_enforced`, `test_every_banned_token_conjugation_blocked`, `test_three_surfaces_render_anchor_copy`, `test_every_copy_string_passes_scrubber`
- `pytest tests/unit -x -q --timeout=10` exits 0 with ≥ 30 passed (10 schema + ~20 scrubber + ~6 disclaimer + ~3 copy-clean tests; XFAIL count drops correspondingly)
- `python -c "from agent.schemas import GroundedAnswer, Claim, RetrievedChunkRef, RefusalReason, RefusalResponse"` exits 0
- `python -c "from agent.state import GraphState"` exits 0
- `python -c "from compliance.scrubber import scrub; from compliance.banned_tokens import BANNED_TOKEN_PATTERN"` exits 0
- `python -c "import ui.copy"` exits 0 (the import-time copy-clean assertion runs)
- `python -c "from ui.disclaimer import DisclaimerSurface; assert 'large language models' in DisclaimerSurface().render_modal()['body']"` exits 0 (SEBI Jan-2025 RA AI-disclosure inline-check)
</verification>

<success_criteria>
- The cross-phase schema contract (SKELETON §B) is in code; Phase 3 METHOD-01 has a stable target
- The compliance posture (PITFALL 1 antibody — banned-token scrubber + 3-surface disclaimer with 12px SEBI floor + AI disclosure) is live before any LLM call exists in the codebase
- Every copy string in our own product passes the scrubber (TRUST-03 anchor; verified at import time)
- The four red xfail stubs from Wave 0 are green; Wave 3 can now build LangGraph nodes on top of GraphState without renegotiating keys
- Disclaimer surfaces are renderable as pure Python (no Streamlit dependency at this layer) — Wave 4 wraps them
</success_criteria>

<output>
Create `.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-02-SUMMARY.md` when done. Summary must include:
- Schema class signatures actually shipped (output of `python -c "import agent.schemas; help(agent.schemas)"` summary)
- Final BANNED_TOKENS list (with rationale for any additions beyond UI-SPEC L-5 — per Open Question 2 planner-discretion)
- The 17 copy-module constant names that passed the scrubber assertion at import time
- Confirmation that all 4 Wave 0 xfail stubs are now green (visible via the test_count delta)
</output>
