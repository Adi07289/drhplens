---
phase: 01-foundation-mvp-a-cited-q-a-on-one-ipo
plan: 06
type: execute
wave: 5
depends_on:
  - 01-04
  - 01-05
files_modified:
  - README.md
  - .env.example
  - .gitignore
  - app/observability/__init__.py
  - app/observability/langfuse_client.py
  - app/observability/trace_decorators.py
  - app/observability/cite_check_metric.py
  - agent/graph.py
  - agent/policies.py
  - scripts/calibrate_gate1.py
  - scripts/run_eval.py
  - scripts/cron_pinger.yml
  - tests/eval/gold_set.jsonl
  - tests/eval/test_phase1_eval.py
  - tests/integration/test_langfuse_trace.py
  - eval/reports/.gitkeep
  - docs/DEPLOY.md
  - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md
autonomous: false
user_setup:
  - service: huggingface-spaces
    why: "OPS-02 — publicly deployed on free-tier host. HF Spaces is the only path; CC cannot create Spaces accounts or configure Spaces secrets via API (Spaces UI gated by HF account auth)."
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio → API keys (https://aistudio.google.com/app/apikey)"
      - name: GROQ_API_KEY
        source: "Groq Console → API Keys (https://console.groq.com/keys)"
      - name: QDRANT_URL
        source: "Qdrant Cloud → Cluster → Endpoint URL (already provisioned in Wave 2)"
      - name: QDRANT_API_KEY
        source: "Qdrant Cloud → Cluster → API Keys (already provisioned in Wave 2)"
      - name: LANGFUSE_PUBLIC_KEY
        source: "Langfuse Cloud → Project Settings → API Keys → Public Key"
      - name: LANGFUSE_SECRET_KEY
        source: "Langfuse Cloud → Project Settings → API Keys → Secret Key"
      - name: LANGFUSE_HOST
        source: "Langfuse Cloud → defaults to https://cloud.langfuse.com (EU) or https://us.cloud.langfuse.com (US)"
    dashboard_config:
      - task: "Create HF Spaces account (or sign in)"
        location: "https://huggingface.co/join"
      - task: "Create a new Space — sdk: streamlit, hardware: CPU basic (free), name: drhplens"
        location: "https://huggingface.co/new-space"
      - task: "Link Space to the GitHub repo (Settings → Repository) OR push directly to the Space's HF git remote"
        location: "HF Space → Settings → Linked Repositories (preferred for CI flow) or Files → upload"
      - task: "Configure secrets via Spaces UI: GEMINI_API_KEY, GROQ_API_KEY, QDRANT_URL, QDRANT_API_KEY, LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_HOST"
        location: "HF Space → Settings → Variables and secrets"
      - task: "Verify cold-start UX on first wake (load https://huggingface.co/spaces/<user>/drhplens in incognito; observe warming-up copy → home renders within 60s per OPS-02 acceptance)"
        location: "Public HF Spaces URL"
  - service: langfuse-cloud
    why: "Cross-cutting `claim_id` trace invariant from Phase 1 day one (per ROADMAP cross-cutting; consumed by Phase 3 METHOD-01). CC cannot create Langfuse accounts or projects via API."
    env_vars:
      - name: LANGFUSE_PUBLIC_KEY
        source: "Langfuse Cloud → Project Settings → API Keys"
      - name: LANGFUSE_SECRET_KEY
        source: "Langfuse Cloud → Project Settings → API Keys"
    dashboard_config:
      - task: "Create Langfuse Cloud account (free tier)"
        location: "https://cloud.langfuse.com/auth/sign-up"
      - task: "Create a project named drhplens-phase1; copy public + secret keys to env vars + HF Spaces secrets"
        location: "Langfuse Cloud → Projects → New"
  - service: cron-pinger
    why: "PITFALL 6 mitigation — HF Spaces free tier hibernates after ~48h idle (and sooner under load). One of cron-job.org or GitHub Actions must ping the public URL every 8 minutes during likely-demo hours (per RESEARCH §Pitfall 6). User picks ONE."
    env_vars: []
    dashboard_config:
      - task: "OPTION A (recommended per RESEARCH — more punctual): Create cron-job.org free account; add cronjob hitting https://huggingface.co/spaces/<user>/drhplens every 8 minutes, active 06:00-23:00 IST"
        location: "https://cron-job.org/en/"
      - task: "OPTION B (no extra account; ~5-15 min skew per RESEARCH): Enable the `scripts/cron_pinger.yml` GitHub Actions workflow shipped by this plan — copy it to `.github/workflows/ping.yml` and ensure GitHub Actions is enabled on the repo"
        location: "Repo Settings → Actions → General → Allow all actions"
requirements:
  - OPS-02
must_haves:
  truths:
    - "Public HF Spaces URL is accessible; renders home page + /methodology stub on mobile (375px) within 60s of cold start (OPS-02; D-08 first-use modal + persistent footer survive deploy)"
    - "Cron pinger configured (one of cron-job.org or GitHub Actions); verified by a fresh 30+ min cold-start retest after pinger is on (PITFALL 6 — recruiter demo doesn't go cold)"
    - "Langfuse trace exists for every agent run; `claim_id` propagated through every Claim trace; trace shape matches RESEARCH §Code Examples Langfuse trace shape (intake/retrieve/rerank/gate1_check/decompose/generate/scrub/cite_check/emit spans) — this is the Phase 3 METHOD-01 consumer contract (cross-cutting invariant from ROADMAP)"
    - "Eval suite first run completes; baseline metrics committed to eval/reports/2026-05-XX-phase1-baseline.md (per D-01 the report is a MEASURED baseline; Phase 1 does NOT release-gate on EVAL-01/03 — those gates live in Phase 3 + 6)"
    - "Gate 1 threshold calibrated against the 10-15 entry gold set; `agent/policies.py:GATE1_THRESHOLD` updated with the recommended value + inline comment recording calibration date + dataset (per RESEARCH Open Question 1 procedure)"
    - "10-15 hand-curated gold-set entries committed at tests/eval/gold_set.jsonl: 5 factual + 3 numeric + 3 risk-factor + 2 refusal-eligible (per 01-VALIDATION.md Wave 0 schema; Wave 5 fills the content)"
    - "01-VALIDATION.md frontmatter `nyquist_compliant: true` (all Per-Task Verification Map rows green or with documented exceptions)"
    - "01-06-SUMMARY.md documents Phase 1 deliverables + links to the public URL + first eval report; satisfies all 13 Phase 1 REQs"
  artifacts:
    - path: "README.md"
      provides: "HF Spaces YAML frontmatter (title, emoji, colorFrom/To, sdk: streamlit, sdk_version, app_file: app.py, pinned: false, license, sleep_time: 1800 per RESEARCH cold-start mitigation) + project README content + methodology summary + link to TODOS.md"
      contains: "sleep_time: 1800"
    - path: ".env.example"
      provides: "Documents the 7 required env vars without committing any secret values (T-1-04 mitigation)"
      contains: "GEMINI_API_KEY"
    - path: "app/observability/langfuse_client.py"
      provides: "Langfuse SDK init from env; no-op fallback when LANGFUSE_PUBLIC_KEY is unset so local dev runs without it (per RESEARCH §langfuse_setup.py code example)"
      exports: ["get_callback_handler", "get_client", "is_enabled"]
    - path: "app/observability/trace_decorators.py"
      provides: "LangGraph callback handler registration that wraps every node in agent/nodes/*; trace_id propagated; claim_id references attached to every Claim span; cost/latency/tool-call counts surfaced; failure-mode taxonomy"
      exports: ["build_callbacks_for_run", "attach_claim_ids_to_span"]
      min_lines: 40
    - path: "app/observability/cite_check_metric.py"
      provides: "Custom Langfuse score: faithfulness-via-cite-check pass/fail per answer; logged post-emit per RESEARCH trace shape"
      exports: ["score_cite_check"]
    - path: "scripts/calibrate_gate1.py"
      provides: "Sweeps GATE1_THRESHOLD from -2.0 to +2.0 in 0.5 steps against tests/eval/gold_set.jsonl; prints precision/recall per value; emits recommended threshold (per RESEARCH Open Question 1 procedure)"
    - path: "scripts/run_eval.py"
      provides: "Full eval suite — RAGAS faithfulness + recall@k + custom citation-accuracy metric (cite-check pass rate) against gold_set.jsonl; emits markdown report to eval/reports/<date>-phase1-baseline.md"
    - path: "scripts/cron_pinger.yml"
      provides: "Optional GitHub Actions workflow (Option B) — commented as such; user picks cron-job.org OR this"
    - path: "tests/eval/gold_set.jsonl"
      provides: "Hand-curated 10-15 entries: 5 factual + 3 numeric + 3 risk-factor + 2 refusal-eligible. qid foreign keys stable since Wave 0; Wave 5 fills question / expected_answer_contains / expected_sources / is_refusal_expected"
      contains: "swiggy-001"
    - path: "tests/eval/test_phase1_eval.py"
      provides: "pytest wrapper for scripts/run_eval.py gated by `--run-eval` (per 01-VALIDATION.md Test Infrastructure)"
    - path: "tests/integration/test_langfuse_trace.py"
      provides: "Gated by `--run-langfuse`; verifies a single agent run produces a Langfuse trace with the expected node spans and claim_id references (closes 01-VALIDATION.md row 1-05-langfuse-trace)"
    - path: "docs/DEPLOY.md"
      provides: "Step-by-step deploy runbook the user executes manually: push → create Space → secrets → cold-start verify → pinger setup"
      min_lines: 60
  key_links:
    - from: "agent/graph.py"
      to: "app/observability/trace_decorators.py build_callbacks_for_run"
      via: "graph.compile() registers the LangGraph callback handler so every node emits a Langfuse span"
      pattern: "from app.observability.trace_decorators import build_callbacks_for_run"
    - from: "agent/nodes/cite_check.py"
      to: "app/observability/cite_check_metric.py score_cite_check"
      via: "Post-emit custom score write — claim_id passed through"
      pattern: "from app.observability.cite_check_metric import score_cite_check"
    - from: "scripts/calibrate_gate1.py"
      to: "agent/policies.py GATE1_THRESHOLD"
      via: "Calibrated value written back as a tuned constant + inline comment with calibration date + dataset"
      pattern: "GATE1_THRESHOLD = <calibrated-value>"
    - from: "scripts/run_eval.py"
      to: "eval/reports/<date>-phase1-baseline.md"
      via: "Markdown report committed at end of Wave 5"
      pattern: "eval/reports/.*-phase1-baseline\\.md"
    - from: "README.md (HF Spaces YAML frontmatter)"
      to: "HF Spaces runtime"
      via: "Spaces reads the YAML frontmatter on git push to set sdk/app_file/sleep_time"
      pattern: "sdk: streamlit"
---

<objective>
Ship Phase 1 publicly. This is the final Phase 1 wave: Waves 0-4 landed the code (scaffold + schemas + ingestion + agent graph + Streamlit UI); Wave 5 deploys it to HF Spaces, instruments Langfuse so every claim trace propagates the `claim_id` from day one (cross-cutting invariant from ROADMAP), calibrates the Gate 1 retrieval-score floor against the 10-15 entry gold set (closes RESEARCH Open Question 1), runs the eval suite for the first time, and closes the Walking Skeleton (SKELETON.md) by attaching the public URL.

Purpose: This plan closes OPS-02 (publicly deployed on free-tier host) and locks the cross-cutting `claim_id` trace invariant in production, not just locally. Without this wave, Phase 3's METHOD-01 ("Show your work" methodology pane, pulled forward from Phase 6 per the CEO review) has no trace data structure to consume. The eval suite first run establishes the baseline measurement against which Phase 3 (EVAL-03 numeric-faithfulness ≥0.95) and Phase 6 (EVAL-01 release gate) will calibrate — Phase 1 does NOT release-gate on these, but baseline-without-measurement is unmeasurable, and unmeasurable means no honest improvement story for the portfolio. Per CEO review T6: this plan also captures the HF Spaces cold-start callout (PITFALL 6 → cron pinger) and the Qdrant 1GB sizing callout (verified-in-Wave-2; re-noted in 01-06-SUMMARY).

This plan has THREE checkpoint:human-verify tasks because OPS-02 deploy requires HF account creation, Spaces secrets configuration via the Spaces UI, and a manual cold-start visual verification — none of which CC can automate (HF does not expose Spaces creation or secrets-write via API for free-tier accounts, and cold-start UX is a human-eyeball judgment per 01-VALIDATION.md Manual-Only Verifications).

Output:
- README.md with HF Spaces YAML frontmatter (sdk: streamlit, sleep_time: 1800, app_file: app.py)
- .env.example documenting 7 required env vars
- app/observability/{langfuse_client.py, trace_decorators.py, cite_check_metric.py} — full Langfuse instrumentation
- agent/graph.py updated to register the LangGraph callback handler
- scripts/{calibrate_gate1.py, run_eval.py, cron_pinger.yml}
- tests/eval/gold_set.jsonl — 10-15 hand-curated entries (content lands in this wave; schema already locked in Wave 0)
- tests/eval/test_phase1_eval.py — pytest wrapper (--run-eval gated)
- tests/integration/test_langfuse_trace.py — closes 01-VALIDATION.md row 1-05-langfuse-trace
- eval/reports/<date>-phase1-baseline.md — first eval report committed
- docs/DEPLOY.md — step-by-step deploy runbook
- 01-VALIDATION.md frontmatter flipped to `nyquist_compliant: true`
- 01-06-SUMMARY.md with the public URL + first eval baseline + all 13 Phase 1 REQs marked complete
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-01-SUMMARY.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-04-SUMMARY.md
@.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-05-SUMMARY.md
@agent/graph.py
@agent/policies.py
@agent/schemas.py
@agent/nodes/cite_check.py
@CLAUDE.md
@/Users/adityasharma/.claude/plans/mighty-noodling-pretzel.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HF Spaces config (README.md YAML frontmatter) + .env.example + .gitignore verify + docs/DEPLOY.md</name>
  <files>README.md, .env.example, .gitignore, docs/DEPLOY.md</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Pitfall 6" — sleep_time + cron pinger mitigation; sleep_time: 1800 is the YAML key (per huggingface.co/docs/huggingface_hub/en/guides/manage-spaces)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Architectural Responsibility Map" — "Secrets: HF Spaces secrets UI / Env vars at runtime / Accessed via os.environ"
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-CONTEXT.md §"Specifics" — Phase 1 anchor copy (D-07) — surface this in README's project description so the honesty-first voice is consistent on the GitHub README + HF Space card
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-01-SUMMARY.md — confirm `.gitignore` already contains `.env`, `__pycache__/`, `.venv/` (Wave 0 set this; Wave 5 only verifies)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md §"Per-Task Verification Map" row `1-05-deploy-smoke` — the manual smoke step DEPLOY.md must enumerate
    - /Users/adityasharma/.claude/plans/mighty-noodling-pretzel.md §"Implementation Tasks" T6 — CEO review callout: HF Spaces cold-start + Qdrant 1GB cap must be in Phase 1 PLAN.md (this is where it lands)
  </read_first>
  <action>
    Create `README.md` with the HF Spaces YAML frontmatter at the very top (per huggingface.co/docs/hub/en/spaces-sdks-streamlit — frontmatter MUST be the first content in the file, between `---` delimiters, no leading blank lines):

    ```
    ---
    title: DRHPLens
    emoji: 📄
    colorFrom: indigo
    colorTo: blue
    sdk: streamlit
    sdk_version: 1.36.0
    app_file: app.py
    pinned: false
    license: mit
    sleep_time: 1800
    ---
    ```

    `sleep_time: 1800` is the maximum free-tier value per HF docs and is the documented PITFALL 6 mitigation (per RESEARCH §Pitfall 6 "HF Spaces hibernates after 48h idle by default ... `sleep_time` parameter to extend keep-alive"). DO NOT use a higher value — HF rejects > 1800 on free tier.

    Below the frontmatter, write the README body:
    1. Title + one-paragraph project description quoting D-07 anchor copy verbatim ("DRHPLens reads prospectuses for you. It cites what the document says and shows historical context. Decisions about investing are yours. This is not investment advice.")
    2. Section "What it does" — bullet list (one IPO Q&A, span-level citations, refusal posture, three disclaimer surfaces)
    3. Section "Methodology summary" — 3-4 sentences naming the stack (LangGraph + LlamaIndex + Docling + Qdrant + bge-m3 + Gemini + Streamlit) and the cite-check invariant; link to the live /methodology stub on the deployed Space (relative URL: `/methodology`)
    4. Section "Roadmap" — link to TODOS.md (relative: `./TODOS.md`) and one-line per Phase 2-6 from ROADMAP.md
    5. Section "Run locally" — one-line `uv pip install -e .[dev] && streamlit run app.py`; reference .env.example for required env vars
    6. Section "License" — MIT

    Per source-audit: this README implements REQ OPS-02 (publicly deployed) because HF Spaces parses the YAML frontmatter on every git push to set sdk/app_file/sleep_time; without this file the Space won't boot.

    Create `.env.example` documenting all 7 required env vars (NO real values; documentation only — T-1-04 secret-leakage mitigation):

    ```
    # LLM providers (Wave 3 dependency)
    GEMINI_API_KEY=
    GROQ_API_KEY=
    # Vector store (Wave 2 dependency — already provisioned in Qdrant Cloud)
    QDRANT_URL=
    QDRANT_API_KEY=
    # Observability (this wave — Langfuse Cloud free tier)
    LANGFUSE_PUBLIC_KEY=
    LANGFUSE_SECRET_KEY=
    LANGFUSE_HOST=https://cloud.langfuse.com
    ```

    Each line has a comment explaining what it's for so a future contributor knows which dashboard to visit. NO real keys; the file is intentionally fillable. Per RESEARCH §Security Domain V6 "Secrets ... stored encrypted in HF Spaces secrets UI; never in code".

    Verify `.gitignore` already contains `.env` (Wave 0 set this per 01-01-PLAN.md acceptance criteria). If missing, append it. DO NOT add `.env.example` to gitignore — that file is documentation, committed intentionally.

    Create `docs/DEPLOY.md` — step-by-step manual deploy runbook. Required sections (each section is a numbered list of clickable / runnable steps):

    1. **Prerequisites** — GitHub repo public, all Phase 1 waves 0-4 merged, Qdrant Cloud cluster live (Wave 2), Langfuse Cloud project live (Task 2 of this plan), 7 env vars filled locally in `.env` (using `.env.example` as template)
    2. **Push to GitHub** — `git push origin main` (HF Spaces will read from the linked GitHub repo)
    3. **Create HF Space** — visit https://huggingface.co/new-space; sdk: streamlit; hardware: CPU basic free; name: drhplens (or user's preferred slug); link to GitHub repo OR push to HF git remote
    4. **Configure Secrets** — visit Space → Settings → Variables and secrets → add all 7 env vars from .env.example. NEVER paste real values into source code or README.
    5. **Verify Boot** — visit https://huggingface.co/spaces/<user>/drhplens; wait for "Building" → "Running"; expect first cold start to take 60-90s while bge-m3 + reranker weights download (~1.7GB total; cached after first run per RESEARCH §"Don't Hand-Roll" → @st.cache_resource)
    6. **Smoke Test** — ask "What is Swiggy's issue size?" → expect cited answer; ask "What does Swiggy say about Mars colonization?" → expect refusal banner with reformulation; click any [1] chip → expect inline source span expansion; refresh on mobile (DevTools 375px width) → expect mobile-responsive
    7. **Configure Cron Pinger (PICK ONE)** —
       - Option A (recommended per RESEARCH §Pitfall 6): cron-job.org → URL: `https://huggingface.co/spaces/<user>/drhplens` → schedule `*/8 * * * *` → active hours 06:00-23:00 IST
       - Option B: Copy `scripts/cron_pinger.yml` to `.github/workflows/ping.yml` → commit → enable Actions in repo settings (NOTE: GitHub Actions schedule cron skews 5-15 min per RESEARCH — Option A is more punctual)
    8. **Verify Cold-Start UX** — wait 35+ min without traffic; visit URL in incognito; verify "warming up" copy renders within 5s; verify home renders within 60s (closes 01-VALIDATION.md row 1-05-deploy-smoke for OPS-02)
    9. **Cost monitoring** — Phase 1 should consume $0 across all services (Gemini free 1500/day; Groq free; Qdrant 1GB free; Langfuse free; HF Spaces CPU basic free). If any service shows charges, file a runbook item BEFORE the next user demo.
    10. **Phase 1 close** — flip `01-VALIDATION.md` frontmatter `nyquist_compliant: true`; commit `01-06-SUMMARY.md` referencing the public URL.

    Inline cross-references to the CEO review's Qdrant 1GB sizing callout: explicitly state in DEPLOY.md Prerequisites "Qdrant Cloud free 1GB is sufficient for the single Swiggy DRHP (~1500-2500 chunks, ~10MB raw vector data + payload — verified in Wave 2 ingest); Phase 2 multi-IPO catalogue will re-evaluate sizing before adding the 5th-10th DRHP."

    Per source-audit: REQ coverage is OPS-02 (the deploy mechanics) — but D-08 first-use modal + persistent footer + per-answer footer are Wave 4 UI work that DEPLOY.md verifies still works on the live Space; smoke checklist enforces this.
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; head -15 README.md | grep -q "sdk: streamlit" &amp;&amp; head -15 README.md | grep -q "sleep_time: 1800" &amp;&amp; head -15 README.md | grep -q "app_file: app.py" &amp;&amp; test -f .env.example &amp;&amp; grep -q "^GEMINI_API_KEY=" .env.example &amp;&amp; grep -q "^LANGFUSE_PUBLIC_KEY=" .env.example &amp;&amp; grep -q "^QDRANT_URL=" .env.example &amp;&amp; (! grep -E "(sk-|AIza|sk_live|whsec_)" .env.example) &amp;&amp; grep -v '^#' .gitignore | grep -qE '^\.env$' &amp;&amp; test -f docs/DEPLOY.md &amp;&amp; test $(wc -l &lt; docs/DEPLOY.md) -ge 60 &amp;&amp; grep -qi "qdrant" docs/DEPLOY.md &amp;&amp; grep -qi "cron" docs/DEPLOY.md &amp;&amp; grep -qi "sleep_time\|cold.start" docs/DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists; first non-empty line is `---`; YAML frontmatter contains exactly: `title`, `emoji`, `colorFrom`, `colorTo`, `sdk: streamlit`, `sdk_version: 1.36.0`, `app_file: app.py`, `pinned: false`, `license: mit`, `sleep_time: 1800`
    - README body quotes the D-07 anchor copy verbatim (substring match: `DRHPLens reads prospectuses for you`)
    - `.env.example` contains 7 KEY= lines (one per required env var) with NO real values after the `=`
    - `.env.example` does NOT contain any real API key prefixes (sk-, AIza, sk_live, whsec_) — T-1-04 verify
    - `.gitignore` line `.env` exists (must be on its own line, not commented out; verified by `grep -v '^#' .gitignore | grep -E '^\.env$'`)
    - `docs/DEPLOY.md` exists with ≥ 60 lines and contains: "qdrant" (Qdrant sizing callout), "cron" (pinger setup), "sleep_time" OR "cold start" (PITFALL 6 mitigation)
    - DEPLOY.md mentions both cron-pinger options (cron-job.org AND scripts/cron_pinger.yml)
    - Implements REQ OPS-02 (YAML frontmatter is the HF Spaces boot contract)
  </acceptance_criteria>
  <done>HF Spaces deploy metadata is live in the repo; secrets documentation pattern is in .env.example with no leakage; user can follow docs/DEPLOY.md step-by-step to ship Phase 1 publicly; CEO-review T6 callouts (HF cold-start + Qdrant 1GB) are documented in the Phase 1 plan as required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Langfuse instrumentation — observability/{langfuse_client, trace_decorators, cite_check_metric}.py + agent/graph.py registration + integration test</name>
  <files>app/observability/__init__.py, app/observability/langfuse_client.py, app/observability/trace_decorators.py, app/observability/cite_check_metric.py, agent/graph.py, tests/integration/test_langfuse_trace.py</files>
  <behavior>
    Wave 0 xfail stub `test_every_node_writes_a_span_with_claim_ids` (in tests/integration/test_langfuse_trace.py per 01-VALIDATION.md row 1-05-langfuse-trace) flips to GREEN when run with `--run-langfuse`.

    Test cases after this task:
    - `test_langfuse_client_noop_when_keys_missing`: with LANGFUSE_PUBLIC_KEY unset, `get_callback_handler()` returns a no-op handler that does not raise; `is_enabled()` returns False. Local dev works without a Langfuse account (per RESEARCH §Code Examples Langfuse trace shape note "no-op fallback when LANGFUSE_PUBLIC_KEY is unset").
    - `test_langfuse_client_initialized_when_keys_present` (--run-langfuse gated): with real env vars, `get_callback_handler()` returns a Langfuse `CallbackHandler` instance; `is_enabled()` returns True.
    - `test_every_node_writes_a_span_with_claim_ids` (--run-langfuse gated): invoke `agent.graph.GRAPH` with a fixture question that exercises the grounded path; assert the Langfuse trace contains spans named `intake`, `retrieve`, `rerank`, `gate1_check`, `decompose`, `generate`, `scrub`, `cite_check`, `emit` (exactly the 9-span shape from RESEARCH §"Trace shape end-to-end"). Assert every `Claim` span has its `claim_id` in span metadata (the Phase 3 METHOD-01 consumer contract). Assert the `gate1_check` span carries `gate1_max_score` and the `cite_check` span carries per-claim `grounded` booleans.
    - `test_cite_check_score_logged`: after `cite_check` runs, a Langfuse custom score named `faithfulness_via_cite_check` is logged for the trace with value 1.0 (all grounded) or 0.0 (any unsupported). Closes the "cite-check-as-Langfuse-score" requirement from the planning context.
    - `test_failure_mode_taxonomy_attached`: on a refusal path (Gate 1 or Gate 2), the trace's top-level metadata includes `refusal_reason ∈ {low_retrieval_score, unsupported_claim, banned_token}` (matching the RefusalReason enum from SKELETON §B).
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Code Examples" "Langfuse decorator + LangChain CallbackHandler on LangGraph" — the exact init pattern (lines 756-795)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Don't Hand-Roll" row "Trace IDs through nodes" — use Langfuse `@observe` + LangChain CallbackHandler, never roll your own
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-SKELETON.md §B — RefusalReason enum (used in failure-mode metadata)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-04-PLAN.md — agent/graph.py topology + nodes — the integration points where callback registration plugs in (no changes to graph TOPOLOGY; only callback registration is added)
    - .planning/ROADMAP.md cross-cutting invariants — "Agent traces carry `claim_id` references from Phase 1 day one (not bolted on at Phase 3) so methodology-pane data exists when E1 lands"
    - https://langfuse.com/integrations/frameworks/langchain — LangChain CallbackHandler init (cross-reference for API surface; do not deviate from CITED RESEARCH code example)
  </read_first>
  <action>
    Create `app/observability/__init__.py` — empty.

    Create `app/observability/langfuse_client.py`:
    - `def is_enabled() -> bool:` — returns `bool(os.environ.get("LANGFUSE_PUBLIC_KEY") and os.environ.get("LANGFUSE_SECRET_KEY"))`. Used by callers to short-circuit Langfuse wiring locally.
    - `@functools.lru_cache(maxsize=1)` `def get_client():` — returns a `langfuse.Langfuse` instance if `is_enabled()`, else `None`. Reads `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST` (defaults to `https://cloud.langfuse.com`).
    - `def get_callback_handler():` — returns a `langfuse.callback.CallbackHandler` if `is_enabled()`, else a no-op object with `.on_*` methods returning `None`. The no-op path makes `agent.demo` runnable in local dev without a Langfuse account (per RESEARCH note).
    - Module-level docstring records: "Per D-04 / SKELETON cross-cutting invariant: every claim_id propagated through the trace; consumed by Phase 3 METHOD-01."

    Create `app/observability/trace_decorators.py`:
    - `def build_callbacks_for_run(question: str, trace_metadata: dict | None = None) -> list:` — returns a list with one CallbackHandler initialized for this run, with `trace_name="answer_question"`, `metadata={"phase": "1", "question_preview": question[:80], **(trace_metadata or {})}`. When `is_enabled()` is False, returns `[]` (LangGraph accepts empty callback list).
    - `def attach_claim_ids_to_span(claim_ids: list[str]) -> None:` — calls `langfuse_context.update_current_observation(metadata={"claim_ids": claim_ids})` if enabled. The `generate` and `cite_check` nodes call this so every `Claim` (from `GroundedAnswer.claims`) has its `claim_id` materialized on its span (the Phase 3 METHOD-01 consumer contract).
    - `def attach_refusal_reason_to_trace(reason: str) -> None:` — calls `langfuse_context.update_current_trace(metadata={"refusal_reason": reason})`. The `refuse_with_reformulation` node calls this with the RefusalReason value before emit.
    - `def attach_gate1_metadata_to_span(max_score: float, threshold: float, passed: bool) -> None:` — wraps the Gate 1 span with these metadata fields (Wave 5 calibration script uses these for the precision/recall sweep).
    - All four helpers no-op gracefully when Langfuse is not enabled.

    Create `app/observability/cite_check_metric.py`:
    - `def score_cite_check(trace_id: str, all_grounded: bool, per_claim_results: list[dict]) -> None:` — calls `langfuse.score(trace_id=trace_id, name="faithfulness_via_cite_check", value=1.0 if all_grounded else 0.0, comment=json.dumps(per_claim_results)[:1000])`. The `cite_check` node calls this post-validation. The custom-score wiring is the Wave-5 piece of the EVAL-01 baseline measurement (Phase 1 measures but does not gate; Phase 3 EVAL-03 and Phase 6 EVAL-01 release-gate against this score's running mean).
    - No-op when `is_enabled()` is False.

    Update `agent/graph.py` (the file already exists from Wave 3): register the LangGraph callback handler at compile time. Add `from app.observability.trace_decorators import build_callbacks_for_run` at the top. Modify the `build_graph()` function so that the compiled graph's `.invoke()` calls accept a `config` dict; specifically, expose a thin wrapper `def invoke_with_tracing(state: dict, question: str) -> dict:` that calls `GRAPH.invoke(state, config={"callbacks": build_callbacks_for_run(question)})`. Wave 4's `app.py` and Wave 3's `agent.demo` MUST call `invoke_with_tracing` instead of `GRAPH.invoke` directly so traces are auto-attached. Do NOT modify the graph topology (per SKELETON §C — topology is locked at end of Wave 3; this wave only adds observability around it).

    Wire the new helpers into `agent/nodes/generate.py`, `agent/nodes/cite_check.py`, and `agent/nodes/refuse_with_reformulation.py`:
    - `generate.py` — after the LLM returns a `GroundedAnswer`, call `attach_claim_ids_to_span([c.claim_id for c in answer.claims])`
    - `cite_check.py` — after the deterministic check completes, call `score_cite_check(trace_id=..., all_grounded=..., per_claim_results=...)` AND `attach_claim_ids_to_span([c.claim_id for c in answer.claims])` (idempotent — generates the same metadata; ensures the cite_check span carries it even if generate's span lost it)
    - `refuse_with_reformulation.py` — call `attach_refusal_reason_to_trace(state["refusal_reason"])` before returning
    - `gate1_check.py` — call `attach_gate1_metadata_to_span(state["gate1_max_score"], GATE1_THRESHOLD, state["gate1_passed"])` after computing the gate result

    All wiring uses the no-op-when-disabled pattern so existing Wave 3-4 tests keep passing without Langfuse env vars.

    Update `tests/integration/test_langfuse_trace.py` (Wave 0 xfail stub): remove xfail mark; add a `pytestmark = pytest.mark.skipif(not os.environ.get("LANGFUSE_PUBLIC_KEY"), reason="requires LANGFUSE_PUBLIC_KEY env (run with `pytest --run-langfuse`)")`; implement the 5 test functions from `<behavior>`. Use a real Langfuse client for the gated tests; mock the Langfuse client for the always-on `test_langfuse_client_noop_when_keys_missing` test (the no-op path is critical for local dev and CI without Langfuse keys).

    Add a pytest CLI flag in `tests/conftest.py` if not already present: `--run-langfuse` (parallel to `--run-eval` from 01-VALIDATION.md). When unset, `test_langfuse_trace.py` skips. When set, the integration tests run against the live Langfuse instance.

    Per source-audit: this task implements the cross-cutting `claim_id` trace invariant from ROADMAP (the E1 dependency from CEO review T5); it also closes 01-VALIDATION.md row 1-05-langfuse-trace. It does NOT cover any REQ-ID directly because trace persistence is a cross-cutting invariant not assigned to a single REQ — it spans RAG-01, RAG-02, RAG-03, TRUST-04 (all the agent-side REQs) by instrumenting their runtime.
  </action>
  <verify>
    <automated>pytest tests/integration/test_langfuse_trace.py::test_langfuse_client_noop_when_keys_missing -x -q --timeout=15 &amp;&amp; python -c "from app.observability.langfuse_client import is_enabled, get_callback_handler; print('enabled:', is_enabled()); h = get_callback_handler(); print('handler-type:', type(h).__name__)" &amp;&amp; python -c "from app.observability.trace_decorators import build_callbacks_for_run, attach_claim_ids_to_span, attach_refusal_reason_to_trace, attach_gate1_metadata_to_span; print('OK')" &amp;&amp; python -c "from app.observability.cite_check_metric import score_cite_check; print('OK')" &amp;&amp; python -c "from agent.graph import invoke_with_tracing, build_graph, GRAPH; print('OK')" &amp;&amp; grep -q "from app.observability.trace_decorators import" agent/graph.py &amp;&amp; grep -q "attach_claim_ids_to_span" agent/nodes/generate.py &amp;&amp; grep -q "score_cite_check" agent/nodes/cite_check.py &amp;&amp; grep -q "attach_refusal_reason_to_trace" agent/nodes/refuse_with_reformulation.py &amp;&amp; grep -q "attach_gate1_metadata_to_span" agent/nodes/gate1_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `app/observability/__init__.py`, `langfuse_client.py`, `trace_decorators.py`, `cite_check_metric.py` all exist
    - `from app.observability.langfuse_client import is_enabled, get_client, get_callback_handler` works
    - `from app.observability.trace_decorators import build_callbacks_for_run, attach_claim_ids_to_span, attach_refusal_reason_to_trace, attach_gate1_metadata_to_span` works
    - `from app.observability.cite_check_metric import score_cite_check` works
    - `is_enabled()` returns False when LANGFUSE_PUBLIC_KEY is unset; `get_callback_handler()` returns a no-op object (does not raise)
    - `agent/graph.py` imports from `app.observability.trace_decorators`; exposes `invoke_with_tracing(state, question)`
    - `agent/nodes/generate.py` calls `attach_claim_ids_to_span`; `agent/nodes/cite_check.py` calls `score_cite_check`; `agent/nodes/refuse_with_reformulation.py` calls `attach_refusal_reason_to_trace`; `agent/nodes/gate1_check.py` calls `attach_gate1_metadata_to_span`
    - `tests/integration/test_langfuse_trace.py` is no longer xfail; runs the no-op test always; skips the live tests when `--run-langfuse` is not set
    - Wave 0 stub row 1-05-langfuse-trace is GREEN (or SKIPPED-by-design without --run-langfuse, with the no-op test passing unconditionally)
    - All existing Wave 1-4 tests still pass (no regression — the no-op-when-disabled pattern is verified by running the full unit suite without LANGFUSE_* env vars)
    - Implements cross-cutting `claim_id` trace invariant from ROADMAP (the E1 dependency from CEO review T5)
  </acceptance_criteria>
  <done>Langfuse is wired into every LangGraph node; claim_id flows through every Claim trace from day one; local dev works without a Langfuse account via the no-op fallback; Phase 3 METHOD-01 has its consumer contract live in production traces.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-curate the 10-15 entry gold set + eval suite first run (scripts/run_eval.py + tests/eval/test_phase1_eval.py + eval/reports/.gitkeep)</name>
  <files>tests/eval/gold_set.jsonl, scripts/run_eval.py, tests/eval/test_phase1_eval.py, eval/reports/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md §"Wave 0 Requirements" line "tests/eval/gold_set.jsonl — 10-15 hand-curated entries (Wave 0 stubs the schema; full content lands in Wave 5)"
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-VALIDATION.md §"Per-Task Verification Map" row 1-05-eval-gold-set
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-01-PLAN.md Task 3 §gold-set schema — the JSON-line schema is LOCKED at Wave 0 (qid, category, question, expected_answer_contains, expected_sources, is_refusal_expected); Wave 5 only fills the content, must NOT change the schema (Phase 3 METHOD-01 joins on `qid`)
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Validation Architecture" §Wave 0 Gaps line "5 factual ... 3 numeric ... 3 risk-factor ... 2 refusal-eligible" — the exact distribution
    - data/swiggy_drhp/swiggy_prospectus_2024_11.pdf — the executor must open this to write factually correct Q/A pairs (each entry's `expected_answer_contains` and `expected_sources.page_start/page_end` must come from the actual DRHP, not from memory)
    - .planning/research/STACK.md §"Evals + tracing" — RAGAS 0.4.3 for faithfulness; DeepEval for guardrails (Phase 6 work); Phase 1 only runs RAGAS + custom citation-accuracy
  </read_first>
  <action>
    Fill `tests/eval/gold_set.jsonl` with the 10-15 hand-curated entries. The executor MUST open `data/swiggy_drhp/swiggy_prospectus_2024_11.pdf` and read the actual DRHP to source each entry's question, expected answer fragments, and source page anchors — DO NOT make up content. The schema is LOCKED at Wave 0; only the values change.

    Required distribution (matching 01-RESEARCH.md §Validation Architecture):
    - 5 factual (qid swiggy-001..005): single-fact lookups like "What is the issue size?", "Who are the lead managers?", "What is the offer for sale size?", "What is the face value per equity share?", "On which exchanges did Swiggy list?"
    - 3 numeric (qid swiggy-006..008): number-bearing lookups like "What is the promoter holding post-issue?", "What was the net revenue from operations for FY24?", "What was the loss for FY24?"
    - 3 risk-factor (qid swiggy-009..011): risk-section extractions like "What does the DRHP say about path to profitability?", "What does the DRHP say about competition?", "What does the DRHP disclose about related-party transactions with promoter entities?"
    - 2 refusal-eligible (qid swiggy-012..013): off-corpus queries like "Compare Swiggy to Zomato's listing-day performance" (post-listing data not in DRHP) and "What is Swiggy's market cap today?" (real-time data not in DRHP) — both must set `is_refusal_expected: true`

    Schema per entry (LOCKED at Wave 0):
    ```
    {
      "qid": "swiggy-NNN",
      "category": "factual" | "numeric" | "risk-factor" | "refusal-eligible",
      "question": "<the user-facing question>",
      "expected_answer_contains": ["<substring 1>", "<substring 2>", ...],
      "expected_sources": [{"section": "<DRHP section name>", "page_start": <int>, "page_end": <int>}],
      "is_refusal_expected": true | false
    }
    ```

    For refusal-eligible entries: `expected_answer_contains` MAY be empty `[]` and `expected_sources` MUST be `[]` (the test verifies the answer is a RefusalResponse, not a GroundedAnswer).

    For grounded entries: `expected_answer_contains` is a list of substrings the answer prose MUST include (normalized comparison: NFKC + lowercase + whitespace-collapsed per the cite-check normalization rule from RESEARCH §Pattern 3). `expected_sources` is the list of DRHP sections + page ranges the answer should cite (cite-check passes if ANY claim's source matches ANY entry in this list).

    Create `scripts/run_eval.py` — a typer CLI that:
    1. Loads `tests/eval/gold_set.jsonl` line-by-line.
    2. For each entry: invokes `agent.graph.invoke_with_tracing(initial_state, question)` (from Task 2) — uses real Qdrant + real Gemini (gated by env vars; fails gracefully with a clear error if env vars are missing).
    3. For each grounded entry: computes
       - **Citation accuracy** (custom metric, per RESEARCH §"RAG Stack Specifics" line "citation accuracy (custom metric — did the cited page actually contain the claim)"): proportion of returned claims whose `sources[0].page_start..page_end` overlaps any entry in `expected_sources`. Threshold: ≥ 0.80 means PASS at Phase 1 baseline (release-gate at 0.95 lives in Phase 3 EVAL-03; Phase 1 only measures).
       - **Answer substring coverage**: proportion of `expected_answer_contains` substrings present in the normalized `answer_prose`. Threshold: ≥ 0.50 at Phase 1 baseline.
       - **RAGAS faithfulness** (via `ragas.metrics.Faithfulness` per STACK.md): runs the LLM-as-judge faithfulness scorer. Records the value; no gate at Phase 1 (Phase 3 EVAL-03 release-gates at 0.95).
       - **Recall@k (k=5)**: proportion of `expected_sources` page-ranges that appear in the top-5 reranked chunks before generation. Records the value; no gate at Phase 1.
    4. For each refusal-eligible entry: asserts the agent returns a `RefusalResponse` (NOT a `GroundedAnswer`). Records pass/fail.
    5. Emits a markdown report at `eval/reports/<YYYY-MM-DD>-phase1-baseline.md` with sections: "Summary" (overall pass count), "Per-Entry Results" (table with qid/category/citation_accuracy/answer_coverage/ragas_faithfulness/recall@5/refusal_correctly_fired), "Gold Set Statistics" (count per category, average scores), and "Notes" (free text — executor fills in any per-entry observations).
    6. Exits 0 if no entry crashed (refusal-fired-when-expected is a pass; refusal-fired-when-grounded-expected is a recorded failure but the script still exits 0 — Phase 1 measures, does not gate).

    Create `tests/eval/test_phase1_eval.py` — a pytest wrapper that:
    - Has a single test `test_phase1_eval_baseline` decorated with `@pytest.mark.eval` and `@pytest.mark.skipif(not pytest.config.getoption("--run-eval"), ...)`.
    - Body: invokes `scripts.run_eval.main()` (the typer CLI function), asserts exit code 0, asserts the markdown report was committed at the expected path.
    - The pytest CLI flag `--run-eval` already exists from 01-VALIDATION.md Test Infrastructure (Wave 0 set it up); this test slots into the existing infrastructure.

    Create `eval/reports/.gitkeep` so the empty directory is tracked. The actual first report file is created by Task 4 (running `scripts/run_eval.py` end-to-end and committing the output).

    Per source-audit: this task implements the eval-suite-first-run requirement from the planning context; covers EVAL-01 (faithfulness, RAGAS) + EVAL-03 (numeric-faithfulness, custom metric) BASELINE MEASUREMENT only — Phase 1 does NOT release-gate these REQs (per planning context: "lays the foundation for measuring (but does NOT release-gate) EVAL-01 + EVAL-03"). The actual release gates live in Phase 3 (EVAL-03) and Phase 6 (EVAL-01).
  </action>
  <verify>
    <automated>test -f tests/eval/gold_set.jsonl &amp;&amp; test $(wc -l &lt; tests/eval/gold_set.jsonl) -ge 10 &amp;&amp; test $(wc -l &lt; tests/eval/gold_set.jsonl) -le 15 &amp;&amp; python -c "import json, pathlib, collections; entries = [json.loads(l) for l in pathlib.Path('tests/eval/gold_set.jsonl').read_text().splitlines() if l.strip()]; assert len(entries) >= 10 and len(entries) <= 15, f'gold set must have 10-15 entries (got {len(entries)})'; categories = collections.Counter(e['category'] for e in entries); print('categories:', dict(categories)); assert categories['factual'] == 5, f'must have 5 factual (got {categories[\"factual\"]})'; assert categories['numeric'] == 3, f'must have 3 numeric (got {categories[\"numeric\"]})'; assert categories['risk-factor'] == 3, f'must have 3 risk-factor (got {categories[\"risk-factor\"]})'; assert categories['refusal-eligible'] == 2, f'must have 2 refusal-eligible (got {categories[\"refusal-eligible\"]})'; assert all(set(e.keys()) >= {'qid','category','question','expected_answer_contains','expected_sources','is_refusal_expected'} for e in entries), 'schema mismatch'; refusals = [e for e in entries if e['category'] == 'refusal-eligible']; assert all(e['is_refusal_expected'] is True for e in refusals), 'refusal-eligible entries must have is_refusal_expected: true'; grounded = [e for e in entries if e['category'] != 'refusal-eligible']; assert all(e['is_refusal_expected'] is False for e in grounded), 'grounded entries must have is_refusal_expected: false'; assert all(len(e['question']) > 10 and e['question'] != 'TBD-WAVE5' for e in entries), 'Wave 0 stub content must be replaced with real questions'; print('OK')" &amp;&amp; test -f scripts/run_eval.py &amp;&amp; python -c "import ast; ast.parse(open('scripts/run_eval.py').read()); print('scripts/run_eval.py parses')" &amp;&amp; test -f tests/eval/test_phase1_eval.py &amp;&amp; test -d eval/reports &amp;&amp; test -f eval/reports/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `tests/eval/gold_set.jsonl` has between 10 and 15 lines, each a valid JSON object
    - Distribution: exactly 5 factual, 3 numeric, 3 risk-factor, 2 refusal-eligible
    - Every entry has all 6 required keys: qid, category, question, expected_answer_contains, expected_sources, is_refusal_expected
    - No entry has `question == "TBD-WAVE5"` (Wave 0 stubs MUST be replaced)
    - All refusal-eligible entries have `is_refusal_expected: true`; all others have `is_refusal_expected: false`
    - `scripts/run_eval.py` exists and parses as valid Python (ast.parse passes)
    - `tests/eval/test_phase1_eval.py` exists and is gated by `--run-eval`
    - `eval/reports/.gitkeep` exists (directory tracked even before first run)
    - Implements EVAL-01 + EVAL-03 baseline MEASUREMENT (not release gate — that's Phase 3 + Phase 6)
  </acceptance_criteria>
  <done>Gold set is hand-curated against the actual Swiggy DRHP content; eval suite is runnable via `scripts/run_eval.py` or `pytest --run-eval`; the first eval report is generated and committed in Task 4. The baseline-measurement infrastructure for Phase 3 EVAL-03 + Phase 6 EVAL-01 release gates is in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HF Spaces deployment + Langfuse project creation + cron pinger setup (manual; gated)</name>
  <what-built>
    The previous three tasks have shipped the code, observability wiring, and eval suite — but Wave 5 is not complete until the Space is live on the public internet, the Langfuse project is receiving traces, and the cron pinger is keeping it warm. None of these three actions can be automated: HF does not expose Space creation or secrets-write via API for free-tier accounts (the Spaces UI is the only path), Langfuse project creation requires an account-bound dashboard click, and cron-job.org account creation is similarly UI-gated. CC has shipped the runbook (`docs/DEPLOY.md` from Task 1) and the optional GitHub Actions workflow alternative (`scripts/cron_pinger.yml`).

    What's been automated and is ready:
    - README.md with HF Spaces YAML frontmatter (Task 1) — ready to be read by HF Spaces on first push
    - .env.example with 7 env vars documented (Task 1) — template for the secrets dashboard
    - docs/DEPLOY.md with the 10-step runbook (Task 1) — the script you follow below
    - app/observability/* with no-op-when-disabled fallback (Task 2) — local dev works without Langfuse; once secrets are set on the Space, traces start flowing automatically
    - tests/eval/gold_set.jsonl with hand-curated entries (Task 3) — ready for the first eval run, which you run AFTER the deploy is live
    - scripts/cron_pinger.yml — optional GitHub Actions cron workflow you can copy to `.github/workflows/ping.yml` if you prefer Option B
  </what-built>
  <how-to-verify>
    Follow `docs/DEPLOY.md` step-by-step. Concretely:

    1. **Push code to GitHub** — `git push origin main` from this repo. Confirm all Wave 0-5 commits are upstream.

    2. **Create Langfuse Cloud account + project** — visit https://cloud.langfuse.com/auth/sign-up; sign up for free tier; create project named `drhplens-phase1`; from project Settings → API Keys, copy the public key (starts `pk-lf-...`) and secret key (starts `sk-lf-...`).

    3. **Create HF Spaces account + Space** — visit https://huggingface.co/join (if no account); visit https://huggingface.co/new-space; choose: sdk: streamlit, hardware: CPU basic FREE, name: `drhplens` (or your preferred slug), visibility: Public. After creation, link to your GitHub repo (Settings → Linked Repositories) OR add the HF git remote and `git push hf main`.

    4. **Configure Space secrets** — Space → Settings → Variables and secrets → Secrets → add 7 entries (one per line in `.env.example`):
       - `GEMINI_API_KEY` (from Google AI Studio)
       - `GROQ_API_KEY` (from Groq Console)
       - `QDRANT_URL` (from Wave 2 Qdrant Cloud setup — must already exist)
       - `QDRANT_API_KEY` (from Wave 2)
       - `LANGFUSE_PUBLIC_KEY` (from step 2)
       - `LANGFUSE_SECRET_KEY` (from step 2)
       - `LANGFUSE_HOST` — `https://cloud.langfuse.com` (or `https://us.cloud.langfuse.com` if you signed up in the US region)

    5. **Wait for first build** — HF Spaces will build automatically on push. First build is slow (3-10 min) because the bge-m3 + reranker weights download (~1.7GB total) and Streamlit cold-boots. Watch the build log; report any errors.

    6. **Smoke-test the live URL** — visit `https://huggingface.co/spaces/<your-username>/drhplens` in an incognito window. Verify:
       - First-use modal appears with the D-07 anchor copy
       - Click "I understand"; home page renders with chat input + persistent footer disclaimer (D-08)
       - Ask "What is Swiggy's issue size?" — expect a cited answer with at least one `[1]` chip
       - Click `[1]` chip — expect inline expander showing the DRHP source span + SEBI link
       - Ask "What does Swiggy say about Mars colonization?" — expect refusal banner with reformulation suggestions (Gate 1)
       - Ask "Should I subscribe to Swiggy?" — expect refusal (Gate 2, banned-token after regenerate-once)
       - Open Chrome DevTools at 375px width — verify mobile-responsive (chips remain tappable at 44×44px)
       - Visit `/methodology` — verify stub page renders (no 404)

    7. **Verify Langfuse traces** — open https://cloud.langfuse.com → drhplens-phase1 → Traces; verify each smoke-test question produced a trace; verify each trace has exactly the 9 spans (intake, retrieve, rerank, gate1_check, decompose, generate, scrub, cite_check, emit per RESEARCH §Trace shape end-to-end); verify Claim spans carry `claim_id` metadata; verify the `faithfulness_via_cite_check` custom score is logged on grounded-answer traces; verify refusal traces carry `refusal_reason` metadata at the trace level.

    8. **Configure cron pinger (PICK ONE)** —
       - **Option A (recommended; more punctual per RESEARCH §Pitfall 6):** create cron-job.org free account; new cronjob; URL: `https://huggingface.co/spaces/<your-username>/drhplens`; schedule `*/8 * * * *`; active hours 06:00-23:00 IST.
       - **Option B (no extra account; ±5-15 min skew):** copy `scripts/cron_pinger.yml` to `.github/workflows/ping.yml`; commit + push; enable GitHub Actions in repo Settings → Actions → General → Allow all actions.

    9. **Cold-start verification** — wait at least 35 minutes without traffic to the Space; visit the public URL in incognito; verify the warming-up copy renders within 5s and the home page renders within 60s (OPS-02 acceptance criterion; PITFALL 6 closure).

    10. **Run the first eval suite** — locally (env vars from `.env`): `python scripts/run_eval.py`. This invokes the live Qdrant + live Gemini + live Langfuse and writes `eval/reports/<YYYY-MM-DD>-phase1-baseline.md`. Commit the report. Verify it has Summary + Per-Entry Results + Gold Set Statistics sections.

    11. **Update agent/policies.py via Gate 1 calibration** — locally: `python scripts/calibrate_gate1.py` (from Task 5). The script sweeps GATE1_THRESHOLD from -2.0 to +2.0; pick the recommended threshold; update `agent/policies.py:GATE1_THRESHOLD` from `0.0` (Wave 3 default) to the calibrated value with an inline comment `# Calibrated <YYYY-MM-DD> against tests/eval/gold_set.jsonl (n=N), recommended value from scripts/calibrate_gate1.py`. Push and let the Space rebuild.

    Report back to me with:
    - The public HF Spaces URL
    - Confirmation that the cold-start retest (step 9) succeeded
    - Which cron pinger option you picked (A or B)
    - The calibrated GATE1_THRESHOLD value
    - Path to the committed `eval/reports/<date>-phase1-baseline.md`
    - Any issues encountered (especially: HF build failures, Langfuse trace shape mismatches, eval failures > expected)
  </how-to-verify>
  <resume-signal>
    Reply with: the public URL + cold-start retest result + cron option (A/B) + calibrated GATE1_THRESHOLD + path to eval report. If any step failed, paste the exact error and I'll diagnose before continuing. If everything is green, type "all green" and I'll proceed to Task 5 (Gate 1 calibration script — already invoked in step 11 above but the script file itself needs to ship in this wave for reproducibility).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Ship Gate 1 calibration script + cron_pinger.yml workflow + update agent/policies.py with calibrated threshold</name>
  <files>scripts/calibrate_gate1.py, scripts/cron_pinger.yml, agent/policies.py</files>
  <read_first>
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Open Questions" Q1 — the EXACT calibration procedure: "Sweep ±2.0 in 0.5 steps. Pick the threshold that maximizes (correct_grounded + correct_refusals) on the gold set. Document the chosen value in agent/policies.py as a named constant."
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-RESEARCH.md §"Code Examples" "Cron pinger via cron-job.org" — schedule `*/8 * * * *`, active hours 06:00-23:00 IST; GitHub Actions has ±5-15 min skew
    - .planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-04-PLAN.md — agent/policies.py exports GATE1_THRESHOLD (currently 0.0 per Wave 3 default)
    - tests/eval/gold_set.jsonl — the calibration dataset (Task 3 of this plan committed the content)
  </read_first>
  <action>
    Create `scripts/calibrate_gate1.py` — a typer CLI that implements the RESEARCH Open Question 1 procedure verbatim:

    1. Loads `tests/eval/gold_set.jsonl`.
    2. For each entry: invokes `agent.graph.GRAPH` with the question; captures `state["gate1_max_score"]` (the post-rerank max score, added in Wave 3 per the `attach_gate1_metadata_to_span` wiring from Task 2). DOES NOT invoke the LLM (gate1_check runs before generate, so we can short-circuit by reading state).
    3. Sweeps candidate thresholds from -2.0 to +2.0 in 0.5 steps (9 values: -2.0, -1.5, -1.0, -0.5, 0.0, 0.5, 1.0, 1.5, 2.0).
    4. For each candidate threshold `τ` and each gold entry: predicts `would_refuse = (gate1_max_score < τ)`; compares against `is_refusal_expected`; counts true-positive refusals (refusal-expected AND would_refuse), true-negative grounded (grounded-expected AND NOT would_refuse), false-positives (grounded-expected AND would_refuse — i.e., over-refusal), false-negatives (refusal-expected AND NOT would_refuse — i.e., LLM gets called when it shouldn't).
    5. For each candidate: prints precision (TP / (TP + FP)), recall (TP / (TP + FN)), and the RESEARCH-locked objective (correct_grounded + correct_refusals = TP + TN).
    6. Prints a recommended threshold: the candidate that maximizes (TP + TN). Ties broken by lower threshold (more permissive = fewer false refusals = better UX).
    7. Prints exact text the executor pastes into `agent/policies.py`:
       ```
       GATE1_THRESHOLD: float = <chosen>  # Calibrated <YYYY-MM-DD> against tests/eval/gold_set.jsonl (n=<count>), recommended value from scripts/calibrate_gate1.py (correct_grounded+correct_refusals=<score>/<total>)
       ```
    8. Output is human-readable markdown to stdout AND a saved snapshot at `eval/reports/<YYYY-MM-DD>-gate1-calibration.md`.

    Implementation notes:
    - Uses the same `from agent.graph import build_graph` interface as Task 2's invoke_with_tracing.
    - DOES NOT actually run the full graph for every threshold — runs the graph ONCE per gold entry (gathering the gate1_max_score for each question), then sweeps thresholds analytically against the captured scores. This keeps total Gemini cost = N gold entries × 1 LLM call (decompose only; gate1 happens pre-LLM) = ~13 calls = trivially under quota.
    - For refusal-eligible entries: still invokes the graph (the gate1 score itself is the data the calibration needs).
    - For each entry, ALSO logs whether the cite_check would have passed (independent of gate1 — surfaces the Gate 2 failure mode separately so the executor knows whether to also tune CITE_CHECK_TOKEN_RATIO).

    After running this script in Task 4 step 11, update `agent/policies.py`:
    - Change line `GATE1_THRESHOLD: float = 0.0  # per Open Question 1; Wave 5 calibrates against gold set` to the calibrated value with the inline comment from step 7 above.
    - Re-export remains unchanged so all Wave 3-4 callers work without modification.
    - The Space rebuilds on push; the new threshold is live.

    Create `scripts/cron_pinger.yml` — a GitHub Actions workflow file (Option B alternative to cron-job.org). Per RESEARCH §Code Examples Cron pinger note "GitHub Actions schedule has documented 5-15 min skew" — annotate this in a top-of-file comment so the user knows Option A is more punctual.

    Workflow content (DO NOT auto-enable — file lives in `scripts/` not `.github/workflows/` so the user moves it intentionally; this is a documented optional path):

    ```yaml
    # OPTIONAL — copy to .github/workflows/ping.yml to enable.
    # Alternative: cron-job.org free account (more punctual per RESEARCH §Pitfall 6).
    # GitHub Actions schedule cron skews 5-15 min; cron-job.org schedule is exact.
    name: HF Spaces Cold-Start Pinger
    on:
      schedule:
        - cron: "*/8 * * * *"   # every 8 min — under HF Spaces sleep threshold per RESEARCH
      workflow_dispatch:
    jobs:
      ping:
        runs-on: ubuntu-latest
        steps:
          - name: Ping HF Space
            run: |
              # Replace <user> with your HF username before enabling
              URL="https://huggingface.co/spaces/<user>/drhplens"
              echo "Pinging $URL ..."
              curl -s -o /dev/null -w "%{http_code}\n" -m 30 "$URL" || true
    ```

    Per source-audit: scripts/calibrate_gate1.py closes RESEARCH Open Question 1 (the Gate 1 threshold floor for D-05); scripts/cron_pinger.yml closes the CEO review T6 callout (HF Spaces cold-start mitigation) by providing Option B of the cron pinger choice (Option A is configured manually in Task 4 step 8).
  </action>
  <verify>
    <automated>test -f scripts/calibrate_gate1.py &amp;&amp; python -c "import ast; ast.parse(open('scripts/calibrate_gate1.py').read()); print('calibrate_gate1.py parses')" &amp;&amp; test -f scripts/cron_pinger.yml &amp;&amp; python -c "import yaml; yaml.safe_load(open('scripts/cron_pinger.yml').read()); print('cron_pinger.yml parses as YAML')" &amp;&amp; grep -q '\*/8 \* \* \* \*' scripts/cron_pinger.yml &amp;&amp; grep -q 'huggingface.co/spaces' scripts/cron_pinger.yml &amp;&amp; grep -q 'GATE1_THRESHOLD' agent/policies.py &amp;&amp; (grep -E 'GATE1_THRESHOLD.*=.*[+-]?[0-9]+\.[0-9]+' agent/policies.py | head -1 | grep -qE 'Calibrated|calibrate_gate1') &amp;&amp; python -c "from agent.policies import GATE1_THRESHOLD; assert isinstance(GATE1_THRESHOLD, float) and -2.0 &lt;= GATE1_THRESHOLD &lt;= 2.0, f'GATE1_THRESHOLD={GATE1_THRESHOLD} out of swept range'; print(f'GATE1_THRESHOLD={GATE1_THRESHOLD}')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/calibrate_gate1.py` exists, parses as valid Python, and implements the RESEARCH Open Question 1 procedure (sweep -2.0 to +2.0 in 0.5 steps; maximize correct_grounded + correct_refusals)
    - `scripts/cron_pinger.yml` exists, parses as valid YAML, contains the `*/8 * * * *` schedule, and references `huggingface.co/spaces` URL
    - `agent/policies.py` GATE1_THRESHOLD value is in range [-2.0, +2.0] (the swept range)
    - `agent/policies.py` GATE1_THRESHOLD line has an inline comment containing "Calibrated" or "calibrate_gate1" (so future readers know the value is empirical, not arbitrary)
    - The script's stdout output (when run in Task 4 step 11) lists all 9 candidate thresholds with their precision/recall/objective metrics
    - Closes RESEARCH Open Question 1 (Gate 1 threshold floor)
    - Closes CEO review T6 (HF Spaces cold-start cron pinger; Option B of the two-option choice)
  </acceptance_criteria>
  <done>Gate 1 threshold is empirically calibrated against the Phase 1 gold set with the procedure documented in RESEARCH, not eyeballed; cron_pinger.yml is shipped as the documented Option B for the cron pinger (Option A configured manually in Task 4 step 8); agent/policies.py records the calibration provenance for future readers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Phase 1 close — verify all 13 REQs satisfied + flip 01-VALIDATION.md nyquist_compliant: true + write 01-06-SUMMARY.md</name>
  <what-built>
    All five preceding tasks (across Waves 0-5 and this wave's Tasks 1-5) have shipped the Phase 1 deliverables. This final task is the formal Phase 1 close: a checklist verification that all 13 REQs from ROADMAP have a satisfied test or manual verification, the public URL is live and pinged, the eval baseline is committed, and the cross-cutting `claim_id` invariant is in production traces. Per 01-VALIDATION.md, this is also where the `nyquist_compliant: true` flag flips — meaning Phase 1 is fully ready for `/gsd-verify-work` and the user can proceed to `/gsd-plan-phase 2` with the Walking Skeleton inherited unchanged.

    What's been automated and is ready:
    - Per-REQ test mapping is complete (01-VALIDATION.md §Per-Task Verification Map has rows for all 13 REQs across Waves 0-5)
    - Eval baseline report committed (Task 4 step 10)
    - Gate 1 threshold calibrated (Task 4 step 11 + Task 5)
    - Langfuse traces visible in production (Task 4 step 7)
    - Public URL accessible (Task 4 step 6)
    - Cron pinger live (Task 4 step 8)
    - Cold-start retest passed (Task 4 step 9)
  </what-built>
  <how-to-verify>
    Walk through this final checklist. Each item maps to one of the 13 Phase 1 REQs (or the cross-cutting invariant) and points to the artifact that closes it.

    **REQ-by-REQ verification (from ROADMAP Phase 1 REQs):**

    1. **INGEST-01** — Swiggy DRHP PDF committed at `data/swiggy_drhp/swiggy_prospectus_2024_11.pdf` with SHA-256 pin; `pytest tests/unit/test_drhp_integrity.py -x -q` passes. (Wave 0 closed this.)
    2. **INGEST-02** — Docling parses Swiggy DRHP into > 100 sections; `pytest tests/unit/test_parser.py tests/integration/test_docling_parse.py -x -q` passes. (Wave 2 closed this.)
    3. **INGEST-03** — Chunker produces 1500-2500 chunks with valid payload; `pytest tests/unit/test_chunker.py tests/integration/test_qdrant_ingest.py -x -q` passes. (Wave 2 closed this.)
    4. **RAG-01** — End-to-end Q&A: `python -m agent.demo "What is Swiggy's issue size?"` returns a cited answer; live URL smoke test in Task 4 step 6 confirmed. (Wave 3 + 4 closed this; Wave 5 confirmed in production.)
    5. **RAG-02** — Every claim has a span-level citation: `pytest tests/unit/test_schemas.py tests/unit/test_citation_renderer.py -x -q` passes; live `[1]` chip click in Task 4 step 6 confirmed inline expansion. (Wave 1 + 3 + 4 closed this; Wave 5 confirmed in production.)
    6. **RAG-03** — Refusal posture: `python -m agent.demo "What does Swiggy say about Mars colonization?"` returns refusal with reformulation; live URL smoke test in Task 4 step 6 confirmed. (Wave 3 closed this; Wave 5 confirmed in production. Gate 1 threshold is now CALIBRATED per Task 5.)
    7. **TRUST-01** — Three disclaimer surfaces (modal + persistent footer + per-answer footer): `pytest tests/unit/test_disclaimer_surface.py -x -q` passes; live URL smoke test in Task 4 step 6 confirmed all three surfaces render. (Wave 1 + 4 closed this; Wave 5 confirmed in production.)
    8. **TRUST-02** — Banned-token scrubber: `pytest tests/unit/test_scrubber.py -x -q` passes (every conjugation blocked); live "Should I subscribe to Swiggy?" smoke test in Task 4 step 6 confirmed Gate 2 fires. (Wave 1 + 3 closed this; Wave 5 confirmed in production.)
    9. **TRUST-03** — SEBI Jan-2025 RA disclosure compliance posture: `pytest tests/unit/test_copy_no_banned_tokens.py -x -q` passes (all user-facing strings clean); D-08 three-surfaces present in deployed app. Note: Final legal-review checkpoint is OUT of Phase 1 scope per 01-VALIDATION.md "Manual-Only Verifications" (gates Phase 6 public launch).
    10. **TRUST-04** — Non-LLM cite-check: `pytest tests/unit/test_cite_check.py tests/integration/test_drhp_prompt_injection.py -x -q` passes (deterministic span-match algorithm rejects unsupported claims). (Wave 1 + 3 closed this.)
    11. **UI-01** — Web app mobile-responsive: live URL DevTools 375px check in Task 4 step 6 confirmed. (Wave 4 closed this; Wave 5 confirmed in production.)
    12. **UI-02** — Citations as superscript chips that expand to source snippets: `pytest tests/unit/test_citation_renderer.py -x -q` passes; live `[1]` chip click in Task 4 step 6 confirmed. (Wave 4 closed this; Wave 5 confirmed in production.)
    13. **OPS-02** — Publicly deployed on free-tier host: live URL `https://huggingface.co/spaces/<user>/drhplens` confirmed in Task 4 step 6; cold-start retest in step 9 confirmed within-60s acceptance; cron pinger live (step 8). **Wave 5 closed this REQ.**

    **Cross-cutting invariant (ROADMAP + CEO review T5):**
    - **claim_id traces from day one** — Langfuse traces in step 7 confirmed every Claim span carries `claim_id` metadata; this is the Phase 3 METHOD-01 consumer contract. CONFIRMED LIVE.

    **CEO review T6 callouts (Phase 1 PLAN.md):**
    - HF Spaces cold-start cron pinger documented in `docs/DEPLOY.md` (step 7 of the deploy runbook) and shipped as `scripts/cron_pinger.yml` (Option B). CONFIRMED.
    - Qdrant 1GB sizing documented in `docs/DEPLOY.md` Prerequisites (Wave 2 ingest verified 1500-2500 chunks fits in free tier). CONFIRMED.

    **Final actions:**

    a. Edit `01-VALIDATION.md` frontmatter: change `nyquist_compliant: false` to `nyquist_compliant: true`. Change `status: draft` to `status: complete`. Change `wave_0_complete: false` to `wave_0_complete: true` if not already done in Wave 0.

    b. Walk through 01-VALIDATION.md §"Per-Task Verification Map" — update the `Status` column for every row: ⬜ pending → ✅ green (or ❌ red with a documented exception). If any row is ❌, file a follow-up task BEFORE closing the phase; do not silently accept a red row.

    c. Walk through 01-VALIDATION.md §"Manual-Only Verifications" — confirm each manual check has been performed per Task 4 step 6.

    d. Walk through 01-VALIDATION.md §"Validation Sign-Off" checklist — flip each `[ ]` to `[x]` only where genuinely true.

    e. Write `.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-06-SUMMARY.md` covering:
       - **Public URL** (from Task 4 step 6)
       - **All 13 Phase 1 REQs** — table form, one row per REQ, status, link to closing artifact (test command or SUMMARY file)
       - **First eval baseline** — values from `eval/reports/<date>-phase1-baseline.md` (per-category citation accuracy, RAGAS faithfulness, recall@5)
       - **Calibrated GATE1_THRESHOLD** value + provenance (from Task 5)
       - **Cross-cutting `claim_id` trace invariant** — confirmed live in Langfuse production traces (with a screenshot link or Langfuse trace URL example)
       - **PHASE 1 CLOSED** — explicit signoff marker
       - **Handoff to Phase 2** — what's locked (SKELETON §A-G; Phase 1 schemas; cite-check algorithm; banned-token list) vs what's plastic (Gate 1 threshold may re-tune in Phase 3 against the bigger gold set)
       - **Phase 1 lessons learned** — feed into RETROSPECTIVE.md if it exists; otherwise note for Phase 2 retrospective

    f. Confirm the commit message uses the project's docs convention: `docs(01): close phase 1 — public URL live + first eval baseline + claim_id traces`.

    Report back to me with:
    - Path to 01-06-SUMMARY.md
    - Confirmation that 01-VALIDATION.md frontmatter is `nyquist_compliant: true`
    - Number of ✅ vs ❌ rows in the Per-Task Verification Map
    - The first-eval-baseline summary numbers (so we know what Phase 3 EVAL-03 will improve against)
  </how-to-verify>
  <resume-signal>
    Reply with: "phase 1 closed — public URL: <url>, eval baseline: <summary>, all 13 REQs green, nyquist_compliant: true". Or report any red rows with the diagnostic detail and I'll plan the gap-closure work (which may need `/gsd-plan-phase 1 --gaps`).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Wave 5 — deploy + observability)

| Boundary | Description |
|----------|-------------|
| repo → HF Spaces runtime | YAML frontmatter + requirements.txt + secrets crossing into the Spaces environment; supply-chain + secret-leak threats apply |
| HF Spaces app → Langfuse Cloud | Trace payloads (questions, generated answers, claim_ids) sent over HTTPS to Langfuse Cloud; information-disclosure threat applies |
| HF Spaces app → Gemini / Groq APIs | LLM API calls with API keys in headers; rate-limit abuse + secret-leak threats apply |
| public internet → HF Spaces app | Unauthenticated user input crossing into the app; prompt-injection + rate-limit threats apply |
| cron-job.org / GitHub Actions → HF Spaces URL | Periodic ping requests; no auth threat (the URL is public anyway) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Denial of Service | Public HF Spaces URL (rate-limit abuse) | mitigate | Gemini 1500/day free quota is the natural circuit breaker (per RESEARCH §Security Domain "Rate-limit abuse"); when exceeded, the rate-limit error-state UI copy from UI-SPEC fires; no per-IP rate limiting needed in Phase 1 (HF Spaces doesn't expose easy hooks; acceptable per RESEARCH disposition). Cron pinger ≤ 8-min cadence stays well under quota (180 pings/day < 1500). DEPLOY.md step 9 includes cost monitoring as the residual-risk control. |
| T-1-04 | Information Disclosure | HF Spaces secrets (Gemini/Groq/Qdrant/Langfuse keys) | mitigate | All 7 secrets are configured via HF Spaces secrets UI per DEPLOY.md step 4; NEVER committed to git; `.env.example` is documentation-only with empty values; `.gitignore` blocks `.env`; verify-step in Task 1 greps for accidental key-prefix leakage. Langfuse traces contain user questions (DRHP content is already public per RESEARCH §V8) but traces themselves are auth'd to the developer's Langfuse project — acceptable per Phase 1 scope. |
| T-1-05 | Tampering | Supply-chain — pyproject.toml dependency installs | mitigate | All packages locked at floor + ceiling versions in Wave 0 (per 01-01-PLAN.md acceptance criteria); Package Legitimacy Audit in RESEARCH §"Package Legitimacy Audit" has all 23 packages dispositioned `Approved`; no `[ASSUMED]`/`[SUS]` entries in this wave's new code (Langfuse SDK is the only new install, well-established 2+ years on PyPI per Audit row). Optional `pip-audit` in CI documented as a Phase 6 hardening task. |
| T-1-06 | Tampering | Streamlit `unsafe_allow_html=True` XSS (citation chips) | accept-as-mitigated | Wave 4 owned the renderer; Wave 5 verifies no regression via the live URL smoke test (Task 4 step 6 — DevTools mobile check exercises the chip render path against real DRHP content; an XSS-attempt input is part of `tests/unit/test_citation_renderer.py` Wave 4 unit test, re-run via `pytest tests/unit -x -q` before deploy push in Task 4 step 1). |
| T-1-08 | Information Disclosure | Langfuse trace exposes user questions | accept | Per RESEARCH §V8 "Langfuse traces are private to the developer's Langfuse project (auth'd via secret key). Acceptable for Phase 1." DRHP content is already public on SEBI's website (no incremental disclosure risk). Phase 2 may want to add a privacy notice (deferred per RESEARCH). |
| T-1-09 | Spoofing | Cron pinger origin (cron-job.org / GitHub Actions) impersonating users | accept | Pings hit the public homepage; no auth gate; no per-IP rate limit. Worst case: a few hundred extra warm-up requests/day, all well within Gemini quota even if the entire quota were consumed by pinger noise (~180/day vs 1500/day budget). |
| T-1-SC | Tampering | npm/pip/cargo installs | mitigate | No `[ASSUMED]`/`[SUS]` packages added in Wave 5; Langfuse is the only new dependency (already Approved in Wave 0 Package Legitimacy Audit). Supply-chain checkpoint not required per planner-discretion disposition note in RESEARCH §"Package Legitimacy Audit". |
</threat_model>

<verification>
- All 6 tasks' automated commands pass (Tasks 1, 2, 3, 5 are automated; Tasks 4 and 6 are blocking human-verify)
- `pytest tests/ -x -q --timeout=60` (full suite, without --run-eval and --run-langfuse) exits 0 — no regression from Wave 0-4
- `pytest tests/integration/test_langfuse_trace.py::test_langfuse_client_noop_when_keys_missing -x -q` exits 0 (no-op fallback works without env vars)
- `python -c "from agent.graph import invoke_with_tracing"` exits 0
- `python -c "from app.observability.langfuse_client import is_enabled, get_callback_handler"` exits 0
- `python -c "import yaml; yaml.safe_load(open('scripts/cron_pinger.yml').read())"` exits 0
- `python -c "from agent.policies import GATE1_THRESHOLD; assert -2.0 <= GATE1_THRESHOLD <= 2.0"` exits 0
- `grep -c '^# ' README.md` and `grep -c "sleep_time: 1800" README.md` both ≥ 1 (frontmatter valid)
- Manual verification (Task 4): public URL returns 200; cold-start retest passes within 60s; Langfuse traces show 9 spans + claim_ids; cron pinger live
- Manual verification (Task 6): 01-VALIDATION.md nyquist_compliant: true; all 13 Phase 1 REQs green or with documented exception; 01-06-SUMMARY.md committed with public URL + eval baseline
</verification>

<success_criteria>
- Public HF Spaces URL is live, mobile-responsive, and cold-start-mitigated (cron pinger live, sleep_time: 1800 in YAML)
- Langfuse traces contain `claim_id` references for every Claim from day one — the Phase 3 METHOD-01 consumer contract is in production, not just in code
- Eval suite first run completed; baseline metrics committed to `eval/reports/<date>-phase1-baseline.md` (RAGAS faithfulness + recall@5 + custom citation accuracy)
- Gate 1 threshold empirically calibrated (RESEARCH Open Question 1 closed); `agent/policies.py` records calibration provenance with inline comment
- 10-15 entry gold set hand-curated against the actual Swiggy DRHP content (5 factual + 3 numeric + 3 risk-factor + 2 refusal-eligible)
- All 13 Phase 1 REQs satisfied per ROADMAP traceability (INGEST-01/02/03, RAG-01/02/03, TRUST-01/02/03/04, UI-01/02, OPS-02)
- 01-VALIDATION.md frontmatter flipped to `nyquist_compliant: true`; all Per-Task Verification Map rows ✅ green
- 01-06-SUMMARY.md committed with public URL + first eval baseline + handoff to Phase 2
- Walking Skeleton (per SKELETON.md) is fully closed — UI from Wave 4 + deploy from Wave 5 attached to the Wave 3 skeleton; Phases 2-6 inherit the skeleton unchanged per SKELETON §F-G
- CEO review T6 callouts addressed (HF Spaces cold-start cron pinger + Qdrant 1GB sizing in `docs/DEPLOY.md`)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-mvp-a-cited-q-a-on-one-ipo/01-06-SUMMARY.md` when done. Summary must include:
- The public HF Spaces URL
- Table mapping all 13 Phase 1 REQs to closing artifact (test command or SUMMARY file)
- First eval baseline numbers (per-category citation accuracy, RAGAS faithfulness, recall@5)
- Calibrated GATE1_THRESHOLD value + calibration date + dataset size
- Cron pinger option picked (A or B) + URL/workflow path
- Confirmation that 01-VALIDATION.md nyquist_compliant: true is flipped
- Cross-cutting `claim_id` trace invariant confirmation (Langfuse trace URL example)
- Handoff to Phase 2 — what's locked vs plastic
- Phase 1 lessons learned for RETROSPECTIVE.md (or noted for Phase 2 retro)
</output>

## WAVE 5 COMPLETE — PHASE 1 CLOSED
