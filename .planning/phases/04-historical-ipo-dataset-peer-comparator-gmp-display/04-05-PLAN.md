---
phase: 04-historical-ipo-dataset-peer-comparator-gmp-display
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - app/static/drhplens.css
  - ui/copy.py
  - ui/snapshot_blocks.py
  - pages/02_snapshot.py
  - tests/unit/test_copy_no_banned_tokens.py
autonomous: false
requirements: [PEER-01, PEER-02, UI-04]
user_setup: []

must_haves:
  truths:
    - "A user sees the DRHP's own disclosed peer set as a table (rows=companies, cols=P/E·P/B·EV/EBITDA·ROE), the peer set anchored by a citation chip to its DRHP section (PEER-01, D4-04)"
    - "Each multiple cell shows its per-cell muted provenance superscript + a legend; missing renders '—', undefined P/E renders 'NM'; no red/green, no fabrication (PEER-02, D4-05, D4-09)"
    - "When the DRHP names no peers, the block shows the honest empty-state, no fabricated set (D4-06)"
    - "The 8 glossary terms (RPT, QIB, NII, RII, GMP, OFS, DRHP, anchor investor) carry pure-CSS keyboard/mobile-accessible tooltips (UI-04, D4-08)"
  artifacts:
    - path: "ui/snapshot_blocks.py"
      provides: "render_peer_table + glossary term helper"
      contains: "def render_peer_table"
    - path: "ui/copy.py"
      provides: "8 glossary definitions + peer heading/sub-line/legend/empty/error copy, scrubber-clean"
      contains: "GLOSSARY"
    - path: "app/static/drhplens.css"
      provides: ".drhp-peer-table, .drhp-provenance-flag/-legend, .drhp-glossary/-pop classes (no new hex, no red/green)"
      contains: "drhp-peer-table"
  key_links:
    - from: "pages/02_snapshot.py"
      to: "ui.snapshot_blocks.render_peer_table"
      via: "wired after Key Financials, cache-read try/except of load_peers"
      pattern: "render_peer_table"
    - from: "ui/snapshot_blocks.py::render_peer_table"
      to: "ui.format_inr.format_inr"
      via: "₹ cells via the shared utility; ratios one-decimal tabular-nums"
      pattern: "format_inr"
    - from: "ui/snapshot_blocks.py::render_peer_table"
      to: "ui.chip / ui.expander"
      via: "peer-SET citation chip (the only accent element, D4-04)"
      pattern: "render_citation_expanders"
---

<objective>
Render the peer-comparator surface (PEER-01, PEER-02) and the app-wide glossary tooltips (UI-04, D4-08) — the user-visible half of the peer feature. A `render_peer_table` (mirroring `render_redflag_table`/`render_financials_table`) shows rows=companies × cols=four multiples with a sticky company column, per-cell muted provenance superscripts + legend (R-3), the peer-SET anchored by the inherited citation chip to its DRHP section (D4-04, the only accent), honest `—`/`NM`/empty-state cells, and no red/green (D4-09). Pure-CSS glossary tooltips (R-1) cover the 8 core terms. All copy lands in `ui/copy.py` under the import-time scrubber.

Purpose: PEER-01 + PEER-02 render + UI-04 glossary — completes the peer vertical slice with 04-03.
Output: peer CSS + glossary CSS + copy + `render_peer_table` + glossary helper + page wiring + 375px human-verify.
</objective>

<phase_goal>
**As a** retail investor on an IPO page, **I want to** see the prospectus's disclosed peers with their market multiples, honestly sourced per cell and with jargon explained on tap **so that** I can judge the IPO in context without being fed a fabricated or colour-coded verdict.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-UI-SPEC.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-PATTERNS.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-CONTEXT.md
@ui/snapshot_blocks.py
@ui/copy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS peer/glossary/provenance classes + copy (peer + 8 glossary terms)</name>
  <read_first>
    - `app/static/drhplens.css` lines ~9-27 (`--drhp-*` tokens — reuse, no new hex), ~463-509 (`.drhp-fin-table*` to mirror), ~419-461 (`.drhp-split-bar*`), ~75 (`.drhp-cite::before` 44px tap enlarger), ~249-330 (the four breakpoints)
    - `ui/copy.py` lines ~408-479 (the import-time scrubber loop) and ~426-448 (`_SAMPLE_FORMAT_VALUES` — add new placeholders here)
    - `.planning/phases/.../04-UI-SPEC.md` §R-1 (glossary CSS), §R-3 (provenance), §Copywriting Contract (verbatim peer + glossary strings), §"New CSS classes"
  </read_first>
  <action>
    Add to `app/static/drhplens.css` (the single stylesheet — no inline `<style>` elsewhere), consuming inherited `--drhp-*` tokens only (NO new hex, NO red/green): `.drhp-peer-table` (mirrors `.drhp-fin-table`: `tabular-nums`, `position: sticky; left:0` company column, `surface-secondary` header + zebra) + `.drhp-peer-ipo-row` (neutral `surface-secondary` fill for the IPO's own row); `.drhp-provenance-flag` (superscript, `text-muted`, unfilled, ~0.7em — deliberately distinct from the accent filled citation chip) + `.drhp-provenance-legend` (Small muted); `.drhp-glossary` (dotted-underline inline trigger, `cursor:help`, 44px `::before` tap enlarger) + `.drhp-glossary-pop` (CSS popover shown on `:hover`/`:focus`/`:focus-within`, `surface-secondary` bg, `border-subtle`, hidden by default); plus peer/glossary rules under the existing four breakpoint media queries. Add to `ui/copy.py` as module-level `str` constants (auto-enrolled in the import-time scrubber): the 8 glossary definitions (RPT, QIB, NII, RII, GMP, OFS, DRHP, anchor investor) and the peer copy (heading `Comparison with listed peers`, sub-line, IPO-row tag `This IPO`, column headers, provenance legend, `—`/`NM` notes, empty-state `This DRHP disclosed no listed-peer comparison.`, error state) — all VERBATIM from the UI-SPEC Copywriting Contract (scrubber-clean; avoid the `sell` stem — use "shares offered by existing shareholders" for OFS). Add any new format placeholders to `_SAMPLE_FORMAT_VALUES`. Extend `tests/unit/test_copy_no_banned_tokens.py` to assert the 8 glossary terms are present and scrubber-clean. Encodes D4-08, D4-09.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_copy_no_banned_tokens.py -q && .venv/bin/python -c "import ui.copy"</automated>
  </verify>
  <acceptance_criteria>`import ui.copy` passes the import-time scrubber (all new copy banned-token-free incl. `sell` stem); 8 glossary definitions present; new CSS classes reference only `--drhp-*` tokens (no new hex, no red/green).</acceptance_criteria>
  <done>New CSS classes + all peer/glossary copy landed and scrubber-green.</done>
</task>

<task type="auto">
  <name>Task 2: render_peer_table + glossary term helper</name>
  <read_first>
    - `ui/snapshot_blocks.py` lines ~329-395 (`render_redflag_table`), ~194-241 (`render_financials_table` — `.drhp-fin-table-wrap` overflow + sticky-left), ~345-353 (the verbatim white-bar `st.container(border=True)` comment), ~180-191 (`_format_fin_value` cell edge cases), ~46-51 (`_render_not_disclosed`), ~54-69 (`_render_expanders` chip/expander reuse), ~387-393 (unique expander `key`)
    - `ui/chip.py` :75-79 + `ui/expander.py` :77-83 (escape-then-interpolate for untrusted strings)
    - `ui/format_inr.py` (Plan 04-02 — for any ₹ cell)
    - `.planning/phases/.../04-UI-SPEC.md` §R-2 (rows=companies, sticky col), §R-3, §Visuals Peer table (cell edge cases + empty-state)
  </read_first>
  <action>
    Add `render_peer_table(record: PeerRecord)` to `ui/snapshot_blocks.py`: wrap in `with st.container(border=True):` (NEVER a split div — Phase 3 white-bar lesson), emit the heading + the peer-SET citation via the UNCHANGED `render_citation_expanders`/chip path (the only accent element, D4-04) with a unique `key`, then a SINGLE self-contained `st.markdown` real `<table class="drhp-peer-table">` inside `.drhp-fin-table-wrap`: rows = companies (row 1 = the IPO with the `This IPO` tag + `.drhp-peer-ipo-row` fill), columns = `Company · P/E · P/B · EV/EBITDA · ROE`, `<th scope="col">`/`<th scope="row">` for a11y. Each cell renders the value (ratios one-decimal `tabular-nums`, no ₹; ROE as `%`; any ₹ magnitude via `format_inr`) followed by its muted `<sup class="drhp-provenance-flag" aria-label="source: …">` per R-3; missing → `—` + `aria-label="Not available from any source"`; undefined/negative P/E → `NM`; negative elsewhere → parens same colour (no red). Render the provenance legend below. HTML-escape every scraped peer name (`_html.escape(..., quote=True)`) before interpolation (XSS control). For the D4-06 empty-state (peer_set is a RefusalResponse), render heading + `_render_not_disclosed` note, no table. Add a small `glossary_term(term)` helper that wraps a term in the `.drhp-glossary` span + its `.drhp-glossary-pop` definition (pure CSS, no JS) for reuse across prose. Encodes PEER-01, PEER-02, D4-04/05/06/09.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit -q && .venv/bin/python -c "import ui.snapshot_blocks as b; assert hasattr(b,'render_peer_table')"</automated>
  </verify>
  <acceptance_criteria>`render_peer_table` exists and imports; uses `st.container(border=True)` (no split div); escapes scraped names; renders `—`/`NM`/empty-state honestly; ratios one-decimal tabular-nums; the peer-SET citation reuses the unchanged chip/expander; no red/green in the markup; unit suite green.</acceptance_criteria>
  <done>`render_peer_table` + `glossary_term` helper added; suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wire peer block into the snapshot page + 375px visual verify</name>
  <action>Wire `render_peer_table` into `pages/02_snapshot.py` after the Key Financials block (UI-SPEC IA block 7) with the redflag-block `load_peers` try/except posture (FileNotFoundError → empty-state; other Exception → amber `.drhp-refusal`, not red), then run the seeded Streamlit app and human-verify the 375px rendering per the how-to-verify steps below.</action>
  <what-built>Peer block wired into `pages/02_snapshot.py` after the Key Financials block (UI-SPEC IA block 7), reading the seeded `data/peers/swiggy_2024_11.json` via a `load_peers` try/except (FileNotFoundError → empty-state; other Exception → inherited amber `.drhp-refusal`, NOT red), the `drhp_id` already allow-list-validated at the top of `main()`. Glossary tooltips active on the 8 terms where they appear.</what-built>
  <how-to-verify>
    1. Run the Streamlit app on the seeded swiggy IPO; open the snapshot page.
    2. At 375px width, confirm the peer table: rows=companies with the IPO row first (`This IPO` tag), columns=the four multiples, company column sticky-left while metric columns scroll, per-cell provenance superscripts legible + legend present, `—`/`NM` cells honest, and NO red/green anywhere (D4-09).
    3. Confirm the peer-SET citation chip expands to the DRHP span/page (D4-04) and is the only accent element in the block.
    4. Tap a glossary term (e.g. RPT) — the CSS popover shows on tap/focus, 44px target, dismisses on tap-away.
    5. Confirm the empty-state path renders `This DRHP disclosed no listed-peer comparison.` for an IPO with no peer set (or a seeded refusal fixture).
  </how-to-verify>
  <acceptance_criteria>Peer table + glossary render correctly at 375px per 04-UI-SPEC; no red/green; provenance legible; citation chip works; empty-state honest; error path is amber not red. Wiring uses the redflag-block try/except posture.</acceptance_criteria>
  <resume-signal>Type "approved" or describe rendering issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped peer name → page HTML | untrusted strings from the peer cache interpolated via `unsafe_allow_html` |
| query param → cache read | `drhp_id` used to load the peer cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-XSS | Tampering/Info-disclosure | scraped peer names in `render_peer_table` | mitigate | `_html.escape(..., quote=True)` on every scraped string before interpolation (reuse ui/chip.py/ui/expander.py escape-then-interpolate). |
| T-04-05-PATH | Tampering | `load_peers(drhp_id)` on the page | mitigate | `drhp_id` allow-list-validated at top of `main()` + `_peers_path` gate (04-03). |
| T-04-05-COPY | Compliance | new glossary/peer copy | mitigate | Import-time scrubber assertion in `ui/copy.py` (TRUST-03) covers all new strings; `sell` stem avoided. |
| T-04-05-VERDICT | (honesty) | colour-coded multiple / badge | mitigate | No red/green, no badges; low and high multiples render identically (D4-09) — verified at the 375px checkpoint. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green.
- 375px human-verify checkpoint approved.
</verification>

<success_criteria>
The peer table renders the DRHP-cited peer set with per-cell provenance, honest `—`/`NM`/empty-state, ratios one-decimal, no red/green; glossary tooltips work keyboard+mobile; wired after Key Financials with the redflag try/except posture; all copy scrubber-clean.
</success_criteria>

<output>
Create `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-05-SUMMARY.md` when done.
</output>
