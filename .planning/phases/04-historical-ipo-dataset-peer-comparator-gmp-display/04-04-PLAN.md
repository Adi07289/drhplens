---
phase: 04-historical-ipo-dataset-peer-comparator-gmp-display
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - agent/gmp_schema.py
  - pipelines/gmp_sources.py
  - pipelines/gmp.py
  - data/gmp/swiggy_2024_11.json
  - data/gmp/hyundai_2024_10.json
  - tests/unit/test_gmp_schema.py
  - tests/unit/test_gmp_isolation.py
  - tests/unit/test_gmp_precompute.py
autonomous: true
requirements: [GMP-01, GMP-02]
user_setup: []

must_haves:
  truths:
    - "GMP is captured from 2-3 public aggregators as separate quotes so their spread/disagreement is preserved (D4-01)"
    - "Absent GMP (the common case for already-listed catalogue IPOs) and single-source GMP are first-class record states, never a fabricated number or zero (D4-06 variant)"
    - "The GMP module imports NOTHING from any forecast/model package — pinned by an inspect.getsource import-audit test (GMP-02, D4-03)"
    - "load_gmp gates drhp_id through the catalogue allow-list before forming any cache path"
  artifacts:
    - path: "agent/gmp_schema.py"
      provides: "GmpRecord / GmpQuote with spread + absent/single-source states, imports only pydantic + stdlib"
      exports: ["GmpRecord"]
      contains: "class GmpRecord"
    - path: "pipelines/gmp.py"
      provides: "precompute_gmp + load_gmp + allow-list gate + Typer CLI; imports no model code"
      exports: ["load_gmp", "precompute_gmp"]
      contains: "is_known_drhp_id"
    - path: "tests/unit/test_gmp_isolation.py"
      provides: "the GMP-02 isolation pin (inspect.getsource import-audit)"
      contains: "inspect.getsource"
    - path: "data/gmp/swiggy_2024_11.json"
      provides: "hand-seeded absent-GMP fixture (already-listed IPO)"
      contains: "swiggy_2024_11"
  key_links:
    - from: "pipelines/gmp.py::_gmp_path"
      to: "data.catalogue_loader.is_known_drhp_id"
      via: "allow-list gate before path formation"
      pattern: "is_known_drhp_id"
    - from: "tests/unit/test_gmp_isolation.py"
      to: "pipelines.gmp + agent.gmp_schema source"
      via: "assert no xgboost/mapie/forecast/sklearn/pipelines.features substring"
      pattern: "forbidden"
---

<objective>
Build the read-only GMP DATA layer: a `GmpRecord` capturing 2-3 aggregator quotes so their disagreement (the honesty signal, D4-01) is preserved, with absent-GMP and single-source treated as first-class states (D4-06 variant — absent is the COMMON case for the catalogue's already-listed IPOs). The GMP module is computationally isolated from any model/feature pipeline (GMP-02, D4-03), pinned from day one by an `inspect.getsource` import-audit test mirroring Phase 3's `test_no_llm_or_qdrant_import`. All scraping is precompute-time only; render (04-06) reads only the cache.

Purpose: GMP-01 (read-only multi-source GMP with caveats) + GMP-02 (isolation) data foundation.
Output: schema + isolation test + sources + pipeline + seed fixtures + unit tests (mocked, no live network).
</objective>

<phase_goal>
**As a** retail investor, **I want to** have grey-market premium captured only as read-only, multi-source, caveated data that can never touch a forecast **so that** I see it as the unreliable, isolated signal it is — not as a demand indicator.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-CONTEXT.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-RESEARCH.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-PATTERNS.md
@agent/redflag_schema.py
@pipelines/redflag.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GmpRecord schema + GMP-02 isolation import-audit test</name>
  <read_first>
    - `agent/redflag_schema.py` (the `to_dict`/`from_dict`/`to_json` codec shape to mirror; do NOT pull model code)
    - `tests/unit/test_methodology_pane.py` lines ~211-221 (`test_no_llm_or_qdrant_import` — the `inspect.getsource` substring import-audit to copy) and `tests/unit/test_cite_check.py` :141-163 (AST variant)
    - `.planning/phases/.../04-PATTERNS.md` §"agent/gmp_schema.py" + §"GMP module-isolation import-audit"
    - `.planning/phases/.../04-UI-SPEC.md` §Visuals GMP (states: default spread, absent, single-source, error)
  </read_first>
  <behavior>
    - `GmpRecord(drhp_id, computed_at, quotes: list[GmpQuote], as_of)` round-trips `to_dict`/`from_dict`
    - `GmpQuote(source: str, value: float, as_of: str)`; spread (`low`/`high`/`n`) is derivable from quotes (compute at render or store precomputed — pick one and pin it)
    - `quotes == []` is the honest absent-GMP state; `len(quotes) == 1` is the single-source state — both valid, neither a fabricated number
    - `agent/gmp_schema.py` imports ONLY from `pydantic` + stdlib — never any forecast/model module
    - the isolation test fails if `pipelines.gmp` or `agent.gmp_schema` source contains any of `xgboost`, `mapie`, `forecast`, `sklearn`, `pipelines.features`
  </behavior>
  <action>
    Create `agent/gmp_schema.py`: `GmpRecord` + `GmpQuote` as Pydantic models importing ONLY `pydantic` + stdlib (D4-03 — no `agent.schemas` if that transitively pulls model code; keep it minimal). Provide `to_dict`/`from_dict`/`to_json` (`json.dumps(indent=2, ensure_ascii=False)`) and a `spread()` helper (min/max/n over quotes, returning None-spread for absent/single-source). Empty `quotes` = absent state; single quote = single-source state. Create `tests/unit/test_gmp_isolation.py` copying the Phase 3 `inspect.getsource` import-audit: iterate `(pipelines.gmp, agent.gmp_schema)` and assert none of the forbidden model tokens appear in their source (this is the GMP-02 pin, foreshadowing the Phase 5 reverse leakage audit). Write `tests/unit/test_gmp_schema.py` covering round-trip, spread math, and the absent/single-source states.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_gmp_schema.py tests/unit/test_gmp_isolation.py -x -q</automated>
  </verify>
  <acceptance_criteria>Schema round-trips; spread derives correctly; absent + single-source states valid; `agent/gmp_schema.py` imports only pydantic+stdlib; isolation audit green over `agent.gmp_schema` (and will extend to `pipelines.gmp` in Task 2).</acceptance_criteria>
  <done>`agent/gmp_schema.py` + `test_gmp_schema.py` + `test_gmp_isolation.py` exist and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gmp_sources.py aggregator scrapers + gmp.py precompute/load + seed fixtures + tests</name>
  <read_first>
    - `pipelines/redflag.py` (`load_redflag`/`_redflag_path` allow-list gate :70-104; the `write: bool = True` kwarg; Typer `precompute-one`/`precompute-all` per-IPO isolation :314-361)
    - `pipelines/ingest.py` (`requests-cache` + `tenacity` + per-item failure-isolation fetch loop)
    - `.planning/phases/.../04-RESEARCH.md` §GMP Scraping, §Pitfall 5 (GMP framed as signal), §Open Question 1 (which aggregators; absent-GMP common for listed IPOs)
    - `data/redflag/swiggy_2024_11.json` (seed-cache shape)
  </read_first>
  <behavior>
    - given mocked aggregator fetchers, `precompute_gmp` assembles one `GmpQuote` per reachable aggregator into a `GmpRecord`
    - per-aggregator failure isolation: one aggregator failing logs + continues (never aborts, never fabricates a value for it)
    - already-listed IPO (no live GMP) → `quotes == []` absent record, not a zero
    - only hard-coded aggregator hostnames fetched (SSRF control)
    - `load_gmp(unknown_id)` raises ValueError (allow-list) before path formation; `pipelines.gmp` imports no model code (isolation audit extended to it)
  </behavior>
  <action>
    Create `pipelines/gmp_sources.py` with 2-3 hard-coded-host aggregator scrapers (`requests-cache` + `beautifulsoup4`+`lxml`, `tenacity` backoff) each returning a `GmpQuote` or None on absence/failure, plus a spread helper. Create `pipelines/gmp.py` mirroring `redflag.py`'s path-gate + CLI (NOT the agent graph): `GMP_DIR`, `load_gmp`/`_gmp_path` allow-list gate, `precompute_gmp(drhp_id, *, write=True)` that scrapes each aggregator with per-source isolation, builds a `GmpRecord` (absent when no aggregator reports), and writes `data/gmp/<id>.json`. Import NOTHING from any forecast/model/feature module (D4-03). Add Typer `precompute-one`/`precompute-all`. Hand-seed two fixtures: `data/gmp/swiggy_2024_11.json` = the absent-GMP state (already listed — the common case), and `data/gmp/hyundai_2024_10.json` = a synthetic 3-source spread (CLEARLY flagged hand-seeded, for the 04-06 spread-visual demo) so the renderer exercises both states offline. Extend `tests/unit/test_gmp_isolation.py` to also audit `pipelines.gmp`. Write `tests/unit/test_gmp_precompute.py` (mock aggregators: multi-source spread, single-source, absent, per-source failure; `::test_path_gate` unknown-id ValueError; `write=False`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_gmp_precompute.py tests/unit/test_gmp_isolation.py tests/unit/test_gmp_schema.py -q && .venv/bin/python -c "from pipelines.gmp import load_gmp; print(len(load_gmp('swiggy_2024_11').quotes), len(load_gmp('hyundai_2024_10').quotes))"</automated>
  </verify>
  <acceptance_criteria>
    - Multi-source, single-source, absent, and per-source-failure paths behave per behavior spec; no fabricated values.
    - `load_gmp('swiggy_2024_11')` = 0 quotes (absent); `load_gmp('hyundai_2024_10')` = 3 quotes (seeded spread).
    - Allow-list gate raises on unknown id; isolation audit green over BOTH `pipelines.gmp` and `agent.gmp_schema`.
    - No live network under `tests/unit`.
  </acceptance_criteria>
  <done>`pipelines/gmp_sources.py` + `pipelines/gmp.py` + two seed fixtures exist; all GMP-data tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query param → cache path | untrusted `drhp_id` reaches `_gmp_path` |
| aggregator site → precompute | unofficial GMP-aggregator HTML is untrusted input |
| GMP data → model pipeline | the boundary GMP-02 forbids crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-ISO | Info-disclosure (data-integrity) | GMP → forecast/model leakage | mitigate | Module boundary: `pipelines.gmp` + `agent.gmp_schema` import no model code; pinned by `test_gmp_isolation.py` inspect.getsource audit (D4-03/GMP-02). Never auto-approvable to weaken. |
| T-04-04-PATH | Tampering | `_gmp_path(drhp_id)` | mitigate | `is_known_drhp_id()` allow-list before path formation (verbatim from redflag.py). |
| T-04-04-SSRF | Tampering | `gmp_sources.py` fetchers | mitigate | Only hard-coded aggregator hostnames; no URL from user/DRHP input. |
| T-04-04-FAB | Info-disclosure (integrity) | fabricated GMP / zero | mitigate | Absent = `quotes==[]`; single-source flagged; never a zero or invented number (Pitfall 5). |
| T-04-04-VALID | Tampering | cache record on load | mitigate | Pydantic validates on `from_dict`; repo-committed + PR-reviewed caches. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green.
- Isolation audit covers both GMP modules.
- Both seed fixtures load and validate.
</verification>

<success_criteria>
Multi-source GmpRecord preserving aggregator disagreement; absent + single-source first-class; GMP-02 isolation enforced by an import-audit test over both GMP modules; allow-list-gated precompute/load; two seed fixtures (absent + synthetic spread) unblocking the renderer. All tests green offline.
</success_criteria>

<output>
Create `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-04-SUMMARY.md` when done, noting the absent-GMP-is-common finding and the deferred live-scrape (open-IPO) runbook step.
</output>
