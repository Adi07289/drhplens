---
phase: 04-historical-ipo-dataset-peer-comparator-gmp-display
plan: 07
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pipelines/historical/__init__.py
  - pipelines/historical/sources.py
  - pipelines/historical/build.py
  - pipelines/historical/validate.py
  - data/historical/ipo_panel.parquet
  - data/historical/ipo_panel.csv
  - tests/unit/test_historical_panel.py
autonomous: false
requirements: [FCAST-03]
user_setup: []

must_haves:
  truths:
    - "The historical IPO universe is survivorship-corrected: sourced issuer-side (SEBI/chittorgarh), with an explicit status column (withdrawn/listed_alive/delisted/merged/name_changed) so withdrawn/delisted IPOs are counted, not dropped (P3)"
    - "A company with no listing-day price is replace-with-NaN, never dropped — absence is counted"
    - "The dataset's median listing-day return is sanity-checked against the ~7% MAAR baseline and a divergence flag surfaces on /methodology as plain text if it materially diverges"
  artifacts:
    - path: "pipelines/historical/validate.py"
      provides: "median MAAR sanity-check vs ~7.19% baseline + divergence flag"
      contains: "median"
    - path: "pipelines/historical/build.py"
      provides: "universe assembly + status column + listing-day return + replace-with-NaN survivorship"
      contains: "status"
    - path: "data/historical/ipo_panel.parquet"
      provides: "committed survivorship-corrected panel (Phase 5 FCAST-03 foundation)"
    - path: "tests/unit/test_historical_panel.py"
      provides: "status taxonomy + NaN-not-drop + median-flag pins"
      contains: "def test_"
  key_links:
    - from: "pipelines/historical/build.py"
      to: "pipelines/historical/validate.py"
      via: "post-build ~7% median sanity check + divergence flag"
      pattern: "validate"
    - from: "pipelines/historical/build.py"
      to: "data/historical/ipo_panel.parquet"
      via: "committed parquet + CSV mirror artifact"
      pattern: "parquet"
---

<objective>
Build the survivorship-corrected historical Indian mainboard IPO dataset (2014-present, ~800-1000 rows) — the backend foundation for Phase 5's forecaster (FCAST-03), not user-visible this phase. Source issuer-side (SEBI/chittorgarh index, which includes withdrawn/pulled IPOs) rather than survivor-only exchange listing feeds (P3); commit a panel with an explicit `status` column, a listing-day return target, and replace-with-NaN handling so absent listings are counted; and validate the median listing-day return against the ~7% MAAR baseline (Shah & Mehta 2015 ≈ 7.19%) with a `/methodology` divergence flag if it materially diverges.

Per orchestration scope: build the universe + status column + listing-day return target + ~7% sanity-check ONLY. Feature engineering is Phase 5 — keep the output schema minimal and Phase-5-extensible.

Purpose: The DS-critical P3 survivorship-bias mitigation; the FCAST-03 data source.
Output: `pipelines/historical/` builder + validator + committed parquet/CSV + unit tests. The live build (network) is a deferred human step.
</objective>

<phase_goal>
**As a** DRHPLens data scientist, **I want to** have a committed survivorship-corrected historical IPO panel with a status column and a validated ~7% median listing return **so that** the Phase 5 forecaster is backtested on an honest universe rather than a survivor-inflated one.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-CONTEXT.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-RESEARCH.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Panel schema + validate.py (~7% median flag) + test_historical_panel.py</name>
  <read_first>
    - `.planning/phases/.../04-RESEARCH.md` §Pitfall 3 (survivorship: issuer-side sourcing, status taxonomy, replace-with-NaN, ~7.19% MAAR baseline + divergence-flag rule) and §Open Question 3 (Phase-4-vs-Phase-5 build depth)
    - `scripts/eval_extraction.py` (`_write_report` at :212-300 — the artifact-write + validation-report shell to mirror)
    - `.planning/phases/.../04-PATTERNS.md` §"pipelines/historical/*" (partial analogs)
  </read_first>
  <behavior>
    - the panel row schema includes at minimum: issuer, drhp/issue date, listing date, issue price, listing-day close, listing-day return, and `status ∈ {withdrawn, listed_alive, delisted, merged, name_changed}`
    - a row whose listing-day price is unavailable has `listing_day_return = NaN` and is RETAINED (replace-with-NaN, never dropped)
    - `validate.py` computes the median listing-day return over rows with a non-NaN return and returns a divergence flag when the median is materially outside the ~7% MAAR band (e.g. > ~20% signals survivor inflation)
    - the divergence flag is a plain-text methodology string, not a UI widget
  </behavior>
  <action>
    Create `pipelines/historical/__init__.py` and a minimal pandas-based row schema (a documented column contract, dtypes chosen so NaN survives — float for returns, categorical/string for status). Create `pipelines/historical/validate.py` with `sanity_check_median(df) -> (median, flag_text | None)` implementing the ~7.19% MAAR comparison (RESEARCH baseline; the divergence flag matters more than the exact target — A4) and returning a `/methodology`-ready plain-text divergence note when it fires. Write `tests/unit/test_historical_panel.py` (mirror `test_redflag_schema.py` assert style) covering: status-column taxonomy present + validated, NaN-not-drop survivorship (a missing listing price stays as a NaN row), and the median divergence flag firing/not-firing on synthetic frames. No live network — tests use small hand-built DataFrames.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_historical_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>Status taxonomy enforced; NaN-not-drop verified on a synthetic frame; median divergence flag fires above the survivor-inflation band and stays quiet in-band; all offline.</acceptance_criteria>
  <done>`pipelines/historical/validate.py` + schema + `test_historical_panel.py` exist and green.</done>
</task>

<task type="auto">
  <name>Task 2: sources.py + build.py (issuer-side universe, status, NaN survivorship, parquet+CSV)</name>
  <read_first>
    - `pipelines/ingest.py` (the `requests-cache` + `tenacity` + `rich` + per-item failure-isolation fetch-loop shell)
    - `CLAUDE.md` §India-Specific Data-Source Notes (chittorgarh index = best historical aggregator incl. withdrawn IPOs; NSE bhavcopy via jugaad-data; SEBI issuer-side)
    - `04-01-SUMMARY.md` (jugaad-data endpoint verdict — use jugaad for NSE bhavcopy/listing candles if healthy, else yfinance price fallback)
    - `pipelines/historical/validate.py` (Task 1 — call it post-build)
  </read_first>
  <action>
    Create `pipelines/historical/sources.py` with hard-coded-host fetchers for the chittorgarh historical-IPO index (incl. withdrawn/pulled entries — P3), SEBI issuer-side offer-document listing, and NSE bhavcopy / listing-day candles via jugaad-data (falling back to yfinance per the 04-01 verdict), all using `requests-cache` + `tenacity` + per-item failure isolation, escaping/validating parsed HTML. Create `pipelines/historical/build.py` that assembles the 2014-present mainboard universe, derives the `status` column, computes the listing-day return, applies REPLACE-WITH-NaN for missing listing prices (never drop), calls `validate.sanity_check_median`, and writes BOTH `data/historical/ipo_panel.parquet` (Phase-5 consumable) and a `data/historical/ipo_panel.csv` mirror (git-diff reviewability) plus a committed `.schema.md`/`README` describing columns + the median sanity result. Keep the schema minimal (no feature engineering — that is Phase 5). Add a Typer CLI (`build`) with `rich` progress + per-source isolation. Do NOT run the live build in this task (network) — leave it to the checkpoint. Provide a small committed SAMPLE parquet+CSV (a few hand-built rows exercising each status value + a NaN row) so the artifact path and validator are exercised in CI without network.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_historical_panel.py -q && .venv/bin/python -c "import pandas as pd; df=pd.read_parquet('data/historical/ipo_panel.parquet'); print(sorted(df['status'].unique()), df['listing_day_return'].isna().any())"</automated>
  </verify>
  <acceptance_criteria>
    - `build.py` + `sources.py` import cleanly with no network at import.
    - The committed sample `ipo_panel.parquet` + `.csv` exist, carry the `status` column with the full taxonomy represented, and include at least one NaN-return row (survivorship retained).
    - `validate.sanity_check_median` is called in `build`; the sample's median result is recorded in the schema README.
    - Only hard-coded source hostnames referenced (SSRF control).
  </acceptance_criteria>
  <done>`sources.py` + `build.py` + committed sample parquet/CSV + schema README exist; offline tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live full build run (deferred human/network step)</name>
  <action>Deferred human/network step: run the `pipelines.historical.build` CLI in an environment with internet egress to produce and commit the full ~800-1000-row `ipo_panel.parquet` + CSV, then record row count, status distribution, and the ~7% median MAAR sanity result (and any divergence flag) in the SUMMARY.</action>
  <what-built>The `pipelines/historical` builder from Tasks 1-2, which must fetch ~800-1000 IPOs from chittorgarh/SEBI/NSE — live egress, deliberately not run in the executor sandbox. The committed artifact so far is the small offline SAMPLE.</what-built>
  <how-to-verify>
    1. In an environment with internet egress, run the builder CLI (`.venv/bin/python -m pipelines.historical.build build`) to produce the full `data/historical/ipo_panel.parquet` + CSV.
    2. Confirm row count is in the expected ~800-1000 range and that withdrawn/delisted statuses are present (zero withdrawn in a 2014-2026 universe is impossible — a survivorship red flag).
    3. Confirm `validate.sanity_check_median` reports a median near the ~7% MAAR band; if it diverges materially, confirm the plain-text divergence flag is captured for `/methodology`.
    4. Commit the full parquet+CSV artifact.
  </how-to-verify>
  <acceptance_criteria>Full survivorship-corrected panel built + committed; row count + status distribution + median sanity result recorded in the SUMMARY; divergence flag captured if it fired. This is the FCAST-03 foundation Phase 5 consumes.</acceptance_criteria>
  <resume-signal>Paste the row count + status distribution + median result, or type "deferred" to record it as a pending runbook step (the offline sample keeps the schema/validator green meanwhile).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external site → build | chittorgarh/SEBI/NSE HTML + bhavcopy is untrusted input |
| pip → jugaad-data | NSE library (gated in 04-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-SSRF | Tampering | `historical/sources.py` fetchers | mitigate | Only hard-coded source hostnames; no URL derived from external/user input. |
| T-04-07-VALID | Tampering | parsed HTML → panel rows | mitigate | Validate/coerce every parsed field to the typed column contract; malformed rows logged + isolated, never silently dropped as survivors. |
| T-04-07-SURV | Info-disclosure (integrity) | survivorship bias | mitigate | Issuer-side sourcing (incl. withdrawn) + status column + replace-with-NaN + ~7% median sanity flag (P3) — the whole plan is this control. |
| T-04-SC | Tampering | jugaad-data | accept | Gated + pinned in 04-01; nightly integration test catches endpoint drift. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green.
- Committed sample parquet/CSV loads with the full status taxonomy + a NaN row.
- Live full build produced (or documented as deferred) with median sanity result.
</verification>

<success_criteria>
A survivorship-corrected historical IPO panel (status column, listing-day return, replace-with-NaN) committed as parquet+CSV with a ~7% median sanity-check + `/methodology` divergence flag; builder + validator + tests green offline; the full live build produced or recorded as a deferred runbook step.
</success_criteria>

<output>
Create `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-07-SUMMARY.md` when done, recording row count, status distribution, and the median MAAR sanity result (or the deferred-build note).
</output>
