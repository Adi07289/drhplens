---
phase: 04-historical-ipo-dataset-peer-comparator-gmp-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/integration/test_jugaad_data_nse.py
  - tests/integration/test_yfinance_nse_smoke.py
  - .github/workflows/nightly-nse.yml
autonomous: false
requirements: [PEER-02]
user_setup: []

must_haves:
  truths:
    - "yfinance is pinned to 1.5.1 and its .NS/.BO Ticker.info returns the peer-multiple keys the peer pipeline reads"
    - "jugaad-data NSE bhavcopy + listing-day-candle endpoints respond with the expected shapes (or are documented as broken so the historical builder falls back to yfinance)"
    - "a nightly integration test exists that catches NSE endpoint drift before it silently breaks a precompute run"
  artifacts:
    - path: "tests/integration/test_jugaad_data_nse.py"
      provides: "nightly NSE endpoint smoke test (marker: integration)"
      contains: "pytest.mark.integration"
    - path: "tests/integration/test_yfinance_nse_smoke.py"
      provides: "yfinance 1.5.1 .NS/.BO Ticker.info key smoke test"
      contains: "trailingPE"
    - path: ".github/workflows/nightly-nse.yml"
      provides: "scheduled GitHub Actions workflow running the integration tests"
      contains: "schedule"
    - path: "pyproject.toml"
      provides: "yfinance==1.5.1 pin + requests-cache + jugaad-data deps"
      contains: "jugaad-data"
  key_links:
    - from: "pipelines/peer_sources.py (Plan 04-03)"
      to: "yfinance Ticker.info keys validated here"
      via: "smoke-tested key names (trailingPE, priceToBook, enterpriseToEbitda, returnOnEquity)"
      pattern: "trailingPE"
    - from: "pipelines/historical/sources.py (Plan 04-07)"
      to: "jugaad-data NSE endpoints validated here"
      via: "bhavcopy + listing-day candle smoke"
      pattern: "jugaad"
---

<objective>
Run the ROADMAP-mandated Wave-0 data-source validation spike before any Phase 4 pipeline commits to an external NSE/market-data source. Pin `yfinance` to 1.5.1 (a major bump from STACK.md's 0.2.50) and smoke-test its `.NS`/`.BO` `Ticker.info` keys; install `requests-cache` and `jugaad-data` (after a package-legitimacy gate) and validate the exact NSE endpoints the historical builder needs; and stand up a nightly `integration`-marked test + GitHub Actions scheduled workflow so NSE site drift is caught early rather than mid-precompute.

Purpose: De-risk the phase's genuine fragility (external sources), per RESEARCH §Pitfall 4 and the ROADMAP research flag. Downstream peer (04-03) and historical (04-07) plans depend on these pins/installs and on the validated key/endpoint shapes.
Output: pinned deps, two integration smoke tests, a scheduled CI workflow, and a documented verdict on jugaad-data endpoint health.
</objective>

<phase_goal>
**As a** DRHPLens maintainer, **I want to** validate and pin the Indian market-data sources (yfinance 1.5.1, jugaad-data 0.33.1) with a nightly drift-detection test **so that** the peer table and historical dataset build on sources that are confirmed working, not assumed.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-RESEARCH.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for requests-cache + jugaad-data</name>
  <action>Human-verify gate BEFORE any install: confirm requests-cache and jugaad-data are legitimate on PyPI and match the RESEARCH-pinned names/versions, then approve or reject. No files change in this task — it authorizes Task 2's install.</action>
  <what-built>Nothing yet — this gate precedes the install. RESEARCH §Package Legitimacy Audit lists `requests-cache` (1.3.3) and `jugaad-data` (0.33.1) as CLAUDE.md-declared but slopcheck was unavailable at research time; both are new installs not yet in `.venv`.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/requests-cache/ — confirm it is the maintained `requests-cache/requests-cache` project (7+ years, recent releases), not a typosquat.
    2. Open https://pypi.org/project/jugaad-data/ — confirm it is `jugaad-py/jugaad-data` (the nsepy successor, ~500 GitHub stars), version 0.33.1.
    3. Confirm both are the exact names/versions in RESEARCH §Standard Stack (`requests-cache==1.3.3`, `jugaad-data==0.33.1`).
  </how-to-verify>
  <acceptance_criteria>Human confirms both packages are legitimate on PyPI and match the RESEARCH-pinned names/versions. This checkpoint is never auto-approvable (workflow.auto_advance ignored — package legitimacy gate).</acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or name the package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin yfinance, add deps, write the two smoke tests + nightly workflow</name>
  <read_first>
    - `pyproject.toml` (dependency declarations; find the current `yfinance` pin ~0.2.50)
    - `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-RESEARCH.md` §Standard Stack, §Code Examples (yfinance `.info` keys), §Pitfall 4 (jugaad endpoint shapes)
    - `tests/integration/test_qdrant_ingest.py` (existing `integration`-marker test shape to mirror)
    - `pyproject.toml` `[tool.pytest.ini_options]` markers (`slow`, `eval`, `integration` — confirm `integration` is registered)
  </read_first>
  <action>
    Pin `yfinance==1.5.1` in `pyproject.toml` (replacing the ~0.2.50 constraint) and add `requests-cache==1.3.3` and `jugaad-data==0.33.1` to the project dependencies, then install into `.venv` (`.venv/bin/pip install ...`). Create `tests/integration/test_yfinance_nse_smoke.py`: `@pytest.mark.integration` tests that `yfinance.Ticker("RELIANCE.NS").info` is a non-empty dict and that the four keys the peer pipeline will read — `trailingPE`, `priceToBook`, `enterpriseToEbitda`, `returnOnEquity` — are present-or-explicitly-absent (assert each is either a number or missing, never a silent wrong type), documenting the observed key set in a module docstring so Plan 04-03 reads the confirmed names. Create `tests/integration/test_jugaad_data_nse.py`: `@pytest.mark.integration` smoke of the exact endpoints 04-07 needs — an NSE bhavcopy fetch for a known past trading date and a listing-day candle for a known symbol — asserting non-empty DataFrame shapes; on endpoint failure the test FAILS with a message that tells the maintainer jugaad is broken and the historical builder must fall back to yfinance (P15/Pitfall-4 posture). Add `.github/workflows/nightly-nse.yml`: a `schedule:` (cron, nightly) + `workflow_dispatch` GitHub Actions job that installs deps and runs `pytest -m integration tests/integration/test_jugaad_data_nse.py tests/integration/test_yfinance_nse_smoke.py`. Mark both tests so the default `pytest tests/unit` run never triggers live network (they run only under `-m integration`). Per D4-05 source-priority ladder, this validates the yfinance rung (PEER-02). Do NOT wire any of this into request-time code.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit -q && .venv/bin/python -c "import yfinance, requests_cache, jugaad_data; print(yfinance.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `yfinance.__version__` prints `1.5.1`; `requests_cache` and `jugaad_data` import cleanly.
    - `tests/integration/test_yfinance_nse_smoke.py` and `tests/integration/test_jugaad_data_nse.py` collect under `-m integration` and are skipped/not-collected under the default `tests/unit` run.
    - `.github/workflows/nightly-nse.yml` contains a `schedule:` cron and runs the two integration tests.
    - The existing unit suite still passes (no regression from the yfinance bump at import time).
  </acceptance_criteria>
  <done>Deps pinned/installed; two integration smoke tests + nightly workflow committed; unit suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-network spike run (deferred human step)</name>
  <action>Deferred human/network step: run the two `integration`-marked smoke tests against live NSE + Yahoo, record the jugaad-data working-vs-fallback verdict and the observed yfinance `.info` key coverage in the SUMMARY. No code changes — this validates the Task 2 artifacts against live sources.</action>
  <what-built>The two `integration`-marked smoke tests from Task 2, which require live egress to Yahoo Finance and NSE — deliberately not run in CI-offline / the executor sandbox.</what-built>
  <how-to-verify>
    1. In an environment with internet egress, run `.venv/bin/python -m pytest -m integration tests/integration/test_yfinance_nse_smoke.py tests/integration/test_jugaad_data_nse.py -q`.
    2. Confirm the yfinance smoke passes and note which of the four `.info` keys are actually populated for `RELIANCE.NS` (report gaps — P15).
    3. Confirm the jugaad bhavcopy + listing-candle smoke passes; if it fails, record the verdict "jugaad-data endpoints broken as of {date} — historical builder (04-07) uses yfinance price fallback."
  </how-to-verify>
  <acceptance_criteria>Live smoke run completed once; the jugaad-data verdict (working vs fall-back-to-yfinance) and the observed yfinance key coverage are recorded in the plan SUMMARY. This gates 04-03/04-07 committing to a primary NSE source.</acceptance_criteria>
  <resume-signal>Paste the pytest result + jugaad verdict, or type "deferred" to record it as a pending runbook step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install ← PyPI | new packages (`requests-cache`, `jugaad-data`) enter the supply chain |
| test ← NSE / Yahoo | untrusted external HTTP responses during the smoke run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | `requests-cache` + `jugaad-data` install | mitigate | Task 1 blocking-human package-legitimacy gate against pypi.org before install; versions pinned exactly (RESEARCH audit). |
| T-04-01-DoS | Denial of Service | NSE bot-detection blocking the smoke test | accept | Integration tests are nightly + `workflow_dispatch`, low frequency; failure is a documented drift signal, not a CI blocker for unit runs. |
| T-04-01-SSRF | Tampering | jugaad/yfinance fetch targets | mitigate | Only hard-coded exchange hostnames/symbols; no URL derived from user input. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green (no regression from the yfinance major bump).
- Integration tests present and marked; excluded from the default unit run.
- Nightly workflow file valid YAML with a `schedule:` trigger.
</verification>

<success_criteria>
yfinance pinned to 1.5.1 and importable; requests-cache + jugaad-data installed after a legitimacy gate; two integration smoke tests + a nightly CI workflow committed; the live spike run either passed or produced a documented jugaad fallback verdict.
</success_criteria>

<output>
Create `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-01-SUMMARY.md` when done, recording the jugaad-data verdict and observed yfinance `.info` key coverage for downstream plans.
</output>
