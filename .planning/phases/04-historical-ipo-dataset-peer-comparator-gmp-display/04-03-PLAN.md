---
phase: 04-historical-ipo-dataset-peer-comparator-gmp-display
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - agent/peer_schema.py
  - pipelines/peer_queries.py
  - pipelines/peer_sources.py
  - pipelines/peers.py
  - data/peers/swiggy_2024_11.json
  - tests/unit/test_peer_schema.py
  - tests/unit/test_peer_sources.py
  - tests/unit/test_peers_precompute.py
autonomous: true
requirements: [PEER-01, PEER-02]
user_setup: []

must_haves:
  truths:
    - "The peer SET is extracted from the DRHP's own 'Comparison with Listed Peers' section via the existing agent graph and carries its DRHP page citation (PEER-01)"
    - "When the DRHP names no peers, the peer-SET value is an honest RefusalResponse, never a fabricated set (D4-06)"
    - "Each (company, metric) multiple carries its per-cell source AND its as-of dimension (current market + DRHP-date where available); a value missing from every source is None, never invented (PEER-02, D4-05)"
    - "load_peers gates drhp_id through the catalogue allow-list before forming any cache path (path-traversal control)"
  artifacts:
    - path: "agent/peer_schema.py"
      provides: "PeerRecord / PeerCompany / PeerMetric / PeerCell with per-cell source+as_of provenance and the {refusal} union codec"
      exports: ["PeerRecord"]
      contains: "class PeerRecord"
    - path: "pipelines/peers.py"
      provides: "precompute_peers + load_peers + allow-list gate + Typer CLI"
      exports: ["load_peers", "precompute_peers"]
      contains: "is_known_drhp_id"
    - path: "pipelines/peer_sources.py"
      provides: "screener.in → yfinance → NSE source-priority fetchers with per-cell provenance"
      contains: "def "
    - path: "data/peers/swiggy_2024_11.json"
      provides: "hand-seeded peer cache so the renderer (04-05) works offline (CODE-NOW-DEFER)"
      contains: "swiggy_2024_11"
  key_links:
    - from: "pipelines/peers.py::precompute_peers"
      to: "agent.graph.GRAPH.invoke"
      via: "deferred import + canned PEER_SET_QUERY (mirror redflag.py)"
      pattern: "GRAPH"
    - from: "pipelines/peers.py::_peers_path"
      to: "data.catalogue_loader.is_known_drhp_id"
      via: "allow-list gate before path formation"
      pattern: "is_known_drhp_id"
    - from: "pipelines/peers.py"
      to: "pipelines/peer_sources.py"
      via: "per-cell source-priority fetch at precompute time only"
      pattern: "peer_sources"
---

<objective>
Build the peer-comparator DATA layer: a `PeerRecord` schema mirroring `RedFlagRecord`'s union-discriminator codec, a canned DRHP peer-SET query run through the existing agent graph (PEER-01, cited), and a per-cell multiples fetcher (`peer_sources.py`) that walks the D4-05 source-priority ladder recording WHICH source and WHICH as-of dimension supplied each value (PEER-02). All external fetches happen at precompute time only; the record is a diff-reviewable JSON cache gated by the allow-list. A hand-seeded `data/peers/swiggy_2024_11.json` unblocks the renderer offline (CODE-NOW-DEFER — live 8×-per-IPO extraction is deferred to the ingest runbook).

Per the locked orchestration decision, each cell carries BOTH a current-market value (primary) AND a DRHP-date value where available, each labelled with its source and as-of dimension; honest `—` when no source has it.

Purpose: PEER-01 + PEER-02 data foundation on the proven precompute→cache→load rails (04-PATTERNS §pipelines/peers.py).
Output: schema + queries + sources + pipeline + seed fixture + unit tests (monkeypatched, no live network).
</objective>

<phase_goal>
**As a** retail investor on an IPO page, **I want to** have the prospectus's own disclosed listed peers and their market multiples gathered with honest per-cell sourcing **so that** I can later see a truthful peer comparison instead of a self-selected or fabricated one.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-CONTEXT.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-RESEARCH.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-PATTERNS.md
@CLAUDE.md
@agent/redflag_schema.py
@pipelines/redflag.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PeerRecord schema (per-cell source + as_of provenance) + test</name>
  <read_first>
    - `agent/redflag_schema.py` (the full union-discriminator codec: `_dump_field_value`/`_load_field_value` at :72-91, `to_dict`/`from_dict`/`to_json` at :165-213, the `frozenset` locked-key + rejecting `field_validator` at :59-69)
    - `agent/schemas.py` lines ~79-83 (`GroundedAnswer`, `RefusalResponse`, the locked `claim_id` regex — import, never redefine)
    - `.planning/phases/.../04-PATTERNS.md` §"agent/peer_schema.py" (per-cell PeerCell delta)
    - `.planning/phases/.../04-UI-SPEC.md` §Visuals Peer table (cell edge cases: `—`, `NM`, negatives)
  </read_first>
  <behavior>
    - `PeerRecord(drhp_id, computed_at, peer_set, companies, as_of)` round-trips `to_dict`/`from_dict` byte-stably (indent=2, ensure_ascii=False)
    - `peer_set` is `GroundedAnswer | RefusalResponse`; a `{"refusal": ...}` dict reconstructs a RefusalResponse (D4-06 empty-state), any other dict a GroundedAnswer (PEER-01 cited)
    - each `PeerCompany` holds `PeerMetric`s for P/E, P/B, EV/EBITDA, ROE; each `PeerMetric` carries a `current` cell (primary) and an optional `drhp_date` cell (locked orchestration decision — BOTH dimensions where available)
    - `PeerCell(value: float | None = None, source: Literal["s","y","n","d"] | None = None, as_of: Literal["current","drhp_date"] | None = None)`; a fully-missing cell is `PeerCell()` (value None) → renders `—`; a negative/undefined P/E carries a sentinel distinguishable for the `NM` render
    - an unknown metric key or unknown source flag is rejected by a field_validator
  </behavior>
  <action>
    Create `agent/peer_schema.py` mirroring `agent/redflag_schema.py`: import `GroundedAnswer, RefusalResponse` from `agent/schemas.py` (never redefine — cross-phase locked `claim_id` contract). Model `PeerRecord` (drhp_id, computed_at, `peer_set: GroundedAnswer | RefusalResponse`, `companies: list[PeerCompany]`, `as_of: str`), `PeerCompany` (name, `is_ipo: bool`, metrics), `PeerMetric` (metric key + `current: PeerCell` + `drhp_date: PeerCell | None`), and `PeerCell(value, source, as_of)`. Reuse the `{"refusal": ...}` discriminator codec verbatim for `peer_set`, and `to_dict`/`from_dict`/`to_json` (`json.dumps(indent=2, ensure_ascii=False)`) for diff-reviewable caches. Add a locked metric-key `frozenset` (`pe`, `pb`, `ev_ebitda`, `roe`) with a rejecting `field_validator`. This encodes D4-05 (per-cell provenance) and the locked BOTH-dimensions decision. Write `tests/unit/test_peer_schema.py` covering the behavior cases (round-trip, refusal codec, per-cell None/source/as_of, unknown-key rejection) — mirror `tests/unit/test_redflag_schema.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_peer_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>Schema round-trips; refusal-vs-grounded discriminator works; per-cell source+as_of present; unknown metric/source keys rejected; imports `GroundedAnswer`/`RefusalResponse` from `agent/schemas.py` (does not redefine them).</acceptance_criteria>
  <done>`agent/peer_schema.py` exists; `test_peer_schema.py` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: peer_queries.py + peer_sources.py source-priority ladder + test</name>
  <read_first>
    - `pipelines/redflag_queries.py` (the canned-query constants module shape) and `pipelines/snapshot_queries.py`
    - `pipelines/ingest.py` (the `requests-cache` + `tenacity` + per-item failure-isolation fetch-loop shell to mirror)
    - `pipelines/risk_idf.py` (existing `rapidfuzz` usage — reuse for peer-name → ticker matching)
    - `.planning/phases/.../04-RESEARCH.md` §Pattern 3 (source-priority ladder), §Code Examples (yfinance fallback fetch + the `.info` keys validated in 04-01), §Pitfall 1/2 (screener rate-limit, yfinance gaps)
    - `04-01-SUMMARY.md` (the confirmed yfinance `.info` key coverage from the live spike)
  </read_first>
  <behavior>
    - given mocked source fetchers, the ladder returns the FIRST available value per cell and records its source flag: screener.in (`s`) → yfinance (`y`) → NSE/BSE (`n`)
    - a metric missing from every source yields `PeerCell(value=None, source=None)` — never interpolated, never zero
    - yfinance `0`/`None`/`NaN` for a ratio is coerced to missing (P15 — a real 0 ROE is treated as missing for ratios)
    - only hard-coded source hostnames are fetched (no URL derived from DRHP/user input — SSRF control)
    - a negative/undefined P/E is preserved as a sentinel the schema/renderer maps to `NM`
  </behavior>
  <action>
    Create `pipelines/peer_queries.py` with `PEER_SET_QUERY` (the RESEARCH/PATTERNS canned "Comparison with Listed Industry Peers / Basis for Issue Price" query string). Create `pipelines/peer_sources.py` with per-source fetchers — `screener_multiples(name)` (primary; `requests-cache` + `beautifulsoup4`+`lxml`, hard-coded `screener.in` host), `yfinance_multiples(ticker)` (fallback; the RESEARCH `clean()` coercion treating 0/None/NaN as missing, reading the `.info` keys confirmed in 04-01), and an NSE/BSE rung — plus a `resolve_multiples(company)` orchestrator that walks the ladder and returns a `PeerMetric` per metric with the winning cell's `source` recorded. Use `tenacity` for backoff and `rapidfuzz` for name→ticker matching. All fetching is import-safe (no network at module import). Write `tests/unit/test_peer_sources.py` monkeypatching each fetcher to assert the behavior cases (ladder order, missing→None, 0-coercion, sentinel). Per D4-05 this is the honest-sourcing engine (PEER-02).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_peer_sources.py -x -q</automated>
  </verify>
  <acceptance_criteria>Ladder picks first-available and records the source flag; missing→None (no fabrication); yfinance 0/None/NaN coerced to missing; only hard-coded hosts referenced; no live network under `tests/unit`.</acceptance_criteria>
  <done>`peer_queries.py` + `peer_sources.py` exist; `test_peer_sources.py` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: peers.py precompute + load + allow-list gate + seed fixture + test</name>
  <read_first>
    - `pipelines/redflag.py` (verbatim: `load_redflag`/`_redflag_path` allow-list gate :70-104, the GRAPH.invoke canned-query loop :129-243, the `write: bool = True` kwarg :129/147-148, the Typer `precompute-one`/`precompute-all` per-IPO failure isolation :314-361)
    - `tests/unit/test_redflag_precompute.py` (:1-90 — monkeypatch `agent.graph.GRAPH.invoke`; grounded-state / refusal-state builders; the `::test_path_gate` unknown-id ValueError)
    - `data/redflag/swiggy_2024_11.json` (the seed-cache shape to mirror for `data/peers/`)
  </read_first>
  <behavior>
    - `precompute_peers(drhp_id, *, write=True)` runs `GRAPH.invoke` for the peer SET, then loops named peers through `peer_sources.resolve_multiples`, and (write=True) writes `data/peers/<id>.json`
    - a graph `RefusalResponse` return is stored as the `peer_set` value (D4-06 honest empty-state), never fabricated
    - `load_peers(unknown_id)` raises `ValueError` (allow-list gate) BEFORE forming a path; `load_peers(known_id)` with no cache raises `FileNotFoundError`
    - `precompute-all` isolates per-IPO failure (logs + continues, P14)
    - tests pass `write=False` and monkeypatch `GRAPH.invoke` + the source fetchers — no live network / LLM
  </behavior>
  <action>
    Create `pipelines/peers.py` mirroring `pipelines/redflag.py`: `PEERS_DIR = .../data/peers`, `load_peers`/`_peers_path` with the `is_known_drhp_id` allow-list gate verbatim (T-02-V5 path-traversal control), `precompute_peers(drhp_id, *, write=True)` that deferred-imports `agent.graph.GRAPH`, invokes `PEER_SET_QUERY`, extracts peer names from the resulting `GroundedAnswer` (or stores the `RefusalResponse` empty-state), calls `pipelines.peer_sources.resolve_multiples` per named peer at precompute time only, assembles a `PeerRecord` (including the IPO's own DRHP-derived row with source `d`, as_of `drhp_date`), and writes the diff-reviewable JSON. Add Typer `precompute-one`/`precompute-all` with per-IPO try/except isolation. Hand-seed `data/peers/swiggy_2024_11.json` (a realistic PeerRecord with the DRHP-cited peer set + a couple of per-cell-sourced multiples + at least one honest `—` cell) so 04-05 renders offline — clearly a CODE-NOW-DEFER seed, flagged for live regeneration. Write `tests/unit/test_peers_precompute.py` (monkeypatch `GRAPH.invoke` grounded + refusal states, monkeypatch source fetchers, `write=False`; include `::test_path_gate`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_peers_precompute.py tests/unit/test_peer_schema.py tests/unit/test_peer_sources.py -q && .venv/bin/python -c "from pipelines.peers import load_peers; r=load_peers('swiggy_2024_11'); print(type(r).__name__, len(r.companies))"</automated>
  </verify>
  <acceptance_criteria>
    - Precompute writes a valid PeerRecord; refusal path stored honestly; allow-list gate raises on unknown id; per-IPO isolation in `precompute-all`.
    - `load_peers('swiggy_2024_11')` loads the seed fixture and returns a PeerRecord with ≥1 company and at least one honest `—` cell.
    - No live network/LLM under `tests/unit` (all monkeypatched).
  </acceptance_criteria>
  <done>`pipelines/peers.py` + seed `data/peers/swiggy_2024_11.json` exist; all three peer-data tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query param → cache path | untrusted `drhp_id` reaches `_peers_path` |
| external site → precompute | screener.in / yfinance / NSE HTML + JSON is untrusted input |
| scraped string → stored record | peer names/values persisted into the JSON cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-PATH | Tampering | `_peers_path(drhp_id)` | mitigate | `is_known_drhp_id()` allow-list BEFORE the path string is built (verbatim from `redflag.py`); unknown id raises ValueError. |
| T-04-03-SSRF | Tampering | `peer_sources.py` fetchers | mitigate | Scrape only hard-coded source hostnames (screener.in, Yahoo, NSE/BSE); never fetch a URL derived from DRHP/user input. |
| T-04-03-VALID | Tampering | cache record on load | mitigate | Pydantic validates every field on `from_dict`; unknown metric/source keys rejected by field_validator; missing values are typed `None`, never coerced. |
| T-04-03-FAB | Info-disclosure (integrity) | fabricated peer/multiple | mitigate | Missing = `PeerCell(None)` → `—`; no DRHP peers = stored `RefusalResponse`; never an invented number or set (D4-05/D4-06). |
| T-04-SC | Tampering | screener/yfinance/nse libs | accept | All pre-vetted in CLAUDE.md + gated in 04-01; no new package introduced here. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green (peer schema/sources/precompute + no regression).
- Seed `data/peers/swiggy_2024_11.json` loads and validates.
</verification>

<success_criteria>
PeerRecord with per-cell source+as_of provenance (current + DRHP-date) and the refusal codec; a canned DRHP peer-SET query producing a cited GroundedAnswer or honest RefusalResponse; a source-priority fetcher recording provenance and never fabricating; allow-list-gated precompute/load; a seed fixture unblocking the renderer. All tests green offline.
</success_criteria>

<output>
Create `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-03-SUMMARY.md` when done, noting the seed-fixture contents and the deferred live-extraction runbook step.
</output>
