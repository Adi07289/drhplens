---
phase: 04-historical-ipo-dataset-peer-comparator-gmp-display
plan: 06
type: execute
wave: 4
depends_on: [04-02, 04-04, 04-05]
files_modified:
  - app/static/drhplens.css
  - ui/copy.py
  - ui/snapshot_blocks.py
  - pages/02_snapshot.py
autonomous: false
requirements: [GMP-01, GMP-02, UI-04]
user_setup: []

must_haves:
  truths:
    - "A user sees a deliberately de-emphasised, monochrome GMP block as the LAST read block, low on the page, with a persistent caveat and a 'What is GMP? Why we don't trust it' disclosure (GMP-01, D4-01/02)"
    - "The multi-source spread (₹low–₹high across N sources) is the headline — the divergence is the honesty signal, never a single authoritative number (D4-01)"
    - "Absent GMP ('No grey-market premium is being reported…') and single-source are first-class rendered states, never a fabricated number (D4-06 variant)"
    - "The GMP block reads only the data/gmp cache and imports no forecast/model module (GMP-02, D4-03)"
  artifacts:
    - path: "ui/snapshot_blocks.py"
      provides: "render_gmp_block (monochrome range strip, no accent, cache-only)"
      contains: "def render_gmp_block"
    - path: "app/static/drhplens.css"
      provides: ".drhp-gmp-block/-caveat/-range/-tick/-source-line (monochrome, no accent)"
      contains: "drhp-gmp-range"
    - path: "ui/copy.py"
      provides: "GMP heading/caveat/disclosure/range/absent/single-source/error copy, scrubber-clean"
      contains: "GMP"
  key_links:
    - from: "pages/02_snapshot.py"
      to: "ui.snapshot_blocks.render_gmp_block"
      via: "wired as the LAST read block immediately above the Q&A 2xl divider (D4-02)"
      pattern: "render_gmp_block"
    - from: "ui/snapshot_blocks.py::render_gmp_block"
      to: "pipelines.gmp.load_gmp"
      via: "cache-only read via try/except (no scrape at render)"
      pattern: "load_gmp"
---

<objective>
Render the read-only GMP surface (GMP-01) as the quietest block in the app: monochrome, no accent, low on the page, behind a "What is GMP? Why we don't trust it" disclosure, with a persistent caveat and a multi-source range strip whose visible spread IS the honesty signal (D4-01/02). Absent-GMP (the common case for already-listed catalogue IPOs) and single-source are first-class states. The block reads only the `data/gmp` cache and imports no model module (GMP-02, D4-03) — completing the GMP vertical slice with 04-04.

Purpose: GMP-01 render + GMP-02 render-side isolation + UI-04 (₹ via format_inr, GMP glossary term).
Output: GMP CSS + copy + `render_gmp_block` + page wiring (last block) + 375px human-verify.
</objective>

<phase_goal>
**As a** retail investor, **I want to** see grey-market premium only as a quiet, caveated, multi-source spread low on the page **so that** I understand it is unofficial and unreliable rather than mistaking it for a demand signal.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-UI-SPEC.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-PATTERNS.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-CONTEXT.md
@ui/snapshot_blocks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GMP CSS (monochrome range strip) + GMP copy</name>
  <read_first>
    - `app/static/drhplens.css` lines ~419-461 (`.drhp-split-bar*` monochrome track/tick grammar to mirror — but GMP uses NO accent), ~9-27 (tokens)
    - `ui/copy.py` ~408-479 (import-time scrubber) + `_SAMPLE_FORMAT_VALUES` (~426-448 — add `low`, `high`, `source`, `value`, `date` placeholders)
    - `.planning/phases/.../04-UI-SPEC.md` §R-4 + §Visuals GMP (structure, states) + §Copywriting Contract (verbatim GMP strings)
  </read_first>
  <action>
    Add to `app/static/drhplens.css` (single stylesheet): `.drhp-gmp-block` (mirrors `.drhp-snapshot-block` chrome but with NO accent — the quietest block), `.drhp-gmp-caveat` (Small muted, persistent), `.drhp-gmp-range` + `.drhp-gmp-tick` (a `surface-secondary` track with muted `border-subtle`/`text-muted` ticks positioned by inline `left:%` — monochrome, no accent, no red/green), and `.drhp-gmp-source-line` (Small muted), plus responsive rules under the four breakpoints. Add to `ui/copy.py` (scrubber-enrolled) the GMP strings VERBATIM from the UI-SPEC Copywriting Contract: heading `Grey-market premium (unofficial)`, the persistent caveat, the range headline template `₹{low}–₹{high} across {n} sources`, the per-source line template, single-source note, absent note `No grey-market premium is being reported for this IPO right now.`, the disclosure body `What is GMP? Why we don't trust it` + its explanation, and the shared error state. Add the new placeholders to `_SAMPLE_FORMAT_VALUES`. Ensure the copy avoids the `sell` stem and any signal framing ("demand"/"strong") — scrubber-clean. Encodes D4-01/02/09.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_copy_no_banned_tokens.py -q && .venv/bin/python -c "import ui.copy"</automated>
  </verify>
  <acceptance_criteria>`import ui.copy` passes the import-time scrubber (GMP copy banned-token-free, no signal framing); GMP CSS classes use only `--drhp-*` tokens, no accent, no red/green; range-strip templates render with sample substitution.</acceptance_criteria>
  <done>GMP CSS + copy landed and scrubber-green.</done>
</task>

<task type="auto">
  <name>Task 2: render_gmp_block + wire as the last read block</name>
  <read_first>
    - `ui/snapshot_blocks.py` ~118-166 (`render_split_bar` monochrome range grammar + single self-contained `st.markdown` + `role="img"`/`aria-label`), ~345-353 (white-bar `st.container(border=True)` rule), ~387-393 (unique expander `key`)
    - `pages/02_snapshot.py` ~41-49 (renderer imports), ~95-119/122-228 (`main()` + `_render_redflag_block` try/except cache posture at ~163-176), ~221-226 (the Q&A `2xl` divider — GMP goes immediately above it)
    - `pipelines/gmp.py` (Plan 04-04 — `load_gmp`) + `ui/format_inr.py`
    - `.planning/phases/.../04-UI-SPEC.md` §Visuals GMP (states table) + §IA (block 10 = last read block)
  </read_first>
  <action>
    Add `render_gmp_block(record: GmpRecord)` to `ui/snapshot_blocks.py`: wrap in `with st.container(border=True):` (no split div), emit the heading (with the `GMP` glossary term via the 04-05 `glossary_term` helper), the persistent caveat, then a SINGLE self-contained `st.markdown` `.drhp-gmp-range` monochrome strip with one muted tick per aggregator positioned by inline `left:%` and the min–max headline (`₹{low}–₹{high} across {n} sources`, ₹ via `format_inr`), `role="img"` + a text `aria-label` stating the spread (WCAG 1.4.1 — never position-only), then the per-source list (each ₹ via `format_inr`, source labels `_html.escape`d), then the `What is GMP? Why we don't trust it` explanation in an `st.expander(expanded=False)` with a UNIQUE `key`. Render the states per the UI-SPEC table: absent (`quotes==[]`) → `_render_not_disclosed` note (never a zero); single-source → value + the single-source note; default → the spread. NO accent, no red/green, no up/down arrow, no single big authoritative number. Wire it into `pages/02_snapshot.py` as the LAST read block immediately above the Q&A `2xl` divider (D4-02), reading `load_gmp(drhp_id)` inside a try/except (FileNotFoundError → absent-state; other Exception → inherited amber `.drhp-refusal`, NOT red), `drhp_id` already allow-list-validated in `main()`. Import `render_gmp_block` alongside the existing snapshot_blocks imports. Encodes GMP-01, GMP-02 (render reads cache only, imports no model), D4-01/02/03.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit -q && .venv/bin/python -c "import ui.snapshot_blocks as b; assert hasattr(b,'render_gmp_block'); import ui.snapshot_blocks, inspect; assert 'xgboost' not in inspect.getsource(ui.snapshot_blocks)"</automated>
  </verify>
  <acceptance_criteria>`render_gmp_block` exists; uses `st.container(border=True)` + a single self-contained markdown for the range strip with `role="img"`+aria; absent/single-source/default states render honestly; ₹ via `format_inr`; source labels escaped; expander has a unique key; wired as the last read block above the Q&A divider; no forecast/model import reachable from the render path; unit suite green.</acceptance_criteria>
  <done>`render_gmp_block` added + wired last; suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 375px de-emphasis + spread + absent-state visual verify</name>
  <action>Run the seeded Streamlit app and human-verify the GMP block at 375px per the how-to-verify steps below: de-emphasised monochrome last read block, spread-as-headline, collapsible disclosure, honest absent-state, persistent caveat. No code changes — Task 2 wired it; this confirms the visual contract.</action>
  <what-built>The GMP block wired as the last read block on `pages/02_snapshot.py`, rendering the seeded `data/gmp/*.json` (swiggy = absent state; hyundai = synthetic 3-source spread) — cache-only, monochrome, no accent.</what-built>
  <how-to-verify>
    1. Run the Streamlit app; open the snapshot page for the hyundai seed (synthetic spread) at 375px.
    2. Confirm the GMP block is the LAST read block (immediately above the Q&A divider), visually the quietest — monochrome, NO accent, NO red/green, no up/down arrow, no single big number.
    3. Confirm the range strip shows the spread `₹low–₹high across 3 sources` as the headline with muted ticks, and the "What is GMP? Why we don't trust it" disclosure is collapsed by default and expands to the genuine explanation.
    4. Open the swiggy seed (absent) and confirm `No grey-market premium is being reported for this IPO right now.` — no fabricated number.
    5. Confirm the persistent caveat is always visible (never collapsed).
  </how-to-verify>
  <acceptance_criteria>GMP block renders de-emphasised + monochrome as the last read block per 04-UI-SPEC R-4; spread is the headline; disclosure collapses/expands; absent-state honest; caveat persistent; no accent/red/green anywhere.</acceptance_criteria>
  <resume-signal>Type "approved" or describe rendering issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped GMP source label → page HTML | untrusted aggregator names interpolated via `unsafe_allow_html` |
| GMP render path → model modules | the boundary GMP-02 forbids crossing (render side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-XSS | Tampering/Info-disclosure | GMP source labels in `render_gmp_block` | mitigate | `_html.escape(..., quote=True)` on every scraped source string before interpolation. |
| T-04-06-ISO | Info-disclosure (integrity) | GMP render importing model code | mitigate | Render reads only `data/gmp` cache via `load_gmp`; verify path imports no forecast/model module (the Task 2 `inspect.getsource` check on `ui.snapshot_blocks` guards against an accidental xgboost import creeping into the shared module). |
| T-04-06-PATH | Tampering | `load_gmp(drhp_id)` on the page | mitigate | `drhp_id` allow-list-validated in `main()` + `_gmp_path` gate (04-04). |
| T-04-06-SIGNAL | Compliance/honesty | GMP framed as a signal | mitigate | Monochrome, no accent, spread-as-headline, persistent caveat, disclosure; copy scrubber-clean (no "demand"/"strong"); verified at the 375px checkpoint (P21, D4-02). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green.
- 375px human-verify checkpoint approved.
- Full suite: `.venv/bin/python -m pytest -q --deselect tests/unit/test_embedder.py::test_bge_m3_real_embed_query_1024_dim` green before phase gate.
</verification>

<success_criteria>
A monochrome, de-emphasised, cache-only GMP block as the last read block; multi-source spread as the honesty headline; absent + single-source first-class; ₹ via format_inr; GMP-02 render-side isolation preserved; all copy scrubber-clean; 375px visual approved.
</success_criteria>

<output>
Create `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-06-SUMMARY.md` when done.
</output>
