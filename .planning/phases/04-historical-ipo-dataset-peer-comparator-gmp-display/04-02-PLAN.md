---
phase: 04-historical-ipo-dataset-peer-comparator-gmp-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/format_inr.py
  - ui/catalogue.py
  - ui/snapshot_blocks.py
  - tests/unit/test_format_inr.py
  - tests/unit/test_format_inr_adoption.py
autonomous: true
requirements: [UI-04]
user_setup: []

must_haves:
  truths:
    - "A user sees every rupee amount app-wide with Indian digit grouping (12,34,567) and auto-scaled lakh/crore, not Western grouping"
    - "The catalogue card issue-size no longer renders the Western-grouping FLAG-FORMAT bug"
    - "A missing rupee value renders the em-dash sentinel, a negative renders in parentheses (no red)"
  artifacts:
    - path: "ui/format_inr.py"
      provides: "the ONE shared Indian rupee formatter (D4-07)"
      exports: ["format_inr"]
      min_lines: 25
    - path: "tests/unit/test_format_inr.py"
      provides: "grouping + lakh/crore + edge-case pins"
      contains: "def test_"
    - path: "tests/unit/test_format_inr_adoption.py"
      provides: "grep-gate proving no bare Western :, ₹ render survives in ui/"
      contains: "format_inr"
  key_links:
    - from: "ui/catalogue.py::_format_issue_size"
      to: "ui/format_inr.format_inr"
      via: "refactor — call the shared utility, delete the f\"₹{n:,}\" bug"
      pattern: "format_inr"
    - from: "ui/snapshot_blocks.py::_format_fin_value"
      to: "ui/format_inr.format_inr"
      via: "refactor to shared utility"
      pattern: "format_inr"
---

<objective>
Deliver the D4-07 invariant: ONE shared `ui/format_inr.py` utility that renders every rupee amount with Indian digit grouping (last three digits, then groups of two) + auto-scaled lakh↔crore + `None → "—"` sentinel + negatives-in-parentheses, and refactor every ad-hoc ₹ render site to call it — fixing the latent Western-grouping FLAG-FORMAT bug in `ui/catalogue.py::_format_issue_size` and `ui/snapshot_blocks.py::_format_fin_value` rather than duplicating it.

This is the formatting half of UI-04 and a foundation the peer table (04-05) and GMP block (04-06) both consume for their ₹ values.

Purpose: Correct Indian-context number formatting app-wide with a single source of truth (D4-07, RESEARCH §Pitfall 6).
Output: `ui/format_inr.py` + two unit tests + refactored call sites.
</objective>

<phase_goal>
**As a** Indian retail investor, **I want to** see rupee amounts in the lakh/crore grouping I actually read (₹12.5 lakh, ₹1,247 crore, ₹45,600) **so that** the numbers are legible and trustworthy rather than Western-formatted.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-RESEARCH.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-PATTERNS.md
@.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test_format_inr.py, then implement ui/format_inr.py</name>
  <read_first>
    - `.planning/phases/.../04-RESEARCH.md` §Code Examples (the `format_inr` / `_group_indian` reference algorithm + the test-case table)
    - `.planning/phases/.../04-UI-SPEC.md` §"One shared format_inr utility (D4-07)" (the contract: scaling thresholds, decimals, negatives, None)
    - `tests/unit/test_catalogue.py` (pure-function assert style to mirror)
  </read_first>
  <behavior>
    - `format_inr(100000) == "₹1 lakh"`; `format_inr(1250000) == "₹12.5 lakh"`
    - `format_inr(124700000)` scales to crore (`₹12.47 crore`); a value ≥ 1e9 scales to crore with Indian grouping on the integer part (`₹1,247 crore` per A8/UI-SPEC — apply `_group_indian` to scaled integer parts > 999)
    - `format_inr(45600) == "₹45,600"`; `format_inr(1234567)` groups Indian-style then scales (`₹12.35 lakh`)
    - `format_inr(None) == "—"` (em dash sentinel)
    - `format_inr(-1234) == "(₹1,234)"` — parentheses, no colour/sign token
    - scaled values trim trailing zeros (`₹1,247 crore` not `₹1247.00 crore`; `₹12.5 lakh` not `₹12.50 lakh`)
  </behavior>
  <action>
    Create `tests/unit/test_format_inr.py` with the behavior cases above (pin the RESEARCH test-case table exactly, including the A8 decision to Indian-group scaled integer parts). Then create `ui/format_inr.py` exposing `format_inr(amount: float | int | None) -> str` with a private `_group_indian(int_str)` helper implementing last-3-then-groups-of-2 grouping, magnitude auto-scale (≥1e7 crore, ≥1e5 lakh, else grouped rupees), 2-decimal-trimmed scaling, `None → "—"`, and negatives wrapped in parentheses in the same (inherited, no-red) convention as `_format_fin_value`. Return a string only — `tabular-nums` is applied by CSS at render, not here. This implements D4-07.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_format_inr.py -x -q</automated>
  </verify>
  <acceptance_criteria>All behavior cases pass; `format_inr` is a pure function (string in the None/negative/lakh/crore/grouped branches, no side effects).</acceptance_criteria>
  <done>`ui/format_inr.py` exists and `test_format_inr.py` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all ₹ call sites to format_inr + adoption grep-gate</name>
  <read_first>
    - `ui/catalogue.py` lines ~37-41 (`_format_issue_size`, the `f"₹{n:,} cr"` Western-grouping bug) and its call site (~61-66)
    - `ui/snapshot_blocks.py` lines ~180-191 (`_format_fin_value`, the second bare-`:,` site) and where the metadata header renders issue size
    - Run `grep -rn "₹\|:,\|cr\"" ui/ pages/` to enumerate every ad-hoc rupee render
  </read_first>
  <action>
    Refactor `ui/catalogue.py::_format_issue_size` to delegate to `format_inr` (preserving its "Issue size not disclosed" None-copy where that is the caller's contract, but routing the numeric branch through `format_inr` so grouping is Indian). Note: catalogue `issue_size_cr` is in CRORE units — convert to rupees (`* 1e7`) before calling `format_inr`, or add a documented `unit="crore"` affordance; keep the displayed `crore` suffix consistent with the utility's own scaling (decide and document in the action so the executor does not double-scale). Refactor `ui/snapshot_blocks.py::_format_fin_value` similarly, preserving its `—` + `aria-label` and no-red-negative behaviour by delegating to `format_inr`. Update any other ₹ site found by the grep (metadata header issue size). Create `tests/unit/test_format_inr_adoption.py`: a grep-style gate asserting no bare Western `₹{...:,}` / `f"₹{...:,}"` pattern survives in `ui/*.py` (filter out comment lines with `grep -v '^#'`-equivalent so header prose does not self-invalidate the gate), and that `_format_issue_size` and `_format_fin_value` reference `format_inr`. Do NOT re-implement grouping anywhere (D4-07 = ONE utility).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_format_inr.py tests/unit/test_format_inr_adoption.py tests/unit/test_catalogue.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The adoption gate passes: no bare Western `:,` rupee render remains in `ui/` (comments excluded).
    - `_format_issue_size` and `_format_fin_value` call `format_inr`; the catalogue-crore unit conversion is correct (no double-scaling — e.g. a ₹11,327 cr issue does not render as ₹1.13 lakh crore).
    - Existing `test_catalogue.py` still passes (behaviour preserved for the None/disclosed cases).
  </acceptance_criteria>
  <done>All ₹ call sites route through `format_inr`; the FLAG-FORMAT Western-grouping bug is gone; adoption gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | `format_inr` takes a numeric amount already inside the app; no external/untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-INJ | Tampering | `format_inr` output interpolated into HTML | accept | Output is a formatted numeric string (digits, ₹, commas, parens) with no user-controlled text; callers already `html.escape` any adjacent untrusted strings. No injection surface. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit -q` green.
- Manual sanity (optional): catalogue card + financials table show Indian grouping.
</verification>

<success_criteria>
One shared `format_inr` utility renders Indian grouping + lakh/crore + `—` + parenthesised negatives; every ₹ render site in `ui/` calls it; the Western-grouping bug is fixed and pinned by an adoption gate.
</success_criteria>

<output>
Create `.planning/phases/04-historical-ipo-dataset-peer-comparator-gmp-display/04-02-SUMMARY.md` when done.
</output>
