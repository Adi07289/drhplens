---
phase: 06.1-eval-harness-inline-metrics-langfuse-ops
plan: 04
type: execute
wave: 3
depends_on: [06.1-01, 06.1-02, 06.1-03]
files_modified:
  - scripts/run_eval.py
  - eval/reports/eval_summary.json
  - tests/eval/test_run_rag_eval.py
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "the runner emits eval/reports/eval_summary.json (validated by the EvalSummary schema) AND eval/reports/<date>-rag-eval.md in one pass"
    - "eval_summary.json is populated with faithfulness_deepeval, citation_accuracy, recall_at_5/10/30, a named judge_model, corpus provenance, and per_ipo only for IPOs with a real gold set"
    - "the .md report carries the judge reason strings AND a per-metric interpretation paragraph, including the P10 recall-saturation caveat (recall is a labelled regression floor, not a headline quality metric)"
    - "recall is computed over retrieved_chunks (50), citation over the reranked chunks, faithfulness is serialized with the -1 sentinel; the runner calls langfuse flush() at the end"
  artifacts:
    - path: "scripts/run_eval.py"
      provides: "run_rag_eval() emitting both artifacts via the eval/metrics implementation"
      contains: "eval_summary.json"
    - path: "eval/reports/eval_summary.json"
      provides: "the committed machine artifact the UI + gate read (the card_data.json analog for RAG eval)"
      contains: "judge_model"
    - path: "tests/eval/test_run_rag_eval.py"
      provides: "fixture-driven test that the emitted JSON validates against EvalSummary + the .md contains the recall-floor caveat"
      contains: "EvalSummary"
  key_links:
    - from: "scripts/run_eval.py"
      to: "eval/metrics (one implementation)"
      via: "imports recall_at_k, citation_accuracy, faithfulness_deepeval, EvalSummary — no re-implementation"
      pattern: "from eval.metrics"
    - from: "run_rag_eval()"
      to: "the 50 candidates vs the reranked 5"
      via: "recall_at_k(expected_sources, final_state['retrieved_chunks'], k) ; citation_accuracy over reranked"
      pattern: "retrieved_chunks"
---

<objective>
Evolve `scripts/run_eval.py` past "Phase 1 baseline, measure-only" into the first-class committed runner: one pass over the gold set that emits BOTH `eval/reports/eval_summary.json` (the pinned machine artifact the UI and release gate read) and `eval/reports/<date>-rag-eval.md` (the human report with judge reasons + interpretation paragraphs). It imports the ONE `eval/metrics/` implementation (06.1-01/02) so the report and the gate can never diverge.

Delivers (measured decisions #2/#3/#7; SPEC requirement 1; design doc §4.1; AI-SPEC §4b/§5):
- A `run_rag_eval()` path that invokes the agent over `tests/eval/gold_set.jsonl`, computes `recall_at_k` over `final_state["retrieved_chunks"]` (the 50 candidates) at k=5/10/30, `citation_accuracy` over the reranked chunks, and `faithfulness_deepeval` serially (5-RPM safe) with the -1 sentinel; assembles an `EvalSummary`; writes `eval_summary.json` via `.model_dump_json(indent=2)` and the dated `.md`.
- Per-metric interpretation paragraphs in the `.md`, including the P10 recall-saturation caveat (recall = a labelled regression floor on the current coarse gold spans, NOT a headline quality metric — spike 003) and the gold-set-tightening follow-up note.
- A `flush()` call at the end so the enriched Langfuse traces (06.1-03) ship.

Purpose: EVAL-01's committed artifact (SPEC requirement 1: running the runner produces a populated eval_summary.json + a matching .md). The JSON is the card_data.json analog the inline surface (06.1-06) reads.
Output: the evolved runner + a committed `eval_summary.json` + a runner test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-SPEC.md
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md
@.planning/spikes/003-recall-baseline/README.md
@scripts/run_eval.py
@eval/metrics/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_rag_eval() — invoke agent, compute the three metrics via eval/metrics, emit eval_summary.json + rag-eval.md</name>
  <files>scripts/run_eval.py, eval/reports/eval_summary.json</files>
  <read_first>
    - scripts/run_eval.py in full (the existing run_eval loop: `invoke_with_tracing(initial_state, question)`, reads `final_state.get("reranked_top_k")` + `final_state.get("retrieved_chunks")` (add), refusal/grounded handling, the `contexts = [chunk payload chunk_text[:500] for chunk in reranked[:5]]` block feeding the judge, and the `_check_env` fail-fast). Do NOT touch `compute_numeric_faithfulness` / the numeric track — that gate is unchanged.
    - eval/metrics/__init__.py (imports available: recall_at_k, citation_accuracy, faithfulness_deepeval, EvalSummary)
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md §4 (retrieval_context = the top-5 reranked chunks the agent actually consumed — P18; do not expand) + §4b (construct EvalSummary from computed floats, `.model_dump_json(indent=2)`; the -1 sentinel for not-measured; surface/fail only when a DETERMINISTIC metric can't compute)
    - docs/superpowers/specs/2026-07-28-6a-eval-harness-design.md §4.3 (the exact eval_summary.json shape: generated, judge_model, corpus{gold_set,ipo,n_questions}, aggregate, per_ipo, report)
    - app/observability/trace_enrichment.py (the flush() helper to call at the end)
    - agent/state.py L46-47 (retrieved_chunks = the 50 candidates for recall; reranked_top_k = the 5 for citation + judge context)
  </read_first>
  <action>
    Add a `run_rag_eval(gold_set_path="tests/eval/gold_set.jsonl", output_dir="eval/reports")` function (leave `run_eval` and `compute_numeric_faithfulness` intact for back-compat; wire `run_rag_eval` as the default `__main__` path or a `--rag` flag). For each grounded gold entry, invoke the agent via `invoke_with_tracing`, then: recall_at_5/10/30 = `recall_at_k(expected_sources, final_state["retrieved_chunks"], k)` for k in (5,10,30) — over the 50 candidates, NOT the reranked list (measured decision #2 / spike 003); citation = `citation_accuracy(expected_sources, reranked_top_k)`; faithfulness = `faithfulness_deepeval(question, prose, contexts)` where `contexts` stays byte-identical to the top-5 reranked chunk text (P18) — call it SERIALLY (one question at a time; async_mode=False + the built-in tenacity backoff handle the 5-RPM ceiling). Aggregate: mean each deterministic metric across grounded questions; for faithfulness, average only the measured (>= 0) values, else store -1. Build a `Corpus(gold_set=..., ipo="Swiggy DRHP", n_questions=<count>)`, an `Aggregate(...)`, and an `EvalSummary(generated=<ISO date>, judge_model="gemini-3.5-flash", corpus=..., aggregate=..., per_ipo={<swiggy_id>: Aggregate(...)}, report="eval/reports/<date>-rag-eval.md")` — populate `per_ipo` ONLY for Swiggy (the one IPO with a real gold set; never fabricate a per-IPO entry for others — honesty invariant). Write `eval_summary.json` via `summary.model_dump_json(indent=2)` (write-time schema validation is automatic — a schema violation raises and fails the runner, which is correct). Write the dated `<date>-rag-eval.md` with a summary table + a per-question table carrying the judge `reason` strings. Call `flush()` (06.1-03) at the very end so enriched traces ship. Fail the runner (non-zero) ONLY when a deterministic metric cannot compute — never on a judge miss (a -1 faithfulness is a valid recorded outcome).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast,inspect; import scripts.run_eval as r; src=inspect.getsource(r); assert 'run_rag_eval' in src and 'from eval.metrics' in src and 'retrieved_chunks' in src and 'model_dump_json' in src and 'flush' in src; print('runner shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `run_rag_eval` imports recall_at_k/citation_accuracy/faithfulness_deepeval/EvalSummary from eval.metrics (no re-implementation).
    - recall uses `final_state["retrieved_chunks"]` (50); citation uses reranked; faithfulness uses the byte-identical top-5 reranked context and is called serially.
    - It writes eval_summary.json via `EvalSummary(...).model_dump_json(indent=2)` and a dated .md, and calls flush() at the end.
    - per_ipo contains only Swiggy; no fabricated per-IPO entries.
    - The existing numeric track / compute_numeric_faithfulness is untouched.
  </acceptance_criteria>
  <done>The runner emits a schema-valid eval_summary.json + a dated rag-eval.md in one pass, importing the single eval/metrics implementation, computing recall over the 50 candidates, and flushing Langfuse at the end.</done>
</task>

<task type="auto">
  <name>Task 2: P10 interpretation paragraphs + recall-saturation / gold-set caveat + fixture-driven runner test</name>
  <files>scripts/run_eval.py, tests/eval/test_run_rag_eval.py</files>
  <read_first>
    - .planning/spikes/003-recall-baseline/README.md (the verdict: recall = 1.000 at every k because expected_sources are 10–101-page section ranges, mean 44.5 — a saturated/low-signal FLOOR; the /gsd-eval-review gate must reject presenting it as a quality win without this caveat; gold-set task = tighten expected_sources to <=2–3-page spans + add RPT/numeric coverage)
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md §5 "Reference Dataset" (the RPT/numeric coverage-gap recommendation) + §8 item 4 (recall is a floor, not a signal)
    - scripts/run_eval.py existing report writers (`_write_numeric_report` — the "Interpretation (P10)" paragraph grammar to mirror)
    - eval/metrics/schema.py (EvalSummary — the test validates the emitted JSON against it)
  </read_first>
  <action>
    In the `<date>-rag-eval.md` writer, add a dedicated "Interpretation (P10)" section with one paragraph PER headline metric: (a) faithfulness — labelled a DeepEval LLM-judge number (judge named), reported not gated, non-deterministic, requires the >=50-example human calibration before it is trusted; (b) citation_accuracy — deterministic span-overlap, the finer-grained real retrieval-quality signal, release-gated >=0.95; (c) recall@k — the P10 SATURATION caveat verbatim in substance: recall reads 1.00 because the gold `expected_sources` are coarse 10–101-page section ranges (mean 44.5), so recall@10 is a labelled REGRESSION FLOOR (>=0.85), NOT a headline quality metric; the real signal is citation-accuracy + faithfulness; the gold-set follow-up (tighten expected_sources to <=2–3-page spans + add RPT-attribution and numeric unit/period coverage, then re-ratify) is FLAGGED here as a documented follow-up, not executed in 6.1 (measured decision #7). Then write `tests/eval/test_run_rag_eval.py`: using a fixture / monkeypatched `invoke_with_tracing` (a stubbed final_state with retrieved_chunks + reranked_top_k + a grounded_answer — no live services) and a monkeypatched `faithfulness_deepeval` returning a fixed value, run `run_rag_eval` against a tmp output dir and assert (1) the emitted `eval_summary.json` loads and validates via `EvalSummary.model_validate_json(...)`, (2) `judge_model` is non-empty, (3) `per_ipo` has only the Swiggy key, (4) the emitted `.md` contains the recall-floor caveat substring, (5) recall was computed off `retrieved_chunks` (assert the stub's 50-candidate list drove the k=10 value, distinct from a reranked-5 computation).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/eval/test_run_rag_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The .md carries a per-metric P10 interpretation section including the recall-saturation floor caveat + the gold-set follow-up note.
    - The runner test drives run_rag_eval fully offline (monkeypatched agent + judge) and asserts the JSON validates against EvalSummary, per_ipo is Swiggy-only, and the recall-floor caveat is present.
    - The test proves recall came from retrieved_chunks (50), not reranked (5).
  </acceptance_criteria>
  <done>The committed report explains every headline metric (P10) and documents the recall floor + gold-set follow-up honestly, pinned by an offline runner test that validates the JSON against the UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gold set + live agent → committed artifacts | the runner turns live retrieval/judge output into a committed JSON + report the UI and gate trust; a mislabelled or over-claimed metric is an honesty (P10) breach |
| judge output → eval_summary.json | the non-deterministic LLM-judge value must be recorded honestly (real value or -1), never inflated or conflated with a deterministic guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-11 | Spoofing | evaluation theater (P10) | mitigate | Mandatory per-metric interpretation paragraphs; recall labelled a floor, not a headline; faithfulness labelled judge-based + calibration-pending |
| T-6.1-12 | Spoofing | fabricated per-IPO figure | mitigate | per_ipo populated ONLY for Swiggy (the one real gold set); asserted by the runner test |
| T-6.1-13 | Tampering | report/gate divergence | mitigate | The runner imports the ONE eval/metrics implementation; the JSON is schema-validated at write time |
| T-6.1-14 | Information Disclosure | GEMINI_API_KEY / raw context in report | mitigate | Only truncated (~500-char) reranked chunk text + judge reasons are written; no keys logged |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/eval/test_run_rag_eval.py -q` passes fully offline.
- The committed `eval/reports/eval_summary.json` validates against EvalSummary and names the judge.
- The dated `.md` contains the recall-saturation floor caveat + the gold-set follow-up note.
- Human-check (batched end-of-phase, P10 governing gate): a >=50-example human spot-check of judge verdicts vs human labels reaches >=0.7 correlation BEFORE the faithfulness figure is surfaced (AI-SPEC §5/§7); confirm no headline metric is presented without its interpretation paragraph.
</verification>

<success_criteria>
- One runner pass emits a schema-valid eval_summary.json + a dated rag-eval.md.
- recall over the 50 candidates; citation over reranked; faithfulness serialized with the -1 sentinel; Langfuse flushed.
- Every headline metric carries an honest interpretation paragraph; the recall floor + gold-set follow-up are documented (P10).
</success_criteria>

<output>
Create `.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-04-SUMMARY.md` when done.
</output>
