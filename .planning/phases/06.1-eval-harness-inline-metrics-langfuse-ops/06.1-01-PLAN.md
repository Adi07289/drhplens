---
phase: 06.1-eval-harness-inline-metrics-langfuse-ops
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pyproject.toml
  - eval/metrics/__init__.py
  - eval/metrics/recall.py
  - eval/metrics/citation.py
  - eval/metrics/schema.py
  - tests/unit/test_eval_metrics.py
  - tests/unit/test_eval_summary_schema.py
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "eval/metrics exposes importable pure functions recall_at_k and citation_accuracy (deterministic span-overlap, no model, no live call)"
    - "recall_at_k is computed over the 50 retrieved candidates and reports k=5/10/30 (NOT over the reranked top-5, which RERANK_TOP_K=5 caps)"
    - "EvalSummary Pydantic schema validates the committed eval_summary.json UI contract and rejects an empty judge_model (honesty invariant)"
    - "langfuse<3 (2.60.10) and deepeval>=4.1,<5 (4.1.4) are pinned in requirements.txt and pyproject.toml; RAGAS stays optional/offline"
  artifacts:
    - path: "eval/metrics/recall.py"
      provides: "recall_at_k(expected_sources, retrieved_chunks, k) span-overlap pure function"
      contains: "def recall_at_k"
    - path: "eval/metrics/citation.py"
      provides: "citation_accuracy(expected_sources, chunks) span-overlap pure function"
      contains: "def citation_accuracy"
    - path: "eval/metrics/schema.py"
      provides: "EvalSummary / Aggregate / Corpus Pydantic models (the pinned UI contract)"
      contains: "class EvalSummary"
    - path: "eval/metrics/__init__.py"
      provides: "package exports recall_at_k, citation_accuracy, EvalSummary"
      exports: ["recall_at_k", "citation_accuracy", "EvalSummary"]
  key_links:
    - from: "eval/metrics/recall.py"
      to: "the 50 Qdrant candidates (state['retrieved_chunks'])"
      via: "top-k slice over retrieved candidates, not reranked_top_k"
      pattern: "retrieved_chunks\\[:k\\]"
    - from: "eval/metrics/schema.py"
      to: "eval_summary.json UI contract"
      via: "EvalSummary._named_judge validator (no figure without a named judge)"
      pattern: "judge_model"
---

<objective>
Stand up the importable `eval/metrics/` package — the ONE deterministic-metric implementation that both the eval runner (06.1-04) and the release gate (06.1-05) import so the committed report and the gate can never diverge — plus pin the phase dependencies.

This plan delivers, test-first (TDD; these are pure functions with defined I/O):
- `recall.py` — deterministic span-overlap `recall_at_k` over the 50 retrieved candidates, reported at k=5/10/30 (measured decision #2; spike 003).
- `citation.py` — deterministic span-overlap `citation_accuracy` (measured decision #2).
- `schema.py` — the pinned `EvalSummary` Pydantic model that IS the UI contract (design doc §4.3; AI-SPEC §4b).
- Dependency pins: `langfuse<3`, `deepeval>=4.1,<5` (measured decision #1; spikes 001/002).

Purpose: EVAL-01 requires one importable metric package (SPEC requirement 1; "One metric implementation" constraint). Lift the private `_recall_at_k`/`_citation_accuracy` out of `scripts/run_eval.py` into reusable, tested pure functions — mirroring how `compute_numeric_faithfulness` is already the single importable numeric implementation.
Output: `eval/metrics/{recall,citation,schema,__init__}.py` + unit tests; pinned deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-SPEC.md
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md
@.planning/spikes/003-recall-baseline/README.md
@.planning/spikes/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin phase dependencies (langfuse<3, deepeval, ragas-optional) + scaffold eval/metrics package</name>
  <files>requirements.txt, pyproject.toml, eval/metrics/__init__.py</files>
  <read_first>
    - requirements.txt (current lines: `langfuse` unpinned at L37, `tenacity` L40, `pydantic>=2.7,<3` L31)
    - pyproject.toml (current deps: `langfuse` L44, `tenacity` L46)
    - .planning/spikes/CONVENTIONS.md (the Pins table: langfuse `<3` (2.60.10); deepeval `>=4.1,<5` (4.1.4); ragas offline-only `>=0.4,<0.5`)
    - .planning/spikes/001-langfuse-v4-migration/README.md (why pin <3, not migrate to v4)
    - .planning/spikes/002-deepeval-gemini-judge/README.md (why deepeval>=4.1,<5)
  </read_first>
  <action>
    Pin the exact spike-validated versions (measured decision #1). In requirements.txt: change the bare `langfuse` to `langfuse<3` (spike 001 — v4 is a full OTEL rewrite that still needs the `langchain` package; pinning keeps the working v2 client + no-op fallback). Add `deepeval>=4.1,<5` (spike 002 — native GeminiModel judge, no OpenAI dep). Add `ragas>=0.4,<0.5` marked as an OPTIONAL offline-only cross-check with an inline comment "offline cross-check only; NEVER imported in CI" (AI-SPEC Pitfall 5). Mirror the same three pins in pyproject.toml's dependency list. Do NOT add `langchain` (EVAL-05 uses the direct client API, not the CallbackHandler — spike 001). Both packages are already installed in .venv at the pinned versions (deepeval 4.1.4, langfuse 2.60.10) and are in the locked research STACK.md, so no new install/legitimacy risk. Create `eval/metrics/__init__.py` as an empty package marker for now (Tasks 2/3 add the exports); add a one-line module docstring naming it the single importable metric implementation shared by run_eval.py and release_gate.py.
  </action>
  <verify>
    <automated>grep -q 'langfuse<3' requirements.txt && grep -q 'deepeval>=4.1,<5' requirements.txt && grep -q 'langfuse<3' pyproject.toml && grep -q 'deepeval' pyproject.toml && test -f eval/metrics/__init__.py && .venv/bin/python -c "import eval.metrics" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt pins `langfuse<3` and `deepeval>=4.1,<5`; ragas is present but annotated offline-only.
    - pyproject.toml carries the same two pins.
    - No `langchain` dependency is added.
    - `eval/metrics/__init__.py` exists and imports cleanly.
  </acceptance_criteria>
  <done>The three pins are set in both dep files, the eval.metrics package imports, and no langchain dep was introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD the deterministic span-overlap metrics — recall_at_k (over the 50 candidates) + citation_accuracy</name>
  <files>eval/metrics/recall.py, eval/metrics/citation.py, tests/unit/test_eval_metrics.py</files>
  <read_first>
    - scripts/run_eval.py lines 65-99 (the private `_citation_accuracy` and `_recall_at_k` to lift — the exact span-overlap logic: `chunk_start <= exp_end and chunk_end >= exp_start`, `page_start`/`page_end` from `chunk["payload"]`)
    - .planning/spikes/003-recall-baseline/README.md (recall MUST be computed over `retrieved_chunks` (50 candidates), NOT `reranked_top_k`; RERANK_TOP_K=5 makes k>5 undefined on the reranked output; recall is saturated at 1.00 on the current coarse gold spans — a labelled floor, not a headline)
    - agent/state.py lines 46-47 (GraphState has `retrieved_chunks: list[dict]` (the 50) and `reranked_top_k: list[dict]` (the 5))
    - agent/policies.py L28/L31 (RETRIEVE_LIMIT=50, RERANK_TOP_K=5)
  </read_first>
  <behavior>
    recall_at_k(expected_sources, retrieved_chunks, k):
    - Test: empty expected_sources -> returns 1.0 (vacuous, matches run_eval semantics).
    - Test: one expected span [30,40] present in a chunk within the top-k -> 1.0; absent from top-k but present later -> 0.0 (top-k slice is enforced).
    - Test: k=5 vs k=10 vs k=30 give different results when the matching chunk sits at index 7 (proves the k slice operates over the full candidate list, not a 5-capped list).
    - Test: fraction is hits/len(expected_sources) across multiple spans.
    citation_accuracy(expected_sources, chunks):
    - Test: empty expected_sources -> 1.0.
    - Test: every expected span overlaps some chunk -> 1.0; one span with no overlapping chunk -> proportional (< 1.0).
    - Test: overlap boundary condition `chunk_start <= exp_end and chunk_end >= exp_start` (adjacent-but-touching ranges count as overlap).
  </behavior>
  <action>
    RED then GREEN. Write `tests/unit/test_eval_metrics.py` first covering the behaviors above (they must fail before implementation). Then implement `eval/metrics/recall.py::recall_at_k(expected_sources: list[dict], retrieved_chunks: list[dict], k: int) -> float` by lifting `run_eval._recall_at_k`'s span-overlap logic VERBATIM but renaming the chunk argument to `retrieved_chunks` and slicing `retrieved_chunks[:k]` — the caller (06.1-04 runner) passes `final_state["retrieved_chunks"]` (the 50), so k=10/30 are meaningful (measured decision #2). Implement `eval/metrics/citation.py::citation_accuracy(expected_sources: list[dict], chunks: list[dict]) -> float` by lifting `run_eval._citation_accuracy` (iterate ALL chunks, not a top-k slice — citation-accuracy asks "did the cited page contain the claim"). Both are pure, model-free, import nothing from agent/qdrant/LLM. Add module docstrings noting recall@k is a labelled REGRESSION FLOOR on the current coarse gold spans (saturated at 1.00; spike 003 / P10), not a headline quality metric. Do NOT delete the private copies in run_eval.py yet — 06.1-04 re-points the runner to import these.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_eval_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `from eval.metrics.recall import recall_at_k` and `from eval.metrics.citation import citation_accuracy` both import with no agent/qdrant/LLM import.
    - recall_at_k slices `retrieved_chunks[:k]` so k=5/10/30 differ when the match sits past index 5.
    - citation_accuracy scans all chunks (no k cap).
    - All tests in tests/unit/test_eval_metrics.py pass; they failed before the implementation was written (TDD RED->GREEN).
  </acceptance_criteria>
  <done>Both pure functions exist, import cleanly with no model dependency, compute span-overlap identically to the run_eval originals, and recall operates over the full candidate list with a real k slice — all covered by passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD the pinned EvalSummary schema (the UI contract) + wire package exports</name>
  <files>eval/metrics/schema.py, eval/metrics/__init__.py, tests/unit/test_eval_summary_schema.py</files>
  <read_first>
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md §4b (the exact EvalSummary / Aggregate / Corpus model + the `_named_judge` field_validator; faithfulness_deepeval Field(ge=-1.0, le=1.0) where -1 = not measured; citation_accuracy/recall gated ranges)
    - docs/superpowers/specs/2026-07-28-6a-eval-harness-design.md §4.3 (the JSON shape: generated, judge_model, corpus{gold_set,ipo,n_questions}, aggregate{faithfulness_deepeval, citation_accuracy, recall_at_5/10/30}, per_ipo map, report)
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-UI-SPEC.md (the render reads exactly these keys; per_ipo only for IPOs with a real gold set)
    - pages/01_methodology.py lines 116-127 (the `card_data.json` read pattern this JSON mirrors)
  </read_first>
  <behavior>
    EvalSummary (Pydantic v2):
    - Test: a well-formed summary (from design doc §4.3) validates and round-trips via model_dump_json / model_validate_json byte-stably.
    - Test: empty judge_model ("") raises ValidationError (the _named_judge validator — honesty invariant: never a figure without a named judge).
    - Test: faithfulness_deepeval accepts -1.0 (the "not measured" sentinel) but rejects -2.0 and 1.5 (Field ge=-1.0, le=1.0).
    - Test: citation_accuracy / recall_at_5/10/30 reject values outside [0,1].
    - Test: per_ipo defaults to {} and each value validates as an Aggregate.
    - Test: n_questions must be > 0.
  </behavior>
  <action>
    RED then GREEN. Write `tests/unit/test_eval_summary_schema.py` first (behaviors above; must fail before implementation). Then implement `eval/metrics/schema.py` with the `Aggregate`, `Corpus`, and `EvalSummary` Pydantic v2 models EXACTLY as specified in AI-SPEC §4b — including `faithfulness_deepeval: float = Field(ge=-1.0, le=1.0)` (the -1 report-only sentinel), the GATED `citation_accuracy`/`recall_at_10` ranges, `per_ipo: dict[str, Aggregate] = {}`, and the `_named_judge` field_validator that raises when judge_model is empty. Update `eval/metrics/__init__.py` to export `recall_at_k`, `citation_accuracy`, and `EvalSummary`. This schema is the pinned UI contract (design doc §4.3, §5 testing) — both the runner (write-time validation) and the inline renderer (read-time re-check) depend on it; changing a field name is a breaking change the schema test must catch.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_eval_summary_schema.py -q && .venv/bin/python -c "from eval.metrics import recall_at_k, citation_accuracy, EvalSummary; print('exports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from eval.metrics import EvalSummary, recall_at_k, citation_accuracy` succeeds.
    - EvalSummary rejects an empty judge_model and out-of-range metric values; accepts the -1 faithfulness sentinel.
    - A design-doc §4.3 sample JSON validates and round-trips.
    - Tests failed before the schema existed (TDD RED->GREEN).
  </acceptance_criteria>
  <done>EvalSummary + Aggregate + Corpus are implemented per AI-SPEC §4b, exported from eval.metrics alongside the two metric functions, and pinned by a passing schema test that guards the UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency install → build | pip resolves langfuse/deepeval/ragas; an unpinned resolve can pull a breaking major (langfuse v4 OTEL rewrite) |
| eval_summary.json → UI/gate | the schema is the contract crossing from the (trusted) runner to the renderer + release gate; a malformed/under-validated artifact could surface a figure without provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-01 | Tampering | dep resolution | mitigate | Pin exact spike-validated ranges (`langfuse<3`, `deepeval>=4.1,<5`); no floating majors; no `langchain` added |
| T-6.1-02 | Information Disclosure | EvalSummary provenance | mitigate | `_named_judge` validator forbids an empty judge_model at write time (honesty invariant: no figure without a named judge) |
| T-6.1-03 | Spoofing | fabricated metric range | mitigate | Field(ge/le) bounds reject impossible scores; -1 is the only sub-zero (documented "not measured") sentinel |
| T-6.1-SC | Tampering | pip installs (deepeval, langfuse) | mitigate | Both packages are in the locked research STACK.md AND spike-validated live against real Gemini/Langfuse APIs (spikes 001/002); already installed in .venv at the exact pinned versions — validated, not [ASSUMED] |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_eval_metrics.py tests/unit/test_eval_summary_schema.py -q` passes.
- `from eval.metrics import recall_at_k, citation_accuracy, EvalSummary` imports with no agent/LLM/qdrant import pulled in.
- recall_at_k demonstrably slices over the full candidate list (k=5/10/30 differ when the match is past index 5).
</verification>

<success_criteria>
- eval/metrics is an importable package exposing the two deterministic pure functions + the EvalSummary schema.
- recall_at_k operates over the 50 retrieved candidates (measured decision #2 / spike 003), citation_accuracy over all chunks.
- EvalSummary enforces the honesty invariant (named judge) and metric ranges.
- langfuse<3 + deepeval pins committed to both dep files.
</success_criteria>

<output>
Create `.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-01-SUMMARY.md` when done.
</output>
