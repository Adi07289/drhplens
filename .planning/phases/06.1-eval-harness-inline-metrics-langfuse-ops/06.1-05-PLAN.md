---
phase: 06.1-eval-harness-inline-metrics-langfuse-ops
plan: 05
type: execute
wave: 4
depends_on: [06.1-01, 06.1-04]
files_modified:
  - scripts/release_gate.py
  - agent/policies.py
  - tests/eval/test_release_gate.py
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "release_gate.py hard-gates citation_accuracy >= 0.95 AND recall@10 >= 0.85, reading the committed eval_summary.json through the EvalSummary schema (shared eval/metrics implementation — report and gate never diverge)"
    - "a faithfulness_deepeval below 0.95 does NOT cause a non-zero exit — it is reported with a >=0.95 target line, not gated"
    - "the existing numeric_faithfulness >= 0.95 gate still fires, unchanged"
    - "recall@10 >= 0.85 is a labelled regression FLOOR ratified against the spike baseline (1.00), never surfaced as a headline quality metric (P10)"
  artifacts:
    - path: "scripts/release_gate.py"
      provides: "enforce_eval_gates() deterministic hard-gate on citation_accuracy + recall@10 (pure, offline-testable)"
      contains: "recall_at_10"
    - path: "agent/policies.py"
      provides: "CITATION_ACCURACY_GATE = 0.95 and RECALL_AT_10_GATE = 0.85 constants"
      contains: "RECALL_AT_10_GATE"
    - path: "tests/eval/test_release_gate.py"
      provides: "tests: below-threshold deterministic -> non-zero exit; low faithfulness -> no block; numeric gate still fires"
      contains: "recall"
  key_links:
    - from: "scripts/release_gate.py"
      to: "eval/reports/eval_summary.json"
      via: "EvalSummary.model_validate_json(...) then threshold checks on aggregate"
      pattern: "EvalSummary"
    - from: "enforce_eval_gates()"
      to: "agent.policies thresholds"
      via: "CITATION_ACCURACY_GATE / RECALL_AT_10_GATE (single source of truth)"
      pattern: "RECALL_AT_10_GATE"
---

<objective>
Extend `scripts/release_gate.py` with the deterministic eval hard-gates: block deploy when `citation_accuracy < 0.95` OR `recall@10 < 0.85`, while the DeepEval faithfulness stays REPORTED (a >=0.95 target line, never a blocking exit). The existing `numeric_faithfulness >= 0.95` gate is unchanged.

Delivers (measured decision #4; SPEC requirement 3; design doc §4.2; AI-SPEC §5):
- `agent/policies.py` — `CITATION_ACCURACY_GATE = 0.95` and `RECALL_AT_10_GATE = 0.85` constants (single source of truth, mirroring `NUMERIC_FAITHFULNESS_GATE`).
- `scripts/release_gate.py` — a pure `enforce_eval_gates(summary, report_dir)` that reads the committed `eval_summary.json` via the `EvalSummary` schema (06.1-01) and `sys.exit(1)` when either deterministic threshold is breached; faithfulness is written to the gate report with a target line but never gates; the numeric-faithfulness enforcement is preserved.
- `tests/eval/test_release_gate.py` — tests that a below-threshold deterministic metric exits non-zero, a low faithfulness does NOT, and the numeric gate still fires.

Purpose: SPEC requirement 3 / the gate posture (deterministic metrics hard-gate; the LLM-judge is reported). recall@10 >= 0.85 is a labelled regression FLOOR ratified against the spike's 1.00 baseline — set just below real performance, never self-blocking nor trivially low (SPEC constraint; spike 003).
Output: the two gate constants + the deterministic eval-gate enforcement + its offline tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-SPEC.md
@.planning/spikes/003-recall-baseline/README.md
@scripts/release_gate.py
@agent/policies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate constants + enforce_eval_gates() deterministic hard-gate (citation + recall@10), faithfulness reported</name>
  <files>agent/policies.py, scripts/release_gate.py</files>
  <read_first>
    - scripts/release_gate.py in full (the two-layer design: pure `enforce_gate(numeric_faithfulness, report_dir)` + `_write_gate_report` writer + `main()` running the live numeric track; MIRROR this exact posture for the new deterministic eval gate — a PURE enforcement function with no live-infra import so it is unit-testable on injected scores)
    - agent/policies.py L161-174 (the `NUMERIC_FAITHFULNESS_GATE: float = 0.95` constant + its docstring grammar — add the two new gate constants beside it with the same "enforced, not a tunable to relax" framing)
    - eval/metrics/schema.py (EvalSummary — parse eval_summary.json via `EvalSummary.model_validate_json(path.read_text())`)
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md §5 CI/CD (the gate exits non-zero if citation_accuracy < 0.95 OR recall@10 < 0.85 OR numeric_faithfulness < 0.95; a faithfulness_deepeval below 0.95 does NOT exit)
    - .planning/spikes/003-recall-baseline/README.md (recall@10 = 0.85 is a conservative FLOOR ratified against the measured 1.00 baseline; explicitly labelled, not a quality signal)
  </read_first>
  <action>
    Add `CITATION_ACCURACY_GATE: float = 0.95` and `RECALL_AT_10_GATE: float = 0.85` to `agent/policies.py` beside `NUMERIC_FAITHFULNESS_GATE`, each with a docstring: citation-accuracy is the CLAUDE.md hard-CI-failure invariant; recall@10 is a labelled REGRESSION FLOOR ratified against the spike-measured 1.00 baseline (measured decision #4 / spike 003) — enforced, never relaxed, never surfaced as a headline. In `scripts/release_gate.py`, add a PURE `enforce_eval_gates(summary: EvalSummary, report_dir: Path | None = None) -> None` (mirror `enforce_gate`): compute `passed = summary.aggregate.citation_accuracy >= CITATION_ACCURACY_GATE and summary.aggregate.recall_at_10 >= RECALL_AT_10_GATE`; write a dated `<date>-eval-gate.md` report (mirror `_write_gate_report`) that records citation/recall PASS/FAIL against their thresholds AND records `faithfulness_deepeval` with a ">= 0.95 target (REPORTED, not gated)" line — the faithfulness value NEVER affects `passed`. `sys.exit(1)` with a clear message when `not passed`; return None otherwise. Keep this function free of any live-infra / eval-runner import so it is unit-tested offline on injected EvalSummary values. Add a `main()` path (or extend the existing `main`) that reads the committed `eval/reports/eval_summary.json` via `EvalSummary.model_validate_json(...)`, calls `enforce_eval_gates(summary)`, AND still runs the existing live numeric-faithfulness `enforce_gate` (both gates fire; neither is removed). Do NOT alter the numeric gate logic.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from agent.policies import CITATION_ACCURACY_GATE, RECALL_AT_10_GATE; assert CITATION_ACCURACY_GATE==0.95 and RECALL_AT_10_GATE==0.85; import inspect, scripts.release_gate as g; src=inspect.getsource(g); assert 'enforce_eval_gates' in src and 'recall_at_10' in src and 'EvalSummary' in src; print('gate shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - agent.policies exposes CITATION_ACCURACY_GATE=0.95 and RECALL_AT_10_GATE=0.85.
    - enforce_eval_gates is pure (no live-infra import), reads an EvalSummary, and exits non-zero only on a deterministic breach.
    - faithfulness_deepeval is written to the report with a target line but never affects the exit code.
    - The existing numeric-faithfulness enforce_gate remains and still runs in main().
  </acceptance_criteria>
  <done>The release gate hard-blocks on citation_accuracy < 0.95 or recall@10 < 0.85 via a pure, offline-testable enforcer sharing the eval/metrics schema, keeps faithfulness reported-only, and preserves the numeric gate.</done>
</task>

<task type="auto">
  <name>Task 2: Gate tests — deterministic breach exits non-zero, low faithfulness does not, numeric gate still fires</name>
  <files>tests/eval/test_release_gate.py</files>
  <read_first>
    - tests/eval/test_release_gate.py (the existing offline gate tests driving enforce_gate at 0.94 fail / 0.95 / 0.96 pass with a tmp report dir + `pytest.raises(SystemExit)` — reuse this exact pattern for the new enforcer)
    - scripts/release_gate.py (enforce_eval_gates from Task 1)
    - eval/metrics/schema.py (build EvalSummary fixtures directly — no live services)
  </read_first>
  <action>
    Extend `tests/eval/test_release_gate.py`. Build EvalSummary fixtures in-memory (a valid judge_model, corpus, and varying aggregate values) and assert against `enforce_eval_gates(summary, report_dir=tmp_path)`: (1) citation_accuracy=0.94 (< 0.95) -> `pytest.raises(SystemExit)` with a non-zero code; (2) recall_at_10=0.80 (< 0.85) -> SystemExit; (3) citation_accuracy=0.96 AND recall_at_10=0.90 with faithfulness_deepeval=0.10 (well below target) -> NO SystemExit (faithfulness is reported, not gated — the key acceptance: a low faithfulness must NOT block); (4) a dated eval-gate report is written on both branches (auditable pass + fail); (5) faithfulness=-1 (not measured) still does not block. Keep the existing numeric-gate tests untouched (they prove that gate still fires). All offline — inject scores, tmp report dir, no live call.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/eval/test_release_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A citation_accuracy < 0.95 fixture raises SystemExit; a recall@10 < 0.85 fixture raises SystemExit.
    - A fixture with passing deterministic metrics but faithfulness 0.10 (or -1) does NOT raise.
    - Both branches write an auditable eval-gate report.
    - The pre-existing numeric-gate tests still pass.
  </acceptance_criteria>
  <done>Offline tests prove the deterministic eval gate blocks on citation/recall breaches, never on a low or unmeasured faithfulness, and the numeric gate is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval_summary.json → release decision | the committed artifact gates deploy; a divergent re-implementation or a gated LLM-judge could either wave through a regression or flake the build |
| threshold definition → enforcement | the gate thresholds must live in one place (policies) and be enforced, not silently relaxed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-15 | Tampering | report/gate divergence | mitigate | The gate reads the runner's committed eval_summary.json via the shared EvalSummary schema; no independent metric re-computation |
| T-6.1-16 | Denial of Service | flaky non-deterministic gate | mitigate | Faithfulness is reported-only; only the deterministic citation/recall (and the existing numeric) gate the exit code |
| T-6.1-17 | Elevation of Privilege | silently lowering a gate | mitigate | Thresholds are policy constants with "enforced, not a tunable" docstrings; a below-floor score physically exits non-zero (tested) |
| T-6.1-18 | Spoofing | recall floor sold as a quality headline | mitigate | recall@10 gate docstring labels it a regression FLOOR ratified against the 1.00 baseline (P10); interpretation lives in the runner report (06.1-04) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/eval/test_release_gate.py -q` passes.
- `enforce_eval_gates` exits non-zero on a citation/recall breach and never on a low/unmeasured faithfulness.
- The two new gate constants resolve from agent.policies.
- Human-check (batched end-of-phase): confirm `make release` (or the gate main) runs both the deterministic eval gate and the live numeric gate against the committed eval_summary.json.
</verification>

<success_criteria>
- Deterministic hard gates on citation_accuracy >= 0.95 and recall@10 >= 0.85; faithfulness reported-only; numeric gate unchanged.
- One shared schema/implementation between runner and gate.
- recall@10 labelled a regression floor, not a headline (P10).
</success_criteria>

<output>
Create `.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-05-SUMMARY.md` when done.
</output>
