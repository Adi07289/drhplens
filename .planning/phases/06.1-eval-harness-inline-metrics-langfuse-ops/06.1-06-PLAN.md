---
phase: 06.1-eval-harness-inline-metrics-langfuse-ops
plan: 06
type: execute
wave: 4
depends_on: [06.1-01, 06.1-04]
files_modified:
  - ui/eval_inline.py
  - app/static/drhplens.css
  - pages/02_snapshot.py
  - pages/01_methodology.py
  - tests/unit/test_eval_inline.py
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "every IPO snapshot page renders an honest system-level eval figure read from the committed eval_summary.json, with full provenance (n, IPO, date, judge model) + a report link"
    - "a per-IPO line renders ONLY when per_ipo[this_ipo_id] exists — never a fabricated per-IPO number"
    - "eval figures are plain IBM-Plex-Mono numbers — no red/green, no badges, no severity icons, no verdict UX; a high and a low score render byte-identically except the digits"
    - "the render module reads ONLY eval_summary.json (.is_file() + try/except) — no LLM/agent/judge import, no live call at render (P19), pinned by an inspect.getsource isolation test"
    - "the /methodology eval placeholder rows show live values via the neutral .drhp-eval-live class (text-primary mono), NOT the amber .m-status"
  artifacts:
    - path: "ui/eval_inline.py"
      provides: "render_eval_inline(drhp_id) — read-only honest eval figure"
      contains: "def render_eval_inline"
    - path: "app/static/drhplens.css"
      provides: "the new .drhp-eval-* classes (inline/figure/report-link/per-ipo/empty/live)"
      contains: ".drhp-eval-live"
    - path: "pages/02_snapshot.py"
      provides: "render_eval_inline wired above the Q&A divider, outside the record guard"
      contains: "render_eval_inline"
    - path: "pages/01_methodology.py"
      provides: "the three eval rows filled with live values via .drhp-eval-live"
      contains: "drhp-eval-live"
    - path: "tests/unit/test_eval_inline.py"
      provides: "isolation + honesty-invariant tests (no-LLM-import, value-invariant styling, no fabricated per-IPO, graceful absence)"
      contains: "inspect.getsource"
  key_links:
    - from: "ui/eval_inline.py"
      to: "eval/reports/eval_summary.json"
      via: ".is_file() guard + try/except json load (the card_data.json pattern)"
      pattern: "eval_summary.json"
    - from: "pages/02_snapshot.py"
      to: "render_eval_inline(drhp_id)"
      via: "call directly above the Q&A divider, outside `record is not None`"
      pattern: "render_eval_inline"
---

<objective>
Surface the honest inline eval figure on every IPO page (EVAL-02) — read from the committed `eval_summary.json`, plain IBM-Plex-Mono, provenance-bearing, no verdict UX — and fill the `/methodology` placeholder rows with the live numbers. Zero live LLM-judge calls at render (P19).

Delivers (measured decision #6; SPEC requirement 2; the 06.1-UI-SPEC contract; design doc §4.3):
- `ui/eval_inline.py::render_eval_inline(drhp_id)` — reads `eval/reports/eval_summary.json` once via `.is_file()` + try/except (the `card_data.json` pattern), renders the system-level figure and a `view report` link, plus a per-IPO sub-line ONLY when `per_ipo[drhp_id]` exists. Missing values render `—` (em dash), never `0.00`. Missing JSON renders the muted fallback line.
- The new `.drhp-eval-*` CSS classes in `app/static/drhplens.css` (inherited tokens only).
- Wiring into `pages/02_snapshot.py` directly above the Q&A divider, OUTSIDE the `record is not None` guard (like the GMP block).
- Filling the three `pages/01_methodology.py` eval rows with live values via the neutral `.drhp-eval-live` class (NOT the amber `.m-status`).

Purpose: EVAL-02 (SPEC requirement 2) + the honesty invariant (SEBI/compliance) — every figure carries provenance, no red/green/badge, never a per-IPO number without a real gold set.
Output: the render module + CSS + both page wirings + isolation/honesty tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-UI-SPEC.md
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-SPEC.md
@pages/01_methodology.py
@pages/02_snapshot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ui/eval_inline.py honest read-only render module + .drhp-eval-* CSS + isolation/honesty tests</name>
  <files>ui/eval_inline.py, app/static/drhplens.css, tests/unit/test_eval_inline.py</files>
  <read_first>
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-UI-SPEC.md (the FULL contract: Copywriting Contract exact strings; Component Contract Surface 1 read pattern; the `_mc_num`-style 2dp formatter with em-dash for NaN; single muted `<p>` with an inline `<a>` link, never a split `<div>`; the New CSS classes table; the Empty/Missing-Data States; the 9 Honesty-Invariant Acceptance Checks)
    - pages/01_methodology.py lines 116-127 (the `_card_data_path.is_file()` read guard + the `_mc_num(value, nd)` em-dash formatter to mirror)
    - tests/unit/test_methodology_pane.py lines 211-223 (the `test_no_llm_or_qdrant_import` inspect.getsource forbidden-token isolation pattern to replicate; add deepeval/GeminiModel/faithfulness_deepeval to the forbidden set)
    - eval/metrics/schema.py (EvalSummary — the keys the render reads: generated, judge_model, corpus.n_questions, corpus.ipo, aggregate.faithfulness_deepeval/citation_accuracy/recall_at_10, per_ipo, report)
    - app/static/drhplens.css (the :root tokens --drhp-font-mono / --drhp-text-primary #ECECEA / --drhp-text-muted #9A9BA0 / --drhp-accent-link #E6AC5C; the `.m-status` amber near line 1691 — the class NOT to use for live eval values; `.drhp-not-disclosed` muted grammar to alias for the empty state)
  </read_first>
  <action>
    Implement `ui/eval_inline.py::render_eval_inline(drhp_id: str) -> None`. Resolve `eval/reports/eval_summary.json`; guard with `.is_file()` then a try/except around `json.loads` (mirror the `_card_data_path` posture) — on absent/unreadable JSON render the single muted fallback line "Eval metrics not yet generated for this release." (class `.drhp-eval-empty`) and return, never crash. Add an `_eval_num(value, nd=2)` helper: 2 decimals; return the em dash for None/NaN/non-numeric — NEVER "0.00". Render the system-level figure as a single muted `<p class="drhp-eval-inline">` using the EXACT copy in the UI-SPEC Copywriting Contract ("Measured across our {corpus.n_questions}-question eval set ({corpus.ipo}, {generated}, judge {judge_model}): faithfulness … · recall@10 … · citation …"), each numeric value wrapped in a `<span class="drhp-eval-figure">` (identical mono styling for every value — a 0.31 and a 0.99 differ only in digits), followed by an inline `<a class="drhp-eval-report-link">view report</a>` whose href is the `report` path; if `report` is absent, OMIT the link (never a dead link). Render a per-IPO sub-line `<p class="drhp-eval-per-ipo">` ONLY when `drhp_id in per_ipo`; otherwise emit nothing extra (never a fabricated per-IPO number). The faithfulness clause always names its judge model (labelled judge-based, reported not gated). HTML-escape every interpolated string. Import ONLY json + pathlib + streamlit — NO agent/model/LLM/deepeval import, NO live call. Add the classes to `app/static/drhplens.css` per the UI-SPEC table, inherited `--drhp-*` tokens only: `.drhp-eval-inline`, `.drhp-eval-figure` (mono, tabular-nums, text-primary, weight 400 — identical for every value), `.drhp-eval-per-ipo`, `.drhp-eval-report-link` (accent-link underlined — the ONE accent element), `.drhp-eval-empty` (reuse `.drhp-not-disclosed` grammar), and `.drhp-eval-live` (mono, tabular-nums, text-primary, weight 400 — Surface 2, NOT amber). Emit NO red/green/success/danger/badge/severity class, NO icon, NO arrow. Write `tests/unit/test_eval_inline.py` covering the 9 honesty checks: (a) inspect.getsource forbids openai/genai/instructor/groq/qdrant/deepeval/GeminiModel/GRAPH.invoke; (b) value-invariant styling — a 0.99 vs 0.31 summary yields markup differing only in digits (same classes); (c) no `This IPO's eval` substring for a drhp_id absent from per_ipo; (d) provenance present (n, ipo, date, judge_model); (e) a missing metric renders the em dash, never "0.00"; (f) a missing report key omits the link; (g) absent JSON yields the empty fallback and no crash; (h) no red/green/badge/severity token in the emitted markup; (i) the judge model is named in the faithfulness clause.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_eval_inline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - render_eval_inline reads only eval_summary.json (.is_file() + try/except) and imports no LLM/agent/deepeval module (inspect.getsource isolation test passes).
    - System-level figure carries n, ipo, date, judge_model + a report link; a low and a high figure render byte-identically except the digits.
    - No per-IPO line for an IPO absent from per_ipo; missing values render the em dash, never 0.00.
    - No red/green/badge/severity/icon on any eval figure; accent used only on the report link.
    - The six `.drhp-eval-*` classes exist in drhplens.css using inherited tokens only.
  </acceptance_criteria>
  <done>A read-only, isolation-pinned render module and its neutral CSS render the honest provenance-bearing eval figure with no verdict UX and no fabricated per-IPO number.</done>
</task>

<task type="auto">
  <name>Task 2: Wire render_eval_inline into pages/02_snapshot.py — above the Q&A divider, outside the record guard</name>
  <files>pages/02_snapshot.py</files>
  <read_first>
    - pages/02_snapshot.py lines 399-412 (the GMP block `_render_gmp_block(gmp_record, gmp_state)` at ~404 is OUTSIDE the `record is not None` guard, then the 2xl divider markdown at ~407, then `render_snapshot_chat(drhp_id)` at ~411 — the exact insertion neighborhood)
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-UI-SPEC.md "Component Contract — Surface 1" (WHERE: rendered directly above the Q&A divider, immediately before render_snapshot_chat, OUTSIDE the record guard — like GMP — so the honest figure appears on every IPO page even while the snapshot is still precomputing)
    - pages/02_snapshot.py imports block (lines 48-66) for the import style to add `from ui.eval_inline import render_eval_inline`
  </read_first>
  <action>
    Import `render_eval_inline` from `ui.eval_inline` in the page's import block. Call `render_eval_inline(drhp_id)` directly ABOVE the Q&A divider markdown and immediately before `render_snapshot_chat(drhp_id)`, placed OUTSIDE the `record is not None` guard (alongside the GMP block) so the figure renders on every IPO page even mid-precompute (UI-SPEC rationale: the eval figures describe the reliability of the very Q&A the user is about to use). Do not move or alter the GMP block or the divider; only insert the one call. `drhp_id` is already allow-list-validated upstream — pass it through unchanged.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import inspect, importlib.util as u; import pathlib; src=pathlib.Path('pages/02_snapshot.py').read_text(); assert 'from ui.eval_inline import render_eval_inline' in src and 'render_eval_inline(drhp_id)' in src; i=src.index('render_eval_inline(drhp_id)'); j=src.index('render_snapshot_chat(drhp_id)'); assert i < j, 'eval figure must render before the Q&A chat'; print('wired above Q&A OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `render_eval_inline(drhp_id)` is called outside the `record is not None` guard and before `render_snapshot_chat(drhp_id)`.
    - The GMP block and the Q&A divider are unchanged.
    - The page still renders for a known drhp_id without error.
  </acceptance_criteria>
  <done>The honest eval figure renders on every IPO snapshot page directly above the Q&A, independent of the snapshot record's readiness.</done>
</task>

<task type="auto">
  <name>Task 3: Fill the /methodology eval placeholder rows with live values via .drhp-eval-live</name>
  <files>pages/01_methodology.py</files>
  <read_first>
    - pages/01_methodology.py lines 78-103 (the `_ROWS` eval-metrics table + the `.m-name`/`.m-how`/`.m-target`/`.m-status` cell structure; the three eval rows currently read "Phase 6")
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-UI-SPEC.md "Component Contract — Surface 2" (the exact row mapping: Faithfulness → `aggregate.faithfulness_deepeval`, how = "DeepEval · LLM-judge · answer grounded in retrieved context", target `≥ 0.95` reported; Citation accuracy → `aggregate.citation_accuracy`; Context recall@k → `recall_at_5 / recall_at_10 / recall_at_30` ` / `-joined; the two NON-eval rows Extraction F1 → "Phase 3" and Forecast coverage → "Phase 5" keep their amber `.m-status`) + the required Surface-2 honesty deviation: live eval values use the neutral `.drhp-eval-live`, NOT the amber `.m-status`
    - pages/01_methodology.py lines 116-127 + 162-169 (the `_card_data_path.is_file()` + try/except read guard + `_mc_num` formatter to reuse for reading eval_summary.json)
  </read_first>
  <action>
    Read `eval/reports/eval_summary.json` at the top of the methodology render using the SAME `.is_file()` + try/except guard already used for `card_data.json` (reuse `_mc_num` for the 2dp em-dash formatting). Replace the three eval rows' `reported · Phase 6` status cells with the LIVE values, wrapping each in `<span class="drhp-eval-live">…</span>` (text-primary mono per the UI-SPEC Surface-2 honesty deviation — NEVER the amber `.m-status`, which would read as a verdict on a live number). Update the `m-how`/`m-target` text per the UI-SPEC row mapping (Faithfulness engine → "DeepEval · LLM-judge …"; Context recall@k → "custom span-overlap · k = 5 / 10 / 30", value = the three recalls joined by " / "). Keep the Extraction F1 ("Phase 3") and Forecast coverage ("Phase 5") rows and their amber `.m-status` UNCHANGED. If `eval_summary.json` is absent or unreadable, the three eval rows fall back to their current "reported"/"Phase 6" text (never a fabricated number) — no crash.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib; src=pathlib.Path('pages/01_methodology.py').read_text(); assert 'eval_summary.json' in src and 'drhp-eval-live' in src and 'is_file()' in src; assert 'Phase 3' in src and 'Phase 5' in src, 'non-eval rows must keep their phase labels'; print('methodology fill OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The three eval rows show live values from eval_summary.json wrapped in `.drhp-eval-live` (not `.m-status`).
    - The Extraction F1 / Forecast coverage rows keep their amber `.m-status` "Phase 3"/"Phase 5" labels.
    - With eval_summary.json absent, the eval rows fall back to their current text and the page does not crash.
  </acceptance_criteria>
  <done>The /methodology eval rows show live numbers in neutral mono via .drhp-eval-live, the non-eval rows are untouched, and absence degrades gracefully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed eval_summary.json → rendered page | an untrusted-at-render artifact crosses into user-visible HTML; unescaped values or a live judge call would breach XSS / P19 demo-safety |
| eval figure → the retail user | the honesty invariant (SEBI/compliance) governs what a number may claim — no verdict styling, no per-IPO number without a real gold set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-19 | Tampering | XSS via card values | mitigate | HTML-escape every interpolated eval_summary.json string before markup interpolation (mirror the methodology card escaping) |
| T-6.1-20 | Denial of Service | live judge call at render (P19) | mitigate | Read-only JSON load; no agent/deepeval import; inspect.getsource isolation test pins it |
| T-6.1-21 | Spoofing | verdict-styled / fabricated figure | mitigate | Plain mono, value-invariant styling; accent only on the link; no per-IPO number unless per_ipo[id] exists — all test-asserted (the 9 honesty checks) |
| T-6.1-22 | Information Disclosure | figure without provenance | mitigate | Provenance (n, ipo, date, judge_model) mandatory on the system-level line; the empty-state fallback shows no number |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_eval_inline.py -q` passes (the 9 honesty-invariant checks).
- `ui/eval_inline.py` imports no LLM/agent/deepeval module (isolation test).
- The eval figure renders above the Q&A on the snapshot page; the /methodology eval rows show live values in `.drhp-eval-live`; the non-eval rows keep amber `.m-status`.
- Human-check (batched end-of-phase, UI visual): load a snapshot page and /methodology with a committed eval_summary.json present and confirm plain mono figures, provenance + report link, no red/green/badge, and no per-IPO line for an IPO without a gold set; confirm the empty-state fallback when the JSON is removed.
</verification>

<success_criteria>
- Honest system-level eval figure on every IPO page with provenance + report link, no verdict UX.
- Per-IPO line only where a real gold set exists; missing values render the em dash.
- /methodology placeholder rows filled with live values via the neutral `.drhp-eval-live`.
- Zero live judge calls at render (P19), pinned by the isolation test.
</success_criteria>

<output>
Create `.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-06-SUMMARY.md` when done.
</output>
