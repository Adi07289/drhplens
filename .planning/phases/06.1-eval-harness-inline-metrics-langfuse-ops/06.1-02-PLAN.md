---
phase: 06.1-eval-harness-inline-metrics-langfuse-ops
plan: 02
type: execute
wave: 2
depends_on: [06.1-01]
files_modified:
  - eval/metrics/faithfulness_deepeval.py
  - eval/metrics/__init__.py
  - tests/eval/test_faithfulness_deepeval.py
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "faithfulness_deepeval(question, answer, contexts) returns (score, reason) via DeepEval FaithfulnessMetric.measure() on a native Gemini judge — it REPORTS and never raises on a low score"
    - "the judge model is gemini-3.5-flash (NOT gemini-2.5-flash, which 404s), backed by deepeval.models.GeminiModel with GEMINI_API_KEY only (no OpenAI dependency)"
    - "infra failures (missing key / rate-limit exhaustion) return the -1 'not measured' sentinel — never a misleading 0.0"
    - "the assert_test lane is opt-in (needs GEMINI_API_KEY), serial (async_mode=False), 429-backoff wrapped, and NEVER gates the deterministic release path"
  artifacts:
    - path: "eval/metrics/faithfulness_deepeval.py"
      provides: "faithfulness_deepeval() report-not-raise wrapper + cached GeminiModel judge"
      contains: "def faithfulness_deepeval"
    - path: "tests/eval/test_faithfulness_deepeval.py"
      provides: "opt-in assert_test faithfulness lane + a no-key -1-sentinel unit test"
      contains: "gemini-3.5-flash"
  key_links:
    - from: "eval/metrics/faithfulness_deepeval.py"
      to: "Gemini free tier (5 RPM)"
      via: "async_mode=False + tenacity 429 backoff + -1 sentinel on exhaustion"
      pattern: "async_mode=False"
    - from: "faithfulness_deepeval()"
      to: "the reported (never gated) posture"
      via: "metric.measure() records score+reason without raising"
      pattern: "\\.measure\\("
---

<objective>
Add the DeepEval LLM-judge faithfulness metric — REPORTED, never gated — as the third `eval/metrics/` member. This is the "measure-not-assert" path that produces `faithfulness_deepeval` for `eval_summary.json`, plus the opt-in `deepeval test run` assert_test lane the ROADMAP's DeepEval-CI research flag calls for.

Delivers (measured decisions #1/#2/#3; spike 002; AI-SPEC §4/§8):
- `eval/metrics/faithfulness_deepeval.py` — `faithfulness_deepeval(question, answer, contexts) -> (score, reason)` on a native `deepeval.models.GeminiModel` judge, model `gemini-3.5-flash` (spike 002 corrected the stale `gemini-2.5-flash`), `async_mode=False`, `temperature=0.0`, wrapped in a tenacity 429-backoff, returning the `-1` "not measured" sentinel on infra failure.
- `tests/eval/test_faithfulness_deepeval.py` — the opt-in, non-blocking `assert_test` lane (skips without `GEMINI_API_KEY`) + an always-on unit test proving the `-1` sentinel path when the key is unset.

Purpose: EVAL-01's faithfulness dimension (SPEC requirement 1). Faithfulness is REPORTED because the LLM-judge is non-deterministic run-to-run and free-tier Gemini is 5 RPM — hard-gating it is itself a critical failure mode (AI-SPEC §5, spikes 002/003). The deterministic metrics (06.1-01) carry the gate.
Output: the faithfulness wrapper + its opt-in test lane; `eval.metrics` export updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md
@.planning/spikes/002-deepeval-gemini-judge/README.md
@.planning/spikes/CONVENTIONS.md
@eval/metrics/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeepEval faithfulness wrapper — reported (never raises), gemini-3.5-flash judge, -1 sentinel, 429 backoff</name>
  <files>eval/metrics/faithfulness_deepeval.py, eval/metrics/__init__.py</files>
  <read_first>
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md §4 (the reference `faithfulness_deepeval` implementation: `@lru_cache` `_judge()` returning GeminiModel, `FaithfulnessMetric(threshold=0.95, model=_judge(), include_reason=True, async_mode=False)`, `metric.measure(tc)` then `return float(metric.score), (metric.reason or "")`) — and §8 item 1 (judge model correction) + item 3 (5 RPM → async_mode=False + tenacity + cache) which SUPERSEDE §4's model id
    - .planning/spikes/002-deepeval-gemini-judge/README.md (measured: gemini-2.5-flash 404s "no longer available to new users"; gemini-3.5-flash works — faithful=1.00, hallucinated=0.50 with correct reason; GeminiModel needs only GEMINI_API_KEY, OpenAI unset; 429 RESOURCE_EXHAUSTED at 5 RPM)
    - scripts/run_eval.py lines 102-118 (the existing `_ragas_faithfulness` -1 sentinel pattern to mirror: "-1 signals 'not measured' vs 0.0 which means 'failed'")
    - requirements.txt (tenacity is already a dependency, L40)
  </read_first>
  <action>
    Implement `eval/metrics/faithfulness_deepeval.py`. Provide a module-cached judge `_judge()` (`@lru_cache(maxsize=1)`) returning `deepeval.models.GeminiModel(model="gemini-3.5-flash", api_key=os.environ["GEMINI_API_KEY"], temperature=0.0)` — model id `gemini-3.5-flash` per spike 002 / AI-SPEC §8 item 1 (NOT `gemini-2.5-flash`, which 404s; note the deviation from AI-SPEC §4's stale literal in a comment). Implement `faithfulness_deepeval(question: str, answer: str, contexts: list[str]) -> tuple[float, str]`: build `LLMTestCase(input=question, actual_output=answer, retrieval_context=contexts)`, construct `FaithfulnessMetric(threshold=0.95, model=_judge(), include_reason=True, async_mode=False)` (threshold is a REPORTED target line, never asserted), call `metric.measure(tc)` (measure != assert_test — records, does not raise on a low score), and return `(float(metric.score), metric.reason or "")`. Wrap the `.measure()` call in a tenacity retry on transient 429/5xx (retry ~30-65s per spike 002's 5-RPM window, a few attempts); if it still fails OR `GEMINI_API_KEY` is unset, return `(-1.0, "<reason: not measured — {cause}>")` — the -1 "not measured" sentinel (mirror the `_ragas_faithfulness` sentinel), NEVER a misleading 0.0 (measured decision #2). Keep `retrieval_context` byte-identical to what the caller passes (the top-5 reranked chunks the agent actually saw — P18; do NOT expand or summarize). Add `faithfulness_deepeval` to `eval/metrics/__init__.py` exports. Never import OpenAI; the judge is Gemini-only.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import os; os.environ.pop('GEMINI_API_KEY', None); from eval.metrics.faithfulness_deepeval import faithfulness_deepeval; s,r=faithfulness_deepeval('q','a',['ctx']); assert s==-1.0, s; print('sentinel OK', r[:40])" && grep -q 'gemini-3.5-flash' eval/metrics/faithfulness_deepeval.py && ! grep -q 'gemini-2.5-flash' eval/metrics/faithfulness_deepeval.py && ! grep -qi 'openai' eval/metrics/faithfulness_deepeval.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - With GEMINI_API_KEY unset, `faithfulness_deepeval(...)` returns `(-1.0, <reason>)` and does NOT raise.
    - The judge model literal is `gemini-3.5-flash`; `gemini-2.5-flash` appears nowhere; no OpenAI import.
    - `async_mode=False` and a tenacity 429 retry are present.
    - `faithfulness_deepeval` is exported from `eval.metrics`.
  </acceptance_criteria>
  <done>The faithfulness wrapper measures (never asserts) on a gemini-3.5-flash judge, returns -1 on any infra failure, is 429-hardened + serial for the 5-RPM free tier, and is exported from the metrics package.</done>
</task>

<task type="auto">
  <name>Task 2: Opt-in assert_test faithfulness lane + always-on sentinel unit test</name>
  <files>tests/eval/test_faithfulness_deepeval.py</files>
  <read_first>
    - .planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-AI-SPEC.md §5 "CI/CD Integration" (the two-lane split: `deepeval test run tests/eval/test_faithfulness_deepeval.py -c -i -n 1`; assert_test RAISES below threshold and is reserved for THIS opt-in lane, never gates deploy) + §3 Key Abstractions (`assert_test(test_case, [metric])`)
    - tests/integration/test_langfuse_trace.py lines 20-40 (the `pytest.mark.skipif` opt-in pattern gating on an env var — mirror it for GEMINI_API_KEY)
    - .planning/spikes/002-deepeval-gemini-judge/README.md (the exact GeminiModel + FaithfulnessMetric + LLMTestCase runnable snippet + the faithful=1.00 / hallucinated=0.50 fixtures)
  </read_first>
  <action>
    Write `tests/eval/test_faithfulness_deepeval.py` with two parts. (a) ALWAYS-ON unit test: with `GEMINI_API_KEY` monkey-removed, assert `faithfulness_deepeval(...)` returns `-1.0` and does not raise (the report-not-crash guarantee; this runs in default CI with no key). (b) OPT-IN assert_test lane guarded by `pytest.mark.skipif(not os.environ.get("GEMINI_API_KEY"))` (mirror the Langfuse test's skip marker): a `LLMTestCase` built from spike 002's faithful fixture (revenue/net-loss context + a faithful answer) and `assert_test(tc, [FaithfulnessMetric(threshold=0.95, model=GeminiModel("gemini-3.5-flash", ...), include_reason=True, async_mode=False)])`. Document at the top that this lane is invoked via `deepeval test run tests/eval/test_faithfulness_deepeval.py -c -i -n 1` (-c cache, -i ignore malformed judge JSON, -n 1 serial for the 5-RPM ceiling) and that it is NON-BLOCKING — it never participates in `scripts/release_gate.py`'s deterministic exit code. Keep the case count to 1-2 (free-tier RPM).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/eval/test_faithfulness_deepeval.py -q -k "sentinel or noop or not_measured" && grep -q 'skipif' tests/eval/test_faithfulness_deepeval.py && grep -q 'assert_test' tests/eval/test_faithfulness_deepeval.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The always-on sentinel test passes with no GEMINI_API_KEY (default CI safe).
    - The assert_test case is skipped (not failed) when GEMINI_API_KEY is absent.
    - The file documents the `deepeval test run ... -c -i -n 1` opt-in invocation and its non-blocking status.
  </acceptance_criteria>
  <done>The faithfulness test file carries an always-on -1-sentinel unit test plus an opt-in, key-gated, non-blocking assert_test lane that never gates the release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → Gemini judge API | GEMINI_API_KEY leaves the process to the judge; a leak or an OpenAI fallback would breach the free-tier/no-OpenAI posture |
| non-deterministic judge → committed artifact | an LLM-judge score entering eval_summary.json must be labelled reported-not-gated and carry a real "not measured" sentinel, never a fabricated 0.0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-04 | Information Disclosure | judge API key | mitigate | Key read only from env at call time; no key echoed to logs/reports; no OpenAI client acquired (grep-gated) |
| T-6.1-05 | Repudiation | conflating "failed" with "not measured" | mitigate | Infra failure returns the -1 sentinel + a reason string; 0.0 is reserved for a real measured zero |
| T-6.1-06 | Denial of Service | free-tier 5-RPM exhaustion flaking CI | mitigate | Faithfulness is REPORTED (never gates); async_mode=False + tenacity backoff + opt-in `-c -i -n 1` lane keep it out of the blocking path |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/eval/test_faithfulness_deepeval.py -q -k "sentinel or noop or not_measured"` passes with no key set.
- `from eval.metrics import faithfulness_deepeval` works.
- Human-check (batched end-of-phase): with GEMINI_API_KEY set, `deepeval test run tests/eval/test_faithfulness_deepeval.py -c -i -n 1` measures the faithful fixture at/near 1.00 and the assert_test lane runs opt-in without touching the release gate.
</verification>

<success_criteria>
- faithfulness_deepeval reports (never raises) on a gemini-3.5-flash judge with a -1 not-measured sentinel.
- The assert_test lane is opt-in, serial, cache-friendly, and non-blocking.
- No OpenAI dependency; free-tier-safe.
</success_criteria>

<output>
Create `.planning/phases/06.1-eval-harness-inline-metrics-langfuse-ops/06.1-02-SUMMARY.md` when done.
</output>
