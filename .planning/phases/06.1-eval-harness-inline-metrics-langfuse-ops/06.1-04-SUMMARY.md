---
phase: 06.1-eval-harness-inline-metrics-langfuse-ops
plan: 04
subsystem: testing
tags: [deepeval, faithfulness, recall, citation-accuracy, eval-harness, langfuse, pydantic, gemini]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: the eval.metrics package (recall_at_k, citation_accuracy)
  - phase: 06.1-02
    provides: the pinned EvalSummary/Aggregate/Corpus schema + faithfulness_deepeval judge wrapper
  - phase: 06.1-03
    provides: app/observability trace enrichment + flush() + the judge_flag failure mode
provides:
  - "run_rag_eval() — the first-class committed runner emitting eval_summary.json + a dated rag-eval.md in one pass"
  - "committed eval/reports/eval_summary.json (REAL recall@5/10/30=1.0 + citation=1.0; faithfulness_deepeval=-1 not-measured)"
  - "committed eval/reports/2026-07-28-rag-eval.md with per-metric P10 interpretation + the recall-saturation floor caveat"
  - "an offline fixture-driven runner test pinning the eval_summary.json UI/gate contract"
affects: [06.1-05-release-gate, 06.1-06-inline-surface, gold-set-tightening-followup]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "One metric implementation, two consumers: the runner imports recall_at_k/citation_accuracy/faithfulness_deepeval from eval.metrics — the same functions release_gate.py imports"
    - "Dependency-injected invoke_fn makes the live-services runner fully offline-testable"
    - "--with-faithfulness opt-in flag separates the $0 deterministic (no-Gemini) default from the judge pass"

key-files:
  created:
    - "tests/eval/test_run_rag_eval.py"
    - "eval/reports/eval_summary.json"
    - "eval/reports/2026-07-28-rag-eval.md"
  modified:
    - "scripts/run_eval.py"

key-decisions:
  - "Default run is deterministic-only via retrieve+rerank (NO Gemini generate) so recall/citation are REAL at $0; faithfulness committed as the -1 not-measured sentinel (P10 honesty guard + exhausted free-tier daily quota)"
  - "Deleted the private _recall_at_k/_citation_accuracy from run_eval.py and re-pointed the legacy run_eval at eval.metrics — one shared impl (06.1-01 deferred this to 06.1-04)"
  - "run_rag_eval is the default __main__ path (per AI-SPEC §5 CI lane); --legacy runs the old Phase 1 baseline, --numeric the numeric track"
  - "per_ipo carries only Swiggy (swiggy_2024_11) — never a fabricated per-IPO figure (T-6.1-12)"

patterns-established:
  - "P10 interpretation-per-metric report section is mandatory; recall is labelled a regression floor, not a headline"
  - "Eval-time judge_flag is wired via the direct Langfuse client, inert until faithfulness is measured"

requirements-completed: [EVAL-01]

# Metrics
duration: ~30min
completed: 2026-07-28
---

# Phase 6.1 Plan 04: RAG Eval Runner Summary

**`run_rag_eval()` emits a schema-valid `eval_summary.json` + a dated `rag-eval.md` in one pass over the Swiggy gold set — REAL recall@5/10/30 and citation over the retrieval path, faithfulness held at the -1 not-measured sentinel, importing the ONE eval.metrics implementation so the report and the release gate can never diverge.**

## Performance

- **Duration:** ~30 min
- **Tasks:** 2
- **Files modified:** 1 modified, 3 created

## Accomplishments
- `run_rag_eval()` computes recall over `final_state["retrieved_chunks"]` (the 50 Qdrant candidates) at k=5/10/30 and citation over the reranked window, then assembles an `EvalSummary` and writes `eval_summary.json` (schema-validated at write time) + a dated `.md`.
- Deleted the private `_recall_at_k`/`_citation_accuracy` from `run_eval.py` and re-pointed the legacy `run_eval` at `eval.metrics` — the single implementation `scripts/release_gate.py` also imports.
- Committed a REAL deterministic artifact today: recall@5/10/30 = 1.000, citation = 1.000 across 11 grounded questions, faithfulness_deepeval = -1 (not measured).
- The `.md` carries a per-metric **Interpretation (P10)** section: faithfulness labelled judge-based + calibration-pending, citation the finer-grained real signal, and recall the **saturated regression floor** (1.00 because gold `expected_sources` are coarse 10–101-page ranges, mean 44.5 — spike 003) with the gold-set-tightening follow-up flagged.
- Wired the eval-time `judge_flag` failure mode (inert until a real faithfulness < threshold) and a `flush()` at the end so enriched Langfuse traces ship.
- Offline runner test proves the JSON validates against `EvalSummary`, `per_ipo` is Swiggy-only, the recall-floor caveat is present, and recall is computed off `retrieved_chunks` (50), not the reranked 5.

## Task Commits

1. **Task 1: run_rag_eval() — compute metrics via eval.metrics, emit eval_summary.json + rag-eval.md** - `08c16a3` (feat)
2. **Task 2: fixture-driven offline runner test** - `c991d11` (test)

_Interpretation paragraphs (Task 2's report-content deliverable) were front-loaded into the Task 1 report writer since both live in the same runner file; Task 2 delivered the test._

## Files Created/Modified
- `scripts/run_eval.py` - Added `run_rag_eval()` + `_retrieval_state`/`_retrieval_only_invoke`/`_attach_judge_flag`/`_write_rag_eval_report`; deleted the two private metric copies and re-pointed `run_eval`; made `run_rag_eval` the default `__main__` path with `--with-faithfulness`/`--legacy`/`--numeric`.
- `eval/reports/eval_summary.json` - The pinned machine artifact the UI + release gate read (judge=gemini-3.5-flash, faithfulness=-1, real recall/citation).
- `eval/reports/2026-07-28-rag-eval.md` - The human report with the per-question table + P10 interpretation.
- `tests/eval/test_run_rag_eval.py` - The offline fixture-driven runner test.

## Decisions Made
- **Deterministic-only default via the retrieval path (no Gemini):** the committed artifact's recall/citation are REAL and $0; faithfulness is the -1 sentinel per the P10 honesty guard (a real judge number is withheld until ≥0.7 human calibration passes) and because the free-tier Gemini daily quota is exhausted today. `--with-faithfulness` opt-in runs the DeepEval judge serially when key+quota exist.
- **One shared impl:** removed the private `_recall_at_k`/`_citation_accuracy` and imported from `eval.metrics` — the exact deferral 06.1-01-SUMMARY flagged for this plan.
- **judge_model = `gemini-3.5-flash`** (spike 002: `gemini-2.5-flash` 404s).
- **corpus.n_questions = full gold-set size (13)**; the 11 grounded / 2 refusal-eligible split is stated in the report header and Notes (honest provenance, no metric over refusal rows).

## Deviations from Plan

None requiring auto-fix rules. Two in-scope structuring choices worth noting:

1. **Interpretation paragraphs written in Task 1's report writer** rather than added in a separate Task 2 edit — the report writer is a single function, so the P10 section landed with the writer. Task 2 delivered its own artifact (the offline runner test). No behavior lost; all Task 2 acceptance criteria are met and verified.
2. **Deterministic default uses `_retrieval_state` (retrieve+rerank), not `invoke_with_tracing`** — required to produce a REAL committed artifact today without a Gemini generate call (quota exhausted). `invoke_with_tracing` is used on the `--with-faithfulness` path exactly as the plan specifies. This honors the plan's `final_state["retrieved_chunks"]` contract (the retrieval-only state is a `final_state`-shaped dict) while satisfying the quota guard.

---

**Total deviations:** 0 auto-fixed. Both notes are in-scope structuring decisions, not deviation-rule fixes.
**Impact on plan:** All acceptance criteria and success criteria met. No scope creep.

## Issues Encountered
- Sourcing `.env` for a full-suite run un-skipped the pre-existing opt-in Gemini lane (`test_faithfulness_deepeval::test_faithful_fixture_assert_test_optin`), which failed because today's Gemini daily quota is exhausted. This is out of scope (Wave 2 file, environment-dependent opt-in lane). The canonical success-criteria command (no `.env`) is fully green: **587 passed, 10 skipped, 2 xfailed, 0 failed**.

## User Setup Required
None - no external service configuration required. To run the opt-in judge pass later: `set -a && . ./.env && set +a; PYTHONPATH=. .venv/bin/python scripts/run_eval.py --with-faithfulness` (needs GEMINI_API_KEY + available free-tier daily quota).

## Next Phase Readiness
- `eval/reports/eval_summary.json` is the committed contract 06.1-05 (release gate) and 06.1-06 (inline surface) read.
- **Flagged follow-up (not executed in 6.1):** tighten gold `expected_sources` to ≤2–3-page spans + add RPT-attribution/numeric coverage, then re-ratify recall@10 and run the ≥50-example human calibration (≥0.7) before surfacing any faithfulness figure.

---
*Phase: 06.1-eval-harness-inline-metrics-langfuse-ops*
*Completed: 2026-07-28*

## Self-Check: PASSED

- Files verified present: `scripts/run_eval.py`, `eval/reports/eval_summary.json`, `eval/reports/2026-07-28-rag-eval.md`, `tests/eval/test_run_rag_eval.py`, `06.1-04-SUMMARY.md`.
- Commits verified in history: `08c16a3` (Task 1 feat), `c991d11` (Task 2 test).
- `eval_summary.json` validates against `EvalSummary`; full unit suite green (587 passed, 0 failed).
