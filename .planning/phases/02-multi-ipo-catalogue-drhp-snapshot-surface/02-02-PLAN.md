---
phase: 02-multi-ipo-catalogue-drhp-snapshot-surface
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - agent/state.py
  - agent/nodes/intake.py
  - agent/nodes/retrieve.py
  - agent/nodes/refuse_with_reformulation.py
  - data/catalogue_loader.py
  - data/catalogue.json
  - tests/unit/test_drhp_id_threading.py
  - tests/unit/test_drhp_id_allowlist.py
  - tests/unit/test_catalogue.py
autonomous: true
requirements: [SNAP-01, OPS-01]

must_haves:
  truths:
    - "drhp_id flows GraphState → intake → retrieve → search(); a caller can scope a query to any IPO"
    - "Omitting drhp_id defaults to DRHP_ID_DEFAULT — every Phase 1 graph test still passes (219 baseline)"
    - "An unknown drhp_id is rejected by an allow-list before it reaches search() (V5)"
    - "data/catalogue.json loads + validates and lists the 8 curated IPOs"
  artifacts:
    - path: "agent/state.py"
      provides: "GraphState with a drhp_id key"
      contains: "drhp_id"
    - path: "data/catalogue_loader.py"
      provides: "load_catalogue() + validated IPO model + drhp_id allow-list check"
      exports: ["load_catalogue", "is_known_drhp_id"]
    - path: "data/catalogue.json"
      provides: "8 hand-curated IPO entries"
  key_links:
    - from: "agent/nodes/retrieve.py"
      to: "storage.vector.search"
      via: "drhp_id read from state, not DRHP_ID_DEFAULT"
      pattern: "search\\([^)]*drhp_id=state"
    - from: "agent/nodes/retrieve.py"
      to: "data.catalogue_loader.is_known_drhp_id"
      via: "allow-list validation before search()"
      pattern: "is_known_drhp_id"
---

<objective>
The walking-skeleton multi-IPO enablement — surgical, per RESEARCH §Pattern 1. Thread
`drhp_id` through `GraphState` → `intake` (default-preserving) → `retrieve` (reads state,
validates against the catalogue allow-list, then calls the already-multi-IPO `search()`).
Do the identical one-line change in `refuse_with_reformulation`. Hand-curate
`data/catalogue.json` with all 8 IPOs and ship a validated `catalogue_loader` that the
agent and (Wave 4) UI both consume.

This proves the multi-IPO retrieval slice end-to-end at the agent layer BEFORE any
ingestion or UI work. The 219-test baseline is the hard guardrail: the default must keep
every Phase 1 graph invocation working unchanged.

Purpose: Implement SNAP-01/OPS-01's foundation (catalogue exists + agent can scope to any
IPO) and the V5 allow-list threat mitigation. RESEARCH confirms `storage/vector.py::search`
already filters by drhp_id — this plan only removes the two hard-codes and adds validation.
Output: drhp_id-aware agent + validated catalogue loader + 8-IPO catalogue.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agent/state.py
@agent/nodes/intake.py
@agent/nodes/retrieve.py
@agent/nodes/refuse_with_reformulation.py
@agent/policies.py
@storage/vector.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread drhp_id through GraphState + intake + retrieve + refuse</name>
  <files>agent/state.py, agent/nodes/intake.py, agent/nodes/retrieve.py, agent/nodes/refuse_with_reformulation.py, tests/unit/test_drhp_id_threading.py</files>
  <behavior>
    - Test: GRAPH/retrieve invoked with state {"question": q, "drhp_id": "hyundai_2024_10"} calls search() with drhp_id="hyundai_2024_10" (assert via monkeypatched search capturing kwargs).
    - Test: retrieve invoked WITHOUT a drhp_id key (Phase 1 call shape {"question": q, "regenerate_attempts": 0}) calls search() with drhp_id=DRHP_ID_DEFAULT ("swiggy_2024_11") — back-compat.
    - Test: intake.run normalizes question AND propagates state["drhp_id"] when present, defaulting to DRHP_ID_DEFAULT when absent.
    - Test: refuse_with_reformulation's search_relaxed call uses state["drhp_id"], not the imported constant.
  </behavior>
  <read_first>
    - agent/state.py (GraphState TypedDict — add `drhp_id: str` per RESEARCH §Pattern 1 change set)
    - agent/nodes/intake.py (initializes all keys; add drhp_id default-or-passthrough)
    - agent/nodes/retrieve.py (line 32 passes DRHP_ID_DEFAULT — change to state["drhp_id"])
    - agent/nodes/refuse_with_reformulation.py (line ~104 search_relaxed uses DRHP_ID_DEFAULT — change to state["drhp_id"])
    - agent/policies.py (DRHP_ID_DEFAULT stays as the fallback — do NOT delete it)
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-RESEARCH.md §Pattern 1 (verified change set + "Why backward-compatible")
  </read_first>
  <action>
    Add `drhp_id: str` to the GraphState TypedDict (between `question` and `retrieved_chunks`), with a docstring line "which IPO this query targets". In `intake.run`, compute `drhp_id = state.get("drhp_id") or DRHP_ID_DEFAULT` (import DRHP_ID_DEFAULT) and include `"drhp_id": drhp_id` in the returned dict — this is the back-compat anchor that keeps the 219 baseline green. In `retrieve.run`, replace `drhp_id=DRHP_ID_DEFAULT` with `drhp_id=state["drhp_id"]` (intake guarantees the key exists; keep the DRHP_ID_DEFAULT import only if still referenced, else remove to satisfy lint). In `refuse_with_reformulation.run`, replace the `drhp_id=DRHP_ID_DEFAULT` argument to `search_relaxed` with `drhp_id=state.get("drhp_id", DRHP_ID_DEFAULT)` (keep the import as the fallback). Flip the threading stub to real assertions per <behavior>. Update retrieve.py's docstring line that says "Phase 2 will introduce dynamic drhp_id selection" to reflect it is now implemented.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_drhp_id_threading.py -x -q && python -m pytest tests/unit tests/integration/test_agent_e2e.py -q --timeout=60</automated>
  </verify>
  <done>test_drhp_id_threading passes; the 219 Phase 1 baseline + agent e2e stay green; retrieve and refuse both read drhp_id from state with a preserved default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Catalogue loader + drhp_id allow-list (V5) + 8-IPO catalogue.json</name>
  <files>data/catalogue_loader.py, data/catalogue.json, agent/nodes/retrieve.py, tests/unit/test_catalogue.py, tests/unit/test_drhp_id_allowlist.py</files>
  <behavior>
    - Test: load_catalogue() returns a list of validated IPO models; every entry has drhp_id, issuer, sector, listing_date, issue_size_cr, doc_type, source_url, front_matter_pages, snapshot_path, status.
    - Test: load_catalogue() contains exactly the 8 curated drhp_ids (swiggy, hyundai, ola_electric, zomato, nykaa, paytm, lic, honasa).
    - Test: is_known_drhp_id("swiggy_2024_11") is True; is_known_drhp_id("'; DROP--") and is_known_drhp_id("unknown_x") are False.
    - Test: retrieve.run with an unknown drhp_id raises/refuses BEFORE calling search() (monkeypatch search to fail the test if invoked with an unknown id).
  </behavior>
  <read_first>
    - data/catalogue.json (Wave 0 schema stub — the Swiggy row is the canonical shape)
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-RESEARCH.md §DRHP Source List (the 8 IPOs: drhp_id, doc_type, SEBI source URL, outcome framing) + §Security Domain V5 (allow-list-before-search)
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-CONTEXT.md D2-01, D2-02 (the curated list + per-IPO requirements)
    - agent/nodes/retrieve.py (where the allow-list guard slots in — before search())
    - storage/vector.py::search (the call the allow-list protects)
  </read_first>
  <action>
    Create `data/catalogue_loader.py` with: a Pydantic model `CatalogueIPO` mirroring the catalogue.json row schema (drhp_id, issuer, sector, listing_date, issue_size_cr: int, doc_type: Literal["DRHP","RHP","Prospectus"], fresh_vs_ofs: dict|None, lead_managers: list[str], source_url, source_sha256: str|None, front_matter_pages: int, snapshot_path, status: Literal["listed","open"]); `load_catalogue() -> list[CatalogueIPO]` reading data/catalogue.json and validating each row (cache with functools.lru_cache); `is_known_drhp_id(drhp_id: str) -> bool` checking membership against the loaded drhp_id set. This is the V5 control.
    Populate `data/catalogue.json` with all 8 IPOs from RESEARCH §DRHP Source List — for each: drhp_id, issuer, sector (text label), listing_date, issue_size_cr (best-known; null only if genuinely unknown — never fabricate), doc_type (prefer RHP/Prospectus where a price band is needed per P14 §4), source_url (the SEBI landing URL), source_sha256: null (pinned at ingest), fresh_vs_ofs: null (Wave 3 fills), lead_managers: [], front_matter_pages (20 default; override per IPO if known), snapshot_path "data/snapshots/<drhp_id>.json", status "listed". Do NOT invent price bands or financials here — only catalogue-level metadata.
    In `agent/nodes/retrieve.py`, before the search() call, add: if not is_known_drhp_id(state["drhp_id"]) → return a state with a RefusalResponse(reason="infrastructure_error", explanation=<unknown-ipo copy>) and empty retrieved_chunks, so the graph routes to emit without hitting Qdrant. Import is_known_drhp_id from data.catalogue_loader. (This keeps untrusted drhp_id out of the Qdrant filter — V5.)
    Flip test_catalogue and test_drhp_id_allowlist stubs to the <behavior> assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_catalogue.py tests/unit/test_drhp_id_allowlist.py -x -q && python -m pytest tests/unit -q --timeout=15</automated>
  </verify>
  <done>Catalogue loads + validates 8 IPOs; allow-list rejects unknown/injection ids; retrieve refuses an unknown drhp_id without calling search(); 219 baseline green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| session/query-param → agent | `drhp_id` originates from the catalogue click / query param — untrusted until allow-listed |
| agent → Qdrant filter | `drhp_id` must be validated before becoming a `search()` filter value |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-V5 | Tampering | retrieve.run → storage.vector.search | mitigate | `is_known_drhp_id()` allow-list check against catalogue keys BEFORE any `search(drhp_id=...)`; unknown id → refusal, never reaches the Qdrant filter (RESEARCH §Security V5) |
| T-02-02 | Tampering | data/catalogue.json | accept | Repo-committed trusted config; the loader Pydantic-validates each row so a malformed entry fails fast at load, not at query time |
| T-02-SC | Tampering | pip installs | mitigate | No new packages (RESEARCH audit: N/A). No install task. |
</threat_model>

<verification>
- `pytest tests/unit/test_drhp_id_threading.py tests/unit/test_catalogue.py tests/unit/test_drhp_id_allowlist.py -x` all green.
- `pytest tests/unit -q --timeout=15` → 219 baseline + new tests green.
- `python -c "from data.catalogue_loader import load_catalogue; assert len(load_catalogue())==8"`.
</verification>

<success_criteria>
drhp_id threads through the agent with a preserving default; unknown ids are allow-list-rejected before Qdrant; 8-IPO catalogue loads + validates; Phase 1 219 baseline stays green.
</success_criteria>

<output>
Create `.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-02-SUMMARY.md` when done.
</output>
