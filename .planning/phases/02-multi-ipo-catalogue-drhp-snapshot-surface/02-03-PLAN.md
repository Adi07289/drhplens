---
phase: 02-multi-ipo-catalogue-drhp-snapshot-surface
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - pipelines/ingest.py
  - pipelines/ingest_swiggy.py
  - storage/vector.py
  - tests/unit/test_ingest_generalize.py
  - tests/unit/test_ingest_idempotent.py
  - tests/unit/test_parse_quality.py
autonomous: true
requirements: [SNAP-01]

must_haves:
  truths:
    - "pipelines/ingest.py exposes ingest_drhp(drhp_id, pdf_path, ...) with no module-level hard-codes"
    - "Re-ingesting an IPO deletes its existing points by drhp_id filter before upsert — no duplicates"
    - "A DRHP that parses to too few sections / a single Full-Document fallback is flagged, not silently ingested (P14)"
    - "Per-IPO front_matter_pages drives pagination, not a hard-coded Swiggy 20-page threshold"
    - "Phase 1 ingest_swiggy tests still pass (shim or repoint)"
  artifacts:
    - path: "pipelines/ingest.py"
      provides: "Parameterized ingest_drhp() + ingest-all CLI looping over catalogue.json"
      exports: ["ingest_drhp"]
    - path: "storage/vector.py"
      provides: "delete_by_drhp_id() for idempotent re-ingest"
      exports: ["delete_by_drhp_id"]
  key_links:
    - from: "pipelines/ingest.py"
      to: "storage.vector.delete_by_drhp_id"
      via: "delete-by-filter before upsert (idempotency)"
      pattern: "delete_by_drhp_id"
    - from: "pipelines/ingest.py"
      to: "data.catalogue_loader.load_catalogue"
      via: "ingest-all loops over catalogue + uses per-IPO front_matter_pages"
      pattern: "load_catalogue"
---

<objective>
Generalize ingestion from one hard-coded IPO to N, per RESEARCH §Pattern 2. Turn
`pipelines/ingest_swiggy.py` into `pipelines/ingest.py` with `ingest_drhp(drhp_id, pdf_path,
...)`, add idempotent re-ingest (delete-by-drhp_id before upsert — the duplicate-points
gotcha A6), a per-IPO parse-quality gate (P14), and per-IPO pagination via
`front_matter_pages` (A5). All four parse→chunk→embed→upsert steps already exist; this
plan parameterizes them and adds the two correctness gates.

CODE-NOW-DEFER-UPSERT: write `ingest_drhp` + all unit tests against parse/chunk/quality-gate
logic now (no live Qdrant needed — `--dry-run` path + monkeypatched upsert). The actual
live multi-IPO upsert (which needs a running Qdrant + bge-m3/torch) is deferred to Plan 06's
runbook. Per-IPO failure isolation (P14) means one un-parseable DRHP never blocks the batch.

Purpose: deliver must-have 8 ("adding IPO N+1 is drop-DRHP-and-run") + the P14/A5/A6
correctness gates RESEARCH flagged. SNAP-01 depends on a working multi-IPO ingest path.
Output: pipelines/ingest.py, storage.vector.delete_by_drhp_id, parse-quality gate, kept-green Swiggy shim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@pipelines/ingest_swiggy.py
@storage/vector.py
@data/catalogue.json
@.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipelines/ingest.py — parameterized ingest_drhp + per-IPO pagination + Swiggy shim</name>
  <files>pipelines/ingest.py, pipelines/ingest_swiggy.py, tests/unit/test_ingest_generalize.py</files>
  <behavior>
    - Test: ingest_drhp(drhp_id="hyundai_2024_10", pdf_path=<fixture docling.json or tiny pdf>, dry_run=True) returns an IngestReport whose chunks all carry drhp_id="hyundai_2024_10" (not the Swiggy constant).
    - Test: front_matter_pages is honored — a doc ingested with front_matter_pages=12 produces printed labels consistent with a 12-page Roman front matter, not 20.
    - Test: importing pipelines.ingest_swiggy still works (shim) and its existing Phase 1 tests pass.
    - Test: no module-level DRHP_ID / PDF_PATH / JSON_CACHE_PATH constants are used inside ingest_drhp (parameters only).
  </behavior>
  <read_first>
    - pipelines/ingest_swiggy.py (the four steps to parameterize; chunk_sections already takes drhp_id; ROMAN_NUMERAL_THRESHOLD_PAGE is the Swiggy-specific constant to make per-IPO)
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-RESEARCH.md §Pattern 2 (recommended signature + IngestReport + per-IPO SHA pin) and Pitfall P14 item 3 (pagination fix via front_matter_pages)
    - data/catalogue.json (front_matter_pages field per IPO)
    - tests/integration/test_qdrant_ingest.py (which tests import ingest_swiggy — keep them working)
  </read_first>
  <action>
    Create `pipelines/ingest.py` containing the generalized pipeline. Define `ingest_drhp(drhp_id: str, pdf_path: Path, *, json_cache_path: Path | None = None, front_matter_pages: int = 20, max_tokens: int = 512, overlap_tokens: int = 100, dry_run: bool = False) -> IngestReport`. Move the parse/chunk/embed/upsert helpers from ingest_swiggy.py into ingest.py, parameterizing every hard-code: pass `drhp_id` explicitly to chunk_sections; thread `front_matter_pages` into `_infer_printed_label` (replace the module-level ROMAN_NUMERAL_THRESHOLD_PAGE with a parameter, defaulting per-IPO from catalogue). `json_cache_path` defaults to `pdf_path.with_suffix(".docling.json")`. Define a small `IngestReport` dataclass (chunk_count, page_coverage, token_stats, sha_verified, extraction_quality). Add SHA-256 verification: if catalogue source_sha256 is set, verify pdf bytes before parsing and refuse on mismatch (P14 §4). Add a Typer CLI: `python -m pipelines.ingest <drhp_id> --pdf <path>` and an `ingest-all` command that loops `load_catalogue()`, calling ingest_drhp per IPO inside a try/except so one failure is logged + skipped (P14 failure isolation) and does not abort the batch.
    Replace `pipelines/ingest_swiggy.py` body with a thin shim: import the moved functions from pipelines.ingest and re-export the Swiggy CLI bound to the Swiggy constants, so existing imports/tests keep working. Do NOT delete names that Phase 1 tests import.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_ingest_generalize.py -x -q && python -m pytest tests/unit -q --timeout=15</automated>
  </verify>
  <done>ingest_drhp produces drhp-tagged chunks with no module-level hard-codes; per-IPO front_matter_pages honored; ingest_swiggy shim keeps Phase 1 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent re-ingest (delete-by-drhp_id) + parse-quality gate (P14)</name>
  <files>storage/vector.py, pipelines/ingest.py, tests/unit/test_ingest_idempotent.py, tests/unit/test_parse_quality.py</files>
  <behavior>
    - Test: delete_by_drhp_id("swiggy_2024_11") issues a Qdrant delete with a drhp_id FieldCondition filter (assert via monkeypatched client capturing the filter).
    - Test: ingest_drhp calls delete_by_drhp_id(drhp_id) before upsert_chunks (ordering asserted via call recorder) so re-ingest cannot duplicate points (A6).
    - Test: a parse producing < MIN_SECTIONS (e.g. 10) or a single "Full Document"/"Page N" fallback section yields extraction_quality="fallback" and the gate flags it (IngestReport.extraction_quality != "ok"), rather than silently upserting.
    - Test: a healthy parse (>= MIN_SECTIONS, at least one section matching a known DRHP section regex like "Risk Factors"/"Objects of the Issue"/"Restated") yields extraction_quality="ok".
  </behavior>
  <read_first>
    - storage/vector.py (upsert_chunks + ensure_collection — add a delete_by_drhp_id using rest.Filter/FieldCondition, mirroring the search() filter)
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-RESEARCH.md §Pattern 2 Idempotency (delete-by-filter recommendation) + Pitfall P14 item 2 (parse-quality gate: section count, known-section regex, page coverage; the "Full Document" fallback signal in extract_sections_from_docling)
    - pipelines/ingest_swiggy.py extract_sections_from_docling (the "Full Document" fallback branch the gate must detect)
  </read_first>
  <action>
    In `storage/vector.py` add `delete_by_drhp_id(drhp_id: str) -> None` that calls `client().delete(collection_name=COLLECTION_NAME, points_selector=rest.FilterSelector(filter=rest.Filter(must=[rest.FieldCondition(key="drhp_id", match=rest.MatchValue(value=drhp_id))])))`. Keep the storage-bus invariant (write path only).
    In `pipelines/ingest.py` add a `parse_quality_gate(sections) -> str` returning "ok" | "fallback": "fallback" if len(sections) < MIN_SECTIONS (define MIN_SECTIONS=10 per P14) OR the only section names match the fallback pattern ("Full Document", "Page N", "Tables", "Preamble" alone) OR no section name matches a KNOWN_DRHP_SECTION regex (Risk Factors | Objects of the (Issue|Offer) | Our Business | Restated). Record the result in IngestReport.extraction_quality. In the non-dry-run upsert path of ingest_drhp, call `delete_by_drhp_id(drhp_id)` immediately before `upsert_chunks` (idempotency). If extraction_quality=="fallback", log a warning and (per P14 failure isolation) still produce the report but mark it so ingest-all can omit that IPO from the catalogue-ready set rather than ship a garbage snapshot.
    Flip the idempotent + parse-quality stubs to the <behavior> assertions (monkeypatch the Qdrant client; no live daemon).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_ingest_idempotent.py tests/unit/test_parse_quality.py -x -q && python -m pytest tests/unit -q --timeout=15</automated>
  </verify>
  <done>delete_by_drhp_id filters correctly; ingest_drhp deletes-then-upserts; parse-quality gate flags fallback parses and passes healthy ones; 219 baseline green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DRHP PDF (SEBI) → index | Trusted source, SHA-pinned; content can't silently change |
| pipeline → Qdrant | Write path; idempotency prevents index pollution |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-A6 | Tampering | re-ingest duplicate points | mitigate | `delete_by_drhp_id` before upsert (RESEARCH Pattern 2 idempotency) — re-ingesting Swiggy with real Docling cannot duplicate points |
| T-02-A5 | Tampering (citation drift) | per-IPO pagination | mitigate | `front_matter_pages` per IPO replaces the Swiggy-tuned 20-page threshold → correct page links (P5 regression defense) |
| T-02-P14 | DoS/quality | one bad DRHP blocks batch | mitigate | parse-quality gate flags fallback parses + per-IPO try/except in ingest-all isolates failures; flagged IPO omitted from catalogue-ready set |
| T-02-V6 | Integrity | DRHP version drift | mitigate | SHA-256 pin verified before parse; refuse on mismatch |
| T-02-SC | Tampering | pip installs | mitigate | No new packages (RESEARCH audit: N/A). Real Docling/torch already in Phase 1 stack. |
</threat_model>

<verification>
- `pytest tests/unit/test_ingest_generalize.py tests/unit/test_ingest_idempotent.py tests/unit/test_parse_quality.py -x` green.
- `pytest tests/unit -q --timeout=15` → 219 baseline + new tests green.
- `python -c "import pipelines.ingest_swiggy"` still imports (shim intact).
- Live multi-IPO upsert intentionally deferred to Plan 06 runbook (CODE-NOW-DEFER-UPSERT).
</verification>

<success_criteria>
ingest_drhp(drhp_id, pdf_path) parameterized + idempotent + parse-quality-gated + per-IPO paginated; Swiggy shim keeps Phase 1 green; live upsert deferred to runbook.
</success_criteria>

<output>
Create `.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-03-SUMMARY.md` when done.
</output>
