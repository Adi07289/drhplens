---
phase: 02-multi-ipo-catalogue-drhp-snapshot-surface
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-04"]
files_modified:
  - app.py
  - pages/02_snapshot.py
  - ui/snapshot_blocks.py
  - ui/catalogue.py
  - ui/copy.py
  - app/static/drhplens.css
  - scripts/smoke.sh
autonomous: false
requirements: [SNAP-01, SNAP-02, SNAP-03, SNAP-04, SNAP-05, SNAP-06, SNAP-07, OPS-01]

must_haves:
  truths:
    - "app.py is the catalogue landing: a grid of factual IPO cards (no green/red, no perf badges) → click → snapshot"
    - "pages/02_snapshot.py renders the 6 cited blocks in the locked UI-SPEC order + the OFS/fresh split bar as Block 5's focal element"
    - "Each snapshot block renders citations via the UNCHANGED Phase 1 chip + expander renderer"
    - "The Phase 1 Q&A chat is co-located below the 6 blocks, bound to the page's drhp_id (chat works for any IPO)"
    - "A not-disclosed field shows the honest 'Not disclosed in this DRHP.' note, never a fabricated value"
    - "Phase 1 ui/copy.py Swiggy hard-codes are templated by issuer while keeping the import-time scrubber assertion intact"
  artifacts:
    - path: "app.py"
      provides: "Catalogue landing page (replaces Swiggy-hardcoded root)"
    - path: "pages/02_snapshot.py"
      provides: "Per-IPO snapshot page (6 blocks + co-located chat)"
    - path: "ui/snapshot_blocks.py"
      provides: "6 block renderers + split bar + financials table + not-disclosed note (reuse chip/expander)"
    - path: "ui/catalogue.py"
      provides: "IPO card grid renderer (factual only)"
  key_links:
    - from: "pages/02_snapshot.py"
      to: "pipelines.snapshot.load_snapshot"
      via: "reads data/snapshots/<drhp_id>.json by query-param drhp_id"
      pattern: "load_snapshot"
    - from: "ui/snapshot_blocks.py"
      to: "ui.chip.render_answer_with_chips"
      via: "snapshot blocks reuse the Phase 1 citation renderer"
      pattern: "render_answer_with_chips"
    - from: "pages/02_snapshot.py"
      to: "agent.graph.GRAPH"
      via: "co-located Q&A chat invokes the graph with this page's drhp_id"
      pattern: "drhp_id"
---

<objective>
Build the catalogue + snapshot UI per 02-UI-SPEC.md (APPROVED), extending — never restyling
— the Phase 1 design system. `app.py` becomes the catalogue landing (factual IPO cards, no
green/red — the hardest invariant); `pages/02_snapshot.py` reads `drhp_id` from the query
param, loads the precomputed snapshot, and renders the 6 cited blocks in the locked order
with the OFS-vs-fresh split bar as Block 5's focal element, then co-locates the Phase 1 Q&A
chat below (bound to this IPO's drhp_id). Every citation reuses the unchanged Phase 1
chip+expander renderer (D2-04, P2-L4). Not-disclosed fields show the honest note (P2-L8).

The structural change (FLAG-ROUTING): extract the chat-rendering code out of today's
Swiggy-hardcoded app.py into a drhp_id-parameterized function, then invoke it from the
snapshot page. Template ui/copy.py's Swiggy hard-codes by issuer while keeping the
import-time scrubber assertion intact (FLAG-COPY-TEMPLATING).

Purpose: deliver the user-facing half of SNAP-01..07 + OPS-01 — browse catalogue → read a
fully-cited snapshot → ask follow-ups, all neutral and honest.
Output: catalogue landing, snapshot page, block renderers, templated copy, Phase 2 CSS classes, extended smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-UI-SPEC.md
@app.py
@ui/chip.py
@ui/expander.py
@ui/disclaimer.py
@ui/copy.py
@pages/01_methodology.py
@data/catalogue.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalogue landing (app.py) + IPO card grid + templated copy + CSS</name>
  <files>app.py, ui/catalogue.py, ui/copy.py, app/static/drhplens.css, agent/nodes/refuse_with_reformulation.py</files>
  <read_first>
    - app.py (the Swiggy-hardcoded root + _render_metadata_header/_render_chat_history/_render_input_and_invoke that must be extracted/parameterized — FLAG-ROUTING)
    - ui/copy.py (HERO_HEADING/SUBHEADING/QUESTION_PLACEHOLDER/EMPTY_STATE_BODY/LOADING_ANSWER_COPY hard-code "Swiggy"; the import-time scrub assertion block at the bottom — FLAG-COPY-TEMPLATING; must keep scrubber running on templated forms)
    - data/catalogue_loader.py (load_catalogue() — drives the grid)
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-UI-SPEC.md §IA Catalogue Page (card contents, no perf badges, card states, accessibility aria-label) + §Color (no green/red) + §Copywriting Contract (catalogue hero, card label, empty state) + §Mobile (3/2/1 col grid)
    - app/static/drhplens.css (inherited --drhp-* tokens; Phase 2 may ONLY add new classes, never redefine tokens)
  </read_first>
  <action>
    Add the Phase 2 catalogue copy constants to `ui/copy.py` (catalogue hero heading "Indian IPOs, read honestly.", subheading, empty-state heading/body, "Open now" tag, the per-block headings, split-bar caption template, "Not disclosed in this DRHP.", precomputing copy, unknown-drhp_id copy) — all from UI-SPEC §Copywriting Contract. Template the existing Swiggy-hardcoded constants into issuer-parameterized format strings (e.g. HERO_HEADING_TEMPLATE) AND keep the import-time scrubber assertion covering the templated/example forms (run scrub on a sample-substituted instance so the "no banned token, enforced at import" guarantee holds).
    Create `ui/catalogue.py` with `render_catalogue_grid(ipos)`: `st.columns(3/2/1)` responsive grid of `.drhp-ipo-card` divs — issuer (Body 600), sector (Small muted), listing date · issue size in lakh/crore (Small muted), each card a full tap target (≥88px) linking to `/snapshot?drhp_id=<id>` with the UI-SPEC card aria-label. NO listing-gain %, NO badge, NO color verdict (winner and flop identical chrome). Currently-open IPOs (status="open") sort first with a neutral "Open now" text tag.
    Rewrite `app.py` main() to render the catalogue: hero (catalogue copy) + render_catalogue_grid(load_catalogue()) + inherited persistent footer + first-use modal gate (unchanged). Extract the chat/metadata/answer-render code into a reusable `render_snapshot_chat(drhp_id)` (placed in a shared module importable by pages/02_snapshot.py — Task 2 consumes it). Keep the Swiggy deep-link working by routing /snapshot?drhp_id=swiggy_2024_11.
    Add the new Phase 2 CSS classes to `app/static/drhplens.css` (`.drhp-catalogue-grid`, `.drhp-ipo-card*`) using only inherited `--drhp-*` tokens — no token redefinition. Update refuse_with_reformulation copy import if a templated constant name changed.
  </action>
  <verify>
    <automated>python -m pytest tests/unit -q --timeout=15 && python -c "import ast,sys; ast.parse(open('app.py').read()); ast.parse(open('ui/catalogue.py').read()); print('parse ok')" && python -c "from ui import copy" && grep -q 'drhp-ipo-card' app/static/drhplens.css</automated>
  </verify>
  <done>app.py renders the catalogue grid from load_catalogue(); copy templated by issuer with scrubber assertion intact (test_copy_no_banned_tokens green); new CSS classes added without redefining tokens; modules import cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot page — 6 cited blocks + split bar + financials table + co-located chat</name>
  <files>pages/02_snapshot.py, ui/snapshot_blocks.py, app/static/drhplens.css, ui/copy.py</files>
  <read_first>
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-UI-SPEC.md §IA Snapshot Page (locked block ORDER 1-9) + §Split-Bar Contract (THE signal element, accent OFS / neutral fresh, role=img aria-label, pure-fresh/pure-OFS copy) + §Financials Rendering (table not sparkline, losses in parentheses same color, — for missing, tabular-nums) + §Risk Block (Risk N of M, no severity color) + §Loading/Empty/Error (not-disclosed note, precomputing, unreachable) + §Routing (query-param drhp_id, unknown id copy)
    - ui/chip.py render_answer_with_chips + ui/expander.py render_citation_expanders (REUSE unchanged — snapshot blocks cite exactly like Q&A answers)
    - ui/disclaimer.py render_per_answer_footer + render_persistent_footer (per-block + page footer)
    - pipelines/snapshot.py load_snapshot + agent/snapshot_schema.py SnapshotRecord (the data source: fields are GroundedAnswer | RefusalResponse; ofs_fresh dict)
    - agent/schemas.py GroundedAnswer/RefusalResponse (model_validate from the loaded record)
    - app.py render_snapshot_chat(drhp_id) (the extracted chat fn from Task 1 — call it for block 9)
  </read_first>
  <action>
    Create `ui/snapshot_blocks.py` with renderers that all consume a SnapshotRecord field (GroundedAnswer | RefusalResponse): `render_grounded_block(field, heading)` reuses render_answer_with_chips + render_citation_expanders + render_per_answer_footer (Phase 1 path, unchanged) for the business/risks/metadata/promoter blocks; when the field is a RefusalResponse, render the `.drhp-not-disclosed` note "Not disclosed in this DRHP." (P2-L8). `render_split_bar(ofs_fresh)` renders the `.drhp-split-bar` (OFS segment accent #1E40AF, fresh segment neutral #F4F5F7, text % labels inside segments, role="img" aria-label, pure-fresh/pure-OFS copy variants; NO red/green) — the focal element at the TOP of Block 5. `render_financials_table(field)` renders `.drhp-fin-table` (years as columns, 6 metric rows, tabular-nums, losses as (₹X cr) in normal #0F172A color, — for missing cells with aria-label, each row carries its citation chip). `render_risk_block(field)` renders prioritized `.drhp-risk-item`s with "Risk N of M" counters (no severity color).
    Create `pages/02_snapshot.py` (Streamlit page, route /snapshot): st.set_page_config first; read `st.query_params["drhp_id"]`; if missing/unknown (not is_known_drhp_id) render the unknown-id honesty note + "← All IPOs" link (no exception); else load_snapshot(drhp_id) and render breadcrumb → Display issuer title → the 6 blocks in the locked UI-SPEC order (metadata, business, financials, risks, use-of-proceeds with split bar first, promoter) → a 2xl gap + divider → call render_snapshot_chat(drhp_id) for the co-located Q&A (block 9) → persistent footer. If the snapshot cache is missing, render the "still being prepared" state but keep the chat usable (UI-SPEC §Loading). Keep initial_sidebar_state collapsed + sidebar-hide CSS (inherited).
    Add the Phase 2 snapshot CSS classes to `app/static/drhplens.css` (`.drhp-snapshot-block*`, `.drhp-split-bar*`, `.drhp-fin-table`, `.drhp-risk-item`, `.drhp-not-disclosed`, `.drhp-breadcrumb`) using inherited tokens only.
    Extend `scripts/smoke.sh`: after the existing `/` and `/methodology` curl checks, add a curl probe against `/snapshot?drhp_id=swiggy_2024_11` asserting HTTP 200 + a DOCTYPE/html marker (mirroring the existing `/methodology` probe block) so the smoke test actually fails if the snapshot page is broken. Keep the existing checks unchanged.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['pages/02_snapshot.py','ui/snapshot_blocks.py']]; print('parse ok')" && python -m pytest tests/unit -q --timeout=15 && grep -q 'drhp-split-bar' app/static/drhplens.css && grep -q 'drhp-not-disclosed' app/static/drhplens.css && grep -q 'snapshot' scripts/smoke.sh</automated>
  </verify>
  <done>Snapshot page reads drhp_id from query param, renders 6 cited blocks in locked order with the split bar as Block 5 focal element + financials table (losses neutral) + co-located chat; not-disclosed fields show the honest note; reuses Phase 1 chip renderer; new CSS classes added without token redefinition.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The catalogue landing (factual IPO cards, no green/red) and a per-IPO snapshot page with 6 cited blocks, the OFS-vs-fresh split bar, the financials table (losses in neutral parentheses), honest "Not disclosed" fields, and the co-located Q&A chat — all reusing the Phase 1 citation chips. Smoke test extended (scripts/smoke.sh boots app.py + the snapshot page).</what-built>
  <how-to-verify>
    1. Run `bash scripts/smoke.sh` — confirm both app.py and pages/02_snapshot.py import/boot without error.
    2. Run `streamlit run app.py`. On `/`: confirm the catalogue shows IPO cards with issuer · sector · listing date · issue size and NO listing-gain %, NO green/red, NO badges — a winner (Zomato/Hyundai) and a flop (Paytm/LIC) look visually identical.
    3. Click an IPO card → confirm it navigates to `/snapshot?drhp_id=<id>`.
    4. On the snapshot page (use swiggy_2024_11, which has a seeded snapshot): confirm the 6 blocks render in order (metadata, Business, Key Financials, Risk Factors, Use of Proceeds, Promoters & Management); the OFS-vs-fresh split bar is the first element of Use of Proceeds and uses accent+grey (no red/green); a loss in the financials table renders as (₹X cr) in normal text color; a not-disclosed field (e.g. pledging) shows "Not disclosed in this DRHP." not a fabricated value.
    5. Click a `[1]` citation chip on any block → confirm the inline source expander opens with the DRHP span + "View DRHP page N" link (Phase 1 renderer).
    6. Scroll below the 6 blocks → confirm the Q&A chat is present and ask a question about this IPO → confirm it answers/refuses bound to this drhp_id.
    7. Resize to 375 / 640 / 1024 (Chrome DevTools) → confirm catalogue grid goes 1/2/3 columns and snapshot stays single-column with the financials table horizontally scrollable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. a green/red leak, wrong block order, a fabricated not-disclosed value, broken chip).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| query param drhp_id → page | untrusted until allow-listed (reuses Plan 02 is_known_drhp_id) |
| committed snapshot JSON → render | trusted (scrubbed at pre-compute) but validated at load |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-V5 | Tampering | pages/02_snapshot.py query param | mitigate | drhp_id validated via is_known_drhp_id before load_snapshot/chat; unknown → honesty note, never an exception or raw filter |
| T-02-03 | Tampering | render of committed snapshot | mitigate | snapshot answer_prose already scrubbed at pre-compute (Plan 04); load-time SnapshotRecord validation; not-disclosed shown as absence (P2-L8) |
| T-02-08 | Information disclosure | external DRHP PDF | mitigate | citations link OUT to SEBI URL; the app never serves the PDF (UI-SPEC Registry Safety) |
| T-02-SC | Tampering | pip installs | mitigate | No new packages (RESEARCH audit: N/A). |
</threat_model>

<verification>
- `bash scripts/smoke.sh` boots app.py + pages/02_snapshot.py (smoke.sh extended to probe `/snapshot?drhp_id=swiggy_2024_11`).
- `pytest tests/unit -q --timeout=15` green — the FULL Phase 1 + Phase 2 unit baseline (this wave refactors app.py + touches refuse_with_reformulation, so the whole 219+ baseline must stay green, not just the copy test).
- Human-verify checkpoint approved (no green/red leak, locked block order, honest not-disclosed, working chips + co-located chat, responsive).
</verification>

<success_criteria>
Catalogue + snapshot pages live, neutral, cited via reused Phase 1 chips, honest about absence, with a working co-located drhp_id-bound chat; human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-05-SUMMARY.md` when done.
</output>
