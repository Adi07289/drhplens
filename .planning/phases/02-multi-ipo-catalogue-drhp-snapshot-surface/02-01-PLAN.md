---
phase: 02-multi-ipo-catalogue-drhp-snapshot-surface
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_drhp_id_threading.py
  - tests/unit/test_drhp_id_allowlist.py
  - tests/unit/test_catalogue.py
  - tests/unit/test_ingest_generalize.py
  - tests/unit/test_ingest_idempotent.py
  - tests/unit/test_parse_quality.py
  - tests/unit/test_snapshot_cache.py
  - tests/unit/test_snapshot_fields.py
  - tests/unit/test_ofs_fresh.py
  - tests/integration/test_second_ipo_e2e.py
  - tests/eval/test_snapshot_eval.py
  - tests/eval/test_p13_recall.py
  - data/catalogue.json
  - data/snapshots/.gitkeep
  - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-VALIDATION.md
autonomous: true
requirements: [SNAP-01, SNAP-02, SNAP-03, SNAP-04, SNAP-05, SNAP-06, SNAP-07, OPS-01]

must_haves:
  truths:
    - "Every 02-VALIDATION.md row has a test file that collects (xfail until its wave)"
    - "Phase 1's 219 tests stay green after the stubs land"
    - "data/catalogue.json exists with a schema the Wave 1 loader will validate against"
    - "data/snapshots/ directory exists and is tracked"
  artifacts:
    - path: "tests/unit/test_drhp_id_threading.py"
      provides: "Wave 0 stub for the core drhp_id generalization"
    - path: "data/catalogue.json"
      provides: "Catalogue schema stub (content fills in Wave 1)"
    - path: "data/snapshots/.gitkeep"
      provides: "Snapshot cache directory tracked in git"
  key_links:
    - from: "tests/*"
      to: "pytest collection"
      via: "xfail markers keep red tests from failing CI"
      pattern: "pytest.mark.xfail"
---

<objective>
Wave 0 scaffold for Phase 2. Create one test stub per 02-VALIDATION.md row (12 rows),
the `data/catalogue.json` schema stub, and the `data/snapshots/` directory. Every stub
must COLLECT cleanly and be marked `xfail` (or `--run-eval`/live-Qdrant gated) so the
Phase 1 219-test baseline stays green while later waves flip stubs to real assertions.

This is the Nyquist scaffold: it makes every later task's `<verify>` command resolvable
from day one. No production code changes in this plan.

Purpose: Lock the test surface (per D2-04 reuse posture + 02-VALIDATION map) before any
production code is touched, so each later wave has a concrete failing test to satisfy.
Output: 12 test stub files, `data/catalogue.json`, `data/snapshots/.gitkeep`, and
`02-VALIDATION.md` front-matter flipped to `nyquist_compliant: true` + `wave_0_complete: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-VALIDATION.md
@.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 12 test stubs (xfail-gated)</name>
  <files>tests/unit/test_drhp_id_threading.py, tests/unit/test_drhp_id_allowlist.py, tests/unit/test_catalogue.py, tests/unit/test_ingest_generalize.py, tests/unit/test_ingest_idempotent.py, tests/unit/test_parse_quality.py, tests/unit/test_snapshot_cache.py, tests/unit/test_snapshot_fields.py, tests/unit/test_ofs_fresh.py, tests/integration/test_second_ipo_e2e.py, tests/eval/test_snapshot_eval.py, tests/eval/test_p13_recall.py</files>
  <read_first>
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-VALIDATION.md (the 12-row Per-Task Verification Map — each row maps to exactly one file here)
    - tests/unit/test_chunker.py (Phase 1 unit-test style: module docstring, plain pytest, no class wrappers)
    - tests/integration/test_qdrant_ingest.py (how Phase 1 gates a live-Qdrant test — reuse the same skip/xfail mechanism)
    - tests/eval/test_phase1_eval.py (how Phase 1 gates eval runs behind a flag)
  </read_first>
  <action>
    Create one stub file per 02-VALIDATION.md row, filename matching the map's "Automated Command" column exactly. Each unit stub: a module docstring naming the requirement + threat from the map, and ONE function `test_<behavior>` decorated `@pytest.mark.xfail(reason="Wave N — not yet implemented", strict=False)` whose body is `raise NotImplementedError` (so it xfails on collect, never errors collection). Wave N in each reason matches the row's Wave column (1, 2, 3, or 5).
    For `tests/integration/test_second_ipo_e2e.py`: gate behind the same live-Qdrant guard Phase 1 uses in test_qdrant_ingest.py (env-var / connection probe → `pytest.mark.skip` or `xfail(run=False)`), per 02-VALIDATION "xfail until live Qdrant".
    For `tests/eval/test_snapshot_eval.py` and `tests/eval/test_p13_recall.py`: gate behind the `--run-eval` mechanism used by tests/eval/test_phase1_eval.py (skip unless the flag is present), per the "(deferred)" annotation in the map.
    Do NOT import any not-yet-existing Phase 2 module at module top level — keep imports inside the test body (after the `raise NotImplementedError`) or absent, so collection never breaks the 219 baseline.
    No fenced code in this plan; follow the exact style of test_chunker.py.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q --co >/dev/null && python -m pytest tests/unit -q --timeout=15</automated>
  </verify>
  <done>All 12 stub files collect; `pytest tests/unit -q` reports the 219 Phase 1 tests still passing plus the new xfails (xfailed, not failed); no collection errors.</done>
</task>

<task type="auto">
  <name>Task 2: Catalogue schema stub + snapshots dir + flip nyquist flags</name>
  <files>data/catalogue.json, data/snapshots/.gitkeep, .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-RESEARCH.md (§4 catalogue data model + DRHP Source List table — the per-IPO fields: drhp_id, issuer, sector, listing date, issue size, doc_type, source URL, front_matter_pages override, snapshot path)
    - data/swiggy_drhp/SOURCE.md (existing per-IPO provenance layout to mirror)
    - .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-VALIDATION.md (front-matter flags to flip)
  </read_first>
  <action>
    Write `data/catalogue.json` as `{"schema_version": 1, "ipos": []}` PLUS one fully-populated Swiggy entry as the canonical schema example, with keys: `drhp_id` ("swiggy_2024_11"), `issuer` ("Swiggy Limited"), `sector` ("Food delivery"), `listing_date` ("2024-11"), `issue_size_cr` (11327), `doc_type` ("Prospectus"), `fresh_vs_ofs` (null — filled by snapshot pipeline in Wave 3), `lead_managers` ([] placeholder), `source_url` (the SEBI Swiggy Prospectus URL from RESEARCH), `source_sha256` (null placeholder — pinned at ingest), `front_matter_pages` (20 — per-IPO pagination override per P14/A5), `snapshot_path` ("data/snapshots/swiggy_2024_11.json"), `status` ("listed"). The remaining 7 IPOs are added by Wave 1 (this is the schema stub only).
    Create `data/snapshots/.gitkeep` (empty file) so the directory is tracked.
    In `02-VALIDATION.md` front-matter set `nyquist_compliant: true` and `wave_0_complete: true`.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('data/catalogue.json')); assert d['schema_version']==1; assert d['ipos'][0]['drhp_id']=='swiggy_2024_11'; assert set(d['ipos'][0]) >= {'issuer','sector','listing_date','issue_size_cr','doc_type','source_url','front_matter_pages','snapshot_path','status'}; print('ok')" && test -f data/snapshots/.gitkeep && grep -q 'nyquist_compliant: true' .planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-VALIDATION.md</automated>
  </verify>
  <done>catalogue.json parses, carries the full Swiggy schema row; snapshots dir tracked; VALIDATION flags flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| repo config → runtime | `data/catalogue.json` is trusted, PR-reviewed config (not user input) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | data/catalogue.json | accept | Repo-committed + PR-reviewed; treated as trusted config. The V5 allow-list (Plan 02) reads from it but the file itself is not user-mutable. |
| T-02-SC | Tampering | npm/pip installs | mitigate | No new packages in Phase 2 (RESEARCH §Package Legitimacy Audit: not applicable). No install task in this plan. |
</threat_model>

<verification>
- `pytest tests/ -q --co` collects with zero errors (all 12 stubs + 219 baseline).
- `pytest tests/unit -q --timeout=15` → 219 passing, new stubs xfailed.
- catalogue.json + snapshots/.gitkeep exist and validate.
</verification>

<success_criteria>
Every 02-VALIDATION row has a collectible stub; 219 baseline green; catalogue schema + snapshot dir in place; `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/02-multi-ipo-catalogue-drhp-snapshot-surface/02-01-SUMMARY.md` when done.
</output>
